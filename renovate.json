{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:best-practices",
    ":semanticCommitType(chore)"
  ],
  "enabledManagers": ["maven", "dockerfile", "github-actions", "mise", "custom.regex"],
  "commitMessagePrefix": "chore(all): ",
  "commitMessageAction": "update",
  "commitBody": "Signed-off-by: Renovate Bot <bot@renovateapp.com>",
  "labels": ["dependencies"],
  "prConcurrentLimit": 50,
  "prHourlyLimit": 0,
  "description": [
    "platformAutomerge is deliberately false: main requires the ci-pass status check, and true triggers the registration race (native auto-merge completes in the ~1s window before ci-pass registers, landing a red bump). false makes Renovate merge via its own run after re-confirming green."
  ],
  "platformAutomerge": false,
  "automerge": true,
  "automergeType": "pr",
  "automergeStrategy": "squash",
  "ignoreTests": false,
  "customManagers": [
    {
      "customType": "regex",
      "description": "Makefile tool versions via inline # renovate: comments (PLANTUML_VERSION).",
      "managerFilePatterns": ["/^Makefile$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)(?: registryUrl=(?<registryUrl>[^\\s]+))?(?: versioning=(?<versioning>[^\\s]+))?\\n[A-Z_]+\\s*[?:]?=\\s*(?<currentValue>[^\\s]+)"
      ]
    },
    {
      "customType": "regex",
      "description": "Air-gap image inventory: each image ref on the line below its # renovate: comment.",
      "managerFilePatterns": ["/^images/images\\.txt$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)(?: versioning=(?<versioning>[^\\s]+))?\\n[^\\s:]+(?::[^\\s:]+)*:(?<currentValue>[^\\s]+)"
      ]
    },
    {
      "customType": "regex",
      "description": "Tekton Pipelines/Triggers release versions pinned in .env.example (only # renovate:-annotated lines are matched).",
      "managerFilePatterns": ["/^\\.env\\.example$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[^\\s]+) depName=(?<depName>[^\\s]+)(?: extractVersion=(?<extractVersion>[^\\s]+))?\\n[A-Z_]+=(?<currentValue>[^\\s]+)"
      ]
    }
  ],
  "vulnerabilityAlerts": {
    "labels": ["security"],
    "automerge": true,
    "minimumReleaseAge": "0 days"
  },
  "packageRules": [
    {
      "description": "Wait 3 days before automerging major updates",
      "matchUpdateTypes": ["major"],
      "minimumReleaseAge": "3 days"
    },
    {
      "description": "Group GitHub Actions into one PR",
      "matchManagers": ["github-actions"],
      "groupName": "GitHub Actions"
    },
    {
      "description": "Group mise tool bumps + 3-day buffer so aqua/github-tags release artifacts publish before the PR opens",
      "matchManagers": ["mise"],
      "groupName": "mise tools",
      "minimumReleaseAge": "3 days"
    },
    {
      "description": "Group Spring Boot dependencies",
      "matchPackageNames": ["/^org\\.springframework\\.boot/"],
      "groupName": "Spring Boot"
    },
    {
      "description": "Group all docker-datasource images (Dockerfile ARG bases + images/images.txt inventory) into one PR so the same image pinned in multiple files bumps together. pinDigests off: images.txt carries tag-only refs (no digest capture), and a mix of digest-pinned + tag-only refs would collide and silently drop bumps; these are build/mirror images for a demo, not a published runtime artifact.",
      "matchDatasources": ["docker"],
      "groupName": "Docker images",
      "pinDigests": false
    }
  ]
}
