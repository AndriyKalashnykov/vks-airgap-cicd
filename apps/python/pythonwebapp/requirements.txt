# Flask over FastAPI: this page is SERVER-RENDERED, and Flask ships Jinja2 in its own wheel set --
# which is what the shared-layout contract needs. FastAPI is API-first and 5.5x heavier here.
# Measured on python:3.14-alpine: 7 wheels, 636 KB, all pure-python (no musl source build).
Flask==3.1.2
