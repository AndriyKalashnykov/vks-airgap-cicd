# Doc truth audit — 2026-07-14

Every claim in the operator docs, checked against the CODE by an adversary (one per doc), then each
finding handed to a SECOND adversary whose job was to REFUTE it.

**READ THIS BEFORE ACTING ON ANY ROW BELOW.** These are the **68 RAW findings from the audit pass**
(13 CRITICAL / 27 HIGH / 28 MEDIUM). **45 of them survived refutation; 23 were REFUTED** — and this
file does not yet mark which is which, so **a row here is a CLAIM, not a verdict.** Re-check a finding
against the code before you act on it. (That is the same discipline the findings themselves are about:
a plausible-looking claim carrying the authority of having supposedly been checked is the failure mode.)

Fixed on 2026-07-14, both CONFIRMED CRITICALs:

- `README.md` Prerequisites ran `make fetch-harbor-ca` **before Harbor exists** — it dialled the
  placeholder `harbor.vks.local`, died, and the two steps after it never ran.
- `docs/sneakernet.md` told the operator to expect `make check-tools` to be clean **before the carry**,
  while the same page says not to install the five tools the bundle carries — so it exits 1, and its
  remediation says `make deps`, which downloads from the internet on an air-gapped box.

Still owed (a `scripts/` change, deferred while the e2e matrix was executing that directory):
`03-check-tools.sh` should know the five carried tools are **carried**, not missing, and must not print
"run `make deps`" on a box that has no internet.

## CRITICAL (13)

### `docs/prerequisites-manual.md:60-63`  — docs/prerequisites-manual.md

- **doc claims:** `curl https://mise.run | sh` + `export PATH="$HOME/.local/bin:$PATH"` + `make deps` = "installs the full jump-box toolchain". Those are the ONLY PATH instructions in the doc.
- **reality:** `~/.local/bin` holds the mise BINARY, not the tools mise manages (those need `mise activate` or ~/.local/share/mise/shims on PATH). crane, helm, yq, kustomize, kind and trivy come from mise and from NOWHERE else, so after following this doc verbatim they are not on PATH. `make deps` still exits 0 — 00-install-prereqs.sh:170-173 prints `crane MISSING` / `helm MISSING` / `kustomize MISSING` / `yq MISSING` as warnings and returns 0 (kubectl survives only because install_kubectl backfills it into BIN_DIR). The next command the operator runs dies: `make mirror` -> 10-mirror-pull.sh:36 `require_cmd crane`; `make check-tools` -> 03-check-tools.sh:29-35 lists helm/yq/crane as REQUIRED; `make e2e-kind` -> no kind. WORSE: the doc's own `export PATH` makes `command -v mise` succeed, which SUPPRESSES the Makefile's only remediation hint (Makefile:88-93 prints the `eval "$(mise activate bash)"` NOTE only when mise is NOT on PATH), and 00-install-prereqs.sh:173's advice ("ensure mise ran and $BIN_DIR is on PATH") is wrong for this exact case. The repo's own tooling does what the doc omits: bootstrap-jumpbox.sh:119 exports the shims dir; scripts/jumpbox-run.sh:140 runs `eval "$(mise activate bash)"`.
- **proof:** Makefile:87 (mise install) + .mise.toml:crane/helm/yq/kustomize/kind + scripts/00-install-prereqs.sh:170-173 + scripts/03-check-tools.sh:29-35 + scripts/10-mirror-pull.sh:36 + bootstrap-jumpbox.sh:119 + scripts/jumpbox-run.sh:140 + Makefile:88-93
- **fix:** Replace lines 60-63 with:

```bash
curl -fsSL https://mise.run | sh
eval "$(mise activate bash)"                       # THIS shell: puts the mise-managed TOOLS on PATH
echo 'eval "$(mise activate bash)"' >> ~/.bashrc   # new shells (the installer does NOT do this)
make deps
make check-tools   # gate: must print "all REQUIRED tools present" (crane/helm/yq are REQUIRED)
```

### `CLAUDE.md:281-285`  — CLAUDE.md

- **doc claims:** "The **bundle carries `crane`** (the mirror engine) — and *only* crane, which is all the mirror half needs (`bundle-load` -> `mirror-push` -> `mirror-verify`). ... A **full** air-gapped install additionally needs `kubectl`/`helm`/`jq`/`envsubst` pre-provisioned on the inside box."
- **reality:** The bundle carries FIVE tools, each resolved via `mise which` and asserted against the .mise.toml pin: crane, kubectl, helm, jq, yq. It ALSO carries the Istio helm charts (bundle/charts/*.tgz) and bundle/manifests (Tekton install YAML + Gateway API CRDs), neither of which CLAUDE.md mentions anywhere. bundle-load installs the five binaries into BIN_DIR (~/.local/bin), keeps a working copy the box already has (BUNDLE_TOOLS_FORCE=1 overrides), and dies if BIN_DIR is off PATH. Of the four names in the 'must be pre-provisioned' list, only envsubst is still true (it is an OS package, warn-only at bundle-load).
- **proof:** scripts/11-bundle.sh:160-164 (stage_tool crane/kubectl/helm/jq/yq) ; scripts/20-bundle-load.sh:76-78,106-177 (installs them, keeps existing, dies on off-PATH BIN_DIR) ; scripts/10-mirror-pull.sh:134-146 (helm pull of the istio charts) ; scripts/46-install-istio.sh:64-78 (installs from the CARRIED charts when present) ; scripts/20-bundle-load.sh:69-73 (manifests/ required) ; scripts/20-bundle-load.sh:202-210 (the OS packages that are genuinely NOT carried: bash tar curl sha256sum make awk git openssl envsubst)
- **fix:** Replace 281-285 with: "The bundle carries the toolchain the far side RUNS — `crane`, `kubectl`, `helm`, `jq`, `yq` — staged from the `.mise.toml` pins and pin-asserted (`scripts/11-bundle.sh:160`), plus `manifests/` (Tekton install YAML, Gateway API CRDs) and `charts/` (the Istio helm charts, so the DEFAULT ingress installs offline — `46-install-istio.sh:64`). `bundle-load` puts the binaries on PATH and KEEPS a working copy the box already has (`BUNDLE_TOOLS_FORCE=1` to override). It does NOT carry OS packages: `git`, `make`, `awk`, `openssl`, `envsubst` must come from the lab's package mirror — `03-check-tools.sh` is the source of truth for the required list."

### `CLAUDE.md:278-280`  — CLAUDE.md

- **doc claims:** "Air-gap exception: `tkn`/`argocd` come from OS packages / pinned releases via `00-install-prereqs.sh`." — the only description in the file of what the bootstrap provides, framing it as a tkn/argocd-only exception.
- **reality:** 00-install-prereqs.sh now also installs gawk + openssl + gettext/gettext-base, because a bare photon:5.0 LACKS awk, openssl, envsubst, git and make (and ubuntu:26.04 lacks openssl, envsubst, git, make, curl). 03-check-tools.sh now lists awk, tar and envsubst as REQUIRED — awk because lib/apps.sh reads apps/registry.tsv with it (EVERY per-app loop) and 23-mirror-verify.sh does its images.lock digest lookup with it. The doc gives an agent no reason to believe awk can be missing on a supported OS, which is exactly the bug that shipped and that RULE ZERO-A:75 records.
- **proof:** scripts/00-install-prereqs.sh:33-45 (the bare-image gap list + GETTEXT_PKG selection) ; scripts/03-check-tools.sh:32-35 (envsubst|required, awk|required, tar|required, crane|required)
- **fix:** Add to the Conventions bullet: "`00-install-prereqs.sh` also installs the OS-package floor a bare image lacks — `gawk`, `openssl`, `gettext`(`envsubst`), `git`, `make`. `scripts/03-check-tools.sh` is the source of truth for REQUIRED vs OPTIONAL; `awk` and `envsubst` are REQUIRED and are NOT on a bare photon:5.0."

### `README.md:201 (inside the block introduced at README.md:193 "Then run these, in order. Nothing else on this page requires action.")`  — README.md

- **doc claims:** `make fetch-harbor-ca # Harbor is self-signed: write its CA to the path HARBOR_CA_FILE points at` — presented as step 5 of 7 in the jump-box Prerequisites, i.e. BEFORE any cluster or Harbor exists.
- **reality:** It cannot work at that point and DIES. `make fetch-harbor-ca` → `scripts/fetch-ca.sh "$(HARBOR_URL)" ...`, which runs `openssl s_client -connect ${host}:443` and `die "could not connect to ${host}:${port} — is ${LABEL} reachable over HTTPS?"` on failure. At Prerequisites time HARBOR_URL is still the committed default `harbor.vks.local` (`.env.example:78`), because `make env-populate` (the step immediately before) explicitly CANNOT discover it without a reachable cluster — it prints `- HARBOR_URL not discovered (Harbor LB not up yet)` (scripts/02-env.sh:76-83). In Scenario 1 the operator has not even installed Harbor yet (it is a Supervisor Service they install later). The repo's own scenario doc gets the order right: `docs/scenario-1.md:188` runs `make vks-login`, and only at `docs/scenario-1.md:213` does it run `make fetch-harbor-ca` — AFTER the Harbor install.
- **proof:** scripts/fetch-ca.sh:41-43 (die "could not connect to ${host}:${port}"); Makefile:265-266 (fetch-harbor-ca → fetch-ca.sh "$(HARBOR_URL)"); .env.example:78 (HARBOR_URL=harbor.vks.local); scripts/02-env.sh:76-83 (env-populate cannot discover HARBOR_URL without a cluster); docs/scenario-1.md:213 (correct placement, after Harbor exists)
- **fix:** DELETE `make fetch-harbor-ca` from the Prerequisites block (README.md:201). It is not a prerequisite — it is a post-Harbor-install step and both scenario docs already place it correctly. If you want to keep a pointer, make it a sentence: "After Harbor exists (Scenario 1 step 6 / Scenario 2 discovery), run `make fetch-harbor-ca`."

### `README.md:203 (same block) —`make env-check # gate: fail now if anything required is still missing``  — README.md

- **doc claims:** Run `make env-check` as the sixth jump-box prerequisite; it is the gate that tells you you are ready.
- **reality:** On a fresh jump box it FAILS for a reason the operator cannot fix at that point: `env_check`'s required list is `(HARBOR_URL HARBOR_USERNAME HARBOR_PASSWORD GITEA_ADMIN_PASSWORD KUBECONFIG)` and `.env.example` contains **no uncommented `KUBECONFIG=` line at all** (`grep -c '^KUBECONFIG=' .env.example` → 0; it is deliberately a snapshot-protected SELECTOR). KUBECONFIG is produced by `make vks-login` — which this README block never tells you to run (it appears only inside the scenario docs, e.g. docs/scenario-1.md:188). So the README's own ordered list ends in a red gate. Worse in the other direction: the gate goes GREEN on `HARBOR_URL=harbor.vks.local` because `is_placeholder()` only rejects empty or `<SET-…>` values (scripts/02-env.sh:30) — so the README's promise "fail now if anything required is still missing" is simultaneously over-strict (KUBECONFIG) and over-permissive (a wrong-but-present Harbor default sails through).
- **proof:** scripts/02-env.sh:105 (`required=(HARBOR_URL HARBOR_USERNAME HARBOR_PASSWORD GITEA_ADMIN_PASSWORD KUBECONFIG)`); scripts/02-env.sh:30 (is_placeholder); .env.example (no `^KUBECONFIG=` line); docs/scenario-1.md:188 (`make vks-login` is what produces KUBECONFIG), docs/scenario-1.md:278-279 (env-populate + env-check run AFTER vks-login)
- **fix:** Re-order the Prerequisites block to only what a jump box can do with no cluster and no Harbor: `make deps` → `make engine-check` → `make check-tools` → `make env-init` → `make env-populate` (and say plainly that it will report Harbor/ArgoCD as "not discovered" — that is expected). Move `make env-check` / `make fetch-harbor-ca` out of Prerequisites entirely and let the scenario docs (which already order them correctly) own them.

### `README.md:64 (container-engine table row: "**You want docker** |`make deps CONTAINER_ENGINE=docker`, then`make trust-harbor`| rootless: none · rootful: one per registry")`  — README.md

- **doc claims:** The two-command docker on-ramp for a jump box: install with `CONTAINER_ENGINE=docker`, then run `make trust-harbor`.
- **reality:** `make trust-harbor` cannot be the second command. The target is `trust-harbor: check-env` (Makefile:280), so it runs `scripts/02-env.sh check` first, which begins with `[ -f "$ENV_FILE" ] || die "no .env yet — run 'make env-init' first"` (scripts/02-env.sh:39 in env_init / the same guard at the head of env_check, line ~100). A box that has just run `make deps` has no `.env`, so `make trust-harbor` dies immediately. Even with a `.env`, `19-trust-harbor.sh` PROVES trust "with a real login handshake" (Makefile:280 help text), so it also requires a **live, reachable Harbor** — which does not exist at the moment this table puts it. The row reads as a jump-box bootstrap step; it is actually a post-Harbor step.
- **proof:** Makefile:279-281 (`trust-harbor: check-env` → scripts/19-trust-harbor.sh); Makefile:122-123 (`env-check: scripts/02-env.sh check`); scripts/02-env.sh:100 (`env_check() { [ -f "$ENV_FILE" ] || die "no .env yet — run 'make env-init' first" ...`)
- **fix:** Change the row's "what you run" cell to `make deps CONTAINER_ENGINE=docker` (bootstrap) and move the trust step to where it can work: "…then, once Harbor exists and `.env` is populated, `make trust-harbor`". Or drop `check-env` from `trust-harbor`'s prerequisites and have it fail with a Harbor-specific message instead of an .env one.

### `docs/sneakernet.md:62 (with docs/sneakernet.md:44)`  — docs/sneakernet.md

- **doc claims:** "**Expect:** `make check-tools` on the jump box lists no missing tools. Run it **before** you carry 12 GB across a room — it is the cheapest failure available." — while line 44 says of `crane`, `kubectl`, `helm`, `jq`, `yq`: "**the bundle carries it** — do NOT install these".
- **reality:** FALSE, and self-contradictory. `03-check-tools.sh` marks kubectl, helm, jq, yq and crane as **required** — exactly the five the doc forbids installing. So on a jump box provisioned per the doc's own table, `make check-tools` exits **1**. I ran it with a stripped PATH (offline, read-only): `MISSING REQUIRED: kubectl helm yq crane`, exit 1. Worse, its failure message is `Install the toolchain with: make deps (mise + scripts/00-install-prereqs.sh)` (03-check-tools.sh:83) — a script that downloads from the internet, on the one box that has none. That is the exact dead end `require_internet()` (lib/os.sh:496-516) was added to eliminate; the doc walks the operator straight back into it, at the step it advertises as the cheap pre-carry safety check.
- **proof:** scripts/03-check-tools.sh:28-38 (kubectl|required, helm|required, jq|required, yq|required, crane|required) and scripts/03-check-tools.sh:81-83 (rc=1 + "make deps")
- **fix:** Split the check by phase. Either (a) run `make check-tools` on the jump box only AFTER `make bundle-load`, and say so: move the Expect line to Step 3 and change it to "after bundle-load, `make check-tools` lists no missing tools"; and in Step 0b replace the pre-carry Expect with the honest one: "Expect: `make check-tools` reports the five CARRIED tools (crane/kubectl/helm/jq/yq) as MISSING — that is correct, the bundle brings them. Every OTHER required tool (awk, tar, curl, git, openssl, envsubst, make, sha256sum) must show OK; if any of those is missing, fix it BEFORE you carry the bundle." Or (b) better: add a `CHECK_TOOLS_PHASE=preload` mode to 03-check-tools.sh that demotes the five carried tools to `carried` (not `required`) and prints "these arrive with the bundle", and have the doc call that. Either way, the `make deps` remediation string must not be shown on a box the doc defines as air-gapped.

### `docs/scenario-1.md:139-146 (A2 "→ now set in .env") and 255-262 (Step 4)`  — docs/scenario-1.md

- **doc claims:** A2's set-these block lists ARGOCD_NAMESPACE / ARGOCD_SERVER / ARGOCD_TRACK_BRANCH (and a commented ARGOCD_CA_FILE) and NOTHING ELSE. Step 4 then says: run `make fetch-argocd-kubeconfig` then `make argocd-preflight`, and "Expect: $ARGOCD_KUBECONFIG is written, and argocd-preflight prints PREFLIGHT OK and ArgoCD is OFF-CLUSTER (the real-lab shape)".
- **reality:** FALSE, and it cascades into install-all. `ARGOCD_KUBECONFIG` is COMMENTED in .env.example and the doc never tells the operator to set it. `31-fetch-argocd-kubeconfig.sh:41` defaults it to `${REPO_ROOT}/secrets/argocd.kubeconfig` for its OWN process only and NEVER persists it (no `env_set`/`state_set` anywhere in that script — grepped, zero hits). Every other consumer defaults it to the GUEST kubeconfig: `23-argocd-preflight.sh:40` `ARGOCD_KUBECONFIG="${ARGOCD_KUBECONFIG:-$KUBECONFIG}"`, same at `70-configure-argocd.sh:50` and `71-argocd-register-guest.sh:32`. So the very next command, `make argocd-preflight`, runs against the GUEST cluster: it prints "ARGOCD_KUBECONFIG is unset -> assuming ArgoCD runs IN the workload cluster" (`:61`), sets OFF=0 so the promised "ArgoCD is OFF-CLUSTER" line NEVER prints (`:70`), and then hits `block "namespace 'argocd-instance-1' not found on the ArgoCD cluster"` (`:108`) → `exit 1` (`:230`). Because `install-all: preflight …` (Makefile:446) and `preflight: check-tools argocd-preflight lab-preflight psa-check` (Makefile:292), **Step 5's `make install-all` dies at its first prerequisite, before the mirror.** The irony: `31`'s own comment (lines 36-41) documents this exact cascade as a bug it fixed — but the fix only lives inside `31`.
- **proof:** scripts/23-argocd-preflight.sh:40,60-63,70,108,230 · scripts/31-fetch-argocd-kubeconfig.sh:41 (defaults, never persists) · .env.example:336 (`# ARGOCD_KUBECONFIG=./secrets/argocd.kubeconfig` — commented) · Makefile:446,292
- **fix:** Add to A2's "→ now set in `.env`" block (docs/scenario-1.md:141-146):

```bash
ARGOCD_KUBECONFIG=./secrets/argocd.kubeconfig   # written by Step 4; every other target reads it from .env
```

Better (belt and braces): make `31-fetch-argocd-kubeconfig.sh` PERSIST the path it wrote — `state_set ARGOCD_KUBECONFIG "$ARGOCD_KUBECONFIG"` after the `argocd-server is visible` proof at :90 — so the value cannot be lost between two `make` invocations. It is a SELECTOR (`lib/os.sh:306`), so it is snapshot-protected and an operator override still wins.

### `docs/scenario-1.md:255-258`  — docs/scenario-1.md

- **doc claims:** Step 4: "```bash\nmake fetch-argocd-kubeconfig    # interactive: prompts for your password\n```" — presented as the only prerequisite being your password.
- **reality:** FALSE — it dies before it prompts for anything. `31-fetch-argocd-kubeconfig.sh:53-68` REQUIRES a TLS decision: if `VKS_CA_CERT_FILE` is not set-and-present AND `VKS_INSECURE_SKIP_TLS_VERIFY` is not truthy, it hits `die "set VKS_CA_CERT_FILE=<path to the Supervisor CA cert> …, or set VKS_INSECURE_SKIP_TLS_VERIFY=true to skip verification."`. NEITHER variable appears anywhere in scenario-1.md (grepped: zero hits) — not in Step 0's four-var block (:43-46), not in A1's, A2's or A3's set-these blocks. So Step 4's only command fails on a clean `.env` built by following this doc exactly. (Both vars ARE documented in .env.example:343 / :761, but the runbook is supposed to be executable without opening another file.)
- **proof:** scripts/31-fetch-argocd-kubeconfig.sh:53-68 (`die "set VKS_CA_CERT_FILE=… or set VKS_INSECURE_SKIP_TLS_VERIFY=true"`)
- **fix:** Add to Step 0's `.env` block (docs/scenario-1.md:42-47), next to SUPERVISOR_HOST:

```bash
VKS_CA_CERT_FILE=./secrets/supervisor-ca.crt   # the Supervisor's CA — ask your vSphere admin
# ...or, if you don't have it:
VKS_INSECURE_SKIP_TLS_VERIFY=true              # skips TLS verification of the Supervisor endpoint
```

and say in Step 4's **For:** line that one of the two is REQUIRED before `fetch-argocd-kubeconfig` will even connect.

### `docs/scenario-1.md:44 —`VKS_USERNAME=<administrator@vsphere.local>``  — docs/scenario-1.md

- **doc claims:** Step 0 instructs the operator to put the FULLY-QUALIFIED SSO user (with `@vsphere.local`) in `VKS_USERNAME`. .env.example:755 agrees (`'user@SSO.DOMAIN'`).
- **reality:** That is correct for `make vks-login` and WRONG for `make fetch-argocd-kubeconfig` (Step 4), because the two scripts disagree. `30-vks-login.sh:55-64` appends `@$VKS_SSO_DOMAIN` ONLY when the username has no `@` (`case "$user" in *@*) : ;; …`). `31-fetch-argocd-kubeconfig.sh:77` appends it UNCONDITIONALLY: `--username "${VKS_USERNAME}@${SSO_DOMAIN}"` with `SSO_DOMAIN="${VKS_SSO_DOMAIN:-vsphere.local}"` (:47). Following this doc verbatim, Step 4 therefore sends `--username administrator@vsphere.local@vsphere.local` to `vcf context create` — an authentication failure the operator will read as "wrong password". The doc's own instruction is what guarantees the malformed value.
- **proof:** scripts/31-fetch-argocd-kubeconfig.sh:47,77 (unconditional append) vs scripts/30-vks-login.sh:55-64 (conditional append) · .env.example:755
- **fix:** CODE fix (the doc and `30` are right; `31` is wrong). In `scripts/31-fetch-argocd-kubeconfig.sh`, replace line 77's `--username "${VKS_USERNAME}@${SSO_DOMAIN}"` with the same guard `30` uses:

```bash
user="$VKS_USERNAME"; case "$user" in *@*) : ;; *) user="${VKS_USERNAME}@${SSO_DOMAIN}" ;; esac
… --username "$user" …
```

Better: hoist that into a single `vks_sso_user()` helper in `lib/os.sh` and call it from BOTH scripts — two copies of one rule is how they drifted.

### `docs/scenario-1.md:154-162 (A3), and again at :269`  — docs/scenario-1.md

- **doc claims:** A3 hands the operator raw shell to paste:

```bash
vcf context use $VKS_CONTEXT_NAME:$VKS_NAMESPACE
vcf cluster kubeconfig get $VKS_CLUSTER_NAME --export-file ./secrets/vks.kubeconfig
```

and Step 4's verification block uses `kubectl --kubeconfig $ARGOCD_KUBECONFIG get pods -A …`.

- **reality:** These `$VARS` live in `.env`, which the operator's SHELL never sources. `make` reads `.env` (`Makefile:19-20 -include .env`) and `load_env` reads it inside scripts (`lib/os.sh`) — but a command the operator PASTES INTO A TERMINAL sees none of it. Nothing in scenario-1.md tells them to `set -a; . ./.env; set +a` (grepped the whole doc: no `source`, no `. ./.env`). Pasted as printed, `vcf context use $VKS_CONTEXT_NAME:$VKS_NAMESPACE` becomes `vcf context use :` and `vcf cluster kubeconfig get --export-file ./secrets/vks.kubeconfig` loses its cluster argument. Compounding it: **`VKS_CONTEXT_NAME` is never listed in ANY of the doc's four "→ now set in `.env`" blocks** — Step 0 lists SUPERVISOR_HOST / VKS_USERNAME / VKS_NAMESPACE / VKS_CLUSTER_NAME (:43-46); A3's own prose note (:164-166) says `30-vks-login.sh` "**requires** it" and to "set it in `.env`", but the set-these block right below (:176-181) omits it. And A2's `vcf context create` (:104) never names a context, so the operator has no idea what value `<context-name>` should be — `30-vks-login.sh:76` reveals the CLI *prompts* for it interactively, which the doc never says.
- **proof:** Makefile:19-20 (`-include .env` — make only) · scripts/lib/os.sh `load_env` (scripts only) · scripts/30-vks-login.sh:52 (`: "${VKS_CONTEXT_NAME:?set VKS_CONTEXT_NAME in .env …}"`), :76 ("INTERACTIVE: at the prompt, enter the context name") · docs/scenario-1.md:43-46, 176-181 (VKS_CONTEXT_NAME absent from both)
- **fix:** Three edits. (a) Put `VKS_CONTEXT_NAME=<a name you choose; you will TYPE it at the vcf prompt>` into Step 0's `.env` block (:42-47) — it is chosen, not discovered, so it belongs with the other four. (b) In A2's step 3, say that `vcf context create` PROMPTS for the context name and that it must equal `$VKS_CONTEXT_NAME`. (c) Before the first raw-shell block that uses a `.env` value (A3, :154), add:

```bash
set -a; . ./.env; set +a   # the commands below use .env values; your SHELL does not read .env (only `make` does)
```

### `docs/scenario-2.md:324 (and :338)`  — docs/scenario-2.md

- **doc claims:** Step 6: run `make install-all` ("preflight → mirror → mirror-verify → builder-image → vks-login → platform → gitops"). And in the cross-cluster note: "Do **not** set `ARGOCD_KUBECONFIG` (you lack Supervisor/ArgoCD-admin access…)".
- **reality:** FALSE / FATAL. `install-all`'s first prerequisite is `preflight` (Makefile:446), which runs `argocd-preflight` (Makefile:292). With ARGOCD_KUBECONFIG unset it defaults to the GUEST kubeconfig (23-argocd-preflight.sh:39) and then does `ka get ns "$NS"` against the guest (23-argocd-preflight.sh:103). On a real lab the shared ArgoCD is a Supervisor Service, so ARGOCD_NAMESPACE does NOT exist in the guest → NotFound (not Forbidden) → `block "namespace '$NS' not found on the ArgoCD cluster"` (23-argocd-preflight.sh:108) → `exit 1` (23-argocd-preflight.sh:228-230). `make install-all` dies at step one, BEFORE the mirror, for every tenant who follows this doc. 31-fetch-argocd-kubeconfig.sh:37-41 documents this exact cascade as a bug it fixed for Scenario 1 by defaulting the kubeconfig path — Scenario 2 gets no such escape because the doc forbids the variable. 70-configure-argocd.sh:106-119 handles the SAME NotFound as a warn-and-continue; the two scripts disagree, and the preflight is the one that is wrong for a tenant.
- **proof:** scripts/23-argocd-preflight.sh:39,103,108,228-230 · Makefile:292,446 · scripts/31-fetch-argocd-kubeconfig.sh:37-41 · scripts/70-configure-argocd.sh:106-119
- **fix:** CODE FIX (the real one): in scripts/23-argocd-preflight.sh, downgrade the NotFound branch from `block` to `warn` when `[ "$ARGOCD_KUBECONFIG" = "$KUBECONFIG" ]` — i.e. when we KNOW we are looking at the guest and ArgoCD is a Supervisor Service — mirroring 70-configure-argocd.sh:114-118 ("Expected when ArgoCD is a Supervisor Service and ARGOCD_KUBECONFIG points at the GUEST … kubectl is not usable here"). Keep the BLOCK only when ARGOCD_KUBECONFIG was explicitly set (then a missing namespace really is a wrong ARGOCD_NAMESPACE). DOC FIX until then: scenario-2.md:324 must not tell a tenant to run `make install-all`; it must either say `make install-all` will fail at preflight and give the per-step sequence (`make mirror && make builder-image && make vks-login && make platform && make gitops`), or ship a tenant-aware preflight.

### `docs/scenario-2.md:324-328 vs :399-411`  — docs/scenario-2.md

- **doc claims:** Step 6 tells the tenant to run `make install-all` … `make verify`. The tenant write mechanism (`ARGOCD_MECHANISM=api`, `ARGOCD_AUTH_TOKEN`, and how to mint the token with `argocd login --sso` + `argocd account generate-token`) is only introduced ~80 lines LATER, in a bullet list AFTER Step 7.
- **reality:** A tenant who follows the doc IN ORDER reaches `make gitops` with no ARGOCD_AUTH_TOKEN. 70-configure-argocd.sh:169 defaults ARGOCD_MECHANISM=auto; `can_kubectl` is no (the ArgoCD namespace is unreadable from the guest — 70:106-119, 70:177-183) and `can_api` is no (the api probe needs ARGOCD_SERVER **and** ARGOCD_AUTH_TOKEN — 70:186-192), so the ladder resolves to **MECH=request** (70:195-204). `request` writes NOTHING: it renders the Applications to ./out/ and **exits 0** (70:455-461). `make install-all` therefore reports SUCCESS having deployed nothing, and the very next doc command (`make verify`, :326) then fails. The token setup is a PRECONDITION of install, not a postscript.
- **proof:** scripts/70-configure-argocd.sh:169,186-192,195-204,455-461 · .env.example:377-389
- **fix:** Move ARGOCD_MECHANISM / ARGOCD_AUTH_TOKEN / ARGOCD_DEST_SERVER into the Step-3 `.env` block (docs/scenario-2.md:81-95), with the token recipe (`argocd login <server> --sso` → `argocd account generate-token --account <you>`) there, and add an explicit `Expect:` to Step 6: `make gitops` must log `write mechanism: api` — if it logs `request`, nothing was applied. Also state that `install-all` exits 0 on the `request` path.

## HIGH (27)

### `docs/prerequisites-manual.md:61-62`  — docs/prerequisites-manual.md

- **doc claims:** "(the installer also adds `mise activate` to your profile for new shells)"
- **reality:** FALSE, primary-sourced from the installer itself (<https://mise.run>, fetched 2026-07-14): its `after_finish_help()` only PRINTS `mise: run the following to activate mise in your shell:` followed by the `echo "eval ..." >> ~/.zshrc` line for the USER to run. It modifies no profile. The repo's own Makefile:92 instructs the operator to append that line by hand — i.e. the code already assumes the opposite of what this doc asserts. This false parenthetical is the reason finding #1 exists: it tells the operator activation is already handled.
- **proof:** Makefile:88-93 (tells the user to add `eval "$(mise activate bash)"` to ~/.bashrc themselves) + <https://mise.run> `after_finish_help()`
- **fix:** Delete the parenthetical on :61-62 and replace with the explicit `eval "$(mise activate bash)"` + `>> ~/.bashrc` lines from finding #1.

### `docs/prerequisites-manual.md:1-6 (and the whole file)`  — docs/prerequisites-manual.md

- **doc claims:** "Prerequisites — the manual path" / README.md:235 bills it as "the step-by-step the bootstrap automates". No scope statement anywhere: it never says it needs the internet, and never mentions the air-gapped box or the sneakernet bundle.
- **reality:** Every step needs the internet (git clone, curl mise.run, `make deps` which downloads tkn/argocd/kubectl + every mise tool). The script it mirrors states the scope explicitly and the doc dropped it: bootstrap-jumpbox.sh:11-12 — "DUAL-HOMED only (needs internet). A fully air-gapped host uses the carried bundle". Meanwhile docs/sneakernet.md:33-35 calls the AIR-GAPPED inside box "the jump box" and says "The jump box is provisioned by hand, once" — and this is the repo's only "the manual path" / hand-provisioning doc. A reader routed here for the air-gapped box gets a recipe whose every command fails, and a Photon package list with NO gawk and NO gettext (they arrive only via `make deps` -> 00-install-prereqs.sh:48, which cannot run there) — which is precisely the box docs/sneakernet.md:55 warns about: "A box without it passes every other check and then dies at mirror-verify."
- **proof:** bootstrap-jumpbox.sh:11-12 + docs/sneakernet.md:33-35,45-55 + scripts/00-install-prereqs.sh:44-48
- **fix:** Add a scope banner as the first line of the doc: "**This provisions the INTERNET-SIDE (dual-homed) box.** The **air-gapped** box cannot run `make deps` — provision it from [Sneakernet Step 0b](sneakernet.md) (it has its own package table: Photon additionally needs `make gawk git openssl gettext`), then `make bundle-load`."

### `CLAUDE.md:137`  — CLAUDE.md

- **doc claims:** "`make e2e-kind-both` / `verify-ingress-both` / `e2e-cross-cluster` / `e2e-sneakernet` | e2e permutations: ... 2-cluster ArgoCD registration ..."
- **reality:** `make e2e-cross-cluster` DOES NOT EXIST. The target is `e2e-kind-cross-cluster`; `e2e-cross-cluster` is only the SCRIPT name it invokes. Verified: `grep -E '^e2e-cross-cluster:' Makefile` returns nothing. Nothing in CI catches this, because check-doc-make-targets — the gate that exists precisely to stop a doc naming a dead command — EXCLUDES CLAUDE.md by design.
- **proof:** Makefile:419 (e2e-kind-cross-cluster:) and Makefile:420 (@$(SCRIPTS)/e2e-cross-cluster.sh) ; scripts/check-doc-make-targets.sh:25-30 ("CLAUDE.md is deliberately EXCLUDED") ; scripts/check-doc-make-targets.sh:31 (docs=$(git ls-files ... 'README.md' 'docs/*.md'))
- **fix:** Row 137: `e2e-cross-cluster` -> `e2e-kind-cross-cluster`. (Also consider whether CLAUDE.md's exclusion from check-doc-make-targets is still right — the rationale was 'nobody executes CLAUDE.md', but an agent does.)

### `CLAUDE.md:144`  — CLAUDE.md

- **doc claims:** "`make install-all` | Full air-gap install: `mirror -> builder-image -> vks-login -> platform -> gitops`"
- **reality:** install-all is `preflight mirror mirror-verify builder-image vks-login platform gitops`. The doc omits BOTH `preflight` — whose entire point (Makefile:292, and CLAUDE.md's own line 940) is to run BEFORE the 20-minute mirror, and which is the fix for a CRITICAL that killed every real-lab first run — and `mirror-verify`, the integrity gate that CLAUDE.md:860 says mirror now depends on. An agent reasoning about 'what an air-gapped box runs first' from this row misses that preflight -> check-tools -> the REQUIRED-tool list is the first thing it hits.
- **proof:** Makefile:446 (install-all: preflight mirror mirror-verify builder-image vks-login platform gitops) ; Makefile:292 (preflight: check-tools argocd-preflight lab-preflight psa-check ... Run it BEFORE the 20-minute mirror)
- **fix:** Row 144: "Full air-gap install: `preflight -> mirror (pull+push+verify) -> builder-image -> vks-login -> platform -> gitops`. `preflight` runs FIRST and is read-only — it is what stops a 20-minute mirror on a box that cannot finish."

### `CLAUDE.md:174-176 (and rows 126, 128)`  — CLAUDE.md

- **doc claims:** "**Mirror mode is not a variable** — dual-homed vs sneakernet is simply which mirror commands you run: dual-homed -> `make mirror`; sneakernet -> `make mirror-pull && make bundle` (carry the bundle) then `make bundle-load && make mirror-push`."
- **reality:** That recipe cannot produce the offline Maven builder image, which the whole air-gap Java build depends on. The sneakernet split exists in code as two targets CLAUDE.md never mentions: `builder-build` ((sneakernet, INTERNET box) build the Maven builder INTO the bundle — needs Maven Central, NOT Harbor) and `builder-push` ((sneakernet, AIR-GAP box) push the CARRIED builder into Harbor — needs Harbor, NOT the internet; uses the carried crane, no container engine). Row 128 only documents `builder-image` (the dual-homed build+push). Follow CLAUDE.md's sneakernet recipe and `platform`/`verify` die at the Kaniko/maven-test step on a box that cannot reach Maven Central.
- **proof:** Makefile:252 (builder-build: ## (sneakernet, INTERNET box) Build the Maven builder into the bundle) ; Makefile:256 (builder-push: ## (sneakernet, AIR-GAP box) Push the CARRIED Maven builder into Harbor ... uses the carried crane; no container engine) ; Makefile:248 (builder-image: dual-homed build+push)
- **fix:** Line 176: "sneakernet -> `make mirror-pull && make builder-build && make bundle` (carry the tarball) then `make bundle-load && make mirror-push && make builder-push`." And add `builder-build`/`builder-push` to the Common-commands table beside `builder-image`.

### `CLAUDE.md:195, 199, 202, 230-231 (and 165)`  — CLAUDE.md

- **doc claims:** The Architecture section repeatedly says the KinD scripts "write `KUBECONFIG` + `VKS_AUTH_METHOD=kubeconfig` to `.env.kind`", "writes `HARBOR_URL`+`HARBOR_INSECURE`+`HARBOR_CA_FILE` to `.env.kind`", "publish `INGRESS_LB_IP` ... to `.env.kind`", "the persisted `.env.kind` value".
- **reality:** NOTHING writes .env.kind any more. The sink is the STAMPED overlay `.env.state` (`VKS_STATE_FILE`), written via `state_set`; `.env.kind` survives only as read-only back-compat. And `set_env_var` — the helper an agent would reach for after reading this section — now HARD-ERRORS without an explicit sink argument (`file="${3:?set_env_var: a SINK is required — use state_set ...}"`), precisely so this class cannot regrow. CLAUDE.md contradicts ITSELF: line 1000 says "`.env.kind` is GONE" and line 1011 says "`set_env_var` has no default sink". The Architecture section — the part an agent reads to WRITE code — is the stale one, and it is 800 lines away from the correction.
- **proof:** scripts/lib/os.sh:277 (local legacy="${REPO_ROOT}/.env.kind"  # read-only back-compat; nothing writes it any more) ; scripts/lib/os.sh:413-419 (set_env_var KEY VALUE FILE — a SINK is required) ; scripts/lib/state.sh:47-50 (state_set -> state_file()) ; contradicted by CLAUDE.md:1000-1011
- **fix:** Rewrite 195-231 to name `.env.state` / `state_set` (the stamped overlay), and delete every `.env.kind` mention outside the dated history section. Same stale name is in Makefile help text at Makefile:357 and Makefile:453 — fix those in the same pass.

### `CLAUDE.md:105 (definition) vs 149, 384`  — CLAUDE.md

- **doc claims:** "An **air-gapped VKS CI/CD demo**: from an internet-connected jump box (Ubuntu or PhotonOS), mirror all required images into Harbor..." — i.e. 'jump box' is DEFINED as the box with internet.
- **reality:** 'Jump box' is overloaded across three different machines and the doc never disambiguates: (a) the INTERNET box (this definition); (b) the AIR-GAPPED inside box — Makefile:433 calls it "[FRESH jumpbox container = air-gap box]", and 20-bundle-load.sh:85-87 addresses a box that 'has no internet and cannot install anything'; (c) the `make jumpbox` / `jumpbox-matrix` TEST container, which runs `make deps` and therefore needs the internet. CLAUDE.md's own RULE ZERO-A:77 records the bug this ambiguity already shipped: "`require_cmd crane` told an air-gapped operator to run a script that DOWNLOADS from the internet."
- **proof:** Makefile:433 ("[FRESH jumpbox container=air-gap box]") ; scripts/20-bundle-load.sh:35 ("this box ... cannot install anything: the bundle must be RE-CUT on the internet side") ; scripts/20-bundle-load.sh:85-87 ; Makefile:519 (make jumpbox runs make deps -> needs the internet) ; CLAUDE.md:77 (the bug it caused)
- **fix:** Define the two roles once, at line 105, and use ONLY those names thereafter: "**internet box** (dual-homed; runs mirror-pull/bundle/builder-build) and **air-gap box** (inside; runs bundle-load/mirror-push/builder-push/platform). 'Jump box' names the HOST CLASS, never its network posture — `make jumpbox*` builds a TEST jump box and needs the internet."

### `README.md:172 — "It needs internet (dual-homed); a fully air-gapped host uses the carried bundle instead."`  — README.md

- **doc claims:** An air-gapped host does not need the bootstrap script because the carried bundle covers it. This is the README's ONLY statement about how an air-gapped box gets bootstrapped.
- **reality:** FALSE, and it is a dead end. The bundle carries exactly five STATIC binaries — `crane`, `kubectl`, `helm`, `jq`, `yq` (`scripts/11-bundle.sh`, the five `stage_tool` calls). It provably cannot carry the OS packages the air-gapped box needs, and the repo says so in two places: `scripts/11-bundle.sh` ("What we CANNOT carry, and therefore must be pre-provisioned on the air-gap box … bash tar curl sha256sum make git openssl envsubst(gettext)") and `docs/sneakernet.md:42-56`, whose measured table says a bare `photon:5.0` is **missing make, gawk, git, openssl, gettext** and a bare `ubuntu:26.04` is **missing make, git, openssl, gettext-base, curl**. Without `make` you cannot even invoke `make bundle-load` to unpack the bundle; without `awk` the box "passes every other check and then dies at mirror-verify" (scripts/00-install-prereqs.sh:32-43). The sentence tells an operator the bundle substitutes for provisioning. It does not.
- **proof:** scripts/11-bundle.sh (stage_tool crane/kubectl/helm/jq/yq + the "What we CANNOT carry" comment block); scripts/00-install-prereqs.sh:32-43 (measured: photon:5.0 LACKS awk, openssl, envsubst, git, make); docs/sneakernet.md:42-56 (the "you must provision it — the bundle cannot carry it" row)
- **fix:** Replace the sentence with: "It needs internet (dual-homed). A **fully air-gapped box cannot run it** — that box is provisioned by hand from your internal package mirror (`bash tar curl sha256sum make git openssl envsubst gawk`), and the bundle then delivers only the static binaries (`crane`, `kubectl`, `helm`, `jq`, `yq`). See [sneakernet → Step 0b](docs/sneakernet.md)."

### `README.md:14-15 — "dual-homed or **[sneakernet](docs/sneakernet.md)** — carry the bundle across on a stick when **the jump box has internet but no route to Harbor**"`  — README.md

- **doc claims:** Sneakernet's trigger is: your jump box has internet but cannot reach Harbor. And the box with the internet is called the "jump box".
- **reality:** Both halves are wrong, and the second is the overloaded-terminology hazard. (1) The trigger was corrected today: `docs/sneakernet.md:3` — "**Use this when no single box reaches both the internet and Harbor.**" (commit f854184: "sneakernet's trigger is 'no ONE box reaches both', not 'your jump box has internet'"). (2) In sneakernet.md's vocabulary the **jump box is the AIR-GAPPED box** — its own table reads `reaches the internet: staging box ✅ / jump box ❌` and `reaches Harbor + the cluster: staging ❌ / jump box ✅` (docs/sneakernet.md:11-13). README:14 calls the internet-side box the "jump box", which is sneakernet.md's **staging box**. The README also contradicts ITSELF: line 232 carries the corrected framing ("**no single box reaches BOTH the internet and Harbor**"). An operator who takes line 14 at face value will map every sneakernet.md step onto the wrong machine.
- **proof:** docs/sneakernet.md:3 and docs/sneakernet.md:11-13 (the staging-box / jump-box reachability table); README.md:232 (the same README's corrected statement); git commit f854184
- **fix:** Rewrite README.md:14-15 to match line 232 and sneakernet.md: "…or **[sneakernet](docs/sneakernet.md)** — when **no single box reaches both the internet and Harbor**, pull on a **staging box**, carry the bundle across on a stick, and push it in from the **jump box**." Then use "staging box" / "jump box" consistently in the intro, the diagram caption (README:28 — "The jump box is the only bridge — it pulls from the internet and pushes into the air-gapped cluster" is the dual-homed picture only) and the Prerequisites section.

### `README.md:190 — "**You need:** a jump box on **Ubuntu** or **PhotonOS**, with network reach to the Supervisor, Harbor, and (dual-homed only) the workload cluster."`  — README.md

- **doc claims:** Reach to the workload cluster is needed only if the box is dual-homed.
- **reality:** FALSE, and it omits the one thing "dual-homed" actually means. Reach to the **workload (guest) cluster** is needed on EVERY path, in every flow: `make platform`, `make gitops`, `make psa-check` (`scripts/psa-check.sh:33 require_cmd kubectl`, `:51 kubectl label --dry-run=server`) and `make verify` (which `kubectl port-forward`s into Gitea and each app — `scripts/99-verify.sh:87,156`) all talk to the guest cluster with kubectl. What is "dual-homed only" is **internet reach** — and the sentence never mentions the internet at all. In the sneakernet split, the box that reaches the cluster (the jump box) is precisely the one that does NOT have internet.
- **proof:** scripts/psa-check.sh:33,51 (kubectl + server-side dry-run against the live cluster); scripts/99-verify.sh:87,156 (kubectl port-forward svc/gitea-http and svc/<app>); Makefile:446 (install-all → platform → gitops, all kubectl-driven); docs/sneakernet.md:11-13 (the jump box reaches the cluster and NOT the internet)
- **fix:** Rewrite as: "…with network reach to the Supervisor, Harbor and the workload cluster — **plus the internet if it is dual-homed**. If no single box has all of those, see [sneakernet](docs/sneakernet.md)."

### `README.md:52 and README.md:53 — the "You need / **Run:**" cells of the two VKS rows:`… → make psa-check` (Scenario 1) and `… → make harbor-robot → make psa-check`(Scenario 2)`  — README.md

- **doc claims:** These command sequences are jump-box **prerequisites** — what you run to get ready, before opening the scenario doc.
- **reality:** The last one or two commands in each sequence cannot run at prerequisite time. `make psa-check` requires a LIVE cluster: `scripts/psa-check.sh:33` does `require_cmd kubectl` and `:51` runs `kubectl label --dry-run=server --overwrite namespace …` — a server-side admission evaluation against a real API server. In Scenario 1 the guest cluster does not exist yet (the admin provisions it inside the doc), and in both scenarios `$KUBECONFIG` is only produced by `make vks-login`, which appears in NEITHER "Run:" cell. `make harbor-robot` (Scenario 2) likewise needs a reachable Harbor and project-admin credentials — neither of which exists before the operator has discovered Harbor inside the scenario doc.
- **proof:** scripts/psa-check.sh:33 (require_cmd kubectl), scripts/psa-check.sh:51 (kubectl label --dry-run=server); Makefile:283 (harbor-robot → 22-harbor-robot.sh, a live Harbor API call); docs/scenario-1.md:188 (make vks-login is what produces KUBECONFIG — and it is not in the README's Run: cell)
- **fix:** Trim both "Run:" cells to what a bare jump box can actually do: `make deps` → `make install-vcf-clis` → `make env-init` → `make env-populate`. Leave `psa-check` / `harbor-robot` / `env-check` to the scenario docs, which have the cluster and Harbor by the time they call them.

### `docs/sneakernet.md:128-135 (the ingress table) and docs/sneakernet.md:121`  — docs/sneakernet.md

- **doc claims:** "**Ingress:** the default (`istio`) **installs from an internet Helm repo and cannot run here.**" · table row: "`INGRESS_CONTROLLER=istio` (default) | ❌ — `helm repo add istio-release.storage.googleapis.com`" · Step 4 command: `make install-ingress INGRESS_CONTROLLER=traefik   # or istio-existing — see below`
- **reality:** FALSE as of today's code. The bundle now carries the Istio charts and the installer prefers them over the network: `10-mirror-pull.sh:134-149` does `helm pull istio-airgap/{base,istiod,gateway} --version $ISTIO_VERSION --destination $BUNDLE_DIR/charts` on the STAGING box, and `46-install-istio.sh:64-79` uses them: "AIR-GAP FIRST … installing istio from the CARRIED charts in ${CHART_DIR} (no network)" — `helm repo add` is now INSIDE the `else` branch (46-install-istio.sh:73-78) and never runs when charts are present. The images already come from Harbor (`--set global.hub=$HARBOR_URL/$HARBOR_INFRA_PROJECT/istio`, 46-install-istio.sh:46,94-95,102-108; istio/pilot + istio/proxyv2 are in images/images.txt:57-60), and the Gateway API CRDs are read from the carried bundle (scripts/lib/istio.sh:146-155). The repo's own bare-box test asserts this works offline: `helm template istio-base $chart` under `--network none` (scripts/test-airgap-toolchain.sh:144-154). The doc denies a capability that exists, and Step 4's command hard-codes the workaround — so the operator lands on a non-default ingress for no reason, diverging from every other doc and from `make e2e-kind`.
- **proof:** scripts/46-install-istio.sh:64-79 + scripts/10-mirror-pull.sh:134-149 + scripts/lib/istio.sh:146-155 + scripts/test-airgap-toolchain.sh:144-154
- **fix:** Rewrite the table to:
| | works air-gapped? |
|---|---|
| `INGRESS_CONTROLLER=istio` (default) | ✅ — installs from the **carried charts** (`bundle/charts/*.tgz`, pulled by `mirror-pull`); images come from Harbor; the Gateway API CRDs come from `bundle/manifests/` |
| `INGRESS_CONTROLLER=traefik` | ✅ — image from Harbor, manifests in the repo |
| `INGRESS_CONTROLLER=istio-existing` | ✅ — attaches to a mesh the platform team already installed |
and change Step 4 line 121 to plain `make install-ingress` (the default), noting traefik/istio-existing as alternatives. Add the caveat: this requires the bundle to have been cut on a staging box that had `helm` and `ISTIO_VERSION` set (see the next finding).

### `docs/sneakernet.md:83-84 (Step 1 "Expect") and docs/sneakernet.md:78-80`  — docs/sneakernet.md

- **doc claims:** Step 1 lists `make mirror-pull` / `make builder-build` / `make bundle` and tells the operator to expect only: "`staged crane / kubectl / helm / jq / yq` … `toolchain staged (…)`, `bundle ready: …tar (12G)`, and a `.sha256` beside it." Nothing about charts.
- **reality:** The chart pull is BEST-EFFORT and fails SILENTLY into a warning: `10-mirror-pull.sh:137` guards the whole block with `if have helm`, and 10-mirror-pull.sh:147-151 only `log_warn`s if `helm` is absent or `ISTIO_VERSION` is unset ("…the air-gapped box will only be able to use INGRESS_CONTROLLER=traefik or istio-existing"). `make bundle` does not check for charts, and — critically — `20-bundle-load.sh:68-73` validates only `images/` and `manifests/`, NOT `charts/`. So a chartless bundle passes `bundle-load` cleanly, crosses the air gap, and dies ~10 minutes later at `make install-ingress`, where 46-install-istio.sh:73-78 falls into the network branch and `helm repo add istio-release.storage.googleapis.com` times out on a box with no route out. The doc's Expect line gives the operator no way to notice on the side of the gap where it is still fixable — and once the istio row of the ingress table is corrected (previous finding), this becomes the load-bearing precondition of the DEFAULT path.
- **proof:** scripts/10-mirror-pull.sh:137,147-151 (warn-only) + scripts/20-bundle-load.sh:68-73 (checks images/ + manifests/, never charts/) + scripts/46-install-istio.sh:72-78 (network fallback)
- **fix:** Add to Step 1's Expect: "`charts carried: base-<ver>.tgz istiod-<ver>.tgz gateway-<ver>.tgz` — **if you instead see `helm is not installed here…` or `ISTIO_VERSION is unset…`, STOP**: the bundle carries no Istio charts and the default ingress cannot install on the far side. Fix it here (`make deps`; ISTIO_VERSION is in `.env.example`) and re-cut the bundle." Separately (code, not doc): 20-bundle-load.sh should warn loudly when `${BUNDLE_DIR}/charts` has no `base-*.tgz`, naming the consequence, since that is the last moment anyone can act on it.

### `docs/sneakernet.md:100-111 (Step 3) and docs/sneakernet.md:44`  — docs/sneakernet.md

- **doc claims:** Step 3 is presented as `make bundle-load BUNDLE_TARBALL=…` → "Expect: `verifying checksum` → `installed crane -> ~/.local/bin/crane` … → `carried toolchain: N installed, M already present and kept`", with line 44 saying `bundle-load` "puts them on `~/.local/bin`". No PATH precondition is stated anywhere in the doc.
- **reality:** `20-bundle-load.sh:185-190` **DIES** (not warns) at the very end of the 12 GB load if `BIN_DIR` (default `${HOME}/.local/bin`, 20-bundle-load.sh:79) is not on `$PATH`: "the carried toolchain is installed in …, but that is NOT on your PATH … Add it and re-run". On a freshly hand-provisioned Photon/Ubuntu jump box — exactly the box Step 0b describes, provisioned by hand with no `make deps`, possibly used as root or from a non-login shell — `~/.local/bin` is frequently NOT on PATH. The doc's prescribed command therefore fails, after the longest step in the flow, for a reason the doc never mentions and the operator cannot anticipate. (The failure is at least actionable, which is why this is HIGH and not CRITICAL.)
- **proof:** scripts/20-bundle-load.sh:79 (BIN_DIR default) and scripts/20-bundle-load.sh:185-190 (`die` when BIN_DIR is not on PATH)
- **fix:** In Step 0b's "What it needs" section add a row/bullet: "**`~/.local/bin` must be on your `PATH`** — that is where `bundle-load` installs the carried toolchain, and it **refuses to finish** if it is not: `export PATH="$HOME/.local/bin:$PATH"` (or run `make bundle-load BIN_DIR=/usr/local/bin …`)." Put it BEFORE Step 3, not in the Expect block.

### `docs/sneakernet.md:22-31 (Step 0 — set up the staging box) and docs/sneakernet.md:78-84 (Step 1)`  — docs/sneakernet.md

- **doc claims:** "**For:** it needs the full toolchain, and it is the only box that can download one. … `make deps` … **Expect:** `make deps` completes. This is the ordinary jump-box bootstrap — nothing sneakernet-specific." — i.e. `make deps` is presented as sufficient provisioning for the staging box, which then runs `make bundle`.
- **reality:** `make bundle` HARD-DIES without `file(1)`: 11-bundle.sh:148-151 — "file(1) is not installed, so the STATIC-LINKAGE check on the carried binaries cannot run. Install it (apt-get install file / tdnf install file)". But `file` is NOT installed by `make deps`: `00-install-prereqs.sh:48` installs `ca-certificates curl git jq tar gzip findutils gawk openssl gettext` — no `file`. It is also NOT listed by `make check-tools` (03-check-tools.sh:27-51 has no `file` row), so the doc's own "cheapest failure available" gate cannot catch it either. And `make e2e-sneakernet` structurally cannot catch it: `make bundle` runs on the HOST (e2e-sneakernet.sh:74), which is a dev box that has `file`. UNVERIFIED: whether bare `photon:5.0` / `ubuntu:26.04` ship `file(1)` — I could not check, because running a container is outside my read-only mandate. That is exactly what would settle it: `docker run --rm photon:5.0 sh -c 'command -v file'` and the same for `ubuntu:26.04`. If either lacks it (likely for these minimal bases), a staging box provisioned strictly per this doc dies at Step 1 after a multi-GB pull.
- **proof:** scripts/11-bundle.sh:148-151 (die when file(1) absent) vs scripts/00-install-prereqs.sh:48 (package list, no `file`) and scripts/03-check-tools.sh:27-51 (no `file` row)
- **fix:** Two-part. Doc: add to Step 0 — "the staging box also needs `file(1)` (`apt-get install file` / `tdnf install file`); `make bundle` refuses to stage a binary it cannot prove is statically linked." Code (recommended, and the durable fix): add `file` to `pkg_install` in 00-install-prereqs.sh:48 and add a `file|required|prove the carried binaries are statically linked (make bundle)` row to 03-check-tools.sh, so the gate the doc points at actually measures it.

### `docs/scenario-1.md:355-360`  — docs/scenario-1.md

- **doc claims:** "Two things to know about `make install-ingress` (the default): **It still needs internet on the jump box — for the Istio helm CHART** (`istio-release.storage.googleapis.com`). … Its **images** come from your Harbor and its **Gateway API CRDs** come from the carried bundle, so those two are air-gap clean — **the chart is the one remaining gap.**"
- **reality:** FALSE as of today's code, in the direction that matters most (it states a limitation that no longer exists, and it will be believed). `10-mirror-pull.sh:134-146` now `helm pull`s base/istiod/gateway into `${BUNDLE_DIR}/charts` ("CHARTS ARE AS AIR-GAP-CRITICAL AS IMAGES, AND WE WERE NOT CARRYING ANY", :41-44), and `46-install-istio.sh:64-84` prefers them: if `bundle/charts/base-*.tgz` exists it logs "installing istio from the CARRIED charts … (no network)" and NEVER runs `helm repo add`. In scenario-1 specifically the claim is doubly wrong: `make install-all` runs `mirror` → `mirror-pull` (Makefile:234,446) BEFORE the operator ever reaches Step 7, so `bundle/charts/*.tgz` is already populated and `make install-ingress` will not touch storage.googleapis.com at all. The doc's sentence is a leftover from before the change and now contradicts both the code and sneakernet.md.
- **proof:** scripts/10-mirror-pull.sh:41-45,134-151 (pulls the charts into `${BUNDLE_DIR}/charts`) · scripts/46-install-istio.sh:64-84 (`CHART_LOCAL=1` → `chart_ref()` resolves to the local `.tgz`; the `helm repo add` at :74 is the ELSE branch) · Makefile:234,446
- **fix:** Replace the first bullet (:357-360) with:
"- **The Istio helm charts are CARRIED.** `make mirror` (which `install-all` runs first) pulls them into `bundle/charts/`, and `install-ingress` installs from there without touching the network — the same charts the sneakernet flow carries. Images come from your Harbor; the Gateway API CRDs come from the carried bundle. If you run `install-ingress` on a box that has never run `mirror-pull`, it falls back to fetching the chart from `istio-release.storage.googleapis.com` and needs the internet."

### `docs/scenario-1.md:287-294 (the fork), read against :183-191 (Step 1), :212-214 (Step 2) and :385 (Preconditions)`  — docs/scenario-1.md

- **doc claims:** Step 5 asks "**First, one fork — can this jump box reach BOTH the internet and Harbor?**" and offers a row: "reaches the **internet only** → **the sneakernet flow** … It replaces `install-all`".
- **reality:** The fork is offered FOUR STEPS TOO LATE, so it can never fire for the person it is aimed at. An internet-only box has already been asked to do things it cannot do: Step 1 (:187-190) runs `make vks-login` + `make lab-preflight` + `make psa-check` — all of which need the GUEST API; Step 2 (:213) runs `make fetch-harbor-ca`, which opens a TLS connection to Harbor (`fetch-ca.sh:41-43`, `die "could not connect to ${host}:${port}"`); Step 3 (:239) calls the Harbor API; Step 4 needs the Supervisor. An operator whose box "reaches the internet only" is dead at Step 1 and never gets to the fork. The doc also contradicts itself: its own Preconditions section states dual-homed reach as a hard precondition of the WHOLE document ("**Network reach from the jump box:** the internet, the **Supervisor** API, the **guest** API, and **Harbor**", :385), which makes the Step-5 "internet only" row impossible by construction.
- **proof:** scripts/fetch-ca.sh:41-43 (dies if Harbor is unreachable) · scripts/24-lab-preflight.sh:39 (needs the guest cluster) · scripts/22-harbor-robot.sh (Harbor API) · docs/scenario-1.md:385 (the contradicting precondition)
- **fix:** Move the fork to **Step 0**, before `make deps`, as the first question the doc asks — and delete the Preconditions line's unconditional "the internet" (:385) or qualify it "(dual-homed path only; see the fork in Step 0)". Reduce the Step-5 occurrence to one sentence: "If you took the dual-homed branch in Step 0, run `make install-all`."

### `docs/scenario-1.md:5 ("The jump box is dual-homed (internet + lab).") and :294 ("→ **the sneakernet flow** — two boxes")`  — docs/scenario-1.md

- **doc claims:** scenario-1 uses "jump box" throughout to mean THE DUAL-HOMED BOX (internet + Harbor + cluster), and at :294 hands the operator straight to sneakernet.md.
- **reality:** Overloaded, and the hand-off is exactly where it bites. In `docs/sneakernet.md:4` and its table at :11-15, **"jump box" is the AIR-GAPPED box** — "reaches the internet ❌ / reaches Harbor + the cluster ✅" — and the internet-side box is called the "**staging box**". So an operator who has spent scenario-1 calling their machine "the jump box" clicks the link and reads "`make deps` **cannot run here** — it downloads" (sneakernet.md:35) about a box they believe is theirs. The same term names the two boxes on OPPOSITE sides of the air gap. (CLAUDE.md and `check-tools`'s own help text also say "jump box" for the dual-homed one, so the collision is repo-wide.)
- **proof:** docs/sneakernet.md:4,11-15,33-35,125 ("Do NOT run `make install-all` on the jump box — it starts with `mirror`, which needs the internet") vs docs/scenario-1.md:5
- **fix:** Pick one and enforce it. Cheapest: in scenario-1, at :5, write "**the DUAL-HOMED box** (internet + lab) — sneakernet.md calls the internet-side box the *staging box* and reserves *jump box* for the air-gapped one; if you go there, your machine is the **staging box**." And at the fork (:294) name the destination role explicitly: "…your box becomes the **staging box**; you will need a SECOND, air-gapped box (sneakernet's *jump box*)."

### `docs/scenario-1.md:277-285 (Step 5) and :319 (Step 6)`  — docs/scenario-1.md

- **doc claims:** Step 5: "`make env-populate`   # DISCOVER what is discoverable (Harbor/ArgoCD endpoints) instead of re-typing it". Step 6: "`make creds-show` … **Expect:** a table with Harbor, ArgoCD, Gitea, the Tekton dashboard…"
- **reality:** `env-populate` does not only DISCOVER — it GENERATES secrets, and the doc never says so. `02-env.sh:62-68` mints `GITEA_ADMIN_PASSWORD`, `GITEA_CI_PASSWORD`, `HARBOR_PASSWORD` ("generated — OVERRIDE for a real lab") and **`ARGOCD_ADMIN_PASSWORD`** ("generated — KinD only; real lab sets its own") whenever the value is empty/placeholder. On a real lab the doc never asks for `ARGOCD_ADMIN_PASSWORD` (A2 tells them to run `argocd account update-password`, :134, and to set only ARGOCD_NAMESPACE/SERVER/TRACK_BRANCH, :141-146) — so it IS a placeholder, and `env-populate` mints a random one. Then `make creds-show` (Step 6) prints it as ArgoCD's password: `argocd-password.sh:40-52` first tries to read `argocd-initial-admin-secret` **from `$KUBECONFIG` (the GUEST)**, where ArgoCD does not live on a real lab, finds nothing, and falls through to `:58-60`, printing `$ARGOCD_ADMIN_PASSWORD` — the fabricated value. The operator gets a confident, wrong credential and will conclude ArgoCD is broken.
- **proof:** scripts/02-env.sh:62-68 (`if is_placeholder "${ARGOCD_ADMIN_PASSWORD:-}"; then a_pw="$(gen_password)"; env_set ARGOCD_ADMIN_PASSWORD "$a_pw"`) · scripts/argocd-password.sh:40-60 (reads the initial-admin secret from `$KUBECONFIG` = the GUEST; falls back to the .env value) · docs/scenario-1.md:141-146 (ARGOCD_ADMIN_PASSWORD never requested)
- **fix:** Two edits. (a) Step 5's comment (:278) → `make env-populate   # GENERATE the secrets only we own (Gitea) + DISCOVER cluster endpoints. It does NOT know Harbor's or ArgoCD's password — you set those in A1/A2.` (b) Add `ARGOCD_ADMIN_PASSWORD=<the one you set with 'argocd account update-password' in A2>` to A2's set-these block (:141-146), so `env-populate` finds it non-placeholder and `creds-show` prints the truth. (CODE follow-up worth filing: `argocd-password.sh` should look in `$ARGOCD_KUBECONFIG`, not `$KUBECONFIG`, when ArgoCD is off-cluster.)

### `docs/scenario-1.md:101-109 (A2 step 3) and :160 (A3)`  — docs/scenario-1.md

- **doc claims:** Two `vcf` commands are printed as bare, executable instructions with no provenance marker:
`vcf context create --endpoint https://<supervisor-IP> --username <user>@<SSO-DOMAIN> --insecure-skip-tls-verify --auth-type basic`
`vcf cluster kubeconfig get $VKS_CLUSTER_NAME --export-file ./secrets/vks.kubeconfig`
- **reality:** Both are DOC-INFERRED and the repo says so ELSEWHERE. `docs/lab-validation-plan.md:204` states in terms: "**Why:** `vcf cluster kubeconfig get --export-file` is a doc-inferred flag shape" and gives it a fallback ("⚠️ if this errors: send us `vcf cluster kubeconfig get --help`", :212). `30-vks-login.sh:77-80` carries `# TODO(verify on a real VKS lab): confirm 'vcf context create --help' …`. scenario-1 strips all of that and presents them as fact. Worse, the doc's own A2 invocation does not match ANY invocation the repo actually runs: `30-vks-login.sh:70` uses `--auth-type basic` with the context name supplied INTERACTIVELY (no positional), while `31-fetch-argocd-kubeconfig.sh:75-79` uses a POSITIONAL context name plus `--type k8s` (not `--auth-type basic`). Three different shapes of the same vendor command in one repo, one of them unmarked in the runbook the admin executes. This repo has already shipped a fabricated `vcf` command once — `check-how-provenance.sh:6-11` exists because of it, but it gates ONLY `.env.example`, not `docs/`.
- **proof:** docs/lab-validation-plan.md:204,211-212 (the same flag graded doc-inferred, with a fallback) · scripts/30-vks-login.sh:70,77-80 (`--auth-type basic`, interactive ctx name, TODO(verify)) · scripts/31-fetch-argocd-kubeconfig.sh:21-24 ("documented for 9.0 and INFERRED for 9.1"), :75-79 (positional ctx + `--type k8s`) · scripts/check-how-provenance.sh:6-20 (gate scoped to .env.example only)
- **fix:** Tag both blocks `⚠️ UNVERIFIED-COMMAND (9.0-doc-inferred-for-9.1)` — the marker the sibling doc already uses — and give each the fallback lab-validation-plan.md already writes: "if this errors, run `vcf context create --help` / `vcf cluster kubeconfig get --help` and send us the output". Then reconcile the three invocations (`30` vs `31` vs the doc) to ONE shape, or state plainly why they differ. Follow-up worth filing: extend `check-how-provenance.sh` to scan `docs/**.md` for bare `vcf` commands — the gate that exists for this failure cannot currently see the document where it happened.

### `docs/scenario-1.md:208-220 (Step 2)`  — docs/scenario-1.md

- **doc claims:** "```bash\nmake fetch-harbor-ca      # HARBOR_URL → HARBOR_CA_FILE\n```\n**Expect:** `secrets/harbor-ca.crt` exists…" — presented as a step that simply works.
- **reality:** On a real lab it can legitimately HARD-FAIL, and the doc offers no fallback. `fetch-ca.sh:60-69`: if Harbor presents exactly ONE certificate and it is NOT self-signed (subject != issuer — the normal cert-manager/corporate-PKI shape the doc itself anticipates at :61 "cert-manager self-issues"), the script `die`s with "Its CA is not on the wire, so it cannot be fetched from here. Ask the platform team for the issuing CA". That is the honest behaviour, but Step 2 gives the operator no branch for it — and Step 2's CA is load-bearing for `make mirror` (SSL_CERT_FILE) and in-cluster Kaniko, both of which the doc promises are "handled for you" (:218-219).
- **proof:** scripts/fetch-ca.sh:60-69 (`die "… presents ONE certificate that is NOT self-signed … Ask the platform team for the issuing CA"`), :41-43 (`die "could not connect"`)
- **fix:** Add a branch to Step 2 after the Expect line:
"**If it says "presents ONE certificate that is NOT self-signed":** Harbor's issuing CA is not on the wire (a corporate/cert-manager PKI). Get the CA PEM from your platform team, drop it at `./secrets/harbor-ca.crt`, and confirm it with `openssl verify -CAfile ./secrets/harbor-ca.crt <(openssl s_client -connect $HARBOR_URL:443 -showcerts </dev/null 2>/dev/null)`. `make env-validate` (Step 5) is the check that proves you got the right one."

### `docs/scenario-2.md:68 and :263`  — docs/scenario-2.md

- **doc claims:** Two literal shell commands: `kubectl --kubeconfig $ARGOCD_KUBECONFIG -n $ARGOCD_NAMESPACE auth can-i create applications.argoproj.io` (:68) and `kubectl --kubeconfig $ARGOCD_KUBECONFIG get pods -A | grep argocd-application-controller` (:263).
- **reality:** Both are DEAD COMMANDS in this runbook. (a) The doc itself tells the tenant NOT to set ARGOCD_KUBECONFIG (:338), and it is not in the Step-3 `.env` block (:81-95). (b) Even if it were in `.env`, `.env` is sourced by `load_env`/make — it is NOT exported into the operator's interactive shell — so `$ARGOCD_KUBECONFIG` and `$ARGOCD_NAMESPACE` expand to EMPTY and `--kubeconfig` swallows the next token: `kubectl --kubeconfig -n auth can-i …` → `error: stat -n: no such file or directory`. (c) `get pods -A` on the Supervisor is a cluster-wide list a locked-down tenant will not hold anyway. The doc's own escape hatch at :266-267 ("ask the platform team … or use ARGOCD_MECHANISM=api") admits the command usually cannot run.
- **proof:** scripts/lib/os.sh (load_env sources .env into the SCRIPT's env, not the operator's shell) · docs/scenario-2.md:338 (the instruction that guarantees the var is unset) · scripts/70-configure-argocd.sh:50 (ARGOCD_KUBECONFIG defaults to $KUBECONFIG *inside* the script only)
- **fix:** Delete both commands, or replace them with the repo's own tool: `make argocd-preflight` (Makefile:295, scripts/23-argocd-preflight.sh) already reports the write mechanism, the namespace, the AppProject and the registered destinations. If a raw command must stay, make it self-contained: `kubectl --kubeconfig ./secrets/argocd.kubeconfig -n <the-argocd-namespace-you-were-told> auth can-i create applications.argoproj.io` — and say plainly that a tenant is EXPECTED to get `no` here, which is why `ARGOCD_MECHANISM=api` exists.

### `docs/scenario-2.md:148-151`  — docs/scenario-2.md

- **doc claims:** "…or the equivalent by hand: `openssl s_client -connect <harbor-host>:443 -showcerts </dev/null 2>/dev/null | openssl x509 -outform PEM > secrets/harbor-ca.crt`"
- **reality:** NOT equivalent — it is the exact bug `make fetch-harbor-ca` was rewritten to eliminate, reproduced verbatim in the doc. `openssl x509` reads only the FIRST PEM block on stdin, which is the SERVER LEAF, not the CA. scripts/fetch-ca.sh:5-23 names this command as "THE BUG THIS REPLACES": the resulting file is handed to crane (SSL_CERT_FILE), podman (--cert-dir) and in-cluster Kaniko (the harbor-ca ConfigMap) as a TRUST ANCHOR, and Go cannot build a chain from a leaf → `x509: certificate signed by unknown authority`. It only looks fine on our KinD Harbor, whose leaf is self-signed (leaf == CA). On a real lab Harbor (cert-manager leaf from a separate CA — exactly the Scenario-2 lab), the by-hand file is NOT a CA and the failure surfaces much later, inside Kaniko. The real script takes the LAST cert in the chain and then `openssl verify`s it before writing (fetch-ca.sh:56-80+).
- **proof:** scripts/fetch-ca.sh:5-23 (the comment naming this exact command as the bug), :44-80 (chain split, take-the-issuer, verify-before-write) · scripts/60-configure-tekton.sh:102-106 (the file becomes the Kaniko trust anchor)
- **fix:** Delete the by-hand `openssl s_client | openssl x509` snippet from docs/scenario-2.md:148-151. `make fetch-harbor-ca` is right there on the line above and it VERIFIES what it writes. If a manual fallback is genuinely wanted, it must extract the LAST certificate of the chain and `openssl verify -CAfile` the leaf against it — otherwise it hands the operator a leaf and calls it a CA.

### `docs/scenario-2.md:243-245`  — docs/scenario-2.md

- **doc claims:** "Point `HARBOR_INFRA_PROJECT` / `HARBOR_APP_PROJECT` at the project(s) you were **granted** — they already exist, so `make mirror` … just pushes to them (it does **not** need to create them)."
- **reality:** FALSE. `make mirror` → `mirror-push` → scripts/21-mirror-push.sh:56-57 calls `ensure_project` for BOTH projects, unconditionally and UNGUARDED, under `set -euo pipefail` (21-mirror-push.sh:8). ensure_project HEADs `/projects?project_name=` and, on anything other than 200, POSTs a CREATE (lib/harbor.sh:92-98). Its 403 branch logs "not permitted to create Harbor project … If the project already exists and you were granted it, this is harmless" and then `return 1` (lib/harbor.sh:128-132) — which under `set -e` KILLS mirror-push. The sibling caller knows this and guards it: 22-harbor-robot.sh:42 uses `ensure_project "$p" || true`. 21-mirror-push.sh:56-57 and 22-builder-push.sh:68 do not. So the code's own "this is harmless" message is immediately followed by a fatal exit. Whether the HEAD returns 200 for a *project-level robot* credential (which is what the doc tells the tenant to put in HARBOR_USERNAME, :83) is **UNVERIFIED** — the code's own comment (lib/harbor.sh:123) anticipates that a robot cannot create projects, which is precisely the path that dies.
- **proof:** scripts/21-mirror-push.sh:8,56-57 · scripts/lib/harbor.sh:90-98,122-132 · scripts/22-harbor-robot.sh:42 (the guarded form) · scripts/22-builder-push.sh:68
- **fix:** CODE: guard the two call sites the same way the robot script does — `ensure_project "$HARBOR_INFRA_PROJECT" || true` (21-mirror-push.sh:56-57, 22-builder-push.sh:68), and follow with a HEAD-only existence assertion that dies with "project '<p>' does not exist and you may not create it — ask your platform team" ONLY when the project is genuinely absent. DOC: until then, scenario-2.md:245 must say `make mirror` still CALLS the project-create API and may fail on a 403 even when the project exists. Settle the HEAD-with-robot-credential behaviour on the lab.

### `docs/scenario-2.md:371-374`  — docs/scenario-2.md

- **doc claims:** "⚠️ **As a tenant you cannot install the Gateway API CRDs.** If they are absent, you are on the classic path … you must **ask the mesh admin** to enable a gateway."
- **reality:** FALSE for the tenant this very runbook describes — and it sends them to file a ticket for something they can self-service in one command. The doc itself says the tenant holds "**cluster-admin on your own guest cluster**" (:20) and "cluster-admin on the workload cluster is required" (:422); Gateway API CRDs are cluster-scoped resources OF THAT GUEST CLUSTER. And the repo installs them: `make install-ingress` runs istio_ensure_gwapi_crds (`kubectl apply --server-side` of the pinned bundle). The code's own preflight offers BOTH branches — "If you OWN this cluster:  make install-ingress   (it installs the CRDs for you)" / "If you are a TENANT: ASK THE MESH ADMIN" (48-istio-preflight.sh:61-64) — and the doc reproduces only the second half.
- **proof:** scripts/48-istio-preflight.sh:58-64 · scripts/lib/istio.sh (istio_ensure_gwapi_crds — kubectl apply --server-side of GATEWAY_API_VERSION) · docs/scenario-2.md:20,422 (this doc's own cluster-admin requirement)
- **fix:** Rewrite scenario-2.md:371-374 to mirror the preflight's two branches: "If Istio was installed into YOUR guest cluster (you hold cluster-admin there), you CAN install the Gateway API CRDs yourself — `make install-ingress INGRESS_CONTROLLER=istio-existing` does it, and Istio then auto-provisions the proxy + LB from the Gateway you create. Only ask the mesh admin if you do NOT hold cluster-admin on the cluster the mesh runs in." Fix the same over-broad line in 48-istio-preflight.sh:63-64 ("cluster-scoped — a tenant cannot" is wrong when the tenant owns the cluster).

### `docs/scenario-2.md:303`  — docs/scenario-2.md

- **doc claims:** "`make env-populate`   # DISCOVER what is discoverable (Harbor / ArgoCD endpoints) instead of re-typing it"
- **reality:** It CANNOT discover them in Scenario 2, and it can CLOBBER what the tenant was granted. env_populate discovers against `$KUBECONFIG` — the GUEST cluster (02-env.sh:73): it looks for a LoadBalancer Service in `${HARBOR_NAMESPACE:-harbor}` (02-env.sh:75) and `svc/argocd-server` in `${ARGOCD_NAMESPACE}` (02-env.sh:76). In Scenario 2 Harbor and ArgoCD are **Supervisor Services** — they are not in the guest cluster at all, which is the doc's own headline fact (:8-10, :258-260). So discovery always prints "not discovered". Worse, the DISCOVER writes are UNCONDITIONAL (`env_set HARBOR_URL "$hip"`, 02-env.sh:77) — unlike the GENERATE branch, which is is_placeholder-guarded (02-env.sh:62-68) — so if the guest cluster happens to carry any LoadBalancer Service in a namespace called `harbor`, env-populate OVERWRITES the granted HARBOR_URL in the tenant's `.env`. env_populate also mints a random HARBOR_PASSWORD if unset (02-env.sh:67) — harmless only because the tenant set it in Step 3.
- **proof:** scripts/02-env.sh:62-68 (guarded GENERATE) vs :73-78 (UNGUARDED DISCOVER, against $KUBECONFIG = the guest)
- **fix:** CODE: make the DISCOVER writes placeholder-guarded like the GENERATE ones (`is_placeholder "${HARBOR_URL:-}" && env_set …`), and skip the Harbor/ArgoCD discovery entirely when the endpoints are known to be Supervisor-side. DOC: at scenario-2.md:303 say plainly "`make env-populate` cannot discover a Supervisor-hosted Harbor/ArgoCD from your guest kubeconfig — you already typed those in Step 3; it only mints the Gitea secrets. Skip it if you set everything by hand."

### `docs/scenario-2.md:308-310`  — docs/scenario-2.md

- **doc claims:** "**Expect:** … `env-validate` → Harbor reachable **and authenticated over HTTPS with your CA**. If the CA you were handed does not actually verify Harbor, it fails **here**, not inside Kaniko an hour later."
- **reality:** The gate passes by not looking, in the most likely failure case. 02-env.sh:169-170: the `--cacert` argument is added ONLY if HARBOR_CA_FILE is non-empty AND the file exists; otherwise, on https, it falls back to **`-k`** (skip verification entirely). So a tenant who mistyped HARBOR_CA_FILE, or who has not fetched the CA yet (the doc's own Step 2 could have failed), gets a GREEN env-validate that proves nothing about the CA — and then discovers it "inside Kaniko an hour later", which is exactly what the doc promises will not happen. A present-but-WRONG CA does fail, but is reported as "Harbor unreachable at https://… (HTTP 000)" (02-env.sh:189) — an error naming the wrong cause.
- **proof:** scripts/02-env.sh:168-172 (the `-k` fallback), :189 (the misattributed error message)
- **fix:** CODE: in env_validate, when scheme=https and HARBOR_INSECURE!=1, DIE if HARBOR_CA_FILE is unset/missing rather than silently `-k` (or add a loud `log_warn "CA NOT EXERCISED — this check proves nothing about your trust anchor"`), and distinguish a TLS-verification failure (curl exit 60) from an unreachable host in the error text. DOC: until then, weaken the Expect line at :308-310 — it is currently a promise the gate cannot keep.

## MEDIUM (28)

### `docs/prerequisites-manual.md:60`  — docs/prerequisites-manual.md

- **doc claims:** `curl https://mise.run | sh`
- **reality:** Missing `-fsSL`. Without `-f`, an HTTP error body (404/5xx HTML) is piped straight into `sh`; without `-L` a redirect is not followed. Both places in the repo that do the same thing use `-fsSL`, so the doc is inconsistent with its own code.
- **proof:** Makefile:83 (`curl -fsSL https://mise.run | sh`) + bootstrap-jumpbox.sh:89 (`curl -fsSL https://mise.run | sh`)
- **fix:** `curl -fsSL https://mise.run | sh`

### `docs/prerequisites-manual.md:64-66`  — docs/prerequisites-manual.md

- **doc claims:** `make deps` "sets up rootless podman for the builder-image build — crun + registries on Photon, uidmap + slirp4netns on Ubuntu"
- **reality:** Contradicts the pure function that IS the package list. `engine_packages podman apt-get` prints `podman crun uidmap passt slirp4netns` — Ubuntu gets **crun too**, and **passt**, which the code's own comment (lib/os.sh:186-190) calls load-bearing (podman 5's default rootless net backend on 26.04) and which the doc omits entirely. `engine_packages podman tdnf` prints only `podman crun`. And the `unqualified-search-registries` write is gated on engine==podman, NOT on Photon (00-install-prereqs.sh:76-83).
- **proof:** scripts/lib/os.sh:196-206 (engine_packages) + scripts/00-install-prereqs.sh:76-83
- **fix:** Replace with: "…it also installs the rootless-podman prerequisites for your OS (crun, uidmap, passt/slirp4netns) and sets podman's `unqualified-search-registries`."

### `docs/prerequisites-manual.md:68-70`  — docs/prerequisites-manual.md

- **doc claims:** "`make deps CONTAINER_ENGINE=docker` installs docker + its rootless prerequisites instead (and NOTHING of podman)."
- **reality:** The "NOTHING of podman" half is TRUE (engine_packages docker:* contains no podman). The "rootless prerequisites" half is FALSE on Ubuntu 24.04: its `docker.io` deb ships ZERO rootless files and the repo deliberately REFUSES to add download.docker.com, so 00-install-prereqs.sh:102-107 warns "docker here is ROOTFUL-ONLY, which costs a SUDO PER REGISTRY". The doc promises something the code explicitly declines to deliver there (partially mitigated by its own pointer to `make engine-check`).
- **proof:** scripts/lib/os.sh:200-203 (engine_packages docker) + scripts/00-install-prereqs.sh:96-107
- **fix:** "…installs docker + (where the distro provides them) its rootless prerequisites. On Ubuntu 24.04 there is no distro rootless helper, so docker there is **rootful-only** — one sudo per registry. `make engine-check` tells you which box you are on."

### `docs/prerequisites-manual.md:63-72`  — docs/prerequisites-manual.md

- **doc claims:** The doc ends at `make deps` with no verification step.
- **reality:** `make check-tools` exists (Makefile:189 -> scripts/03-check-tools.sh) and is in the README's own bootstrap checklist (README.md:203). It is exactly the gate that would turn finding #1 from a silent wrong-box into a red line (`MISSING REQUIRED: helm yq crane`). Its absence is why the mise-shims gap is invisible to an operator following this doc.
- **proof:** Makefile:189 + scripts/03-check-tools.sh:81-83 + README.md:203
- **fix:** Append `make check-tools   # must print "all REQUIRED tools present"` as the final step of the doc.

### `CLAUDE.md:125 and 127`  — CLAUDE.md

- **doc claims:** Row 125: "`make mirror` | (dual-homed) pull images -> push to Harbor." Row 127: "`make mirror-verify` | ... read-only; **run after `make mirror`**".
- **reality:** `mirror` already runs `mirror-verify` (it is a prerequisite), so row 125 understates it and row 127's 'run after' is stale advice for a step that has already run. CLAUDE.md:860 states the correct fact ("**`make mirror` now depends on `mirror-verify`.** A push you have not verified is not a mirror") — so the Common-commands table contradicts the SETTLED section.
- **proof:** Makefile:234 (mirror: mirror-pull mirror-push mirror-verify) ; contradicted by CLAUDE.md:860
- **fix:** Row 125: "pull -> push -> **VERIFY** (mirror-verify is a prerequisite; a push you have not verified is not a mirror)." Row 127: "standalone re-verification of an existing mirror (also runs automatically as part of `make mirror`)."

### `CLAUDE.md:121, 122, 123`  — CLAUDE.md

- **doc claims:** Row 121: "`make ci` | Offline gate: `static-check` + `docs-lint`". Row 122 lists static-check as 10 gates. Row 123: "`make sec` | ... `secrets` (gitleaks) + `prose-secrets` + `trivy-fs` + `trivy-config`".
- **reality:** `ci` = static-check docs-lint **diagrams-check** (3, not 2). `static-check` runs SEVENTEEN prerequisites — the doc omits check-doc-make-targets, check-gwapi-istio-alignment, check-vks-terminology, check-env-clobber, check-app-hardcodes, check-app-toolchains, check-pull-secret-alignment. `sec` = secrets-scan trivy-fs trivy-config, and secrets-scan = **check-secrets-untracked** + secrets + prose-secrets — the untracked-secrets gate is missing from the doc.
- **proof:** Makefile:812 (ci: static-check docs-lint diagrams-check) ; Makefile:809 (the full static-check prereq list) ; Makefile:720 (secrets-scan: check-secrets-untracked secrets prose-secrets) ; Makefile:723 (sec: secrets-scan trivy-fs trivy-config)
- **fix:** Regenerate rows 121-123 from `Makefile:809/812/723` verbatim rather than re-typing them — this list has now drifted twice.

### `CLAUDE.md:384`  — CLAUDE.md

- **doc claims:** In the owed re-test matrix: "`make e2e-sneakernet`            # photon + ubuntu; the whole carry"
- **reality:** `e2e-sneakernet` is SINGLE-OS (driven by `SNEAKERNET_OS`); the OS matrix is `e2e-sneakernet-both`, which sets `SNEAKERNET_OS="photon ubuntu"`. CLAUDE.md:373 gets this right ("+ `e2e-sneakernet-both`") and CLAUDE.md:384 does not — so an agent working the matrix runs ONE OS and reports two, which is exactly the false-green the matrix exists to prevent (and the Photon/Ubuntu split is where the toolchain bugs live).
- **proof:** Makefile:433 (e2e-sneakernet: passes SNEAKERNET_OS) ; Makefile:437-439 (e2e-sneakernet-both: $(MAKE) e2e-sneakernet SNEAKERNET_OS="photon ubuntu")
- **fix:** Line 384: `make e2e-sneakernet-both   # photon + ubuntu; the whole carry, both far-side OSes`.

### `CLAUDE.md:149 (and the Common-commands table as a whole)`  — CLAUDE.md

- **doc claims:** "`make jumpbox` / `jumpbox-both` | ... `JUMPBOX_OS=photon` (default) or `JUMPBOX_OS=ubuntu`; **rootless podman**, joined to the kind network..."
- **reality:** `jumpbox`/`jumpbox-image` are parameterized by JUMPBOX_OS **x JUMPBOX_ENGINE** (podman|docker) — the row is engine-blind. And `jumpbox-matrix`, the 4-leg {photon,ubuntu} x {podman,docker} run that the handoff (CLAUDE.md:427, 508) leans on as the PROOF of the docker claim, is not in the table at all. Also absent from the agent's map of the repo: preflight, lab-preflight, engine-check, trust-harbor, state-show, airgap-toolchain-test, bootstrap-engine-test, builder-build, builder-push, mirror-verify-red-test, e2e-sneakernet-both.
- **proof:** Makefile:494 (jumpbox-image: JUMPBOX_OS x JUMPBOX_ENGINE) ; Makefile:519 (jumpbox: jumpbox-image) ; Makefile:534 (jumpbox-matrix) ; Makefile:511 (airgap-toolchain-test) ; Makefile:515 (bootstrap-engine-test) ; Makefile:252/256 (builder-build/builder-push) ; Makefile:292 (preflight)
- **fix:** Add `JUMPBOX_ENGINE=podman|docker` to row 149, and add rows for `jumpbox-matrix`, `bootstrap-engine-test`, `airgap-toolchain-test`, `preflight`, `engine-check`, `trust-harbor`, `builder-build`/`builder-push`, `state-show`, `e2e-sneakernet-both`. The table is the agent's map; a dozen operator-invocable targets are off it.

### `CLAUDE.md:136 (correct) vs Makefile:608,611 (wrong)`  — CLAUDE.md

- **doc claims:** CLAUDE.md:122/136: `test-scripts` (offline script unit tests) is "Part of `static-check`".
- **reality:** CLAUDE.md is RIGHT (Makefile:809 lists test-scripts as a static-check prerequisite) — but the Makefile's OWN comment block says the opposite: "##@ Offline script tests (unit tests for script logic; NOT in static-check/ci)" and "Deliberately NOT wired into static-check/ci so the core gate stays fast — run explicitly via `make test-scripts`." One of the two will mislead the next agent, and it is the comment. Reporting it here because CLAUDE.md is the contract that says which is authoritative.
- **proof:** Makefile:809 (static-check: ... test-scripts app-test) contradicting Makefile:608 and Makefile:611
- **fix:** Code fix, not a doc fix: correct/delete the stale Makefile:608-611 comment so it says test-scripts IS part of static-check.

### `README.md:52-53 vs README.md:193-204 vs docs/scenario-1.md:188-283`  — README.md

- **doc claims:** README.md:193 — "Then run these, in order. **Nothing else on this page requires action.**"
- **reality:** There are THREE mutually inconsistent "run this" sequences for the same VKS onboarding, in the same README plus its own scenario doc: (a) README:52 — deps → install-vcf-clis → env-init → env-populate → env-check → psa-check; (b) README:195-204 — deps → engine-check → env-init → env-populate → fetch-harbor-ca → env-check → check-tools (no install-vcf-clis, no vks-login); (c) docs/scenario-1.md — … → vks-login (:188) → fetch-harbor-ca (:213) → env-populate (:278) → env-check (:279). Sequence (b) claims completeness ("nothing else on this page requires action") while omitting `make install-vcf-clis` — without which `make vks-login` has no `vcf` CLI (03-check-tools.sh:40 marks `vcf` lab-only, installed by `make install-vcf-clis`).
- **proof:** Makefile:99 (install-vcf-clis → 01-install-vcf-clis.sh); scripts/03-check-tools.sh:40 (`vcf|lab-only|Supervisor login … Installed by 'make install-vcf-clis'`); docs/scenario-1.md:188,213,278,279
- **fix:** Keep ONE authoritative sequence in the README (the bare-jump-box one) and say explicitly that everything after it lives in the scenario doc. Delete the phrase "Nothing else on this page requires action" — it is false for both VKS paths.

### `README.md:196 — "`make deps`# toolchain: mise + podman (git must already be present)"`  — README.md

- **doc claims:** `make deps` installs mise and podman, and git must already be there.
- **reality:** Understated and half-false. `deps` → `deps-prereqs` → `scripts/00-install-prereqs.sh:48`, which runs `pkg_install ca-certificates curl git jq tar gzip findutils gawk openssl "$GETTEXT_PKG"` — it INSTALLS git, and as of today it also installs **gawk, openssl and gettext(-base)**, which are precisely the packages a bare `photon:5.0` lacks and without which the flow dies later (at `mirror-verify`, or in any per-app loop in `lib/apps.sh`). The parenthetical is true only in the trivial sense that you need git to have cloned the repo. A reader tuning a minimal jump-box image from this line will under-provision it.
- **proof:** scripts/00-install-prereqs.sh:44-48 (GETTEXT_PKG selection + the pkg_install line); scripts/00-install-prereqs.sh:32-43 (the measured "photon:5.0 LACKS awk, openssl, envsubst, git, make" comment); Makefile:95-96 (deps-prereqs → 00-install-prereqs.sh)
- **fix:** "`make deps` # mise-managed tools (kubectl, helm, kind, crane, jq, yq…) + OS packages (git, curl, gawk, openssl, gettext/envsubst, tar) + the container engine (podman by default)."

### `README.md:16-17 ("an **optional** pluggable ingress (**Istio** default…)") and README.md:23 ("Add one when you want`*.vks.local`URLs (`make install-ingress`…)")`  — README.md

- **doc claims:** `make install-ingress` is presented with no air-gap caveat; Istio is the default.
- **reality:** On an air-gapped box the DEFAULT ingress works only if the bundle happens to carry the Istio charts. `scripts/46-install-istio.sh:64-78` uses `bundle/charts/*.tgz` when present, and otherwise falls back to `helm repo add https://istio-release.storage.googleapis.com/charts` — "this needs the internet". The charts are carried by `scripts/10-mirror-pull.sh:138-151` ONLY if helm is installed on the staging box AND `ISTIO_VERSION` is set; otherwise it logs `the air-gapped box will only be able to use INGRESS_CONTROLLER=traefik or istio-existing`. The README — the front door, and the place that names Istio as the default — says nothing about this dependency, so an operator whose bundle predates today's chart-carrying change (or was cut with `ISTIO_VERSION` unset) will find the default ingress silently reaching for storage.googleapis.com from a box with no internet.
- **proof:** scripts/46-install-istio.sh:64-78 (carried charts preferred, else `helm repo add` — "this needs the internet"); scripts/10-mirror-pull.sh:138-151 (charts carried only if helm present AND ISTIO_VERSION set; warns otherwise)
- **fix:** Add one sentence to the ingress paragraph (README:20-24): "Air-gapped? `make install-ingress` with the default Istio installs from the charts the bundle carries (`bundle/charts/*.tgz`, pulled by `make mirror-pull`). If your bundle has none, only `INGRESS_CONTROLLER=traefik` or `istio-existing` will work there."

### `README.md:170 — "It's idempotent (re-run skips what's present); **pin a ref with`REF=<tag-or-sha>`**."`  — README.md

- **doc claims:** You can pin the bootstrap to a ref by setting `REF=<tag-or-sha>`.
- **reality:** True for the download-then-run form, but for the **piped** form the README leads with (README:159, `curl … | bash`), `REF=v1 curl … | bash` sets REF in **curl's** environment, not bash's — the script reads `REF="${REF:-main}"` from its own env (bootstrap-jumpbox.sh:24), so it silently checks out `main` and the operator believes they pinned it. The working form is `curl … | REF=v1 bash`.
- **proof:** bootstrap-jumpbox.sh:24 (`REF="${REF:-main}"`), bootstrap-jumpbox.sh:94-100 (fetch/checkout at $REF); README.md:159 (the piped invocation)
- **fix:** Show the pinned form literally: `curl -fsSL https://…/bootstrap-jumpbox.sh | REF=v1.2.3 bash` (and note that with the download-then-run form it is `REF=v1.2.3 bash bootstrap-jumpbox.sh`).

### `README.md:232 — sneakernet "…replaces`make mirror` (and `make install-all`, **which starts with it**)"`  — README.md

- **doc claims:** `make install-all` starts with `make mirror`.
- **reality:** It does not. `install-all: preflight mirror mirror-verify builder-image vks-login platform gitops` (Makefile:446) — it starts with `preflight` (itself `check-tools argocd-preflight lab-preflight psa-check`, Makefile:292). This matters for the air-gap reader: `preflight` does NOT probe the internet (that is exactly why `require_internet()` was added to mirror-pull — see the comment at scripts/lib/os.sh:486-495, "Nothing in `preflight` probes the internet … so `make install-all` on an air-gapped box sails through preflight"), so "install-all starts with mirror" mis-describes both what runs first and where an air-gapped box will actually stop.
- **proof:** Makefile:446 (install-all: preflight mirror mirror-verify builder-image vks-login platform gitops); Makefile:292 (preflight: check-tools argocd-preflight lab-preflight psa-check); scripts/lib/os.sh:486-495 (require_internet's rationale: preflight does not probe the internet)
- **fix:** "…it replaces `make mirror` (and therefore `make install-all`, whose `mirror` step needs the internet)."

### `docs/sneakernet.md:155-157`  — docs/sneakernet.md

- **doc claims:** "`make airgap-toolchain-test` proves the **toolchain** half, which `e2e-sneakernet` cannot: **its jump-box image runs `make deps` at build time**, so it already has `kubectl`/`helm`/`jq`/`yq` and could never notice if the bundle failed to carry them."
- **reality:** The stated REASON is FALSE, twice over. (a) The jump-box images do NOT run `make deps` at build time — there is no `RUN make deps` in either Dockerfile, and their own headers say the opposite: "The image installs only the documented bootstrap set; `make deps` (**at container start**) installs the rest" (jumpbox/Dockerfile.photon:6, jumpbox/Dockerfile.ubuntu:6). (b) In the sneakernet leg `make deps` is not run AT ALL: jumpbox-run.sh:96-104 — "# NO `make deps` HERE. THAT WAS THE LIE." — precisely because running it downloaded crane over the network and made the e2e prove the opposite of its own name. So `e2e-sneakernet` does exercise the toolchain install (bundle-load puts all five on the box); what it does NOT do is run with `--network none`, assert the tools are ABSENT first, assert each one EXECUTES, or render the carried Istio chart offline — which is the real, and sufficient, justification for `airgap-toolchain-test` (test-airgap-toolchain.sh:10-15,40-41,53-59,118-120,144-154). The same false rationale is duplicated in Makefile:507-509 and jumpbox/Dockerfile.airgap:4, so it will keep propagating.
- **proof:** scripts/jumpbox-run.sh:96-104 ("NO `make deps` HERE. THAT WAS THE LIE.") + jumpbox/Dockerfile.photon:6 + jumpbox/Dockerfile.ubuntu:6 (deps at container START, not build) + scripts/test-airgap-toolchain.sh:10-15,144-154
- **fix:** Replace the sentence with the true one: "`make airgap-toolchain-test` proves the **toolchain** half on a box `e2e-sneakernet`'s cannot represent: `e2e-sneakernet`'s air-gap container is a jump-box image that still has a package manager and a network route (it just doesn't run `make deps`). The toolchain test uses a **genuinely bare** box (`jumpbox/Dockerfile.airgap` — OS packages only) with **`--network none`**, asserts all five tools are **absent first**, then that each one is installed **and executes**, and that `helm` can **render the carried Istio chart offline** — on both Photon and Ubuntu." Fix the same claim at Makefile:507-509 and jumpbox/Dockerfile.airgap:4.

### `docs/sneakernet.md:3-4, 11-16, 31, 33-35, 62`  — docs/sneakernet.md

- **doc claims:** The doc defines "**jump box**" as the box that does NOT reach the internet (line 4, table line 13: "reaches the internet ❌") — and then at line 31 calls the STAGING box's `make deps` "the ordinary **jump-box** bootstrap", and at line 62 tells you to run "`make check-tools` on the **jump box**".
- **reality:** "Jump box" is overloaded across the repo, in the opposite sense: in README.md:26-28 ("The jump box is the only bridge — it pulls from the internet and pushes into the air-gapped cluster") and README.md:52-53 ("**Reachable from the jump box:** the internet, the Supervisor API, Harbor"), the jump box is the DUAL-HOMED box. `00-install-prereqs.sh:36,54` and `03-check-tools.sh:2` ("is this jump box actually able to run the flow?") are written for THAT box. This is not a cosmetic collision: it is the direct cause of finding #1 — the doc tells you to run, on its air-gapped "jump box", a tool written for the dual-homed one, whose remediation is `make deps`. Line 31 shows the doc losing its own thread ("jump-box bootstrap" for the staging box).
- **proof:** README.md:26-28,52-53 (jump box = dual-homed) vs docs/sneakernet.md:13-14 (jump box = no internet); scripts/03-check-tools.sh:2 + scripts/00-install-prereqs.sh:36,54
- **fix:** Pick unambiguous names in THIS doc and use them everywhere: "**staging box** (internet, no Harbor)" and "**inside box** / **air-gap box** (Harbor + cluster, no internet)" — and add one line under the intro table: "Elsewhere in this repo (README, Scenario 1/2) *'jump box'* means the **dual-homed** box that reaches both. In sneakernet there is no such box — that is the whole point." Then fix line 31 ("the ordinary jump-box bootstrap" → "the ordinary `make deps` bootstrap") and line 62 ("`make check-tools` on the **inside box**").

### `docs/sneakernet.md:84, 97, 109-111`  — docs/sneakernet.md

- **doc claims:** "`bundle ready: …tar (**12G**)`" (line 84) vs "this bundle is **~11 GB**" (line 97) vs "before you carry **12 GB** across a room" (line 62). And the Expect strings: "`installed crane -> ~/.local/bin/crane`" and "`✓ mirror-verify: N images intact in Harbor`".
- **reality:** Minor drift, all verified against the code. (a) The size is quoted as 11 GB and 12 GB in three places; the code prints whatever `du -h` says (11-bundle.sh:190,197) — pick one number or say "~11–12 GB". (b) `bundle-load` prints the **expanded** BIN_DIR, e.g. `installed crane -> /home/vks/.local/bin/crane`, not the literal `~/.local/bin/crane` (20-bundle-load.sh:176 uses `${BIN_DIR}`). (c) `mirror-verify`'s line is emitted by `pg_done`, which appends a duration: `✓ mirror-verify: 36 images intact in Harbor — done in 2m14s` (scripts/lib/progress.sh:45-51, scripts/23-mirror-verify.sh:88). Each is close enough to recognise, but an operator matching output literally against the doc will second-guess a correct run. Separately worth noting (code, not doc): the FAT32 warning at 11-bundle.sh:195 lists exFAT in BOTH the "cannot" and the "use instead" lists — the DOC (line 97) is the one that is right.
- **proof:** scripts/11-bundle.sh:190,197 (size from du -h) + scripts/20-bundle-load.sh:176 (expanded ${BIN_DIR}) + scripts/lib/progress.sh:45-51 with scripts/23-mirror-verify.sh:88 ("— done in …") + scripts/11-bundle.sh:195 (exFAT in both lists)
- **fix:** Use one size ("~11 GB") in all three places; render the Expect strings as they actually print (`installed crane -> <BIN_DIR>/crane`, `✓ mirror-verify: N images intact in Harbor — done in …`). Separately fix 11-bundle.sh:195: drop `exFAT` from the "CANNOT be copied to" clause — it has no 4 GiB limit, as the doc correctly says.

### `docs/scenario-1.md:216`  — docs/scenario-1.md

- **doc claims:** "**Expect:** `secrets/harbor-ca.crt` exists **and its SAN covers `HARBOR_URL`**."
- **reality:** Category error. The file `fetch-harbor-ca` writes is the **ISSUER CA**, not the server leaf — `fetch-ca.sh:56` takes `last="$(… | sort | tail -1)"` (the last cert in the chain) and copies THAT to `$OUT` (:75). A CA certificate does not carry the server's SAN; the SAN lives on the LEAF. What the script actually proves is different and better: it runs `openssl verify` of the leaf against what it wrote ("THE PROOF. We do not ship a trust anchor we have not verified is one", :79-80). The doc's Expect line asks the operator to check a property the artifact does not have. (The SAN check that DOES matter — an IP SAN when HARBOR_URL is an IP — is correctly explained at :73-77, on the leaf, in A1.)
- **proof:** scripts/fetch-ca.sh:55-56,75,79-80
- **fix:** Replace :216 with: "**Expect:** `secrets/harbor-ca.crt` exists, and the script prints that it **VERIFIED** Harbor's certificate against it (it refuses to write a trust anchor it has not proven is one). The SAN check is on Harbor's *leaf* cert, not on this CA — see the IP-SAN note in A1."

### `docs/scenario-1.md:314-325 (Step 6) placed BEFORE :334 (Step 7)`  — docs/scenario-1.md

- **doc claims:** Step 6: "`make creds-show` … **Expect:** a table with Harbor, ArgoCD, Gitea, the Tekton dashboard, and one row per app … **It also prints the exact `/etc/hosts` line for the ingress hosts.**"
- **reality:** It does NOT, at Step 6. `creds.sh:166-170` guards the /etc/hosts helper with `if [ -n "${INGRESS_LB_IP:-}" ]`, and `INGRESS_LB_IP` is only published by the ingress install — which is **Step 7**, the step AFTER this one, and which the doc marks "(optional)". So at Step 6 the hint is silently absent, and the app rows' URLs are suppressed too (`creds.sh:38-40` documents exactly this: "nothing serves that host, and no /etc/hosts entry can make it… and printed the URLs anyway" — the bug they fixed). The Expect line describes the POST-Step-7 output.
- **proof:** scripts/creds.sh:166-170 (the guard), :36-40 (why the URLs are hidden when there is no ingress LB), :187 (`ingress_url` per app)
- **fix:** Either move `make creds-show` to after Step 7, or qualify :324: "It prints the `/etc/hosts` line **only once an ingress LoadBalancer exists** — i.e. after Step 7. Before that, the app rows have no URL, which is correct: nothing serves those hosts yet."

### `docs/scenario-1.md:185 vs :189-193`  — docs/scenario-1.md

- **doc claims:** Step 1 **For:** "**four** preconditions that each kill the run later if missing." Three commands follow; the Expect line then says `lab-preflight` "checks **all three**".
- **reality:** Self-contradictory in six lines. `24-lab-preflight.sh` checks exactly three (CRD-create · a DEFAULT StorageClass · a working LoadBalancer provider — Makefile:288, script :88), and the doc's own Expect line (:193) says three. The "four" appears to count `psa-check`, which the very next paragraph (:202-206) explains **cannot measure anything at this point** and is "**not** a pass" — so it is not a precondition this step checks at all.
- **proof:** scripts/24-lab-preflight.sh:88 · Makefile:288
- **fix:** Change :185 to "**three** cluster preconditions that each kill the run later if missing — plus one (PSA) that cannot be measured until Step 5, and says so."

### `docs/scenario-1.md:294`  — docs/scenario-1.md

- **doc claims:** "It replaces `install-all` (**which starts with `mirror`**)"
- **reality:** It starts with `preflight`. `Makefile:446`: `install-all: preflight mirror mirror-verify builder-image vks-login platform gitops`. The doc itself prints the correct chain five lines later (:299: "preflight → mirror → …"). Minor, but it is the sentence that justifies sending the operator away, and it misstates the target it is describing — and per CRITICAL #1 above, `preflight` is precisely where a doc-following operator's `install-all` will actually die, so getting this sentence right matters.
- **proof:** Makefile:446
- **fix:** ":294 → "It replaces `install-all` (whose second step is `mirror`, which needs the internet)."

### `docs/scenario-2.md:83 vs :254-256`  — docs/scenario-2.md

- **doc claims:** Step 3 tells the tenant to set `HARBOR_USERNAME='robot$<name>'` / `HARBOR_PASSWORD=<robot-secret>` in `.env`. Step 4 then says: "`make harbor-robot`  # → secrets/harbor-robot.env (if you hold project-admin) … then copy its two lines … into `.env`".
- **reality:** Ordering chicken-and-egg: `make harbor-robot` authenticates with the CURRENT HARBOR_USERNAME/HARBOR_PASSWORD (22-harbor-robot.sh:26-27) and asks Harbor "am I a sysadmin?" via `GET /users/current` (lib/harbor.sh:81-86) before POSTing a robot. That must be a *user* credential holding project-admin or system-admin — a robot account cannot mint a robot. The doc never says "put your personal Harbor login in `.env` FIRST, run `make harbor-robot`, THEN replace HARBOR_USERNAME/HARBOR_PASSWORD with the robot". A tenant who follows Step 3 literally has already put the robot there.
- **proof:** scripts/22-harbor-robot.sh:26-27,63 · scripts/lib/harbor.sh:81-86
- **fix:** In Step 3 (scenario-2.md:81-95) write: "`HARBOR_USERNAME`/`HARBOR_PASSWORD` = the Harbor login you were granted. If that is a *user* with project-admin and you intend to self-service the robot, put the USER credential here first, run `make harbor-robot` (Step 4), then REPLACE these two lines with the robot's." Cross-reference it from :254.

### `docs/scenario-2.md:71, :348-349, :423`  — docs/scenario-2.md

- **doc claims:** "the flow creates the `gitea` / `ci` / `javawebapp` namespaces" (:71 and again at :423); the port-forward examples only cover `javawebapp` (:349).
- **reality:** Stale after the 1→N app refactor. apps/registry.tsv lists TWO apps (`javawebapp`, `gowebapp`), and 70-configure-argocd.sh:361 creates one PSA-labelled namespace PER APP (`ensure_namespace "$APP_NAMESPACE"` inside `for_each_app`), plus one `harbor-pull` Secret per app namespace (:383-390) and one ArgoCD Application per app. A tenant sizing their cluster-admin grant, their AppProject `spec.destinations`, or their Harbor pull secret off this list will be short by one namespace. (The doc's own line :19 correctly says "per app, the admin must add the app's namespace to `spec.destinations`" — the namespace list contradicts it.)
- **proof:** apps/registry.tsv (2 rows) · scripts/70-configure-argocd.sh:352-390 (per-app namespace + pull secret) · scripts/lib/apps.sh (app_names / for_each_app)
- **fix:** Replace the hardcoded list at :71 and :423 with "`gitea`, `ci`, and ONE NAMESPACE PER APP in `apps/registry.tsv` (today: `javawebapp`, `gowebapp`)", and add the `gowebapp` port-forward line at :349 (or make the example generic: `kubectl -n <app> port-forward svc/<app> 18080:80`).

### `docs/scenario-2.md:23-24, :169, :312-319, :428`  — docs/scenario-2.md

- **doc claims:** "Dual-homed: the jump box reaches both the internet and the lab" (:24); Step 3 says run `make deps` (:169) on it; the fork table (:316-319) routes an internet-only box to "[the sneakernet flow](sneakernet.md)".
- **reality:** TERMINOLOGY HAZARD — "jump box" means the OPPOSITE thing in the doc it hands you off to. In docs/sneakernet.md the two boxes are the **staging box** (internet, no Harbor) and the **jump box** (Harbor + cluster, NO internet) — sneakernet.md:11 — and sneakernet.md:35 states outright "`make deps` **cannot run here** — it downloads. The jump box is provisioned by hand." Scenario-2's "jump box" is the dual-homed one, on which `make deps` is exactly right. A tenant carrying scenario-2's vocabulary into sneakernet.md will try to `make deps` on the air-gapped box. Additionally the fork table has only two rows (dual-homed / internet-only) and no row for the tenant whose box reaches Harbor + the cluster but NOT the internet — which is the sneakernet *destination* box, and the most common locked-down-tenant shape.
- **proof:** docs/sneakernet.md:11,31,33-35,125 (jump box = the AIR-GAPPED box; `make deps` cannot run there) · docs/scenario-2.md:24,169 (jump box = the DUAL-HOMED box; run `make deps` there)
- **fix:** Pick ONE noun per box, repo-wide. Suggest: **staging box** (internet) and **install box** (Harbor + cluster), with "jump box" reserved for the dual-homed single-box case — and say so in one line at the top of both docs. Add the missing third row to the fork table at :316-319: "reaches Harbor + the cluster but NOT the internet → you are the sneakernet *inside* box; you also need a second box with internet."

### `docs/scenario-2.md:335-343`  — docs/scenario-2.md

- **doc claims:** The cross-cluster note tells the tenant only: "Do **not** set `ARGOCD_KUBECONFIG` … Instead **request** that the platform team register your guest cluster … then set **`ARGOCD_DEST_SERVER`**."
- **reality:** Incomplete: the code ships a knob designed for exactly this tenant that the runbook never mentions — `ARGOCD_REGISTER=auto|never|force` (71-argocd-register-guest.sh:41-51: "`never` — a TENANT: registration is someone else's job. Skip, quietly."; .env.example:390-394). The doc's "don't set ARGOCD_KUBECONFIG" works only by ACCIDENT: with it unset, `argocd_is_off_cluster` compares the guest kubeconfig with itself, decides ArgoCD is in-cluster, and skips registration (71:53-57). A tenant who legitimately obtains a Supervisor kubeconfig for read purposes would then trip the admin-only registration path.
- **proof:** scripts/71-argocd-register-guest.sh:41-51,53-57,69-76 · .env.example:390-394
- **fix:** In the cross-cluster note (:335-343) add: "Set `ARGOCD_REGISTER=never` in `.env` — that is the tenant setting; registration is the platform team's job. (Leaving `ARGOCD_KUBECONFIG` unset also skips it, but only because ArgoCD then *looks* in-cluster.)"

### `docs/scenario-2.md:85, :141, :244, :269, :297`  — docs/scenario-2.md

- **doc claims:** "`HARBOR_CA_FILE=./secrets/harbor-ca.crt  # fetched in Step 2 below`" (:85); "Point HARBOR_INFRA_PROJECT / HARBOR_APP_PROJECT at the project(s) you were **granted** (Step 2)" (:244).
- **reality:** "Step 2" names TWO different things in one document. The first section numbers its items 1/2/3 ("**2 — Request grants**", :40); the second section restarts at "Step 0 … Step 7" ("**Step 2 — save the Harbor CA certificate**", :141). So :85's "Step 2" means the CA step while :244's "Step 2" means the grants step. Same collision affects "Step 4"/"Step 5"/"Step 6" cross-references.
- **proof:** docs/scenario-2.md:40 (section-1 item 2) vs :141 (section-2 Step 2) — the doc is its own evidence
- **fix:** Renumber the discovery section's items as "Step 1 / Step 2 / Step 3" continuing into the install steps (so there is ONE sequence), or label them distinctly ("Discovery A/B/C"), and fix the three cross-references at :85, :244, :271.

### `docs/scenario-2.md:88, :246-250`  — docs/scenario-2.md

- **doc claims:** "`HARBOR_PUBLIC_PROJECTS=false`  # tenant projects are typically private (see Step 4)" and "Because a tenant project is typically **private** (`HARBOR_PUBLIC_PROJECTS=false`), the workload's kubelet cannot pull the app image anonymously — so `make gitops` creates the image-pull secret (`harbor-pull`)…"
- **reality:** Misleading causation. `HARBOR_PUBLIC_PROJECTS` is read in exactly two places: as the `public:` field when CREATING a project (lib/harbor.sh:98) and as a value-format guard (21-mirror-push.sh:55). For a tenant whose projects ALREADY EXIST it is a **no-op** — it cannot make an existing project private or public. And the `harbor-pull` Secret is created whenever HARBOR_USERNAME **and** HARBOR_PASSWORD are set (70-configure-argocd.sh:372-390), regardless of this flag. So setting it changes nothing for a tenant, and a reader may conclude that NOT setting it would skip the pull secret (it would not).
- **proof:** scripts/lib/harbor.sh:98 · scripts/21-mirror-push.sh:55 · scripts/70-configure-argocd.sh:372-390 (pull secret gated on the CREDENTIALS, not on this flag)
- **fix:** At :88 mark it "(no-op when the project already exists — it only sets `public:` at project-CREATION time)", and at :246 rephrase: "`make gitops` creates the `harbor-pull` Secret in every app namespace whenever `HARBOR_USERNAME`/`HARBOR_PASSWORD` are set — which is what makes a PRIVATE tenant project work."

### `docs/scenario-2.md:154`  — docs/scenario-2.md

- **doc claims:** "(Or download it from the Harbor UI → your project → **Registry Certificate**.)"
- **reality:** Likely a dead end on the lab, and it is UNVERIFIED. Harbor's registry-certificate download is backed by `/api/v2.0/systeminfo/getcert`, which returns "No certificate found" whenever Harbor does not hold a CA at its core path — the normal case when TLS is terminated by an ingress / cert-manager, which is precisely the Scenario-2 shared-lab shape (goharbor/harbor#6603, #18912; recorded as settled in this repo's CLAUDE.md). The doc offers it as an equal alternative to `make fetch-harbor-ca` with no caveat.
- **proof:** CLAUDE.md ("ADVERSARY FINDINGS 2026-07-12" — the getcert endpoint exists but returns 'No certificate found' when Harbor holds no CA at its core path) · scripts/fetch-ca.sh (the supported path: read the issuer off the wire and VERIFY it)
- **fix:** Demote the UI tip to: "(Harbor's project page may offer a **Registry Certificate** download — but it is empty on a Harbor whose TLS is terminated by an ingress/cert-manager, which is the usual shared-lab shape. `make fetch-harbor-ca` is the reliable path; if neither works, ask the platform team for the issuing CA PEM.)"
