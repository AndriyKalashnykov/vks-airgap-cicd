apiVersion: v1
kind: Service
metadata:
  name: webui
  labels:
    app.kubernetes.io/name: webui
    app.kubernetes.io/part-of: vks-airgap-cicd-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: webui
  ports:
    - name: http
      port: 80
      targetPort: http
