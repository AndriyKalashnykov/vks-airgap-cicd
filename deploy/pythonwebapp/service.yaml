apiVersion: v1
kind: Service
metadata:
  name: pythonwebapp
  labels:
    app.kubernetes.io/name: pythonwebapp
    app.kubernetes.io/part-of: vks-airgap-cicd-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pythonwebapp
  ports:
    - name: http
      port: 80
      targetPort: http
