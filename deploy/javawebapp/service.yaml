apiVersion: v1
kind: Service
metadata:
  name: javawebapp
  labels:
    app.kubernetes.io/name: javawebapp
    app.kubernetes.io/part-of: vks-airgap-cicd-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: javawebapp
  ports:
    - name: http
      port: 80
      targetPort: http
