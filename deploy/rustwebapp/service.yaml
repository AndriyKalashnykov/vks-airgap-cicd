apiVersion: v1
kind: Service
metadata:
  name: rustwebapp
  labels:
    app.kubernetes.io/name: rustwebapp
    app.kubernetes.io/part-of: vks-airgap-cicd-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rustwebapp
  ports:
    - name: http
      port: 80
      targetPort: http
