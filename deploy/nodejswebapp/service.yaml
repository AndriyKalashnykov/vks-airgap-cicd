apiVersion: v1
kind: Service
metadata:
  name: nodejswebapp
  labels:
    app.kubernetes.io/name: nodejswebapp
    app.kubernetes.io/part-of: vks-airgap-cicd-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nodejswebapp
  ports:
    - name: http
      port: 80
      targetPort: http
