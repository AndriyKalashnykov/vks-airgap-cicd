# Backlog — vks-airgap-cicd

> **Moved out of `CLAUDE.md` on 2026-07-22, and this file is deliberately NOT auto-loaded.**
>
> `CLAUDE.md` is injected into every session in this repo **and into every subagent** — measured:
> one `Read` in a subagent re-injected it at **141,071 B ≈ 48.6k tokens**, on top of the ~311k-token
> always-loaded rules corpus. This backlog was **85,504 B — 61% of that file** — and it is *task
> state*, not instructions: nothing here changes how an agent approaches a problem, which is the
> test for what belongs in `CLAUDE.md`.
>
> `CLAUDE.md`'s own handoff rule already says **"Facts → the docs. Tasks → the Backlog."** This
> makes that literal.
>
> **Read this file when you pick up work** — a handful of `scripts/*.sh` cite row IDs (`B22`, `B26`,
> `B42`, `B45`, `B50`) as provenance for why a given gate exists, and those citations resolve here:
> most as open rows, and `B42` as a *closed* one recorded in the session-3 note below. A citation
> that lands on a closed row is still resolved — it tells you the gate's reason shipped.

## Open rows

Every item below is the OPEN set, re-verified against the tree — most recently on **2026-07-17
(session 3)**, when **B29, F8, B41, B44, B23, B30, B34 and B42 were REMOVED** because they
shipped/resolved (#295–#297 + #304 + the B42 fix; evidence in each PR + the code), and **B26, B33 were
COLLAPSED** to their open residuals (their done parts dropped). The history that produced these is in git and `docs/reviews/`;

**Pruned 2026-08-13:** 21 rows whose work had SHIPPED were removed —
`B3`, `B13`, `B18`, `B19`, `B22`, `B27`, `B28`, `B45`, `B47`, `B48`, `B49`, `B55`, `B56`, `B58`, `B59`, `B71`, `B73`, `B74`, `B79`, `B80`, `B85`.
Each cited its own PR; full text is in git history (`git log -p -- BACKLOG.md`) and `docs/reviews/`.

**Deliberately KEPT, and this is the load-bearing half of the prune:**

- every 🔵 REFUTED / ⛔ DO-NOT-BUILD row — their value is stopping someone rebuilding a design
  that was already refuted, so they are not "closed work";
- seven rows marked ✅ that a CONTENT review showed are not actually finished. A marker scan
  would have deleted them: **B20** carries *"Still open, lab-gated"*, **B50** carries *"BUILD
  THIS INSTEAD (not yet done)"*, **B46** is *"DECLINED ON EVIDENCE — do not re-open"*, and
  B17/B40/B52/B65 quote open-language that is history rather than residual. Prune this file by
  READING rows, never by grepping their status emoji.
carrying a closed item forward is the class this file was pruned to end.

**Code — no lab needed:**

| ID | Item |
|----|------|
| **B64** | 🔴 **§2's "browser work — not scriptable" is a claim about THIS REPO presented as a claim about the PLATFORM — and the obvious port to fix it was REFUTED (idea-round, `vks-adversary`, 2026-08-05).** The source repo (`nested-vsphere-lab`) installs Supervisor Services entirely over vCenter REST, so the doc claim is false. But the proposed ~1,700-line port (a `lib/vcenter.sh` shim + `service-catalog.sh` + an `svc_main` equivalent, phased ArgoCD-instance-first) was refuted on 12 findings, 3 CRITICAL. **(1)** Blanking `_SS_ROWS` field 6 (`dns-label`) does not drop DNS — it feeds `svc_dns_fqdn`, so it kills `_ss_render_harbor_values` AND `harbor_set_admin_password`, dying with an error naming `.lab.domain`, **a config key this repo does not have**. Blank field **5 only**. **(2)** The install POST/PUT need `_SS_MOID`, which comes from `_cluster_moid` in the source's `steps/40-supervisor.sh` (not `lib/govc.sh`), plus an entire vCenter credential surface this repo lacks — `VCSA_IP`/`VCSA_USER`/`VCSA_SSO_PASSWORD` (**SSO administrator**, not the tenant identity Scenario 2 assumes), `CLUSTER_NAME`, `DOMAIN` (we have `APP_DOMAIN`), `LAB_STATE_DIR`. ~10–15 new `.env.example` keys, each owing a graded `# how:`. **(3) SECURITY:** with no VMCA anchor producer here, `_vc_tls` falls back to `-k`, so the **SSO admin password crosses an unverified TLS connection** on every first run, signalled only by two WARNING lines in a 20-minute build. The source's warn-and-continue exists for a bootstrap ordering (the VCSA does not exist yet) that **does not apply here** — vCenter is pre-existing customer infrastructure, so this repo must **fail closed**. **PHASING WAS BACKWARDS:** the "kubectl-only" ArgoCD-instance phase depends on the vCenter phase in four places, incl. a `die` telling the operator to go do the browser flow — the very claim being refuted. **COUNTER-PROPOSAL (adopt):** ONE script, four calls — `checkContent` → `register` (`carvel_spec`) → `install` (POST/PUT) → poll `config_status` — parameterised on (service-yaml, data-values, cluster). No catalog, no DNS, no instance CR, no teardown verbs (~800 lines serving a story this repo does not have). ~350–450 lines, it IS the §2 rewrite, and it keeps the vCenter-admin blast radius small enough to RED-prove. **Also carry:** the expose-key precondition verbatim (`yq -e 'has("enableNginxLoadBalancer") and has("enableContourHttpProxy")'` — `yq '.k = v'` CREATES missing keys, so without it a vendor rename ships a Harbor with no reachable ingress, green all the way down) — but note it has a **multi-document hole** (`has()` passes if ANY doc satisfies it; measure `select(di == 0)` against the real licensed artifact first). **Gates it will trip:** `check-grep-q-pipe` (the ported `printf \| grep -qxF` is the SIGPIPE-under-pipefail class), `check-env-coverage`, `check-how-provenance`, `check-env-clobber` (new selectors must stay COMMENTED), `check-doc-target-coverage`. **Doc honesty:** §2 is scripted through step 4 and **manual at step 5** — customer DNS is unscriptable from here, and claiming otherwise just mirrors the error being fixed. **OWNER DECISION NEEDED before writing:** does vCenter **SSO admin** belong in a repo whose Scenario-2 persona is explicitly a tenant? **Cheap unblock:** one `openssl s_client -connect $VCSA:443 -showcerts` against a real lab settles whether `./secrets/supervisor-ca.crt` already anchors vCenter too, which would delete a whole fetch path.  **SECOND ROUND (2026-08-05, `adversary-bash-git-cli`, Step-0 instrument PERMITTED — findings MEASURED): verdict SPLIT — the `checkContent`-only slice is CLEARED as not-worth-building, but on a stronger argument than the one I gave, and the DELIVERABLE was REFUTED.** **(F1, the decisive argument — lead with it)** `checkContent` is call 1 of 4 and NON-MUTATING: it cannot register, cannot install, and cannot move §2 off the browser. The operator gains a script demanding a new vCenter endpoint, a session handshake and a TLS anchor, and then still performs all six §2 steps by hand — the full credential surface of the four-call script for ZERO capability. "It validates the wrong file" invites "then also add the data-values check"; "call 1 of 4 delivers no capability" is unanswerable. **(F2, CRITICAL)** A doc correction MUST NOT close this row. The recorded owner action (`docs/reviews/2026-08-04-first-real-lab-measurements.md:56`) is verbatim *"port the capability, then rewrite §2/§3 scripted-first … This is a FEATURE plus a doc rewrite, not a doc edit."* Closing B64 on a doc edit also orphans **B78**, which is blocked on it, and reads six weeks later as "B64 was handled". Ship any §2 correction explicitly labelled **INTERIM**; B64 stays 🔴. **(F3)** This row's own "no VMCA anchor producer" claim is **CORRECT — do not "correct" it.** MEASURED: `command grep -rn 'supervisor-ca.crt' Makefile scripts/` returns ONE hit, a COMMENT, and `scripts/lib/tls.sh:87` says verbatim *"Nothing in the repo re-fetches it"*; `.env.example`'s `VKS_CA_CERT_FILE` is COMMENTED, i.e. a path, not a producer. A proposed edit to soften it was refuted. **This is also the strongest argument FOR refusing the slice:** a fail-closed `checkContent` at §2 hard-blocks the FIRST scripted step on an artifact the docs never tell anyone how to obtain, and the obvious bootstrap (fetch the VMCA over the connection you are verifying) is TOFU. **(F4)** The COST objection is OVERSTATED — drop it. MEASURED as already present: `VKS_USERNAME` (documented as `administrator@vsphere.local`, which IS the vCenter SSO principal), `VKS_SSO_DOMAIN`, `VCF_CLI_VSPHERE_PASSWORD`, `SUPERVISOR_HOST`, `VKS_CA_CERT_FILE`/`VKS_CA_SHA256`, and a RED-proven fail-closed `ca_verifies_endpoint`. Genuinely new is the **vCenter endpoint** (`SUPERVISOR_HOST` is the Supervisor VIP, not vCenter), and `check_content` needs only `System.Read`. The "~10–15 new keys" figure belongs to the four-call script, not this slice. **(F5, UNMEASURED)** a candidate consumer exists at `docs/scenario-1.md:259` and was never checked. |
| **B60** | **A THIRD box vocabulary — "outside box" / "inside box" — for the same two boxes, in 8 script comments (owner call, no lab).** B59 retired "staging box"/inverted-"jump box" and standardized the docs on **internet box** / **air-gap box**; but `scripts/11-bundle.sh:34,35,52`, `14-builder-build.sh:12`, `15-build-push-builder.sh:10,11`, `e2e-sneakernet.sh:18,19` still say "outside box" (= internet box) / "inside box" (= air-gap box) — a self-consistent spatial framing, but a THIRD term for the same pair. Surfaced by the B59 straggler-sweep adversary (2026-07-23); deliberately NOT folded into that sweep (outside B59's scope, comment-only). **OWNER CALL:** unify those comments to "internet box"/"air-gap box" (a guarded `scripts/` change, no lab), or keep "outside/inside" as an accepted in-code spatial framing. |
| **B61** | **A MEASURED-FALSE fact sits in the ALWAYS-LOADED rules corpus — `claude-config:rules/common/git-workflow.md:377-379` (other repo, but it loads into EVERY session here).** It claims *"`$(...)` capture is also not word-split in zsh"*. **Measured FALSE** (zsh 5.9, `zsh -f`, `SH_WORD_SPLIT` unset): `for x in $(printf 'a\nb\nc\n')` → **3** iterations — command substitution DOES split; `v=$(...); for x in $v` → **1** — only *variable* expansion does not. So the paragraph carries the correct rule at `:383-385` and its own contradiction five lines above it, in a file injected into every session in every repo **and every subagent**. The same lines also hold ~29 B of **mangled duplication**: the phrase *"capture is also not"* appears twice, split across an unrelated PIPESTATUS sentence, so the sentence is unparseable. Introduced by an auto-sync **cron** commit (`1dc33a3`, 2026-07-09) plus `716abaf` — **not** by the 2026-07-23 session; but that session edited this exact paragraph under ratchet pressure and shipped it, which is the *"a FACT inside a CONTROL is part of the control"* class. **Minimal fix:** delete the duplicated fragment and correct to *"unquoted **variable** expansion is not word-split in zsh; **command substitution IS**"*. Removing a false fact is a **correction** — the ratchet docstring names that always-legitimate — and it SHRINKS the corpus, so it also partly repays the 627 B raise of 2026-07-23. Found by the session-end `vks-adversary`, measured with a real shell. |
| **B62** | **`resolve_archive`'s `vcf` glob is arch-BLIND while the operator's folder holds BOTH arches — and its stated rationale is false.** `scripts/01-install-vcf-clis.sh:63` uses `VCF-Consumption-CLI-*${VCF_CLI_VERSION}*.tar.gz`, justified at `:64-67` as letting *"the arch-agnostic `…-Binaries-…` bundle"* match. **Measured 2026-07-23 against the operator's real 9.1.0.0400 portal folder: there is NO `-Binaries-` bundle for either the CLI or the plugins** — Broadcom ships `Linux_AMD64` + `Linux_ARM64` per-arch for both. So on an arm64 box the arch-blind glob can `-print -quit`-pick the AMD64 CLI. It **fails safe** (the `vcf-cli-${os}_${go_arch}` content assertion at `:134` dies), but with a confusing error, and the comment's premise is simply wrong. ⛔ **Do NOT "fix" this by loosening the PLUGINS glob or deleting the nested handler.** A session-end adversary graded exactly that **HIGH** — *"the arch-bound plugins glob at `:68` regresses a multi-arch bundle that `:152-155` proves exists"* — and the premise is **REFUTED by measurement**: the real per-arch bundle nests under `<plugin>/<version>/` (`cluster/v3.6.1/vcf-cluster-linux_amd64`), **not** `<os>/<arch>/`, so `find -path "*/${os}/${go_arch}"` never fires for it and its existence proves nothing about an arch-agnostic bundle. The adversary's own residual conceded the `-Binaries-` plugin name was *"my hypothesis, not a Broadcom fact"* — it had tested a fixture shaped like its own hypothesis. **Minimal fix:** correct the `:64-67` comment to measured reality; optionally arch-bind the `vcf` glob **with** an arch-agnostic fallback so a future `-Binaries-` bundle still resolves; and add the missing plugins case to `scripts/test-vcf-cli-resolve.sh` (it has a nested-bundle case for **vcf** at `:94-105` and **none** for plugins — the 9/9 green is a subset green). |
| **B63** | **The `CLAUDE.md` HANDOFF section states a `main` SHA, which its own banner forbids.** `CLAUDE.md:540-541` says *"NO BUILD-STATUS CLAIMS … a task status is true when written and falsified by the next commit"*; `:543` then says *"State: `main` at `<sha>`"* — which the handoff's OWN merge falsifies, structurally and every time. Same mechanism `hooks.md` records for PR numbers: a squash merge assigns the SHA at merge time, so a document can never cite the commit that contains it. Measured 2026-07-23: the handoff said `c8ae5ac`, `main` was `62cd61c`, `git rev-list --count c8ae5ac..main` = 1. **Minimal fix:** drop the SHA — *"State: `main`, clean, nothing in flight — query `git rev-parse HEAD`"*. Found by the session-end `vks-adversary`. |
| **B50** | ✅ **DONE 2026-07-19 (#349) — the blocking measurement was taken and the check is built, reviewed and shipped as `make verify-ingress-rendered`.** MEASURED on a live KinD cluster with the app backends deliberately absent (install-ingress run, `gitops` NOT run): **`nosuch.vks.local` → 404**, **`javawebapp`/`gowebapp` → 503**, **`gitea`/`tekton` → 200**. So the 404-vs-503 split is REAL, the `inferred` grade is retired, and assertion (b) does NOT collapse to "not 000". A 503 proves Envoy matched a RENDERED route and found no endpoints; a 404 would mean the carried `yq`/`envsubst`/`kubectl` never rendered the Gateway host + VirtualService — a path nothing else in the repo exercises. `98-verify-ingress.sh` is UNTOUCHED, as the refuted-skip analysis below demanded. An implementation-round adversary then found **five** defects, each re-verified and fixed: the split is measured on istio ONLY so the gate now **dies on an unmeasured controller** rather than asserting it (traefik renders per-app routes but a missing backend Service makes it 404, INVERTING the meaning); the app hosts had **no poll** while `lib/istio.sh` applies the app VirtualServices at `:604` AFTER the infra ones at `:599`, so the infra poll never covered them (K1.5 race → flaky RED blaming the carried toolchain); `route_code` returned **`000000`** because `curl -w '%{http_code}'` already prints `000` AND exits non-zero, so the `\|\| echo "000"` appended a second one and the best diagnostic branch was **dead code** (⚠️ **the same bug is inherited from `98-verify-ingress.sh:56-59`**, where it also degrades the `000\|50[234]` retry arm — worth fixing there); a deployed-but-CRASHLOOPING backend would pass as "absent" (istio 503s identically for missing-Service, zero-endpoints and all-unhealthy) so that is now a STATED limit; and the `infra_checked` guard **could never fire** over a literal 2-element list while the comment claimed both counts fall independently — deleted, because a guard that cannot fire reads as a guarantee it does not give. ⚠️ **My own RED-proof for the controller guard silently PASSED first time**: `INGRESS_CONTROLLER=traefik ./97…` does not fire it, because `load_env` sources `.env.state` AFTER the environment so the PUBLISHED value wins (the state-overlay clobber). That is correct for this gate — it must check what was INSTALLED, not what the caller claims — but the proof has to rewrite the published value, and the working form is recorded in the script. The refuted-skip analysis is kept below because it is why the design is additive: 🔵 **THE SKIP DESIGN IS REFUTED (idea-round 2026-07-18, two measured CRITICALs) — but the round produced a BETTER, cheaper replacement that is NOT yet built.** ⛔ **Do NOT make `98-verify-ingress.sh` skip hosts whose backend is absent.** (1) A kubectl predicate **cannot distinguish "backend absent" from "I could not ask"** — measured `rc=1` alike for NotFound, an unreachable API server, AND a bad kubeconfig (which also silently falls back to `http://localhost:8080`, this repo's own C13/B32 hazard). A stale `KUBECONFIG` would make ALL FOUR hosts "absent" → all skip → the gate prints success having checked nothing; on a real lab a tenant lacking get-deployment rights makes Forbidden indistinguishable from NotFound. And it would convert a gate that today needs **only curl** (`:26`) into one requiring cluster API access. (2) **The objects a predicate would key on are created by `install-ingress` ITSELF** — `lib/istio.sh:284,572` `ensure_namespace` per app and `:611` applies the per-app VirtualService unconditionally from the registry — so the namespace and VS ALWAYS exist and discriminate nothing; only Service/Deployment differ, and **Endpoints — the intuitive choice — is wrong in 4 of 6 states because "no endpoints" IS the failure this gate exists to catch**. No workload-keyed predicate gets the ArgoCD-sync-FAILED row right (it looks identical to never-deployed). Also: "fail if ALL were skipped" is theatre here (the air-gap steady state is 2 checked / 2 skipped, so it never fires); the skip would need threading through TWO loops (`:79` route and `:114-119` body); and it would **permanently vacuum two working callers** — `verify-ingress-both` (`Makefile:499-500`) and `e2e-kind-istio-existing` leg 2 (`:522`) run verify-ingress with NO preceding `verify`, so they FAIL loudly and correctly today. 🟢 **BUILD THIS INSTEAD (not yet done): the 503 is EVIDENCE, not an embarrassment.** 503 = Envoy matched a route and resolved a cluster with no healthy endpoints; 404/000 = the route was never configured (this repo says so itself at `98-verify-ingress.sh:140`). So an **ADDITIVE** check in the air-gap leg — leaving `98-verify-ingress.sh` COMPLETELY untouched — asserting gitea+tekton are 2xx/3xx with their body markers AND each app host is **NOT 404, NOT 000, NOT 2xx** proves the carried `yq` appended the app hosts to the Gateway (`lib/istio.sh:588`) and the carried `envsubst`+`kubectl` rendered the per-app VirtualServices (`:611`) — **the carried-toolchain RENDERING path, which nothing currently checks**. Strictly more coverage than the skip, zero blast radius, no kubectl, and it is NOT the rejected host-subset knob because it is a separate check with its own fixed scope that cannot shrink the real gate. ⚠️ **SETTLE ONE THING FIRST — the 404-vs-503 split is graded `inferred`:** on the next air-gap run, `curl` a host with NO VirtualService (e.g. `nosuch.vks.local`) and confirm it is 404/000 while `javawebapp` is 503. If BOTH come back 503, assertion (b) collapses to "not 000" and is much weaker. Also pin the controller: the leg installs istio, and traefik's no-backend status may differ. |
| **B15** | 🔴 **DISPROVED — do NOT back-port the 47-attach re-resolve into `98-verify-ingress.sh` (vks-adversary idea-round, 2026-07-17 s3; correction now lives in the code comment near `98-verify-ingress.sh:27`).** `98-verify` is a **PURE CONSUMER** of `INGRESS_LB_IP` — it never `state_set`s it (grep-confirmed), so it structurally cannot reproduce the 47 resolve-then-publish-stale false-green. Every caller (`Makefile:421` e2e-kind, `:493-494` verify-ingress-both, `:513/516` istio-existing, `docs/lab-validation-plan.md:561→565`) runs `install-ingress` immediately before `verify-ingress`, republishing the value for the current mode → never stale in any orchestrated/documented path. A stale STANDALONE read is the SAFE direction (a dead IP → loud route FAIL, self-diagnosed at `~98:112`; a false-green would need the "stale" IP to actually route the current hosts to the current backends serving the right markers, i.e. not stale), and the real-lab standalone case is further guarded by `state_check`'s cluster stamp. Back-porting the 47 pattern would DUPLICATE installer logic and depend on `INGRESS_CONTROLLER`, which has the identical read-back class — no net gain. Shipped: the anti-"fix" comment + a published-not-live hint in the failure diagnostic. |
| **B17** | ✅ **DONE 2026-07-18 (#320/#322/#323/#325).** 🔵 **RE-SCOPED — as written this row is STALE, and its own audit's "Still open" paragraph is a CLAIM that no longer holds.** Re-verified 39 of the audit's 68 findings against the current tree (all 29 in the named scope): **25 of 29 are FIXED**, 2 were moot. `prerequisites-manual.md` (6/6) and `scenario-2.md` (13/13) are **DONE**. The "CLAUDE.md gate-list drift" half is structurally dead for `static-check` (that row now points at the Makefile as authoritative) and the two rows that HAD rotted — `sec` omitting `check-secrets-untracked`, `mirror` omitting `mirror-verify` — landed in **#320**. **So the named scope is CLOSED.** What survives is a *different*, still-real set of **nine rows found OUTSIDE that scope**, listed in the audit; the highest-value is `docs/scenario-1.md:419` claiming the Istio helm chart still needs internet, which the carried-charts branch in `46-install-istio.sh` and `docs/sneakernet.md:115` both refute — it states a limitation that no longer exists, in the direction a reader will believe. Others: `file(1)` used by `11-bundle.sh:149` but neither installed by `00-install-prereqs.sh` nor gated by `03-check-tools.sh` (HIGH); the scenario-1 SAN `Expect:` line; "install-all starts with mirror" (it starts with `preflight`); the four-vs-three preconditions count; the sneakernet `make deps` rationale; a 12 GB/11 GB bundle-size drift; exFAT listed in both the cannot-use and use-this lists. **All nine landed 2026-07-18** across #322 (the Photon toybox false-die), #323 (six doc rows, two of which an adversary refuted before I wrote them) and #325 (the false rationale inside `test-airgap-toolchain.sh`). **B17 is CLOSED.** |
| **B24** | 🟢 **TWO OF THE THREE GAPS CLOSED 2026-07-19 from primary sources; only the PSA one is genuinely lab-gated.** **(1) `-n tkg-system` vs `-n istio-installed` — HALF of this was a confusion and half is a REAL variant; an adversary round on 2026-07-19 caught me overstating it, so the corrected form is:** on Broadcom's page they are two flags on **two different commands** — **discovery** `vcf package available get istio.kubernetes.vmware.com **-n tkg-system**` (the repo lands in `tkg-system`, a *global* kapp-controller namespace) vs the **install target** `vcf package install istio -p … **-n istio-installed**`. So seeing `tkg-system` in a *discovery* command is **not** evidence of an install-namespace variant, and using `istio-installed` for discovery is simply wrong. ⚠️ **But the INSTALL namespace itself IS a genuine variant, and my first pass wrongly said it was not**: both community walkthroughs run `vcf package install istio … **-n tkg-system**` (quoted verbatim in `docs/vks-services/istio.md`'s "Install namespace variant" row), and `-n` on `vcf package install` simply selects where the PackageInstall CR lands — `tkg-system` is a real namespace, so that install works. **`docs/vks-services/istio.md:249` is therefore CORRECT as written and must NOT be "fixed"** — an adversary prescribed deleting it on the strength of my overstatement, which would have replaced a right row with a wrong one. Keep `istio-installed` (better-graded, `9.0-doc`) as ours; record `tkg-system` as a community variant, not an error. Also newly `9.0-doc`: the package needs **VKr 1.29 and later**. **(2) Multi-primary multi-cluster went from ZERO coverage to `9.0-doc`** — Broadcom ships a dedicated *Configure Istio multi-clusters* page. Graded facts now in the adversary + the private reference: the floor is `1.28.2+vmware.1-vks.1` *"including 1.28.1+vmware.1-vks.1"* (upgraded from `community`, and still the **PACKAGE's** floor not Istio's); **both** topologies are supported (*"Primary-remote on different networks"* and *"Multi-primary on different networks"*); **sidecar mode ONLY**, because *"the upstream ambient multi-cluster feature has not yet reached GA"* — so flag any ambient+multi-cluster design on sight; the schema is `meshConfig.{meshID,network,multiCluster.{enabled,clusterName,clusterProfile,primaryClusterNames,remotePilotAddress}}` + `externalIstiod: true` on primaries; the east-west gateway is **REQUIRED** in the primary but **its creation method is NOT STATED** (Broadcom defers to upstream — precisely where the two air-gap traps bite: upstream `istioctl` pulls `docker.io/istio/proxyv2` instead of Harbor, and a Layer-7 LB breaks `AUTO_PASSTHROUGH`); and inter-cluster **network reachability is NOT STATED**, so it stays an unknown rather than an assumed prerequisite. ✅ **This RESOLVED a flagged `[UNVERIFIED-BY-US]`: `clusterProfile` EXISTS** (`primary`\|`remote`) — the community walkthrough was right and our doubt was the error, recorded that way round per the rule that a retraction needs the same evidence as the thing it retracts. ⚠️ **PARTIAL, and the open half is the one that matters: `cacerts`.** Broadcom confirms the Secret **name**, its **namespace** (root / `istio-system`) and that it is needed in **each PRIMARY** cluster — but **NOT its data keys**, so the cert-manager (`tls.crt`/`tls.key`) vs upstream (`ca-cert.pem`/`ca-key.pem`/`root-cert.pem`/`cert-chain.pem`) key-shape question is neither confirmed nor refuted and that section stays RED. Do **not** let "Broadcom documents `cacerts`" be read as "the cert-manager recipe works". **Still lab-only:** the gateway-namespace PSA minimum (`make psa-check`), and reading the actual `cacerts` keys off a real cluster. The **manifests and the air-gap delta live in `~/projects/claude-config/reference/istio-on-vks.md`** (private — this repo is public, and unrun manifests do not belong in a provenance-graded record). Of the **three** things flagged **UNVERIFIED-BY-US** there, **one is now RESOLVED** — `clusterProfile` exists in the VKS package schema — and **two remain, each settled by one lab command**: whether cert-manager's `cacerts` (`tls.crt`/`tls.key`/`ca.crt`) is readable by istiod at all, given upstream documents `ca-cert.pem`/`ca-key.pem`/`root-cert.pem`/`cert-chain.pem` (`kubectl get secret cacerts -n istio-system -o jsonpath='{.data}'` names the keys); and the gateway-ns PSA minimum (`make psa-check`). |
| **B25** | DONE 2026-07-19/20 — both halves, and the audit refuted the premise I gave it. REVIEW half (#371): every claim in docs/vks-services/istio.md was re-judged, and it measured that B24 never landed in that file at all (its diff is CLAUDE.md, one line) — so the doc had not rotted, the update never arrived. Seven deltas fixed, including a bullet still declaring multi-cluster UNVERIFIED that B24 had already falsified, a community-graded row split so each grade matches its source, and a claim that discovery is skipped entirely which was measured FALSE. scenario-1.md reviewed separately: no deltas — it already carried B24's namespace distinction correctly. BUILD half: the ranked candidate shipped as 96-verify-gateway-image (#369). Candidates 1 and 3 were REFUTED — the server-header assertion is redundant for controller identity because each controller gets its own LB IP (measured: istio 172.18.0.6, traefik 172.18.0.7), and mTLS-via-istioctl is vacuous in a sidecar-free repo that deliberately ships no istioctl. Candidate 2 was downgraded to an optional diagnostic: a 200 plus a body marker already proves a route is programmed more strongly than a config dump does. Residual: the URL-liveness audit found 13 of 15 cited URLs resolve; the two Medium 403s are recorded as cannot-distinguish-blocked-from-removed, NOT as link rot, with the verbatim quote in each src token named as the durable evidence. ORIGINAL ASK, for context: **Validate Scenario 1's Istio material end-to-end with the enriched `vks-adversary` — and land it as EXECUTABLE AUTOMATION, not prose.** Every Istio claim in `scenario-1.md` + `istio.md` + the install/attach scripts gets re-judged against the enriched brief and `~/projects/claude-config/reference/istio-on-vks.md`. **The deliverable is code**: each verifiable claim becomes an assertion in `make istio-preflight` / `verify-ingress` / a new gate — not a paragraph. Concrete candidates already identified: assert `server: istio-envoy` on the response (the PATH, not just the body marker); assert the route **PROGRAMMED** into the proxy (`istioctl proxy-config routes …`), not merely `Accepted`; report mTLS mode from `istioctl x describe pod`; assert the mirrored-image alignment for any istioctl-rendered image. Anything that cannot become an assertion goes to `lab-validation-plan.md` as a numbered step with its command, its expected observable, and what to send back — never as a doc sentence. |
| **B26** | 🔴 **Fixes (1)(2)(4) SHIPPED (#295/#296); this row is now fix (3) ONLY.** The pod templates carry `sidecar.istio.io/inject: "false"` (all 4, `check-pod-inject-label` green); `ensure_namespace` stamps `istio-injection=disabled` (`psa.sh:174`); the attach e2e now injects, not simulates the safe case (shipped #296). 🔴 **Fix (3) AS SPECIFIED IS REFUTED — `build-a-different-thing` (idea-round 2026-07-18, 28 cells measured with real apimachinery selector semantics). Do NOT build "render the chart and assert every rule is defeated".** The INVARIANT survived scrutiny (the adversary could not refute it): on `istiod-1.30.3.tgz`, base → **4** rules, `enableNamespacesByDefault=true` → **5**, `revision=canary` → **2**, and in every config each rule is defeated **twice over, independently** — by `istio-injection=disabled` on the namespace AND by `sidecar.istio.io/inject="false"` on the pod. **But the GATE built on it is vacuous, and the number that settles it is: ONE NON-ZERO CELL IN TWENTY-EIGHT.** Istio does not inject without a **positive opt-in** (`istio.io/rev In [default]`, `istio-injection In [enabled]`, `inject In [true]`), so an unlabelled namespace satisfies nothing and "every rule is defeated" is satisfied **by the chart, not by us** — delete BOTH our defences and the gate stays **green** on base, as-installed, revision, and revision+ensbd. Only `enableNamespacesByDefault=true` discriminates. **So the proposed RED-proof FALSE-PASSES on the default render**: "drop both labels → must go RED" holds ONLY for `ensbd`; run it on base (the natural thing) and you either "fix" a working gate or conclude it works when it never fired. Two further blockers: `global.operatorManageWebhooks=true` and `global.resourceScope=namespace` render **ZERO** webhooks, so "every rule is defeated" is trivially true over an empty set; and **the gate cannot run in CI at all** — `bundle/` is gitignored (`.gitignore:7`, `git ls-files bundle/` = 0), charts appear only after `10-mirror-pull.sh` (needs the internet), so it would SKIP (the repo's named "passes by not looking") or be permanently red. **Worst of all, its coverage is INVERSE to the hazard:** it renders the chart WE install — where we ALSO set `global.proxy.autoInject=disabled`, a *third* defence rendering `policy: disabled` in the injector ConfigMap — while B26's actual hazard is **attach mode**, where the mesh is VMware's `1.28.2+vmware.1-vks.1` and we render nothing. **IF BUILT, build it as:** (a) assert **DISCRIMINATION**, not defeat — per config compute the verdict *with* and *without* our labels and require they DIFFER, else record that config as "inherently safe, this gate does not measure us here"; (b) **commit the rendered webhook fixtures** (~2 KB, upstream Apache-2.0) so CI evaluates those offline, plus an operator-local drift gate that re-renders when the chart is present; (c) a denominator guard demanding ≥1 MutatingWebhookConfiguration and a per-config minimum rule count; (d) derive the config list from the **template's** `.Values.` references, not a hand list; (e) single-source our label pair from `lib/psa.sh` rather than hardcoding a second copy; and (f) **name it what it is** — a regression gate on the upstream chart we install ourselves, explicitly zero evidence for attach mode or the lab. ⚠️ Also corrected: "the knob is IGNORED on the revision path" is imprecise — it adds no webhook **RULE** there, but still renders into the injector ConfigMap (unreachable when no rule matches). And "defeated twice" is true of the **chart**, false of the **deployment**: `check-pod-inject-label.sh:45` scopes to IN-TREE pod templates, so Harbor/ArgoCD/Tekton/istiod pods carry only the namespace label — do not let the double-coverage framing license weakening the ns label. ⚠️ **Lab-gated residual:** the shipped fixes are upstream **1.30.3**-rendered; VKS ships `1.28.2+vmware.1-vks.1` [community] and Broadcom could patch the injector template. One command settles it: `kubectl get mutatingwebhookconfiguration -o yaml \| grep -A8 namespaceSelector`. 🟢 **SOFTENED 2026-07-22 — the capability half of this row is probably moot.** Broadcom's Istio Package Reference documents `istio.istioCNI.enabled` as defaulting to **`true`** ("DaemonSet istio-cni-node is deployed if it is true", fetched 2026-07-22 — now recorded in `docs/vks-services/istio.md`). With the CNI DaemonSet handling traffic redirection, injected pods get **no `istio-init`**, hence no `NET_ADMIN`/`NET_RAW` request — which is the exact conditional this row's PSA hazard rests on. So on a package-installed VKS mesh the residual narrows to **`seccompProfile`**, not capabilities. `inferred` from the documented default, **not** measured on a lab: step 13 of `docs/lab-validation-plan.md` now checks whether `istio-cni-node` actually exists. Do not close this row on the inference alone. |
| **B32** | 🔴 **DISPROVED as a gate (adversary-bash-git-cli idea-round, 2026-07-17 s3). Do NOT build a vacuous-`:?` detector.** The MEASUREMENT holds (~62 `:?` guards; 43 vacuous — 41 uncommented-in-`.env.example` + KUBECONFIG(19 sites)+ARGOCD_NAMESPACE(4) via `load_env` code defaults at `os.sh:406/415`), but **"vacuous" ≠ "harmful"**: the 41 class-A guards each ship a valid, usable default and are **defensive documentation-in-code** — commenting the var out to make the guard fire is a *regression*, deleting the guard loses the assertion, so a hard gate would flag 42 non-defects with no sane remediation → the disabled-gate trap. The ONLY reachable harm is C13 on the KUBECONFIG *read* sites (default is a PATH that may not exist → `:?` proves set-not-present → kubectl silently falls back to `http://localhost:8080`), and it is **LOW** (UX only; no data-loss/fake-green) and **already handled** by `env-check`'s `[ -f ]` (`02-env.sh:130-150`); it only bites on out-of-order/standalone invocation. Shipped (#313): an NB comment at the `os.sh` KUBECONFIG default so no NEW path-valued `load_env` default gets a bare `:?`. **Residual DONE (s3):** an adversary idea-round REFUTED the 16-site sweep (it would regress the read-only preflight ACCUMULATORS `23`/`49`, misfit `71` [already `[ -f ]`s a different file] and `06` [deferred-export]) — narrowed to a `kubeconfig_ready` `[ -f ]` helper in `lib/os.sh` at the 6 standalone-plausible consumers (`40`/`41`/`50`/`60`/`70`/`99`), all producer-first so no false-die in the blessed flows; plus fixed the stale `.env.kind` hint (legacy — nothing writes it) → a producer hint (`make vks-login`/`make kind-up`) at `07`/`45`/`46`/`47`/`48`/`49`. `test-kubeconfig-ready.sh` RED-proves the C13 gate (old bare `:?` passes a missing file; helper dies) + a regression guard that `23`/`49`/`30`/`71`/`06` never call it. |
| **B35** | 🟢 **PREMISE RE-CONFIRMED on 1.30.3, 2026-07-18 (measured offline, `helm template` on the carried chart).** Rule counts: base **4**, `enableNamespacesByDefault=true` **5**, `revision=canary` **2**, `revision=canary`+`enableNamespacesByDefault=true` **2** — the knob is genuinely IGNORED on the revision-tag path. ⚠️ **A doubt was raised and REFUTED by measurement:** `istiod/templates/revision-tags-mwc.yaml:131` *does* contain an `if .Values.sidecarInjectorWebhook.enableNamespacesByDefault` conditional, which reads as if the knob applies — it does not, on the rendered output. Reading the template is not reading the render. (Also worth recording: the first counter I wrote returned **1 for all four configs** — an instrument that gives the same answer for different inputs is broken, not a finding; the pattern was `^  - name:` when webhook entries sit at `^- name:`.) 🔵 **RE-SCOPED 2026-07-18: this is NOT a coverage gap to close — the revision path is INHERENTLY SAFE BY OPT-IN.** Both of its 2 rules require `istio.io/rev In ["canary"]` on the namespace or the pod, and our namespaces never carry that label, so the revision path **structurally cannot exercise the hazard even with zero defences** (measured: 0 undefeated rules with NEITHER label). The original framing — "a revision-tag e2e would silently stop exercising the hazard" — is right about the effect and wrong about the cause: it never could. Record it as inherently-safe, not as a gap. **B26's revision-tag case is NOT covered by the shipped design.** A platform mesh installed with a REVISION TAG renders only **2** webhook rules and **ignores `enableNamespacesByDefault` entirely** — so a revision-tag e2e would silently stop exercising the hazard. B26's own text names the revision-tag case. Our `istio-injection=disabled` label DOES still defeat the rev-tagged rule (it demands `istio-injection DoesNotExist`), so this is a COVERAGE gap, not a correctness one — but a gate that cannot fire on the revisioned shape must say so rather than imply it. |
| **B37** | 🔴 **`make red-prove` — a RED-proof must mutate the PRODUCT AS IT SHIPPED, not the code you just wrote.** The rule already exists (`testing.md:281`, *"would this still pass if I deleted the feature?"*; `testing.md:1426`, *"distrust your own RED-TEST"*), was loaded, and was violated anyway on 2026-07-17 — so per the escalation doctrine the deliverable is a GATE, not another paragraph. **The incident:** `test-ensure-namespace-labels.sh` was RED-proven by mutating the `allow-inject` **branch in `psa.sh` that had just been written**, and passed. An adversary then reintroduced the bug **at the call site** (`istio.sh:285`, where #290's defect actually lived) and the gate reported **10/10 ok, rc=0** — blind to the very defect it was named after. I RED-proved the INSTRUMENT, not the DEFECT: the self-authored mutation encodes my own model of the bug, which is exactly what is wrong when the model is wrong. **Design:** run the test against a `git worktree` of `origin/main` with **only the test file transplanted in** (a worktree, NOT `git stash`/`reset --hard` — it cannot touch the working tree, dodging both the data-loss traps), then **diff the per-case verdicts**. The house `ok    <name>` / `FAIL  <name>` format makes that mechanical. A case that does NOT flip `FAIL`→`ok` is a case measuring nothing. ⚠️ **A whole-file red-prove is INSUFFICIENT — I refuted my own first draft in 30 seconds:** any *other* case failing on the pre-change tree turns the file red and masks the blind case (`test-ensure-namespace-labels`'s typo-dies case would have flipped, hiding that the F1 case never did). It MUST be per-case. ⚠️ Applies to **regression** tests only — a test of pre-existing behaviour correctly fails to flip, so the tool must distinguish, or it false-blocks every ordinary unit test. 🔴 **REFUTED a SECOND time — do NOT build the automatic `make red-prove` (idea-round, 2026-07-17 session 2, `adversary-bash-git-cli`, findings VERIFIED against the tree):** **(F1, the killer)** flip-detection measures whether the test's EXISTING cases discriminate baseline↔current — but the incident was a **COVERAGE GAP** (the test asserted "the helper labels" — always true — and never "the installer REACHES the helper", which was the bug). A missing assertion is invisible to a flip-detector, so red-prove would **CERTIFY the exact blind test it is named after** (mutate the helper on baseline → the helper-case flips → "GOOD"). The incident's real remedy is STRUCTURAL and already shipped: `check-namespace-labelled.sh`, keyed on the psa-check inventory ("every namespace we own must be REACHED by an ensure_namespace call") — that is what catches a missing call; a unit-test-flip cannot. **(F2, verified)** a `set -e` house test that sources a helper absent on baseline aborts `rc=1` with **ZERO `ok`/`FAIL` lines** (reproduced in /tmp) — so a loose predicate ("not-ok on baseline") false-PASSES red-prove on a test that never ran, and a strict one ("`FAIL` on baseline") false-BLINDs a good test. **(F3, verified)** `origin/main:scripts/lib/istio.sh:277-279` ALREADY carries the `ensure_namespace` calls — the fix is merged, split across #290/#295/#296/#297, so "baseline = origin/main" finds no bug → false BLIND; there is **no automatic baseline**, and a manually-named one reintroduces the author's-mental-model risk the tool exists to kill. **(F4)** red-prove has no self-check constructible without circularity (the known-blind fixture must be author-fabricated with the same model) → it violates B39, the rule it is filed under. **Conclusion:** the incident class (coverage gap) is **un-gateable by flip-detection** — keep the structural gate (`check-namespace-labelled.sh`, shipped) + B39's per-instrument known-answer discipline, labelled discipline. A NARROWER `red-prove` the adversary sketched (operator-named baseline + explicit `# redcase:` manifest + literal-`FAIL`/literal-`ok` predicate treating any abort as ERROR + a committed known-good/known-blind self-check pair) IS sound but catches a **different, lesser** bug ("a regression test whose declared RED case does not discriminate the pre-fix code") and by the adversary's own verdict **does NOT close this incident** — so it would need its own idea-round before building, not this row. |
| **B39** | 🔴 **THE META-GATE IS REFUTED — do NOT build "assert every `check-*.sh` prints a denominator and dies on zero" (idea-round 2026-07-18; the adversary IMPLEMENTED it, ran it against all 21 gates, and measured it **67% accurate — wrong about 7 of 21, in BOTH directions**). It is a FOURTH instance of this row's own class, not a fix for it.** The property that matters is not *"is there a zero-guard"* but **"is the guard on the ITEM count or the FILE count"** — a semantic distinction no regex can see. 🔴 **REPRODUCED INDEPENDENTLY, and it is a live defect:** empty every tracked `.md` (still tracked) and `check-doc-novels` reports `OK — scanned 21 doc(s)` **rc=0**, `check-doc-robot-quoting` reports `OK — scanned 22 file(s)` **rc=0** — healthy denominators over a corpus with **zero content**. `check-doc-make-targets.sh:70` is the cleanest proof: it prints BOTH numbers and emits `0 command(s) across 25 doc(s)` rc=0, and `:40` explicitly `exit 0`s when no docs match. **Filed as B49.** ⚠️ **MY OWN NUMBERS IN THIS ROW WERE WRONG IN BOTH DIRECTIONS, and the error is the row's thesis in miniature:** there are **21** gates not 20; **~17 print a denominator** (not ~10); **10 have a zero-guard** (not 5); only **6** do both on the *meaningful* count. Two of the five I named as "prints a number that gates nothing" are **false** — `check-image-alignment.sh:73` and `check-doc-target-coverage.sh:56` both carry an explicit `the gate has gone BLIND` guard. **I mis-scored `check-image-alignment` BY HAND in exactly the way my classifier had**, i.e. the manual re-check reproduced the instrument's error rather than correcting it. **THE BUILDABLE THING IS NARROWER AND MUST BE CALLED WHAT IT IS: a VACUITY HARNESS for committed gates, via CORPUS STARVATION** — empty each gate's corpus in a throwaway tree and assert rc≠0. It caught the three blind gates the static version *certified*, runs ~30 s for 21 gates, and **cannot be satisfied by `echo "checked 1"`**. Its self-check is non-circular (a committed blind/good fixture pair with a known answer — unlike B37, where the fixture had to encode my own model of the defect). Its own residual: the per-gate corpus declaration is an **enumerated list that will rot**, and a wrong declaration makes the starvation test vacuous — the same class one level up. **THE ORIGINAL INCIDENT CLASS STAYS DISCIPLINE, labelled as discipline:** all three motivating failures were **throwaway instruments in a shell**, which no repo gate can ever see. The discipline is: *an instrument must be shown to DISCRIMINATE before its output is believed* — run it on ≥2 inputs known to differ and confirm the outputs differ. It would have caught the webhook counter instantly (1,1,1,1). **A fourth instance occurred while verifying THIS row**: `bash gate \| tail -2; echo rc=$?` reported `tail`'s status, not the gate's. |
| **B40** | ✅ **DONE 2026-07-19 (#355) — `make check-ns-chokepoint`, built NARROW with its blind spots named.** **The gap is real and was NOT subsumed by the three assertions in `check-namespace-labelled.sh`** — measured: append `run kubectl <create> <namespace> foo-system` to an installer and all three stay GREEN with **byte-identical counts**, because the derived cross-check only sees namespaces created VIA `ensure_namespace` and `psa-check` iterates `NS_SPEC` without ever asking the cluster what it has. That is this repo's own *"a gate green at the SAME COUNT is blind to it"*, firing on a gate shipped the same day. **COVERAGE IS 4 OF 10 AND THE HEADER SAYS SO**: covered are a literal kubectl invocation, the same via a wrapper (`ka`/`kg`/`hub`/`guest`/`run`), the helm create-ns flag, and the ArgoCD create-ns syncOption; **permanently invisible** are the upstream-manifest and chart-shipped mechanisms, which live in gitignored `bundle/` (`git ls-files bundle/` = 0) so a gate scanning there passes vacuously — out of scope BY CONSTRUCTION; **statically undetectable** are `envsubst \| apply` and heredoc applies. **The corpus of bypass forms is OPEN**: an adversary broke a best-faith version in ten minutes with two forms outside all ten (a runtime-generated manifest, and a verb held in a variable), and both are still missed — recorded in the header, because the alternative is a green that reads as closure. ⚠️ **The comment polarity is INVERTED relative to `check-namespace-labelled`, deliberately** — that gate must NOT strip comments (a commented-out `ensure_namespace` means the call is GONE, its defect), while this one hunts a call that must NOT exist, so a commented-out one is harmless; stripping comment LINES took the hit count 25 → 10 with zero false negatives, and the test is on the FIRST non-space char, not `${line%%#*}`. ⚠️ **The self-test earned its keep immediately**: the patterns are composed at runtime so the file never contains the literals it hunts, and the first version STILL flagged itself — not from prose, but because the **ALLOWLIST REASONS** said "hub create namespace" and "helm --create-namespace". The gate's own DATA was voting on the answer, one level below where I was looking. ⚠️ **My own sizing scan was wrong twice**, both caught by making the instrument disagree with itself: an awk pattern used `\b`, a GNU grep-ism POSIX awk lacks, so the kubectl/wrapper arm matched NOTHING and reported a plausible **6** (real: 10, and the missing 4 included the chokepoint itself); and the first count included comments (25), which reads as "this gate needs 25 exemptions" — the wrong-shape signal that killed the earlier design. RED-proven four ways (erosion, wrapper, helm flag, dead allowlist entry). The refuted history follows, because it is why the gate is shaped this way: **The syntax chokepoint gate (B30a) — DESIGN REFUTED, still unbuilt.** An adversary built and RAN my design against a real corpus: it caught **0 of 8** namespace-creating forms, one **live on the tree** (`hub create namespace` at `e2e-cross-cluster.sh:57`). Namespaces appear here by **SEVEN mechanisms** and a literal-`kubectl` grep sees three: a **wrapper** (`hub`/`guest`/`ka`/`kg` — 6 wrappers across 5 scripts), an **upstream manifest** (tekton's release YAML), a **controller** (ArgoCD `CreateNamespace=true`, from no file at all), a chart's own Namespace, helm `--create-namespace`, an in-tree `kind: Namespace`, and the literal form. **The structural lesson: I enumerated the bypasses with the gate's OWN grep, so the gate could not fail on what the premise missed** — self-confirming, the same shape as the F1 gate one level up. Two concrete bypasses to pin in its test: `echo "# ns-ok: lol"; kubectl create namespace sneaky` **exempts itself** (the raw-line marker check does not parse quotes, and it inflates the `exempt` count); and `${line%%#*}` **false-negatives** on `${#HITS[@]}`, `${v#pfx}`, `grep "#foo"`, and a URL fragment — so do NOT strip comments for detection (a flagged commented-out call is the cheaper error). Refuted hypotheses, do not re-raise: `kubectl create ns foo` and a TAB separator **are** caught; the gap is *internal* whitespace. **`check-namespace-labelled.sh` (shipped) covers the real hazard better** — keyed on the INVENTORY, immune to all six blind spots — so this row is "no NEW bare create appears", a genuine but lesser guard. Needs the 7-mechanism model + a bypass corpus before it is built. 🟢 **THE MECHANISM MODEL IS NOW BUILT AND VERIFIED (2026-07-19), and it corrects this row twice.** There are **TEN** mechanisms, not seven, and two of the seven named above **do not exist on the tree**: M1 literal `kubectl create namespace` (3 sites); M2 a kubectl WRAPPER (`e2e-cross-cluster.sh:57` `hub create namespace`); **M3 an in-tree `kind: Namespace` — ZERO, verified, only two comments asserting its deliberate absence**; M4 an upstream manifest we apply (tekton ONLY — gateway-api, tekton-triggers/dashboard and ArgoCD's install.yaml carry none); M5 `helm --create-namespace` (5 sites); **M6 a chart shipping its own Namespace — none in any chart readable offline, and the Harbor chart is pulled at install time so it is UNKNOWN, not absent**; M7 ArgoCD `CreateNamespace=true`; **M8 `ensure_namespace` itself, the repo-local chokepoint**; **M9 `envsubst \| kubectl apply -f -`**; **M10 heredoc `kubectl apply -f - <<EOF` (7 sites)**. Wrapper count corrected: **7 wrappers across 4 scripts** (not 6 across 5), PLUS the universal `run()` at `lib/os.sh:662` — so a gate anchored on `^kubectl` misses all of them. ⛔ **M4 and M6 are PERMANENTLY INVISIBLE TO CI**: they live in `bundle/`, which is gitignored (`git ls-files bundle/` = **0**), so a gate that greps there in CI scans an empty directory and passes vacuously — this repo's named "passes by not looking". Any B40 gate must declare M4/M6 out-of-scope-by-construction rather than claim coverage. **The research also found a live defect worth more than the gate** — see the namespace-inventory row below. |
| **B43** | 🔵 **REFUTED as specified 2026-07-18 — do NOT build `make e2e-matrix` as an 8-cell grid.** An idea-round adversary measured the topology and the "{podman,docker} × {photon,ubuntu} × {dual-homed,sneakernet} = 8 cells" framing is a **category error**: the axes are not orthogonal. Dual-homed runs the engine **on the box under test** → a real 2×2. Sneakernet's engine is a **host-side** property (one machine, shared by both OS legs) → 2 OS legs × 1 engine choice. The cube does not exist. **Proven, not inferred:** the air-gap half of `jumpbox-run.sh` runs `check-tools`→`bundle-load`→`mirror-push`→`builder-push`→`mirror-verify` and then **`exit 0` at :152**; every `"$ENGINE"` invocation and `engine-trust-check` is *after* that line, so `JUMPBOX_ENGINE=docker` on a sneakernet leg executes **zero additional lines** (`22-builder-push.sh:7-9`: "THIS BOX NEEDS NO CONTAINER ENGINE"). The honest artifact is **6 invocations / 8 legs / 3 groups**, not 8 runs — `SNEAKERNET_OS ?= photon ubuntu` already runs both OS legs in ONE invocation. **The proposed "loud SKIP for vacuous cells" was refuted too**, and this is the subtle part: announcing a cell that does not exist manufactures a known-gap and invites a future session to "close" it by building the vacuous leg — the exact outcome this row was written to prevent. Group by what the axis MEANS instead; the headers are the explanation. **Landed from this review:** `jumpbox-matrix`'s verdict is now COMPUTED (it printed a hardcoded `4/4 PASSED` while tracking only `rc` — narrow the axes to one OS and it still said 4/4; the repo's own "the measured column must be MEASURED" failure, in the summary line of its own matrix), and the retired compressor justification is recorded rather than left to rot. **Still open, and it is a QUESTION FOR THE OWNER, not a judgement call:** B19 (the sneakernet builder half under docker) is on the handoff's LAB-GATED leave-alone list, but it needs **docker + KinD, not a VKS lab** — it may be mislabeled. If it is genuinely open, closing it is **one command** (`make e2e-sneakernet CONTAINER_ENGINE=docker`, ~40 min, destroys and recreates the cluster per leg), not a driver. **Also note:** `e2e-sneakernet`'s EXIT trap runs `kind-down` **on success too**, so any matrix leaves a bare box; and B43 would make **B3** twice as expensive, since B3's Step-4 content is engine-irrelevant — land B3 first. |
| **B38** | **A claim about an upstream ARTIFACT must cite THE ARTIFACT, not prose that uses the same words — candidate gate: an anchor-resolving `[src:]` for upstream code.** 2026-07-17: an adversary claimed the Gateway-API auto-provisioned proxy ships `image: auto`, so `istio-injection=disabled` on `vks-ingress` would break it (filed CRITICAL "F1"). I "verified" it by fetching `istio.io/latest/docs/setup/additional-setup/gateway/`, which says exactly that — **about the `istio/gateway` HELM CHART** (`gateway/templates/deployment.yaml:73`). Our path is istiod's **auto-provisioned template**, `istiod/files/kube-gateway.yaml`: **`image: auto` → 0 hits**, renders `.ProxyImage`, and sets `sidecar.istio.io/inject:"false"` on the pod itself (`:62`) — it is IMMUNE (ran-it, `release-1.30` AND `release-1.28` = what VKS ships, identical). Same sentence, same words, **different mechanism**: I matched the STRING and called it verification, then deleted a TRUE comment from `psa.sh:78-79` and shipped a FALSE one into a control's rationale. Caught only by a second adversary round refuting the first's own prescription. 🔴 **THE CANDIDATE GATE IS REFUTED — do NOT build it (idea-round, 2026-07-18, every finding `ran-it`).** This row used to say a fetch-and-assert-anchor gate "WOULD have caught this instance". **That is measurably FALSE, and it is the most important correction in this row.** The false CRITICAL cited `istio/gateway`'s `templates/deployment.yaml`, and `image: auto` **is** at line 73 of that file (`grep -c` = 1) — so the citation **RESOLVES** and the gate goes **GREEN on the exact incident it was designed to catch**. Worse than useless: it would **launder a false CRITICAL into a cited one**, which is the manufactured-confidence failure. Two structural reasons it can never work: (1) the claim was never wrong about the STRING, only about **which chart is our path** — an interpretive error no resolver can see; (2) the fact that IS true is a **NEGATIVE** ("`kube-gateway.yaml` contains ZERO `image: auto`", confirmed upstream), and a presence-asserting gate is structurally incapable of expressing an absence. Also fatal in practice: the token's own extractor (`check-vks-provenance.sh:43`, `\[src:[^]]*\]`) **truncates at the first `]`**, so the most discriminating anchors in this domain — the injector selectors, literally `inject NotIn ['false']` — cannot be expressed and RED with a diagnostic naming the wrong cause (**the same shape as B28, second occurrence**); and the gate would **fail OPEN** on any network blip because the script is `set -uo pipefail` with no `-e`. **DO THIS INSTEAD (the fourth option, already working in-tree): when an upstream fact is load-bearing, do NOT cite it — assert its CONSEQUENCE offline over OUR OWN artifacts.** `check-pod-inject-label.sh` is the model: it parses our in-tree pod templates rather than citing istio's chart, so it fails on the thing that actually hurts us regardless of what any upstream file says. B38 therefore stays open as a **DISCIPLINE row, not a gate row**. ⚠️ **Honest limit — it does NOT close the class:** it verifies a citation RESOLVES, never that the INTERPRETATION is right. The interpretive half (*"this file is the template the webhook applies TO a gateway"* — backwards) is a **judgment act and is UN-GATEABLE** (`hooks.md`). Do not report B38 as closing B38's class. The un-gateable residual stays **DISCIPLINE, labelled as discipline**: when a claim names an artifact, the evidence must BE that artifact at a pinned ref — a doc that uses the same words is not the artifact, and neither is an adversary that quotes it. |

| **B51** | 🔵 **THREE namespaces reach `ensure_namespace` and are measured by NEITHER inventory, and the drift check is STRUCTURALLY BLIND to that** — found while building B40's mechanism model, verified directly. `tekton-pipelines-resolvers` (`41-install-tekton.sh:90`), `argocd` (`07:78`) and `harbor` (`06:151`) are absent from both `49-psa-check.sh`'s `NS_SPEC` and `check-namespace-labelled.sh`'s `OWNED`. The drift check compares `owned_rows + 2` against `spec_rows`, so a namespace missing from **both** keeps the arithmetic balanced — and comparing the two inventories as SETS would not help either; only a **third, independent ground truth** (the `ensure_namespace` call sites) can see it. Sharpest detail: the gate's own die message cites `tekton-pipelines-resolvers` as the precedent for why the inventories must agree, and it is still in neither. ✅ **SHIPPED in #347:** the counter now sees a LITERAL row (it could not, so adding the very row the header asks for would have made the drift check DIE on a correct change — measured 7 vs 8), and the header's false "It is now listed" is corrected. ⛔ **DO NOT "just add all three to both inventories" — that was REFUTED by measurement and is worse than the bug.** `harbor`/`argocd` are deliberately passed NO level by `ensure_namespace`; `psa-check` reads an absent label as `restricted`; both charts are documented as not restricted-clean — so adding them as `ours` makes `psa-check` rc=1, and `psa-check` is the FIRST prerequisite of `install-all`, converting a reporting gap into a **broken install**. Inventing `PSA_LEVEL_HARBOR` is worse still: the label would then land BEFORE `helm upgrade --install` and could reject harbor's own pods at admission, and `check-env-coverage`'s `INTERNAL` list wildcard-exempts `PSA_LEVEL_*` so the new var would never be required in `.env.example` — a trap `07-install-argocd.sh:70-73` already documents. ✅ **C2 DONE (#352) and C4 DONE (#353) — B51 is CLOSED.** **C2**: `NS_SPEC` rows are now `ns|level|ownership`, `want` is `count(ours)` and the `+2` magic constant is gone. Two idea-round CRITICALs were defects I would have shipped: a **static `mesh` would have DISARMED A GUARD THAT FIRES TODAY** (in the default `istio` mode WE install the mesh, so those namespaces ARE ours and a too-strict level must go red — only `istio-existing` makes them the platform's, so the column removes the hardcoded NAMES from the `case` but the MODE TEST STAYS); and the `kind-only` harbor row would have **MANUFACTURED A FALSE "PSA OK"**, because `measured` is incremented before `ours` is known, so an honest "measured NOTHING" would have become "PSA OK (1 measured)" over the one namespace the column declares NOT ours — now `measured_ours`, **and the verdict gates on it** (my first pass only REPORTED it while the condition still tested `measured`: a number that gates nothing). Also: a 3-field row silently **mis-parses** under a 2-variable `read` (the last var absorbs the rest WITH delimiters), and the column is an operator-typed **escape hatch** — a typo silently un-gates a namespace — so a reconciliation guard requires `ours + mesh + kind-only == total`. **C4**: a THIRD assertion deriving namespace names from the `ensure_namespace` CALL SITES — the only ground truth independent of both hand-typed inventories, which is what makes a namespace missing from BOTH visible. **Divergence proven in both directions** (a namespace in neither inventory → C4 flags it, OWNED blind; deleting the gitea call → OWNED flags it, C4 blind because `lib/istio.sh` still calls it), so it is additive and `OWNED` stays hand-typed rather than derived-and-tautological. Its implementation round found six more, including a **MEASURED silent exemption** (the derived name was interpolated into an ERE, so `"${SNEAKY}|a"` ALTERNATED and matched an exempt entry — green, 6 exempt against a 5-entry list) and a **header that stated the OPPOSITE of the finding beneath it**, telling the operator to add a call that already exists. ✅ **RESIDUAL SETTLED 2026-07-19 on a live cluster — C2 is now proven END-TO-END, not at logic level.** `measured_ours`: harbor (8 pods, `kind-only`) is correctly EXCLUDED and the summary reads `measured 2 namespace(s) with running pods (1 of them ours)`, with harbor's verdict rendering as **"KinD stand-in only (needs restricted) — a Supervisor Service on a real lab, not ours"**. The **mode-dependent mesh is proven in BOTH directions**: in the default `istio` mode `istio-system` gets a GATING verdict (`UNLABELLED -> VKS default 'restricted' REJECTS these pods`), and with the controller set to `istio-existing` the SAME row flips to `platform-owned (needs baseline) — informational, not ours to label`. ⚠️ **A METHOD NOTE THAT COST ME THE FIRST ATTEMPT, for the third time today:** `INGRESS_CONTROLLER=istio-existing make psa-check` does **NOT** flip it — `load_env` sources `.env.state` AFTER the environment, so the PUBLISHED value wins (the state-overlay clobber). Correct behaviour for the gate, wrong test method; the proof has to rewrite the published value. Also note the run's `platform`/`install-ingress` steps FAILED, and that was MY setup — I skipped `make mirror`, so Harbor was empty and gitea sat in `ImagePullBackOff` (verified, not assumed). The original residual note follows: `49-psa-check.sh` cannot run without a cluster, so C2's ownership/`measured_ours` logic was proven at LOGIC level only. Settle with `make kind-up && make psa-check` before and after `make platform`, plus `INGRESS_CONTROLLER=istio-existing make psa-check` to watch the mesh rows flip between gated and informational. The original design follows: 🟢 **THE DESIGNED REMEDIATION (adversary-vetted, now BUILT):** **C2** add an ownership column (`ns\|level\|ownership`, `ours\|mesh\|kind-only`) routing platform-owned namespaces through the EXISTING `ours=0` informational branch — this also deletes the `+2` magic constant; **C3** add `tekton-pipelines-resolvers` as `ours` with a **literal-matching** `OWNED` form (do NOT invent `TEKTON_RESOLVERS_NAMESPACE` to satisfy a regex — that would need four coordinated edits including a call-site rewrite); **C4** add the derived call-site cross-check as a **THIRD** assertion, never a replacement — deriving `OWNED` makes the gate tautological and would un-catch the very F2 regression it was written for, and only **12 of 19** call sites are statically resolvable (7 are loop/local vars, two of them ephemeral e2e fixtures that must never enter `NS_SPEC`), so it must PRINT the 7 it cannot resolve with a reason each. ✅ **C3 DONE 2026-07-19 (#350), measured safe FIRST**: `tekton-pipelines-resolvers` measured `pods=1, current=restricted, psa_min_level=restricted` — ALREADY at its true minimum, so adding it was a pure measurement gain and the predicted red did NOT apply. It went in as a **LITERAL** row (the call site at `41:90` passes a literal, and inventing `TEKTON_RESOLVERS_NAMESPACE` to satisfy a regex would have meant four coordinated edits including a call-site rewrite); the matcher now accepts either shape, discriminated by form alone — a DNS-1123 namespace can never look like an UPPER_SNAKE var. Only possible because #347 taught the counter to see a literal row. `psa-check` now measures **6** namespaces, up from 5. RED-proven by commenting out the call. ⚠️ **A MEASUREMENT THAT CORRECTS THIS ROW:** harbor measures **restricted-CLEAN** on this chart version (`pods=8, UNLABELLED, min=restricted`), so the adversary's premise that "both charts are documented as not restricted-clean" is **FALSE for harbor here** — its `psa-check`-goes-red argument does not hold as stated. **C2 is still the right design anyway**, for a different reason: it gets harbor MEASURED without LABELLING it, and labelling it `restricted` would reject any future harbor pod that is not restricted-clean (a runtime behaviour change on a chart we do not control). `argocd` remains UNMEASURED — it is not installed in this flow. ⚠️ **The original C3 gating note, kept for the arc**: C3 was gated on one live-cluster measurement: `make kind-up && make platform`, then `kubectl label --dry-run=server ns tekton-pipelines-resolvers pod-security.kubernetes.io/enforce=restricted`. Expect `preflight` may go red once — that red is a TRUE finding about an already-shipped label, and the fix is the level, not backing out the row. |
| **B53** | 🔴 **REFUTED AND CLOSED 2026-07-21 — do NOT declare `check-grep-q-pipe` in `test-gate-vacuity`; it is NOT STARVABLE, and one natural declaration BANKS A FALSE PASS.** The row's premise ("it was arguably too slow to declare before; at 1s that objection is gone") was wrong: **speed was never the blocker**. The gate's corpus contains its OWN source AND `lib/os.sh` (which it sources), so no pathspec starves a meaningful corpus while leaving it able to run. All three options MEASURED: **(1)** preserve gate+lib → `OK — scanned 125`, rc=0 → **false RED** (12 real files survive, so the green is a true statement about a corpus that is not empty); **(2)** `:(exclude)…check-grep-q-pipe.sh` → rc=1 with **67 bytes**, `REPO_ROOT: unbound variable` → `assert_starved` scores it **ok** — **a FALSE PASS certifying a gate that died sourcing an emptied lib having judged nothing**, and it is the option a future session will reach for; **(3)** starve everything → rc=0, 0 bytes. ⚠️ **A correction to this row's own earlier text:** option (3) was described as "the documented INCONCLUSIVE case" — it was **not**. `assert_starved` tested `rc -eq 0` *before* the zero-bytes clause, making that clause **unreachable for rc=0**, so a silent death was labelled **VACUOUS**, sending the reader to fix a gate that never ran. **What shipped instead:** the ordering bug fixed (silence is now tested first, whatever the exit code); the gate moved into the header's NOT STARVABLE list carrying all three measured outcomes; the harness output **split** into "NOT STARVABLE (reasoned)" vs "UNDECLARED (unaudited)" — it had been printing four reasoned gates under *"UNAUDITED, NOT proven healthy"*, a false claim about all four — plus a rot guard (RED-proven) that fails if a `NOT_STARVABLE` name stops being a real gate. Undeclared is now **none**: 19 of 23 starved, 4 reasoned. **Also fixed, and the reason the row is not simply deleted:** the gate's denominator comment claimed a guard it did not have — *"zero-guard on the ITEM count, not the file count"* while guarding `scanned`, a FILE count, so 113 of 125 files could be emptied and it still reported `OK — scanned 125`. An `items` guard now exists, but ⚠️ **it does NOT close corpus vacuity and cannot be made to** (its own source supplies items; measured floor **372** under maximal starvation — an items-based vacuity guard is unfireable by construction). It guards **TOTAL FOLD DEATH ONLY**: with `sed` shimmed to exit 127 the gate previously reported `scanned 125, items 0, rc=0` — green over zero examined lines. ⚠️ **That RED-proof is now STALE and the sed case is caught EARLIER**, by the fourth positive control's emptiness arm — a later change preempted the guard it was written for, which is why the control's message names sed explicitly instead of blaming the expression. `items` also does **not** prove the loop READ THE FILES: with `"$@"` dropped from `_fold` it read the heredoc's FILE LIST and reported `scanned 1, items 125` while missing a planted violation, with all four controls green. Corpus also widened `scripts/*.sh` → `*.sh` (125→126), picking up `bootstrap-jumpbox.sh`, which `lint` lints and which carries `set -euo pipefail`. |
| **B52** | ✅ **CLOSED 2026-07-19 — the sweep was REFUTED BY MEASUREMENT, and it was aimed BACKWARDS. Do NOT reopen it as a conversion sweep.** The row proposed converting ~70 bounded `printf '%s' "$var" \| grep -q` sites. An idea-round adversary refuted BOTH halves of its premise, and I re-measured every number independently. **(a) "not safe in principle" is FALSE — the safety boundary is STRUCTURAL, not a hope.** Below the 64KB pipe buffer the producer's write COMPLETES and SIGPIPE cannot occur (**4KB: 0/120**); above it, it always does (**82KB multiline: 120/120**); the transition is narrow (41KB: 4/120, 61KB: 3/120). **And data with NO NEWLINES is immune at ANY size — 3MB single-line: 0/120** — because grep buffers hunting a line terminator and thereby drains the producer. Risk therefore needs **>32KB AND multiline**, and **not one** `printf` site qualifies. **(b) "the remaining are bounded printf sites" was ALSO false**, and this is the part that mattered: **six were EXTERNAL producers whose size we do not control** — `kubectl get deploy -A -o name \| grep -q .`, `docker ps -a --format \| grep -qxF`, `kind get clusters \| grep -qxF` — and the gate waved every one through, because none is a file reader. **The sweep would have churned 30 files to remove a structurally-zero risk while leaving the only real ones untouched.** ⚠️ **The `<<<` conversion is itself a hazard, which is the second reason not to sweep:** `printf '%s' "$v"` on an EMPTY value emits zero bytes (rc=1) while `<<< "$v"` emits ONE EMPTY LINE, so every nullable pattern (`''`, `^$`, `.*`, `^`, `$`, `x*`) flips rc 1→0. Several of those sites are `if`-conditions in TESTS, where that flip is a silent verdict change — a false green in a test. (`printf '%s\n'` is identical to `<<<`, so those few are free; the DRAIN form `\| grep PAT >/dev/null` has no empty-var divergence at all and is what shipped.) **WHAT SHIPPED INSTEAD:** the six unbounded sites converted to the drain form, and `check-grep-q-pipe` widened with an `_EXTERNAL='kubectl\|docker\|kind\|helm\|crane\|curl\|argocd\|tkn\|podman'` producer class (RED-proven in three directions: a kubectl form caught, a docker form caught, a bounded printf form still ALLOWED — no false positive). Their POLARITY is why it was worth doing: `48-istio-preflight.sh:39` inverts with `!`, so a SIGPIPE there reports **"no istiod"** when istiod EXISTS; `05-kind-up.sh:55` reports **"cluster absent"** and re-creates it; `kind-down.sh:23` **skips the sidecar cleanup**. ⚠️ ✅ **Residual SETTLED 2026-07-20 on a live, fully-loaded cluster** (argocd + harbor + gitea + tekton + istio + both apps): `kubectl get deploy -A -o name` = **941 bytes / 27 deployments**; `docker ps -a --format '{{.Names}}'` = **174 bytes**. Both are **nowhere near** the measured 32KB risk threshold — ~35 B per deployment, so you would need roughly **900 deployments** to reach it. So those six conversions were **DEFENSIVE, not urgent**, and nobody should cite this row as evidence that a SIGPIPE was actually occurring here. The polarity argument for making them still stands unchanged (a SIGPIPE at `48-istio-preflight.sh:39` inverts `!` and reports "no istiod" when istiod EXISTS), and the external-producer class remains the right thing to gate — it is only the urgency that was overstated. The fix is cheap and correct regardless. |
| **B89** | 🔴 **Step 4's Harbor credential proof CANNOT FAIL, and its measured third outcome is undocumented.** `docs/scenario-1.md:335` ends `…; rm -f /tmp/h.cfg`, so the command's status is **`rm`'s** — curl's exit is discarded. MEASURED, walk row 1 (ubuntu, NOTHING exists, 2026-08-12, block `[13]`): it printed **`000`** after **135 s** and the block scored **rc=0**. The expectation at `:337-338` enumerates only `200` and `401`; `000` (the name resolves, nothing is serving there) is unlisted, so a reader gets an unexplained value from a step that "passed", then carries a broken `HARBOR_URL` into Step 11. The cause had already been named four blocks earlier — `make harbor-reachable` (block `[11]`) printed the stale-A-record PROBLEM — but its own text predicts *"a 129s hang in **Step 8**"* and never mentions Step 4, which is where the hang actually landed. **Minimal fix:** `--max-time` on the curl; stop masking its status (`rc=$?` on its own line, `rm` after); add `000` to the expectation table pointing at the A record + `make harbor-reachable`; correct the `harbor-reachable` message to name Step 4 as well as Step 8. |
| **B90** | 🔴 **The walk box resolved a STALE Harbor A record that the lab's DNS attendant had ALREADY fixed — "verified" is verified from the wrong vantage.** MEASURED 2026-08-12, row 1: `/tmp/walk/dns-r1.log` ends `nested-mgmt: harbor.env1.lab.test -> 192.168.101.139` / `supwl: … -> .139` / `verified: … resolves to 192.168.101.139`, while the walk box at `04:29:29Z` resolved **192.168.101.136** (block `[11]`) and was still on `.136` at block `[13]` (135 s, `000` — see B89). The attendant verifies from **its own** vantage (nested-mgmt + supwl), never from the box that is doing the walking, so its `verified:` line is not evidence the walker can resolve the name. The record moved `.130 → .133 → .134 → .135 → .136 → .139` across reinstalls — every Harbor reinstall mints a new LB IP — so this is **structural, not incidental**. ⚠️ The mechanism (resolver cache on the box vs. update-after-query ordering) is **NOT established** — do not claim which until it is measured. **Fix lands in `nested-vsphere-lab`'s harness** (verify the record FROM the walkbox, and/or flush its resolver, before letting the walk proceed), not here. |
| **B91** | **A doc-acknowledged BENIGN non-zero still scores the walk block RED, because the prose that excuses it is not machine-readable.** `docs/scenario-1.md:252-253` says `vcf context use` *"can print a 'system Harbor registry' error and still have worked — judge it by the next command, not its exit code."* MEASURED row 1 block `[08]`: exactly that — `[x] : failed to discover plugin sources from the system Harbor registry` with rc=1, while `[ok] Successfully activated context 'vks-cicd:cicd'` printed and the next block `[09]` ran clean. The walk records `--- [08] rc=1` with **no** `expect:` line, because the doc's `:254` **Expect:** is a file-existence claim (`./secrets/supervisor.kubeconfig` exists), not an output string the harness can match — so the run's red-block count over-reports. **Owner call between two shapes:** give the block a machine-readable expectation the walk can match, or teach the harness that a block whose doc says *"judge it by the next command"* is judged by the next block. Do NOT simply drop the prose — it is correct and a reader needs it. |
**Needs a real lab or a heavy run:**

| ID | Item |
|----|------|
| **B54** | 🔵 **IDEA-ROUND DONE 2026-07-23 (`vks-adversary`, primary-sourced) → DON'T BUILD (both scenarios).** The ArgoCD `ApplicationSet` cluster-generator earns its keep at **N≥2 clusters**; we have ONE, so at N=1 it delivers ~zero AND **discards the accreted `70`/`71` safety net** — refuse-to-guess among registered clusters, exact-match destination, refuse an in-cluster destination when ArgoCD is off-cluster (`70`'s CRITICAL #158/#160) — which a `matchLabels:{type:vks}` + `prune/selfHeal` ApplicationSet cannot express; on a SHARED lab ArgoCD that is exactly the cross-tenant mis-deploy the imperative path was rewritten to prevent. **q1** (beats `make gitops`?): NO, net loss — a guest re-provision forces re-running `71` regardless, and the "add-app→reconciles" benefit is a **list/git** generator's (needs a new Gitea repo the controller polls → re-incurs the `GITEA_ARGOCD_URL` reachability problem). **q2** (Scenario-2 RBAC): WORSE — `applicationsets` is project-scoped in `argocd-rbac-cm` BUT ArgoCD's Security doc says **creation is admin-only** and templating can ESCAPE the tenant's AppProject unless `project:` is hard-coded; it degrades the `argocd app create` self-service the tenant already has. **q3** (rejected credential?): NO — the cluster-generator reads only name/server/labels; the sync credential stays `71`'s durable bearer token, no #13175. **Correction:** the row's "real-lab only, no KinD" was wrong for the MECHANIC — `e2e-cross-cluster.sh` already validates the identical Supervisor→guest topology offline; only the Scenario-2 TENANCY is real-lab-only. **If the owner ever overrides (Scenario-1 only):** use a **list generator** with `project:` hard-coded + destination pinned + re-implement `70`'s ambiguity guard — NOT the cluster generator; and settle two lab questions (does the VKS-package ArgoCD expose `argocd appset create`; does a tenant's AppProject role include `applicationsets`). |
| **B2** | **The Gateway-API CRD version, not its presence.** A VKS 9.1 guest ships the CRDs from the VKr (ON by default, opt-out label), while `istio_ensure_gwapi_crds` server-side-applies our pinned `GATEWAY_API_VERSION` — so we may be fighting the VKS add-on manager. Settle with the `bundle-version` jsonpath + the add-on label. Full grading: `docs/vks-services/istio.md` §4. **Lab commands added to step 13 of `docs/lab-validation-plan.md`** (bundle-version + add-on label + VAP), 2026-07-23. |
| **B46** | ✅ **CLOSED 2026-07-18 — DECLINED ON EVIDENCE by the owner. Do not re-open without a new reason; the alternatives are measured and two of them do not work.** The finding is real: for identical content, `podman save` emits **623,348,736 B** (docker-archive v1, layers **plain tar**) while `docker save` 29.6.2 emits **286,969,856 B** (**oci-layout + `index.json` + a compat `manifest.json`**, layers **gzip**) — **2.17×**. **The number that decided it:** the saving is **338 MB against a ~12 GB bundle = 2.8% of the carry** (the builder tarball is only 5.2% of the bundle to begin with). Both ways of capturing it add a moving part to the **air-gap path**, which is the one place a failure is most expensive, so the trade was declined. **Options measured, for the record:** (a) `podman save --format oci-archive` → 297,119,232 B but **`crane push` REJECTS it** — `Error: loading …: file manifest.json not found in tar`; docker's archive only works because it is a **hybrid** carrying the compat `manifest.json` alongside the OCI layout. (b) `podman save --compress` → **not supported** for docker-archive (fails). (c) gzip the podman tarball → **285,575,559 B** (i.e. ~97% of docker's win, podman retained) but requires a `gunzip` step + transient disk in `22-builder-push.sh` **on the air-gap box**. (d) prefer docker in `14-builder-build.sh` → 287 MB with no decompress step, but the artifact's shape then depends on which engine the internet box happens to have, and it pushes against the **"docker is opt-in, never required"** invariant that `test-container-engine.sh` check 7 enforces. ⚠️ **The genuinely useful half of this row is NOT the size** — it is that `test-builder-save-crane.sh`'s honesty block claimed both engines default to the SAME format and **that was false**; the claim survived because a successful `crane push` proves crane handles **both** and says nothing about them being the **same**. The discriminating command was `tar tf`. That correction is already in the script's header and in `rules/common/testing.md`. |
| **B20** | ✅ **RESEARCH DONE 2026-07-19 — the question is answered from primary sources; the WIRING stays gated on lab step 3, and the review found a bigger bug than the one I was chasing.** **The answer:** the only supported mechanism is the env var **`VCF_CLI_VSPHERE_PASSWORD`** [`9.0-doc`, verbatim: *"You can also set an environment variable VCF_CLI_VSPHERE_PASSWORD with the password value"*] — it reaches the CLI through `execve` envp, never argv, so it satisfies `security.md`. There is **no `--password` flag** (confirmed by the reference AND by a practitioner) and **no documented stdin form** (NOT STATED — do not invent one). The page 301-redirects from `/latest/` into the `/9-0/` tree, which is what fixes the grade at `9.0-doc`. ⛔ **NEVER `vcf config set env.VCF_CLI_VSPHERE_PASSWORD`** — documented on the same page, it writes the password **plaintext to `$HOME/.config/vcf/config.yaml`** and it *"persists until you unset"* it: outside the repo, invisible to gitleaks and to `check-secrets-untracked` (tracked paths only), surviving every teardown — the `/etc/docker/certs.d` residue class. That warning is the highest-value line of the change and is now in the script. 🔴 **MY DESIGN WAS REFUTED, and the refutation found a CRITICAL I was not looking for.** I intended to wire the env var and add it to `.env.example`. An adversary round killed three things, all verified by me against the tree: **(1)** the premise. `30-vks-login.sh`'s comment claimed the CLI *"prompts interactively for the context name AND the password"* — **REFUTED**: Broadcom's synopsis is `vcf context create CONTEXT_NAME [flags]`, a **required positional**, no `--name` flag, four examples all positional, no documented prompt. `30` passes **none**, so it most likely errors `accepts 1 arg(s), received 0` — while our own `31-fetch-argocd-kubeconfig.sh:77` passes it **and** `--type k8s`. Wiring a password into a call missing a required argument would only make a broken path LOOK automated. **(2)** the new variable. **`VKS_PASSWORD` already exists** (`.env.example:805`, already commented, already documented "never argv") and `check-env-coverage.sh:42` **wildcard-exempts `VCF_[A-Z_]*`**, so a new `VCF_*` var would have had no gate — the second instance of the `PSA_LEVEL_*` trap (B51). **(3)** `export`. The right shape is a **command-scoped prefix**, the precedent for which is on line ~92 of the very file I was editing (`KUBECTL_VSPHERE_PASSWORD="$VKS_PASSWORD" kubectl vsphere login`); a bare `export` would put a vCenter SSO **admin** password in the environment of every `kubectl`/`helm`/`crane`/`git` the script later spawns, readable in each `/proc/<pid>/environ`. The guarded one-liner is recorded in the script, unwired, with the non-empty guard flagged as **not optional** (an unguarded empty assignment CLOBBERS an operator who exported it themselves). **What shipped:** the facts + the forbidden path + the refuted-premise note in `30-vks-login.sh`; an operator-facing warning that now names the **exact recovery command** if the positional error appears; and lab step 3 sharpened from "paste `--help`" to **three named yes/no questions** (positional? `--type k8s`? no `--password`?) so it can be settled in seconds. **Still open, lab-gated:** land the positional fix + `--type k8s` + the guarded env prefix in BOTH scripts, together, from the real `--help`. Also unverified [`community`]: the prompt **recurs at token refresh**, so a long `make install-all` can block mid-run — which is the argument for exporting it for the session deliberately rather than scoping it to `context create` alone. |

| **B65** | ✅ **DONE 2026-08-05 — `fetch-ca.sh` accepted a MITM and printed `VERIFIED`; 4 commits, both adversary rounds run, both REFUTED me.** It took a CA off the wire, `openssl verify`d the served leaf against it, and reported success — self-consistency, which a MITM satisfies BY CONSTRUCTION, and in the single-cert branch (the one we test) it reduces to `verify(X,X)`. MEASURED with a live oracle: an evil self-signed cert was written and reported VERIFIED, rc 0. Blast radius is **credential disclosure**, not image trust — `lib/harbor.sh` submits `admin:password` over this anchored connection and `22-harbor-robot.sh` mints the robot on it. **Now:** out-of-band SHA-256 pin (`HARBOR_CA_SHA256`/`ARGOCD_CA_SHA256`/`VKS_CA_SHA256`); mismatch and malformed both refuse; no-pin+tty discloses and asks; no-pin+no-tty refuses. **Design round killed my escape hatch** — the lab NEVER CALLS this script (`06-install-harbor.sh` mints its own CA), so a `LAB_ONLY` knob would be settable only on the customer path. It also found the plumbing that would have made the whole control INERT: `-include .env` creates MAKE vars, not environment (MEASURED: recipe `[AA:BB:CC]`, script `[]`). **Implementation round found a CRITICAL I introduced:** the copy to `$OUT` ran ~50 lines before the first check and each refusal `rm -f`'d it — so DETECTING AN INTERCEPTOR DELETED THE OPERATOR'S GOOD ANCHOR, in gitignored `./secrets/`, while the die said "refusing to WRITE". Also: an invalid label FAILED OPEN (1135 B of unauthenticated 0644 trust material); the consent block went to stdout so `>log` made it a silent hang; and **the test could not discriminate** — killing the entire interactive branch still passed 7/7. Now staged to a temp candidate, 25/25, RED-proven three ways. |
| **B66** | 🔴 **The Tier-1 CONSUME pattern already exists in this repo — for Istio. Harbor never got it.** `44-install-ingress.sh` has `INGRESS_CONTROLLER=istio\|istio-existing\|traefik`, with `istio-existing` logging *"attaching to an Istio we did NOT install"* and its own e2e (`90-e2e-istio-existing.sh`). MEASURED: `grep -rnE "HARBOR_MODE\|harbor-existing\|HARBOR_INSTALL"` across `scripts/`, `Makefile`, `.env.example` = **ZERO**. Harbor's consume path is IMPLICIT — set `HARBOR_URL` and rely on the right branches not running. ⚠️ **MY OWN JUSTIFICATION FOR THIS ROW IS PARTLY REFUTED, BY ME, BEFORE ANY WORK STARTED.** I first wrote *"nothing structurally prevents a customer run reaching it"* — **MEASURED FALSE**: `make -n install-all` contains **ZERO** references to `06-install-harbor.sh` (the chain is `preflight mirror mirror-verify builder-image vks-login platform gitops`, and `platform` = `install-gitea seed-gitea install-tekton configure-tekton`). `install-harbor` is a standalone operator-invoked target, NOT on the customer path, and there is no flow discriminator in `lib/os.sh` (`is_kind`/`FLOW=` = 0 hits). So the safety argument is gone and what may remain is a DOCUMENTATION problem (two personas in scenario-1, no machine-readable mode) — which is a much smaller thing, and possibly nothing. **A design round is running on exactly the question "is this justified at all, or is it tidiness dressed as safety?" — do not implement before reading its verdict; closing this row is an acceptable outcome.** This is the shape the TLS adversary's Tier-1 recommendation needs, and it is **not new architecture**: copy the ingress precedent, including its clobber workaround (`44-install-ingress.sh` captures the explicit override BEFORE `load_env`, which is why an uncommented `INGRESS_CONTROLLER` in `.env.example` does not defeat a per-run override). Design round required — the ingress analogy is a starting point, not a settled design. |
| **B67** | 🔴 **The remaining `--insecure` sites, now that this repo mostly targets environments we do NOT own.** `91-e2e-tenant-mechanism.sh:163,222` exports `ARGOCD_OPTS="--insecure"` and **:173 submits the ArgoCD ADMIN password over `curl -s -k`** (its own comment shows care about argv leakage while disabling verification on the same request); `e2e-cross-cluster.sh:95` `insecure-skip-tls-verify: true` on a kubeconfig carrying a bearer token; `06-install-harbor.sh:230` `skip_verify` under `HARBOR_INSECURE=1`; `16-engine-trust-check.sh:148`; `test-builder-save-crane.sh`. All KinD/e2e-only today, which is why this is a sweep and not a blocker. ⛔ **The obvious fix — gate them behind `LAB_ONLY=1` — is the exact escape-hatch shape the B65 design round REFUTED.** That refutation turned on the lab never calling `fetch-ca.sh`, which does NOT apply here (these ARE the lab paths), so the analogy is not automatic and the design round must be re-run rather than assumed. |
| **B68** | 🔴 **ArgoCD's cert carries NO externally-valid name, which is why `make creds` shows an IP while Harbor shows an FQDN.** MEASURED: Harbor's SAN is `DNS:harbor.env1.lab.test` (the lab minted it for a name it chose via the service's `hostname:`); ArgoCD's SANs are ONLY `localhost, argocd-server, argocd-server.lab, argocd-server.lab.svc, argocd-server.lab.svc.cluster.local`. `argocd.<domain>` resolves and answers 200 **solely because every caller passes `--insecure`**. So the asymmetry is not a display bug: `creds.sh` falls to a discovery branch that reads `.status.loadBalancer.ingress[0].ip`, which is an IP BY DEFINITION — a Service cannot know its DNS name. Both generic auto-name mechanisms are dead here (MEASURED: the LB `hostname` field is empty; reverse DNS returns nothing). **Printing the FQDN would be a display the certificate disclaims.** The fix is the cert. ⚠️ The adversary claimed a replaceable `argocd-tls` secret exists; MEASURED it does NOT in the `lab` namespace (only `lab-gc1-auth-svc-cert`) — the mechanism may still work by CREATING it, but settle that before designing. |
| **B69** | 🔴 **`*.vks.local` is on a suffix RFC 6762 reserves for mDNS, and it blocks every resolver-based option.** MEASURED, the discriminating pair: `resolvectl query x.vks.local` -> *"No appropriate name servers"* (**NOT ROUTED**) vs `resolvectl query x.vks.test` -> NXDOMAIN (routed). Also measured here: `avahi-daemon` is **active** and nsswitch is `files mdns4_minimal [NOTFOUND=return] dns` — it works today ONLY because `/etc/hosts` short-circuits first, i.e. the very line we would remove. The repo is already inconsistent: Harbor uses `.test` (RFC 6761, correct), the ingress uses `.local`. **CUT-TIME change:** MEASURED **14 files**, not the 4 an adversary estimated (`.env.example` 14 · `test-creds-show.sh` 13 · `creds.sh` 12 · `Makefile` 10 · `lib/os.sh` 8 · 4 test files · `README`/`CLAUDE`), and `APP_DOMAIN` feeds both the live HTTPRoutes and the hostnames force-pushed into the seeded Gitea deploy repos — so changing it against a running lab means force-pushing the app repos and re-applying routes. ⚠️ **NOT on the critical path** for the recommended default: MEASURED, a proxied client never resolves at all (`curl --proxy` -> exit 7 *couldn't reach proxy*; unproxied -> exit 6 *couldn't resolve*), so a browser proxy sidesteps the mDNS trap entirely. |
| **B70** | 🔴 **THE MINIMAL CUSTOMER BASE — what we must be given, and what we must stop requiring.** Owner reframing 2026-08-05: normally we get only a vCenter IP + login + password for someone else's vSphere. **MEASURED: DNS is NOT required for our tooling** — `curl -H 'Host: …'` against the ingress IP returns 200 for all four app hostnames, `--resolve` returns 200, and `git ls-remote http://<gitea-lb>:3000/…` works with no name at all. The ONE hard name requirement is the **image reference** (containerd cannot set a Host header). **Trust is already solved by the platform:** `tkg.tanzu.vmware.com/tkg-registry-additional-ca-cert` on the Cluster carries `CN = Harbor CA` (notAfter 2036) and MEASURED **nothing in either repo sets it** — VKS does, and it is an annotation on the Cluster WE create, so we can set it for a customer's own registry with only a vCenter credential. **IP-based refs are dead** (Harbor's Supervisor Service `hostname` is an FQDN by contract; the IP-SAN two-phase pattern exists only in the KinD path; `newName` is force-pushed into the Gitea repos and only `newTag` is ever repaired; RFC 6066 forbids IP literals in SNI, so IP-only is incompatible with Contour, which our own runbook offers). ⚠️ One adversary INFERENCE refuted by measurement: the token realm is **request-derived** (`realm="https://<whatever-Host-you-send>/service/token"`), so it does NOT force a name. **RECOMMENDED DEFAULT: ask for ONE A record — Harbor's — not a zone**, and ship a local unprivileged browser proxy + PAC snippet instead of `/etc/hosts`. |
| **B72** | ⛔ **CLOSED 2026-08-05 — DO NOT BUILD, on two structural grounds verified directly.** Do not re-file. |
| **B75** | 🔴 **The rebuild-staleness class is NOT closed, and the two paths that WERE fixed are the two whose consumers did the checking.** Two HIGH: a TLS-trust failure against argocd-server is reported as a PERMISSION denial (`argocd account can-i … >/dev/null 2>&1` — x509 and RBAC are both rc=1 and the discriminator is discarded), and a stale kubeconfig CA is reported as "the cluster is unreachable" when the cluster is up. Round verdict **ship-with-changes** with three corrections: **`argocd account can-i` EXITS 0 ON A DENIAL** (rc files a refusal under "permitted" — cited from upstream pinned to the installed v3.4.5); `91-e2e-tenant-mechanism.sh`'s RED-1 is a **negative control that passes vacuously** on any transport failure; and **redact-before-classify CHANGES the verdict** (measured: a 1-char secret turned STALE_CA into UNKNOWN). Also: the `401` arm collides with kubectl's **klog microsecond timestamp** — 4/60 = 6.7% non-deterministic on ANY kubectl failure — and, being arm #2, SHADOWS FORBIDDEN. Fix = extend `classify_kube_failure` (do not fork), anchor `401` AND move it below the network arm, add kubectl's real expiry phrasing (its 401 text contains neither "Unauthorized" nor "401"), add a PLAINTEXT bucket, and cover all 7 sites across 5 files with THREE behaviours, not one. |
| **B75-a** | 🔴 **`ARGOCD_CA_FILE` has ONE producer and ZERO consumers — and the e2e that would have caught it passes `--insecure`.** `argocd`'s global `--server-crt` is one line away, so "delete the target" is refuted: it would remove the only fix for B75's HIGH-1. The gate is green precisely because the only test exercising the `api` mechanism disabled the verification the operator path leaves ON. |
| **B75-b** | ✅ **DONE 2026-08-05 — `ca_verifies_endpoint` reported an endpoint that served NO CERTIFICATE as verified.** Plaintext → rc **4** (was 0: s_client prints both `no peer certificate available` AND `Verify return code: 0 (ok)`); missing/empty anchor → rc **5** (was 1 = "stale", the opposite remedy). ⚠️ The FIRST fix was WORSE than the bug — testing `no peer certificate available` alone collapsed THREE verdicts into one, because `-verify_return_error` aborts the handshake on a failed verification too. Only the regression arm caught it. New suite `test-ca-verifies-endpoint.sh` (7 cases, real oracles); the function previously had NO tests at all. |
| **B76** | 🔴 **Operator-UX, both repos: ETA from `TIMINGS.md`, `--debug` provenance, self-explanatory failures.** Not started. |
| **B77** | 🔴 **Fold the scenario-1 walk results back into the docs + `.env.example`.** Inputs already exist (the walks against CUT #8 and CUT #9, and B222's §1→§11 pass) — **no new lab needed** for the §5–§9/§11 corrections or the `.env.example` work. Carry this lab's values as working defaults while the prose stays parameterised: respect `check-env-clobber` (per-run knobs stay COMMENTED) and `check-env-coverage`. Storage policy and SSO domain are **parameters, not constants** — two real labs disagree (`wcp-vmfs`/`vsphere.local` here vs `vsan-default-storage-policy`/`wld.sso` there); present both and give the discovery command. Grade Broadcom facts `9.0-doc (inferred for 9.1)` — the 9.1 URLs 301-redirect. ⚠️ The subset of B222's 7 findings that are visible ONLY on a rebuilt lab (5 of them) IS gated on the next cold cut. |
| **B81** | 🟠 **§8's VMCA pointer is CIRCULAR — the doc sends you round in a loop and never gives a command.** MEASURED (`adversary-bash-git-cli`, 2026-08-05): `docs/scenario-1.md:209` says the Supervisor's VMCA root *"see §8 for getting it"*; `:614` says *"re-obtain it (Step 8)"*; and §8 itself (`:570-600`) says only *"it is the Supervisor's VMCA root"* — **no command, no producer.** `command grep -rn 'supervisor-ca.crt' Makefile scripts/` returns ONE hit, a comment, and `scripts/lib/tls.sh:87` states verbatim *"Nothing in the repo re-fetches it"*. So a reader following the docs cannot obtain the artifact the docs require. Independent of B64 (found while attacking it) and it does NOT need a lab to fix: either write the producer (a `fetch-supervisor-ca` target, matching the existing `fetch-harbor-ca`/`fetch-argocd-ca`) or state plainly in §8 that it is operator-supplied and how. ⚠️ The obvious producer — fetching the VMCA over the very connection it is meant to anchor — is **TOFU**; decide that deliberately, do not let it appear by default. ⚠️ **AND IT IS WORSE THAN A DOC LOOP — there is a RUNTIME `die` naming a target that DOES NOT EXIST.** MEASURED 2026-08-05: `scripts/30-vks-login.sh:196` tells the operator *"Point VKS_CA_CERT_FILE at the Supervisor's CA (**make vks-ca**, or ask the platform team for ...)"* — and `command grep -nE '^vks-ca:' Makefile` returns **NOTHING**. An operator who hits that error and follows its instruction gets `make: *** No rule to make target vks-ca`. That is strictly worse than the §8 loop, which at least fails silently. **Fix it in the same change**, and note the class: no gate caught a `make <target>` string inside a `die` — `check-doc-make-targets` scans DOCS, not runtime messages; whether to extend it to scripts is its own call. ⚠️ **The B81 adversary round was DISPATCHED and STOPPED mid-run at the handoff, so this finding is the only part of it that survived — the round still needs running before any fix is written.** Two things it must settle: (a) `fetch-ca.sh` is TOFU by its OWN comments (*"reduces to verify(X,X) -> OK, rc=0 for ANY self-signed cert, attacker's included"*), which Harbor and ArgoCD already accept — but this anchor carries the vSphere ADMIN password, so a convenient target nobody fingerprint-checks may make the posture WORSE than the honest gap; (b) whether the Supervisor kube-apiserver cert is issued by the SAME VMCA root `--ca-certificate` wants, or a different intermediate — if different, fetching against `SUPERVISOR_HOST` writes the WRONG anchor and it would still "verify" by construction. |
| **B78** | 🔴 **§4a/§4b CONSUMER — the script that creates the namespace + guest cluster.** Blocked on B64. |
| **B82-a** | 🔵 **The install half IS automatable — `nested-vsphere-lab` already installs Supervisor Services over the vCenter REST API, no web UI.** MEASURED 2026-08-09 (`grep` of that repo's Makefile): `services` (*"Register + install Supervisor Services over the vCenter REST API (no web UI)"*), `services-remove SERVICE=<id> CONFIRM=yes` (uninstall + deregister), `services-uninstall`, `services-status`, `services-deactivate/activate/deregister`, plus `istio-uninstall WL_NS= WL_GC= CONFIRM=yes` and `namespace-delete`. So scenario-1 Steps 2/3's INSTALL half — which every walk to date has skipped because the lab already had Harbor and ArgoCD (both Supervisor Service namespaces measured **19 h old** and carrying **no** `vks-airgap-cicd.local/owned-by` label) — can be exercised end to end: remove the services, install them from this repo, verify. ⚠️ Two things to settle first: (a) whether this repo should DEPEND on the lab repo (it must not — borrow the REST call shape, do not shell out to another repo's Makefile); (b) the data-values files carry credentials (`supervisor-service-harbor-data-values-*.yml` sets the admin password), so whatever we build inherits the secret contract (`umask 077`, never argv, never `.env`). Owner 2026-08-09. |
| **B82** | 🔴 **Install the Supervisor Services (Harbor, ArgoCD) from this repo — Steps 2 and 3's install half, which NOTHING here exercises.** MEASURED 2026-08-09 walking scenario-1 on a bare photon jump box: of the 37 files in the operator's Broadcom folder, the jump box consumes **5** (the VCF CLI tarball, the plugin bundle, argocd-cli) and the other **~30 are vCenter-side Supervisor Service definitions** (`supervisor-service-harbor-*.yml`, `supervisor-service-argocd-*.yml`, contour, extdns, vks, plus their `*-data-values-*.yml`). Every walk to date has SKIPPED that half because the lab already had Harbor and ArgoCD installed, so the runbook's Steps 2/3 install path has **never been executed by anything in this repo**. Owner 2026-08-09: this is the likely next step, and the mechanism can be borrowed from `AndriyKalashnykov/nested-vsphere-lab` — but **minimally relevant to this repo**: take the service-install call shape, not that repo's lab-provisioning scope. ⚠️ Before designing it, settle two things that decide whether it belongs here at all: (a) installing a Supervisor Service is a **vCenter-admin** action, and scenario-1's own Step 1b already forks on the reader NOT being an admin — so this may be an admin-only sub-path, not a step every reader runs; (b) the data-values files carry **credentials** (`supervisor-service-harbor-data-values-*.yml` sets the admin password), so a target that applies them inherits the secret-handling contract (`umask 077`, never argv, never `.env`). |
| **B83** | 🟠 **`27-harbor-ca-from-cluster.sh` reads the wrong Secret, and accepts a LEAF as a trust anchor.** Now the FALLBACK (scenario-1 §6 leads with `curl .../api/v2.0/systeminfo/getcert`, MEASURED 2026-08-09: http 200, 1155 B, `CN = Harbor CA`, sha256 identical to the trusted CA — no login, no Secret read). Findings from `adversary-security-secrets`, all measured: **(a)** the script reads `harbor-ca-key-pair`, whose response also carries `tls.key` — **the CA's signing key**, which mints a valid cert for ANY name every consumer trusts, offline, until 2036. `harbor-tls`'s `ca.crt` is byte-identical (measured, `cmp` identical) and its `tls.key` is only the leaf's. ⚠️ **Settle first**: if `harbor-tls`'s `tls.crt` == its `ca.crt`, that secret is ALSO a CA keypair and the swap is cosmetic — one `openssl x509 -noout -subject -issuer` decides it. **(b)** `harbor-tls` does NOT reliably carry `ca.crt`: `kubectl create secret tls` produces only `tls.crt`/`tls.key` (measured), and **`06-install-harbor.sh:202` creates this repo's own `harbor-tls` that way** — so prefer a LADDER (the `tkg.tanzu.vmware.com/tkg-registry-additional-ca-cert` Cluster annotation from B70, which carries NO key material at all → `harbor-tls` → `harbor-ca-key-pair` with the 🔴 warning printed at point of use), never a hardcoded name. **(c)** line 64 only checks the blob PARSES as a certificate — it accepts a **leaf**. Harmless today only because `ca.crt == tls.crt` in the keypair secret; reading `harbor-tls` makes a one-token jsonpath slip install a leaf as the anchor and exit 0. Add `subject == issuer` + `basicConstraints CA:TRUE`. **(d)** `scripts/test-*.sh` has **ZERO** coverage of this script. **(e)** three other sites name the secret, incl. `fetch-ca.sh`'s operator-facing `die` (shared with ArgoCD — keep it generic). |

| **B84** | 🔴 **The four Broadcom portal links in `docs/scenario-1.md` land on an EMPTY file list — MEASURED in a logged-in browser 2026-08-09.** Navigating the Harbor link (`servicePk=542081`) while authenticated renders the page fine and shows **"No data found"**: there is a `Select Release List` combobox that starts **blank**, and no files appear until a release is chosen. So the download table merged in #498 gives the reader the *page* but not the step that reveals the *files* — a reader who clicks it concludes the artifact does not exist. ⚠️ Only the Harbor link was opened; the other three (`538499` ArgoCD, `542815` VCF CLI/Plugins) are **UNVERIFIED** and presumed identical, which is an inference, not a measurement. Note the irony: the notes directly beneath that table already say *"a `release=` in the URL is ignored — use the on-page selector"*, so the trap is documented three lines from the table it breaks. **Fix:** state the selector step in the table's preamble (one sentence: "each link opens a page whose Release list starts empty — pick your release before the files appear"), and open the remaining three to confirm the behaviour is uniform. Downloading was blocked on a separate issue: the download icons require ticking "I agree to the Terms and Conditions", which is accepting an agreement on the operator's behalf. |
| **B85** | 🔴 **`docs/scenario-1.md` Steps 2 and 3 have no "already installed → skip" branch, and collide with any Supervisor built by the sibling `nested-vsphere-lab`.** That repo's `make services` installs Harbor **and** ArgoCD as Supervisor Services — its `supervisor-service.sh` registers `argocd-service.vsphere.vmware.com 1.1.0-25100889`, the **same service at the same version** as the YAML our download table tells the reader to fetch. A reader on such a lab opens vCenter → Supervisor Services → Add New Service and finds it already there, with no guidance on what to do. **This is the COMMON case, not an edge one:** the runbook's own premise is *"you have a Supervisor endpoint, a login and a password"* — a Supervisor someone else built, where these services are frequently pre-installed. **The doc already knows the pattern** — Step 1b does exactly this (*"If you already have one — put its name in `VKS_NAMESPACE`, then go to Step 2"*); Steps 2 and 3 simply never got it. **Fix:** an "already installed" branch on each, telling the reader to take the FQDN/storage-class from the running instance and set the same `.env` values without installing. That also demotes **three of the six** download-table rows from *required* to *required only if you are installing*, which is what makes the table honest. ⚠️ **What happens if you install over an existing service is UNMEASURED** — Add New Service with a duplicate could no-op, error, or offer an upgrade. Uninstall targets exist for a clean test: lab `make services-uninstall SERVICE=<id> CONFIRM=yes` (destroys data; `services-deactivate` only *blocks* installs, it does not remove) and this repo's `make uninstall-all`. |
| **B92** | 🔴 **`_cp_endpoint_check` never runs on the SKIP-CREATE path, and the 30-minute burn it prevents lives in a DIFFERENT script.** `25-vks-cluster-create.sh:78` (`ALREADY EXISTS — not re-applying`, exit 0) returns before the gate at :182, so a reader whose cluster already exists is not warned about a stale `spec.controlPlaneEndpoint`. ⚠️ **The obvious fix — gate 25's skip path — is REFUTED as the primary fix** (`vks-adversary`, 2026-08-12): `docs/scenario-1.md:402` tells that exact persona to SKIP `make vks-cluster-create`, so a gate there reaches nobody in that flow, and the walk cannot trigger it either (`walk-matrix.sh:68` rotates the cluster name per invocation, so `adv == lb` on the first read). **The burn is in `26-vks-cluster-status.sh`:** `endpoint_report` is PRINTS-ONLY by its own comment (:113) and the `WAIT_SECONDS > 0` branch enters its loop **without consulting the endpoint at all** — it only reports after the full budget is spent. **Fix:** ~6 lines in 26's wait branch, immediately before `end=$((SECONDS + WAIT_SECONDS))` — read `adv` and `lb`; both readable and different → print the existing remedy and exit 1; either empty → fall through unchanged (no false-RED path). Covers create, skip-create, and never-ran-create. The 25 skip-path gate is optional afterwards; if added, expect +90 s on rows 2/4 whenever the VirtualMachineService name does not resolve. |
| **B93** | 🔴 **`READY_TIMEOUT_SECONDS` is INERT from the command line, and commenting it in `.env.example` is NOT a one-liner because the code-side defaults DISAGREE.** MEASURED (`vks-adversary`, 2026-08-12): `READY_TIMEOUT_SECONDS=600 make …` → `load_env` → **300**. It ships uncommented at `.env.example:1292` and is absent from `load_env`'s snapshot list (`lib/os.sh:492`) and from `check-env-clobber.sh:91`'s `SELECTORS` — this repo's own clobber class, uncovered by its own gate. **But the obvious fix changes behaviour**: MEASURED, the scripts read three different defaults — `${READY_TIMEOUT_SECONDS:-300}`, `:-600`, `:-90` (plus bare `$READY_TIMEOUT_SECONDS` refs) — so commenting the line makes each script fall back to its OWN number instead of the 300 they all get today. Order: unify the code-side defaults (or make every site explicit), THEN comment it, THEN add it to the clobber gate's recognizer. Same question for its siblings `POLL_INTERVAL_SECONDS` (2 defaults, 9 bare refs) and `CURL_MAX_TIME_SECONDS` (3 defaults, 4 bare refs) — fix the class, not the instance. Documented meanwhile in `docs/scenario-1.md` Step 12 as ".env only". |
| **B94** | 🟠 **The doc's own example for `VKS_STORAGE_POLICY` is the inversion `04-vsphere-namespace.sh:52` warns against.** `docs/scenario-1.md:191` offers `vsan-default-storage-policy` — the lowercased-dashed **class** form of vCenter's stock policy name *"vSAN Default Storage Policy"* — while `04-vsphere-namespace.sh:27` consumes the variable as the vCenter policy **NAME** (`.env.example:1193` confirms) and :52 says in as many words that the class is the policy lowercased with dashes and *"that is NOT invertible"*. Irrelevant to a single-host VMFS lab (`wcp-vmfs` is a genuine policy name there) and self-correcting on a vSAN one (the create lists every available policy). ⚠️ **UNVERIFIED — do not "fix" it by guessing the capitalisation**: settled by one look at a vSAN lab's Policies and Profiles page. The prior review's *stronger* claim — that Step 2 prescribes deriving the policy from the class — was checked and is **stale/false**: line 191 routes to `make vsphere-namespace` (which prints the policy by name) and to the vCenter UI, and no line in the document derives one from the other. |
| **B95** | 🟠 **SUSPECT, same skip-when-it-should-reconcile class as B96's two confirmed members: `71-argocd-register-guest.sh:70` matches an existing registration by the guest API URL.** If a guest cluster is RECREATED AT THE SAME API ADDRESS while ArgoCD persists (a persistent-Supervisor lab — not this one, where each lab gets its own ArgoCD), the stored ServiceAccount token and CA in the ArgoCD Cluster Secret are STALE and this line accepts them as "already registered". `lib/argocd.sh:110-125` records that this exact line already produced one wrong-cluster deploy. **Fix:** validate the stored Secret's token against the guest before accepting the registration — the pattern `50-seed-gitea-repos.sh` already uses for the CI token (validate against the live system, re-mint on failure). ⚠️ Not reachable in the walk, so a green 4/4 says nothing about it. |
| **B96** | 🟠 **THE CLASS: 16 sites skip-on-existence; 2 were confirmed defects and 1 is suspect (B95).** Enumerated by `vks-adversary` 2026-08-12 under the new mandatory class-denominator clause — the first finding it produced was a sibling I had not asked about. Both confirmed members are fixed (the Gitea webhook secret + the Gitea admin password, PR #582). **The other 13 are CORRECT**, and four are the patterns to copy when fixing the rest: `50-seed-gitea-repos.sh` ~185 (CI token — validate against the live system, re-mint on failure), `22-harbor-robot.sh:130` (explicit two-case die), `50-seed`'s `push_repo` (`git push -f` reconciles content rather than skipping), `25-vks-cluster-create.sh:75` (loud refuse-to-clobber). **The discriminator that makes a skip a DEFECT:** the thing being skipped carries a LOCALLY-GENERATED value (a token, a password) that the other side regenerates independently — then "already present" means "diverged", not "done". A skip over content the operator owns is fine. |
| **B97** | 🟠 **`31-fetch-argocd-kubeconfig.sh`'s `vcf context use` FAILS on every observed run — so Step 10's context selection is a measured no-op.** LAB-MEASURED across today's rows and 2026-08-11: `selecting the vSphere-Namespace context 'argocd-supervisor:<ns>'` is immediately followed by `could not select … — list them with: vcf context list` (a warn, not a die). What still works is the final `kubectl get deploy argocd-server`, against the file **Step 3 already wrote and verified**. So the open question is what `make fetch-argocd-kubeconfig` still accomplishes — diagnose the `use` failure, or drop the create/use cycle and keep the verification. ⚠️ Silver lining, and it is LATENT not fixed: because `use` fails, the collision it would otherwise cause (31 repointing `supervisor.kubeconfig`'s current-context away from `vks-cicd:<ns>`) is not realised. Also UNVERIFIED: whether `vcf context create` MERGES or TRUNCATES an existing kubeconfig — if it truncates, a working `use` would destroy the `vks-cicd` context. Settle with `vcf context list` before/after on a real box. |
| **B98** | 🟠 **`30-vks-login.sh:406` publishes ARGOCD_KUBECONFIG only `if [ -z ... ]`, so a STALE path survives a lab rebuild forever.** The guard is emptiness, not existence — once any value is in `.env`/`.env.state`, 30 never re-publishes, and MEASURED `is_placeholder '/nonexistent/stale-lab.kubeconfig'` returns NOT-placeholder, so every non-destructive publisher in the repo would preserve it too. Same class as the `~/.config/vcf` store that survived a lab rebuild and blocked `vcf context create` against a dead endpoint (`docs/scenario-1-notes.md:208`). **Fix:** key the guard on the file EXISTING (and belonging to this lab), not on the variable being empty — `is_placeholder` is the right test for a PASSWORD and structurally incapable of asking the question that matters for a PATH. ⚠️ Do NOT close this by adding a second publisher in 31: that would ARM this very guard (refuted, `vks-adversary` 2026-08-12). Residual it does close: a flow where 30's `vcf` arm never runs (VKS_AUTH_METHOD=kubeconfig from the start, or scenario 2) — fix that in 30's OTHER arms, one sink (`.env.state`), one value. |
| **B99** | 🟠 **The Gitea webhook-rotation PROBE is proven and uncommitted, so it EXPIRES.** A throwaway `gitea/gitea:1.27.1-rootless` + a python receiver + an HMAC check measured, 2026-08-12, the two upstream facts `50-seed-gitea-repos.sh`'s delete+recreate rests on: create-with-T1 signs T1; **PATCH-with-T2 returns http 200 and STILL signs T1** (a silent no-op — this refuted the original design before it shipped); DELETE+POST-with-T2 signs T2, leaving exactly one hook. ⚠️ **Do NOT promote it to a gate** — a `vks-adversary` round refuted that: `50-seed` needs a kubeconfig, `kubectl exec deploy/gitea` and `port-forward`, so a standalone container executes **zero lines of `seed_app`** and would stay 4/4 green with the whole reconcile block deleted. Commit it as `scripts/probe-gitea-webhook-secret.sh` — help text "PROBE (needs an engine + Docker Hub; NOT a gate)" — **nonce-bound** (the "PATCH still signs T1" assertion is byte-identical to a stale signature when NO delivery arrives, so silence must be RED), printing `deliveries N/3` + the image ref it actually ran. Run it on a Gitea bump; its output is a graded provenance note. Also UNVERIFIED: whether `POST /hooks/{id}/tests` delivers through the same code path as a real push — if not, the probe measures a path the product never uses (5 minutes in `services/webhook/` at the pinned tag settles it). |
| **B100** | 🟠 **`e2e-cross-cluster.sh:139` pins `gitea/gitea:1.26.4-rootless` while `images/images.txt:50` says `1.27.1-rootless` — already drifted, and the alignment gate is STRUCTURALLY BLIND to it.** `check-image-alignment.sh` matches only `${HARBOR_URL}/${HARBOR_INFRA_PROJECT}/<repo>:<tag>` refs in `k8s/` and `scripts/`; a bare `docker.io` ref scores **0**. Found 2026-08-12 by an adversary asked a different question. Fix: derive the tag from `images/images.txt` at runtime (the pattern the new `test-gitea-hook-ids` uses), or widen the gate's second arm to bare refs the repo owns — the second is the one that closes the class rather than this instance. |
| **B101** | 🟠 **SEVEN merged code paths rest on an UNTESTED claim about a third party ("we do X because Y cannot do Z"); 4 of 11 are guarded.** Enumerated by `vks-adversary` 2026-08-12 (phrase-based grep, so a LOWER BOUND). Unguarded: `50-seed:327` (Gitea PATCH cannot set config.secret) · `50-seed:332` (Gitea never returns the secret) · `24-vks-k8s-version.sh:72` (the VKS selector is a PREFIX matcher) · `30-vks-login.sh:310` (the `vcf` CLI leaves `.kubeconfigpath` null) · `71-argocd-register-guest.sh:70` (B95) · `harbor.md:23` (VKS does not watch the user-trusted-ca-secret) · `22-harbor-robot.sh:163` (partial). **The repo already has the right pattern and it is used 3×** — `verify-gateway-image`, `mirror-verify`, `check-pod-inject-label` all assert the CONSEQUENCE over OUR OWN artifacts, and **no test here runs a third-party service to assert what it does**. Guard the rest that way, or record the fact as graded provenance and accept it; do not start running third parties in unit tests. |
| **B102** | 🟠 **11 of 26 `Expect:` lines in scenario-1 are NOT CHECKABLE by the walk, and that reservoir hides at least one real defect.** MEASURED (`vks-adversary`, 2026-08-12, with a simulator validated against the live run — it reproduced `15 blocks CHECKABLE` exactly): the PYX matcher drops a literal carrying a `<placeholder>` or an ellipsis, so Step 2's `created vSphere Namespace '<name>' …` was **structurally unreportable** — the walk could never have told us it names only one of two success branches. Fixed for the human this pass; the other 10 are unaudited. Audit them one by one and either (a) reword to a literal the walk can match, or (b) record explicitly that the claim is human-only, so `15 of 26 CHECKABLE` stops reading as coverage. ⚠️ Two kinds hide in there and only one is fixable: a placeholder-bearing literal can be reworded; a FILE-EXISTENCE claim (`./secrets/supervisor.kubeconfig`) can never appear in stdout and is permanently unmatchable — say which is which. |
| **B103** | 🟠 **`/tmp/walk/ROWS12.log` is STALE and the handoff's own verdict recipe greps it.** MEASURED 2026-08-12: that file's last write is 01:02 (the previous night's REFUSED run, `MATRIX_RC=2`), while today's run writes only the per-row `MATRIX-row*.log` files. Anyone following the handoff's "the verdict" command reads LAST NIGHT'S FAILURE as today's result — the stale-evidence class this repo has already been bitten by twice (a monitor counting previous runs' logs as green, and a `git archive HEAD` proof measuring the tree without the fix). Fix: have the driver write ONE verdict file per invocation, named with the run id, and have the handoff cite that — never a fixed path a previous run also wrote. |
| **B104** | 🟠 **NINE more operator-path network fetches have NO retry — this pass fixed 3 of 12 (25%), and the largest untreated group is on the DEFAULT ingress path and inside `make mirror`.** Enumerated by `adversary-bash-git-cli` 2026-08-13 after a `curl: (52) Empty reply from server` on tkn killed `deps-prereqs`: `00-install-prereqs.sh:88` (the OS floor `pkg_install`, BARE under `set -e` — a tdnf/apt blip kills deps outright) · `Makefile:212` (`curl https://mise.run \| sh` — the mise BOOTSTRAP itself) · `06-install-harbor.sh:79-80` (helm repo add+update) · `46-install-istio.sh:85,89,104` (helm repo add+update+pull — the DEFAULT ingress) · `10-mirror-pull.sh:141-144` (helm repo add+update+pull, inside `make mirror`) · `walkbox.sh:152` (cloud image) · `fetch-supervisor-ca.sh:33` (vmca.zip — the probe at :62 retries, the DOWNLOAD does not). Already retried: `http_get_retry` at `10-mirror-pull.sh:75` + `07-install-argocd.sh:111`, `mirror_retry` at `:108`, and deps-mise's one manual retry. **Fixing 3 and stopping is the enumerated-list rot** — the next transient lands on `helm repo add` and reads as a brand-new class. ⚠️ `helm` has no `--retry`, so those six need a wrapper, not a flag; and any use of `http_get_retry` for a LARGE artifact must override `HTTP_GET_MAX_TIME_SECONDS` (its 60s default demands 33 Mbit/s sustained for the 238 MiB argocd — measured). |
| **B105** | 🟠 **Step 13's port-forward fallback covers ONE of the repo's TWO apps, and omits Tekton.** `apps/registry.tsv` carries `javawebapp` AND `gowebapp`; scenario-1's Step 13 gives port-forwards for `gitea` and `javawebapp` only. Step 12 (ingress) is explicitly OPTIONAL, so a reader who skips it has no route to `gowebapp` or the Tekton dashboard at all. Both commands that ARE given were verified correct (`svc/gitea-http:3000`, `svc/javawebapp:80` in ns `javawebapp`). Fix: add the missing forwards, or state the pattern generalises per app and name where the app list lives — the second is the 1→N-registry-correct shape, since a hand-written list of apps in a doc is the same enumerated rot the registry exists to remove. |
| **B106** | 🟠 **~130 statements in scenario-1.md are fixed by content elsewhere; 10 are MEASURED rotted, and the densest site is not the most consequential one.** Enumerated by `vks-adversary` 2026-08-13: summary-table counts 7 (4 rotted — fixed this pass) · inline durations vs the doc's OWN Timings table 7 (2 rotted — fixed) · doc↔code behavioural claims 3 (2 rotted — fixed) · which-file-a-value-lands-in 5 (1 rotted — fixed) · make-target names 39 (0) · in-document anchors 16 (0) · `Step N` cross-refs 48 (0 dangling, semantics spot-checked only) · port-forward object names 4 (0) · apps named by hand 1 of 2 (B105). ⚠️ **A GATE FOR THE COUNTS WAS DESIGNED AND REFUTED — do not rebuild it:** a `claim == table-row-count` gate is mechanically sound and RED-provable, and it scores the WORST row (6, where the operator types zero) **GREEN** while pushing rows 9/10 toward "set 0 values" instead of deletion — a gate whose only remedy degrades the artifact. The right invariant ("keys the operator TYPES") needs a classifier that was measured wrong 5 of 6, including conditionals like "VKS_SSO_DOMAIN is required iff VKS_USERNAME lacks an @". Discipline, labelled as discipline. **Unique to this document**: scenario-2.md and README.md carry ZERO counted claims. |
| **B107** | 🔴 **~42 of 48 `Step N` cross-references in scenario-1.md are semantically UNCHECKED, and Step 14's teardown claims are UNVERIFIED.** None DANGLE (all resolve to a real step), and ~6 were spot-checked correct — but "Step 7 tells you X" is only true if Step 7 still says X, and step numbering shifted when 8.5 was inserted. Separately, `98-uninstall-all.sh`'s label-scoping and its "prints what it left alone" claim were never read against the doc. Both are cheap to settle and neither is walk-checkable: the teardown block is SKIPPED by every row by design ("would destroy the lab mid-walk"), so it has NEVER been executed by any run. |
**The rest of the real-lab unknowns** — the Supervisor topology, the `vcf` auth flow, tenant RBAC into
the ArgoCD namespace, and whether the Supervisor can route to a guest LoadBalancer VIP — are tracked in
[`docs/lab-validation-plan.md`](docs/lab-validation-plan.md), in a better form than a backlog line: each
is a numbered step with its command, its expected observable, and what to send back.

## B84 — `export VAR=x; make <target>` is SILENTLY defeated by `-include .env`

MEASURED 2026-08-09. GNU make lets a **makefile assignment beat the environment**, and the
Makefile's `-include .env` is exactly that. So:

| form | effective value |
|---|---|
| `export VKS_NAMESPACE=wld03; make vsphere-namespace` | **`lab`** (from `.env`) |
| `make vsphere-namespace VKS_NAMESPACE=wld03` | `wld03` |

Only the command-line form wins, and make then EXPORTS its own value to the recipe, so
`load_env`'s selector snapshot cannot rescue it — the script never sees the operator's value.

scenario-1 already uses the command-line form, so no documented instruction is wrong. The
hazard is the natural `export`-then-`make` habit, which fails silently. It caught me while
testing `make vsphere-namespace`: it operated on `lab` while I believed it was creating
`wld03`. Running the script DIRECTLY (bypassing make) used the override correctly.

Options: document it beside the `.env` clobber rule; or have `load_env` prefer a
`VKS_*_OVERRIDE`-style key. NOT yet decided - recorded so the next person does not
re-discover it by having a target act on the wrong namespace.

## B86 — `values-provenance` can never read `DISCOVERED` on the real-lab path

**HIGH.** `state_stamp` is called from exactly two places: `scripts/05-kind-up.sh:156` (KinD
only) and `Makefile:264` (a manual `make state-stamp`). **No real-lab install script calls
it**, and neither `scenario-1.md` nor `scenario-2.md` mentions `state-stamp`. So every
real-lab `make creds-show` — including one run minutes after a clean install — prints
`STORED` plus the five-line *"an overlay SURVIVES A REBUILD, so a password or an IP here may
be from a lab that no longer exists"* banner.

Reproduced on this box 2026-08-10: `make creds-show` printed exactly that banner, and
`scenario-1.md:628` promises the reader a state (`DISCOVERED`) the documented path cannot
reach.

**Why it matters, and it is not cosmetic:** it is alarm fatigue on the one command whose job
is to tell the operator what is true. A banner that fires on every correct install trains
them to ignore it — and then the case it exists for (a genuinely stale overlay after a lab
rebuild) is ignored too, because **the two are byte-identical**.

⚠️ **Design change — `vks-adversary` BEFORE implementing.** `scripts/05-kind-up.sh:148-155`
documents the trap in detail: `state_stamp` reads the AMBIENT `KUBECONFIG`, so stamping at
the wrong moment on a lab box can archive a live sink and **destroy the only copy of the
generated passwords**. It has to go where `KUBECONFIG` already points at the guest cluster.

## B87 — `test-creds-show.sh` has ZERO coverage of the real-lab provenance path

**HIGH.** Its three states are: no overlay, and two overlays that **both** set
`VKS_STATE_KIND=1` (`scripts/test-creds-show.sh:67,87`). That flag short-circuits at
`scripts/creds.sh:217`, *before* the stamp comparison at `:218` — so the `STORED` branch,
the only one a real lab ever reaches, is never rendered, and the `DISCOVERED` assertion at
`:105` passes through the KinD shortcut rather than the logic under test.

The gate written to keep `creds-show` honest *"in every state"* is blind to the state the
operator is actually in. Fix: two more states — unstamped non-KinD (expect `STORED`), and a
matching `VKS_STATE_SERVER` (expect `DISCOVERED`). Fully offline: `state_kubeconfig_server`
parses the file and never dials (`scripts/lib/state.sh:38-39`).

Both found by an idea-round `vks-adversary` on 2026-08-10 that **refuted** the proposed
`make creds-check` (measured 67% false-RED — nothing in this repo writes `/etc/hosts`, so
`*.vks.local` is NXDOMAIN by design, and §11 prints two port-forward URLs live only while a
port-forward runs). The reachability question it was meant to answer is already answered by
`verify-ingress` (Host header, no DNS, per-host body marker), now cited in §10.

## B88 — the trivy-DB cache was rejected on a measurement that is 2.5x too small

`.github/workflows/ci.yml:147` records the 2026-07-14 decision to DROP a trivy-DB cache,
reviewed by java-adversary + docker-adversary, on the grounds that it *"would silently serve
a DB up to trivy's 24h client interval, missing same-day CVEs, **for ~10s**"*.

The correctness objection stands and this row does NOT propose overturning it. What is wrong
is the number it was weighed against. MEASURED on run 31451956799, job `static-check`:

    02:20:04  trivy-fs: scanning javawebapp -> .../javawebapp-0.1.0.jar
    02:20:32  (first Report Summary)                                    <- 27.5s
    02:20:32  trivy-fs: scanning gowebapp -> /tmp/.../gowebapp
    02:20:32  trivy-fs: OK - 2 app artifact(s) scanned                  <-  0.07s

Same process family, same machine, same run: the FIRST scan is 27.5s and the SECOND is 0.07s.
The delta is the vulnerability-DB download. So the trade is **~25s, not ~10s** — 15% of the
164s `make static-check` step, against a staleness bound trivy's own 24h client interval
already permits by default.

Whoever revisits it should also weigh a DATE-KEYED cache (`trivy-db-<YYYY-MM-DD>` with a
`trivy-db-` restore-key), which bounds staleness to <24h explicitly rather than relying on
trivy's internal interval — that option does not appear in the recorded decision.

⚠️ Do NOT re-add it without a fresh adversary round: the original rejection was a CORRECTNESS
call about a security gate, and a bigger saving is not by itself a reason to overturn one.
