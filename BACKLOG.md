# Backlog — vks-airgap-cicd

> **Moved out of `CLAUDE.md` on 2026-07-22, and this file is deliberately NOT auto-loaded.**
>
> `CLAUDE.md` is injected into every session in this repo **and into every subagent** — measured:
> one `Read` in a subagent re-injected it at **141,071 B ≈ 48.6k tokens**, on top of the ~311k-token
> always-loaded rules corpus. This backlog was **85,504 B — 61% of that file** — and it is *task
> state*, not instructions: nothing here changes how an agent approaches a problem, which is the
> test for what belongs in `CLAUDE.md`.
>
> `CLAUDE.md`'s own handoff rule already says **"Facts → the docs. Tasks → the Backlog."** This
> makes that literal.
>
> **Read this file when you pick up work** — a handful of `scripts/*.sh` cite row IDs (`B22`, `B26`,
> `B42`, `B45`, `B50`) as provenance for why a given gate exists, and those citations resolve here:
> most as open rows, and `B42` as a *closed* one recorded in the session-3 note below. A citation
> that lands on a closed row is still resolved — it tells you the gate's reason shipped.

## 🟢 B499 — `kind` wrote the AMBIENT `$KUBECONFIG`, a LAB slot on every lab box 🟢 closed

**CLOSED by #1050.** Kept because the *gate design* was refuted once and should not be rebuilt.

`kind` writes `$KUBECONFIG` when `--kubeconfig` is absent (its help: *"sets kubeconfig path instead of
`$KUBECONFIG` or `$HOME/.kube/config`"*). `load_env` applies the real-lab path as a CODE default
(`lib/os.sh:692`), so on any lab box that value **is a lab slot** — and after the scenario docs' own
`export KUBECONFIG=./secrets/supervisor.kubeconfig` (`scenario-1.md:322`, `scenario-2.md:148`) it is
the **Supervisor**.

### What it actually does — MEASURED, kind v0.32.0

kind **MERGES**; it does not truncate, so **no credential is lost**. The damage is a **context hijack**:

| | effect |
|---|---|
| `kind create` | adds its entries, repoints `current-context` to `kind-<name>` |
| `kind delete` | removes its own entries; if `current-context` was the kind one, **deletes the key** |
| `kind delete`, file with no `kind-*` entries | **byte-identical** |

That last row is why fixing CREATE defuses DELETE.

### Blast radius is NOT symmetric — the worst case is the TENANT

| | hits | recovery |
|---|---|---|
| scenario-1 | `supervisor.kubeconfig` or `secrets/<cluster>.kubeconfig` | `make vks-login` — re-fetchable |
| **scenario-2** | **`secrets/vks.kubeconfig`** — the credential the platform team HANDED them | **none.** RULE ZERO-A0: a tenant cannot self-service it. A support ticket. |

Observed on the dev box: `secrets/vks.kubeconfig` read `server: https://127.0.0.1:36653`, and
`~/.kube/config` had lost `current-context` and carried stale `kind-cc-hub`/`kind-cc-guest` from
`e2e-cross-cluster.sh`.

### 🔴 The obvious gate was BUILT and REFUTED — do not rebuild it

A gate that greps each call for `--kubeconfig` is **structurally blind to this bug**:
`05-kind-up.sh` assembles the subcommand in an array (`create_args=(create cluster …)`;
`run kind "${create_args[@]}"`), so the string `kind create` never appears. Measured: it returned an
**identical clean 6-of-6 on the fixed AND the unfixed tree** — it would have shipped green over the
defect it was written for.

`check-kind-kubeconfig.sh` therefore keys on **ORDERING**: each writing `kind` invocation must carry
`--kubeconfig` **or** be preceded by an `export KUBECONFIG=` in the same file. RED-proven both ways —
against `origin/main` it names all 7 sites; removing the early export re-REDs it.

### Why it took a session to find

It was recorded **nowhere**: 0 mentions in `CLAUDE.md`, 0 gates over `kind` invocations, and none of
the 10 BACKLOG kubeconfig hits was this bug. That absence was the real defect; the gate plus one
`CLAUDE.md` DISTRUST row is the fix for it.

---

## 🔵 B485 — an air-gapped "depot" WITHOUT VCF Operations: plausible, untested, and worth one experiment 🔵 idea

**Grade: NOT-ESTABLISHED.** Everything below the "what is settled" table is a *hypothesis*. Nothing in
it has been run. Filed so the context is not lost, not as a plan of record.

### Why this exists

Broadcom's [air-gapped VCF 9.1 guide](https://github.com/vmware/vsphere-supervisor/blob/main/airgapped/air-gapped-vcf91.md)
relocates VKS Standard Packages (istio among them) into the Supervisor's **Software Depot**, after
which package bundles resolve from `depot.kube-system.svc` instead of `projects.packages.broadcom.com`.
That matters to us for exactly one reason: `43-install-istio-package.sh`'s air-gap check accepts only
`localhost:*`, `127.0.0.1:*` and `$HARBOR_URL*`, so a correctly relocated lab is **REFUSED** — see
[[B484]] F5. We cannot currently reproduce a relocated lab to settle it.

### What is settled (measured 2026-08-26 on this lab, vCenter 9.1 build 25629530)

| | |
|---|---|
| **`depot.kube-system.svc`** — what the non-legacy service YAMLs reference | needs **VCF Operations' Software Depot**. §5c drives it with `toggle_software_depot_oci_image_upload.sh --vsp-host <vsp-fqdn> --admin-username admin@vsp.local`. This lab builds **no VCF Operations**, `kubectl -n kube-system get svc depot` is **NotFound**, and VCF Operations is **out of scope** (owner, 2026-08-26). So the VCF Depot itself: **no**. |
| **`docker-registry.kube-system.svc:5000`** | **ALREADY RUNNING** on the Supervisor — pod `docker-registry-<id>`, image `vmware/docker-registry:2.8.3.25018815`, ClusterIP `10.96.1.158:5000`. This is what `localhost:5000` in our allowlist refers to. |
| the `supervisor-management-proxy-0.4.1` YAML we hold in `VCF_CLI_SRC_DIR` | **NOT the depot proxy.** Its data-values are `nsxManagers` / `nsxSvcLoadBalancerIP` — it is the **Antrea-NSX Adapter** proxy. We hold **zero** of the §7a pieces. |
| the artefact difference F5 turns on | the two shipped service YAMLs differ by **one line**, in exactly the field the check reads: `vsphere-kubernetes-service-legacy-3.7.1+v1.36.yaml` -> `projects.packages.broadcom.com/...`; `vsphere-kubernetes-service-3.7.1+v1.36.yaml` -> `depot.kube-system.svc/vcf/supervisor-service-vks/ga/3.7.1/...`. Both sha256 match the lab repo's pinned checksums. |

### The hypothesis

A depot is not magic: it is an OCI registry the Supervisor can reach, plus a package YAML pointing at
it. So relocate the Standard Packages bundle into a registry we control and repoint
`spec.template.spec.fetch[0].imgpkgBundle.image` — §7b's move with a different registry underneath.
Two candidates, **neither needing VCF Operations**:

1. **The Supervisor's own `docker-registry:5000`.** Architecturally the right answer — it is already
   the relocation target for bootstrap images, which is *why* the check whitelists `localhost:5000`.
   ⚠️ **UNPROBED**: this identity gets `pods/portforward: no` on the Supervisor, so its catalog could
   not be read and push-ability could not be tested. Whether anything but WCP may write to it is
   **NOT-ESTABLISHED**.
2. **Our Harbor** (`svc-harbor-*`, already a Supervisor Service) — reachable, writable, and we push to
   it every run. Lower fidelity to the vendor flow, far cheaper to try.

`oci_image_depot_migrator.py` is missing either way. Its documented job is download/upload/copy of
imgpkg bundles and **`imgpkg copy` runs inside it**, so a plain
`imgpkg copy --to-repo <registry>/<path>` should substitute. Untested.

### The experiment, if someone picks this up

Non-destructive and reversible — a copy into a registry we own, plus registering a NEW service version
(reversible with the lab repo's deregister path). Do NOT uninstall or overwrite the running one.

    # 1. relocate the bundle (from an internet-connected box)
    imgpkg copy -b projects.packages.broadcom.com/vsphere/supervisor/vks-standard-packages/<ver>/vks-standard-packages:<ver>             --to-repo <harbor>/<project>/vks-standard-packages
    # 2. take a copy of the legacy service YAML and repoint the ONE line
    #    spec.template.spec.fetch[0].imgpkgBundle.image -> <harbor>/<project>/...
    # 3. register that YAML as a new version (vCenter UI: Services -> Kubernetes Service ->
    #    ACTIONS -> Add New Version; the API 403s for a Core Service)
    # 4. install it on the Supervisor, then read what the guest's Package CRs actually say:
    kubectl get packages -A -o json | jq -r '.items[]|select(.spec.refName|test("istio"))
      |"\(.spec.version) \(.spec.template.spec.fetch[0].imgpkgBundle.image)"'

### What it would settle

- **[[B484]] F5** on real hardware instead of artefact-verification: does a relocated Package CR's
  `imgpkgBundle` field actually carry the relocated host? If YES the fix is "widen the allowlist"; if
  NO the check is **reading the wrong field entirely** and needs redesigning. That branch is currently
  unknowable here, and it is the single highest-value thing this experiment buys.
- Whether an air-gapped VKS package install is reachable for an operator who has **no VCF Operations**
  — which is this repo's entire audience per RULE ZERO-B.

### Explicitly NOT in scope

- Building VCF Operations into the lab (owner: out of scope, 2026-08-26).
- §7a's `manage-depot-image-proxy.sh` — needs vCenter **root SSH + `sshpass` + a Supervisor ID**, which
  is above Supervisor admin and above what a tenant can ever have.

## 🔵 B481 — REFUTED: do NOT wire the proxy-readiness check into `istio-preflight` 🔵 refuted

**Recorded so it is not rebuilt.** After [[B477]] it looks obvious that `make istio-preflight` should
call the readiness check that already exists (`istio_proxy_ready`, `lib/istio.sh`), because during
the outage preflight printed `PREFLIGHT OK`. An `adversary-k8s` idea round refuted it before anything
was written.

**F1 — it has ZERO discrimination, and false-REDs 100% of the time on its normal input.** Preflight
runs *before* attach, and in the incident the Gateway was created *at attach time*. So at preflight
time there is no proxy pod to be Ready — and `istio_proxy_ready` returns **1 for an empty pod set**:

| state at preflight time | result |
|---|---|
| healthy mesh, not yet attached (the normal case) | **1** |
| B477 mesh (istiod endpoints zeroed), not yet attached | **1** |

Identical. Downgrading `PREFLIGHT OK` on rc≠0 would redden every pre-install run and catch nothing —
including `make e2e-kind-istio-existing`, which runs preflight immediately before `install-ingress`
and is healthy by construction.

**F2 — it would not merely false-RED, it would CRASH preflight.** `ISTIO_GWAPI_NAMESPACE` and
`ISTIO_GATEWAY_NAME` are commented in `.env.example` and defaulted only inside a function preflight
never calls, and `48-istio-preflight.sh` sets `set -euo pipefail`. Measured:
`ISTIO_GWAPI_NAMESPACE: unbound variable`, rc=1.

**F3 — the istiod-endpoints variant is the design [[B477]] ALREADY refuted** (it needs
`istio-system`, which a tenant does not own — RULE ZERO-B — and false-REDs on canary/multi-revision
and ambient). Do not re-enter it sideways.

**F4 — the cost it targets is already fixed at the right place.** PR #995 made both wait loops fail
attach rather than publish a dead address, so B477 now costs one 300s timeout at attach, not 8×300s
at verify. And the "green preflight misled us" gap is already disclosed where the operator reads it,
at `98-verify-ingress.sh:180`.

### What to do instead

1. **Nothing on the gateway-api path.** At preflight time there is nothing in the tenant's own
   namespaces to observe. That is the honest answer, not a gap.
2. **If preflight should say more, make it a PRINTER, never a verdict change** — one line stating
   that it has *not* checked the data plane, true in every state, no RBAC cost, cannot false-RED.
3. **The genuinely better target is a DIFFERENT bug, on the CLASSIC path**: `lib/istio.sh:65` finds a
   Service with `:15021` but never checks its selector matches a pod. Unlike the refuted proposal
   this **does** discriminate at preflight time — the platform's gateway Service exists *before*
   attach, so "it has zero ready endpoints" is a real pre-install finding, in a namespace preflight
   already reads. Needs its own idea round; `rc=2` (could-not-ask) must degrade to a warning, since
   preflight's verdict is currently a two-way boolean with no third state.

## 🔴 B482 — `verify-gateway-image` WORKS, and can never run on the lab path 🔴 open

**Measured 2026-08-25.** The air-gap breakage in [[B477]] went unnoticed for hours even though this
repo ships a gate whose entire job is to catch it. The gate is not broken — it is **structurally
unable to run where the defect lives**:

| | |
|---|---|
| wired into | `Makefile:845` only — the **KinD** e2e (`e2e-kind`) |
| named in `docs/scenario-1.md` / `scenario-2.md` | **0 times** — the lab path never invokes it |
| the lab rows run | `INGRESS_CONTROLLER=istio-existing`, and `96-verify-gateway-image.sh:60` **SKIPs** in that mode |

So on the one platform where a mesh can silently reach a public registry, the check is absent twice
over: not called, and it would opt out if it were.

**It genuinely works.** Run by hand against the guest cluster in `istio` mode it exits **2** and
names every offender:

    FAIL  istio-system/istio-cni-node-{48c5l,g4xpv,p5kzg}      <- projects.packages.broadcom.com
    FAIL  istio-system/istio-support-55c844f4cb-bbqmj          <- projects.packages.broadcom.com
    FAIL  vks-ingress/vks-uis-istio-6955cdc9b7-6v4zx           <- projects.packages.broadcom.com
    ok    istio-system/istiod-...            <- harbor.env1.lab.test/cicd/istio/pilot:1.30.3
    ok    istio-ingress/istio-ingressgateway <- harbor.env1.lab.test/cicd/istio/proxyv2:1.30.3
    gateway image provenance: FAILED — 5 of 7 container image(s) did not come from harbor…

### The DENOMINATOR makes it sharper: the gate runs where the defect cannot occur

Measured 2026-08-25, the same gate, the same day, on both platforms:

| where it runs | images it checked | result |
|---|---|---|
| **KinD** — wired into `Makefile:845`, `INGRESS_CONTROLLER=istio` | **2** | `gateway image provenance: OK — all 2 running container image(s) came from 172.18.0.3` |
| **the lab** — never wired, and would skip in `istio-existing` | **7** | `FAILED — 5 of 7 container image(s) did not come from harbor…` ⚠️ *those five are a **deliberate, warned** `ISTIO_PACKAGE_ALLOW_REMOTE=1` override, not damage — see [[B477]]* |

KinD's mesh is a helm-only install, so it genuinely has **two** Istio containers — istiod and the
gateway proxy. There is no `istio-cni`, no `istio-support`, and no VKS package, so the mixed-registry
class this gate exists to catch **cannot arise there**. The lab has 7 containers precisely because the
package path added the extra components, and five of them came from a public registry.

So the gate's green is honest and worth almost nothing: it is measured over a denominator of 2, on
the one platform where the answer can only ever be "all ours". The place where the denominator is 7
and the answer is "5 of them are not" is the place it never runs.

⚠️ **Read the two columns for what they are.** The lab's five failures are the *recorded consequence
of an intentional override* ([[B477]]), so this table is **not** "the gate would have caught a live
defect". It is narrower and still worth the row: whatever the cause, the lab is the only platform
where this gate's answer can be anything other than trivially green — and it is the platform where
the gate never runs. A future genuine mixed-registry mesh would be equally invisible.

That is this portfolio's own rule — *a gate's trigger must cover its own inputs* — with numbers
attached, and it is why the fix is about WHERE the gate runs, not about making it stricter.

⚠️ **The `istio-existing` skip is defensible and is NOT simply a bug to delete.** Its stated reason
is sound: the mesh is the platform's, so its image hub is theirs to choose, and failing a tenant's
run over a decision the tenant cannot make would be wrong. But the skip is currently
**unconditional**, so it also excuses the case where *we* caused the mixture — which is exactly
[[B480]].

**Not yet idea-reviewed.** The shape of the fix is the open question, and the obvious one (make it
assert on `istio-existing` too) is refuted by the paragraph above. Candidates worth putting to a
round: report **without failing** on `istio-existing` (a printer, so a tenant sees the mixture and
can raise it with their platform team); or assert only over objects *we* created; or key the skip on
whether the namespace shows evidence of our own install rather than on the mode string.

## 🔴 B480 — the two Istio install paths ADOPT each other's objects, silently and permanently 🔴 open

**Measured 2026-08-25 on the live lab. This is reachable by running the DOCUMENTED commands in the
DOCUMENTED order — it is not lab damage.**

`44-install-ingress.sh:45` dispatches on `ISTIO_INSTALL_METHOD` (default `helm`) to either
`46-install-istio.sh` (helm charts, images from Harbor) or `43-install-istio-package.sh` (the VKS
Standard Package, via Carvel/kapp). **Neither checks whether the other already owns the namespace:**

| | greps for the other's ownership marker |
|---|---|
| `43-install-istio-package.sh` (package) | `sh.helm.release` / `helm list` / `helm status` → **0 hits** |
| `46-install-istio.sh` (helm) | `kapp` / `pkgi` / `packageinstall` → **0 hits** |
| `44-install-ingress.sh:45` | dispatches on the variable with **no state check at all** |

So `make install-ingress` (default helm) followed by
`make install-ingress ISTIO_INSTALL_METHOD=package` hands a helm-installed control plane to kapp,
which **adopts** it. Measured: **30 of the 50** objects kapp claimed carried helm's original
`creationTimestamp`. kapp then wants `kapp.k14s.io/app` inside istiod's Deployment `spec.selector`,
Kubernetes forbids it as immutable, and the reconcile **can never converge** — see [[B477]] for the
full mechanism and the resulting outage.

**It is not self-healing and not obviously reversible.** Removing the reconciler leaves the mesh
running but permanently mixed: measured, `istio-cni-node` and `istio-support` still pull from the
**public** `projects.packages.broadcom.com` (breaking the air-gap invariant `make
verify-gateway-image` exists to assert) at **1.28.5**, under an istiod from our Harbor at **1.30.3**.

**⚠️ It CANNOT be reproduced on KinD — measured 2026-08-25.** The local stand-in has none of the
machinery the package path needs:

    packageinstalls.packaging.carvel.dev       ABSENT
    packagerepositories.packaging.carvel.dev   ABSENT
    apps.kappctrl.k14s.io                      ABSENT

So `43-install-istio-package.sh` can never run there, no kapp adoption can occur, and KinD's mesh is
permanently helm-only (measured: exactly **2** Istio containers, which is also why the gate in
[[B482]] sees a denominator of 2 there). A behavioural RED for any guard therefore has to come from
**the lab** or from an **offline fixture** — the repo already does the latter for the sibling gate
(`make test-gateway-image` RED/GREEN-proves its classifier via fixtures, no cluster). Prefer the
fixture: it is the only form that can run in CI.

**Proposed guard — both directions, and it is offline-RED-provable:**

- `43` dies if any `sh.helm.release.v1.istiod.*` Secret exists in `$ISTIO_NAMESPACE`.
- `46` dies if any object in `$ISTIO_NAMESPACE` carries a `kapp.k14s.io/app` label.
- Each names the other path and the uninstall that would make the switch safe, rather than just refusing.

⚠️ **Not yet reviewed.** Per RULE ZERO this needs an idea round before it is built — in particular
whether a *deliberate* migration between paths is a supported workflow (in which case the answer is
an explicit `--force`/uninstall step, not a hard refusal), and whether the helm-side check is safe
for `INGRESS_CONTROLLER=istio-existing`, where a platform-owned mesh may legitimately be
kapp-managed and we must attach without installing anything.

## 🟡 B473 — the PRUNE is REFUTED (this repo shipped it once and it destroyed an operator's kubeconfig); the real gap was ONE offline check, now FIXED 🔴 open

**MEASURED 2026-08-25, on a real re-cut.** After `make destroy` + `make lab`, `secrets/` still held
**14 files and 2 directories** from labs that no longer exist:

    vcenter-ca.pem        Aug 21   CN = CA, DC = vsphere, DC = local     <- OLD VMCA
    supervisor-ca.crt     Aug 21   IDENTICAL SUBJECT                     <- OLD
    harbor-ca.crt         Aug 24   from the lab just destroyed
    vmca-root.pem         Aug  1 · ca-lab-gc1.pem  Aug  4                <- ancient
    harbor-tls/           ca.crt ca.key tls.crt tls.key                  <- PRIVATE KEYS, dead Harbor
    vmca/50ab00a9.0                                                      <- stale c_rehash dir
    6x *.kubeconfig       ALL -> https://192.168.101.132:6443            <- dead guest clusters
    vks.kubeconfig        -> 127.0.0.1:42749                             <- a long-dead KinD

### Why it is a real hazard, not untidiness

1. **The CAs are stale in the DANGEROUS way.** `lib/vcenter.sh:214` says it in as many words:
   *"every cut produces a new CA with a BYTE-IDENTICAL subject, so a stale anchor looks correct and
   only a handshake tells you otherwise."* A subject comparison passes; only TLS fails.
2. **The guest API VIP is STABLE across cuts** — all six kubeconfigs name `192.168.101.132:6443`.
   So a stale guest kubeconfig **connects to the NEW cluster and fails on credentials**, which
   reads as an auth problem rather than a stale-file problem. That is the worse failure.
3. **Private keys for a destroyed Harbor sit on disk indefinitely** (`harbor-tls/ca.key`,
   `tls.key`). Whoever holds them can mint a cert for anything that trusted that CA.

### Why nothing deletes them — an OWNERSHIP SEAM, and each side is individually CORRECT

| | |
|---|---|
| `nested-vsphere-lab`'s `destroy` | correctly does NOT reach into another repo's `secrets/`. Its own `check-teardown` says *"everything this lab **OWNS**"*, and the teardown rule is *delete only what you created* |
| `vks-airgap-cicd` | has no "the lab I derived from was destroyed" hook — **it never learns the lab died**. `uninstall-all` removes what scenario-1 created ON the lab, not the artifacts derived FROM it. `make clean` removes only the bundle and `app/target` — measured: **nothing** in this Makefile clears `secrets/` |

Both correctly decline, so the state accretes forever. Same shape as `patterns.md`'s sudo-residue
rule — *state your tooling created outside its own teardown's reach* — except the boundary here is a
REPO, not a privilege level.

⚠️ The lab side ALSO archives rather than deletes, deliberately and correctly: `~/.local/state/nested-lab/`
now holds **~105** `*.destroyed-<ts>` files (35 kubeconfigs + 35 fingerprints + 35 PEMs, back to
Aug 15). That is a separate, lower-severity accretion and belongs to the lab repo, not here.

### What would close it — NOT DESIGNED, needs an idea round first

Sketch only: a `make secrets-reset` on THIS side (the owner), clearing lab-derived artifacts while
KEEPING `secrets/.env.make` (a 73 KB derived make cache, not lab state), plus a staleness detector.
Half of that detector ALREADY EXISTS — `make ca-status` is exactly "is this anchor still the right
one for its server", and it would have flagged the Aug 21 VMCA against an Aug 25 lab.

**Do NOT build the destructive target casually.** RULE ZERO names inventing a new control as the act
that most needs an idea round, and a `rm -rf` over an operator's gitignored credential directory is
the sharpest version of that. Open questions it must answer FIRST: what is the KEEP-set (`.env.make`
today — what else?); is it opt-in or wired into an existing flow; and how does it avoid the
`nested-vsphere-lab` mistake in reverse (deleting something another tool owns)?

**Workaround until then:** `tar czf ~/secrets-backup-$(date -u +%Y%m%dT%H%M%SZ).tar.gz secrets/` then
remove the lab-derived files by hand. That is what was done on 2026-08-25; the backup was 68K.

### 🔴 REFUTED 2026-08-26 by an idea round — do NOT build a prune

**This exact design has already shipped in this repo, and it already caused the harm.**
`scripts/kind-down.sh:121-128`, verbatim: *"It used to delete any kubeconfig living under ./secrets —
and the comment claimed that protected a real-VKS one. It did the opposite: the DOCUMENTED real-lab
default IS `./secrets/vks.kubeconfig`. So `make kind-down` — which BOTH real-lab runbooks tell you to
run at Step 0 — **DELETED THE OPERATOR'S LAB KUBECONFIG**."* A prune re-widens the
"looks stale ⇒ delete" predicate that incident narrowed, over the same directory.

And the repo has already ratified the opposite position at the exact moment a prune would run —
`98-uninstall-all.sh:294`: *"NOT removing secrets/ automatically — it may hold a credential this repo
did not create"* / *"delete only what you can prove you created"*.

Four more findings, each fatal on its own:

- **The KEEP and DELETE sets are INVERTED.** `secrets/.env.make` — the one file this row said to KEEP
  — is regenerated at make PARSE time on every invocation (`Makefile:127-131`), i.e. the safest thing
  in the directory to delete. `secrets/harbor-tls/ca.key` — implicitly in the delete set — is
  **deliberately stable** (`lib/tls.sh:32-37` re-mints the leaf but REUSES the CA), so deleting it
  silently **rotates a CA** that node containerd `certs.d`, an `SSL_CERT_FILE` bundle, or the
  in-cluster `harbor-ca` ConfigMap may still be pinned to. The prune would cause the regression.
- **The discriminator is ANTI-correlated with age**, measured with a paired control:
  `supervisor-ca.crt` (notBefore Aug 23, 3 days) verifies `return code: 0`; `harbor-ca.crt`
  (notBefore Aug 26, ~5 h) fails `error 20`. The NEWER file is the dead one. This reproduces
  `29-ca-status.sh`'s own 2026-08-16 measurement — two operating points ten days apart, same
  direction — so no mtime/age/"belongs to the previous cut" heuristic can work.
- **12 of 13 artifacts carry NO provenance** (only `supervisor-ca.crt.fetched` does), so the refusal
  this design most needs — *never delete what the operator supplied* — has no offline discriminator
  for 92% of the directory. A prune honouring it would decline to touch almost everything; one that
  did not would be guessing. In Scenario 2 the guessed-wrong case is a platform-issued credential a
  **tenant cannot re-obtain** (RULE ZERO-A0: no self-service recovery).
- **The pruner and the hazard are largely DISJOINT sets.** The staleness that actually bit today
  happened while the lab was ALIVE: `harbor-ca.crt`'s subject is byte-identical to the live one,
  different key, five hours apart. A "prune what belongs to a destroyed lab" pass would have removed
  three harmless orphans and LEFT the one anchor that was actually broken.

Also corrected: this row claimed a stale guest kubeconfig *"fails on credentials, which reads as an
auth problem"*. **Measured, it is `x509: certificate signed by unknown authority`** — a TLS/STALE_CA
failure, which `classify_kube_failure` (`lib/os.sh:2153-2169`) already returns *distinctly* from
`UNAUTHORIZED`. That is better news than the row assumed, and it is why the detector below can be
purely offline.

### ✅ WHAT SHIPPED INSTEAD — one offline check, deleting nothing

Three of the four cases already have a correct detector (`ca_status_report`, wired into
`make preflight` via `24-lab-preflight.sh:246`; `env-validate`'s `STALE_CA` arm; the `.fetched`
digest marker). The genuine gap was narrower than this row implied, and needs no network:

**A LOOPBACK server in the LAB slot.** `./secrets/vks.kubeconfig` is the documented real-lab guest
default, and an older KinD run wrote ITS kubeconfig there — `server: https://127.0.0.1:42961`.
`env-check`'s `-s` test passed happily; the first thing that noticed was `env-validate`, a
REACHABILITY gate that needs the network and is a separate step. A loopback address is structurally
impossible for a VKS guest cluster, so it is decidable offline, and `VKS_STATE_KIND` (already written
by `state_claim_kind`) distinguishes the KinD flow, where loopback is CORRECT.

Shipped in `02-env.sh`'s presence gate, with four cases in `test-env-check.sh` — and the GREEN ones
are the load-bearing half, since a check that only ever refuses is indistinguishable from one that
refuses everything:

    RED    loopback + not-KinD          -> refused, naming the address AND the file
    GREEN  routable server              -> passes
    GREEN  loopback + VKS_STATE_KIND=1  -> passes (KinD; loopback is correct there)
    GREEN  kubectl cannot read it       -> SKIPS (fails OPEN, deliberately)

RED-proven in a detached worktree at HEAD: against the pre-change script the RED case **PASSES the
loopback through** (the defect) while all three GREENs still pass — so the controls are not vacuous
and only the intended assertion flips.

**Residual, named:** `env-check` stays existence-only for everything else, and that is deliberate and
documented (*"env-check is the STRICTER PRESENCE gate … env-validate is the REACHABILITY gate … so it
can be run standalone"*). Making it reach the cluster is refused by its own contract. And if key
destruction is ever genuinely wanted, this repo's precedent is **`shred`** (`04-install-harbor-service.sh:9`),
not `rm` — shipping `rm` while calling it key destruction would be its own false-green.

## 🔵 B472 — OPTIONAL: derive the tenant's required permission set EMPIRICALLY, not from the code 🔵 optional

**Optional by design.** [[B471]] is the finding; this is one *possible* way to make its answer
empirical instead of derived. Nothing depends on it, and it can be declined without leaving B471
worse off.

### The measurement that motivates it (2026-08-24, live lab, cut B)

There is **no tenant identity in the lab at all**. The guest cluster kubeconfig
(`secrets/cicd-gc0824103354.kubeconfig`) is:

    Username   kubernetes-admin
    Groups     [system:masters system:authenticated]
    can-i      *.*   []   []   [*]        <- cluster-admin on everything

So every scenario-2 row runs as **cluster-admin**. B471 is therefore not "the matrix fails to test
the tenant path" but "there is no tenant to test".

### The five axes, DERIVED FROM THE CODE (this is the part that is a hypothesis)

| # | axis | what the scenario-2 path needs | the undecided choice |
|---|---|---|---|
| 1 | namespaces | every script creates/labels one (`50-seed-gitea-repos`, `70-configure-argocd`, `99-verify`, `49-psa-check`) | tenant creates their own, or platform pre-creates and grants admin *within* |
| 2 | **node `allocatable`** | `46-install-istio.sh` + `lib/capacity.sh` read it for the scheduling preflight | grant node read, or make the preflight degrade. **This axis already produced a shipped bug** — the istiod capacity preflight worked for an admin and failed CLOSED for a tenant |
| 3 | Istio route objects | `47-attach-istio` + `98-verify-ingress` use `Gateway`/`VirtualService` | Gateway-API needs only the tenant's own ns; the classic path needs the mesh admin to expose a selector + TLS secret |
| 4 | Harbor | `make harbor-robot` needs project-admin | tenant is project-admin, or is handed a pull-only robot and CANNOT self-recover a stale one (see RULE ZERO-B) |
| 5 | ArgoCD | AppProject **destination** + **repo** grants, plus cluster registration | tenant gets a scoped AppProject, or submits manifests to an admin |

Axes 3 and 5 are **already modelled** — `istio-preflight` and `argocd-preflight` exist to print what
to REQUEST. Axes 1, 2 and 4 are the undecided ones.

⚠️ **That table is DERIVED, not measured.** It is a hypothesis about what a tenant needs, and this
repo's record is that derivations get refuted roughly half the time. Do not treat it as the answer.

### The optional probe

Create a **minimal** ServiceAccount in the guest cluster — namespace-admin only, no node read, no
cluster scope — run the scenario-2 path against it, and record exactly which commands fail and with
what message. That converts *"a tenant probably needs these five things"* into *"it breaks in these
N places, here are the errors"*, which is what a matrix row would have to encode anyway.

**Cost/risk:** it WRITES to the lab (an SA + Role + RoleBinding, scoped to one namespace of a guest
cluster that exists to be destroyed). Reversible, but it is a mutation, so it needs the operator's
word before running — hence optional, not queued.

### 2026-08-25 — a large part of that probe is available READ-ONLY, via impersonation

The probe above is filed as optional *because it mutates the lab* (an SA + Role + RoleBinding), so it
needs the operator's word. That blocker is smaller than it looks: `kubectl auth can-i --as=<subject>`
issues a **SubjectAccessReview**, which is a read of the authorizer. It creates nothing, and it
answers "would this identity be permitted" for any subject the API server can name — including one
that does not exist yet.

Measured on the live lab (guest `https://192.168.101.132:6443`), impersonating
`system:serviceaccount:javawebapp:default`:

    no    get secrets                                 -n istio-system
    no    create gateways.gateway.networking.k8s.io   -n vks-ingress
    no    create httproutes.gateway.networking.k8s.io -n javawebapp
    no    create virtualservices.networking.istio.io  -n javawebapp
    no    create deployments                          -n javawebapp
    no    get pods                                    -n javawebapp        <- its OWN namespace

⚠️ **Read what that does and does not establish.** The `default` SA carries no RoleBinding at all, so
this is the **floor**, not a realistic tenant — it proves "admin is not a tenant", which B472 already
knew. Its value is the *technique*: point `--as` at the subject the five-axis table proposes and the
derived hypothesis becomes a measured answer, with no write to the lab.

**Three limits, so nobody mistakes it for the probe:**

1. It answers **RBAC only**. It cannot tell you a script fails for a non-RBAC reason (a webhook, a
   quota, PSA, a missing CRD), and this repo has already shipped one bug of exactly that shape — the
   istiod capacity preflight that read node `allocatable` and failed CLOSED for a tenant.
2. It gives a **verdict, not an error message**. The probe's stated deliverable is *"here are the N
   places it breaks and what they say"*; `can-i` yields `no`, which is not a diagnosis an operator
   can act on.
3. `--as` itself requires **impersonate** permission, so this is a measurement an ADMIN takes *about*
   a tenant — it is not something the tenant can run to diagnose themselves.

So: use it to cheaply refute or confirm axes 1–5 before spending a mutation, and still run the real
probe for the error text. It shrinks the probe; it does not replace it.

**Do NOT skip straight to building a matrix row from the derived table.** That is the failure mode
B471 already warns about: a row built on a guessed permission model certifies a permission model
nobody runs. Measure first, or decide the policy first — either is fine, guessing is not.

## 🟢 B483 — the ONLY tenant-RBAC test was wired to nothing AND broken 🟢 closed 2026-08-25

Found by the B471 idea round (finding 4: "invoked by nothing"), then MEASURED — and the measurement
was worse than the finding.

`make e2e-kind-tenant` runs `91-e2e-tenant-mechanism.sh`, the repo's only real tenant-RBAC test
(339 lines, AppProject + `argocd-rbac-cm` role + apiKey account + a kubeconfig with zero RBAC in the
ArgoCD namespace, with a positive control and hard-fail-on-`unknown` negative controls). It was a
prerequisite of **no** target and appeared in **zero** workflows.

Running it revealed it was also **broken**: rc=2, `FATAL ArgoCD is not installed`. The cluster was
fine — `deploy/argocd-server` was Running. The target lacked the `export SKIP_DOTENV` line that
`e2e-kind` and `e2e-kind-istio-existing` both carry, so it read the operator's LAB-configured
`.env`, where `ARGOCD_NAMESPACE=cicd` (the Supervisor's vSphere Namespace). It looked in the WRONG
NAMESPACE of the RIGHT CLUSTER, and its error named neither.

**That pairing is the lesson.** A test nothing invokes cannot tell you it has rotted, so "unwired"
and "broken" arrive together and the second is invisible until someone types the command. The tell
was an error that named a component rather than a lookup.

Fix: `export SKIP_DOTENV = $(E2E_SKIP_DOTENV)` on `e2e-kind-tenant`, and the same guard on
`e2e-kind-cross-cluster` (which reads `${ARGOCD_NAMESPACE:-argocd}` from the ambient env and is safe
today only because it happens not to call `load_env` — a uniform invariant beats relying on an
absence). `.env.state` is still loaded, so `KUBECONFIG` keeps resolving to the KinD cluster.

Verified: before, `FATAL ArgoCD is not installed`; after, the run reaches
`RED 1 OK - the tenant CANNOT create Applications in ns/argocd with kubectl (Forbidden)` and mints
its API token. `e2e-kind-both` was checked and is NOT affected — it delegates to `$(MAKE) e2e-kind`,
which carries its own target-specific export.

**Gated so it cannot recur:** `scripts/test-e2e-dotenv-guard.sh` asserts that every `e2e-kind*`
target which invokes a script directly carries the export. The exemption is DERIVED from the recipe
(does it call `$(SCRIPTS)/`?), never a hand-typed target list. RED-proven: delete the guard line and
it fails naming `e2e-kind-tenant`; restore and it passes.

**Writing that gate produced its own worked example of a classifier that lies.** Two bugs, both
caught by reading the output instead of the rc:

1. It reported `e2e-kind` as "delegates only" — but `e2e-kind` DOES call
   `$(SCRIPTS)/e2e-ordering-verdict.sh`. The extractor stopped at the target's FIRST rule line
   (`e2e-kind: export SKIP_DOTENV = ...`) and read an empty recipe. A target can have several rule
   lines; the body after every one of them counts.
2. After fixing that it STILL false-exempted, because a column-0 `#` comment INTERLEAVED inside a
   recipe does not end that recipe in GNU make — but it did end my scan.

Both were harmless on today's tree (that target happens to be guarded), which is exactly why they
would have shipped: the gate was GREEN and its exemption line was wrong. The denominator is the tell
— `3 checked / 2 exempt` before, `4 checked / 1 exempt` after.

**2026-08-25 — the REVERT TRAP is now in place (the prerequisite an adversary named).** The test
MUTATED a shared ArgoCD and nothing undid it: it added `accounts.tenant` to `argocd-cm`, rewrote
`policy.csv` in `argocd-rbac-cm`, and created an AppProject, a ServiceAccount, a ClusterRole and a
ClusterRoleBinding. The only `trap` removed a tempdir. Since `e2e-kind` REUSES a warm cluster by
default, every run left a live apiKey account and a hand-written RBAC policy behind — which is
exactly why wiring it into an automated path would have shipped a leak.

The residue was not hypothetical: the pre-state of the verification run READ
`accounts.tenant=apiKey,login`, a 414-byte `policy.csv`, and all four objects, left by an earlier
run in this same session.

`revert_all()` now runs on EXIT and is **key-scoped, never a whole-object restore** — putting back a
captured `argocd-cm` would clobber anything else that changed meanwhile. `policy.csv` IS restored
from its captured value (we overwrite the whole key), and the key is REMOVED when there was none.

⚠️ **A bug in the trap itself, caught by running it rather than reading it:** the JSON-Pointer path
must be `/data/accounts.<name>`, NOT `/data/accounts~1<name>`. `~1` escapes a literal `/`, and this
key's separator is a `.`, which needs no escaping. The escaped form is a **silent no-op** — measured,
it reported success while `accounts.tenant` was still `apiKey,login`.

VERIFIED by measurement, not by the revert's own claim: baseline cleaned, test run (rc=0, the tenant
created every Application through argocd-server), then re-read — `accounts.tenant` empty,
`policy.csv` 0 bytes, AppProject / SA / ClusterRole / ClusterRoleBinding all absent. Exactly the
baseline.

Still open, deliberately: WIRING it into an automated path. It needs a live KinD cluster with ArgoCD
and mutates that cluster's ArgoCD, so where it belongs is a design question, not a one-liner. B471
carries it.

## ✅ B489 — `vks-package.sh install` ADOPTS AND OVERWRITES an existing PackageInstall, and re-grants cluster-admin — FIXED 2026-08-26

**Found by an adversary 2026-08-26 while reviewing an unrelated design; VERIFIED from source before
filing.** `install` (`scripts/vks-package.sh` ~:184-207) does a bare `kubectl apply -f -` creating:

- ServiceAccount `<name>-pkg-sa`
- **`ClusterRoleBinding <name>-pkg-sa-cluster-admin` -> `roleRef: ClusterRole/cluster-admin`** — cluster-scoped
- `PackageInstall <name>` in `$PKG_NS`

with **no check that any of them already exists**, and — per the file's own header ~:70 — **no CONFIRM
gate on install**. `uninstall` DOES check (`:232`):

    kubectl -n "$PKG_NS" get pkgi "$name" >/dev/null 2>&1 || { log_info "... nothing to do"; exit 0; }

⚠️ **CORRECTED by the design round:** I first wrote that the only `get pkgi` calls are at
`:216 :225 :232 :246`, *all in the uninstall path*. **`:216` and `:225` are in the INSTALL path**
(the reconcile-wait loop and its timeout `die`). The conclusion survives — none is a **pre-apply**
guard — but the correction matters constructively: install ALREADY resolves `$PKG_NS` and ALREADY
calls `get pkgi`, so the guard costs one more call, not new machinery.

**Consequence.** A `PackageInstall/istio` in `vmware-system-tkg` created by anyone else — a second
operator, a re-run at a different `PKG_VERSION`, a platform hand-install, or later the VKS addon
framework — is **silently adopted and overwritten**, along with a cluster-admin ClusterRoleBinding.

**Why this outranks the addon tripwire it was found next to.** `43-install-istio-package.sh` carries
17 lines of warning about "two competing writers on one name" from the VKS 3.7 addon framework — a
hazard that is (a) speculative, (b) guarded by a probe measured to be structurally unable to fire
(see B484), and (c) about a package Broadcom has NOT migrated to the addon path. **This is the same
hazard class, reachable today by mundane means, on the same code path, entirely unguarded.**

**Done-when.** Before the apply, read the existing `PackageInstall`; adopt-vs-refuse is a deliberate,
tested decision rather than an accident; and the cluster-scoped ClusterRoleBinding gets its own
ownership consideration. ⚠️ The obvious fix — mirror uninstall's `CONFIRM` gate — **may be refuted**:
`install` is called unattended by `44-install-ingress.sh` inside the six-row certification matrix, so
requiring `CONFIRM=yes` could convert an idempotent step into an interactive one and break the
matrix. That is exactly what the design round must settle before anything is written.

### The design round REFUTED my proposed fix — and both halves of my premise

**1. "A CONFIRM gate could break the unattended matrix" is FALSE.** MEASURED: `ISTIO_INSTALL_METHOD`
appears in **0 of 37** `docs/**.md` and **0** Makefile lines; `44-install-ingress.sh:45` defaults to
`helm`, and `.env.example` keeps the toggle commented. **The matrix cannot reach this code path.**
Consequence beyond the premise: **a green six-row matrix is VACUOUS evidence for this change** and
must not be cited as proof it is safe.

**2. The CONFIRM gate is refuted anyway, by a stronger case.** `install` **dies AFTER the apply** —
it applies at `:184`, waits, then `die`s at `:224` on reconcile timeout. So the most likely reason an
operator re-runs leaves all three objects behind, and the gate would fire on **their own retry**,
training them to type `CONFIRM=yes` reflexively — which is exactly what makes a gate useless on the
day it matters.

**So: gate on DIVERGENCE, never on existence.** Identical `refName` + version + SA -> adopt silently
(that IS idempotency, and it is the retry). Different version -> gate (an operator re-running at an
older `PKG_VERSION` over a live mesh is the real hazard; `43:32-33` already calls the two-minor gap a
deliberate downgrade). Different `refName` under the same derived name -> die.

### Three findings that reshape the fix

- **`managedFields` cannot work.** MEASURED: `kubectl` defaults to `--server-side=false`, so the
  manager is `kubectl-client-side-apply` for **any** apply — ours, a second operator's, a hand-run.
  Use the repo's OWN ownership label instead; the precedent is
  `71-argocd-register-guest.sh:268-283` (check-then-refuse) consumed by `98-uninstall-all.sh`.
- **That precedent's idiom FAILS OPEN**, and fails open into *granting cluster-admin*. `if
  _owner="$(... 2>/dev/null)"` cannot distinguish absent from **Forbidden** from unreachable, so a
  namespaced tenant skips the guard entirely. Use `kube_is_notfound` (`lib/os.sh:2115` — requires the
  server's own `Error from server (NotFound)` **and** the object token on one line): provably absent
  -> proceed; anything else -> refuse and name why. ⚠️ Do **not** write it as
  `case "$(classify_kube_failure …)"` — `check-classifier-consumers.sh` matches that literal form and
  would pull this file into a 9-consumer denominator.
- **"Leave `uninstall` alone" is REFUTED.** `:241` deletes the cluster-scoped
  `<name>-pkg-sa-cluster-admin` **by name, output discarded, errors swallowed** — a direct violation
  of this repo's own doctrine at `98-uninstall-all.sh:17-18` (*"deletes ONLY objects carrying our
  ownership label, NEVER by name alone"*). The two halves compose: install ADOPTS a foreign pkgi, the
  operator now believes it is theirs, and uninstall then deletes **their** pkgi and **their**
  cluster-admin CRB. **Two destructive acts from one lossy derivation** (`cut -d. -f1` maps
  `istio.kubernetes.vmware.com` -> `istio`, so any two refNames sharing a first DNS label collide on
  a CLUSTER-SCOPED object). The uninstall half must ship in the SAME change.

**Status: found, verified from source, DESIGNED AND THE DESIGN REFUTED ONCE, redesigned per the
above, NOT YET IMPLEMENTED, NOT TESTED.** RED cases R1-R7 and green controls C1-C6 are specified —
including **C2 (identical -> proceeds silently)**, without which the fix is unproven, and **R5
(Forbidden -> must NOT fail open)**, without which it ships the bug it was written to fix. Blocked on
the tree being frozen mid-certification.

⚠️ Also flagged, not fixed: `test-vks-package-guard.sh:37`'s stub arm `*" get "*" packages "*)`
**can never match** (the leading space) — harmless today only because that arm and the fallthrough
both yield empty output, and actively misleading the moment the harness returns a real package list.

### SHIPPED 2026-08-26 — and the implementation round REFUTED the first attempt

`scripts/test-vks-package-adoption.sh`, **17 cases, 0 failed**. RED-proven in a
`git worktree --detach HEAD`: the SAME test against the PRE-CHANGE script scored **9 failed / 3
passed** — the old code silently `installed` in every refusal scenario and DID delete the foreign
cluster-scoped binding. The stub-arm bug above is fixed too (6/6).

**The implementation adversary refuted the first version**, and the three findings that mattered are
the reason this row is worth reading:

1. **CRITICAL — the PackageInstall was STILL deleted by name.** I had protected the SA, the
   ClusterRoleBinding and the values Secret, and left the pkgi — the object that owns the entire
   deployed mesh, i.e. the expensive one — deleted by name four lines under a comment quoting
   *"NEVER by name alone"*. Measured: install REFUSED a foreign-owned pkgi and uninstall destroyed
   it one command later. Pinned by R8, which asserts BOTH the refusal AND that no delete call was
   issued at all.
2. **HIGH — a REGRESSION I introduced.** `kubectl create secret generic` does not take the heredoc's
   labels, so `<name>-pkg-values` was the one object we create UNLABELLED — after which the new
   uninstall correctly refused to delete an object we had made ourselves, on **every** real istio
   uninstall (`43-install-istio-package.sh` always passes `PKG_VALUES`), desensitising the operator
   to the genuine warnings. Fixed by labelling at creation; pinned by C7.
3. **HIGH — C6 was VACUOUS.** `! grep -q 'NOT deleting'` is a pure negative: an adversary mutated
   the delete-when-ours away entirely and the suite stayed **12/12 green** while cluster-admin was
   abandoned on every uninstall. It also passed on a script that FATAL'd at startup. Now the stub
   logs every kubectl invocation and C6 asserts the delete was **ISSUED**.

Also fixed from the same round: a failed owner read no longer reports `owned-by='<none>'` (asserting
a value never obtained, then offering a `kubectl delete` the operator was just told they cannot run);
uninstall's early exit no longer reads Forbidden as *"not installed — nothing to do"* with rc=0; an
unparseable read now dies naming what it saw (R4); and the temp file no longer leaks.

⚠️ **A green six-row matrix is NOT evidence for this change** — measured: `ISTIO_INSTALL_METHOD`
appears in 0 of 37 docs and 0 Makefile lines, the default is `helm`, so the matrix cannot reach this
code path. The decisive live run is a VKS guest with `ISTIO_INSTALL_METHOD=package`; everything here
is stub-measured.

## 🔴 B486 — `ARGOCD_SERVER` is published as an **IP**, and `.env.example` says it must be a **NAME** 🔴 open

**MEASURED on the live 3.7 lab, certification row 1, 2026-08-26.** `09-argocd-address.sh` publishes
`ARGOCD_SERVER=<LB IP>`. `.env.example:439` says it should be `<SET-a-name-the-cert-carries>`, and the
lab-verified SAN list beside it carries **no IP SAN** at all (`DNS:localhost, DNS:argocd-server,
DNS:argocd-server.cicd…`). The IP works only because every consumer runs `--insecure`/`-k`.

**Why it matters, with the control from the same run.** The VIP pool shifted: harbor `.130 -> .137`,
argocd `.131 -> .138`. **Harbor did not break**, because `HARBOR_URL` is a NAME and DNS re-resolved
it. ArgoCD broke, because a bare IP has no indirection. `show-dns-records.sh:74` **already emits an
A-record row for `argocd-server`**, so the name path is built and simply unused here.

The re-resolve fix shipped alongside this row makes the IP *survivable*; it does not make it
*correct*, and it removes the pressure to fix the kind. `rules/common/coding-style.md` §"A COUPLED
SET is checked in BOTH DIRECTIONS" already names this script as publishing "the one value the docs
forbid".

**Done-when.** `ARGOCD_SERVER` carries a name the certificate actually presents, with the A record a
documented step of scenario-1 §5 exactly as §4 does it for Harbor; the LB IP is published separately
as `ARGOCD_LB_IP` in `.env.state`. Then `-k` stops being load-bearing and TLS can be *verified*.

**Also open, from the same review:**

- **F6 SETTLED 2026-08-26, MEASURED: the CR DOES carry a status stanza, and this repo has ZERO
  readers of it.** `crd argocds.argocd-service.vsphere.vmware.com` defines `.status.conditions`, and
  the live instance reports `PackageInstallReady=True`, `SecretReady=True`,
  `SyncPackageInstallStatus=True`. So a readiness signal EXISTS to wait on before reading the
  Service's address, and nothing waits on it.
  ⚠️ **Do NOT overstate this.** Those conditions describe the **PackageInstall**, not whether the
  LoadBalancer VIP is stable or routable — and the incident was a VIP that moved on an already-created
  Service. Waiting on them is plausibly necessary and is **NOT proven sufficient**, so the re-resolve
  loop stays until someone measures whether Ready precedes VIP stability. Done-when: wait on the
  condition in `08-install-argocd-service.sh` AND show, with timestamps, that the VIP no longer moves
  after it goes True.
- **F7** — this is now the **third** hand-rolled endpoint poller, and the existing two disagree on
  what counts as answering (`argocd-auth-check.sh` accepts any status ≠ 000; `91-e2e-tenant-mechanism.sh`
  requires curl success). Extract one `argocd_endpoint_answers` into `lib/argocd.sh` beside
  `argocd_session_token`, which was single-sourced on 2026-08-17 for exactly this reason.
- **The VIP-reallocation mechanism is UNDERDETERMINED.** Measured: the Service was created 05:56:14Z
  and kept the **same uid**, so it was never recreated — its LB status was re-programmed. Whether
  that was a per-Service reprogram or a Supervisor-wide pool reallocation is unknown; if the latter,
  a reallocation can land *after* publication and no publisher-side fix is ever sufficient, which is
  a second argument for the name.

## 🔴 B484 — the Forbidden-reads-as-absent sweep: a FAIL-OPEN air-gap check outranks the wrong-message bug 🔴 open

**UPDATE 2026-08-26 — F1/F2/F5 DONE; F3 re-graded and CLOSED differently than designed; two NEW
findings, one of them a shipped CRITICAL that neither the backlog nor I had noticed.**

**F2/F5 (host comparison + Software Depot) — DONE, PR #1028.** The bundle host is compared as an
exact `host:port` through a new `registry_hostport()` in `lib/os.sh`, lifted verbatim from `tls.sh`'s
`_ca_hostport` so there is one parser. RED-proven: reverting to the prefix form fails 3 of the 4 new
cases (both false-GREEN directions and the false-BLOCK).

**F3 — the designed fix was REFUTED by BOTH adversary rounds, independently, and MEASURED.** Adding a
`NO_SUCH_RESOURCE` class to `classify_kube_failure` turns `check-classifier-consumers` RED on **9/9**
consumers (that gate DERIVES its class list from the function body and requires a named arm in every
case-form consumer, each needing a *different* remedy), and breaks `test-tkr-classify`, which
explicitly PINS an absent CRD as `UNKNOWN`. `vks-package.sh` is not one of the nine. Closed instead
with a **LOCAL** discriminator, which is what both rounds prescribed. **Re-graded HIGH -> MED**: the
path is opt-in (`ISTIO_INSTALL_METHOD` defaults to `helm`), so it fires on KinD / a non-VKS cluster.

**NEW, CRITICAL, SHIPPED — `vks-package.sh install` died SILENTLY.** `_versions` is a `kubectl | jq`
pipeline and the call site had no `|| true`, so under `set -euo pipefail` a failing kubectl killed the
script ONE LINE ABOVE its own guard. MEASURED; the CONTROL is the discriminator:

| kubectl says | before | after |
|---|---|---|
| no Carvel API | rc=1, **262 B, silent** | rc=1, 674 B, names the cause |
| Forbidden (a namespaced tenant) | rc=1, **262 B, silent** | rc=1, 721 B, names RBAC |
| API present, 0 items (CONTROL) | rc=1, 545 B | **unchanged** |
| API present, package exists (CONTROL) | installs | **unchanged** |

In the field: `make install-ingress ISTIO_INSTALL_METHOD=package` created two namespaces and exited 1
with no message. `_die_unknown` carried the same shape independently. Fixed + pinned by
`scripts/test-vks-package-error-discrimination.sh`.

**NEW, HIGH — `.env.example` stated the OPPOSITE default, twice, since 98276c1.** It called
`ISTIO_INSTALL_METHOD=package` the default; the code has always read `${ISTIO_INSTALL_METHOD:-helm}`.
Its "override per run" example was a no-op against the real default. Material, not cosmetic: the two
methods install different Istio minors under **different** provenance guards, so a reader who believed
`package` was the default believed 43's air-gap check had protected them when helm had run under a
different gate. Fixed. **No gate covers doc-vs-code default agreement** — that is new-control design
and needs its own idea round.

**STILL OPEN, carried forward:**

- **F5 (live)** — still artefact-verified only; we cannot reproduce a relocated depot.
- **`vks-package.sh`'s `kubectl apply -f -` discards stderr**, so the *second* half of the no-Carvel
  path is unclassifiable no matter what the probe says. Named, not fixed.
- **SETTLED 2026-08-26, MEASURED: the VKS 3.7 addon tripwire IS DEAD CODE.** On the live 3.7 lab,
  guest `cicd-gc0826033451`: **SUPERVISOR addon CRDs = 9, GUEST addon CRDs = 0**. The tripwire in
  `43-install-istio-package.sh:115` probes the GUEST with the ambient KUBECONFIG, and
  `vks-package.sh:77-88` eagerly REFUSES a Supervisor kubeconfig — so the guest is the only thing it
  can ever see, and it cannot fire. 17 lines of warning that can never print, on a risk
  `docs/vks-services/istio.md:539` calls live on VKS 3.7+. **Re-point it at the Supervisor
  kubeconfig, or delete it and restate the risk honestly** — leaving it is worse than nothing,
  because it manufactures the appearance of handling.
- **SETTLED, and my FIRST probe was WRONG in the alarming direction.** The package path's premise
  holds: the same guest serves **112 packages**, 7 `*.carvel.dev` CRDs, and 2 api-resources in
  `data.packaging.carvel.dev`. An earlier probe reported **0** and would have implied the whole
  package path was impossible on a guest; it had run before the guest was ready. An instrument
  artifact, caught by re-measuring rather than repeating it.

> **2026-08-25 — the sweep half is DONE; #1 grew and is now the whole row.** The bare-substring
> discriminator is fixed at all five sites (`48` ×2, `23`, `26`, `98`, `08`) behind one shared
> `kube_is_notfound` in `lib/os.sh`, RED-proven by three new cases in
> `test-istio-preflight-absence.sh` and guarded by `check-notfound-discriminator.sh`. What remains
> is **#1**, and an idea round plus Broadcom's own air-gap guide grew it from "add an else" to five
> defects:
>
> | | |
> |---|---|
> | **F1 CRITICAL** | **RE-MEASURED ON 3.7.1 (2026-08-26): now EIGHT Packages, not six** (`+1.28.9`, `+1.30.2`), and a SECOND repository (`vks-addons-3.7.0-20260723`) shares the namespace — so `tail -1` is more arbitrary than before. The prescribed fix holds: **0** duplicate `refName`+`version` pairs across both repos. **LAB-VERIFIED 2026-08-25** on `cicd-gc0825181952`: the `refName` filter matches **one Package per VERSION** — six of them (`1.27.1 · 1.27.4 · 1.27.5 · 1.27.8 · 1.28.2 · 1.28.5`) and `tail -1` picks by API order — so it can die over a bundle that will never be installed, or clear one while a DIFFERENT version installs from Broadcom. Resolve the version first, then filter on `refName AND version`, and assert exactly one match. |
> | **F2 HIGH** | the `case` pattern is not a host comparison — measured **4/12 wrong in BOTH directions** (`https://harbor.x` false-blocks; `harbor.x.evil.io` false-passes). `lib/tls.sh` already ships `_ca_hostport()` for exactly this; lift it rather than write a third implementation. |
> | **F3 HIGH** | `classify_kube_failure` has no arm for "this cluster has no Carvel packaging API", so that operator would get "could not determine where the images come from" in place of today's correct "no such Package". |
> | ~~F2 namespace~~ | **REFUTED ON THE LAB 2026-08-25.** All **84** Packages are in `vmware-system-tkg` and `vmware-system-vks-public` **does not exist on the guest** — our default is correct. The fail-OPEN half of F2 (empty ⇒ skip) still stands. |
> | **F4 HIGH** | the namespaced fallback I proposed is **harmful** — `Package` is namespaced, so `-A` is strictly harder to satisfy, and the fallback's only outcome is a misleading error. Probe with the installer's own `-A` and delete it. |
> | **F6 HIGH — sibling nobody had looked at** | `96-verify-gateway-image.sh` has **zero** references to `ISTIO_INSTALL_METHOD` (`grep -c` = 0) and branches on `INGRESS_CONTROLLER` alone, so in `package` mode it demands `$HARBOR_URL/*` provenance for images that legitimately come from the addon repository — it **fails a CORRECT install**. Make it SKIP loudly, as it already does for `istio-existing`. |
> | **F5 — false-BLOCK on the DOCUMENTED air-gap config** (9.1-primary-doc, NOT lab-verified: this lab is not relocated, so its bundles read `projects.packages.broadcom.com` and the `depot*` case cannot be confirmed here) | Broadcom's [air-gapped VCF 9.1 guide](https://github.com/vmware/vsphere-supervisor/blob/main/airgapped/air-gapped-vcf91.md) relocates the Standard-Packages bundle into the **Software Depot OCI registry** via `oci_image_depot_migrator.py`, after which the istio package resolves from **`depot.kube-system.svc/vcf/vks-standard-packages/ga/...`** (or `depot-image-proxy.kube-system.svc.cluster.local`). Our safe-list is only `localhost:*\|127.0.0.1:*\|$HARBOR_URL*`, so a CORRECTLY air-gapped lab **dies** and is told to request a relocation it already did. Details + the open namespace question: `docs/vks-services/istio.md`. |
>
> **The VKS-3.7 collision risk is REFUTED, lab-verified 2026-08-26.** The lab was upgraded to
> `3.7.1+v1.36` and the guest rebased to `builtin-generic-v3.7.0` / k8s `v1.35.6`: the guest still
> carries **0 addon CRDs** — the addon framework is Supervisor-side only — so nothing competes with
> our `PackageInstall`. The tripwire stays (it costs one `kubectl get crd`) and is now proven silent
> on the version it guards. Details: `docs/vks-services/istio.md`.
>
> Note `ISTIO_INSTALL_METHOD` defaults to **`helm`** (`44-install-ingress.sh:45`), so #1 sits on an
> **opt-in** path — it is not a blocker for the default walk.

Found by the implementation round on B471/F-c4 (#1016), which swept **72 stderr-discarding kubectl
sites across 145 non-test scripts** and flagged 24. Two rank ABOVE the two I fixed, because a wrong
MESSAGE costs an operator time while a fail-OPEN costs them the guarantee.

**#1 — `scripts/43-install-istio-package.sh:86-113` FAILS OPEN.** `BUNDLE_HOST=""` is set, filled
from `kubectl get package … 2>/dev/null`, and then guarded by `if [ -n "$BUNDLE_HOST" ]; then … fi`
**with no `else`** (verified by reading it). So any failure of that read — a tenant Forbidden on a
cluster-scoped `get package`, an expired credential, an API blip — leaves it empty and the WHOLE
air-gap check is skipped, including the `die` that otherwise stops an install whose images resolve
from a vendor bundle repo the mirror cannot stage. The comment directly above says the point is that
*"The operator learns on the box that can still fix it."* A silently-disabled control does the
opposite, and it is invisible: the run goes green.

**#2 — `scripts/48-istio-preflight.sh:206-209` asserts a false claim and exits 1.** `GW_TYPE` comes
from a `2>/dev/null` jsonpath; when the Service is unreadable it is not `LoadBalancer`, so the check
tells a tenant *"the gateway is not a LoadBalancer"* — a statement about the CLUSTER derived from a
question nobody answered — and sets `rc=1`. This is tenant-facing (`make istio-preflight`) and the
remedy it prints is roughly right for the wrong reason.

**Lower-ranked, recorded so the sweep is not re-run from scratch:** `lib/vks-package.sh:84,95`
(`get packages -A`, cluster-wide, unmeasured); `vks-trust-probe.sh:91` and `:66` (assert absence but
are non-blocking and exit 0); `lib/istio.sh:67` (leads with a false claim, then hedges RBAC with the
exact remedy on the next line — lowest of the six).

**The exemplar to copy is `lib/capacity.sh:146-175`**, which separates "nodes unlistable (RBAC),
normal for a tenant … NOT a memory problem" from "all excluded", and SKIPs.

**Done-when.** Both #1 and #2 key on kubectl's EXIT STATUS, not on emptiness — the same rule B471/F-c4
had to be corrected to (keying on stderr-non-emptiness regressed an admin; keying on the CLASS was
shadowed by `classify_kube_failure`'s unanchored UNREACHABLE arm). #1 must FAIL CLOSED: an unreadable
package list is not evidence the images are mirrored.

⚠️ **Do NOT "fix" this by widening `classify_kube_failure`.** Its UNREACHABLE arm sits above
FORBIDDEN and is unanchored, and every other `case "$(classify_kube_failure …)"` consumer inherits
that. Keying on rc makes a call site immune without touching the shared classifier.

## 🟡 B471 — RE-SCOPED: the tenant gap is HARBOR, not the guest cluster 🟡 open

> **CORRECTED 2026-08-25 by an idea round + my own measurements. The original row was ~5x too big
> and its central piece of evidence was INVERTED.** Kept in place rather than rewritten away, so the
> next reader does not re-derive the refuted version.

### What the original row claimed, and why most of it is false

It claimed the matrix's scenario-2 rows certify the DOCUMENT but not the PERMISSION MODEL, because
the row is handed (a) admin credentials and (b) a driver-supplied guest kubeconfig that "a real
tenant" would not have. **Both halves are wrong for the guest cluster**, and `docs/scenario-2.md`
says so itself, five times (measured, verbatim):

| line | text |
|---|---|
| `:20` | "**cluster-admin on your own guest cluster** (we create namespaces, RBAC, PSA labels)" |
| `:170` | "You need **cluster-admin** on the workload cluster either way" |
| `:876` | "you hold cluster-admin on your own guest cluster" |
| `:895` | "You hold **cluster-admin on your own guest cluster**" |
| `:952` | "**cluster-admin** on the workload cluster is required" |

and `:83` — "**Most tenants are handed the workload kubeconfig and never touch the Supervisor.**"
So a driver-supplied guest kubeconfig IS the documented tenant path. This repo's tenant is a tenant
of the SUPERVISOR and an admin of their OWN guest cluster.

**Of the 36 make targets scenario-2 names, exactly 3 need a permission a tenant lacks** — and the
document already flags each: `fetch-argocd-kubeconfig` (`:518`, declared optional),
`argocd-register-guest` (`:828`, "**they** run..."), `harbor-admin-password` (`:808`, whose own
Expect line says it REFUSES a robot account). Nothing in the `install-all` chain is Supervisor-side.

### 🔴 Its cited evidence was INVERTED

The row said four skipped blocks were "the branch a credential-less tenant would actually take".
Measured at `scenario-2.md:113-116`, that branch needs the **licensed `vcf` CLI + vSphere SSO
credentials + Broadcom entitlement** — strictly MORE privilege, not less. The row takes the branch
the document calls the normal tenant case.

The genuinely tenant-relevant skip is one the original row never mentioned: block **`[22]`**,
"a Harbor robot already exists; its secret is shown ONCE" — i.e. **`make harbor-robot` never runs**.

### What actually survives — ONE axis

The scenario-2 rows are supplied a **Harbor admin** credential (`walk-matrix.sh:832-838`, logged at
`:1119`), `harbor-robot` is skipped, and the sysadmin / project-admin-one-project /
two-projects-IMPOSSIBLE decision at `22-harbor-robot.sh:79` is untested — only its *payloads* are
(`test-harbor-robot-payload.sh`). The project-scoped-robot path where `HEAD /projects` is denied and
project-CREATE returns 403-on-existing (`scenario-2.md:677-683`) is exercised nowhere.

Everything else is already covered: `24-lab-preflight.sh:123-135`, `22-harbor-robot.sh:79-123`,
`lib/capacity.sh:167-171` (degrades to WARN on the tenant shape), plus `23-argocd-preflight.sh`,
`48-istio-preflight.sh`, `70-configure-argocd.sh` — **11 distinct `can-i` probes**.

### Done-when (re-scoped, and item 2 was REFUTED by a second round)

1. an OFFLINE stub test for `22-harbor-robot.sh:79`'s 3-way decision and the 403-on-existing
   fallback, beside `test-harbor-robot-payload.sh` in `make test-scripts`. **STILL OPEN**, and it
   must carry four changes a second idea round measured as CRITICAL: pin `REPO_ROOT` to a
   `mktemp -d` **and `cd` into it BEFORE sourcing `lib/os.sh`** (`os.sh:31` EXPORTS `REPO_ROOT`, so
   it leaks into the child); pass the project pair through a temp **`.env`**, never env vars (the
   uncommented `HARBOR_INFRA_PROJECT`/`HARBOR_APP_PROJECT` in `.env.example` are re-sourced by
   `load_env`'s `set -a` and silently select the WRONG branch — the round hit exactly that); assert
   only on the three operator-facing strings, never on the stub's own responses; and add a
   HERMETICITY case, because the success path WRITES `secrets/harbor-robot.env` and publishes to
   `./.env` — auto-discovery would run that inside `static-check` and **overwrite the operator's
   real Harbor credentials on every run**.

2. ~~`can-i create namespaces` + `can-i list nodes` added to `24-lab-preflight.sh`~~ — **REFUTED by
   the round that proposed it.** Both are false-BAD generators: `ensure_namespace` uses
   `create --dry-run=client | apply`, so `create namespaces` is never exercised on a pre-created
   namespace; and a `bad` on `list nodes` would contradict `lib/capacity.sh:167-171`, which calls
   unreadable nodes "normal for a tenant". Do not build these.

### ✅ DONE 2026-08-25 — F-d1 and F-c4, the two the round ranked ABOVE the stub test

- **F-d1 (#1011)** — `make vks-login` died FATAL for a tenant because it asserted the
  CLUSTER-SCOPED `list nodes` on the same `&&` as `cluster-info`. Fixed; measured that
  `cluster-info` alone still returns rc=1 `Unauthorized` on a corrupted token, so the connectivity
  proof the code comment protects is intact.
- **F-c4 (this row's other half)** — `24-lab-preflight.sh` discarded kubectl's stderr on TWO checks
  and judged the empty result, so a Forbidden tenant was told **"no StorageClass at all — Gitea's
  PVC can never bind"**. MEASURED before the fix (`default_sc=[] n_sc=[0]` → that PROBLEM). Both
  checks now classify via `classify_kube_failure` and report `unk`, whose own comment already said
  a check "must not assert a permissions verdict nobody obtained" — the file had the answer and
  applied it to 1 of its 4 checks. Guarded by `scripts/test-lab-preflight-forbidden.sh`, which
  tests BOTH directions (a fix that never judges anything would pass the tenant case and destroy
  the check) and is RED-proven: re-discarding stderr flips exactly the two StorageClass assertions
  while the LoadBalancer and admin ones stay green.

### REFUTED — do not build these

- **An offline grep gate over scenario-2 for admin verbs.** Built and measured during the idea
  round: **17 of 18 hits were false** (94%). Every hit landed on a line where the document was
  already doing the right thing, so its only remedy is deleting an honest disclosure.
- **`make tenant-preflight` keyed on `can-i --list`.** It does not discriminate: it goes green on
  any cluster where nobody downscoped anything, and B472 measured the guest kubeconfig answering
  `*.* [] [] [*]`. It measures the caller, not the design.
- **A 7th matrix row with a downscoped kubeconfig.** It would certify our GUESS at tenant RBAC
  (B472's five-axis table is derived, not measured), needs a lab mutation, and its green would read
  as "tenants work".

### Cannot be settled without a real tenant on a real lab

There is **no tenant identity in the lab at all** — B472, lab-verified: the guest kubeconfig is
`kubernetes-admin` / `system:masters`. Creating one is a lab-policy decision. And `can-i` is
structurally blind to the class that actually bit us: admission webhooks, ResourceQuota, PSA on a
namespace the tenant did not create, and computed values like node `allocatable`.

### The premise-4 bug is FIXED

`lib/capacity.sh:23-30,167-171` records the istiod capacity defect and its fix. It was found by
REASONING about the tenant path — which argues that reasoning works here, not that a row is needed.

## 🟠 B470 — `test-cluster-status-wait-gate.sh` TRUNCATES a pre-existing `secrets/testcluster.kubeconfig`

The test drives the real `26-vks-cluster-status.sh`, which writes
`${REPO_ROOT}/secrets/${VKS_CLUSTER_NAME}.kubeconfig` (`:43`), with `VKS_CLUSTER_NAME=testcluster`.
So it writes into the REAL repo.

**FIXED (litter):** the test now removes that file if it did NOT pre-exist. Measured before the fix:
a 0-byte `secrets/testcluster.kubeconfig` was sitting in the tree after `make static-check`. Stale
kubeconfigs in `secrets/` are B467's hazard — no target takes a cluster argument, so an operator
exporting the wrong one acts on the wrong cluster. (Six were present this session; all became dead
after a lab teardown.)

**NOT FIXED (truncation):** if an operator genuinely has a cluster named `testcluster`, the script
**truncates their kubeconfig to 0 bytes** before the cleanup trap ever runs. Measured with an md5:
`d80fe9f4…` -> `d41d8cd9…` (the empty-file hash). The trap correctly does not delete it — the
content is simply already gone.

⚠️ **The obvious fix was TRIED and FAILED, so do not re-try it blind.** Pointing the child at a
throwaway `REPO_ROOT="$TMP/repo"` on its invocation line (`os.sh:20` honours a pre-set `REPO_ROOT`)
took the suite from **13/13 to 9 passed / 4 failed** AND still left the litter — so the script needs
the real root for more than that one path, and the write is not reaching `secrets/` only through
the variable I assumed. Find the actual write path before changing the invocation.

**Done when:** the test cannot write into the real `secrets/` at all, with 13/13 still green — or,
if that proves structurally impossible, the collision is removed by giving the fixture a name no
real cluster would take (the cheap option, and it needs measuring for the same reason: the name may
be load-bearing in the stubs).

## 🟠 B469 — the guest-scope sweep B467 did NOT close: 14 mutating scripts, shape common, reachability unmeasured

The idea round that produced B467's fix named a follow-up it could not do: *"whether any OTHER
guest-scoped target shares F1's shape — succeeds wrongly rather than failing. That sweep is the
denominator this design is missing."* Here is the denominator; the exposure is not.

**MEASURED 2026-08-24** — guest-scoped scripts (they do NOT call `supervisor_kubeconfig`) that
mutate a cluster (`kubectl apply/create/delete/label/patch/annotate`, `helm upgrade/install/uninstall`):

| script | mutating calls | would it SUCCEED on a Supervisor? |
|---|---|---|
| `91-e2e-tenant-mechanism.sh` | 10 | likely — plain k8s API |
| `90-e2e-istio-existing.sh` | 10 | likely |
| `07-install-argocd.sh` | 9 | likely |
| `60-configure-tekton.sh` | 8 | likely |
| `46-install-istio.sh` | 7 | likely (helm) |
| `70-configure-argocd.sh` | 6 | likely |
| `06-install-harbor.sh` | 6 | likely (helm) |
| `45-install-traefik.sh` | 4 | likely |
| `49-psa-check.sh` · `24-lab-preflight.sh` · `24-builder-probe.sh` · `23-argocd-preflight.sh` · `20-bundle-load.sh` · `41-install-tekton.sh` | 1–3 each | mixed |

**So the SHAPE is common. The EXPOSURE is not, and that distinction is the whole row.** All 14 run
inside `make install-all` / `make platform`, i.e. a flow the operator entered *after* switching to
the guest — scenario-1 does that at its Step 6, and scenario-2 now does too (this session). What
made `vks-package.sh` different is that it is invoked **ad-hoc**, outside any flow that switches
first, **and** its install binds cluster-admin without a CONFIRM gate.

⚠️ **Do NOT "fix" this by adding the eager discriminator to 14 scripts.** The round was explicit:
both pre-existing consumers call `kubeconfig_is_supervisor` **LAZILY inside a failure branch** —
zero happy-path cost, structurally zero false-RED — and an eager pre-flight costs up to **20s** on a
blackholed endpoint (measured), on every one of them, for a hazard most cannot reach. B467's eager
gate is justified by ONE property that does not generalise: its operation **succeeds** on the wrong
cluster instead of failing.

**What is actually UNMEASURED, and how to settle it:** for each of the 14, does the operation
SUCCEED or FAIL against a Supervisor? Nobody has run them there, and running them there is exactly
what must not happen casually. The cheap, safe probe is per-script and read-only: does its FIRST
mutating call target a resource a Supervisor serves? A helm install into a fresh namespace does; a
`kubectl apply` of a Tekton CRD probably does; anything keyed on a guest-only object does not.

**Done when:** each of the 14 is classified SUCCEEDS-WRONGLY vs FAILS-LOUDLY with evidence, and only
the SUCCEEDS-WRONGLY ones — if any — get a guard, using the lazy pattern unless their operation
succeeds, in which case B467's eager form applies with the same rc=2-fails-open contract.

## ✅ B467 — `vks-package.sh` could install onto a SUPERVISOR, unconfirmed, binding cluster-admin — CLOSED

There is no `CLUSTER=` argument: the target is whatever `$KUBECONFIG` points at
(`Makefile:561` -> `scripts/vks-package.sh`). The override half was already sound (`os.sh:481`
snapshot-protects it). Two things were not.

⚠️ **The hazard was far worse than this row first said, and an idea-round adversary MEASURED it
against a live Supervisor AND a live guest.** This row claimed the `_list` "are Carvel Packages
visible" check was "the only thing between you and a wrong-cluster install". **That guard does not
exist:**

| | measured |
|---|---|
| a Supervisor serves the SAME package refNames at the SAME versions | `ako`, `cert-manager`, `cilium`, `cluster-autoscaler`, `contour` — **five byte-identical rows**; a Supervisor has MORE packages than a guest, not fewer |
| so `_list`'s "no Carvel Packages visible" die | **never fires** |
| and `install` never reaches `_list` anyway | it calls `_versions`, which **returns versions** on a Supervisor |
| and `install` has a CONFIRM gate | **NO** — only `uninstall` does |

So `make install-vks-package PACKAGE=<any>` aimed at a Supervisor proceeded **unconfirmed** and
created a **ClusterRoleBinding to cluster-admin** on the Supervisor control plane. Reachable because
`scenario-1.md:303` and `scenario-2.md:148` both tell the operator to
`export KUBECONFIG=./secrets/supervisor.kubeconfig` and nothing un-exported it. (Fairly: the package
targets appear **zero** times in either scenario, so this is an operator improvising, not a
documented walk.)

**DONE:**

- `vks-package.sh` prints `context @ api-server [KUBECONFIG=path]` on entry, on the install action
  line, and twice on the destructive path including inside the `CONFIRM=yes` refusal.
- `install` and `uninstall` now **REFUSE** a Supervisor via `kubeconfig_is_supervisor` (`os.sh:907`,
  API-GROUP discovery on `vmoperator.vmware.com`). **rc=2 (unreachable) FAILS OPEN** — an air-gapped
  or slow lab must not be blocked by a probe that could not reach it; the honest cost is that the
  gate is silent exactly when the operator is most confused. Measured latency: 0.09s warm guest,
  0.11s Supervisor, 3.1s host-unreachable, 20s blackholed.
- `list` stays **ungated** — it mutates nothing.
- `scripts/test-vks-package-guard.sh` — 6 offline cases (stubbed kubectl): Supervisor refused on
  both mutating verbs, the refusal names the cluster and the remedy, a guest is NOT refused,
  unreachable fails OPEN, `list` is not gated.

⚠️ **This is the ONE place an EAGER discriminator is justified, and the round was explicit about
why.** Both pre-existing consumers (`24-vks-k8s-version.sh:126`, `26-vks-cluster-status.sh:109`)
call it **LAZILY inside a failure branch** — zero happy-path cost, structurally zero false-RED,
because the operation FAILS on the wrong cluster and only the message needs correcting. Here the
operation **succeeds**. Do NOT promote the eager pattern elsewhere on the strength of this row.

**Follow-up the round named and this row does NOT close:** sweep for any OTHER guest-scoped target
with the same shape — succeeds wrongly rather than failing. That denominator is unmeasured.

## 🔴 B468 — a per-run kubeconfig override is BROKEN for Supervisor-scoped targets

The owner asked for ".env variables for supervisor and guest kubeconfigs, used as defaults, but able
to point a target at a different kubeconfig". Measured against the tree, the guest half already
works and the Supervisor half does not:

| requirement | guest | Supervisor |
|---|---|---|
| sane default | bare `$KUBECONFIG` — works | `supervisor_kubeconfig()` — works |
| **point a target at a different kubeconfig** | **WORKS** (`make <t> KUBECONFIG=…`, proven live in both forms) | **BROKEN** |

`supervisor_kubeconfig()` resolves by a RANKED candidate list, and MEASURED:

    VKS_SUPERVISOR_KUBECONFIG=<sup> KUBECONFIG=<other>  ->  <sup>        the explicit KUBECONFIG LOST
    KUBECONFIG=<other>  (nothing else set)             ->  the MAINTAINER-CONVENIENCE entry won

`load_env`'s own header states the invariant it breaks: *"A variable that selects WHICH CLUSTER you
are talking to must be owned by the caller. Config may supply a DEFAULT; it may not overrule an
explicit choice."*

**Interim, shipped:** `make help` now documents the asymmetry — `KUBECONFIG=` for guest-scoped
targets, `VKS_SUPERVISOR_KUBECONFIG=` for Supervisor-scoped ones, with the reason and the
`grep -rl supervisor_kubeconfig scripts/` derivation of which is which.

⚠️ **FOUR designs are REFUTED — do not rebuild them.** All measured by the idea round:

1. **A `guest_kubeconfig()` resolver mirroring the Supervisor one.** There is NO ordering that works:
   put `$KUBECONFIG` anywhere but first and `make <t> KUBECONFIG=…` becomes a silent no-op; put it
   first and the resolver degenerates to bare `$KUBECONFIG`, doing nothing for the hazard. The repo
   already reached this conclusion in production — `argocd-password.sh:113`: *"NOT
   `supervisor_kubeconfig` ALONE, WHICH WOULD INVERT THE SAME BUG … Try BOTH, take the first that
   ANSWERS, and say which one did: evidence, not ranking."*
2. **UNCOMMENTING the five kubeconfig vars** (the literal first step of the ask). It walks into the
   clobber class, and `check-env-clobber` would say **`ok`** on an unsound premise: its exemption
   ("snapshot-protected, so a per-run override survives") is true for a bare `$VAR` read and FALSE
   once a resolver ranks two protected selectors against each other — exactly what the measurement
   above shows. Leave all five commented.
3. **Declaring a SCOPE on each Makefile target.** 230 targets; an enumerated list with no gate, and
   a stale tag is worse than none (mis-tagged guest ⇒ false-block; mis-tagged supervisor ⇒ silently
   permits). Scope is already DERIVABLE: a script that calls `supervisor_kubeconfig*` is
   Supervisor-scoped (14 files).
4. **"Just un-export KUBECONFIG in the docs."** Wrong for scenario-1: every target between the
   export (`:303`) and the guest switch (`:528`) is Supervisor-scoped. The export IS the mechanism.

**The real fix, if pursued:** make `supervisor_kubeconfig()` honour an EXPLICITLY-SET `$KUBECONFIG`
— a distinction it currently cannot make — or adopt the `argocd-password.sh` shape (probe both,
take the one that ANSWERS, print which).

## 🟠 B466 — `.env` switch: install Istio via the VKS Standard Package instead of helm

Istio is the **only** one of our three platform components with an addon form to switch to. Harbor
and ArgoCD are **Supervisor Services** (scenario-1.md:6, "installs Harbor and ArgoCD as Supervisor
Services") — a different family, installed on the Supervisor, with no guest-cluster package
equivalent. Istio is a guest-cluster **Standard Package**, so on a real lab the mesh a tenant meets
is normally the package, not ours.

Today `INGRESS_CONTROLLER` has three values: `istio` (we helm-install), `istio-existing` (attach to
a mesh someone else installed, install nothing), `traefik`. The gap is the middle ground: **install
it the VKS way, then attach**. The repo already has the generic half —
`make install-vks-package PACKAGE=istio.kubernetes.vmware.com [PKG_VERSION=] [PKG_VALUES=]`,
lab-verified 2026-08-10 (install 20 s, uninstall 11 s).

**What is already MEASURED (2026-08-24), so nobody re-derives it:**

| | |
|---|---|
| this guest cluster offers | `istio.kubernetes.vmware.com` at **6 versions**, 1.27.1 → 1.28.5 (`+vmware.1-vks.1`) |
| is it installed? | **no** — the only PackageInstalls are antrea, gateway-api, guest-cluster-auth-service, metrics-server. That is why `istio-preflight` said "NO Istio detected" and rows fell to the helm branch |
| does the package pin istiod's memory? | **NO.** `istio.pilot.resources{,.requests,.limits}` all default `null`, so it inherits the upstream `{cpu: 500m, memory: 2048Mi}` — read from the package's own bundle, `config/upstream/istiod.yaml:3114-3116`, no overlay overriding it |
| consequence | **the addon path hits the SAME scheduling wall.** Switching to it does not avoid B-this-fix; it needs the same value via `PKG_VALUES` |

Full evidence + the bundle ref + the data-values snippet: `docs/vks-services/istio.md` §"The package
does NOT pin istiod's memory".

**Design questions the implementation must answer (do NOT assume):**

1. A fourth `INGRESS_CONTROLLER` value (`istio-package`?) vs a separate `ISTIO_INSTALL_METHOD`
   (`helm`|`package`) orthogonal to it. The second is probably right — "which mesh" and "how it got
   there" are different axes, and `istio-existing` already means "not us".
2. **Air-gap.** Our helm path pulls istio images from OUR Harbor via `global.hub`. The package pulls
   from Broadcom's registry unless `istio.meshConfig.imagePullSecrets` + a mirrored path are wired.
   An air-gapped install through the package is UNVERIFIED and is the hard part of this row.
3. The package version is Broadcom's line (1.27/1.28); ours is 1.30.3. `check-gwapi-istio-alignment`
   pins gateway-api against the istio minor, so switching methods changes that pairing.
4. Who owns uninstall — `make uninstall-vks-package` removes the package's own SA/binding, but our
   routes live in app namespaces.

**Done when:** one `.env` variable selects the method; both methods reach the same
`make verify-ingress` green; the air-gap question in (2) is answered with a measurement, not a
sentence; and the memory pin is applied on BOTH paths.

## 🔴 B462 — a THIRD copy of the Harbor-CA path, on the VM side, that B461's fix cannot protect

`walk-matrix.sh:530` hardcodes `~/vks-airgap-cicd/secrets/harbor-ca.crt` for the walkbox, while the
VM's effective `HARBOR_CA_FILE` comes from `.env.example:137` — `walk_env` emits `HARBOR_URL` but
**never** `HARBOR_CA_FILE` (grep: 0 emissions). **Four literals govern one value**: driver-host
`:997`, driver-VM `:530`, `.env.example:137`, `Makefile:635` fallback. B461 collapsed the host pair
only. If `.env.example`'s default moves, `:530` writes where the VM does not look — the same bug one
hop out, surfacing ~40 min in as a scenario-2 walk failure instead of a fast UNRUNNABLE.
**Fix:** emit `HARBOR_CA_FILE` into the VM `.env` from `walk_env`, derived from the same literal as
`:530` — one variable, both sides.

## 🟠 B463 — the diagnostic that MISLED the diagnosis is unchanged

`walk-matrix.sh:1002` prints `no Harbor CA minted` for **all seven** ways the producer can fail,
while `27-harbor-ca-from-cluster.sh:130-176` distinguishes them precisely (wrong CA / no answer /
SAN mismatch / not-an-anchor / zero bytes / FORBIDDEN / STALE_CA), each ending *"'$OUT' was NOT
touched."* The sentence was **FALSE in this very incident** — the CA *was* minted — and that is what
made a path bug read as a missing CA. **Fix:** append the log's last ERROR line, or just say
`no Harbor CA at $hca`.

## 🟠 B464 — the overlay-clobber CLASS is recorded only in a commit message; nothing gates it

`.env.state` overrides **14** variables for any host-side make, including the selector `KUBECONFIG`.
`check-env-clobber` reads `.env.example` and is **structurally blind** to the overlay — `load_env`'s
own comment already calls it *"a THIRD clobber channel that no gate can see"*. Two call sites are
pinned by hand today; the third, written in a hurry, re-enters this.
**Candidate:** assert that every `make -C "$WALK_REPO"` in `walk-matrix.sh` pins the selectors its
consumer then checks; RED-prove by deleting a pin. Needs an idea round — a naive version rots.

## ✅ B465 — CLOSED: collateral damage from the B461 bug, repaired 2026-08-24

The pre-fix bug wrote the **lab's** Harbor CA over `secrets/harbor-tls/ca.crt` — the **KinD**
Harbor's trust anchor. Confirmed: that file carried the buggy run's logged
`SHA-256 E7:FD:1E:C3:…`, while the (since-rebuilt) lab's CA is `13:A9:88:94:…`, and a KinD cluster
`vks-airgap-cicd` was live with its Harbor serving at `172.18.0.3`. The stale anchor **failed** to
verify it. `ca.key` (Jul 11) survived, so a CA regenerated from it with the leaf's exact issuer
(`CN = vks-lab-harbor-ca`) was the exact repair. Proven by handshake, not file presence:
`curl --cacert` → **200**; controls with the stale CA and with no CA → **rc=60**.

## 🔴 B461 — a stale `.env.state` silently makes BOTH scenario-2 rows UNRUNNABLE (measured 2026-08-24)

**Symptom.** Matrix run `run-20260824T040634Z-1480291`: `ROW 5 UNRUNNABLE — could not supply
scenario-2's credentials`, reason `cannot supply S2 contract: no Harbor CA minted`. Row 6 uses the
same `supply_s2_contract`, so it goes the same way. The harness says *"Not counted as a failure"*,
so the run reports `walked 4 of 6` rather than FAILED — easy to read as fine. **It is not: a third
of the matrix never ran.**

**The CA was minted.** `S2-HARBORCA.log` shows `wrote …/secrets/harbor-tls/ca.crt`, subject
`CN = Harbor CA`, with a SHA-256. The file exists and parses.

**Root cause — producer and consumer disagree because a STATE OVERLAY redirected the producer.**

| | |
|---|---|
| `walk-matrix.sh:975` checks | `${WALK_REPO}/secrets/harbor-ca.crt` (hardcoded) |
| the Makefile target writes | `$(if $(HARBOR_CA_FILE),$(HARBOR_CA_FILE),./secrets/harbor-ca.crt)` |
| `.env:997` | `HARBOR_CA_FILE=./secrets/harbor-ca.crt` |
| **`.env.state:13`** | **`HARBOR_CA_FILE=…/secrets/harbor-tls/ca.crt`** — a leftover **KinD** discovery |
| `make -pn` resolves | the `harbor-tls` path — the overlay is `-include`d LAST and wins |

⚠️ **CORRECTED 2026-08-24 — the mechanism above was WRONG TWICE.** An adversary round measured it
and I verified both:

- *"`SKIP_DOTENV=1` suppresses `.env` for the scripts but NOT for make's `-include`"* — **FALSE**.
  `Makefile:162` is `ifneq ($(SKIP_DOTENV),1)` around the `.env` include at `:178`, and its own
  comment says *"at the MAKE level here, and inside every script"*. It gates `.env` at **BOTH**
  levels. What it gates at **NEITHER** is **`.env.state` (`:150`) and `.env.kind` (`:135`)** — that
  is the real hole, and it is why naming the variable is the fix.
- *"the overlay is `-include`'d LAST"* — **FALSE and INVERTED**. `.env.state.make` is included
  **FIRST** (`:150`), `.env.make` second (`:178`); `regen_overlay_mk` rewrites `KEY=` to `KEY ?=`,
  so **FIRST wins**.
The cost of the wrong version: a reader widening the fix reaches for *"make SKIP_DOTENV cover make's
includes"* — already true, zero help — instead of the real lever. **Limit of my own check:** claim 1
is settled by READING the `ifneq`, not measured — `.env`'s value and the Makefile default are the
same string, so no probe can discriminate them.

**Fix (one line, in the LAB repo — §H.1 forbade touching it while the matrix was executing it):**
pass the path explicitly on the invocation at `walk-matrix.sh:981`, so producer and consumer agree
by construction —

    VKS_SUPERVISOR_KUBECONFIG="$kc" HARBOR_URL="${HARBOR_URL:-harbor.env1.lab.test}" \
      HARBOR_CA_FILE="$hca" SKIP_DOTENV=1 make -C "$WALK_REPO" harbor-ca-from-cluster

This is exactly the defensive form the comment at `:977-980` already prescribes for `HARBOR_URL`,
applied to the sibling variable it missed.

**Done-when:** a matrix run walks rows 5 and 6 (not UNRUNNABLE) on a box that has a `.env.state`
carrying a KinD `HARBOR_CA_FILE`. Prove it by leaving that line in place.

**The exposure is BOUNDED — audited, not left open.** There are exactly TWO host-side
`make -C "$WALK_REPO"` calls in the driver, both inside `supply_s2_contract`:

| call | state | why |
|---|---|---|
| `:952` `vks-cluster-status` | ✅ SAFE | passes `VKS_SUPERVISOR_KUBECONFIG="$kc"` explicitly, and `supervisor_kubeconfig()` takes that env var as its FIRST candidate. `S2-KUBECONFIG.log` proves it read the real lab (`phase: Provisioned`, `ControlPlaneInitialized: True`), not KinD. |
| `:982` `harbor-ca-from-cluster` | ⛔ BROKEN | passes `HARBOR_URL` and `SKIP_DOTENV` but **not** `HARBOR_CA_FILE`. |

`HARBOR_CA_FILE` is **not** emitted by `walk_env`, so the VMs never see it and **rows 1–4 are
unaffected**. The blast radius is exactly the two scenario-2 rows.

**How bad the overlay is, for context:** that `.env.state` (written by a KinD run at
`2026-08-24T00:13:47Z`) overrides **14** variables for any host-side make — including the selector
`KUBECONFIG` (KinD's), `HARBOR_URL=172.18.0.3`, `VKS_CONTEXT=kind-vks-airgap-cicd`. Only the two
that the driver names explicitly are safe. Every future host-side `make -C "$WALK_REPO"` must name
what it depends on.

**The fix is MEASUREMENT-VALIDATED, not assumed.** I suspected an `-include`d `VAR=x` would beat an
environment variable, which would have made the prescribed env-prefix fix useless. Measured — it
does not:

    HARBOR_URL=SENTINEL_ENV make -pn   ->  HARBOR_URL = SENTINEL_ENV     (env prefix WINS)
    make HARBOR_URL=SENTINEL_CMD -pn   ->  HARBOR_URL = SENTINEL_CMD
    neither                            ->  HARBOR_URL = 172.18.0.3       (the KinD leak)

So the env-prefix form is the right shape, the driver's existing `HARBOR_URL` defence genuinely
works, and the bug is purely the omitted sibling variable.

## RUN CONTRACT — the walkthrough matrix (rows 1–6)

**This is the operator's standing order, written down because it has had to be re-issued five times.
Nothing in it is optional and nothing in it may be narrowed. A run that violates any line of it is
not a run — it is a harness exercise, and reporting it as a matrix result is the failure this
contract exists to stop.**

### 0. The order, verbatim

1. *"cut a new lab and do rows 1,2,3,4,5 until they green from the first time, while watchfully whole
   reading scenario-1 and scenario-2, executing your own instructions line by line, observing full and
   real output, not using your mockery with exit codes and grepping for parts of the documents and
   output, do real job, do not assume, do not guess, verify and validate every fact you see."*
2. *"cut a new lab and run all rows again, from both vSphere VMs photon and ubuntu"* — **both OSes,
   both scenarios, six rows.**
3. *"After every lab cut, verify `make creds-show` / lab `make creds` endpoints and credentials
   ACTUALLY work — hit them, authenticate, drive the web UI in Chrome where an HTTP probe is not proof
   a human can log in."*
4. Read scenario-1 and scenario-2 **as a whole, as an end user**, executing line by line, observing
   full output, navigating **only** from the instructions below the commands just entered.
5. **Green FROM THE FIRST GO** — a fresh matrix over the FINAL tree passing on its FIRST pass. Every
   fix resets that clock. A row that goes green on a re-run after a fix is **not** a first-pass green.

### 1. What the six rows ARE (grepped from `nested-vsphere-lab/scripts/walk-matrix.sh`, not recalled)

| row | OS | lab state | doc | cut |
|---|---|---|---|---|
| 1 | ubuntu | NOTHING exists | scenario-1 | **A** |
| 2 | photon | EVERYTHING exists (what row 1 created) | scenario-1 | **A** |
| 5 | photon | EVERYTHING exists | **scenario-2** | **A** (runs LAST in A) |
| 3 | photon | NOTHING exists | scenario-1 | **B** (after the rebuild) |
| 4 | ubuntu | EVERYTHING exists | scenario-1 | **B** |
| 6 | ubuntu | EVERYTHING exists | **scenario-2** | **B** |

Cut A → **rebuild the lab** → cut B. Rows 5 and 6 are the *same* scenario-2 document on *different
OSes*; they are not a duplicate and neither substitutes for the other.

### 2. What rows 5 and 6 specifically require

Both are gated on `supply_s2_contract`, which must hand the row **ADMIN credentials AND a
driver-supplied GUEST kubeconfig — NOT the tenant path**. If it cannot, the row is
`UNRUNNABLE_ROWS`, **never walked**, and that is *not* a pass and *not* a failure: it is *no result*.
Reporting an UNRUNNABLE row as anything other than "never walked" is a false claim.

Their failures land in `S2_FAILED_ROWS`, tracked **separately** from `FAILED_ROWS`, because a
scenario-2 refusal must never gate cut B's rebuild.

### 3. Knobs (the complete set `walk-matrix.sh` reads)

`WALK_ROWS` · `WALK_SCENARIOS` · `WALK_CLUSTER_NAME` (+`_EXPLICIT`) · `WALK_SKIP_REBUILD` ·
`WALK_SKIP_CLONE` · `WALK_CLONE` · `WALK_REPO` · `WALK_RUN_ID` · `WALK_OUT_ROOT` (default
`/tmp/walk`, mode 0700) · `WALK_OUT_DIR` · `WALK_DNS_ATTEND_SECONDS` · `WALK_TKR_WAIT_SECONDS` ·
plus the lab/credential set: `VCENTER_HOST`/`VCENTER_USERNAME`/`VCSA_SSO_PASSWORD` ·
`VKS_NAMESPACE`/`VKS_USERNAME`/`VKS_SSO_DOMAIN`/`VKS_CONTEXT_NAME`/`VKS_STORAGE_POLICY` ·
`HARBOR_URL`/`HARBOR_USERNAME`/`HARBOR_PASSWORD`/`HARBOR_STORAGE_CLASS` ·
`ARGOCD_NAMESPACE`/`ARGOCD_PROJECT` · `VCF_CLI_SRC_DIR` · `LAB_ROOT`/`LAB_STATE_DIR`.

⚠️ `WALK_ROWS`'s default has **already shipped wrong once** (`"1 2 3 4"`, covering **neither**
scenario-2 row — nvl #65). Pass rows explicitly; never trust the default to mean "all".

### 4. Execution discipline — what "do real job" forbids

- **Read the whole document as an end user.** Not the blocks the harness extracted — the prose
  between them is the navigation, and a reader who cannot get from block N to block N+1 by reading
  it has found a defect even when every command succeeds.
- **Observe FULL, REAL output.** Not `rc=$?`. Not `| grep -c`. Not a tail. The verdict of a block is
  what it printed, read.
- **No exit-code proxies.** A completion notification's "exit code 0" is the LAST command's status
  and is a documented liar here (`; echo`, `| tail`, a cancelled run, a timeout-cancel). Never
  report pass/fail from it.
- **Verify and validate every fact.** A fact recalled is a claim; a fact grepped, fetched, or run is
  a measurement. Grade them differently and say which you have.
- **Do not assume, do not guess.** If a step cannot be settled, say it is unsettled and name the one
  command that would settle it.

### 5. After every lab cut — credential verification (item 3, in full)

`make creds-show` (repo) and lab `make creds` must be **exercised, not printed**: hit every endpoint,
authenticate for real, and **drive the web UI in Chrome** wherever an HTTP probe is not proof a human
can log in. A 200 from `curl` is not a login. A cert error in Chrome is a *trust* finding, not a
reachability finding — distinguish them and say which.

### 6. Security constraints — in force for the whole run

- **Secrets never in argv.** `curl -K` config under `umask 077`, `--data @-` on stdin, or env-by-name.
- **The vCenter SSO account LOCKS OUT AFTER 3 FAILED ATTEMPTS.** Never brute a password, never retry
  a failed auth blind.
- **Do not type passwords into web login forms.**
- **`harbor-robot.env`, `gitea-ci-token`, `webhook-token` must NEVER be blanket-deleted** — they are
  the only copy in existence.
- Walk logs carry live credentials: `/tmp/walk` and every per-run dir are **0700**, and a row log is
  citable only with that in mind.

### 7. Measurement discipline — how a result is read

- **The verdict is the per-invocation file**, `${WALK_OUT_ROOT}/run-${WALK_RUN_ID}/VERDICT-${WALK_RUN_ID}.txt`.
  Never a fixed path (a stale log lies), never stdout scrollback, never the exit code.
- **Read every row's own `WALK DONE` / `DOCUMENT` lines** plus the **"N of 6 designed rows"**
  denominator. `MATRIX FAILED` is the harness's wording for *did not complete successfully* — rows
  can reach `WALK DONE` and still be red.
- **`FAILED` and `UNMET` are different**: a failed block is a command that broke; an UNMET Expect is
  the document promising something the run did not print. Report both counts per row.
- **Do NOT edit `walk-matrix.sh` (or any script) while a matrix runs** — bash reads a script
  incrementally, so a rewrite mid-run yields `unexpected EOF` that reads as a product bug.
- **Kill the process GROUP, never the pid** (`setsid` + `kill -- -PGID`), and read `etime` after any
  kill. An orphaned driver has rebuilt a lab for 25 minutes while every later symptom was blamed on
  the lab.
- **A row's red is a hypothesis until root-caused.** Half of a matrix's reds have been the harness,
  not the product — and half of those "obviously harness" ones were real bugs. Prove which.

### 8. Rule Zero still applies

Adversary-review a **design** BEFORE implementing it, and again on the **diff** after. A fix to a
scenario doc or a script is a design. `isolation: "worktree"` on every dispatch.

### 9. Reporting

Times in **EST/EDT**. Commit and push roughly every 30 minutes. Report only: what shipped, what is
verified, what is still unknown.

## Open rows

> ⚠️ **"Open rows" IS A LOCATION, NOT A STATUS — 44% of what follows is already CLOSED.**
> Census 2026-08-17: **93 rows here, of which 41 are done** — 29 marked ✅ and 12 marked 🟢.
> Genuinely open: **17 🔴 · 8 🟠 · 14 🟡** (the remaining 13 carry ⚠️ REFUTED or no marker,
> which are also settled). So a session that reads this section top-to-bottom re-reads more
> settled ground than live work, and "work until the backlog is empty" measures the wrong thing.
>
> **Recount rather than trusting the numbers above** — they are a snapshot and will rot. Total rows:
> `grep -cE '^\| \*\*B[0-9]+\*\* \|' BACKLOG.md`. By marker:
> `for g in ✅ 🟢 🟡 🟠 🔴; do printf '%s %s\n' "$g" "$(grep -cE "^\| \*\*B[0-9]+\*\* \| $g" BACKLOG.md)"; done`
>
> ⚠️ Those are INLINE code spans on purpose, not a fenced block. This file's established
> code-block style is INDENTED (markdownlint MD046 is `consistent`, and it infers the style from
> the FIRST block). Adding one ```` ```sh ```` fence here flipped the inferred style to *fenced* and
> reclassified **10 pre-existing indented blocks at lines 325–976 as violations** — `docs-lint`
> went rc=2 with 11 findings when `main` was clean, and only ONE of them was in this text.
>
> ⚠️ **DO NOT "TIDY" THIS BY DELETING THE CLOSED ROWS.** Most of them are not records of work —
> they are **REFUTATIONS**, and several exist specifically so a design is not rebuilt (B130, B37,
> B67, B151, B154, B162 all say so in their own text). Deleting a refutation while leaving the
> claim it refutes elsewhere is the documented failure mode in `rules/common/configuration.md`:
> the file stops contradicting itself and starts being confidently wrong. MOVING them to a
> `## Closed rows` section is safe; deleting them is not, and neither is doing it unsupervised —
> this file is also two representations (this table, plus long-form `## Bxx — …` sections from
> line ~294), so a partial move would split a row from its own evidence.

| **B179** | ✅ **SHIPPED 2026-08-18 (PR #860) — the fix is at the END of this row; the headline below is the ORIGINAL problem statement, kept because the shipped design is NOT what it asked for.** **The `MECH=api` path probes `create applications` and then, since #835, unconditionally `get`s — probe BOTH, ADD not replace.** `70-configure-argocd.sh:289` asks only `argocd_can_i create applications "${ARGOCD_PROJECT}/*"`. #835 made the readback go through `argocd app get` on every api run, so a tenant AppProject role granting `create` WITHOUT `get` now selects `MECH=api` and dies ~500 lines later at the readback. **This repo already learned this exact lesson on the kubectl side** — `:233-250`: *"a Role with [get,list,create] selected MECH=kubectl and then died ~500 lines later at the refresh … BOTH verbs, because the path needs both and either one alone is a different late failure"*, and it warns the fix is **ADD, not REPLACE** (probing the second verb alone regresses the mirror case). Same shape, same file, second path. **NOT a blocker for certification**: rows 5/6 use an admin-minted token, so `get` is granted and the path works — this bites a REAL tenant, which is precisely who scenario-2 is written for. **Deliberately NOT fixed on 2026-08-18** even though the pattern to copy is 50 lines away and already reviewed: a certifying matrix was about to run over the just-merged tree, and tonight's `require_cmd` regression is the standing proof that an unreviewed "obvious" fix landed against a live run costs the whole run. Fix it in its own change, with its own round, and RED-prove the ADD (a `create`-only role must still select api; a `get`-only role must NOT). ⚠️ Note `walk-matrix.sh:141,146` REFUSE a `WALK_REPO` that is dirty or ahead of `origin/main`, so any fix must be merged and synced before the next matrix launch — you cannot carry it as a working-tree edit. 🔴 **IDEA ROUND 2026-08-18 — REFUTED, and the row's SCOPE is OVERSTATED.** Every technical premise held (`get` IS the right RBAC action — measured on the installed v3.0.19 CLI `[get create update delete sync override action invoke]` AND in pinned server v3.5.1 `application.go:795` `rbac.ActionGet`, with no further `EnforceErr` on the `--refresh` branch; `proj/*` IS the right scope — `glob.Match` has no separator so `*` spans `/`), and the fix is still wrong for three reasons. **(1) THERE ARE ≥3 VERBS, NOT 2.** `apply_application`'s api arm is `argocd app create --upsert` (`:625`); v3.5.1 enforces `ActionCreate` always (`:354`) and `ActionUpdate` (`:419`) whenever the spec CHANGED (`:408` `equalSpecs` short-circuits only when identical). `DEPLOY_REPO_CLONE_URL` derives from `GITEA_ARGOCD_URL`, and `os.sh`'s own comment records those addresses moving between cuts, so a re-run routinely changes the spec and dies at `:629` with *"Does your AppProject permit this destination and repo?"* — a MISDIAGNOSIS (it is `update` RBAC). Not inferred: `91-e2e-tenant-mechanism.sh:277` records the denial verbatim and WORKS AROUND it by deleting the Applications first. **(2) 3 OF 4 SITES CARRY IT** — `70-configure-argocd.sh:289` (this row) plus `23-argocd-preflight.sh:155` and `:172`, neither filed; only `:240/:247` got the lesson. And preflight is the tool `:629` NAMES as the remedy, so it keeps reporting *"argocd API: YES"* to a tenant whose run will die, while `:186` tells them to request `applications, create` — now demonstrably an INSUFFICIENT grant. **(3) THE FIX REGRESSES INTO THE WORST STATE.** The probe compares PATTERN vs PATTERN, so a name-scoped grant (`applications, get, myproj/javawebapp`) against the request `myproj/*` returns NO. A tenant with wildcard `create` + name-scoped `get` WORKS TODAY and would be downgraded to `MECH=request`, which renders files and EXITS 0 HAVING APPLIED NOTHING — the state `:310-316` calls *"SILENT SUCCESS … the worst possible default"*. `request` must never be reachable from a DERIVED denial. **SCOPE CORRECTION:** this bites only under `ARGOCD_MECHANISM=auto`; `:304` bypasses `can_api` for explicit api, and `:339-341` records that scenario-2 AND `walk-matrix.sh:393` both set api EXPLICITLY — so it affects no documented tenant path and did not affect rows 5/6. **AND THE ROW'S RED-PROOF IS HALF-VACUOUS:** *"a `get`-only role must NOT select api"* is ALREADY TRUE (`create=no` takes the `no)` arm and `get` is never probed), so it passes before and after — zero discrimination. The discriminating set is five cases, and they are not expressible today because the decision is INLINE at `:280-330`; `test-argocd-app-fetch.sh`'s PATH-stub + WITNESS device works only on a FUNCTION. **Revised plan:** extract the api-capability decision into `lib/argocd.sh`, then probe create → get → update as a graded ladder where only `create` gates and the rest WARN naming their late failure, one shared helper across all four sites, five-case hermetic suite. Residuals: whether a real VKS AppProject is ever written name-scoped (one read of `argocd-rbac-cm` settles it), and whether `equalSpecs` is stable across a byte-identical re-render (`make gitops` twice with a create+get-only token). ⚠️ **AND THE OBVIOUS NEXT FIX — call it B179a — WAS BUILT, RED-PROVEN, AND THEN REFUTED BY THIS REPO'S OWN TEST. Do not rebuild it.** Once the probe design above was refuted, the natural next move is to fix the CLASSIFIER instead: add a `permission denied|PermissionDenied -> FORBIDDEN` arm to `classify_argocd_failure`, since an RBAC denial currently classifies `UNKNOWN`. That was implemented and RED-proven in a scratch copy (RBAC `UNKNOWN` -> `FORBIDDEN`, with both shadow controls held). It is still WRONG, for three measured reasons. **(1)** `scripts/test-argocd-classify.sh:50-61` pins the current behaviour **deliberately**, with the rationale written out: *"A GENUINE RBAC DENIAL MUST NOT BE CLAIMED AS TRANSPORT … it must fall through to the caller's own AppProject message"*. So `UNKNOWN` is INTENDED, not a defect. **(2)** It is a **measured NO-OP at both write sites**: `argocd_transport_die` has arms for `STALE_CA|UNREACHABLE` and `UNAUTHORIZED` ONLY, and returns 0 silently for everything else — so `FORBIDDEN` and `UNKNOWN` take the IDENTICAL path to the caller's own die (*"argocd-server refused to create Application … Does your AppProject permit this?"*), which is already the RIGHT message for an RBAC denial. **(3)** At the THIRD site it is actively **wrong**: `argocd_can_i` (`lib/os.sh:1434`) returns `unknown|<class>` and the consumer interpolates that class into *"the argocd API probe FAILED TO ANSWER (…) — argocd-server never received the request"*, which for a real denial is doubly false (it IS a denial, and the server DID receive it). ⚠️ **A green suite would NOT have caught this**: `FORBIDDEN` hits that test's `*)` arm and PASSES. The test's PROSE caught it, not its assertion — which is why reading the consumer of a value before changing the value is the discipline here, not a RED-proof of the change in isolation. **The row's underlying observation — a create-only role dies late at the readback — remains CORRECT and OPEN. The fix must be re-designed; neither the two-verb probe nor the classifier arm is it.** ✅ **SHIPPED 2026-08-18 (PR #860) — the GRADED ladder, i.e. the shape the refutation prescribed rather than the one this row originally asked for.** `argocd_api_capability()` (`lib/argocd.sh:960`) probes `create` FIRST and returns immediately on `no`/`unknown`; only on `create=yes` does it additionally probe `get` and `update`. **ONLY `create` GATES.** A `get`/`update` denial emits a WARN naming the verb, the site that will fail late (`argocd app get`, the `--upsert`), AND the FALSE-ALARM disclosure — the probe asks `proj/*`, so a name-scoped grant (`applications, get, proj/oneapp`) answers `no` while the tenant can read the very Application about to be written. **That asymmetry is the whole point:** a downgrade on a DERIVED denial would make `MECH=request` reachable, and `request` renders files and EXITS 0 HAVING APPLIED NOTHING. Wired at `70-configure-argocd.sh:292`, pinned by `scripts/test-argocd-api-capability.sh` (17 assertions, hermetic PATH stub — no server, cluster or network), whose load-bearing cases are (2) and (3): `get`-denied and `update`-denied must STILL return `yes`. It also carries an invocation WITNESS proving the extra verbs are NEVER probed after a `create` denial — without it, an implementation that always probed all three would satisfy every verdict assertion while adding two unanswerable RPCs to a run that is already dead. ⚠️ **The row's OWN prescribed RED-proof was VACUOUS, and that is why the extraction is part of the fix rather than tidying:** it asked that *"a `get`-only role must NOT select api"* — already true and always was (`create=no` takes the `no` arm, `get` is never reached), so it passes identically before and after: ZERO discrimination. The five discriminating cases were not expressible at all while the decision lived inline at `70-configure-argocd.sh:280-330`. |
| **B180** | ✅ **CLOSED — one documented residual remains, an INFERENCE to re-measure, not open work.** **`test-argocd-addr-verdict.sh` is NOT hermetic under load — it went RED inside a full `static-check` and 20/0 standalone, on the same tree, minutes apart.** MEASURED 2026-08-18 while the certifying matrix was still finishing: `make static-check` reported `FAIL IP + correct CA -> rc 4 (want 3)`, `FAIL SAN name + correct CA -> rc 4 (want 0)`, `FAIL the SAN probe returned nothing usable: ''`. Four independent checks say the logic is fine: the suite passes **20/0 standalone on the same branch**, passes **20/0 on `main`**, **no commit in the branch touches** `lib/argocd.sh` / `os.sh` / the suite, and a re-run of the identical `make static-check` with the lab idle returned **rc=0**. `rc 4` on both cases plus "returned nothing usable" is a probe that never connected — the suite mints certs and drives a local TLS listener, so it needs a free port and a responsive box. **Why it matters more than a flake normally would:** it fails INSIDE the composite gate, whose fail-fast then MASKS every check after it, and it will do so at random in CI — where the diagnosis starts in the wrong file, exactly as it did here (my first move was to bisect 27 commits for a break that did not exist). Fix by making the listener's port ephemeral / retried rather than by loosening the assertions, and RED-prove the flake is gone by running the suite under deliberate load (`stress`/parallel `make`) rather than on an idle box, which is the condition that hides it. ⚠️ **CAUSE SEPARATED 2026-08-18 by a 4-arm measurement (N=4 each): it is a FIXED-PORT COLLISION, not load.** ARM 1 alone/serial → PASS×4. ARM 2 alone under FULL CPU saturation → **PASS×4**. ARM 3 two concurrent instances on the SAME port → **FAIL×3, PASS×1**. ARM 4 two concurrent on DIFFERENT ports (via the existing `TEST_ADDR_VERDICT_PORT` override) → PASS×4. Mechanism: `:95` `PORT="${TEST_ADDR_VERDICT_PORT:-18443}"` is a FIXED default, so a second instance cannot bind and its probes reach the first instance's listener (different CA → chain fails) or, once that one exits, a closed port. The readiness loop is 10 tries × up-to-2s + 1s sleeps ≈ 30s of budget, which is why a slow box was always a weak explanation — and ARM 2 settles it. **THE ROW'S PRESCRIBED FIX IS RIGHT** (ephemeral port; the repo already has the sanctioned `pick_port` idiom and the suite already has the env override to thread it). **THE ROW'S PRESCRIBED RED-PROOF IS WRONG:** it says to prove the fix under *"deliberate load (`stress`/parallel `make`) rather than on an idle box, which is the condition that hides it"* — but load is NOT the condition; ARM 2 is 4/4 clean under saturation. Someone following that recipe sees green on a test that never reproduced the defect and ships the fix unproven. The RED-proof must run **two instances concurrently**. ⚠️ Honest residual: my repro yields `rc 1 (want 3)` where the original logged `rc 4` twice plus an empty SAN — coherent if the difference is whether the other listener was still alive (mine start together; the original's had likely torn down, leaving a closed port), but that is an inference. A third arm (start A, let it exit, then run B) would settle it and is worth adding with the fix. ⚠️ **ROUND 2026-08-18 (measured, 6-arm oracle) — THE ROW ABOVE MIS-READS ITS OWN EVIDENCE.** An adversary drove the REAL suite against each occupant of its port (OpenSSL 3.0.13). Only ONE arm reproduces the recorded signature: a **PLAINTEXT/non-TLS squatter** (rc 4/4, 3 failures, 11s). Nothing listening -> rc **2**; the listener SIGSTOPped (maximum starvation) -> rc **2**; accept-then-close -> rc **2**; a foreign-CA TLS squatter -> rc **1** with only TWO failures and section 3 PASSING. So: (a) **`rc 4` is NOT "never connected"** — never-connected is rc 2; rc 4 means it DID connect, to something answering with non-TLS bytes, and the row's diagnosis is built on the inverted reading; (b) **"not load" is now MEASURED, not inferred** — even SIGSTOP never yields 4; (c) **the row's own RED-proof recipe (`stress`/parallel make) is structurally incapable of producing the defect**, so a fix certified under it would never have executed the decisive path — the house failure, committed by the recipe written to prevent it. The recipe that DOES reproduce: `python3 -m http.server 18443 --bind 127.0.0.1 &` then run the suite; it must report INCONCLUSIVE, not FAIL. (d) **The load-bearing defect is NOT the port literal**: `openssl s_server -accept` fails loudly on a busy port but into `>/dev/null 2>&1`, `SRV=$!` then holds a DEAD PID, and the readiness loop falls through with no failure — so ANY occupancy silently degrades into probing a stranger and is reported as a failure OF THE CODE UNDER TEST. Fix the ASSERTION first: `kill -0 "$SRV" || inconclusive ...` with an `inconclusive()` that EXITS NON-ZERO. (e) **The repo already solved this in a sibling and it was never carried across** — `test-fetch-ca-pin.sh` has the port scan, the liveness assert AND the non-zero `inconclusive()`, carrying its own note that using `exit 0` there once produced ZERO assertions read as a pass; whoever fixes B180 will reach for `exit 0`/SKIP and convert this false RED into a fake GREEN. (f) **The two suites contend BY CONSTRUCTION**: `test-fetch-ca-pin.sh` scans `seq 18443 18470`, starting on the exact literal this one hardcodes; serial order hides it only by luck of `$(sort)`, and both are in TEST_FAST so it can redden every PR — give this one a disjoint band (18471-18499). (g) **`pick_port` is the WRONG primary fix**: it only lowers the probability, and it moves the listener INTO the ephemeral range (measured `ip_local_port_range` 32768-60999) whereas 18443 sits safely below it, with its own TOCTOU window. (h) The masking claim is overstated — `run-test-set.sh` does not stop at first failure, so only `app-test` is masked. (i) A free discriminator went unrecorded: the failing run takes ~11s vs ~1s healthy. **Residual:** what was listening at the moment of the incident is unknowable now — the signature proves the CLASS, not the culprit; add `ss -ltnp \| grep ':18443'` to this row for next time. The rc map is OpenSSL-3.0.13-specific. ✅ **CLOSED 2026-08-18 — THE CULPRIT IS NAMED, and the fix shipped (PR #857).** This row's own residual said *"what was listening at the moment of the incident is unknowable now — the signature proves the CLASS, not the culprit"*. It is knowable now: a full `static-check` went RED again with the same shape, and `ss -ltnp | grep :18443` (the command this row asked for) found **`python3` pid 579792, started 14:05:01, cwd `/tmp/tmp.CkGUD4TLHb`, fds open on THIS session's task output** — a stray TLS oracle left by an EARLIER MEASUREMENT IN THE SAME SESSION, presenting `CN=vcsa.env1.lab.test`. Killing it took unmodified `origin/main` straight back to **20/0**, which is also the proof that `main` was never broken. ⚠️ **AND TODAY'S OBSERVATION PARTLY REFUTES THIS ROW'S OWN 6-ARM ORACLE.** The oracle predicts a foreign-CA TLS squatter yields *rc 1 with only TWO failures and section 3 PASSING*. MEASURED today against a real one: **rc 1, THREE failures, and section 3 FAILED** — informatively, printing the squatter's own SAN (`' DNS:vcsa.env1.lab.test'`). Section 3 greps for `DNS:argocd-server`, which no foreign cert carries, so "section 3 PASSING" cannot be right for that arm unless the oracle's squatter cert happened to carry that SAN. The oracle's rc map still stands; its per-arm failure COUNT for arm 5 does not. n=1 on my side, so this is a contradiction to re-measure, not a settled inversion. **THE FIX** (`ffad5d4`, refined in the follow-up): (1) a DISJOINT port band **18471-18499** — MY FIRST DRAFT SCANNED 18443-18470, i.e. straight into the collision (f) warns about, and the row is what caught it; measured `ip_local_port_range` 32768-60999, so the band is clear of the ephemeral range and (g)'s objection to `pick_port` does not apply; (2) **the load-bearing half** — assert the served cert's subject contains `argocd-server`, which covers (d) strictly better than `kill -0 "$SRV"`: it catches the dead-PID case AND the live-but-foreign case in one check; (3) it reports **INCONCLUSIVE and exits NON-ZERO**, per (e). RED-proven by re-creating the exact squatter (a `CN=vcsa.env1.lab.test` server pinned via `TEST_ADDR_VERDICT_PORT=18443`): the suite stops immediately naming it. GREEN 20/0 alone, and **20/0 concurrently with `test-fetch-ca-pin.sh` (25/0)** — (f) closed by construction. **Residual, stated not hidden:** a session-end sweep that looks for the WORK (clusters, containers, `make` jobs) does NOT see a test oracle bound to a loopback port; this one silently reddened an unrelated gate hours later. A second stray from the same session (`python3 - 56663`) was found and killed by the same sweep. |
| **B181** | ✅ **`require_internet` has ZERO blip tolerance (`--retry 0`), so ONE unlucky 5-second sample cost a 25-minute matrix row and the certifying property of a 3-hour run.** MEASURED from run 6 (`/tmp/walk/run-20260818T110322Z-3616190/MATRIX-row3-photon.log`), not inferred: `scripts/lib/os.sh:1106` printed `NO INTERNET on this box` and killed `make mirror-pull`, taking the rest of row 3 with it (3 FAILED blocks, ONE root cause). **The timing is the discriminator.** The probe ran `13:27:51Z` -> `13:27:56Z` = **5 seconds for BOTH hosts**, against ~20s had they timed out (`--connect-timeout 5 --max-time 10`, two hosts). Both failed **FAST**, which is resolution or routing — NOT packet loss, and NOT the slow-network case the function's own header comment was written against. ⚠️ **A confound I nearly shipped a wrong diagnosis on:** rows 3 and 4 ran on **DIFFERENT** throwaway VMs (photon vs ubuntu), so *"the internet blipped"* and *"this photon box's egress was never up"* were BOTH live, and row 4's success 11 minutes later cannot separate them. **What closes it:** row 3's OWN log shows it downloading OS packages from the internet ~18 minutes earlier (tdnf, `Total installed size: 34.69M` etc. at the head of the same file). So that box HAD egress and lost it mid-walk. ⇒ genuinely transient, on the box that failed. **Why this is worth fixing rather than absorbing:** the standing order is six rows green **on the FIRST pass**, and a probe that cannot survive one bad sample makes every future matrix hostage to a 5-second window. The asymmetry is stark — a FALSE no-internet costs a ~25-min row plus the run's certifying property; a slower diagnosis on a genuinely air-gapped box costs seconds, once. **NOT yet designed**: an idea round is out on whether a bounded retry is even the right LAYER (vs. more/different probe hosts, a DNS-or-TCP fallback, harness-level block retry, or reporting WHICH failure mode — DNS vs connect vs TLS — so the next occurrence is diagnosable instead of guessed at). ⚠️ Do NOT implement before that round lands: the function's existing design is deliberate (its header records that the prior behaviour ground **>2 minutes** against an absent network and then blamed a URL), so a naive retry risks re-creating exactly the failure it was built to remove. Any change must also keep `check-env-clobber` and `check-env-coverage` green — `INTERNET_PROBE_*` already live in `.env.example`. 🔴 **ROOT CAUSE CORRECTED 2026-08-18 — MY OWN FRAMING ABOVE IS REFUTED. The headline "zero blip tolerance" is the WRONG DIAGNOSIS, and it points at the wrong fix.** The idea round ran the probe and found that **`storage.googleapis.com` answers `HEAD /` with HTTP 400**, so `curl -sSf`'s `-f` turned it into exit 22 and **probe host #1 had NEVER succeeded in the life of that code**. VERIFIED INDEPENDENTLY, 5/5, on a box with working internet (github 200 in the same second). Consequences: **(1)** the comment claiming *"two hosts, so one provider being down is not read as 'no internet'"* asserted a redundancy that **did not exist** — github.com alone decided, so any 403/429/5xx from it (the abuse-detection class `http_get_retry` already fights) read as NO INTERNET. **(2)** all ~5 s of row 3's window therefore belongs to **github alone**, inside the 5.009 s `--connect-timeout` expiry — i.e. a connect TIMEOUT, which is blackhole/packet-loss, the **opposite** of the "failed FAST, so it is resolution or routing" conclusion I filed above. **(3)** decisively: an HTTP status line of ANY kind requires DNS + TCP + TLS + a full round trip to SUCCEED, so **that 400 was POSITIVE PROOF of internet that `-f` discarded** — under a correct predicate **row 3 would have been GREEN**, and nothing about the box's connectivity had to change to explain row 4 succeeding 11 minutes later. ⚠️ **AND THE RETRY I PROPOSED IS REFUTED**: it re-runs a predicate that is wrong by construction — it cannot fix a permanent false negative, it converts a github-specific outage into 3x the wait and then the SAME wrong message, and it costs ~31–45 s against ~10 s today, attacking the fast-diagnosis property the function's header spends 13 lines defending. **Do not build it.** **SHIPPED INSTEAD (PR #850, Stage 1):** the predicate is now *did anything ANSWER* (`%{http_code} != 000`; measured 4/4 — storage 400 -> internet, github 200 -> internet, NXDOMAIN -> 000/rc6, blackhole -> 000/rc28); the die NAMES the mode per host from curl's rc (6=DNS, 7=refused, 28=timeout, 35/60=TLS, 22=HTTP status) instead of naming none of them; and curl's own error text is KEPT — the old form passed `-S` to enable it and then `2>/dev/null`'d it away, asking for the diagnosis and deleting it. `scripts/test-require-internet.sh` pins it (26 offline assertions, curl stubbed; `test-scripts-fast` 61 -> 62). ⚠️ **A correction to the ROUND's framing too, measured here: the new predicate is IMMUNE to `-f`** — `-w '%{http_code}'` emits the code even when `-f` makes curl exit 22 (real network: `code=[400] rc=22`, predicate says INTERNET). So the load-bearing change is `-w` PLUS judging on the code; dropping `-f` is hygiene. Proven by mutation: restoring `-f` reddens ONLY the structural check, and all three behavioural cases still pass — correctly. **STILL OPEN (Stage 2, deliberately deferred):** whether a retry is ever warranted. It is now answerable rather than blind — the next occurrence arrives carrying a MODE: `28` might argue for one, `6` argues for fixing the resolver, `22` is the class Stage 1 closed. **Premise 1 of the original filing (structural vs transient) remains UNSETTLED and is not settleable retrospectively** — the evidence was discarded by the very `2>/dev/null` Stage 1 removed. Do NOT build a retry on the strength of "it was probably transient". ✅ **CLASS DENOMINATOR: 1 of 1 — SWEPT AND CLOSED 2026-08-18. Do not re-sweep.** Every other `curl -f`/`-sSf` in the repo was read and classified, and the distinction is sharp: **`-f` is wrong only when the question is REACHABILITY; it is RIGHT when the question is HEALTH or when you are fetching CONTENT.** **Correct (fetching content — a 4xx MUST fail):** `http_get_retry` (`lib/os.sh:1255`), `check-gwapi-istio-alignment.sh:43` (a go.mod), `walkbox.sh:152` (a cloud image), the Makefile's mise + PlantUML downloads. **Correct (health, where the status code IS the contract):** the `/api/healthz` waits in `99-verify.sh` and `50-seed-gitea-repos.sh`, and Harbor's `06-install-harbor.sh:279` — a healthz answering 4xx/5xx genuinely means NOT ready, which is the opposite of the internet probe, where **any** status answers the question. **Already correct by construction:** `91-e2e-tenant-mechanism.sh:211` passes **no** `-f`, so any response counts as "the LB is answering" — the right predicate; and `lib/harbor.sh:84` already captures `%{http_code}`. **The one candidate that LOOKED like the same bug and is not:** `test-builder-save-crane.sh:76` probes `/v2/` with `-sf`, and `/v2/` returns **401** on an AUTHENTICATED registry — which would read as never-ready. It is safe *only* because that test starts its OWN throwaway unauthenticated `registry:2` (line 69), so a 401 is unreachable there. ⚠️ **If anyone ever points that probe at a real registry, it acquires this exact defect** — change it to `%{http_code} != 000` first. ⚠️ **One live sibling risk, named not hidden:** `storage.googleapis.com` is ALSO the Istio chart host (`.env.example` `ISTIO_CHART_REPO`, consumed by `10-mirror-pull.sh`), so **any** future health/liveness check aimed at that host inherits the HTTP-400-on-`HEAD /` behaviour. A content fetch there (`helm pull`) is unaffected. 🔴 **IMPLEMENTATION ROUND 2026-08-18 ON THE MERGED CODE — IT FOUND A HIGH REGRESSION THAT #850 INTRODUCED. The row is REOPENED.** **(F1, HIGH — the fix traded a false NEGATIVE for a false POSITIVE, and the new one is worse in this repo's own threat model.)** Both arms measured by executing the pre-fix (`865a93a^`) and post-fix code against a stub returning curl's documented rcs: | middlebox | pre-fix | post-fix (shipped) | |---|---|---| | 403 corporate web-filter "Access Denied" | DIED ✓ | **ONLINE ✗** | | 407 proxy auth required, no creds | DIED ✓ | **ONLINE ✗** | | 200 captive portal (answers everything) | DIED ✓ | **ONLINE ✗** | | 404 air-gap DNS wildcard → internal page | DIED ✓ | **ONLINE ✗** | | 502 TLS-intercepting filter that blocks | DIED ✓ | **ONLINE ✗** | | 000 rc 6 / 28 / 7 (genuine blackhole) | DIED ✓ | DIED ✓ | ⇒ an air-gapped enterprise box behind a **filtering proxy** — the *normal* enterprise air gap, far likelier than a blackhole — is now told **"internet OK"**, and `make mirror-pull` proceeds into the >2-minute retry storm naming `storage.googleapis.com` that `require_internet`'s own header (`os.sh:1095-1102`) says it exists to prevent. **The generalisation is where it broke:** the suite's case 2 (`github 429 + storage 400 → ONLINE`) is RIGHT — a 429 from a real origin does prove reachability — but "any non-`000` answer proves reachability" over-reaches from it, and a filter's 403/407/502 is not a real origin's answer. ⚠️ **NO FIX APPLIED, deliberately.** The round's own `minimal_fix` (allow-list the codes a real origin returns: `case "$code" in 000|403|407|502|503) : ;;`) is explicitly marked **unverified by its author** and **trades the FP class for a NEW false-NEGATIVE class** (a genuine 503 from GCS would read as air-gapped). Its stated honest alternative — **accept the FP and say so loudly in the die and the header** — is a different decision, not a lesser one. This needs its own idea round before anything is built; do NOT reach for the allow-list because it appears here. **(F2, HIGH — MY TEST WAS NEARLY BLIND TO ITS OWN SUBJECT, and this half IS FIXED + RED-PROVEN.)** Three assertions grepped the die's **static closing prose** (*"a DNS line means the resolver, a TIMED OUT line means a firewall…"*), so they matched whether or not the per-host diagnostic was correct — or present. Measured mutants: **(A)** delete the per-host diagnostic entirely, the fix's whole point → **33/34**; **(B)** `_curl_rc_label "$rc"` → `_curl_rc_label 6`, labelling every host DNS whatever happened → **34/34 ALL PASS**, printing `storage… rc=28 (DNS — could not resolve the host)` / `github… rc=28 (DNS …)` and sending a blackholed operator to fix a working resolver, green. ⇒ **FIXED:** the four assertions now key on the **per-host line** (`rc=6 (DNS`, `rc=28 (TIMED OUT`, `rc=60 (TLS failed`, plus a new one for `rc=35` on the SECOND host to prove the label is per-host and not one value applied to both) — strings the static prose cannot contain. **RED-proven both ways: real code ALL PASS (35, up from 34 — the denominator MOVED, which is the tell the corpus grew); mutant B now 3 FAILED / 32 passed where it previously scored 34/34.** Same "a gate that reads its own prose" trap the file already guarded for its *structural* check and not for its *behavioural* ones. **(F3, MED — pre-existing, in the control under review.)** The die's sneakernet recipe (`os.sh:1174`) tells the air-gap operator `bundle-load && mirror-push && mirror-verify`, omitting **`make builder-push`**, which `docs/sneakernet.md:19`/`:141-143` puts between them. An operator following the FATAL verbatim never pushes the carried Maven builder, and Tekton's `maven-test` fails to pull `cicd/maven` several steps later **on a box with no way to re-fetch**. The asymmetry is the tell — the die's *internet* half correctly lists `builder-build`. **(F4, MED — the control covers 1 of ≥3 internet-requiring entry points.)** `require_internet` has exactly **one** caller (`10-mirror-pull.sh:34`), while `14-builder-build.sh` (pulls the maven base, resolves Maven Central) and `00-install-prereqs.sh` (downloads argocd/tkn/mise) both need the internet and bypass it — so the operator who confuses the two boxes, the exact scenario CLAUDE.md warns about, gets a registry error instead of the sneakernet guidance. **(F5, MED — B181's OWN no-retry rationale is now STALE.)** `man curl`: rc 28 covers `--max-time` expiry, not only connect timeout, so one slow-but-working link is still a FATAL with `--retry 0`. The header discloses this and argues no-retry because *"a bounded retry re-runs the predicate, and the predicate was the bug"* — **that reason no longer holds now the predicate is fixed**, so the retry decision is re-opened on its merits. **(F6/F7/F8, LOW.)** The `rc=22` branch is unreachable without `-f` (kept, to be commented as such); rc **5** (could not resolve **PROXY**) / 52 / 56 fall to the generic `curl exit N` label though their verdicts are correct; and the `[ "$errf" = /dev/null ] ||` rm-guard is pinned by no test — removing it scores 34/34, and it matters only as **root** on the read-only-`/tmp` box the fallback exists for. ✅ **CLEARED BY EXECUTION, recorded so nobody re-derives them:** the `code="$(…)" && rc=0 || rc=$?` idiom (captures curl's real rc, does not trip a caller's `set -e`); the first-host `break` (the die is reachable only after every host was probed, so it never claims what it did not measure); the `errf`/`/dev/null` paths (`[ -s /dev/null ]` reliably false, per-host truncation, `rm` before the die on both paths — residual: no `trap`, so a SIGINT leaks one `/tmp/internet-probe.*`); every rc→label mapping against `man curl`; **and this row's OWN "1 of 1" class denominator, RE-DERIVED rather than trusted** — all 13 other `curl -f` sites are downloads (where `-f` is *required*, to stop an error page being saved) or health checks that genuinely want 2xx. `test-builder-save-crane.sh:76` still holds, safe-by-construction. ⚠️ **Unverified and load-bearing:** that `storage.googleapis.com` still answers `HEAD /` with **400** and `github.com` with **200** — the live fact the whole design rests on. Settle with the one command the test file already prints. 🔴 **OWNER DECISION 2026-08-18 — CORPORATE / FILTERING NETWORKS ARE IN SCOPE. A constraint, not a preference, and it CHANGES THE ANSWER.** Asked directly whether anyone runs `make mirror-pull` from behind a corporate proxy — the operator on a work VPN, or someone following the demo at their employer — the answer was **YES**. Binding consequences: **(1) "Accept the false positive and disclose it loudly" is REJECTED.** That was my stated default and it was **wrong**. It was defensible only while the filtered-proxy case was hypothetical; it is not, so a guard that knowingly waves a blocked box through is unacceptable — and the softened form ("disclose plus a warning") is rejected with it. **(2) The HTTP-status ALLOW-LIST is now the WEAKER candidate, not the safe one.** A captive portal answers **200** — the single most likely portal response — defeating a `000|403|407|502|503` allow-list outright, and it inherits the enumerated-list rot this corpus keeps recording. Do not reach for it merely because F1 lists it first. **(3) The bar the fix MUST clear, in BOTH directions:** RED on a simulated **403** (web filter), **407** (proxy auth), **200** (captive portal) and **404** (air-gap DNS wildcard serving an internal page); and still GREEN on `storage.googleapis.com`'s genuine **HTTP 400** to a `HEAD /`, because re-breaking that is the failure that already cost a 25-minute matrix row. A design satisfying only one direction is not a fix. **(4) The shape that can meet it is IDENTITY, not STATUS** — verify *who answered*, not *that something did*, since only the former is unforgeable by a middlebox. Candidates: assert the **bytes of a known small object** (a portal's 200 fails a content check); **TLS identity** via `%{ssl_verify_result}`, the peer certificate's subject/issuer, or `--pinnedpubkey` (an intercepting proxy presents its OWN certificate — and this repo **already has `ca_verifies_endpoint` in `lib/tls.sh`** doing a related job, so this may be reuse rather than a new mechanism); plus `http_proxy`/`https_proxy`/`no_proxy` and `%{num_connects}`/`%{num_redirects}` as a cheap pre-check. ⚠️ **Until that fix ships the regression STANDS on `main`:** a filtered box is told "internet OK" and proceeds into the retry storm. So #850 left the guard **WEAKER than before** for exactly the population now confirmed in scope, and stronger only for the clean-network case. **State that plainly in any status; do NOT describe #850 as a net improvement.** 🔬 **DECISION ROUND 2026-08-18, run against REAL curl 8.5.0 — IT REFUTES THE PREVIOUS ROUND'S FP TABLE, which was a STUB ARTIFACT. Verdict: C, but NOT the C that was proposed. DO NOT touch `require_internet`'s predicate.** **(F1, CRITICAL — 3 of the 5 "false positives" ALREADY FAIL CLOSED, correctly.)** The probe hosts are `https://`, so an **explicit** proxy (the `http_proxy`/`https_proxy` corporate case) denies the **CONNECT** and curl never receives a status line. Measured with real curl: proxy-denies-CONNECT **407 → `%{http_code}=000`, rc 56, DIES ✓**; same for **403** and **502**; transparent MITM whose CA is **not** trusted → **000, rc 60, DIES ✓**. **The FP requires ALL THREE of: transparent (not env-configured) interception, TLS termination, AND the corporate root already in the box's trust store.** That is **ONE** real class, not five. ⇒ the owner's bar *"must distinguish 407"* **is already met**, and `http_proxy`-style environment detection is near-useless: it detects exactly the configuration that already fails closed, and is blind to the transparent MITM that is the only real gap. **(F2, CRITICAL — the rows are TWO cost classes and only ONE is dangerous; neither option A nor B touches it.)** Measured against the real `http_get_retry`: **403 / 404 / 502 / 407 → rc 1, `FATAL msg=failed to download <url> after 5 attempts`, 65.1 s, destination NOT written** (`--remove-on-error`) — loud, correct, and self-describing. **200 captive portal → rc 0, 0.0 s, and `<html>CORPORATE BLOCK PAGE</html>` is WRITTEN AS THE TEKTON MANIFEST.** `mirror_collect_images` then silently yields only `images.txt` entries (the Tekton controller images vanish), and in the sneakernet flow **that bundle is carried across the air gap** before `kubectl apply` fails on HTML. ⇒ **option A's allow-list `000|403|407|502|503` contains neither 200 nor 404** — it fixes the two-thirds that already fail loudly and misses the one that silently corrupts. ⚠️ **My own ">2-minute retry storm" framing is STALE** — that was the pre-2026-08-17 code with curl's inner `--retry`; the real cost for 403/404/502 is 65 s and a true error naming the URL. **(F3, HIGH — the suite cannot see this decision at all.)** Option A was patched into a scratch copy and the suite run: **ALL PASS (34)**, identical to `main`. Not one existing assertion discriminates A from what is shipped. Whatever lands needs new cases before it can be called covered. **(F4, HIGH — two of the three TLS discriminators I proposed are REFUTED, and a third rots lethally.)** Measured against a transparent MITM with the corporate root trusted: **`%{ssl_verify_result}` → 0** (curl DID verify, against the store that trusts the MITM) — **refuted**; **`%{num_redirects}` → 0 always** (`--head` without `-L` never follows) — **refuted**; **`--pinnedpubkey`** discriminates (real origin 400/rc 0, MITM rc 90) **but a stale pin against the GENUINE origin gives rc 90 / code 000 — a false air-gap FATAL on leaf-key rotation (~90 days at Google/GitHub), i.e. the row-3 failure on a timer. DO NOT SHIP IT.** Restricted **`--cacert`** works (real 400/rc 0, MITM rc 60/code 000) and rots only on a multi-year timescale — the sole TLS option worth keeping, and its failure direction is still a false FATAL. **(F5, MED — A's own false-negative is reachable.)** Correlated genuine **503**s from GCS and GitHub during a cloud incident (429 rate-limiting is the same shape one code over) would read as air-gapped and print the sneakernet instruction — putting back on the fatal side the very class the repo's comment says `http_get_retry` already fights. **(F6, MED — one caller, two UNGUARDED siblings.)** `grep -c require_internet` → **0** in both `00-install-prereqs.sh` and `07-install-argocd.sh`, and both fetch from the internet via `http_get_retry`. **`make deps` on a filtered box has no guard at all** — further evidence the verdict belongs where the fetches converge, not in a probe with one caller. 🟢 **PRESCRIBED (a separate claim from the findings): put the middlebox verdict where the USABLE question already lives, and leave the ANSWER probe alone.** **C1 (must-have, ZERO rot, closes the only silent failure):** after the fetch at `10-mirror-pull.sh:75` and `07-install-argocd.sh:111`, assert `grep -qE '^apiVersion:' "$dest"` and die naming a portal/filtering proxy. **RED/GREEN-proven on the REAL artifacts: GREEN 12 of 12 files in `bundle/manifests/` carry a column-0 `apiVersion:`; RED 4 of 4 portal bodies caught** (HTML block page, `<!DOCTYPE html>` sign-in, `Access Denied by Policy`, `{"error":"blocked"}`). It cannot rot, because it asserts the contract of the file **we require**, not a third party's incidental behaviour — and a `-L` 302-to-portal lands on the same 200 and is caught identically. **C2 (should-have, zero rot, zero extra requests):** `http_get_retry` already runs curl — add `-w '%{http_code}'` and carry the last attempt's code into its `die`; non-`000` → *"something answered with HTTP N but would not serve the file: you are behind a filtering proxy, not air-gapped"*, `000` → the existing sneakernet guidance. **This routes 403/407/404/502 correctly with NO status allow-list, and fixes all five call sites at once, including the two with no guard.** **C3 (optional, only if 65 s is too slow):** a second HEAD with restricted `--cacert`, requiring **both** hosts to fail before declaring middlebox, so one root rotation cannot produce a false FATAL. ⚠️ **Residuals, named:** whether the probe hosts emit a stable identifying response header was NOT measured (the round was forbidden from touching them; settles with `curl -sSI https://github.com | grep -iE '^(server|x-github-request-id):'`); the real-world frequency of transparent-MITM-with-trusted-root is unmeasured and the cost table assumes it is rarer than the row-3 false negative, which **has happened once in production** — **if it is common, C3 moves from optional to required**; `helm pull` of the Istio charts does not go through `http_get_retry` and writes `.tgz`, so `apiVersion:` does not apply and a gzip-magic analogue is unexamined; and `00-install-prereqs.sh` downloads a 238 MiB binary with **no content check of any kind** — C2 covers its die message, nothing covers a 200-portal body landing at `${BIN_DIR}/argocd`. That is its own row. ⚠️ **SUCCESSOR WORK 2026-08-18 — a DECISION ROUND on the corporate-proxy question prescribed C1/C2/C3; C1 and C2 are SHIPPED (PR #857), and C3'S SPECIFICATION IS NOT RECORDED ANYWHERE.** The round was run after the owner confirmed **corporate networks are in scope**, and it REFUTED the earlier stub-based false-positive table: 3 of 5 proxy shapes already fail closed, only transparent-MITM-with-a-trusted-root is real, and — the decisive finding — a **200 captive portal writes its block page as the Tekton manifest**, which is then CARRIED ACROSS THE AIR GAP. **C1 (shipped):** `assert_k8s_manifest` in `lib/os.sh`, wired inside `10-mirror-pull.sh`'s loop (all 5 manifests) and OUTSIDE `07-install-argocd.sh`'s if/else so it validates the CACHED file too — a portal-poisoned cache is non-empty, so `[ ! -s ]` would happily reuse it. 19 cases, GREEN 12/12 on real manifests, RED 5/5 on portal bodies; the mutation flips exactly 5, not 6, because the empty case is independently caught by the `-s` guard. **C2 (shipped):** `_http_fail_hint` + the three `http_get_retry` dies now name the status, so 000 / 4xx / 5xx / budget-expiry stop looking alike; it matters most at `00-install-prereqs.sh` and `07-install-argocd.sh`, which never call `require_internet` at all. Measured curl 8.5.0 first (`-w` DOES emit on the failing path under `-f`), and pinned the regression the fix could have introduced: a SUCCESSFUL `file://` transfer reports `http_code 000` with `rc 0`, so success must key on the EXIT STATUS. RED-proven in two parts (stub the function 13/20; strip the hint from the dies 3/20) — and B's first attempt reported 0 RED because the mutation string used a literal backslash-n against a real newline and matched ZERO sites, which looked exactly like a blind gate until the mutation was verified to land. **C3 IS NOT SPECIFIED HERE.** Its text lived only in the round's output and is not in this row, the PR, or any committed doc. **Do NOT invent it** — recover the round's verdict, or re-run the decision round with the same premise (corporate proxies in scope), before building anything called C3. ⚠️ **CORRECTED 2026-08-19 — THAT CLAIM IS FALSE, AND THIS ROW CONTRADICTS ITSELF.** C3 **is** specified, in this very row, ~400 characters earlier: *"**C3 (optional, only if 65 s is too slow):** a second HEAD with restricted `--cacert`, requiring **both** hosts to fail before declaring middlebox, so one root rotation cannot produce a false FATAL."* So the blocker was illusory — nothing needed recovering and no round needed re-running. This is the same defect the row's siblings keep hitting: **a later paragraph asserting the absence of something an earlier paragraph states.** ⇒ **No `C3` may be built until the tension below is resolved, but it is a DECISION, not a lost artifact.** ⚠️ **AND THE SPEC'S GATE DOES NOT MATCH ITS MECHANISM — do not dispose of C3 on the wrong axis.** The parenthetical gates C3 on **SPEED** (*"only if 65 s is too slow"*, where 65 s is this row's own measured cost for the 403/404/502 path, *"and a true error naming the URL"*), but the mechanism it describes is a **CORRECTNESS** guard: requiring both hosts to fail so that **one root rotation cannot produce a false FATAL**. Those are different justifications. **Concluding "65 s on a failure path is acceptable, therefore C3 is unnecessary" would be measuring the wrong thing** — the speed question would be settled and the false-FATAL question untouched. **The decision actually owed:** is a single-host middlebox verdict acceptable, given that a root rotation on that one host produces a FATAL on a healthy box? If yes, C3 is genuinely optional and this row closes with C1+C2 (shipped, PR #857). If no, C3 is REQUIRED regardless of the 65 s, and the parenthetical is simply mis-framed. **Nothing here should be built before that is answered** — and the answer needs no new round, only the owner. | ⛔ **DO NOT BUILD — re-confirmed 2026-08-21, and MY OWN premise was refuted by measurement.** I briefed a round on “give it `--retry 2 --retry-delay 3`”, asserting as load-bearing that **`curl --retry` does not retry a DNS failure**. **FALSE** (curl 8.5.0, NXDOMAIN, `--retry-delay 3`): retry 0 → **0.06s**, 1 → **3.11s**, 2 → **6.17s**, 4 → **12.29s** — perfectly linear, so it **does** retry DNS. The inverse also measured: **connection-refused is NOT retried** (0.00s at every count) and **timeout IS** (5.00s → 21.01s at `--retry 2`). And the fix is a **4.2× regression on the property the function exists for**: a genuinely air-gapped box goes ~10s → **~42s** (up to ~72s if the failure hits `--max-time`), walking back toward the >2-minute grind its own 13-line header was written to end. The row **and** `scripts/lib/os.sh:1266-1268` already said do not build it; I briefed it anyway. What is actually owed is the row's **C3 owner decision** on the single-host middlebox verdict — explicitly **not** a speed question. ⚠️ curl-version axis sampled at ONE point (8.5.0). ✅ **C3 IS CLOSED 2026-08-21 — MOOT, and the owner decision is RETIRED AS MALFORMED. No owner input needed.** ⚠️ **My first stated reason was WRONG and is corrected here** — I wrote *"require_internet already requires both hosts to fail before declaring MIDDLEBOX"*. It requires both to fail before declaring **NO INTERNET**; it never declares middlebox at all. Measured truth table from the loop (`lib/os.sh:1280-1292`): `400/200`, `400/000`, `000/200` → **ONLINE**; `000/000` → **DIES**; and `403/403` → **ONLINE**, which IS the middlebox case. The correct statement: **`require_internet` renders exactly ONE verdict, and there is no middlebox verdict anywhere in the internet path** (`grep -rn cacert scripts/` → 15 hits, **zero** in that path, all our own self-signed CA; `pinnedpubkey`/`ssl_verify_result` → **0**). So C3 guards a mechanism that does not exist — and the false-FATAL it exists to prevent **can only be produced by C3's own second probe**. Two further measurements close it: `require_internet` has exactly **ONE** caller (`10-mirror-pull.sh`), which is precisely the site C1's `assert_k8s_manifest` already protects (`:80` + `07-install-argocd.sh:118`), while `00-install-prereqs.sh` — which downloads a 238 MiB binary with no content check — never calls it, so C3 could never reach the one gap; and the row's *"helm pull is uncovered"* residual is weaker than stated, because `helm repo add` rejects a captive-portal body first (**measured**, helm v4.2.4: `not a valid chart repository ... cannot unmarshal string into repo.IndexFile`). ⚠️ **Do NOT overread this:** transparent MITM with a trusted root remains undetected by the probe. That is a **placement** answer — it is caught downstream at the artifact by C1 — not an absence of risk.
| **B186** | 🟢 **CLOSED 2026-08-18 — fixed, RED-proven, and the row UNDERCOUNTED BOTH SIDES.** Enumerating both sides mechanically rather than trusting the row: **10** files are `mktemp`'d (this row says 8) and **4** survived the trap (it says 3). The one it misses is **`PREENV_FILE`**, created at `:101` — LATER than the trap at `:96` — which is exactly how a file escapes a trap written above it, and is the reason the count was wrong. `OUT_FILE` / `STEP_OUT_FILE` / `EXPECT_LOG` carry step OUTPUT (the credential-bearing ones); `PREENV_FILE` carries only exported variable NAMES — lower risk, still not ours to leave behind. All four added to the trap: 10 mktemp'd, 10 trapped, **0 survivors**. 🔒 **AND THE ENUMERATED LIST IS NOW PINNED** — `test-walk-doc.sh` gained a case that DERIVES both sides from `walk-doc.sh`'s own source and fails naming any file that is created but not trapped, so adding a `mktemp` cannot silently pass. RED-proven: dropping `EXPECT_LOG` from the trap fails the case and names `EXPECT_LOG`. Deliberately a CASE in an existing test, not a new gate — inventing a control is the act Rule Zero says most needs its own idea round. Unblocked the moment the matrix exited (`walk-doc.sh` was being executed live; bash reads scripts incrementally). Original follows. — 🔴 **`walk-doc.sh` leaves a SECOND COPY of every step's output — credentials included — on every walk box, because three of its eight temp files are absent from the EXIT trap.** `:94` `mktemp`s eight; `:96` removes **six**. `OUT_FILE`, `STEP_OUT_FILE` and `EXPECT_LOG` survive every run on every VM. Found by the B153 idea round while inventorying the leak — it is INDEPENDENT of any redaction decision and of B182/B153's printer guards, because those govern what is PRINTED, not what is left behind. The walk boxes are throwaway VMs, which bounds the exposure but does not remove it: a box lives for the duration of a row and anything with shell on it can read the file. **Fix: add the three to the trap — one line.** ⚠️ **BLOCKED ONLY BY TIMING:** the live matrix is executing `walk-doc.sh` right now and bash reads a script incrementally (rule H.1), so land it after `ps -p <matrix-pid>` returns nothing. Provenance: `read-from-file` (the round did not execute it). |
| **B187** | 🟢 **PARTIALLY CLOSED 2026-08-18 (PR #868, 1e1c6a1) — BY A DIFFERENT FIX, AND THIS ROW'S OWN PREMISE WAS MEASURED FALSE.** ⚠️ *"a single transient kills the whole verify"* is **WRONG**: `23-mirror-verify.sh:68` is `fails=$((fails+1)); continue`, so the loop **COMPLETES** and dies at the end with a count. A transient costs **one false FAIL among N**, not the run. **AND THE FIX THIS ROW PROPOSED (wrap them in `mirror_retry`) IS REFUTED, measurably worse than nothing.** `lib/mirror.sh:87` justifies `mirror_retry` with *"crane has no built-in retry"* — **factually false** for crane 0.21.9, whose binary contains `remote/transport.NewRetry` and whose upstream `options.go` pins `defaultRetryBackoff{1s, factor 3, 3 steps}` over a predicate covering ETIMEDOUT/EOF/EPIPE/ECONNRESET plus 408/429/499/5xx. So `mirror_retry 5` × crane's 3 = **15 attempts per image**, two nested backoffs: Harbor down goes **0.12s → ~8 MINUTES of pure sleep**, a blackhole **~3.3h → ~16.8h**, and a REAL corruption takes 5× as long to reach the identical verdict — while the gate's *designed-for RED* is exactly N/N failing (the incident that motivated it wiped 36/36). It would also add ~20s of sleep to the repo's own `24-mirror-verify-red-test.sh`, whose `MANIFEST_UNKNOWN` is a 404 and therefore not even in crane's retry set. **THE REAL DEFECT, which no retry touches, IS FIXED:** `crane validate --remote` fails for two reasons with OPPOSITE remedies and both were reported as *"Harbor's copy is corrupt/incomplete (re-mirror)"*. Real crane stderr is **339 bytes** and the old `cut -c1-200` left the operator with `… dial tcp: lookup harbor.env1.lab.test on 12` — **`no such host` cut off entirely** — followed by an instruction that, on the air-gap box, means **re-carrying a 12 GB bundle across the gap. For a DNS typo.** Now classified (TRANSPORT / CORRUPT / UNCLASSIFIED), tallied separately, two verdicts, truncation 200→800, with `UNCLASSIFIED` deliberately failing toward CORRUPT because `unexpected EOF` is BOTH the network-cut and the 2026-07-13 corruption signature. 14 cases on REAL captured stderr, RED-proven 3 ways. **WHAT REMAINS OPEN, and it is NOT one policy:** `22-builder-push.sh:82` `crane push` is idempotent and its sibling `21-mirror-push.sh:77` IS wrapped — **that inconsistency is the real gap**, and the round found two MORE unwrapped sites this row missed (`21-mirror-push.sh:51` and `22-builder-push.sh:66`, both `crane auth login`, which kill the push before any image moves). Meanwhile `16-engine-trust-check.sh` must **NOT** retry at all — a one-shot probe whose `x509: certificate signed by unknown authority` is not transient and is not in crane's predicate; retrying makes a correct "no" take 5× longer. **Applying one wrapper to all eight creates a new defect at two of them.** Also open: `mirror_collect_images` depends on `$BUNDLE_DIR/manifests` being populated and, unlike `mirror_prune_cache`, has **no guard** — so a stale/empty manifests dir silently shrinks the verified set while reporting "N images intact". The original report follows. 🔴 **`crane validate --remote` and `crane digest` in `23-mirror-verify.sh` have NO RETRY AT ALL — and `mirror-verify` is a PREREQ of `make mirror`, iterates EVERY image, and runs on the AIR-GAP box.** MEASURED by the B185 round: `grep -c mirror_retry 23-mirror-verify.sh` = **0** at both `:65` and `:73`; same for `22-builder-push.sh:82,98` and `16-engine-trust-check.sh`. So the exposure is **8 crane call sites, not the 2** B185 named, and the unwrapped ones are exactly the ones on the box with no internet to diagnose from. On the blackhole shape the pull path at least gets Go's 165s-keepalive death followed by a retry; `mirror-verify` gets neither — **one transient kills the whole verify**. The irony worth naming: `crane validate --remote` is the command this repo added *because of* the Harbor blob-store incident (the only thing that ever caught it) and it is the least-protected call in the flow. ⚠️ **NEEDS ITS OWN IDEA ROUND BEFORE ANY FIX — do NOT just wrap it in `mirror_retry`:** retrying an **integrity** check 5x multiplies a LEGITIMATE corruption failure by 5 and turns a clean RED into a 14-minute one, which is the opposite of what the gate is for. The round declined to test that fix for exactly this reason. |
| **B188** | ✅ **CLOSED 2026-08-19 — the round REFUTED half my plan and I shipped only what it cleared.** Verdict `refuted-in-part`, instruments confirmed working. **STEPS 1+2 DROPPED, NOT DEFERRED.** Step 1 (add `ALLOW_PUBLIC_CHARTS`/`BUNDLE_SKIP_STATIC_CHECK` to both lists) is **not** the no-op I suspected — it does change `load_env`'s behaviour — but the behaviour it changes has **ZERO CALLERS**: measured, neither toggle appears as an assignment anywhere in `scripts/`, `Makefile`, `docs/` or `README.md`, not `=1` and not `=0`. Its entire value is threat (A), *an operator explicitly re-asserting `=0` — the default they already have — while their `.env` says `1`*. The documented usage (`ALLOW_PUBLIC_CHARTS=1`, no `.env`) already works and is **unchanged** by step 1, because the var is commented in `.env.example` so there is nothing to clobber. And step 2 existed **only to pay for step 1**: arm (c) processes exactly **one** variable in the whole file (of 56 uncommented vars only `HARBOR_CA_FILE` and `INGRESS_CONTROLLER` intersect `SELECTORS`, and the latter is `EXEMPT`-`continue`d first), `HARBOR_CA_FILE` is a path so arm (d) never flags it ⇒ **nothing to suppress today**. Measured both arms: enforcement change **zero** (`rc=1` either way); the only delta is a misleading `ok` line that step 1 introduces and step 2 removes. ⇒ (1)+(2) is *introduce a defect, then patch it*. **Arm (d) already catches these two if anyone uncomments them** — `rc=1`, clean, no misleading line. ⚠️ **The round ALSO refuted its own counter-hypothesis, and it matters:** it predicted arm (c)'s error branch was dead code (SELECTORS ≡ PROTECTED as sets — measured **33/33 identical**, not the row's old "14 and 32"). **Measured FALSE:** removing `HARBOR_CA_FILE` from the snapshot produced a genuine `rc=1 CLOBBER`, because the drift assertion is one-directional so *shrinking* PROTECTED keeps the subset and falls through to the error. **Arm (c) has a live, demonstrable RED — do not simplify it away.** And snapshot-alone really does give `FATAL: SELECTORS has FALLEN BEHIND`, so F1's one-commit requirement was real. **STEP 3 SHIPPED**, with the three things the round said it was missing: `71-argocd-register-guest.sh` now snapshots `ARGOCD_REGISTER_INSECURE` **before** `load_env` and restores after (closing BOTH threat A and B, and removing an existing **ratchet** — `.env=1` was unreachable by any per-run override); `.env.example:618-620` now states the invocation form and that a `.env` value is ignored **by design** (F7: the cited precedent `CA_STATUS_STRICT` justifies itself with *"there is NOTHING to protect — it appears NOWHERE in `.env.example`"*, which is **not** true here, so the transferable precedent is `SHOW_SECRETS`, which documents its prefix form); and the ignore is now **loud** (F8 — previously an ignored `.env=1` did not fail clean, it took the sibling `else` arm and registered insecure anyway with a *different* message). **Measured, all three arms, real `load_env`:** `.env=1`+caller `0` → **0** (was 1, the ratchet); `.env=1`+caller silent → **0** (was 1); **no `.env`+caller `1` → 1**, so `e2e-cross-cluster.sh:72`'s prefix still wins — the round traced it and confirmed a direct script invocation with no `make`, wrapper or `env -i` between. Pinned by `test-argocd-register-insecure.sh` (**6 arms**), whose **arm 6 binds it to the REAL script**: arms 1-5 replay the ordering inline, so deleting the snapshot from the product would leave them green — RED-proven exactly that way (delete the restore → only arm 6 fires; remove it from the probe → arms 1/2/4/5 fire and **arm 3 stays green**). **STEP 4 KEPT:** declining `ENGINE_SKIP_CA_RED` is correct. ⚠️ **The round's CRITICAL-class finding is now B196** — `VKS_INSECURE_SKIP_TLS_VERIFY`, the same shape and worse covered, filed rather than shipped because it has two consumers (needs a shared helper) and one is the VKS auth path. **Residual, explicitly not verified:** no e2e was run against step 3 — settle with `make e2e-cross-cluster`; and whether an ArgoCD Cluster Secret written `insecure:true` is re-read on every sync stays write-path-read / consume-side inferred. ⚠️ **One guard deliberately NOT built:** F8's `log_warn` (the "your `.env` value was ignored" message) has **no test arm** — deleting it would go unnoticed. Judged acceptable because it is a **diagnostic, not the control**: with it gone the ignore still happens and the ratchet stays closed; only the loudness is lost, which is a legibility regression rather than a security one. Arm 6 guards the actual control. If you add the arm, key it on the CONDITION SHAPE (`[ "${ARGOCD_REGISTER_INSECURE:-0}" = "1" ] && [ "$_argocd_register_insecure_snapshot" != "1" ]`), never on the warn text — every one of those words also appears in that file's comment block, which is the grep-the-docstring trap. ⏸️ Original follows. — ⚠️ **STATE RE-MEASURED 2026-08-19, so the next session need not re-derive it — and a naive grep gets it WRONG.** `grep -c <toggle> scripts/check-env-clobber.sh` returns **1 or 2** for all four remaining toggles, which reads as *already in SELECTORS*. It is not: extracting the actual `SELECTORS='…'` string and grepping THAT returns **0 for all four** — the hits are COMMENT prose. Same trap as B193 (a grep finds the string but not the kind of thing), twice in one session. **Measured truth:** the snapshot list in `lib/os.sh` currently protects 14 names (`HARBOR_CA_FILE`, `VKS_CA_CERT_FILE`, `ARGOCD_CA_FILE`, `HARBOR_PASSWORD`, the three `*_CA_SHA256`, `HARBOR_INSECURE`, `ARGOCD_INSECURE`, `MIRROR_VERIFY_FAST`, `ARGOCD_ADMIN_PASSWORD`, `GITEA_ADMIN_PASSWORD`, `VCENTER_CA_FILE`, `VCENTER_INSECURE`) and `SELECTORS` holds **32** entries; **`ALLOW_PUBLIC_CHARTS`, `BUNDLE_SKIP_STATIC_CHECK`, `ARGOCD_REGISTER_INSECURE` and `ENGINE_SKIP_CA_RED` are in NEITHER.** That is consistent — the drift assertion is `PROTECTED ⊆ SELECTORS`, deliberately one-directional — so the gate is green today and adding a name to `os.sh` ALONE would turn it RED, which is F1. ⇒ **The remaining work is unchanged and still needs both lists in one commit**, plus F2's suppressed `ok`, scoped to the two toggles where (A) is the real threat, with `ARGOCD_REGISTER_INSECURE` taking the `creds.sh` pre-`load_env` pattern instead. ⏸️ Original follows. — 🟡 **ROUND 2026-08-18: my (A)/(B) SPLIT WAS CONFIRMED BY MEASUREMENT WITH A WORKING CONTROL — and the fix I proposed does not ship as specified.** I asked whether I was inventing a distinction to let me ship the cheap half. Measured against the real `load_env`, with `HARBOR_INSECURE` (already snapshotted) as the control: **(A) caller=0 + `.env`=1** → the control reads **0 SURVIVED** while all four unprotected toggles read **1 DEFEATED**; **(B) caller silent + `.env`=1** → **all five read 1 ARMED**, control included. The same mechanism protects in (A) and does nothing in (B); `[ -n "${!_sel:-}" ]` is exactly why. So the split is real, threat (B) is genuinely unclosable by the snapshot, and `.env.state` is NOT a channel for these four (none is `state_set` — unlike the `HARBOR_INSECURE` trio). ⚠️ **F1 — my plan was INCOMPLETE and would have turned the gate FATAL:** `PROTECTED` is `sed`-derived from `load_env`'s list and a drift assertion requires `PROTECTED ⊆ SELECTORS`, so adding the four names to the snapshot alone gives `rc=1 FATAL: SELECTORS has FALLEN BEHIND load_env's snapshot list`. Both lists must move together. ⚠️ **F4 — A UNIFORM FIX IS WRONG; the four differ sharply.** `ENGINE_SKIP_CA_RED` skips a *diagnostic* positive control and never gated anything (no `die` on either branch; the LEG row visibly reads `not tested`) — legitimate `.env` config. `ALLOW_PUBLIC_CHARTS` removes a `die` so a chartless bundle becomes a green internet install — a **fidelity/false-green** problem, not confidentiality, and legitimate on a deliberately dual-homed box. `BUNDLE_SKIP_STATIC_CHECK` removes a `die` on carried-binary linkage — **availability**, and the doc already says install `file(1)` instead. `ARGOCD_REGISTER_INSECURE` is **categorically different and the only one that is never legitimate**. 🟢 **SHIPPED 2026-08-18 — the two independent halves the round told me to land FIRST, both of which attack threat (B) at its source and neither of which touches `load_env`.** **(F7) THREE toggles shipped their commented line at the ENABLING value, inverting the file's own convention** — measured with an anchored grep (my first grep was the sloppy instrument: it matched the *usage example* `SHOW_SECRETS=1 make creds-show` at `:82` instead of the toggle's own `# SHOW_SECRETS=0` at `:83`). Eight ship `=0`, matching their code default, so uncommenting is a **no-op**; `BUNDLE_TOOLS_FORCE` (`:914`), `BUNDLE_SKIP_STATIC_CHECK` (`:920`) and `ALLOW_PUBLIC_CHARTS` (`:1607`) shipped `=1`, so uncommenting **ARMS** the weakening behaviour — in a file `make env-init` copies verbatim, with the operator's mental model set by the majority. All three consumers traced and confirmed `${V:-0}` (the round classified `BUNDLE_TOOLS_FORCE` by shape and left tracing it as a residual; traced here: `20-bundle-load.sh:143`/`:253`). Flipped to `=0` — three characters. **Two-arm proof:** operator uncomments the line → NEW file gives effective **0** (no-op), OLD file gives **1** (armed). `check-env-coverage` and `check-env-clobber` both still rc=0. **(F3) `ARGOCD_REGISTER_INSECURE` had its visibility exactly BACKWARDS** — `71-argocd-register-guest.sh:198` set `"insecure":true` on the DELIBERATE opt-in with **no `log_warn`**, while the *accidental* no-CA fallback at `:205` warned. And this is the worse branch, because the choice **does not end with the run**: it is written into the ArgoCD **Cluster Secret**, so every later sync to that destination dials with TLS verification off. Now warns in three lines, naming the persistence and the remedy. **(F8) A control's own comment stated something measurably FALSE** — `check-env-clobber.sh:264` said adding these to the snapshot *"would silently disable this arm"*. Settled by source-read: the protected branch at `:156` only LOGS and does **not** `continue`, so execution falls through and the arm still fires. Corrected in place, with what is actually true (it adds a reassuring `ok` line above the error — a legibility regression, not an enforcement one). This repo's own rule is that a fact inside a control is part of the control. ⚠️ **STILL OPEN, sequenced by the round:** the snapshot addition **with** F1's `SELECTORS` update and F2's suppressed `ok`, scoped to the two toggles where (A) is the real threat; and `ARGOCD_REGISTER_INSECURE` gets the **`creds.sh` pre-`load_env` pattern instead** (it closes BOTH (A) and (B), which the snapshot cannot), because for that one an accepted residual is NOT acceptable. **(F6) The `WCP_POLL_SECONDS` `how:` gate arm is SOUND** — 0/56 false-RED reproduces, and it is robust rather than lucky (0 uncommented vars carry a `# how:` line at all; reach is 44 commented vars, so it is genuinely derived). ⚠️ **Anyone implementing it must break the block walk at `^# *[A-Za-z_][A-Za-z0-9_]*=`** — the round's own first walker ran back past a preceding commented assignment and manufactured 6 phantom near-misses. **Not measured:** whether an ArgoCD Cluster Secret written `insecure:true` is re-read on every sync (write path read, consume side inferred). ⏸️ Original follows. — 🟡 **The boolean-toggle gate (PR #868, 1ae45ec) closes the `.env.example` half ONLY — an operator's OWN `.env` can still arm four security toggles, and nothing scans it.** `lib/os.sh:501` records verbatim that the clobber is armed by *'the operator doing the DOCUMENTED thing in their `.env`'*, which `check-env-clobber` never reads (by design — it is a gate over the COMMITTED file). `creds.sh` is immune there because it honours ONLY the pre-`load_env` environment; **`ENGINE_SKIP_CA_RED`, `ALLOW_PUBLIC_CHARTS`, `BUNDLE_SKIP_STATIC_CHECK` and `ARGOCD_REGISTER_INSECURE` have NO such snapshot**, so a line in a personal `.env` silently weakens them for every run on that box. Do NOT describe the toggle class as closed. **Open question for a round, not a prescription:** whether the answer is the creds.sh pattern applied to each (a pre-`load_env` snapshot per toggle — four small changes, each needing its own RED-proof), or something structural. Note the snapshot list in `load_env` is NOT the answer: measured, its `if [ -n "${!_sel:-}" ]` guard restores only a value the CALLER set, and the threat here is the caller setting nothing. 🟢 **THE ROUND ON THIS ROW FOUND A FIFTH TOGGLE AND IT WAS A LIVE CRITICAL — SHIPPED 2026-08-18 (PR #868, `7b93ac9`).** `CA_STATUS_STRICT` is not an operator knob at all: `Makefile:623` does `preflight: export CA_STATUS_STRICT = 1` and `:620` says why — inside `preflight` a missing Harbor CA is fatal because *"nothing else catches it"*. But `24-lab-preflight.sh` calls `load_env`, whose `set -a` sources `.env` **after** the Makefile's export, so **one line in a personal `.env` disarmed the only check for a missing Harbor CA** — MEASURED: exports 1, `.env` says 0, effective **0**. It is also a RATCHET: `CA_STATUS_STRICT=1 make preflight` loses to the same `set -a`, so there was no per-run way back. Fixed with the pre-`load_env` snapshot (the `creds.sh` pattern this row names), `test-ca-status-strict.sh`, 4 cases. **Arm 2 is not padding** — a "fix" that hardcodes 1 passes arm 1 and makes every non-preflight caller unconditionally strict. The test also asserts `CA_STATUS_STRICT` is **absent** from `.env.example`, because the moment it becomes a documented knob, ignoring the operator's `.env` stops being obviously right. ⚠️ **The four toggles this row was filed for are STILL OPEN** — `ENGINE_SKIP_CA_RED`, `ALLOW_PUBLIC_CHARTS`, `BUNDLE_SKIP_STATIC_CHECK`, `ARGOCD_REGISTER_INSECURE` have no snapshot. Do not read the CRITICAL as closing them; it closed a fifth case nobody had listed. 🟢 **A SIXTH AND A SEVENTH CHANNEL FOUND + CLOSED 2026-08-18 (PR #868, `3d68b34`) — and one of them was `.env.state`, which NO gate can read.** Two independent rounds found it; I re-measured with a discriminating control: `HARBOR_INSECURE=0` + overlay `1` → **1**, `ARGOCD_INSECURE=0` + overlay `1` → **1**, `MIRROR_VERIFY_FAST=0` + `.env` `1` → **1**, while the already-snapshotted `HARBOR_URL=X` + overlay `Y` → **X** (the control proving the probe was not simply clobbering everything). `.env.state` is written by our OWN `state_set` and sourced LAST by `load_env`, so it outranks the command line — and CLAUDE.md already records this incident through the `.env.example` channel (`make e2e-kind HARBOR_INSECURE=1` silently ran the full SECURE stack), so it is a RECURRENCE via a new sink. The first two pin **TLS verification** off with no per-run way back; the third makes `crane validate` `--fast` (`23-mirror-verify.sh:14-15`: manifest/config only), skipping the layer-blob download that caught the 2026-07-13 Harbor wipe — 153 manifest links, ZERO blobs, a state `--fast` passes clean. All three snapshot-protected; `test-insecure-toggle-snapshot.sh`, 8 cases, and **ARM 2 is the half that constrains the fix** (an unconditional snapshot would force the secure path onto an insecure Harbor and break every pull). ⚠️ **STILL OPEN, all measured, none built:** (a) **`WCP_POLL_SECONDS`** is a clobber the boolean arm structurally CANNOT see — its default is `10`, not a boolean — and `.env.example` itself says in capitals *"LEAVE THIS COMMENTED"*; a round measured uncommented→10 beats a caller's 1, commented→1 survives, and the gate passed either way. Its prescribed fix (flag an uncommented var whose own block carries a `# how: … <VAR>=` line, claimed 0/56 false-RED) is DERIVED rather than an enumerated list, so it is a real candidate — but it is a NEW GATE ARM, i.e. a new control, and Rule Zero wants its own idea round. (b) **`.env` is the INVITED channel**: `make env-init` is `cp .env.example .env` (`02-env.sh:15`), so the operator's file is a verbatim copy carrying every commented toggle, and uncommenting one there is the documented action — measured to defeat an explicit `VAR=0` for all five of `ENGINE_SKIP_CA_RED` / `ALLOW_PUBLIC_CHARTS` / `BUNDLE_SKIP_STATIC_CHECK` / `ARGOCD_REGISTER_INSECURE` / `SHOW_SECRETS`. (c) **`--raw`** is a second reveal path with no `.env.example` mention. 🟡 **AND RE-GRADE THE `CA_STATUS_STRICT` ITEM ABOVE FROM CRITICAL TO LATENT.** A round measured that with today's `.env` the Makefile's export SURVIVES — the check is ARMED — because the variable is absent from `.env.example` and `check-env-coverage.sh:66` classifies it INTERNAL, so nothing invites an operator to set it. The mechanism is real and the fix is right; the severity was mine and it was too high. |
| **B182** | 🟢 **GUARD SHIPPED 2026-08-18 (PR #868, ae56ad7) — but the CLASS IS NOT CLOSED, and that distinction is the whole point of this marker.** `creds-show` now masks passwords when stdout is not a terminal; `SHOW_SECRETS=1` forces the reveal. Built to this row's idea round, not to the row as written: the toggle is snapshotted BEFORE `load_env` (F5 — a `SHOW_SECRETS=1` line in the operator's own `.env` is IGNORED by design, because `set -a` would otherwise re-arm the leak invisibly), the mask is applied to the three real-secret VALUES and not the column (F4 — the `(no login; …)` notes, the `_unset_pw` placeholders, the `<ARGOCD_AUTH_TOKEN … not a password>` row and the `<- INITIAL secret; superseded …` annotation all stay legible), and `docs/access-uis.md`, `docs/make-targets.md` and `.env.example` were updated in the SAME commit (F8). **THREE RED-PROVEN CASES** where the suite previously asserted NOTHING about that column: `_reveal=1` always -> two fail; snapshot-after-`load_env` -> the `.env` case fails; clean -> all pass. ⚠️ **My first `.env` case was VACUOUS and passed against BOTH mutants** — without copying `.env.example` into the throwaway `REPO_ROOT`, `load_env` FATALs and "the secret is absent" is trivially true; fixed, plus a positive control that the render produced a real row. ⚠️ **RESIDUAL F1 IS NOW CLOSED, AND BOTH OF ITS CONCLUSIONS WERE MEASURED FALSE (2026-08-18, PR #868 745259b/e6ea5c1) — corrected here rather than left to mislead.** F1 said the completion was *"BLOCKED because `creds.sh` reads that script through a command substitution"* and that *"the thing that actually closes the class is the value-keyed redactor B153 prefers"*. **Neither holds.** (1) NOT BLOCKED: `creds.sh:226` already passed `--wait 0` as an ARGUMENT and `argocd-password.sh` already had a `while [ $# -gt 0 ]` parser, so `--raw` was one line on an existing path — the command substitution is precisely why an explicit flag is the right answer and a `[ -t 1 ]` test is the wrong one. (2) THE REDACTOR DOES NOT CLOSE IT: measured across all six of run 6's row logs, 16 credential occurrences reached the walk logs — the `creds-show` guard alone closes **14**, the value-keyed sink redactor alone closes **12**, and **both together still close 14**, i.e. the redactor adds **ZERO**. What closes it is masking at the PRINTER: `argocd-password.sh` now carries the same pre-`load_env` snapshot and a single `_emit` for all three exit-0 paths, taking it to **16/16**. **So the two leaks F1 identified were real and are now gone; the remedy it named was not the one that worked.** **Still do not read this 🟢 as "every credential class is closed"** — see B153's own residual and the `.env`-half residual of the toggle gate. The original report follows. 🔴 **`make creds-show` has NO tty/SHOW_SECRETS guard, so it prints every credential in CLEARTEXT into any non-terminal sink — and the walkthrough makes that a WALKED STEP.** MEASURED 2026-08-18 by running it from a non-tty: it emitted the Gitea admin, Harbor admin and ArgoCD initial passwords as plain text. **The sibling repo already solves this and this one does not** — `nested-vsphere-lab/Makefile:88` sets `SHOW_SECRETS ?= 0` and `:363` gates on `if [ -t 1 ] || [ "$(SHOW_SECRETS)" = "1" ]; then reveal=1; else reveal=0; fi`, whose own comment states the intent verbatim: *"capture must not silently harvest them"*. A grep of this repo's `creds-show` for `SHOW_SECRETS`/`-t 1`/`reveal` returns **zero hits**: it always reveals. 🔴 **THIS IS B153's MECHANISM, IDENTIFIED — and it moves that row's fix UPSTREAM.** `docs/scenario-1.md:34` lists **step 13** as `make creds-show`, and the walk harness EXECUTES the document's commands with stdout redirected to a per-row log — i.e. **non-tty by construction**. So every row that reaches step 13 writes live credentials into `/tmp/walk/MATRIX-row*.log`. B153 has been chasing this DOWNSTREAM (redact the log; its preferred robot-shaped `sed` was measured to cover only 1 of 2 real credential lines, and a shape-keyed redactor misses 3 of 4 printers). ⇒ **the single upstream point is the PRODUCER**: a `creds-show` that hides by default in a non-tty cannot leak into a log, a pipe, a CI capture or an agent transcript, whatever the consumer does. ⚠️ **NOT FIXED — it needs an idea round, and the naive port has TWO known hazards.** (1) **It may break the walk's own Expect checks.** If any `Expect:` line in scenario-1 §13 asserts on a credential's presence or shape, hiding it turns a passing row RED — so the round must enumerate what §13 actually asserts BEFORE the guard lands, or the first certifying run after it goes red for a reason that is not a defect. (2) **It changes what an operator piping the target sees**, which is a documented workflow (`docs/access-uis.md:5` calls it *the* way to get credentials) — nvl's answer is the `SHOW_SECRETS=1` escape hatch, and the doc must be updated in the SAME change or the runbook silently stops working. Both are exactly the sibling-port trap B133 recorded: the pattern exists next door, and porting it is a hypothesis, not a solution. ⚠️ **Disclosure: I demonstrated this defect by committing it** — running `make creds-show` from a non-tty during run 7b put three cleartext credentials into an agent transcript. nvl's equivalent would have printed `<hidden: not a terminal — re-run with SHOW_SECRETS=1>`. The credentials belong to a lab whose Harbor does not exist yet (the NOTHING cell), so the practical exposure is low; the CLASS is not. 🟢 **HAZARD 1 RESOLVED 2026-08-18 — and it resolves FAVOURABLY, which materially de-risks the fix.** I read §13 instead of speculating. Its Expect is: *"every URL and login — Harbor, ArgoCD, Gitea, Tekton, one row per app. Each value is labelled **DISCOVERED** (read live) or **STORED** (remembered)."* ⇒ it asserts on **URLs, LOGINS (usernames), one row per app, and the DISCOVERED/STORED labels** — and **NOT on password VALUES**. So a guard that hides ONLY the password column, leaving the URL, the username and the provenance label intact, satisfies this Expect **unchanged**, and hazard 1 does not block the port. That leaves hazard 2 (the operator piping the target, `docs/access-uis.md:5`) as the only one the round still has to answer, and nvl's `SHOW_SECRETS=1` is the candidate answer. ⚠️ **A SEPARATE QUESTION I RAISED AND DELIBERATELY DID NOT ANSWER, because I am standing in the wrong cell to answer it.** §13's Expect promises each value is labelled **DISCOVERED** or **STORED**. The output I captured shows a *report-level* provenance block (`values below : from YOUR .env …`, `state overlay: none`) and a table whose columns are `Service | URL | Username | Password` — **with no per-value DISCOVERED/STORED label visible**. That is either (a) correct for this state, since in the NOTHING cell nothing has been discovered and there may be nothing to label, or (b) a real doc-vs-output gap. **I ran it in the NOTHING cell only, so concluding either way would be the one-operating-point error this corpus keeps recording.** The run itself decides it for free: rows 2 and 4 walk §13 in the **EVERYTHING** cell with Harbor/ArgoCD/Gitea installed, so if the labels are genuinely absent it surfaces there as an UNMET, and if they appear only when something IS discovered, (a) is confirmed. **Read those rows' §13 blocks before filing anything.** 🔴 **IDEA ROUND 2026-08-18 — `ship-with-changes`, and it REFUTES THIS ROW'S CENTRAL CLAIM. Do not build the guard as specified above.** **(F1, HIGH — "the producer is the single upstream point" is FALSE, and the obvious completion is BLOCKED.)** `docs/scenario-1.md:376` runs **`make argocd-password`** inside a live ```bash block at **Step 5 — EIGHT steps before `creds-show`** — and `scripts/argocd-password.sh` `printf`s the password to stdout unconditionally. ⇒ guarding `creds-show` alone takes the per-row leak from *3 credentials at step 13 + 1 at step 5* down to *1 at step 5*: **the log still contains a live ArgoCD admin password, and the row now LOOKS remediated.** That is B153's own recorded shape (*"the preferred sed covered 1 of 2 real credential lines"*) repeating one level up, which is exactly what this row claimed to escape. **And the obvious completion cannot be taken:** `scripts/creds.sh:167` calls `argocd-password.sh` through a **command substitution**, i.e. always a pipe — so porting `[ -t 1 ]` into that script makes `creds-show`'s Password cell read `<hidden…>` **on the operator's own terminal** (measured). If it is ever guarded it needs an explicit machine-consumption flag (`--raw`) that `creds.sh` passes, **not** a tty test. **(F2, HIGH — `[ -t 1 ]` FAILS OPEN under a PTY, and the canonical capture tool allocates one.)** Measured, four shapes: bare / redirect / `| tee` → **HIDDEN**; `walk-doc.sh:675-676`'s verbatim inner pipe → **HIDDEN**; `$( )` capture → **HIDDEN**; **`script -qec … /dev/null` → REVEAL**. So the guard fires correctly for the walk (`walk-matrix.sh`/`walkbox-vm.sh` ssh carry no `-t`), but is defeated by `script(1)`, `ssh -t … | tee`, `tmux capture-pane` and any harness allocating a pty. ⇒ the sibling's own comment *"capture must not silently harvest them"* is **false for the tool literally named `script`**. Direction is right where it fires, so this is a scope disclosure, not a refutation — **state it in the code comment and do NOT try to detect `script`** (an enumerated list that rots). **(F3, MED-HIGH — a guard without cleanup is a false sense of safety.)** Measured on this box: **16** files under `/tmp/walk/*.log` already contain the credential table (of 98 logs), all mode **0600** — which limits exposure but does not remove it, and the same logs exist on each walkbox VM. The commit that adds the guard must say the pre-existing corpus is unremediated, or add a purge; it must **not** be described as "the leak is fixed". **(F4, MED — hide the VALUES, not the COLUMN.)** Four of the Password column's contents are **not secrets**: `(no login; read-only dashboard)`, `(no login; health at …)`, `<ARGOCD_AUTH_TOKEN from .env — not a password>` (`creds.sh:195`), the `_unset_pw` placeholders (`:141-144`), and the `<- INITIAL secret; superseded if you ran 'argocd account update-password'` annotation (`:349`) — which the repo added **specifically** because a bare secret there reads as *"this is your password"*. A column-level mask deletes the tenant/KinD/lab distinction ~60 lines of comments exist to preserve. ⇒ apply the mask at the three assignment sites (`harbor_pw`, `gitea_pw`, `argo_pw`) only when the value is a real secret, keeping the annotation outside it. **(F5, MED — `SHOW_SECRETS` in the operator's OWN `.env` disables the guard forever, invisibly.)** `load_env`'s `set -a` exports it, and `check-env-clobber.sh` scans **`.env.example` only** — `lib/os.sh:501-502` records this exact gap verbatim (*"the clobber is armed by the operator doing the DOCUMENTED thing in their .env"*). One `SHOW_SECRETS=1` line, added by an operator tired of typing it, permanently re-arms the leak **including inside the walk**. ⇒ snapshot `SHOW_SECRETS` from the **pre-`load_env`** environment and honour only that; document it **COMMENTED** in `.env.example`. **(F6, MED — it would ship with ZERO RED-proof, and the gate would exercise only the hidden arm.)** Measured: grepping `test-creds-show.sh` for any Password-column sentinel returns **0 hits** — the repo's own creds-show gate makes **no assertion on that column at all** — and its `render()` (`:95`) calls `creds.sh` inside `$( )`, a pipe, so post-change **all 11 cases would run the hidden path with nothing noticing**. ⇒ two new cases are mandatory: piped + `SHOW_SECRETS=0` → sentinel present AND the known value absent; `SHOW_SECRETS=1` → value present. **(F7 — MY HAZARD-1 RESOLUTION WAS RIGHT, FOR A WEAKER REASON THAN THE TRUE ONE.)** I concluded §13's Expect survives because it asserts labels not values. The stronger fact: `walk-doc.sh:710` extracts **only backtick-quoted literals** (`` `([^`]+)` ``, len ≥ 6, no `<>`), and §13's Expect contains **ZERO backticks** ⇒ `_tot = 0` ⇒ **the block is not CHECKABLE at all**. Independently, the checker only tests `lit in out`, so added text can only turn NOT-SEEN → SEEN, and no doc literal can match a random password. Hazard 1 holds, harder. **(F8 — hazard 2 ANSWERED: the documented workflow is intact.)** An operator at a terminal is unaffected (tty → reveal), so `docs/access-uis.md:20` stays true. But `docs/access-uis.md` and `docs/make-targets.md:14` must mention `SHOW_SECRETS=1` **in the same commit**, or the first operator who pipes it files a bug against a working guard. 🟢 **THE RESIDUAL NAMES THE REAL DESIGN, AND IT IS ALREADY IN-TREE.** The value-keyed redactor B153 prefers exists at **`scripts/50-seed-gitea-repos.sh:111` AND `:149`** — `${pw_out//"$GITEA_ADMIN_PASSWORD"/<redacted>}` (I re-grepped: **two** sites, where the round cited one). It is the only design that covers **all** printers at once, and it is **complementary to the guard, not superseded by it**. ⇒ the honest plan is: guard `creds-show` (values not column, pre-`load_env` snapshot, two RED-proven cases), **disclose that the class stays open at `argocd-password.sh`**, and treat the value-keyed redactor as the thing that actually closes B153. ⚠️ **Verification order matters, and the round names the arm most likely to be got wrong:** the `.env`-immunity test (F5), because the naive `${SHOW_SECRETS:-0}` *after* `load_env` fails it. And after any fix, grep the next row's log for the ArgoCD password — **if that grep comes back clean, distrust the grep before believing the fix.** |
| **B185** | ⚠️ **ROUND VERDICT 2026-08-18: DO NOT BUILD A BOUND — and this row's CORE MECHANISM CLAIM IS HALF-WRONG, which is what made a bound look necessary.** (1) **`mirror_retry` is NOT 'structurally incapable of helping'.** MEASURED by `getsockopt` on a socket dialled identically: Go's `DefaultTransport` sets **`TCP_KEEPIDLE=30 TCP_KEEPINTVL=15 TCP_KEEPCNT=9`** (the kernel default is 7200s — Go OVERRODE it), so a **blackholed** read errors at **30+9x15 = 165s**, crane returns non-zero, and `mirror_retry` DOES fire. Only **accept-then-stall** (a hung proxy / MITM appliance / registry deadlock) is genuinely unbounded — measured at 90s and 300s with rc=124 and **zero bytes** of output. The row justified a bound on the premise that covers the RARER shape, and never stated that for the common shape `mirror_retry` makes it **5x worse**: 5x165s + backoff(2+4+6+8) ~ **14.5 min per image**, which LOOKS exactly like the hang it is not. (2) **The exposure is 8 crane sites, not the 2 named** — and the UNWRAPPED ones are on the AIR-GAP box: `23-mirror-verify.sh:65,73` (`crane validate --remote`, `crane digest`) have **`grep -c mirror_retry` = 0**, plus `22-builder-push.sh:82,98` and `16-engine-trust-check.sh`. `mirror-verify` is a **prereq of `make mirror`**, iterates EVERY image, and is the command this repo added *because of* the Harbor blob-store incident — the only thing that ever caught it — and it is the least-protected call in the flow. **That, not the bound, is the genuine defect; it needs its OWN round** (retrying an INTEGRITY check 5x has its own failure mode). (3) **The row's own leading candidate — an OCI-cache-size stall watchdog — is REFUTED on COVERAGE, not feasibility.** The dir does grow incrementally (measured 0 -> 91.7 MB -> 114.8 MB in ~4s), but `crane push` writes to a REGISTRY, `validate --remote` DISCARDS what it fetches, and `digest` fetches a manifest — none produce local growth. It covers **1 of 8** sites, and specifically not the air-gap ones. **Do not build it; it is the candidate that looks best on paper.** (4) **B113 is the in-repo precedent this row omits, and it CONTRADICTS the naive fix**: it measured the sibling class at BOTH failure modes and concluded verbatim that *'a `timeout` wrapper would have been the wrong fix'* — the accepted remedy was removing the dependency. (5) **'Not observed happening' is WRONG for the class**: B113 hung `make ci` 22+ minutes and `creds.sh:158` records a **45-minute CI hang** from exactly it. (6) **The operator is NOT blind** — `lib/progress.sh:39` already prints a timestamped `[N/TOTAL]` heartbeat BEFORE each pull, so a wedge presents as *'the last line is 40 minutes old'*. **The asymmetry that settles it: the option with a defensible N covers 1 site; the options covering all 8 have no defensible N.** The original report follows. 🟡 **NOTHING IN THE STACK CAN END A WEDGED `crane pull` — a hung image pull hangs a matrix row, or a real air-gap `make mirror`, FOREVER and with no diagnostic.** MEASURED in-tree 2026-08-18: `scripts/walk-doc.sh` contains **zero** occurrences of `timeout`; `walk-matrix.sh`'s timeouts (`:207 :227 :238 :269 :296 :309 :318 :582 :703 :708 :711 :719 :722 :790 :872`) cover only its kubectl/openssl/supervisor probes and never the walk itself; and `mirror_retry` (`scripts/lib/mirror.sh:89`) is `while :; do if "$@"; then return 0; fi; ...` — it retries a command that **RETURNS**. A TCP read that never completes never returns, so `MIRROR_RETRIES` (default 5) is structurally incapable of helping. The same holds for `helm repo add`/`helm pull` at `10-mirror-pull.sh:165-168`, which use the identical wrapper. ⚠️ **HONEST PROVENANCE — THIS HAS NOT BEEN OBSERVED HAPPENING.** It was found while diagnosing what looked like a stall in run 8 row 2 and turned out **not to be one** (the host log was simply ~9 minutes behind the box; see the D.4 sharpening in PR #868). So the gap is real and structural, and its blast radius is unmeasured. **DO NOT "fix" this by adding `timeout N crane pull` without an idea round** — the naive fix converts a working air-gap mirror into a failing one the first time a large image crosses a genuinely slow link, and there is no defensible N: the only number this project has measured is a **15s** maximum inter-image gap on THIS lab, which says nothing about a customer's constrained WAN. A wall-clock cap is also the wrong shape — the thing to bound is **stall** (no bytes for N seconds), not total duration, and `crane` exposes no such flag, so the candidate space includes a curl-style low-speed limit, a watchdog on the OCI cache dir's size, and doing nothing. **Round it before building it** (RULE ZERO: inventing a control is the act that most needs the idea round). **Cheap partial mitigation that needs no round and no code:** the D.4 probe now in `docs/matrix-standing-rules.md` lets an operator tell a hang from a lag in 40 seconds, which is what turns this from an unbounded outage into a bounded one. |
| **B130** | ✅ **CLOSED — REFUTED (idea-round, `vks-adversary`, 2026-08-17); the premise was FALSE and the fix would have remedied a failure that does not occur.** This row claimed the missing `osConfiguration` stanza means `make vks-cluster-create` builds a cluster that CANNOT PULL FROM HARBOR. I measured the ABSENCE of the stanza and asserted a PULL FAILURE — two different claims, joined by an unearned "so". **MEASURED, and it already works:** `scripts/40-install-gitea.sh:30` pulls Gitea FROM HARBOR into the GUEST cluster and `:62` BLOCKS on `rollout status --timeout`; Harbor's cert is privately issued (`CN = Harbor CA`, scenario-1.md:520); and the Timings table records `install-all` completing in **10 m 26 s** and **8 m 14 s** with `verify` green — on a tree with **zero** `osConfiguration` (`grep -rn osConfiguration scripts/ k8s/` = 0). An x509 failure would have timed that rollout out. It did not. **THE FREE WIN, now taken:** harbor.md's same-Supervisor auto-trust row was graded `community — verify on a lab` while the flow already DEPENDED on it and passed; promoted to **lab-verified** with the evidence chain, which `docs/lab-validation-plan.md:463` had already specified as the action. **FOUR MORE REASONS NOT TO BUILD IT:** (1) the CA does not EXIST at create time — script numbering encodes it, `25`-vks-cluster-create runs before `27`-harbor-ca-from-cluster — so a mandatory `secretRef` names an absent Secret on rows 1 and 3 every time; not circular (Harbor exists at §4) but a walkthrough REORDERING. (2) the `--dry-run=server` gate CANNOT catch it — an absent Secret is a cross-object existence question, not a schema one — so it would land 4–9 min later inside `vks-cluster-status`, the worst place. (3) `envsubst` has no conditional, so the stanza is in EVERY render; moving it script-side voids this row's own "mechanical coverage" argument by taking the YAML out of `k8s/` where yamllint and `check-cluster-template-vars` see it. (4) a TENANT never runs `make vks-cluster-create` — scenario-2 §3c is explicitly the `kubectl edit cluster/<name>` path — so the change helps only the scenario-1 admin, the one persona measured not to need it. **IF the capability is ever wanted** (defensible for a Harbor on a DIFFERENT Supervisor, which the evidence above does NOT cover): a post-create `kubectl patch` target guarded on an explicitly-set env var — rows 1/3 byte-identical, no template change, no reordering. Four lines, not thirty-six. **Residual, unmeasured:** whether admission accepts a `secretRef` to an absent Secret is INFERRED, not measured — it is the single highest-value lab check if this is ever revisited. |
| **B129** | 🟢 **CLOSED 2026-08-17 by the run that reproduced its path — it was a CASCADE, not an independent defect, and BOTH of this row's proposed fixes were moot.** Measured on run `run-20260817T061515Z-1427190`, row 5 (`MATRIX-row5-photon.log`): at **07:17:35Z** the walk logged *`stored CI token -> /home/vks/vks-airgap-cicd/secrets/gitea-ci-token`*, and a grep for `missing .*secrets/gitea-ci-token` across the entire row returns **ZERO hits**. `make verify` then ran to completion — pushed a marker, `PipelineRun javawebapp-ci-vq9v2` SUCCEEDED, ArgoCD rolled `harbor.env1.lab.test/apps/javawebapp:0.1.0`. **THE MECHANISM:** `install-all` → `platform` → `seed-gitea` is what writes that token (`50-seed-gitea-repos.sh:45`), and `platform` runs BEFORE `gitops`. The original FATAL came from a run where `install-all` died **before** reaching `platform`; on any run that gets that far the token exists. So this row's fix A (*add `make seed-gitea` to the documented sequence*) would have appended a command to a sequence that already runs it, and fix B (*say why a tenant skips `verify`*) would have documented a restriction that does not exist. **The row was right to say "decide which, do not just append a command"** — and the answer was neither. ⚠️ Three backlog prescriptions were refuted on 2026-08-17 alone (B130, B37, B67); acting on this one without waiting for the discriminator would have been a fourth. **The same block DID surface a real defect** — `install-all` failed at `gitops` with `x509: cannot validate certificate for 192.168.101.131 because it doesn't contain any IP SANs`, reported to the operator as an AppProject/RBAC question. That is **B137**, and it is a different row. |
| **B128** | ✅ **CLOSED (#699, vks main e36704a) — and this row's own diagnosis was WRONG.** It said a tenant has no route to googleapis. MEASURED false: the box pulled 24/24 images in 6m28s immediately before, `helm repo add` SUCCEEDED against the same host seconds earlier, and a re-probe returned HTTP 200 for index.yaml and all three pinned charts. ⚠️ AND "it was transient" was ALSO unsupportable: `>/dev/null` had DELETED the only line naming the cause (helm puts it on stdout; stderr carries a bare restatement), so the real fix is `1>&2` — the next failure names itself instead of being retried five times in silence. The retry stands on its own merit: this was the ONLY un-retried network call, and it runs AFTER the images. **scenario-2 §6 tells a TENANT to run `make install-all`, which runs the MIRROR and needs INTERNET.** MEASURED, row 5: `make mirror-pull` -> `Error: failed to update the following repositories: [https://istio-release.storage.googleapis.com/charts]` (`Makefile:399`, rc=2). A tenant CONSUMES an existing Harbor and is typically on a box with no route to googleapis.com, so the documented command cannot work for the persona the section addresses. Either §6 must not run the mirror on the tenant path, or it must state that the box needs internet for the Istio charts. ⚠️ Do not fix this in isolation — it may share a root cause with **B127**/**B129** (the row supplies ADMIN creds and a GUEST kubeconfig, which is not what §6 assumes). |
| **B127** | ✅ **CLOSED (nvl #64, main 2e7db9c).** Both candidate causes SEPARATED and measured: (a) CONFIRMED the harness staged zero ArgoCD CA; (b) REFUTED — the doc DOES document it. The doc in fact PREDICTS the failure verbatim ("skip the login and a pasted token yields an opaque TLS failure"), and STALE_CA is classify_kube_failure's x509 verdict. Fixed by staging the artifact `argocd login` PRODUCES. ⚠️ My first design was refuted for the WRONG REASON: I wrote that staging a CA could not help — FALSE, the CLI config supports `certificate-authority-data`, so it now PINS the server cert and falls back to insecure only when the cert cannot be read. PROVEN LIVE on the lab: "argocd CLI trust: VERIFIED (server cert pinned)". **The tenant's ONLY write mechanism was never proven: `argocd API: COULD NOT DETERMINE (STALE_CA)`.** MEASURED, row 5, block [23]: `kubectl : YES — you may create Applications in ns/cicd (the Scenario 1 / KinD path)` but `argocd API: COULD NOT DETERMINE (STALE_CA) — argocd-server never received the request, so this is NOT a permissions answer and an AppProject role will not fix it. If the reason is a trust anchor: make fetch-argocd-ca, then set ARGOCD_CA_FILE.` The TOOL is exemplary (it refuses to turn an unreachable server into a permissions verdict) — but scenario-2's entire tenant story is "you write through the ArgoCD API", and that path has still never been demonstrated. TWO candidate causes, separate them before fixing: **(a)** the harness stages an argocd TOKEN but no matching argocd CA (walk-matrix's S2 contract line lists a harbor CA and no argocd CA), or **(b)** the DOC never tells the reader to `make fetch-argocd-ca` + set `ARGOCD_CA_FILE` before §6. If (b), the tool already prints the exact remedy the doc is missing. |
| **B126** | ✅ **CLOSED (#699, vks main e36704a).** The block is STRUCTURALLY uncheckable — branch 2's entire output is echo text, which walk-doc correctly filters as vacuous, and an exhaustive search found 22 common substrings >=6 chars with ZERO outside the command text. So the Expect now carries no checkable literal (verified 0 with the real PYX filter) and the prose got STRONGER, naming which branch is which. A walk-doc note marks the continuation-line placement DELIBERATE so PR #696's opposite lesson cannot silently re-break it. Permitted false-positive carve-out, not a contract downgrade: it was never a contract that could be true in both states. **scenario-2's discovery block has TWO branches and a ONE-branch Expect, so the branch the doc itself calls normal scores UNMET.** MEASURED, row 5, block [12]: the block is `if [ -s "$SUP" ] && [ -n "$HARBOR_NS" ]; then <read the LBs> else <ask the platform team> fi` and it printed `DISCOVERY: skipping the LB read — ask the platform team for the Harbor and ArgoCD addresses` — the ELSE branch, CORRECT for a tenant holding a guest kubeconfig rather than a Supervisor one, which the doc describes two paragraphs earlier as the normal case. The Expect names only `harbor-nginx`. Same class as scenario-1 §8.5: a two-branch block needs a two-branch Expect, and BOTH literals must sit on the FIRST line (`walk-doc.sh:331` appends only the line that starts with `**Expect`; continuations are invisible). |
| **B125** | ✅ **CLOSED (#700, vks main b5a7dff).** All ELEVEN sites — I had filed 10 and MISSED FOUR, including argocd.sh:229, a THIRD copy in the same function on the TENANT path. My 9.0-vs-9.1 scoping axis would have SHIPPED A NEW ERROR (2.14.13 is a 9.1 fact for Service 1.0.0; the discriminator is the ArgoCD SERVICE version). The worst instance carried NO version number — kind-tls-fidelity's conclusion "a real server-generation delta DOES exist", now false since lab 3.0.19 vs pin v3.5.1 are both 3.x — retracted by APPENDING a dated correction, the 07-11 block left byte-identical (diff-verified). Also relabelled argocd.sh:218's "Supported server versions" over `kubectl explain`, which our own script measures as a pattern + prose example. PNG regenerated; 4 Harbor false friends left alone. **`scripts/lib/argocd.sh` asserts a 2.x ArgoCD SERVER three lines above measuring 3.0.19 — in the tool whose job is to stop that inference.** MEASURED on a live 9.1 lab (2026-08-17): the Supervisor publishes exactly ONE ArgoCD Carvel package for Service `1.1.0-25100889` — `argocd.kubernetes.vmware.com 3.0.19+vmware.1-vks.1` — and the running instance is on it. `:205` and `:212` hardcode "a 2.x SERVER"; `:212` is worse because it fires on the UNAVAILABLE path where the reader cannot check. **Sites to fix together (verified by grep, 2026-08-17): `scripts/lib/argocd.sh:205,212` · `docs/vks-services/argocd.md:21` (the VALUE column is unscoped; `:219`/`:268`/`:23` ARE correctly scoped to Service 1.0.0 and must be LEFT) · `docs/diagrams/vks-topology.puml:17` · `README.md:152` · `docs/decisions/kind-tls-fidelity.md:66`.** ⚠️ `kind-tls-fidelity.md:229`'s "Harbor ≈ 2.14.3" is a DIFFERENT product that happens to share the number — do not touch it. ⚠️ `kind-tls-fidelity.md:218-225` is a DATED CORRECTION BLOCK: append a second correction, never rewrite (decision records are history). ⚠️ Editing the `.puml` REQUIRES regenerating and committing `docs/diagrams/out/vks-topology.png` in the SAME commit — `diagrams-check` byte-compares them. Do NOT replace 2.x with 3.x: the number does not belong in the string at all; the function already prints `argocd.spec.version`, the instance table and the running image. Evidence: `/tmp/evidence-argocd-pkgs-20260817T024007Z.txt`. |
| **B124** | ✅ **CLOSED 2026-08-17 — VERIFIED against the three files, not from the row's own FIXED claim.** `Makefile:817` is `install-all: preflight mirror mirror-verify builder-image vks-login platform gitops`, and BOTH docs now list exactly those seven in that order — `docs/scenario-1.md:754` and `docs/scenario-2.md:762`. The two-steps-missing/one-misnamed drift is gone, including the `mirror-verify` integrity gate whose absence was the point of the row. **A doc comment that ENUMERATES a make target's prerequisite chain is unverified, and one was two steps stale.** MEASURED 2026-08-17 while answering "what can scenario-2 reuse from scenario-1": `docs/scenario-1.md:731` described `make install-all` as `preflight -> mirror -> builder -> platform -> gitops` while `Makefile:793` is `preflight mirror mirror-verify builder-image vks-login platform gitops` — **two steps missing, one misnamed**, and the missing `mirror-verify` is the integrity gate this repo calls load-bearing (a push you have not verified is not a mirror; `crane` establishes blob existence with a HEAD, so a registry that answers HEAD-200 for blobs it cannot serve makes the whole push a silent no-op). `docs/scenario-2.md:599` had it right, which is the only reason it was caught — two documents describing one target differently means at most one is correct. **FIXED in #687**, verified token-for-token rather than by eye. **The GAP that remains:** `check-doc-make-targets` proves every `make <target>` a doc names EXISTS (299 of them across 31 docs), and **nothing proves a described PIPELINE is real**. A gate could compare any `# a -> b -> c` comment beside a `make X` against `X`'s actual prerequisite list. ⚠️ **NOT BUILT ON PURPOSE** — inventing a new control is the act that most needs an idea-round first, and this one has obvious false-positive surface (a comment may legitimately abbreviate, may describe a subset, or may name user-facing phases rather than make targets). Design it, RED-prove it against #687's own before-state, and measure its false-RED rate over the real doc corpus BEFORE shipping. |

| **B123** | ✅ **CLOSED (#685 + #686).** Both fixed; the class was scanned (8 pipelines across the three docs, 5 correctly guarded, these 2 not) and the round REFUTED my first fix — `[ -s "$CA" ] || { echo …; }` returns 0 and was still a fake green; the shipped form is `b64=$(base64 -w 0 "$CA") && printf … | base64 -w 0`, rc=1 absent / rc=0 present. Original text: **Two `scenario-2` blocks have an UNGUARDED pipeline exit status, and they fail in OPPOSITE directions — one is a FAKE GREEN, one is a silent RED. Both are in row 5's path.** Found 2026-08-16 by reading the document line by line as an end user, not by grepping; every number below was RUN. **(a) `:430` — FAKE GREEN.** The block is `base64 -w 0 harbor-ca.crt \| base64 -w 0`, a **bare** path — while the same document says the file is `./secrets/harbor-ca.crt` at **`:211`** (the `.env` key table) and **`:292`** (Step 2 prose), and nothing `cd`s. Measured in a dir without the file: prints `base64: harbor-ca.crt: No such file or directory` and exits **`RC=0`** without `pipefail` (**`RC=1`** with; walk-doc sets no `pipefail` — grepped, 0 hits). So the reader pastes an **empty** value into their Cluster `trust.additionalTrustedCAs` and the walk scores the block a **PASS**. It is invisible to the harness by construction. Fix: read the document's own key — `CA="${HARBOR_CA_FILE:-./secrets/harbor-ca.crt}"` — and make the absent-file case LOUD. ⚠️ Check `-w 0` against **toybox** `base64` before shipping: row 5 runs on Photon. **(b) `:475` — silent RED.** `if [ -s "$SUP" ]; then kubectl … get pods -A \| grep argocd-application-controller; else echo …; fi` has **THREE** outcomes, not the two the prose at `:479-480` claims (*"Either branch tells you what you need"*): kubeconfig absent → echo → **rc=0**; present **and** argocd found → **rc=0**; present and **NOT** found → `grep` exits 1 → the whole `if` returns **rc=1** and **prints nothing at all** — the reader is told neither what happened nor what to do, and the walk reddens the block. Measured both non-obvious cases directly. Fix: give the no-match case its own message and its own exit status; the block is informational, so it should never be able to fail the walk. **The CLASS, scanned:** 7 pipelines live inside runnable fences across both documents; **5 are correctly guarded** (`scenario-1:532` `\|\| true` + a `$code` check; `:542` guarded by the `[ ! -s ]` outcome test B119 added; `scenario-2:129` captures stderr and prints it at `:133`; `:130`/`:131` check `${VAR:-NOT FOUND}`) and **these 2 are not**. Do not fix only the instances — the shape is *"a command whose failure is the non-final element of a pipeline"*, and neither `check-doc-expect-leak` nor any gate in `static-check-fast` can see it. |

| **B122** | ✅ **CLOSED (#684).** All 14 commented (the round found a 14th, `BUILDER_PROBE_TIMEOUT`, outside §8 and invisible to a 13-key list); RED 14/14 clobbered → GREEN 14/14 survive; effective values unchanged; `scripts/test-env-timing-overrides.sh` added, DERIVED not enumerated, with a liveness sentinel because the naive probe passes when `load_env` never runs. The round also caught that this PR shipped its OWN vacuous-green claim, now retracted in the file. Original text: **ALL 14 uncommented timing keys in `.env.example` §8 are INERT from the command line — B93 was 1 of 14. Filed as ONE row, deliberately, because 14 rows would rot.** MEASURED 2026-08-16, each probed with a `99999` override through `load_env` (`SKIP_DOTENV=1` so the box's own `.env` cannot mask it): **14 of 14 CLOBBERED** — `READY_TIMEOUT_SECONDS`→300, `POLL_INTERVAL_SECONDS`→5, `CURL_MAX_TIME_SECONDS`→10, `VCENTER_CA_FETCH_TIMEOUT_SECONDS`→30, `GWAPI_FETCH_TIMEOUT_SECONDS`→15, `UNINSTALL_POLL_INTERVAL_SECONDS`→10, `DOCKER_ROOTLESS_READY_TIMEOUT_SECONDS`→90, `VERIFY_READY_TIMEOUT_SECONDS`→600, `EL_READY_TIMEOUT_SECONDS`→180, `PIPELINERUN_WAIT_SECONDS`→120, `HTTP_CONNECT_TIMEOUT_SECONDS`→10, `INTERNET_PROBE_TIMEOUT_SECONDS`→5, `INTERNET_PROBE_MAX_TIME_SECONDS`→10, `TOOL_VERSION_TIMEOUT_SECONDS`→5. **B93 (`READY_TIMEOUT_SECONDS`) is fixed in PR #675; the other 13 are this row.** ⚠️ **The sharpest one is `VERIFY_READY_TIMEOUT_SECONDS`:** `99-verify.sh:23` rebinds `READY_TIMEOUT_SECONDS="${VERIFY_READY_TIMEOUT_SECONDS:-600}"`, so fixing B93 alone makes 13 sites overridable and leaves **`make verify` pinned at 600** — an operator lengthening timeouts on a slow lab still watches `make verify` die. **Precedent says the repo already decided this class-wide**, though the exact count depends on the predicate and I am stating mine rather than borrowing one: **37** commented keys file-wide match `^# [A-Z_]*(TIMEOUT|WAIT|INTERVAL|RETRIES|MAX_TIME|SECONDS)[A-Z_]*=`, of which **6** are inside §8 itself (a B93 adversary round counted **20** by an unstated predicate — the three it named, `HTTP_GET_RETRIES:1384`, `PREREQ_DOWNLOAD_MAX_TIME_SECONDS:1397`, `VC_API_TIMEOUT:1283`, are all real and all commented). **Apply B93's proven layout per key** — `# how:` marker ABOVE, key commented, and an UNCOMMENTED key below to break contiguity — and note the trap that makes the batch different from the single: `check-env-coverage` walks UP over a *contiguous comment block*, and **a commented key is itself a comment line**, so commenting all 14 in a run lets 13 pass on their neighbour's marker (measured). Either interleave, or teach the gate to treat `^#\s*[A-Z_]+=` as a block terminator FIRST. **No gate covers this class today** — `check-env-clobber`'s four arms cannot fire for a literal-default key, and extending the dynamic-default arm would touch 45 of 69 keys. |

| **B119** | ✅ **CLOSED 2026-08-16 — SHIPPED + MERGED in PR #664.** Fix: `:443` when `HARBOR_URL` carries no port, `timeout 15` (matching the repo's five other `s_client` calls), a failure message that no longer blames DNS *inside the branch where curl already returned 200*, prose quoting the real strings, and an `**Expect:**` on the fetch block (which had none, and is the block that can fail). Proven end-to-end against the real lab Harbor: §8 → one line `harbor.crt: OK`; `sha256sum` → digest; `make ca-status` → **`CA-STATUS: ALL-MATCH n=1`, rc=0**. `make ci` green. **The adversary round REFUTED my second proposed change and I dropped it** — Expect is STEP-scoped (`walk-doc.sh:530`), so the `sha256sum` block's `harbor.crt: OK` is satisfied by the fetch block above it; it was unmet because the fetch FAILED, not by construction (I had measured `sha256sum` in isolation — a fragment of a program is a different program). My replacement text would also have produced **zero** checkable literals, lowering coverage while every counter stayed green. Residual carried forward: block 37's istiod link is still INFERRED — the adversary names ≥2 other recorded causes for `Available: 0/1` (PSA rejection, invisible on KinD; and the `image: auto` rewrite), so if the re-walk does not clear 37, that is a SECOND bug. Original evidence follows. |
| **B119-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B119`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **scenario-1 §8 could NEVER save the CA — `openssl s_client -connect` was given a BARE HOST, and it needs `host:PORT`.** MEASURED on the row-1 ubuntu VM walk (`/tmp/walk/MATRIX-row1-ubuntu.log`, block 23, rc=0, 135s): `Could not read certificate from <stdin>` → `that file does not vouch for harbor.env1.lab.test`. Harbor **did** publish its CA (the block took the 200 branch) — the documented happy path was broken by this alone. Measured directly: `-connect example.com` → **0 cert bytes**; `-connect example.com:443` → **1411 bytes**. `HARBOR_URL` is documented bare (line 277) and `scripts/02-env.sh:253` hard-rejects a scheme, so this could never work on any lab. **This ONE bug is the sole root cause of ALL EIGHT non-zero blocks in row 1** — 24/25 (`sha256sum`/`ca-status`: no file), 27 (`harbor-admin-password`: *"no CA yet (Step 8 fetches it)"*), 28 (`harbor-robot`: `curl (60) SSL certificate problem`), 31 (`env-validate`: `curl exit 60`), 32 (`lab-preflight`: *"Harbor CA … is missing or empty"*), 37 (`install-ingress`: istiod cannot pull `harbor.env1.lab.test/cicd/istio` because the mirror never ran → `Available: 0/1` after 303s), 38 (cascade). Every downstream message named the cause correctly; only §8 was wrong. **FIX on branch `fix/ca-fetch-port`**, both directions proven; plus the misattributed message split (a connection failure was reported as *"that file does not vouch for"*, sending the reader to fix a file that was fine). ⚠️ **PROVEN AGAINST THE REAL LAB HARBOR** (`harbor.env1.lab.test` = 192.168.101.130, still up): `getcert` → 200 / 1155 B; OLD → leaf **0 bytes**, `Unable to load certificate`, **nothing saved**; NEW → leaf **1143 bytes**, `openssl verify` → **OK**, CA saved (`CN = Harbor CA`, leaf CN `harbor`). Also measured: the old form does not fail fast, it **STALLS** — 135s on the VM, >120s locally — so the reader waits two minutes for a wrong answer; a `timeout` wrapper is portable (Photon toybox `/usr/bin/timeout`, `timeout 2 sleep 5` → rc=124) and already used by 5 repo scripts, pending the adversary's verdict on whether it is warranted. ⚠️ **ONE LINK IS INFERRED, NOT MEASURED:** that block 37's istiod was `Available: 0/1` *because* it could not pull from an unmirrored Harbor. MEASURED in support: `make` stopped at `lab-preflight` and never reached `mirror` (the log's next command is `make verify`), and the lab Harbor today exposes **only the `library` project** — no `cicd` (caveat: a private `cicd` would not show anonymously). NOT measured: the istiod pod's actual status, because the walk's guest cluster kubeconfig lived on the VM and my Supervisor kubeconfig is **stale from an earlier rebuild** (`x509: certificate signed by unknown authority` — the B98/B118 class, firing for real). So **if fixing §8 does not clear block 37, the residue is a SECOND, independent bug** and must not be reported as fixed. |
| **B120** | ✅ **FIXED 2026-08-16 — `lib/state.sh` three lines → one; measured 105 → 35 lines across a scenario-1 walk.** See **B86** below for the design that was already refuted; this row is its message half and should be read with it. ⚠️ **My own premise in this row was FALSE and the adversary measured it:** `02-env.sh` (env-populate) contains **zero** `state_set` calls — the sink is written by `30-vks-login.sh:447` at **Step 3**, and the first warning fires ten minutes BEFORE env-populate runs. ⚠️ **And the removed prescription was ACTIVELY HARMFUL, not merely noisy:** scenario-1 legitimately runs under **two** API servers (Supervisor at §3, then the guest via `27-use-guest-kubeconfig.sh`), so a reader who ran `make state-stamp` would arm `state_check` to REFUSE the other one — losing `HARBOR_USERNAME`/`HARBOR_PASSWORD`/`ARGOCD_KUBECONFIG`/`GITEA_LB_IP`/`INGRESS_LB_IP` from Step 6 onward, turning a WARN into a hard red. It also **launders** the hazard it warns about (stamping silences the alarm while KEEPING a stale value from a dead lab, and flips `creds.sh`'s banner STORED→DISCOVERED over it), and its precondition is false anyway — `state_stamp` only PARSES a kubeconfig, never dials. **Also refuted: warn-once is a measured NO-OP (35 → 35)** — each of the 35 warnings already comes from a distinct script process, and a per-`make` marker cannot cross the walk's per-statement `bash -i -c`. Deliberately NOT touched: `creds.sh:262`, which carries the same phrasing but has **two** assertions in `test-creds-show.sh` — that is B86's scope. Verified: `test-creds-show.sh` and `test-state-overlay.sh` both green after the change. Original row follows. |
| **B120-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B120`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **A scenario-1 reader sees 35 UNSTAMPED WARNINGS across 37 commands, and all three causes it names are FALSE on the real-lab path.** MEASURED (same log): every `make` prints 3 lines — *"state: .env.state is UNSTAMPED (legacy .env.kind, a jumpbox file, or hand-written). Sourcing it anyway… Run 'make state-stamp'…"*. On the real-lab path that file was written by **`make env-populate`** (Step 11) — it is not legacy, not a jumpbox file, and not hand-written; and `make state-stamp` is a command **scenario-1 never introduces** (`grep` finds 1 mention of `.env.state` in the whole document and 0 of `state-stamp`). So the reader is told, ~35 times, to run something they have never met, to fix a condition described by an enumerated cause-list that is wrong in every arm — the classic enumerated-list rot. Note `state.sh:176` already carries the honest phrasing (*"it does not say which cluster it belongs to"*) while `state.sh:93` carries the false one. Fix candidates: stamp at write time once a cluster is reachable (`env-populate` knows the context), or at minimum drop the false cause-list. Needs an adversary round before implementing — a stamp-on-write touches the sink-precedence machinery B84 just fixed. |

| **B121** | ✅ **FIXED 2026-08-16 (minimally, and the adversary round is why it is minimal).** The emitted line asserting *"most likely: no RBAC"* is gone (0 emitted occurrences); the message is now SEARCH-shaped, states that CRDs alone do not mean a mesh is installed, defers to what `istio_discover` already printed, and makes the RBAC ask CONDITIONAL. ⚠️ **My proposed fix was REFUTED, and it was WORSE than the bug.** It would have advised `make install-ingress INGRESS_CONTROLLER=istio` — which `scenario-1.md:775-779` forbids **in bold** because it helm-installs a second istiod over the platform's AND relabels `istio-system`'s PSA level (`46-install-istio.sh:162`), breaking the platform's pods **ACROSS TENANTS**. And that arm is the one the COMMON real-lab state lands in: VKS ships the shared ingress gateway **disabled by default** (`lib/istio.sh:196-198`), so istiod-present-with-no-gateway is normal, not an error. Three more refutations, all recorded in the code comment so nobody rebuilds them: `k_can_i` returns **four** shapes (`os.sh:1189-1205`) so a two-way branch turns an UNREACHABLE cluster into a confident claim about its contents — the exact mistake `48-istio-preflight.sh:131-135` already documents; `istio_discover` has **three** distinct `return 1` sites (none/AMBIGUOUS/pin-miss, `lib/istio.sh:83,88,100`) so an unconditional message contradicts the ambiguous one; and `auth can-i` is an authorization *prediction*, whereas the repo's doctrine for a PRESENCE question is `classify_kube_failure` (`lib/istio.sh:218-222`). **Deferred to a follow-up** (needs a new `test-istio-preflight-discovery.sh` + doc edits to `scenario-1.md:757-766` and `scenario-2.md:608-616`): distinguishing the three discovery outcomes inside `istio_discover`, which would also fix `47-attach-istio.sh:96`, the second consumer with the same ambiguity. **F5 was the trap that nearly bit:** `walk-doc.sh:67-72` navigates §12 by grepping `INGRESS_CONTROLLER=istio-existing` out of *this very error text*, so deleting the block would have flipped step 36 FAILED→SKIPPED and silently changed matrix scoring hours before a certification run — the string is deliberately preserved. |
| **B121-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B121`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **`istio-preflight` ASSERTS a cause it never tested — *"Istio is present but discovery failed (most likely: no RBAC to read it)"* — and sends the reader to ask a mesh admin for three values that do not exist.** MEASURED on the row-2 photon walk (`/tmp/walk/MATRIX-row2-photon.log`, blocks 35+36): the cluster had `Gateway API CRDs: PRESENT` and `VirtualService CRD=yes`, so `48-istio-preflight.sh` took the classic path; `istio_discover` found no Service with port 15021 + an `istio` selector, and the script fell through to `48-istio-preflight.sh:99`, which **states the RBAC cause as fact**. The real cause was neither RBAC nor a platform mesh: **row 1's `install-ingress` had TIMED OUT** installing istiod (`Available: 0/1` after 303s), leaving the CRDs and namespaces behind with no gateway — so "Istio is present" was inferred from *leftover CRDs*, which is not the same claim. Note both `k_can_i` and the honest-message pattern are already in this same file (see the ⚠️ at `:84-85`, which warns about exactly this — *"printed a PERMISSIONS ANSWER for a question that never reached the cluster"*), so the fix is to USE it: probe `auth can-i list services -A` (or equivalent) and branch — readable ⇒ *"no Istio ingress gateway exists on this cluster; the CRDs alone do not mean a mesh is installed"*; not readable ⇒ the existing RBAC guidance. Same family as B119's misattributed §8 message: **an error that names the wrong cause is worse than a crash**, because it sends the operator to fix something that is not broken. ⚠️ Also note this row exposes that **matrix rows share a guest cluster by design** (row 1 NOTHING installs, row 2 EVERYTHING attaches), so a row-1 failure contaminates row 2 — expected, but it means row 2's verdict is not independent evidence until row 1 is green. |
| **B108** | ✅ **CLOSED 2026-08-20 — CERTIFIED 6/6 ON THE FIRST PASS, AND TAGGED. Both halves of this row's closing condition are met; do not re-open it to re-certify the same tree.** `MATRIX COMPLETE — walked 6 of 6 designed rows (1 2 3 4 5 6)` over a FRESHLY CUT lab, every row `0 FAILED` / `0 UNMET`, on its **first** pass — no fix reset the clock. Evidence lives at `~/walk-evidence/run-20260820T022721Z-272401/` (per-invocation VERDICT + six row logs, `0700`), which is under `$HOME` deliberately: the previous certification's evidence was destroyed by a reboot because `walk-matrix.sh` defaults `WALK_OUT_ROOT=/tmp/walk` (filed as **B454** in `nested-vsphere-lab`). Run: 2026-08-19 22:27:22 → 2026-08-20 01:33:36 EDT (**3h06m**); lab cut 39m47s, 7 steps, 0 skipped, rebuilt once mid-matrix between rows 2 and 3. Rows: 1 ubuntu/NOTHING and 3 photon/NOTHING both `37 ran, 0 FAILED, 7 skipped | 25 checkable, 0 UNMET`; 2 photon/EVERYTHING and 4 ubuntu/EVERYTHING both `31 ran, 0 FAILED, 13 skipped | 21 checkable, 0 UNMET`; 5 photon and 6 ubuntu (scenario-2) both `18 ran, 0 FAILED, 9 skipped | 8 checkable, 0 UNMET`. ⚠️ **BOUNDARY, from the harness's OWN log — `0 FAILED / 0 UNMET` is NOT "the documents executed end to end".** Every scenario-1 row logs **`6 line(s) neutralized`**, two annotated `covered-by: UNCOVERED` verbatim: **`argocd login`** (*"nothing else in the matrix authenticates to ArgoCD"* — which is EXACTLY why the post-run `/api/v1/session` check below exists; hole and patch, read them together) and **`argocd account update-password`** (deliberately). The other 4 are port-forward fallbacks, covered by `make verify-ingress`. Rows 1/3 also carry 3 `block(s) with a MASKED literal`, rows 2/4 five. The one skipped block worth naming is `[37]`, a `kubectl annotate sc <name>` line the document itself says is not runnable as written. 🔴 **THE EVIDENCE DIRECTORY IS ALSO A LIVE SECRET STORE** — `.hp` (this lab's live Harbor admin password, which differs from the repo's stale `.env`), `.at`/`.acfg` (ArgoCD token + CLI config), `.gkc` (guest kubeconfig), `.hca` (Harbor CA), all plaintext. Dir `0700`; every file inside is `0600` **as of 2026-08-20 — six of twenty-six were `0664` before that**, and the original "`0700`" claim was wrong in the reassuring direction. Positive control: password and token appear **0 times** in the row logs. Do not copy, archive or attach that directory. **The identical counts across DIFFERENT OSes on DIFFERENT lab cuts are the strongest part of the result** — the documents reproduce across OS and across a full rebuild, which no single row can show. **TAGGED `v1.1.0` on `778fa77`**, annotated, deliberately NOT on main's head: two commits landed after the certified tree and neither was walked (`6cb7937`, Renovate's renovate-CLI pin, which AUTO-MERGED 12 s before row 1 started, and `1e75332`, a docs-only findings PR held unmerged for the run). `assert_repo_published` correctly passed throughout — it refuses a WALK_REPO that is *ahead*, not one that is *behind*, and local HEAD never moved, so all six rows shipped `git archive HEAD` = 778fa77. **VERIFIED AFTER the run** (C.6 — never during, a run reinstalls Harbor between cells): Harbor by API basic-auth (200, sysadmin) AND by its UI's own `/c/login` form endpoint (200 + `sid` cookie, wrong password → 401); ArgoCD by `/api/v1/session` over FULLY VERIFIED TLS (200 + `argocd.token` cookie, cookie-only `userinfo` → `loggedIn: True`, wrong password → 401); both UIs rendered in Chrome under a THROWAWAY `HOME`. End state: Harbor `cicd` 25 repos / `apps` 2, and ArgoCD carries **2 Applications, `gowebapp` and `javawebapp`, both Synced AND Healthy** — the full `git push → Tekton → Harbor → ArgoCD → running workload` loop, for both languages. — 🟡 **RE-FOLDED 2026-08-16 — the five fixes are back on the scenario-1 execution path, and are NOT yet certified.** The owner approved unblocking the hold as part of rebuilding the walkthrough matrix, so `cecda14` was reverted, restoring **B62** (arch-bound vcf CLI glob), **B83c/d** (a LEAF could be installed as the trust ANCHOR — the security fix this hold existed to protect), **B94**, **B98** (a STALE `ARGOCD_KUBECONFIG` surviving a lab rebuild) and **B105**, together with their tests and Makefile wiring. **Verified, not asserted:** the three restored tests pass — `test-ca-anchor-validation` 10/10, `test-argocd-kubeconfig-stale` 10/10, `test-vcf-cli-resolve` 11/11. ⚠️ **`v1.0.1` certified a tree WITHOUT these**, so `main` is now deliberately uncertified: the frozen-path guarantee is suspended until the rebuilt **8-row** matrix (both scenarios × ubuntu/photon × NOTHING/EXISTS) runs green, which is the single run that certifies both these fixes and the rewritten documents. **This row closes when that run is green and a new tag replaces `v1.0.1` as the frozen reference.** Do NOT re-revert to restore certification — that would drop the leaf-as-trust-anchor fix again. ⚠️ **CLOSE CONDITION CORRECTED 2026-08-18 — it demanded an "8-row matrix" that CANNOT EXIST.** `walk-matrix.sh:76` sets `DESIGNED_ROWS="1 2 3 4 5 6"`: scenario-1 genuinely gets 2 OS × 2 cells = 4 (rows 1-4), and scenario-2 gets **2** (row 5 photon in cut A, row 6 ubuntu in cut B). It cannot get 4, because scenario-2's PREMISE is that Harbor and ArgoCD already exist — there is **no NOTHING cell** to walk. So `2×2×2` is a CATEGORY ERROR, not an unimplemented feature. **This is B43's refutation in a second location** — that row killed an 8-cell `make e2e-matrix` for the same reason (*"the axes are not orthogonal … the cube does not exist"*) and named the hazard of leaving one written down: *"announcing a cell that does not exist MANUFACTURES A KNOWN-GAP and invites a future session to 'close' it by building the vacuous leg."* Left as-is, this row is unclosable after a green **6-row** run. **Read the condition as: six rows green FROM THE FIRST GO, AND a new tag replaces `v1.0.1` as the frozen reference — the tag is half the deliverable, not a formality.** |

Every item below is the OPEN set, re-verified against the tree — most recently on **2026-07-17
(session 3)**, when **B29, F8, B41, B44, B23, B30, B34 and B42 were REMOVED** because they
shipped/resolved (#295–#297 + #304 + the B42 fix; evidence in each PR + the code), and **B26, B33 were
COLLAPSED** to their open residuals (their done parts dropped). The history that produced these is in git and `docs/reviews/`;

**Pruned 2026-08-13:** 21 rows whose work had SHIPPED were removed —
`B3`, `B13`, `B18`, `B19`, `B22`, `B27`, `B28`, `B45`, `B47`, `B48`, `B49`, `B55`, `B56`, `B58`, `B59`, `B71`, `B73`, `B74`, `B79`, `B80`, `B85`, `B99`, `B100`, `B63`, `B98`, `B105`, `B94`, `B103`, `B60`, `B62`, `B89`.
Each cited its own PR; full text is in git history (`git log -p -- BACKLOG.md`) and `docs/reviews/`.

**Also unaccounted, recorded 2026-08-22:** `B14` (`scripts/05-kind-up.sh:128`), `B36`
(`scripts/46-install-istio.sh:93`), `B117` (`scripts/test-cluster-status-wait-gate.sh:116`) and
`B231` (`scripts/lib/tls.sh:223`) are cited as provenance by scripts, have no row here, and appear in
neither prune note above. Recover them with `git log -p -- BACKLOG.md CLAUDE.md` — **both paths**,
because rows lived in `CLAUDE.md` until the 2026-07-22 split (`f7f6c30`), so BACKLOG-only history
misses them.

⚠️ **Two classes that LOOK dangling and are NOT — measured 2026-08-22, after a session reported six
of them to the owner as broken citations.** (a) **Heading-form rows.** `B86`, `B87`, `B88` and
`B109`–`B114` are `## B<N> —` sections further down this file, invisible to a grep for the bolded
table form; the real inventory is **153 table rows + 11 heading rows = 146 distinct ids**, not the
137 a table-only regex reports. (b) **Hex fragments.** `B0`, `B4` and `B5` come from certificate
fingerprints and MAC addresses (`B0:E5:E6`, `31:B4:DB:16`, `C0:8B:3E:B5`), not from citations. Any
future audit of this file must enumerate BOTH row forms and exclude hex, or it will manufacture
findings — and a prune driven by it would delete rows that open rows still cite as evidence.

**Deliberately KEPT, and this is the load-bearing half of the prune:**

- every 🔵 REFUTED / ⛔ DO-NOT-BUILD row — their value is stopping someone rebuilding a design
  that was already refuted, so they are not "closed work";
- seven rows marked ✅ that a CONTENT review showed are not actually finished. A marker scan
  would have deleted them: **B20** carries *"Still open, lab-gated"*, **B50** carries *"BUILD
  THIS INSTEAD (not yet done)"*, **B46** is *"DECLINED ON EVIDENCE — do not re-open"*, and
  B17/B40/B52/B65 quote open-language that is history rather than residual. Prune this file by
  READING rows, never by grepping their status emoji.
carrying a closed item forward is the class this file was pruned to end.

**Code — no lab needed:**

| ID | Item |
|----|------|
| **B64** | ✅ **CLOSED BY VERIFICATION 2026-08-18 — the adopted COUNTER-PROPOSAL SHIPPED.** `scripts/lib/vcenter.sh` (288 lines) implements exactly the four calls it prescribed — `vc_ss_check_content` (checkContent) → `vc_ss_register` (carvel_spec) → `vc_ss_install` (POST/PUT) → poll, plus `vc_supervisor_id`/`vc_cluster_moid`/`vc_api`/`vc_login`/`vc_logout` — with the consumers `08-install-argocd-service.sh`, `deregister-`/`list-`/`uninstall-`/`unwedge-supervisor-service.sh`. scenario-1 §2 is now SCRIPTED (`make install-harbor-service`, `make install-argocd-service` as steps 4-5); the single surviving vSphere-Client reference is for READING the Supervisor IP, a value the operator looks up, not an install action. The counter-proposal's "keep the vCenter-admin blast radius small" also held: secrets reach curl through a `-K` config under `umask 077` escaped with `esc_curlk`, and the session token via `-H @file` — never argv, never `-u`. ⚠️ **Finding (3) is REAL and is NOT closed by this** — `vc_login`/`vc_logout` still use `curl -sS -k` (`lib/vcenter.sh:48`/`:73`), i.e. the SSO administrator password still crosses an unverified TLS connection. That lives on as **B133** (CRITICAL, blocked on two operator decisions) and must not be considered resolved here. Original text follows. — §2's "browser work — not scriptable" is a claim about THIS REPO presented as a claim about the PLATFORM — and the obvious port to fix it was REFUTED (idea-round, `vks-adversary`, 2026-08-05).** The source repo (`nested-vsphere-lab`) installs Supervisor Services entirely over vCenter REST, so the doc claim is false. But the proposed ~1,700-line port (a `lib/vcenter.sh` shim + `service-catalog.sh` + an `svc_main` equivalent, phased ArgoCD-instance-first) was refuted on 12 findings, 3 CRITICAL. **(1)** Blanking `_SS_ROWS` field 6 (`dns-label`) does not drop DNS — it feeds `svc_dns_fqdn`, so it kills `_ss_render_harbor_values` AND `harbor_set_admin_password`, dying with an error naming `.lab.domain`, **a config key this repo does not have**. Blank field **5 only**. **(2)** The install POST/PUT need `_SS_MOID`, which comes from `_cluster_moid` in the source's `steps/40-supervisor.sh` (not `lib/govc.sh`), plus an entire vCenter credential surface this repo lacks — `VCSA_IP`/`VCSA_USER`/`VCSA_SSO_PASSWORD` (**SSO administrator**, not the tenant identity Scenario 2 assumes), `CLUSTER_NAME`, `DOMAIN` (we have `APP_DOMAIN`), `LAB_STATE_DIR`. ~10–15 new `.env.example` keys, each owing a graded `# how:`. **(3) SECURITY:** with no VMCA anchor producer here, `_vc_tls` falls back to `-k`, so the **SSO admin password crosses an unverified TLS connection** on every first run, signalled only by two WARNING lines in a 20-minute build. The source's warn-and-continue exists for a bootstrap ordering (the VCSA does not exist yet) that **does not apply here** — vCenter is pre-existing customer infrastructure, so this repo must **fail closed**. **PHASING WAS BACKWARDS:** the "kubectl-only" ArgoCD-instance phase depends on the vCenter phase in four places, incl. a `die` telling the operator to go do the browser flow — the very claim being refuted. **COUNTER-PROPOSAL (adopt):** ONE script, four calls — `checkContent` → `register` (`carvel_spec`) → `install` (POST/PUT) → poll `config_status` — parameterised on (service-yaml, data-values, cluster). No catalog, no DNS, no instance CR, no teardown verbs (~800 lines serving a story this repo does not have). ~350–450 lines, it IS the §2 rewrite, and it keeps the vCenter-admin blast radius small enough to RED-prove. **Also carry:** the expose-key precondition verbatim (`yq -e 'has("enableNginxLoadBalancer") and has("enableContourHttpProxy")'` — `yq '.k = v'` CREATES missing keys, so without it a vendor rename ships a Harbor with no reachable ingress, green all the way down) — but note it has a **multi-document hole** (`has()` passes if ANY doc satisfies it; measure `select(di == 0)` against the real licensed artifact first). **Gates it will trip:** `check-grep-q-pipe` (the ported `printf \| grep -qxF` is the SIGPIPE-under-pipefail class), `check-env-coverage`, `check-how-provenance`, `check-env-clobber` (new selectors must stay COMMENTED), `check-doc-target-coverage`. **Doc honesty:** §2 is scripted through step 4 and **manual at step 5** — customer DNS is unscriptable from here, and claiming otherwise just mirrors the error being fixed. **OWNER DECISION NEEDED before writing:** does vCenter **SSO admin** belong in a repo whose Scenario-2 persona is explicitly a tenant? **Cheap unblock:** one `openssl s_client -connect $VCSA:443 -showcerts` against a real lab settles whether `./secrets/supervisor-ca.crt` already anchors vCenter too, which would delete a whole fetch path.  **SECOND ROUND (2026-08-05, `adversary-bash-git-cli`, Step-0 instrument PERMITTED — findings MEASURED): verdict SPLIT — the `checkContent`-only slice is CLEARED as not-worth-building, but on a stronger argument than the one I gave, and the DELIVERABLE was REFUTED.** **(F1, the decisive argument — lead with it)** `checkContent` is call 1 of 4 and NON-MUTATING: it cannot register, cannot install, and cannot move §2 off the browser. The operator gains a script demanding a new vCenter endpoint, a session handshake and a TLS anchor, and then still performs all six §2 steps by hand — the full credential surface of the four-call script for ZERO capability. "It validates the wrong file" invites "then also add the data-values check"; "call 1 of 4 delivers no capability" is unanswerable. **(F2, CRITICAL)** A doc correction MUST NOT close this row. The recorded owner action (`docs/reviews/2026-08-04-first-real-lab-measurements.md:56`) is verbatim *"port the capability, then rewrite §2/§3 scripted-first … This is a FEATURE plus a doc rewrite, not a doc edit."* Closing B64 on a doc edit also orphans **B78**, which is blocked on it, and reads six weeks later as "B64 was handled". Ship any §2 correction explicitly labelled **INTERIM**; B64 stays 🔴. **(F3)** This row's own "no VMCA anchor producer" claim is **CORRECT — do not "correct" it.** MEASURED: `command grep -rn 'supervisor-ca.crt' Makefile scripts/` returns ONE hit, a COMMENT, and `scripts/lib/tls.sh:87` says verbatim *"Nothing in the repo re-fetches it"*; `.env.example`'s `VKS_CA_CERT_FILE` is COMMENTED, i.e. a path, not a producer. A proposed edit to soften it was refuted. **This is also the strongest argument FOR refusing the slice:** a fail-closed `checkContent` at §2 hard-blocks the FIRST scripted step on an artifact the docs never tell anyone how to obtain, and the obvious bootstrap (fetch the VMCA over the connection you are verifying) is TOFU. **(F4)** The COST objection is OVERSTATED — drop it. MEASURED as already present: `VKS_USERNAME` (documented as `administrator@vsphere.local`, which IS the vCenter SSO principal), `VKS_SSO_DOMAIN`, `VCF_CLI_VSPHERE_PASSWORD`, `SUPERVISOR_HOST`, `VKS_CA_CERT_FILE`/`VKS_CA_SHA256`, and a RED-proven fail-closed `ca_verifies_endpoint`. Genuinely new is the **vCenter endpoint** (`SUPERVISOR_HOST` is the Supervisor VIP, not vCenter), and `check_content` needs only `System.Read`. The "~10–15 new keys" figure belongs to the four-call script, not this slice. **(F5, UNMEASURED)** a candidate consumer exists at `docs/scenario-1.md:259` and was never checked. ⚠️ **BLOCKED ON B133 — an unrecorded dependency, found 2026-08-17.** This row's finding (3) is that the port makes THIS repo send the vCenter **SSO administrator** password over a `-k` connection on every first run, and concludes it *"must fail closed"*. That is not a detail of the port — it IS B133, whose two pending OPERATOR decisions are exactly (1) whether Step 2 hard-fails on a box with no vCenter CA (breaking every current operator and every walk row) or keeps sending the password unverified, and (2) whether scenario-1 may be REORDERED so a vCenter CA is obtained first. Implementing B64's counter-proposal introduces the very credential surface those decisions govern, so starting it before they are made would either bake in the insecure default or break the walk. **Do not pick this up as 'the highest-severity open row' — it is not startable.** B78 (*the §4a/§4b consumer*) is blocked on B64 and is therefore blocked on B133 too. 🟢 **THE 'CHEAP UNBLOCK' IS MEASURED, 2026-08-17, on a FRESH cut — and the answer is YES BY KIND, which does delete a fetch path, but it came with a second finding that matters more.** This row proposed: *one `openssl s_client -connect $VCSA:443 -showcerts` settles whether `./secrets/supervisor-ca.crt` already anchors vCenter too, which would delete a whole fetch path.* Run against the new lab's vCenter (192.168.100.50): **(1) vCenter presents exactly ONE certificate** — leaf `subject=CN = vcsa.env1.lab.test, C = US`, `issuer=CN = CA, DC = vsphere, DC = local, … O = vcsa.env1.lab.test, OU = VMware Engineering` (the **VMCA**), `SAN=DNS:vcsa.env1.lab.test`, and it does **NOT** send its issuer — `Verify return code: 21 (unable to verify the first certificate)` against the system store. So the *read-the-chain-off-the-wire* shortcut is **unavailable**: an anchor must come from somewhere else, which is why `make trust-vcsa` exists and why every build prints *"no VMCA anchor … talking to vCenter with TLS VERIFICATION OFF"*. **(2) `secrets/supervisor-ca.crt` IS the VMCA root** — `subject == issuer == CN = CA, DC = vsphere, DC = local, … O = vcsa.env1.lab.test`, i.e. **the very DN that issued vCenter's leaf**, not a Supervisor-specific CA. ⇒ on a MATCHING cut that one file anchors BOTH the Supervisor and vCenter, and the separate vCenter-CA acquisition path this row worried about can be elided. ⚠️ **(3) AND THE MEASUREMENT ACCIDENTALLY DEMONSTRATED THE STALENESS HAZARD, WITH A MISLEADING ERROR.** The `supervisor-ca.crt` on disk is dated **Aug 16 12:55** — the DESTROYED cut — so against the new vCenter it FAILS: `curl --cacert` gives **rc=60 http=000** (insecure control: rc=0 http=200), and `openssl verify` reports **error 20, *unable to get local issuer certificate*** — NOT error 7 (signature failure). Same subject DN, different key, so the anchor is never even considered a candidate and the diagnostic says *no issuer found*, which reads as **"wrong KIND of CA"** when the truth is **"right kind, wrong cut"**. That is the B161/B133 family exactly: a stale credential-adjacent artifact surviving a rebuild and producing an error naming the wrong cause. **RESIDUAL, stated rather than hidden:** this compared a STALE anchor to a NEW vCenter. The full claim — that a MATCHING `supervisor-ca.crt` verifies vCenter — needs the pair from ONE cut, and settles the moment this build's Supervisor is up: `make fetch-supervisor-ca && curl --cacert secrets/supervisor-ca.crt https://<vcsa>/` must return **200**. |
| **B61** | ✅ **CLOSED 2026-08-17 — STALE ON BOTH HALVES, verified by measurement AND by reading the file.** **A MEASURED-FALSE fact sits in the ALWAYS-LOADED rules corpus — `claude-config:rules/common/git-workflow.md:377-379` (other repo, but it loads into EVERY session here).** It claims *"`$(...)` capture is also not word-split in zsh"*. **Measured FALSE** (zsh 5.9, `zsh -f`, `SH_WORD_SPLIT` unset): `for x in $(printf 'a\nb\nc\n')` → **3** iterations — command substitution DOES split; `v=$(...); for x in $v` → **1** — only *variable* expansion does not. So the paragraph carries the correct rule at `:383-385` and its own contradiction five lines above it, in a file injected into every session in every repo **and every subagent**. The same lines also hold ~29 B of **mangled duplication**: the phrase *"capture is also not"* appears twice, split across an unrelated PIPESTATUS sentence, so the sentence is unparseable. Introduced by an auto-sync **cron** commit (`1dc33a3`, 2026-07-09) plus `716abaf` — **not** by the 2026-07-23 session; but that session edited this exact paragraph under ratchet pressure and shipped it, which is the *"a FACT inside a CONTROL is part of the control"* class. **Minimal fix:** delete the duplicated fragment and correct to *"unquoted **variable** expansion is not word-split in zsh; **command substitution IS**"*. Removing a false fact is a **correction** — the ratchet docstring names that always-legitimate — and it SHRINKS the corpus, so it also partly repays the 627 B raise of 2026-07-23. Found by the session-end `vks-adversary`, measured with a real shell. 🟢 **CLOSED 2026-08-17 — STALE ON BOTH HALVES, verified by measurement and by reading the file.** (1) The false fact is GONE: grepping `claude-config:rules/common/git-workflow.md` for `capture is also not`, for a `$(...)`-near-`not word-split` pattern, and for `is also not word` returns **ZERO hits** on all three. The line now reads *"an unquoted VARIABLE expansion runs ONCE in zsh — a `$(...)` capture DOES split; measured 1 vs 3, zsh 5.9 `-f`"*, which I RE-MEASURED independently on zsh 5.9 with `-f`: iterating a command substitution of three lines gave **3 iterations**; assigning it to a variable first and iterating that gave **1**. The corpus is correct as it stands. (2) The "~29 B of mangled duplication" is **NOT mangled** — lines 386-387 are the inline code span `` `while read` `` legitimately wrapped across a line break. ⚠️ **AND MY OWN INSTRUMENT MANUFACTURED THAT SECOND FINDING**: I first read the region through a viewer that truncated each line at 170 columns, which cut line 386 mid-span and removed the opening `` `while ``, leaving a dangling closing backtick before *"from a file"* that looked exactly like corruption. Reading the raw bytes dissolved it. The lesson is the deliverable: **a TRUNCATING viewer is an instrument**, and a defect visible only through it belongs to the instrument, not the file — read raw bytes before reporting a file corrupted. (3) Closing this by READING rather than by a status grep is exactly what this file's own prose above demands; a keyword census would have left it open, since it carries no closure marker. |
| **B50** | ✅ **DONE 2026-07-19 (#349) — the blocking measurement was taken and the check is built, reviewed and shipped as `make verify-ingress-rendered`.** MEASURED on a live KinD cluster with the app backends deliberately absent (install-ingress run, `gitops` NOT run): **`nosuch.vks.local` → 404**, **`javawebapp`/`gowebapp` → 503**, **`gitea`/`tekton` → 200**. So the 404-vs-503 split is REAL, the `inferred` grade is retired, and assertion (b) does NOT collapse to "not 000". A 503 proves Envoy matched a RENDERED route and found no endpoints; a 404 would mean the carried `yq`/`envsubst`/`kubectl` never rendered the Gateway host + VirtualService — a path nothing else in the repo exercises. `98-verify-ingress.sh` is UNTOUCHED, as the refuted-skip analysis below demanded. An implementation-round adversary then found **five** defects, each re-verified and fixed: the split is measured on istio ONLY so the gate now **dies on an unmeasured controller** rather than asserting it (traefik renders per-app routes but a missing backend Service makes it 404, INVERTING the meaning); the app hosts had **no poll** while `lib/istio.sh` applies the app VirtualServices at `:604` AFTER the infra ones at `:599`, so the infra poll never covered them (K1.5 race → flaky RED blaming the carried toolchain); `route_code` returned **`000000`** because `curl -w '%{http_code}'` already prints `000` AND exits non-zero, so the `\|\| echo "000"` appended a second one and the best diagnostic branch was **dead code** (⚠️ **the same bug is inherited from `98-verify-ingress.sh:56-59`**, where it also degrades the `000\|50[234]` retry arm — worth fixing there); a deployed-but-CRASHLOOPING backend would pass as "absent" (istio 503s identically for missing-Service, zero-endpoints and all-unhealthy) so that is now a STATED limit; and the `infra_checked` guard **could never fire** over a literal 2-element list while the comment claimed both counts fall independently — deleted, because a guard that cannot fire reads as a guarantee it does not give. ⚠️ **My own RED-proof for the controller guard silently PASSED first time**: `INGRESS_CONTROLLER=traefik ./97…` does not fire it, because `load_env` sources `.env.state` AFTER the environment so the PUBLISHED value wins (the state-overlay clobber). That is correct for this gate — it must check what was INSTALLED, not what the caller claims — but the proof has to rewrite the published value, and the working form is recorded in the script. The refuted-skip analysis is kept below because it is why the design is additive: 🔵 **THE SKIP DESIGN IS REFUTED (idea-round 2026-07-18, two measured CRITICALs) — but the round produced a BETTER, cheaper replacement that is NOT yet built.** ⛔ **Do NOT make `98-verify-ingress.sh` skip hosts whose backend is absent.** (1) A kubectl predicate **cannot distinguish "backend absent" from "I could not ask"** — measured `rc=1` alike for NotFound, an unreachable API server, AND a bad kubeconfig (which also silently falls back to `http://localhost:8080`, this repo's own C13/B32 hazard). A stale `KUBECONFIG` would make ALL FOUR hosts "absent" → all skip → the gate prints success having checked nothing; on a real lab a tenant lacking get-deployment rights makes Forbidden indistinguishable from NotFound. And it would convert a gate that today needs **only curl** (`:26`) into one requiring cluster API access. (2) **The objects a predicate would key on are created by `install-ingress` ITSELF** — `lib/istio.sh:284,572` `ensure_namespace` per app and `:611` applies the per-app VirtualService unconditionally from the registry — so the namespace and VS ALWAYS exist and discriminate nothing; only Service/Deployment differ, and **Endpoints — the intuitive choice — is wrong in 4 of 6 states because "no endpoints" IS the failure this gate exists to catch**. No workload-keyed predicate gets the ArgoCD-sync-FAILED row right (it looks identical to never-deployed). Also: "fail if ALL were skipped" is theatre here (the air-gap steady state is 2 checked / 2 skipped, so it never fires); the skip would need threading through TWO loops (`:79` route and `:114-119` body); and it would **permanently vacuum two working callers** — `verify-ingress-both` (`Makefile:499-500`) and `e2e-kind-istio-existing` leg 2 (`:522`) run verify-ingress with NO preceding `verify`, so they FAIL loudly and correctly today. 🟢 **BUILD THIS INSTEAD (not yet done): the 503 is EVIDENCE, not an embarrassment.** 503 = Envoy matched a route and resolved a cluster with no healthy endpoints; 404/000 = the route was never configured (this repo says so itself at `98-verify-ingress.sh:140`). So an **ADDITIVE** check in the air-gap leg — leaving `98-verify-ingress.sh` COMPLETELY untouched — asserting gitea+tekton are 2xx/3xx with their body markers AND each app host is **NOT 404, NOT 000, NOT 2xx** proves the carried `yq` appended the app hosts to the Gateway (`lib/istio.sh:588`) and the carried `envsubst`+`kubectl` rendered the per-app VirtualServices (`:611`) — **the carried-toolchain RENDERING path, which nothing currently checks**. Strictly more coverage than the skip, zero blast radius, no kubectl, and it is NOT the rejected host-subset knob because it is a separate check with its own fixed scope that cannot shrink the real gate. ⚠️ **SETTLE ONE THING FIRST — the 404-vs-503 split is graded `inferred`:** on the next air-gap run, `curl` a host with NO VirtualService (e.g. `nosuch.vks.local`) and confirm it is 404/000 while `javawebapp` is 503. If BOTH come back 503, assertion (b) collapses to "not 000" and is much weaker. Also pin the controller: the leg installs istio, and traefik's no-backend status may differ. |
| **B15** | ✅ **CLOSED — REFUTED + SHIPPED, not open work.** The back-port was disproved by an idea-round and the residual (the anti-"fix" comment + a published-not-live hint in the failure diagnostic) is shipped. Kept red until 2026-08-17 only because nobody re-read it. **DISPROVED — do NOT back-port the 47-attach re-resolve into `98-verify-ingress.sh` (vks-adversary idea-round, 2026-07-17 s3; correction now lives in the code comment near `98-verify-ingress.sh:27`).** `98-verify` is a **PURE CONSUMER** of `INGRESS_LB_IP` — it never `state_set`s it (grep-confirmed), so it structurally cannot reproduce the 47 resolve-then-publish-stale false-green. Every caller (`Makefile:421` e2e-kind, `:493-494` verify-ingress-both, `:513/516` istio-existing, `docs/lab-validation-plan.md:561→565`) runs `install-ingress` immediately before `verify-ingress`, republishing the value for the current mode → never stale in any orchestrated/documented path. A stale STANDALONE read is the SAFE direction (a dead IP → loud route FAIL, self-diagnosed at `~98:112`; a false-green would need the "stale" IP to actually route the current hosts to the current backends serving the right markers, i.e. not stale), and the real-lab standalone case is further guarded by `state_check`'s cluster stamp. Back-porting the 47 pattern would DUPLICATE installer logic and depend on `INGRESS_CONTROLLER`, which has the identical read-back class — no net gain. Shipped: the anti-"fix" comment + a published-not-live hint in the failure diagnostic. |
| **B17** | ✅ **DONE 2026-07-18 (#320/#322/#323/#325).** 🔵 **RE-SCOPED — as written this row is STALE, and its own audit's "Still open" paragraph is a CLAIM that no longer holds.** Re-verified 39 of the audit's 68 findings against the current tree (all 29 in the named scope): **25 of 29 are FIXED**, 2 were moot. `prerequisites-manual.md` (6/6) and `scenario-2.md` (13/13) are **DONE**. The "CLAUDE.md gate-list drift" half is structurally dead for `static-check` (that row now points at the Makefile as authoritative) and the two rows that HAD rotted — `sec` omitting `check-secrets-untracked`, `mirror` omitting `mirror-verify` — landed in **#320**. **So the named scope is CLOSED.** What survives is a *different*, still-real set of **nine rows found OUTSIDE that scope**, listed in the audit; the highest-value is `docs/scenario-1.md:419` claiming the Istio helm chart still needs internet, which the carried-charts branch in `46-install-istio.sh` and `docs/sneakernet.md:115` both refute — it states a limitation that no longer exists, in the direction a reader will believe. Others: `file(1)` used by `11-bundle.sh:149` but neither installed by `00-install-prereqs.sh` nor gated by `03-check-tools.sh` (HIGH); the scenario-1 SAN `Expect:` line; "install-all starts with mirror" (it starts with `preflight`); the four-vs-three preconditions count; the sneakernet `make deps` rationale; a 12 GB/11 GB bundle-size drift; exFAT listed in both the cannot-use and use-this lists. **All nine landed 2026-07-18** across #322 (the Photon toybox false-die), #323 (six doc rows, two of which an adversary refuted before I wrote them) and #325 (the false rationale inside `test-airgap-toolchain.sh`). **B17 is CLOSED.** |
| **B24** | 🟢 **TWO OF THE THREE GAPS CLOSED 2026-07-19 from primary sources; only the PSA one is genuinely lab-gated.** **(1) `-n tkg-system` vs `-n istio-installed` — HALF of this was a confusion and half is a REAL variant; an adversary round on 2026-07-19 caught me overstating it, so the corrected form is:** on Broadcom's page they are two flags on **two different commands** — **discovery** `vcf package available get istio.kubernetes.vmware.com **-n tkg-system**` (the repo lands in `tkg-system`, a *global* kapp-controller namespace) vs the **install target** `vcf package install istio -p … **-n istio-installed**`. So seeing `tkg-system` in a *discovery* command is **not** evidence of an install-namespace variant, and using `istio-installed` for discovery is simply wrong. ⚠️ **But the INSTALL namespace itself IS a genuine variant, and my first pass wrongly said it was not**: both community walkthroughs run `vcf package install istio … **-n tkg-system**` (quoted verbatim in `docs/vks-services/istio.md`'s "Install namespace variant" row), and `-n` on `vcf package install` simply selects where the PackageInstall CR lands — `tkg-system` is a real namespace, so that install works. **`docs/vks-services/istio.md:249` is therefore CORRECT as written and must NOT be "fixed"** — an adversary prescribed deleting it on the strength of my overstatement, which would have replaced a right row with a wrong one. Keep `istio-installed` (better-graded, `9.0-doc`) as ours; record `tkg-system` as a community variant, not an error. Also newly `9.0-doc`: the package needs **VKr 1.29 and later**. **(2) Multi-primary multi-cluster went from ZERO coverage to `9.0-doc`** — Broadcom ships a dedicated *Configure Istio multi-clusters* page. Graded facts now in the adversary + the private reference: the floor is `1.28.2+vmware.1-vks.1` *"including 1.28.1+vmware.1-vks.1"* (upgraded from `community`, and still the **PACKAGE's** floor not Istio's); **both** topologies are supported (*"Primary-remote on different networks"* and *"Multi-primary on different networks"*); **sidecar mode ONLY**, because *"the upstream ambient multi-cluster feature has not yet reached GA"* — so flag any ambient+multi-cluster design on sight; the schema is `meshConfig.{meshID,network,multiCluster.{enabled,clusterName,clusterProfile,primaryClusterNames,remotePilotAddress}}` + `externalIstiod: true` on primaries; the east-west gateway is **REQUIRED** in the primary but **its creation method is NOT STATED** (Broadcom defers to upstream — precisely where the two air-gap traps bite: upstream `istioctl` pulls `docker.io/istio/proxyv2` instead of Harbor, and a Layer-7 LB breaks `AUTO_PASSTHROUGH`); and inter-cluster **network reachability is NOT STATED**, so it stays an unknown rather than an assumed prerequisite. ✅ **This RESOLVED a flagged `[UNVERIFIED-BY-US]`: `clusterProfile` EXISTS** (`primary`\|`remote`) — the community walkthrough was right and our doubt was the error, recorded that way round per the rule that a retraction needs the same evidence as the thing it retracts. ⚠️ **PARTIAL, and the open half is the one that matters: `cacerts`.** Broadcom confirms the Secret **name**, its **namespace** (root / `istio-system`) and that it is needed in **each PRIMARY** cluster — but **NOT its data keys**, so the cert-manager (`tls.crt`/`tls.key`) vs upstream (`ca-cert.pem`/`ca-key.pem`/`root-cert.pem`/`cert-chain.pem`) key-shape question is neither confirmed nor refuted and that section stays RED. Do **not** let "Broadcom documents `cacerts`" be read as "the cert-manager recipe works". **Still lab-only:** the gateway-namespace PSA minimum (`make psa-check`), and reading the actual `cacerts` keys off a real cluster. The **manifests and the air-gap delta live in `~/projects/claude-config/reference/istio-on-vks.md`** (private — this repo is public, and unrun manifests do not belong in a provenance-graded record). Of the **three** things flagged **UNVERIFIED-BY-US** there, **one is now RESOLVED** — `clusterProfile` exists in the VKS package schema — and **two remain, each settled by one lab command**: whether cert-manager's `cacerts` (`tls.crt`/`tls.key`/`ca.crt`) is readable by istiod at all, given upstream documents `ca-cert.pem`/`ca-key.pem`/`root-cert.pem`/`cert-chain.pem` (`kubectl get secret cacerts -n istio-system -o jsonpath='{.data}'` names the keys); and the gateway-ns PSA minimum (`make psa-check`). ✅ **BOTH LAB-ONLY ITEMS PROGRESSED ON A REAL LAB, 2026-08-17 (guest `cicd-gc0817163305`, VKr `v1.35.5+vmware.1`).** **(Q1) THE GATEWAY-NAMESPACE PSA MINIMUM IS `baseline`, MEASURED, and the reason is a SINGLE field.** `make psa-check` reports `istio-ingress -> baseline`, and its stated why is exactly one thing: `istio-ingressgateway-…: seccompProfile`. The same single reason holds for `istio-system` (`istiod-…: seccompProfile`) and for `vks-ingress` (`vks-uis-istio-…: seccompProfile`). By contrast `ci` needs `baseline` for FIVE reasons (Kaniko: `allowPrivilegeEscalation`, unrestricted capabilities, `runAsNonRoot`, `runAsUser=0`, `seccompProfile`), and `gitea` / `tekton-pipelines` / `javawebapp` / `gowebapp` all sit at `restricted` cleanly. This CONFIRMS on real VKS what `CLAUDE.md` previously asserted from KinD — *"the Gateway namespace (Istio's auto-provisioned proxy sets no seccompProfile) needs `baseline` or their pods are REJECTED"* — and it sharpens it: the mesh namespaces are ONE field away from `restricted`, so a future Istio that sets `seccompProfile` would let all three tighten, whereas `ci` would not. **(Q2) `cacerts` IS NOT OBSERVABLE ON THIS LAB, and the reason is structural, not incidental — this is a RE-CHARACTERISATION, not a measurement failure.** `istio-system` exists and is Active, and `cacerts` is `NotFound`; the FULL secret list (`istio-ca-secret` type `istio.io/ca-root`, plus two `sh.helm.release.v1.*`) confirms absence rather than a query miss. The cause: this mesh is **OURS**, helm-installed from our own registry (`istiod` image `harbor.env1.lab.test/cicd/istio/pilot:1.30.3`), and our install never supplies an operator CA — **istiod SELF-SIGNS**. `cacerts` is the operator-supplied path, which nothing in this repo exercises, so **no amount of looking at this lab can settle the key-shape question**; settling it requires either installing the VKS Istio PACKAGE or actually supplying a `cacerts` and observing what Istio accepts. ⚠️ **ADJACENT EVIDENCE, and it is NOT an answer — graded that way deliberately:** the secret istiod DID create, `istio-ca-secret`, carries `ca-cert.pem ca-key.pem cert-chain.pem key.pem root-cert.pem` — the **upstream `*.pem` shape**, not cert-manager's `tls.crt`/`tls.key`. That is a measurement about a DIFFERENT Secret (self-generated, not operator-supplied), so it SUPPORTS the upstream shape without confirming it for `cacerts`; the section stays RED as this row requires, and the "Broadcom documents `cacerts`" caution still stands. **CORRECTION recorded per the rule that a retraction needs its own evidence:** my first probe printed `<no cacerts / no istio-system>` as ONE message and I read it as "no Istio anywhere on this lab", which was FALSE — the discriminator was mine, not the cluster's. Re-measured separately above. |
| **B25** | ✅ **CLOSED — BOTH HALVES DONE (#371 review, #369 build), verified: both PRs MERGED, `scripts/96-verify-gateway-image.sh` exists and is wired into the Makefile at 5 sites.** ⚠️ **This row is a WORKED EXAMPLE of B54's rule that the tail reads like a work order.** Its final paragraph — *"assert the route PROGRAMMED into the proxy … report mTLS mode … anything that cannot become an assertion goes to lab-validation-plan.md"* — looks exactly like open prescriptions, and is in fact the **ORIGINAL ASK quoted for context**, which the row says in as many words. The real outcome is in the middle: candidates 1 and 3 were **REFUTED with measurements** (the server-header assertion is redundant for controller identity because each controller gets its own LB IP — istio `172.18.0.6`, traefik `172.18.0.7`; mTLS-via-istioctl is vacuous in a sidecar-free repo that deliberately ships no istioctl), and candidate 2 was **downgraded to an optional diagnostic** because a 200 plus a body marker already proves a route is programmed more strongly than a config dump does. The URL-liveness residual is a RECORDED FINDING, not open work: 13 of 15 cited URLs resolve and the two 403s are logged as cannot-distinguish-blocked-from-removed, with the verbatim quote in each `src` token as the durable evidence. DONE 2026-07-19/20 — both halves, and the audit refuted the premise I gave it. REVIEW half (#371): every claim in docs/vks-services/istio.md was re-judged, and it measured that B24 never landed in that file at all (its diff is CLAUDE.md, one line) — so the doc had not rotted, the update never arrived. Seven deltas fixed, including a bullet still declaring multi-cluster UNVERIFIED that B24 had already falsified, a community-graded row split so each grade matches its source, and a claim that discovery is skipped entirely which was measured FALSE. scenario-1.md reviewed separately: no deltas — it already carried B24's namespace distinction correctly. BUILD half: the ranked candidate shipped as 96-verify-gateway-image (#369). Candidates 1 and 3 were REFUTED — the server-header assertion is redundant for controller identity because each controller gets its own LB IP (measured: istio 172.18.0.6, traefik 172.18.0.7), and mTLS-via-istioctl is vacuous in a sidecar-free repo that deliberately ships no istioctl. Candidate 2 was downgraded to an optional diagnostic: a 200 plus a body marker already proves a route is programmed more strongly than a config dump does. Residual: the URL-liveness audit found 13 of 15 cited URLs resolve; the two Medium 403s are recorded as cannot-distinguish-blocked-from-removed, NOT as link rot, with the verbatim quote in each src token named as the durable evidence. ORIGINAL ASK, for context: **Validate Scenario 1's Istio material end-to-end with the enriched `vks-adversary` — and land it as EXECUTABLE AUTOMATION, not prose.** Every Istio claim in `scenario-1.md` + `istio.md` + the install/attach scripts gets re-judged against the enriched brief and `~/projects/claude-config/reference/istio-on-vks.md`. **The deliverable is code**: each verifiable claim becomes an assertion in `make istio-preflight` / `verify-ingress` / a new gate — not a paragraph. Concrete candidates already identified: assert `server: istio-envoy` on the response (the PATH, not just the body marker); assert the route **PROGRAMMED** into the proxy (`istioctl proxy-config routes …`), not merely `Accepted`; report mTLS mode from `istioctl x describe pod`; assert the mirrored-image alignment for any istioctl-rendered image. Anything that cannot become an assertion goes to `lab-validation-plan.md` as a numbered step with its command, its expected observable, and what to send back — never as a doc sentence. |
| **B26** | 🟠 **PARTIALLY ADDRESSED — READ THIS ROW TO ITS END.** The injector half is ANSWERED (2026-08-22, from the RENDERED VMware package bundle: stock upstream, zero selector modifications, two digests); what remains is a RUNNING injected pod on a package mesh, which NO matrix run can produce. — 🔴 **Fixes (1)(2)(4) SHIPPED (#295/#296); this row is now fix (3) ONLY.** The pod templates carry `sidecar.istio.io/inject: "false"` (all 4, `check-pod-inject-label` green); `ensure_namespace` stamps `istio-injection=disabled` (`psa.sh:174`); the attach e2e now injects, not simulates the safe case (shipped #296). 🔴 **Fix (3) AS SPECIFIED IS REFUTED — `build-a-different-thing` (idea-round 2026-07-18, 28 cells measured with real apimachinery selector semantics). Do NOT build "render the chart and assert every rule is defeated".** The INVARIANT survived scrutiny (the adversary could not refute it): on `istiod-1.30.3.tgz`, base → **4** rules, `enableNamespacesByDefault=true` → **5**, `revision=canary` → **2**, and in every config each rule is defeated **twice over, independently** — by `istio-injection=disabled` on the namespace AND by `sidecar.istio.io/inject="false"` on the pod. **But the GATE built on it is vacuous, and the number that settles it is: ONE NON-ZERO CELL IN TWENTY-EIGHT.** Istio does not inject without a **positive opt-in** (`istio.io/rev In [default]`, `istio-injection In [enabled]`, `inject In [true]`), so an unlabelled namespace satisfies nothing and "every rule is defeated" is satisfied **by the chart, not by us** — delete BOTH our defences and the gate stays **green** on base, as-installed, revision, and revision+ensbd. Only `enableNamespacesByDefault=true` discriminates. **So the proposed RED-proof FALSE-PASSES on the default render**: "drop both labels → must go RED" holds ONLY for `ensbd`; run it on base (the natural thing) and you either "fix" a working gate or conclude it works when it never fired. Two further blockers: `global.operatorManageWebhooks=true` and `global.resourceScope=namespace` render **ZERO** webhooks, so "every rule is defeated" is trivially true over an empty set; and **the gate cannot run in CI at all** — `bundle/` is gitignored (`.gitignore:7`, `git ls-files bundle/` = 0), charts appear only after `10-mirror-pull.sh` (needs the internet), so it would SKIP (the repo's named "passes by not looking") or be permanently red. **Worst of all, its coverage is INVERSE to the hazard:** it renders the chart WE install — where we ALSO set `global.proxy.autoInject=disabled`, a *third* defence rendering `policy: disabled` in the injector ConfigMap — while B26's actual hazard is **attach mode**, where the mesh is VMware's `1.28.2+vmware.1-vks.1` and we render nothing. **IF BUILT, build it as:** (a) assert **DISCRIMINATION**, not defeat — per config compute the verdict *with* and *without* our labels and require they DIFFER, else record that config as "inherently safe, this gate does not measure us here"; (b) **commit the rendered webhook fixtures** (~2 KB, upstream Apache-2.0) so CI evaluates those offline, plus an operator-local drift gate that re-renders when the chart is present; (c) a denominator guard demanding ≥1 MutatingWebhookConfiguration and a per-config minimum rule count; (d) derive the config list from the **template's** `.Values.` references, not a hand list; (e) single-source our label pair from `lib/psa.sh` rather than hardcoding a second copy; and (f) **name it what it is** — a regression gate on the upstream chart we install ourselves, explicitly zero evidence for attach mode or the lab. ⚠️ Also corrected: "the knob is IGNORED on the revision path" is imprecise — it adds no webhook **RULE** there, but still renders into the injector ConfigMap (unreachable when no rule matches). And "defeated twice" is true of the **chart**, false of the **deployment**: `check-pod-inject-label.sh:45` scopes to IN-TREE pod templates, so Harbor/ArgoCD/Tekton/istiod pods carry only the namespace label — do not let the double-coverage framing license weakening the ns label. ⚠️ **Lab-gated residual:** the shipped fixes are upstream **1.30.3**-rendered; VKS ships `1.28.2+vmware.1-vks.1` [community] and Broadcom could patch the injector template. One command settles it: `kubectl get mutatingwebhookconfiguration -o yaml \| grep -A8 namespaceSelector`. 🟢 **SOFTENED 2026-07-22 — the capability half of this row is probably moot.** Broadcom's Istio Package Reference documents `istio.istioCNI.enabled` as defaulting to **`true`** ("DaemonSet istio-cni-node is deployed if it is true", fetched 2026-07-22 — now recorded in `docs/vks-services/istio.md`). With the CNI DaemonSet handling traffic redirection, injected pods get **no `istio-init`**, hence no `NET_ADMIN`/`NET_RAW` request — which is the exact conditional this row's PSA hazard rests on. So on a package-installed VKS mesh the residual narrows to **`seccompProfile`**, not capabilities. `inferred` from the documented default, **not** measured on a lab: step 13 of `docs/lab-validation-plan.md` now checks whether `istio-cni-node` actually exists. Do not close this row on the inference alone. 🟢 **LAB-MEASURED 2026-08-17 (cut B, guest `cicd-gc0817071000`) — the row SPLITS: the GATEWAY half is SETTLED, the ATTACH half is still unmeasured, and the softening's REASONING is superseded even though its conclusion holds.** Ran both commands this row names. **(1) The injector keys on exactly what we assume.** `kubectl get mutatingwebhookconfiguration istio-sidecar-injector` renders **4** rules and every one carries `istio-injection` (`DoesNotExist` on rules 1/2/4, `In [enabled]` on rule 3) **and** `sidecar.istio.io/inject` (`NotIn [false]` on 1/2/3, `In [true]` on 4) — so both defences are live against this build, and all **7** of our workload namespaces do carry `istio-injection=disabled` (`ci`, `gitea`, `gowebapp`, `javawebapp`, `tekton-pipelines`, `tekton-pipelines-resolvers`, `vks-ingress`), with 8 of 64 pods additionally carrying the pod-level label. **(2) `istio-cni-node`: ABSENT — ZERO DaemonSets cluster-wide.** ⚠️ **But NEITHER of those settles the lab-gated residual, because this mesh is OURS:** `istiod` runs `harbor.env1.lab.test/cicd/istio/pilot:**1.30.3**`, not `1.28.2+vmware.1-vks.1`. A helm install with `istioCNI` unset is *expected* to have no CNI DaemonSet; that is not evidence about the **package** default, and a 1.30.3 injector template is not evidence about VMware's. The residual stands, unchanged, and needs a cluster with the package installed. **(3) The result that DOES survive the package question — and it is stronger than the softening's inference.** Server-side dry-run PSA (`kubectl label --dry-run=server --overwrite`, this row's own MEASURED-not-guessed method) evaluated against the real running pods: `istio-ingress` and `vks-ingress` violate `restricted` for **`seccompProfile` and nothing else**, and both are **OK at `baseline`**. Both gateway pods — the classic helm `istio-ingressgateway` **and** the Gateway-API auto-provisioned `vks-uis-istio` — carry **zero init containers**, `runAsNonRoot=true`, `runAsUser=1337`, `allowPrivilegeEscalation=false`, `readOnlyRootFilesystem=true`, `capabilities.drop=[ALL]`, `add=None`, `seccompProfile=null`. So for the **gateway namespaces** the `NET_ADMIN`/`NET_RAW` half does not fire **and structurally cannot**, for a reason INDEPENDENT of `istioCNI`: **a gateway proxy has no init container in the first place** — it is a listener, not an iptables redirect target. The softening's *conclusion* (the residual narrows to `seccompProfile`) is CONFIRMED for the gateway; its *stated reason* (the CNI DaemonSet) is not the operative one there, so do not carry the CNI framing forward as the justification. This also confirms CLAUDE.md's `psa-check` line verbatim, on a real guest cluster for the first time. **(4) STILL UNMEASURED — an INJECTED WORKLOAD pod on a package mesh.** Nothing on this cluster is injected (every workload ns is `disabled`), so the cluster-wide `istio-init` count of 0 is a consequence of **our own defence**, not evidence about the hazard. The capability half remains open exactly where the row says it lives: **attach mode against VMware's build**. **(5) Incidental, same run:** `ci` violates `restricted` on four counts (`allowPrivilegeEscalation`, `runAsNonRoot`, `seccompProfile`, unrestricted capabilities) — the Kaniko-as-root shape CLAUDE.md predicts — while `javawebapp` is **OK at `restricted`**, so the app namespaces need no exemption at all. 🟢 **(4) THE INJECTOR HALF IS ANSWERED — OFFLINE, 2026-08-22 — and no matrix run could ever have settled it.** VMware's Istio package bundle was pulled anonymously **by digest** and rendered with `ytt` at **default** values (`crane export … \| ytt -f pkg/config`, rc=0): the injector is **stock upstream** (`# Source: istiod/templates/mutatingwebhook.yaml`) — **4 rules, 4 `istio-injection` keys, 4 `sidecar.istio.io/inject` keys**, so both of our defences defeat all four — and the package's **overlay carries ZERO selector modifications** (a grep of `pkg/config/overlay/` for `namespaceSelector`/`objectSelector`/`istio-injection`/`sidecar.istio.io/inject` returns **0 hits**; it only rewrites `clientConfig.service.namespace`). Confirmed on **two different bundle digests** (1.27.1 `245463cc…`, 1.28.5 `52fba008…`), 4/4/4 each. Re-run that two-command recipe per package bump. ⚠️ **AND THE MATRIX IS STRUCTURALLY INCAPABLE of reaching this row:** injection needs a POSITIVE opt-in (`istio.io/rev In [default]`, `istio-injection In [enabled]`, or pod `sidecar.istio.io/inject In [true]`) and we set **none**, so even an *unlabelled* namespace is not injected — a 4-hour matrix would report the same `istio-init` count of **0** it reports today, from our own defence, and a green run would be misread as 'B26 exercised'. What is still genuinely unobserved is a **running** injected pod on a package mesh: a deliberate `istio-injection=enabled` throwaway namespace, ~15 min on a throwaway cluster, **never the certified one**. 🔴 **CORRECTION to this row's OWN softening — the CNI mechanism sentence is wrong, in the false-green direction.** CNI does not remove the init container, it **RENAMES** it: `{{ if .Values.pilot.cni.enabled }}- name: istio-validation {{ else }}- name: istio-init {{ end }}`, with the capability block conditionally emptied. So a check counting containers named `istio-init` returns **0** while an init container **is** present. Key any such check on `initContainers[] != []`, never on the name. (This also upgrades `istioCNI.enabled: true` from doc-inferred to bundle-rendered.) ⚠️ **Two findings that do not close the row but must not be lost:** installing the package stamps `istio-system` with `pod-security.kubernetes.io/enforce: **privileged**` — ours is `baseline`, so it would relabel a shared namespace DOWNWARD, the mirror of the hazard `48-istio-preflight.sh` already warns about; and the Gateway-API auto-provisioned proxy takes its image from the **MESH's** hub, which on a package mesh is `projects.packages.broadcom.com/…/proxyv2`, **not our Harbor** — harmless on this internet-connected lab, a hard failure on a genuinely air-gapped one. |
| **B32** | ✅ **CLOSED — REFUTED + SHIPPED, not open work.** The gate was disproved by an idea-round; the narrowed fix (`kubeconfig_ready` in `lib/os.sh` at the 6 standalone-plausible consumers, plus `test-kubeconfig-ready.sh` RED-proving it) is shipped. **DISPROVED as a gate (adversary-bash-git-cli idea-round, 2026-07-17 s3). Do NOT build a vacuous-`:?` detector.** The MEASUREMENT holds (~62 `:?` guards; 43 vacuous — 41 uncommented-in-`.env.example` + KUBECONFIG(19 sites)+ARGOCD_NAMESPACE(4) via `load_env` code defaults at `os.sh:406/415`), but **"vacuous" ≠ "harmful"**: the 41 class-A guards each ship a valid, usable default and are **defensive documentation-in-code** — commenting the var out to make the guard fire is a *regression*, deleting the guard loses the assertion, so a hard gate would flag 42 non-defects with no sane remediation → the disabled-gate trap. The ONLY reachable harm is C13 on the KUBECONFIG *read* sites (default is a PATH that may not exist → `:?` proves set-not-present → kubectl silently falls back to `http://localhost:8080`), and it is **LOW** (UX only; no data-loss/fake-green) and **already handled** by `env-check`'s `[ -f ]` (`02-env.sh:130-150`); it only bites on out-of-order/standalone invocation. Shipped (#313): an NB comment at the `os.sh` KUBECONFIG default so no NEW path-valued `load_env` default gets a bare `:?`. **Residual DONE (s3):** an adversary idea-round REFUTED the 16-site sweep (it would regress the read-only preflight ACCUMULATORS `23`/`49`, misfit `71` [already `[ -f ]`s a different file] and `06` [deferred-export]) — narrowed to a `kubeconfig_ready` `[ -f ]` helper in `lib/os.sh` at the 6 standalone-plausible consumers (`40`/`41`/`50`/`60`/`70`/`99`), all producer-first so no false-die in the blessed flows; plus fixed the stale `.env.kind` hint (legacy — nothing writes it) → a producer hint (`make vks-login`/`make kind-up`) at `07`/`45`/`46`/`47`/`48`/`49`. `test-kubeconfig-ready.sh` RED-proves the C13 gate (old bare `:?` passes a missing file; helper dies) + a regression guard that `23`/`49`/`30`/`71`/`06` never call it. |
| **B35** | 🟢 **PREMISE RE-CONFIRMED on 1.30.3, 2026-07-18 (measured offline, `helm template` on the carried chart).** Rule counts: base **4**, `enableNamespacesByDefault=true` **5**, `revision=canary` **2**, `revision=canary`+`enableNamespacesByDefault=true` **2** — the knob is genuinely IGNORED on the revision-tag path. ⚠️ **A doubt was raised and REFUTED by measurement:** `istiod/templates/revision-tags-mwc.yaml:131` *does* contain an `if .Values.sidecarInjectorWebhook.enableNamespacesByDefault` conditional, which reads as if the knob applies — it does not, on the rendered output. Reading the template is not reading the render. (Also worth recording: the first counter I wrote returned **1 for all four configs** — an instrument that gives the same answer for different inputs is broken, not a finding; the pattern was `^  - name:` when webhook entries sit at `^- name:`.) 🔵 **RE-SCOPED 2026-07-18: this is NOT a coverage gap to close — the revision path is INHERENTLY SAFE BY OPT-IN.** Both of its 2 rules require `istio.io/rev In ["canary"]` on the namespace or the pod, and our namespaces never carry that label, so the revision path **structurally cannot exercise the hazard even with zero defences** (measured: 0 undefeated rules with NEITHER label). The original framing — "a revision-tag e2e would silently stop exercising the hazard" — is right about the effect and wrong about the cause: it never could. Record it as inherently-safe, not as a gap. **B26's revision-tag case is NOT covered by the shipped design.** A platform mesh installed with a REVISION TAG renders only **2** webhook rules and **ignores `enableNamespacesByDefault` entirely** — so a revision-tag e2e would silently stop exercising the hazard. B26's own text names the revision-tag case. Our `istio-injection=disabled` label DOES still defeat the rev-tagged rule (it demands `istio-injection DoesNotExist`), so this is a COVERAGE gap, not a correctness one — but a gate that cannot fire on the revisioned shape must say so rather than imply it. |
| **B37** | ✅ **REFUTED (incident CLOSED #295/#296/#297) — DO NOT BUILD `make red-prove`. This verdict leads the row on purpose; read it before the design below.** Refuted THREE times: twice inside this row already, and again on 2026-08-17 by a dedicated idea round that measured it across all 56 `scripts/test-*.sh`. **206 of 505 cases (40.8%) do NOT flip, and 40 of 47 files (85.1%) contain at least one non-flipping case.** Under the row's LITERAL spec (baseline = `origin/main`) it is **100% vacuous today**, since `HEAD == origin/main` — it measures nothing on `main` by construction. **IT CONDEMNS THE POSITIVE CONTROL that `testing.md` makes BLOCKING:** the first two non-flipping cases in `test-namespace-gates.sh` — the successor test that CLOSED this row's own incident — are literally named *positive control: check-namespace-labelled GREEN on the unmodified tree*, and every ALLOW/negative-control case and every `mutation landed:` self-check goes with them. `gates.md`: *a gate whose only remedy degrades the artifact is refuted on sight.* **IT IS ANTI-CORRELATED WITH TEST QUALITY:** the 7 files where every case flips pass trivially (their adding commits rewrote 3-23 other scripts, so everything failed on baseline because the subject did not exist), while the best-controlled test in the repo — rc=0 on baseline, 13 ok, 0 FAIL — is BLOCKED. **AND THE MECHANISM DOES NOT EXIST:** *the house ok/FAIL format makes the per-case diff mechanical* is FALSE — FAIL lines carry the DIAGNOSTIC, not the case name, and in **41 of 42 files (97.6%) zero baseline-FAIL names match any current-ok name** (two FAIL lines in `test-vks-sso-user.sh` are byte-identical). Ordinal alignment fails too: 21/56 route FAIL to stderr, 12/56 emit `PASS` not `ok`, 4/56 emit `FAIL:`. Also 6/56 abort on baseline emitting ZERO verdicts, so *errored* and *no case flipped* are the same observation; and `git worktree add` is BLOCKED by the subagent-readonly hook, so the adversary most likely to run a RED-proof cannot execute the prescribed mechanism (#304 already chose `git archive` + `mktemp -d` for exactly this). **A PRINTER IS ALSO REFUTED HERE**, unlike `handoff-status.sh`: its output would be *206 cases measure nothing*, most of them the mandated controls — 40.8% wrong trains people to ignore it. **THE INCIDENT IS CLOSED** (#295/#296/#297; the successor test shipped in #304 as a MUTATION test whose own header says it is *not* a flip-detector; `check-namespace-labelled.sh` shipped). ⚠️ **WHY THE VERDICT MOVED TO THE FRONT:** it used to sit mid-paragraph BELOW the design, so a reader extracted the design and dispatched a fresh idea round on a dead proposal — costing a full adversary session on 2026-08-17. That is `configuration.md` §*a correction placed BELOW the artifact it corrects*, committed in this backlog. **Everything below is the historical record, not a proposal.** ——— ORIGINAL ROW ——— 🔴 **`make red-prove` — a RED-proof must mutate the PRODUCT AS IT SHIPPED, not the code you just wrote.** The rule already exists (`testing.md:281`, *"would this still pass if I deleted the feature?"*; `testing.md:1426`, *"distrust your own RED-TEST"*), was loaded, and was violated anyway on 2026-07-17 — so per the escalation doctrine the deliverable is a GATE, not another paragraph. **The incident:** `test-ensure-namespace-labels.sh` was RED-proven by mutating the `allow-inject` **branch in `psa.sh` that had just been written**, and passed. An adversary then reintroduced the bug **at the call site** (`istio.sh:285`, where #290's defect actually lived) and the gate reported **10/10 ok, rc=0** — blind to the very defect it was named after. I RED-proved the INSTRUMENT, not the DEFECT: the self-authored mutation encodes my own model of the bug, which is exactly what is wrong when the model is wrong. **Design:** run the test against a `git worktree` of `origin/main` with **only the test file transplanted in** (a worktree, NOT `git stash`/`reset --hard` — it cannot touch the working tree, dodging both the data-loss traps), then **diff the per-case verdicts**. The house `ok    <name>` / `FAIL  <name>` format makes that mechanical. A case that does NOT flip `FAIL`→`ok` is a case measuring nothing. ⚠️ **A whole-file red-prove is INSUFFICIENT — I refuted my own first draft in 30 seconds:** any *other* case failing on the pre-change tree turns the file red and masks the blind case (`test-ensure-namespace-labels`'s typo-dies case would have flipped, hiding that the F1 case never did). It MUST be per-case. ⚠️ Applies to **regression** tests only — a test of pre-existing behaviour correctly fails to flip, so the tool must distinguish, or it false-blocks every ordinary unit test. 🔴 **REFUTED a SECOND time — do NOT build the automatic `make red-prove` (idea-round, 2026-07-17 session 2, `adversary-bash-git-cli`, findings VERIFIED against the tree):** **(F1, the killer)** flip-detection measures whether the test's EXISTING cases discriminate baseline↔current — but the incident was a **COVERAGE GAP** (the test asserted "the helper labels" — always true — and never "the installer REACHES the helper", which was the bug). A missing assertion is invisible to a flip-detector, so red-prove would **CERTIFY the exact blind test it is named after** (mutate the helper on baseline → the helper-case flips → "GOOD"). The incident's real remedy is STRUCTURAL and already shipped: `check-namespace-labelled.sh`, keyed on the psa-check inventory ("every namespace we own must be REACHED by an ensure_namespace call") — that is what catches a missing call; a unit-test-flip cannot. **(F2, verified)** a `set -e` house test that sources a helper absent on baseline aborts `rc=1` with **ZERO `ok`/`FAIL` lines** (reproduced in /tmp) — so a loose predicate ("not-ok on baseline") false-PASSES red-prove on a test that never ran, and a strict one ("`FAIL` on baseline") false-BLINDs a good test. **(F3, verified)** `origin/main:scripts/lib/istio.sh:277-279` ALREADY carries the `ensure_namespace` calls — the fix is merged, split across #290/#295/#296/#297, so "baseline = origin/main" finds no bug → false BLIND; there is **no automatic baseline**, and a manually-named one reintroduces the author's-mental-model risk the tool exists to kill. **(F4)** red-prove has no self-check constructible without circularity (the known-blind fixture must be author-fabricated with the same model) → it violates B39, the rule it is filed under. **Conclusion:** the incident class (coverage gap) is **un-gateable by flip-detection** — keep the structural gate (`check-namespace-labelled.sh`, shipped) + B39's per-instrument known-answer discipline, labelled discipline. A NARROWER `red-prove` the adversary sketched (operator-named baseline + explicit `# redcase:` manifest + literal-`FAIL`/literal-`ok` predicate treating any abort as ERROR + a committed known-good/known-blind self-check pair) IS sound but catches a **different, lesser** bug ("a regression test whose declared RED case does not discriminate the pre-fix code") and by the adversary's own verdict **does NOT close this incident** — so it would need its own idea-round before building, not this row. |
| **B39** | ✅ **CLOSED — REFUTED, nothing to build, by decision (the DISCIPLINE persists; the work does not).** The class was deliberately left as DISCIPLINE because all three motivating failures were throwaway instruments in a shell, which no repo gate can see. **THE META-GATE IS REFUTED — do NOT build "assert every `check-*.sh` prints a denominator and dies on zero" (idea-round 2026-07-18; the adversary IMPLEMENTED it, ran it against all 21 gates, and measured it **67% accurate — wrong about 7 of 21, in BOTH directions**). It is a FOURTH instance of this row's own class, not a fix for it.** The property that matters is not *"is there a zero-guard"* but **"is the guard on the ITEM count or the FILE count"** — a semantic distinction no regex can see. 🔴 **REPRODUCED INDEPENDENTLY, and it is a live defect:** empty every tracked `.md` (still tracked) and `check-doc-novels` reports `OK — scanned 21 doc(s)` **rc=0**, `check-doc-robot-quoting` reports `OK — scanned 22 file(s)` **rc=0** — healthy denominators over a corpus with **zero content**. `check-doc-make-targets.sh:70` is the cleanest proof: it prints BOTH numbers and emits `0 command(s) across 25 doc(s)` rc=0, and `:40` explicitly `exit 0`s when no docs match. **Filed as B49.** ⚠️ **MY OWN NUMBERS IN THIS ROW WERE WRONG IN BOTH DIRECTIONS, and the error is the row's thesis in miniature:** there are **21** gates not 20; **~17 print a denominator** (not ~10); **10 have a zero-guard** (not 5); only **6** do both on the *meaningful* count. Two of the five I named as "prints a number that gates nothing" are **false** — `check-image-alignment.sh:73` and `check-doc-target-coverage.sh:56` both carry an explicit `the gate has gone BLIND` guard. **I mis-scored `check-image-alignment` BY HAND in exactly the way my classifier had**, i.e. the manual re-check reproduced the instrument's error rather than correcting it. **THE BUILDABLE THING IS NARROWER AND MUST BE CALLED WHAT IT IS: a VACUITY HARNESS for committed gates, via CORPUS STARVATION** — empty each gate's corpus in a throwaway tree and assert rc≠0. It caught the three blind gates the static version *certified*, runs ~30 s for 21 gates, and **cannot be satisfied by `echo "checked 1"`**. Its self-check is non-circular (a committed blind/good fixture pair with a known answer — unlike B37, where the fixture had to encode my own model of the defect). Its own residual: the per-gate corpus declaration is an **enumerated list that will rot**, and a wrong declaration makes the starvation test vacuous — the same class one level up. **THE ORIGINAL INCIDENT CLASS STAYS DISCIPLINE, labelled as discipline:** all three motivating failures were **throwaway instruments in a shell**, which no repo gate can ever see. The discipline is: *an instrument must be shown to DISCRIMINATE before its output is believed* — run it on ≥2 inputs known to differ and confirm the outputs differ. It would have caught the webhook counter instantly (1,1,1,1). **A fourth instance occurred while verifying THIS row**: `bash gate \| tail -2; echo rc=$?` reported `tail`'s status, not the gate's. |
| **B40** | ✅ **DONE 2026-07-19 (#355) — `make check-ns-chokepoint`, built NARROW with its blind spots named.** **The gap is real and was NOT subsumed by the three assertions in `check-namespace-labelled.sh`** — measured: append `run kubectl <create> <namespace> foo-system` to an installer and all three stay GREEN with **byte-identical counts**, because the derived cross-check only sees namespaces created VIA `ensure_namespace` and `psa-check` iterates `NS_SPEC` without ever asking the cluster what it has. That is this repo's own *"a gate green at the SAME COUNT is blind to it"*, firing on a gate shipped the same day. **COVERAGE IS 4 OF 10 AND THE HEADER SAYS SO**: covered are a literal kubectl invocation, the same via a wrapper (`ka`/`kg`/`hub`/`guest`/`run`), the helm create-ns flag, and the ArgoCD create-ns syncOption; **permanently invisible** are the upstream-manifest and chart-shipped mechanisms, which live in gitignored `bundle/` (`git ls-files bundle/` = 0) so a gate scanning there passes vacuously — out of scope BY CONSTRUCTION; **statically undetectable** are `envsubst \| apply` and heredoc applies. **The corpus of bypass forms is OPEN**: an adversary broke a best-faith version in ten minutes with two forms outside all ten (a runtime-generated manifest, and a verb held in a variable), and both are still missed — recorded in the header, because the alternative is a green that reads as closure. ⚠️ **The comment polarity is INVERTED relative to `check-namespace-labelled`, deliberately** — that gate must NOT strip comments (a commented-out `ensure_namespace` means the call is GONE, its defect), while this one hunts a call that must NOT exist, so a commented-out one is harmless; stripping comment LINES took the hit count 25 → 10 with zero false negatives, and the test is on the FIRST non-space char, not `${line%%#*}`. ⚠️ **The self-test earned its keep immediately**: the patterns are composed at runtime so the file never contains the literals it hunts, and the first version STILL flagged itself — not from prose, but because the **ALLOWLIST REASONS** said "hub create namespace" and "helm --create-namespace". The gate's own DATA was voting on the answer, one level below where I was looking. ⚠️ **My own sizing scan was wrong twice**, both caught by making the instrument disagree with itself: an awk pattern used `\b`, a GNU grep-ism POSIX awk lacks, so the kubectl/wrapper arm matched NOTHING and reported a plausible **6** (real: 10, and the missing 4 included the chokepoint itself); and the first count included comments (25), which reads as "this gate needs 25 exemptions" — the wrong-shape signal that killed the earlier design. RED-proven four ways (erosion, wrapper, helm flag, dead allowlist entry). The refuted history follows, because it is why the gate is shaped this way: **The syntax chokepoint gate (B30a) — DESIGN REFUTED, still unbuilt.** An adversary built and RAN my design against a real corpus: it caught **0 of 8** namespace-creating forms, one **live on the tree** (`hub create namespace` at `e2e-cross-cluster.sh:57`). Namespaces appear here by **SEVEN mechanisms** and a literal-`kubectl` grep sees three: a **wrapper** (`hub`/`guest`/`ka`/`kg` — 6 wrappers across 5 scripts), an **upstream manifest** (tekton's release YAML), a **controller** (ArgoCD `CreateNamespace=true`, from no file at all), a chart's own Namespace, helm `--create-namespace`, an in-tree `kind: Namespace`, and the literal form. **The structural lesson: I enumerated the bypasses with the gate's OWN grep, so the gate could not fail on what the premise missed** — self-confirming, the same shape as the F1 gate one level up. Two concrete bypasses to pin in its test: `echo "# ns-ok: lol"; kubectl create namespace sneaky` **exempts itself** (the raw-line marker check does not parse quotes, and it inflates the `exempt` count); and `${line%%#*}` **false-negatives** on `${#HITS[@]}`, `${v#pfx}`, `grep "#foo"`, and a URL fragment — so do NOT strip comments for detection (a flagged commented-out call is the cheaper error). Refuted hypotheses, do not re-raise: `kubectl create ns foo` and a TAB separator **are** caught; the gap is *internal* whitespace. **`check-namespace-labelled.sh` (shipped) covers the real hazard better** — keyed on the INVENTORY, immune to all six blind spots — so this row is "no NEW bare create appears", a genuine but lesser guard. Needs the 7-mechanism model + a bypass corpus before it is built. 🟢 **THE MECHANISM MODEL IS NOW BUILT AND VERIFIED (2026-07-19), and it corrects this row twice.** There are **TEN** mechanisms, not seven, and two of the seven named above **do not exist on the tree**: M1 literal `kubectl create namespace` (3 sites); M2 a kubectl WRAPPER (`e2e-cross-cluster.sh:57` `hub create namespace`); **M3 an in-tree `kind: Namespace` — ZERO, verified, only two comments asserting its deliberate absence**; M4 an upstream manifest we apply (tekton ONLY — gateway-api, tekton-triggers/dashboard and ArgoCD's install.yaml carry none); M5 `helm --create-namespace` (5 sites); **M6 a chart shipping its own Namespace — none in any chart readable offline, and the Harbor chart is pulled at install time so it is UNKNOWN, not absent**; M7 ArgoCD `CreateNamespace=true`; **M8 `ensure_namespace` itself, the repo-local chokepoint**; **M9 `envsubst \| kubectl apply -f -`**; **M10 heredoc `kubectl apply -f - <<EOF` (7 sites)**. Wrapper count corrected: **7 wrappers across 4 scripts** (not 6 across 5), PLUS the universal `run()` at `lib/os.sh:662` — so a gate anchored on `^kubectl` misses all of them. ⛔ **M4 and M6 are PERMANENTLY INVISIBLE TO CI**: they live in `bundle/`, which is gitignored (`git ls-files bundle/` = **0**), so a gate that greps there in CI scans an empty directory and passes vacuously — this repo's named "passes by not looking". Any B40 gate must declare M4/M6 out-of-scope-by-construction rather than claim coverage. **The research also found a live defect worth more than the gate** — see the namespace-inventory row below. |
| **B43** | 🔵 **REFUTED as specified 2026-07-18 — do NOT build `make e2e-matrix` as an 8-cell grid.** An idea-round adversary measured the topology and the "{podman,docker} × {photon,ubuntu} × {dual-homed,sneakernet} = 8 cells" framing is a **category error**: the axes are not orthogonal. Dual-homed runs the engine **on the box under test** → a real 2×2. Sneakernet's engine is a **host-side** property (one machine, shared by both OS legs) → 2 OS legs × 1 engine choice. The cube does not exist. **Proven, not inferred:** the air-gap half of `jumpbox-run.sh` runs `check-tools`→`bundle-load`→`mirror-push`→`builder-push`→`mirror-verify` and then **`exit 0` at :152**; every `"$ENGINE"` invocation and `engine-trust-check` is *after* that line, so `JUMPBOX_ENGINE=docker` on a sneakernet leg executes **zero additional lines** (`22-builder-push.sh:7-9`: "THIS BOX NEEDS NO CONTAINER ENGINE"). The honest artifact is **6 invocations / 8 legs / 3 groups**, not 8 runs — `SNEAKERNET_OS ?= photon ubuntu` already runs both OS legs in ONE invocation. **The proposed "loud SKIP for vacuous cells" was refuted too**, and this is the subtle part: announcing a cell that does not exist manufactures a known-gap and invites a future session to "close" it by building the vacuous leg — the exact outcome this row was written to prevent. Group by what the axis MEANS instead; the headers are the explanation. **Landed from this review:** `jumpbox-matrix`'s verdict is now COMPUTED (it printed a hardcoded `4/4 PASSED` while tracking only `rc` — narrow the axes to one OS and it still said 4/4; the repo's own "the measured column must be MEASURED" failure, in the summary line of its own matrix), and the retired compressor justification is recorded rather than left to rot. **Still open, and it is a QUESTION FOR THE OWNER, not a judgement call:** B19 (the sneakernet builder half under docker) is on the handoff's LAB-GATED leave-alone list, but it needs **docker + KinD, not a VKS lab** — it may be mislabeled. If it is genuinely open, closing it is **one command** (`make e2e-sneakernet CONTAINER_ENGINE=docker`, ~40 min, destroys and recreates the cluster per leg), not a driver. **Also note:** `e2e-sneakernet`'s EXIT trap runs `kind-down` **on success too**, so any matrix leaves a bare box; and B43 would make **B3** twice as expensive, since B3's Step-4 content is engine-irrelevant — land B3 first. |
| **B38** | ✅ **CLOSED (#803) — the last residual closed 2026-08-17 with evidence and WITHOUT a new gate.** **A claim about an upstream ARTIFACT must cite THE ARTIFACT, not prose that uses the same words — candidate gate: an anchor-resolving `[src:]` for upstream code.** 2026-07-17: an adversary claimed the Gateway-API auto-provisioned proxy ships `image: auto`, so `istio-injection=disabled` on `vks-ingress` would break it (filed CRITICAL "F1"). I "verified" it by fetching `istio.io/latest/docs/setup/additional-setup/gateway/`, which says exactly that — **about the `istio/gateway` HELM CHART** (`gateway/templates/deployment.yaml:73`). Our path is istiod's **auto-provisioned template**, `istiod/files/kube-gateway.yaml`: **`image: auto` → 0 hits**, renders `.ProxyImage`, and sets `sidecar.istio.io/inject:"false"` on the pod itself (`:62`) — it is IMMUNE (ran-it, `release-1.30` AND `release-1.28` = what VKS ships, identical). Same sentence, same words, **different mechanism**: I matched the STRING and called it verification, then deleted a TRUE comment from `psa.sh:78-79` and shipped a FALSE one into a control's rationale. Caught only by a second adversary round refuting the first's own prescription. 🔴 **THE CANDIDATE GATE IS REFUTED — do NOT build it (idea-round, 2026-07-18, every finding `ran-it`).** This row used to say a fetch-and-assert-anchor gate "WOULD have caught this instance". **That is measurably FALSE, and it is the most important correction in this row.** The false CRITICAL cited `istio/gateway`'s `templates/deployment.yaml`, and `image: auto` **is** at line 73 of that file (`grep -c` = 1) — so the citation **RESOLVES** and the gate goes **GREEN on the exact incident it was designed to catch**. Worse than useless: it would **launder a false CRITICAL into a cited one**, which is the manufactured-confidence failure. Two structural reasons it can never work: (1) the claim was never wrong about the STRING, only about **which chart is our path** — an interpretive error no resolver can see; (2) the fact that IS true is a **NEGATIVE** ("`kube-gateway.yaml` contains ZERO `image: auto`", confirmed upstream), and a presence-asserting gate is structurally incapable of expressing an absence. Also fatal in practice: the token's own extractor (`check-vks-provenance.sh:43`, `\[src:[^]]*\]`) **truncates at the first `]`**, so the most discriminating anchors in this domain — the injector selectors, literally `inject NotIn ['false']` — cannot be expressed and RED with a diagnostic naming the wrong cause (**the same shape as B28, second occurrence**); and the gate would **fail OPEN** on any network blip because the script is `set -uo pipefail` with no `-e`. **DO THIS INSTEAD (the fourth option, already working in-tree): when an upstream fact is load-bearing, do NOT cite it — assert its CONSEQUENCE offline over OUR OWN artifacts.** `check-pod-inject-label.sh` is the model: it parses our in-tree pod templates rather than citing istio's chart, so it fails on the thing that actually hurts us regardless of what any upstream file says. B38 therefore stays open as a **DISCIPLINE row, not a gate row**. ⚠️ **Honest limit — it does NOT close the class:** it verifies a citation RESOLVES, never that the INTERPRETATION is right. The interpretive half (*"this file is the template the webhook applies TO a gateway"* — backwards) is a **judgment act and is UN-GATEABLE** (`hooks.md`). Do not report B38 as closing B38's class. The un-gateable residual stays **DISCIPLINE, labelled as discipline**: when a claim names an artifact, the evidence must BE that artifact at a pinned ref — a doc that uses the same words is not the artifact, and neither is an adversary that quotes it. ⚠️ **THE CANDIDATE GATE IS REFUTED — by a MEASURED result whose worked example IS THIS INCIDENT, and it is already in the always-loaded corpus (`claude-config:rules/common/hooks.md:226-248`).** The proposal here is, verbatim, the one measured there: *a gate where every claim about upstream carries `[src: repo@ref path=… anchor=…]`, and the gate fetches the file at the pinned ref and asserts the anchor is present.* **Measured, it goes GREEN on this very incident** — the cited string genuinely IS in the cited file at the cited line. The claim was never wrong about the STRING; it was wrong about **which component is our path**. So the gate would not merely miss the error, it would **stamp it as cited** — strictly worse than no gate. Two structural reasons no version works: interpretation (*'this file is the template that applies here'*) is a judgment act and un-gateable; and the TRUE fact here was a **NEGATIVE** (*that file contains ZERO occurrences*), which a presence-asserting gate cannot express. ⚠️ **DO NOT confuse this with `scripts/check-vks-provenance.sh`, which already exists and is SOUND.** Measured 2026-08-17: it is **OFFLINE** — `code:<FILE>:<LINE>` is resolved against **our own tree** (file+line must exist), while `url=`/`cmd=` are **shape-checked only and never fetched**. It therefore cannot launder an upstream claim, because it never asserts anything about upstream content. The refuted design is the *fetch-and-anchor* one; this is not it. **THE CONSTRUCTIVE DIRECTION, and the corpus prescribes it:** when an upstream fact is load-bearing, do NOT cite it — **assert its CONSEQUENCE offline over YOUR OWN artifacts.** That pattern is already shipped here: `scripts/96-verify-gateway-image.sh` asserts every **RUNNING** Istio container image came from our Harbor, with explicit SKIP branches that say *'NOTHING was verified here'* rather than passing quietly. **RESIDUAL (unmeasured, stated not hidden):** whether B38's *specific* consequence — the auto-provisioned proxy under `istio-injection=disabled` — has its own assertion was NOT checked; `96-` asserts the image SOURCE, which is adjacent, not identical. That is the only thing left in this row; the gate itself is closed. ✅ **THE LAST RESIDUAL IS CLOSED, 2026-08-17, WITH EVIDENCE AND WITHOUT A NEW GATE — this row's own prescription (assert the CONSEQUENCE over your own artifacts).** The residual asked whether the auto-provisioned proxy under `istio-injection=disabled` has its own assertion, noting `96-verify-gateway-image.sh` covers image SOURCE, which is adjacent rather than identical. **It does: `98-verify-ingress.sh`, which routes THROUGH that proxy.** MEASURED from certification row 2 (the GREEN scenario-1 row): `route API: gateway-api`, `applying Gateway(API) vks-ingress/vks-uis`, `verifying ingress routing via INGRESS_CONTROLLER=istio at 192.168.101.135` — all four hosts HTTP 200 **with per-host BODY MARKERS**, not merely a status (*"served the Gitea UI through the ingress"*, *"served the javawebapp greeting page"*, *"served the gowebapp greeting page"*). `192.168.101.135` is the **auto-provisioned** `vks-ingress/vks-uis`; I confirmed on the live lab that the Gateway is `vks-uis` (class `istio`, `Programmed=True`) and that the HTTPRoutes' parent is `vks-uis`. If that proxy were broken by the label, all four assertions would go red. `96-`'s SKIP text already CLAIMED this coverage; this MEASURES it. **B38 is fully closed: gate REFUTED, constructive direction shipped, residual measured.** The install-vs-attach question the trace surfaced is filed separately as B169, deliberately not folded in here. |

| **B51** | ✅ **CLOSED — C2 (#352), C3 (#350) and C4 (#353) all merged, and the residual was SETTLED on a LIVE cluster, not at logic level.** **THREE namespaces reach `ensure_namespace` and are measured by NEITHER inventory, and the drift check is STRUCTURALLY BLIND to that** — found while building B40's mechanism model, verified directly. `tekton-pipelines-resolvers` (`41-install-tekton.sh:90`), `argocd` (`07:78`) and `harbor` (`06:151`) are absent from both `49-psa-check.sh`'s `NS_SPEC` and `check-namespace-labelled.sh`'s `OWNED`. The drift check compares `owned_rows + 2` against `spec_rows`, so a namespace missing from **both** keeps the arithmetic balanced — and comparing the two inventories as SETS would not help either; only a **third, independent ground truth** (the `ensure_namespace` call sites) can see it. Sharpest detail: the gate's own die message cites `tekton-pipelines-resolvers` as the precedent for why the inventories must agree, and it is still in neither. ✅ **SHIPPED in #347:** the counter now sees a LITERAL row (it could not, so adding the very row the header asks for would have made the drift check DIE on a correct change — measured 7 vs 8), and the header's false "It is now listed" is corrected. ⛔ **DO NOT "just add all three to both inventories" — that was REFUTED by measurement and is worse than the bug.** `harbor`/`argocd` are deliberately passed NO level by `ensure_namespace`; `psa-check` reads an absent label as `restricted`; both charts are documented as not restricted-clean — so adding them as `ours` makes `psa-check` rc=1, and `psa-check` is the FIRST prerequisite of `install-all`, converting a reporting gap into a **broken install**. Inventing `PSA_LEVEL_HARBOR` is worse still: the label would then land BEFORE `helm upgrade --install` and could reject harbor's own pods at admission, and `check-env-coverage`'s `INTERNAL` list wildcard-exempts `PSA_LEVEL_*` so the new var would never be required in `.env.example` — a trap `07-install-argocd.sh:70-73` already documents. ✅ **C2 DONE (#352) and C4 DONE (#353) — B51 is CLOSED.** **C2**: `NS_SPEC` rows are now `ns|level|ownership`, `want` is `count(ours)` and the `+2` magic constant is gone. Two idea-round CRITICALs were defects I would have shipped: a **static `mesh` would have DISARMED A GUARD THAT FIRES TODAY** (in the default `istio` mode WE install the mesh, so those namespaces ARE ours and a too-strict level must go red — only `istio-existing` makes them the platform's, so the column removes the hardcoded NAMES from the `case` but the MODE TEST STAYS); and the `kind-only` harbor row would have **MANUFACTURED A FALSE "PSA OK"**, because `measured` is incremented before `ours` is known, so an honest "measured NOTHING" would have become "PSA OK (1 measured)" over the one namespace the column declares NOT ours — now `measured_ours`, **and the verdict gates on it** (my first pass only REPORTED it while the condition still tested `measured`: a number that gates nothing). Also: a 3-field row silently **mis-parses** under a 2-variable `read` (the last var absorbs the rest WITH delimiters), and the column is an operator-typed **escape hatch** — a typo silently un-gates a namespace — so a reconciliation guard requires `ours + mesh + kind-only == total`. **C4**: a THIRD assertion deriving namespace names from the `ensure_namespace` CALL SITES — the only ground truth independent of both hand-typed inventories, which is what makes a namespace missing from BOTH visible. **Divergence proven in both directions** (a namespace in neither inventory → C4 flags it, OWNED blind; deleting the gitea call → OWNED flags it, C4 blind because `lib/istio.sh` still calls it), so it is additive and `OWNED` stays hand-typed rather than derived-and-tautological. Its implementation round found six more, including a **MEASURED silent exemption** (the derived name was interpolated into an ERE, so `"${SNEAKY}|a"` ALTERNATED and matched an exempt entry — green, 6 exempt against a 5-entry list) and a **header that stated the OPPOSITE of the finding beneath it**, telling the operator to add a call that already exists. ✅ **RESIDUAL SETTLED 2026-07-19 on a live cluster — C2 is now proven END-TO-END, not at logic level.** `measured_ours`: harbor (8 pods, `kind-only`) is correctly EXCLUDED and the summary reads `measured 2 namespace(s) with running pods (1 of them ours)`, with harbor's verdict rendering as **"KinD stand-in only (needs restricted) — a Supervisor Service on a real lab, not ours"**. The **mode-dependent mesh is proven in BOTH directions**: in the default `istio` mode `istio-system` gets a GATING verdict (`UNLABELLED -> VKS default 'restricted' REJECTS these pods`), and with the controller set to `istio-existing` the SAME row flips to `platform-owned (needs baseline) — informational, not ours to label`. ⚠️ **A METHOD NOTE THAT COST ME THE FIRST ATTEMPT, for the third time today:** `INGRESS_CONTROLLER=istio-existing make psa-check` does **NOT** flip it — `load_env` sources `.env.state` AFTER the environment, so the PUBLISHED value wins (the state-overlay clobber). Correct behaviour for the gate, wrong test method; the proof has to rewrite the published value. Also note the run's `platform`/`install-ingress` steps FAILED, and that was MY setup — I skipped `make mirror`, so Harbor was empty and gitea sat in `ImagePullBackOff` (verified, not assumed). The original residual note follows: `49-psa-check.sh` cannot run without a cluster, so C2's ownership/`measured_ours` logic was proven at LOGIC level only. Settle with `make kind-up && make psa-check` before and after `make platform`, plus `INGRESS_CONTROLLER=istio-existing make psa-check` to watch the mesh rows flip between gated and informational. The original design follows: 🟢 **THE DESIGNED REMEDIATION (adversary-vetted, now BUILT):** **C2** add an ownership column (`ns\|level\|ownership`, `ours\|mesh\|kind-only`) routing platform-owned namespaces through the EXISTING `ours=0` informational branch — this also deletes the `+2` magic constant; **C3** add `tekton-pipelines-resolvers` as `ours` with a **literal-matching** `OWNED` form (do NOT invent `TEKTON_RESOLVERS_NAMESPACE` to satisfy a regex — that would need four coordinated edits including a call-site rewrite); **C4** add the derived call-site cross-check as a **THIRD** assertion, never a replacement — deriving `OWNED` makes the gate tautological and would un-catch the very F2 regression it was written for, and only **12 of 19** call sites are statically resolvable (7 are loop/local vars, two of them ephemeral e2e fixtures that must never enter `NS_SPEC`), so it must PRINT the 7 it cannot resolve with a reason each. ✅ **C3 DONE 2026-07-19 (#350), measured safe FIRST**: `tekton-pipelines-resolvers` measured `pods=1, current=restricted, psa_min_level=restricted` — ALREADY at its true minimum, so adding it was a pure measurement gain and the predicted red did NOT apply. It went in as a **LITERAL** row (the call site at `41:90` passes a literal, and inventing `TEKTON_RESOLVERS_NAMESPACE` to satisfy a regex would have meant four coordinated edits including a call-site rewrite); the matcher now accepts either shape, discriminated by form alone — a DNS-1123 namespace can never look like an UPPER_SNAKE var. Only possible because #347 taught the counter to see a literal row. `psa-check` now measures **6** namespaces, up from 5. RED-proven by commenting out the call. ⚠️ **A MEASUREMENT THAT CORRECTS THIS ROW:** harbor measures **restricted-CLEAN** on this chart version (`pods=8, UNLABELLED, min=restricted`), so the adversary's premise that "both charts are documented as not restricted-clean" is **FALSE for harbor here** — its `psa-check`-goes-red argument does not hold as stated. **C2 is still the right design anyway**, for a different reason: it gets harbor MEASURED without LABELLING it, and labelling it `restricted` would reject any future harbor pod that is not restricted-clean (a runtime behaviour change on a chart we do not control). `argocd` remains UNMEASURED — it is not installed in this flow. ⚠️ **The original C3 gating note, kept for the arc**: C3 was gated on one live-cluster measurement: `make kind-up && make platform`, then `kubectl label --dry-run=server ns tekton-pipelines-resolvers pod-security.kubernetes.io/enforce=restricted`. Expect `preflight` may go red once — that red is a TRUE finding about an already-shipped label, and the fix is the level, not backing out the row. |
| **B53** | ✅ **REFUTED AND CLOSED 2026-07-21 — do NOT declare `check-grep-q-pipe` in `test-gate-vacuity`; it is NOT STARVABLE, and one natural declaration BANKS A FALSE PASS.** The row's premise ("it was arguably too slow to declare before; at 1s that objection is gone") was wrong: **speed was never the blocker**. The gate's corpus contains its OWN source AND `lib/os.sh` (which it sources), so no pathspec starves a meaningful corpus while leaving it able to run. All three options MEASURED: **(1)** preserve gate+lib → `OK — scanned 125`, rc=0 → **false RED** (12 real files survive, so the green is a true statement about a corpus that is not empty); **(2)** `:(exclude)…check-grep-q-pipe.sh` → rc=1 with **67 bytes**, `REPO_ROOT: unbound variable` → `assert_starved` scores it **ok** — **a FALSE PASS certifying a gate that died sourcing an emptied lib having judged nothing**, and it is the option a future session will reach for; **(3)** starve everything → rc=0, 0 bytes. ⚠️ **A correction to this row's own earlier text:** option (3) was described as "the documented INCONCLUSIVE case" — it was **not**. `assert_starved` tested `rc -eq 0` *before* the zero-bytes clause, making that clause **unreachable for rc=0**, so a silent death was labelled **VACUOUS**, sending the reader to fix a gate that never ran. **What shipped instead:** the ordering bug fixed (silence is now tested first, whatever the exit code); the gate moved into the header's NOT STARVABLE list carrying all three measured outcomes; the harness output **split** into "NOT STARVABLE (reasoned)" vs "UNDECLARED (unaudited)" — it had been printing four reasoned gates under *"UNAUDITED, NOT proven healthy"*, a false claim about all four — plus a rot guard (RED-proven) that fails if a `NOT_STARVABLE` name stops being a real gate. Undeclared is now **none**: 19 of 23 starved, 4 reasoned. **Also fixed, and the reason the row is not simply deleted:** the gate's denominator comment claimed a guard it did not have — *"zero-guard on the ITEM count, not the file count"* while guarding `scanned`, a FILE count, so 113 of 125 files could be emptied and it still reported `OK — scanned 125`. An `items` guard now exists, but ⚠️ **it does NOT close corpus vacuity and cannot be made to** (its own source supplies items; measured floor **372** under maximal starvation — an items-based vacuity guard is unfireable by construction). It guards **TOTAL FOLD DEATH ONLY**: with `sed` shimmed to exit 127 the gate previously reported `scanned 125, items 0, rc=0` — green over zero examined lines. ⚠️ **That RED-proof is now STALE and the sed case is caught EARLIER**, by the fourth positive control's emptiness arm — a later change preempted the guard it was written for, which is why the control's message names sed explicitly instead of blaming the expression. `items` also does **not** prove the loop READ THE FILES: with `"$@"` dropped from `_fold` it read the heredoc's FILE LIST and reported `scanned 1, items 125` while missing a planted violation, with all four controls green. Corpus also widened `scripts/*.sh` → `*.sh` (125→126), picking up `bootstrap-jumpbox.sh`, which `lint` lints and which carries `set -euo pipefail`. |
| **B52** | ✅ **CLOSED 2026-07-19 — the sweep was REFUTED BY MEASUREMENT, and it was aimed BACKWARDS. Do NOT reopen it as a conversion sweep.** The row proposed converting ~70 bounded `printf '%s' "$var" \| grep -q` sites. An idea-round adversary refuted BOTH halves of its premise, and I re-measured every number independently. **(a) "not safe in principle" is FALSE — the safety boundary is STRUCTURAL, not a hope.** Below the 64KB pipe buffer the producer's write COMPLETES and SIGPIPE cannot occur (**4KB: 0/120**); above it, it always does (**82KB multiline: 120/120**); the transition is narrow (41KB: 4/120, 61KB: 3/120). **And data with NO NEWLINES is immune at ANY size — 3MB single-line: 0/120** — because grep buffers hunting a line terminator and thereby drains the producer. Risk therefore needs **>32KB AND multiline**, and **not one** `printf` site qualifies. **(b) "the remaining are bounded printf sites" was ALSO false**, and this is the part that mattered: **six were EXTERNAL producers whose size we do not control** — `kubectl get deploy -A -o name \| grep -q .`, `docker ps -a --format \| grep -qxF`, `kind get clusters \| grep -qxF` — and the gate waved every one through, because none is a file reader. **The sweep would have churned 30 files to remove a structurally-zero risk while leaving the only real ones untouched.** ⚠️ **The `<<<` conversion is itself a hazard, which is the second reason not to sweep:** `printf '%s' "$v"` on an EMPTY value emits zero bytes (rc=1) while `<<< "$v"` emits ONE EMPTY LINE, so every nullable pattern (`''`, `^$`, `.*`, `^`, `$`, `x*`) flips rc 1→0. Several of those sites are `if`-conditions in TESTS, where that flip is a silent verdict change — a false green in a test. (`printf '%s\n'` is identical to `<<<`, so those few are free; the DRAIN form `\| grep PAT >/dev/null` has no empty-var divergence at all and is what shipped.) **WHAT SHIPPED INSTEAD:** the six unbounded sites converted to the drain form, and `check-grep-q-pipe` widened with an `_EXTERNAL='kubectl\|docker\|kind\|helm\|crane\|curl\|argocd\|tkn\|podman'` producer class (RED-proven in three directions: a kubectl form caught, a docker form caught, a bounded printf form still ALLOWED — no false positive). Their POLARITY is why it was worth doing: `48-istio-preflight.sh:39` inverts with `!`, so a SIGPIPE there reports **"no istiod"** when istiod EXISTS; `05-kind-up.sh:55` reports **"cluster absent"** and re-creates it; `kind-down.sh:23` **skips the sidecar cleanup**. ⚠️ ✅ **Residual SETTLED 2026-07-20 on a live, fully-loaded cluster** (argocd + harbor + gitea + tekton + istio + both apps): `kubectl get deploy -A -o name` = **941 bytes / 27 deployments**; `docker ps -a --format '{{.Names}}'` = **174 bytes**. Both are **nowhere near** the measured 32KB risk threshold — ~35 B per deployment, so you would need roughly **900 deployments** to reach it. So those six conversions were **DEFENSIVE, not urgent**, and nobody should cite this row as evidence that a SIGPIPE was actually occurring here. The polarity argument for making them still stands unchanged (a SIGPIPE at `48-istio-preflight.sh:39` inverts `!` and reports "no istiod" when istiod EXISTS), and the external-producer class remains the right thing to gate — it is only the urgency that was overstated. The fix is cheap and correct regardless. |
| **B90** | ⚠️ **ROUND VERDICT 2026-08-18: DO NOT BUILD THE BARRIER — it ALREADY EXISTS, on the BOX, and is STRICTLY STRONGER.** `scripts/04-harbor-reachable.sh` + `lib/harbor.sh:338-371` runs INSIDE the walk and does `getent hosts` **from the box** — the vantage this row says nothing checks — waits 900s polling 15s, and `exit 1`s on timeout. `lib/harbor.sh:334`'s comment names this defect verbatim. **It FIRED in run 6 row 1** (line 761 prints only when the state is not-serving; 764 converged). It is stronger on the axis this corpus cares about most: a box==host barrier compares a **proxy** and goes green when both are stale together; this measures whether the address actually **SERVES**. **And the proposed barrier has no defined placement**: rows 1 and 3 are NOTHING-exists, so Harbor is installed BY the walk and at barrier time there is no LB IP and no A record to verify; rows 2/4 already resolve. Only `dns-r1.log`/`dns-r3.log` exist at all — four of six rows never publish. Were it built anyway it would fail OPEN three ways, and at rows 1/3 its only correct behaviour IS to pass — a vacuous gate. **Two of this row's own remedies are also refuted:** *'flush its resolver'* is dead (MEASURED today on the current cut: `dig @192.168.100.1` returns **TTL 0** — not cacheable, no staleness window to flush), and *pinning the LB IP* must not be done (the Service is `svc-harbor-q2z8m/harbor-nginx`, VMware-managed; `grep loadBalancerIP 06-install-harbor.sh` has no hits; pinning trades an intermittent stale record for a hard outage on pool collision). **The counterexample is stronger than this row states, and it is INSIDE run 6**: `dns-r1.log` published `.130` at 07:06, `REBUILD2.log` finished a full destroy+rebuild at 09:08, and `dns-r3.log` published **`.130` again** at 09:17 — so run 6's cut B did not exercise the hazard either. **THE ONE THING WORTH BUILDING** (nvl, one line, gates nothing): `dns-record.sh:22`'s `_log` carries NO TIMESTAMP while the walk log is timestamped to the second, so this row's surviving time-of-comparison hypothesis is currently **untestable**. Add a UTC stamp — a printer, not a gate. Also latent: `dns-record.sh:52` swallows the DELETE (`|| true`) while only the ADD dies, and its own comment says duplicates round-robin between a live and a dead IP. **BOTH edit a file the live matrix is invoking (rule H.1) — land only after `ps -p <pid>` returns nothing.** The original report follows. 🔴 **The walk box resolved a STALE Harbor A record that the lab's DNS attendant had ALREADY fixed — "verified" is verified from the wrong vantage.** MEASURED 2026-08-12, row 1: `/tmp/walk/dns-r1.log` ends `nested-mgmt: harbor.env1.lab.test -> 192.168.101.139` / `supwl: … -> .139` / `verified: … resolves to 192.168.101.139`, while the walk box at `04:29:29Z` resolved **192.168.101.136** (block `[11]`) and was still on `.136` at block `[13]` (135 s, `000` — see B89). ⚠️ **CORRECTED 2026-08-17 — this row described the vantage WRONGLY, and the correction matters.** It said the attendant "verifies from its own vantage (nested-mgmt + supwl)". Read `nested-vsphere-lab/scripts/dns-record.sh`: `:57` prints `${net}: ${NAME} -> ${IP}` **once per network** — those are the two PUBLISH lines, not verifications — and `:62-66` verifies exactly once, with `getent hosts` **from the HOST running the harness**. So there are THREE vantages, not two: the two dnsmasq instances it writes to, the HOST it reads back from, and the WALK BOX, which nothing checks. Getting this wrong points a fix at the networks when the gap is host-vs-walkbox. **PARTIALLY ADDRESSED 2026-08-17 (nvl):** the message now says `verified FROM THIS HOST: … (a walk box resolves separately and is NOT checked here — B90)`. That is a false-confidence fix only — it removes the misreading that cost row 1, and changes no behaviour. The row STAYS OPEN for the real fix. The record moved `.130 → .133 → .134 → .135 → .136 → .139` across reinstalls — every Harbor reinstall mints a new LB IP — so this is **structural, not incidental**. ⚠️ The mechanism (resolver cache on the box vs. update-after-query ordering) is **NOT established** — do not claim which until it is measured. **Fix lands in `nested-vsphere-lab`'s harness** (verify the record FROM the walkbox, and/or flush its resolver, before letting the walk proceed), not here. ⚠️ **SHARPENED 2026-08-17 — the attendant is STRUCTURALLY INCAPABLE of noticing this, and rows 5/6 have NO attendant at all.** Read `walk-matrix.sh:559-575`: `dns_attendant` is a LOOP (20 s poll, `WALK_DNS_ATTEND_SECONDS` 5400 s), so my first reading — *the box is built at `:626` before the attendant is launched at `:644`* — is TRUE but NOT the cause; the loop would fix a late publish within 20 s. The cause is its TRIGGER: `:567` reads `cur` with `getent hosts` **on the HOST**, and it republishes only `if [ "$cur" != "$ip" ]`. So in the exact state B90 measured — HOST already correct at `.139`, BOX stale at `.136` — the condition is FALSE, the attendant does nothing, and its own log line is true of the host and false of the box. It cannot see the vantage that matters, by construction. **AND `:643-648` disables it entirely for scenario-2** (a deliberate, correct choice: publishing an A record is an ADMIN act, and a tenant row that silently fixed the shared Harbor's DNS could never discover it was broken) — so for **rows 5 and 6 a stale box resolution is wholly unmitigated.** ⚠️ **NOT FIXED BEFORE RUN 4, on purpose:** changing DNS/attendant behaviour immediately before a six-row certification run risks all six rows to fix one, and the standing bar is green FROM THE FIRST GO. Watch for it instead — a row failing against a Harbor IP that the host resolves correctly is this bug. 🔬 **MECHANISM NARROWED 2026-08-17 — the RESOLVER-CACHE candidate is REFUTED for this lab, which changes the fix from a FLUSH to a BARRIER.** This row forbade claiming a mechanism until measured; here is the measurement, taken on cut B from all three vantages. **The walk box does run a caching stub** — `systemd-resolved` **active** on `nameserver 127.0.0.53`, `nsswitch hosts: files dns`, and **0** `/etc/hosts` entries for the name, so resolution is pure DNS through a cache-capable resolver. That is what makes the cache hypothesis plausible. **But the record is served with `TTL 0`** — measured three ways: through the stub, and directly against BOTH lab resolvers (`dig @192.168.100.1` and `@192.168.101.1` each return `harbor.env1.lab.test. 0 IN A 192.168.101.130`). A TTL-0 answer is **not cacheable**, so there is no staleness window on the walk box to flush — and the remedy this row floated first (*flush its resolver*) would fix nothing. ⇒ the surviving candidate is the other one it named, **update-after-query ordering**: the walk resolved before the attendant's write had taken effect. That implies the fix is a **BARRIER** — verify the record FROM the walk box (poll until it matches the expected IP) before letting the walk proceed — which is the other half of what this row proposed, and is the half that actually applies. ⚠️ **LIMITS, stated rather than glossed:** (1) all three vantages AGREED at measurement time (host `.130`, walk box `.130`, ground truth `.130` — `https://192.168.101.130/` answers 200 with title `Harbor`, while `.136` and `.139` from the original incident are both dead), so this measures the MECHANISM's preconditions, not a reproduction of the divergence; (2) `TTL 0` is what dnsmasq serves **on cut B today** — the incident was 2026-08-12 on a different cut, and I did NOT verify the TTL was 0 then, so a cache could in principle have applied to that cut; (3) `resolvectl statistics` printed nothing readable here, so the *no cache hits* arm is **INCONCLUSIVE** — the refutation rests on the TTL, which is a mechanism argument, not on observed counters. Anyone implementing the barrier should re-measure the TTL first: if a future dnsmasq config serves a non-zero TTL, the cache candidate returns and the fix needs BOTH halves. 🟢 **TTL RE-MEASURED 2026-08-17 on a FRESH cut, which is the check this row's own tail demands before anyone implements the barrier.** The tail says: *"re-measure the TTL first: if a future dnsmasq config serves a non-zero TTL, the cache candidate returns and the fix needs BOTH halves."* Measured against the lab's own resolver while the new lab was building: `dig +noall +answer @192.168.100.1 esxi01.env1.lab.test` → `esxi01.env1.lab.test. **0** IN A 192.168.100.51`. **TTL is ZERO**, and it is zero by DEFAULT rather than by configuration — grepping all three libvirt dnsmasq conf files (`default.conf`, `nested-mgmt.conf`, `supwl.conf`) for `local-ttl=`/`max-ttl=`/`no-negcache` returns **nothing**, so dnsmasq's built-in `local-ttl` of 0 is what is in force. ⇒ **the cache candidate does NOT return**, and the refutation's TTL mechanism argument now rests on a measurement of THIS cut rather than on inference. ⚠️ Two scope limits, stated rather than hidden: (1) this is the DEFAULT, so it holds only until someone adds an explicit `local-ttl` — the row's conditional is still the right thing to re-check on any dnsmasq config change, and this measurement is the baseline to compare against; (2) `harbor.env1.lab.test` returned **no answer** at measurement time, which is EXPECTED and not a finding — the lab was mid-build and Harbor is not installed until later, so do not read that as a DNS defect. The `resolvectl statistics` arm the row calls INCONCLUSIVE is unchanged; nothing here addresses it. 🔬 **THREE VANTAGES MEASURED LIVE 2026-08-18, on the row-6 walk box that is STILL RUNNING (`vks-walkbox-ubuntu`, `192.168.100.217`) — plus ONE NEW FACT that changes what any fix must verify against.** All three agree at `192.168.101.130`: HOST `getent`, both lab resolvers (`dig @192.168.100.1` and `@192.168.101.1`), **and the walk box itself** — the vantage nothing checks. The box's preconditions are re-confirmed on a live box rather than inferred: `systemd-resolved` **active** on `nameserver 127.0.0.53`, **ZERO** `/etc/hosts` entries for the name, and `Cache=` left commented in `resolved.conf` (so the default applies, and TTL 0 stays uncacheable). ⇒ **no divergence to reproduce on this cut**, which is a limit on what follows, not a refutation of the row. 🆕 **THE NEW FACT, and it is load-bearing for the barrier: the walk box is DUAL-HOMED but resolves through exactly ONE upstream.** `ip -4 -o addr` shows `enp1s0 192.168.100.217/24` AND `enp3s0 192.168.101.64/24`, yet `resolvectl status` configures DNS on **link 2 only** — `Current DNS Server: 192.168.100.1`, with no DNS Servers on link 3. So the box's answer comes from **nested-mgmt alone**, even though the harness publishes to both networks. Consequence: a barrier must verify **what the BOX queries**, and a publish that landed on only the *supwl* network would be **invisible to the box** while looking correct from the host. Nothing in the row or the harness accounted for that. 🔬 **A THIRD MECHANISM CANDIDATE — reachable by construction, measured NOT ACTIVE, and it fits the SYMPTOM better than either candidate the row names.** `nvl/scripts/dns-record.sh` publishes as **delete-then-add** per network: the DELETE is `virsh net-update … delete dns-host … --live --config >/dev/null 2>&1 **|| true**` (failure SWALLOWED) and only the ADD `_die`s. Its own comment says what that costs: *"dnsmasq would otherwise serve both, round-robin, and half your requests would reach a dead IP — an intermittent failure far more expensive than a missing record."* **A round-robin between an old and a new A record is exactly the measured symptom** (box `.136` while host `.139`), which neither a cache (refuted, TTL 0) nor update-after-query ordering explains — ordering produces a **missing** record in the delete/add window, not a **stale** one. **MEASURED TODAY, and it does NOT reproduce:** each of `nested-mgmt` and `supwl` carries **exactly ONE** `harbor.env1.lab.test` entry, both `192.168.101.130`, and **12 of 12** queries to the box's only upstream returned the same address. ⇒ record it as a live *possibility* created by the swallowed delete, **not** as the explanation — and note the cheap hardening it implies regardless: stop swallowing that delete, or assert **exactly one** record per network after publishing. ⚠️ **STILL UNMEASURED, and I deliberately did NOT measure it: the DIVERGENCE DURATION** — the bound any barrier needs. Measuring it means publishing a record and timing box visibility, i.e. **mutating the operator's LIVE lab DNS**, which is a hard-to-reverse admin act on their infrastructure and not mine to take unprompted. A throwaway name under the lab domain would confine the blast radius; that is the experiment to run **with the owner's go-ahead**, and until then the only datum remains "exceeded 135 s once". ⚠️ **Practical note for whoever implements it:** the box is reachable from the host with the harness's own key at `/var/tmp/vks-walkbox/id_ed25519` (`WALKBOX_SSH_KEY`, default `${WALKBOX_DIR}/id_ed25519` per `walkbox-vm.sh:39`) — so a barrier needs no new credential. Build nothing until B90's idea round clears the design; the barrier is a NEW CONTROL. 🟢 **DIVERGENCE DURATION MEASURED 2026-08-18 WITH THE OWNER'S EXPLICIT GO-AHEAD — the precise gap this row's tail names as unmeasured — AND IT REFUTES THE BARRIER, i.e. the fix this row has been proposing since it was filed.** The tail says the only datum is *"exceeded 135 s once"* and that measuring the bound requires publishing a record on the live lab. Permission was asked for and granted, so here is the bound. **Method, blast-radius-confined:** a THROWAWAY name (`b90probe.env1.lab.test`) under the lab's own domain — never `harbor.env1.lab.test`; `dns-record.sh` itself refuses any name outside the lab domain (it reads the domain from the nested-mgmt network XML), so a typo cannot escape — published, then **CHANGED to a second address**, timing when the walk box first saw each. Deleted in an `EXIT` trap with **0 residual entries verified on both networks**. **Result: first publish → box in 201 ms; UPDATE to a different address → box in 207 ms.** The second number is the one this row needed, because the measured incident was a STALE value (not a missing one), so the question was never "how fast does a NEW name appear" but "how fast does a CHANGED name converge". ⇒ **convergence is ~200 ms against an incident window of ≥135 000 ms — a factor of ~650.** Update-after-query ordering **cannot** produce that, so the row's last surviving candidate is refuted alongside the cache (TTL 0) and the duplicate-record path. ⇒ **DO NOT BUILD THE BARRIER.** A poll-until-match barrier against 200 ms convergence would pass on its first probe essentially always — a **vacuous gate** by construction, whose green would license exactly the trust this row exists to withhold. Same shape as the refuted render-and-assert gate in B26: the invariant holds, and a gate built on it measures nothing. ⇒ **Phase 3 also re-confirms the duplicate-record refutation under the conditions that would provoke it** — after a genuine *update* (delete-then-add on both networks) each network carried **exactly one** entry and **12 of 12** queries to the box's only upstream returned the new address; so the swallowed `|| true` on the delete is a latent hazard, not an active one, and the cheap hardening this row already proposes for it still stands on its own merits. ⚠️ **WHAT THIS LEAVES OPEN, stated rather than glossed: the 2026-08-12 incident is now UNEXPLAINED.** Three candidates are refuted (cache, duplicate records, update ordering) and no remaining mechanism has been measured. The most economical account, and it is a **HYPOTHESIS** I cannot prove retroactively, is that it was never a *vantage* bug at all but a **time-of-comparison artifact**: the box read `.136` when `.136` was genuinely the record, the attendant later moved it to `.139`, and the two log lines being compared were written at different moments. That would make this row's founding framing ("verified from the wrong vantage") wrong in KIND, not merely in detail — which is why the row is not being closed on a refutation of its remedy. **Testing it costs nothing on the next run:** log the box's resolution and the host's resolution **with timestamps, from the same instant**, once per row; if they never diverge at a common instant, the vantage framing is dead and the row closes. That is a LOGGING addition, not a gate — and it is the honest next step instead of a barrier. | 🟢 **MECHANISM NARROWED 2026-08-18 by run 8 — and the narrowing is that a CLEAN RUN IS NOT EVIDENCE.** Cut A (rows 1, 2 and 5) shows **ZERO divergence**: the attendant published `harbor.env1.lab.test -> 192.168.101.130` and all THREE walk boxes resolved `.130`. But the reason is not that anything was fixed — MEASURED, the record published **ONE distinct IP across 4 publish lines** and never moved. **The hazard REQUIRES the record to CHANGE inside the window; a single stable publish cannot diverge.** So cut A did not exercise it, and reading its green as a fix would be the one-operating-point error this corpus keeps recording. ⇒ the mechanism is narrower than "resolver cache vs update-after-query ordering" in the abstract: it needs an actual IP MOVE concurrent with a walk, which is what "every Harbor reinstall mints a new LB IP" supplies. **FALSIFIABLE CHECK AVAILABLE ON THE NEXT CUT-B ROWS — with its own premise stated honestly:** cut B reinstalls Harbor, which USUALLY mints a new LB IP, but this row already records a counterexample (a full destroy+rebuild that REUSED the same IP), so the record moving is likely, not certain. Compare the attendant's published IP against what rows 3/4/6 resolve, and FIRST check whether it moved at all — if it stayed `.130`, cut B did not exercise the hazard either and the run says nothing about it, exactly as cut A did not. If they agree, the ordering is benign for this cadence; if they diverge, that is the same defect with a second, fully-logged datapoint. ⚠️ Note the surviving `run-*/dns-r1.log` files all show ONE distinct IP, so the 2026-08-12 evidence exists only in this row — a second cost of the flat log layout the per-run directory fixes.
| **B91** | **A doc-acknowledged BENIGN non-zero still scores the walk block RED, because the prose that excuses it is not machine-readable.** `docs/scenario-1.md:252-253` says `vcf context use` *"can print a 'system Harbor registry' error and still have worked — judge it by the next command, not its exit code."* MEASURED row 1 block `[08]`: exactly that — `[x] : failed to discover plugin sources from the system Harbor registry` with rc=1, while `[ok] Successfully activated context 'vks-cicd:cicd'` printed and the next block `[09]` ran clean. The walk records `--- [08] rc=1` with **no** `expect:` line, because the doc's `:254` **Expect:** is a file-existence claim (`./secrets/supervisor.kubeconfig` exists), not an output string the harness can match — so the run's red-block count over-reports. **Owner call between two shapes:** give the block a machine-readable expectation the walk can match, or teach the harness that a block whose doc says *"judge it by the next command"* is judged by the next block. Do NOT simply drop the prose — it is correct and a reader needs it. | ⚠️ **DID NOT FIRE in the 4/4 green acceptance run (2026-08-13).** The `system Harbor registry` error printed **6 times in every row**, yet block `[08]` was **rc=0** and no block in any row was non-zero — so `vcf context use` returns 0 *while printing it*, intermittently. The row is LATENT, not currently broken, and that is why the fix is being held: scenario-1 has just been certified 4/4, and editing a block for a condition that is not firing is the shape that breaks it. Settle by capturing a run where it DOES return non-zero, then make the block's rc honest (end it with the check the prose already says decides it) rather than teaching the walker a new annotation. ⚠️ **DID NOT FIRE AGAIN — third independent run, 2026-08-18** (certifying matrix `run-20260818T053556Z-2571541`). The `system Harbor registry` error printed **6 times in EACH of rows 1 and 2** — the same count as the 2026-08-13 acceptance run, on both OSes — and block `[08]` was **rc=0 in both** (ubuntu 20s, photon 1s), with **zero** non-zero `vcf context use` statements anywhere. So the tally is now **3 runs / 10+ rows / ~60 prints / 0 captures**. That is no longer "intermittent, we happened not to see it": it is evidence that `vcf context use` returns 0 while printing this error, and that the rc=1 recorded once in an earlier row had a DIFFERENT cause which this row has never isolated. **Do not implement either shape until that cause is named** — a fix aimed at the wrong producer would edit a block that is passing, during a certification, which is precisely the hazard the hold was written to avoid. The cheap next step is not a fix but a discriminator: when a capture finally appears, diff its `vcf` invocation and plugin state against a rc=0 run rather than assuming it is the same condition the prose describes. ⚠️ **FOURTH INDEPENDENT RUN, 2026-08-18 (run 7b) — DID NOT FIRE AGAIN, and the shape is now DETERMINISTIC rather than intermittent.** Rows 1 (ubuntu) and 2 (photon) each printed the `system Harbor registry` error **exactly 6 times** — the SAME count as the 2026-08-13 acceptance run and the 2026-08-18 `run-20260818T053556Z-2571541` matrix, on both OSes — and block `[08]` was **rc=0 in both**. Zero non-zero `vcf context use` statements anywhere in the run. (Row 5 is scenario-2 and prints it 0 times, as expected — different document.) **TALLY: 4 runs / 13+ rows / ~72 prints / 0 captures.** **WHY THE NEW DATA POINT CHANGES THE READING.** The row already said *"no longer intermittent, we happened not to see it"*. Four runs at an IDENTICAL count of 6, across two operating systems, is stronger than that: it is not a flaky condition sampled four times, it is a DETERMINISTIC behaviour — `vcf context use` prints this error exactly six times and returns **0**. A genuinely intermittent producer would vary the count. So the single historical `rc=1` is now very unlikely to be this producer at all, which is precisely what the row suspects and has never isolated. **THE HOLD STANDS AND IS STRENGTHENED:** do not implement either shape. The prose at `scenario-1.md:252-253` remains correct advice for a reader, and the block it describes has been rc=0 for four consecutive runs — editing it would change a passing block for a condition that has never been captured. **The discriminator to run WHEN a capture finally appears is unchanged**, and now has a sharper baseline to diff against: a healthy run prints it 6 times with rc=0, so compare the capture's print COUNT as well as its `vcf` invocation and plugin state. A capture that also prints 6 is the same producer behaving differently; a capture that prints some other number is a different producer, and the row's prose is not about it. |
**Needs a real lab or a heavy run:**

| ID | Item |
|----|------|
| **B54** | ✅ **CLOSED 2026-08-16 — DECIDED, NO ACTION. Nothing to build; this row is a decision record and was only ever open because it carried no closure marker.** The idea-round answered both of its own questions (q1 NO, q2 WORSE) and the verdict is DON'T BUILD for both scenarios. Re-opening needs a NEW fact — specifically N≥2 clusters, which is the condition the round says the ApplicationSet cluster-generator needs to earn its keep. Evidence kept below. 🔵 **IDEA-ROUND DONE 2026-07-23 (`vks-adversary`, primary-sourced) → DON'T BUILD (both scenarios).** The ArgoCD `ApplicationSet` cluster-generator earns its keep at **N≥2 clusters**; we have ONE, so at N=1 it delivers ~zero AND **discards the accreted `70`/`71` safety net** — refuse-to-guess among registered clusters, exact-match destination, refuse an in-cluster destination when ArgoCD is off-cluster (`70`'s CRITICAL #158/#160) — which a `matchLabels:{type:vks}` + `prune/selfHeal` ApplicationSet cannot express; on a SHARED lab ArgoCD that is exactly the cross-tenant mis-deploy the imperative path was rewritten to prevent. **q1** (beats `make gitops`?): NO, net loss — a guest re-provision forces re-running `71` regardless, and the "add-app→reconciles" benefit is a **list/git** generator's (needs a new Gitea repo the controller polls → re-incurs the `GITEA_ARGOCD_URL` reachability problem). **q2** (Scenario-2 RBAC): WORSE — `applicationsets` is project-scoped in `argocd-rbac-cm` BUT ArgoCD's Security doc says **creation is admin-only** and templating can ESCAPE the tenant's AppProject unless `project:` is hard-coded; it degrades the `argocd app create` self-service the tenant already has. **q3** (rejected credential?): NO — the cluster-generator reads only name/server/labels; the sync credential stays `71`'s durable bearer token, no #13175. **Correction:** the row's "real-lab only, no KinD" was wrong for the MECHANIC — `e2e-cross-cluster.sh` already validates the identical Supervisor→guest topology offline; only the Scenario-2 TENANCY is real-lab-only. **If the owner ever overrides (Scenario-1 only):** use a **list generator** with `project:` hard-coded + destination pinned + re-implement `70`'s ambiguity guard — NOT the cluster generator; and settle two lab questions (does the VKS-package ArgoCD expose `argocd appset create`; does a tenant's AppProject role include `applicationsets`). ✅ **RE-CONFIRMED CLOSED 2026-08-17 by an idea round, and it corrected the READING RULE.** The round was briefed with this repo's heuristic *"a row's verdict lives at its TAIL"* and **refuted it for this row**: B54's closure marker is at the **HEAD**, and its tail is a conditional hypothetical guarded by *"If the owner ever overrides"* which, read alone, looks exactly like a work order — **it recruited a whole session that way.** The rule is therefore **"read the WHOLE row and find the MARKER"**, not "read the tail". **TWO OF THE FOUR QUESTION-HALVES ARE NOW SETTLED WITHOUT A LAB:** q-lab-1's **CLI** half — MEASURED, the VCF build `v3.0.19+d67e6eb90-vcf` ships `argocd appset create/delete/generate/get/list`; q-lab-2's **schema** half — settled from ArgoCD SOURCE in-tree (`docs/reviews/2026-07-14-vks-deep-research.md:161` quoting `types.go:~2748`): `applicationsets` **IS** grantable in an AppProject role. **Only "is an applicationset-CONTROLLER actually running on the Supervisor" is genuinely lab-only** — and `70-configure-argocd.sh:199-206` already teaches the trap that decides it: *"THE CRD IS NOT THE CONTROLLER"*. **A NEW MEASUREMENT TIGHTENS THE VERDICT:** the guest cluster carries **80 CRDs, ZERO argoproj/applicationset** — so there is no "put the ApplicationSet beside the workload" option; it could only live on the Supervisor in the shared `cicd` namespace, which is exactly the multi-tenant blast radius this row rejected. The row reached the right answer without that fact. **THE TAIL'S DESIGN IS ALSO WRONG WHERE IT IS CONCRETE:** hard-coding `project:` contradicts the shipped manifest's own written rationale (`k8s/argocd/application.yaml:24-26`: *"a TENANT is given their own AppProject … So this is a variable, not a literal"*) — inert in Scenario 1 (it already resolves to `default`) and REJECTED under a tenant AppProject. And the row costed creation but never **teardown**: `98-uninstall-all.sh` has **zero** ApplicationSet handling (measured), so teardown would delete the generated Applications and the controller would **recreate them** while `make uninstall-all` exits 0 over the resurrection. ⚠️ **PSA is NOT an axis here** — an ApplicationSet is a control-plane CR that schedules no pods; the round was invited to attack under PSA and **declined on the evidence** rather than manufacture a plausible hazard. **REDUCTION (still open as an edit):** keep the marker, the verdict and the re-open condition (**N>=2 clusters**); fold in the two settled halves so nobody re-asks; and DEMOTE the list-generator design sketch — it is unreviewed, contradicted by `application.yaml:24-26`, untested against teardown, and as a trailing sentence in a closed row it reads as a plan. |
| **B2** | **The Gateway-API CRD version, not its presence.** A VKS 9.1 guest ships the CRDs from the VKr (ON by default, opt-out label), while `istio_ensure_gwapi_crds` server-side-applies our pinned `GATEWAY_API_VERSION` — so we may be fighting the VKS add-on manager. Settle with the `bundle-version` jsonpath + the add-on label. Full grading: `docs/vks-services/istio.md` §4. **Lab commands added to step 13 of `docs/lab-validation-plan.md`** (bundle-version + add-on label + VAP), 2026-07-23. ✅ **ANSWERED ON A REAL LAB 2026-08-17 (cut B, guest `cicd-gc0817071000`, VKr v1.35.6+vmware.2) — and it is the bad answer: WE LOSE. The add-on manager owns the CRDs at a DIFFERENT version than our pin, and our pin is NOT in effect.** MEASURED via the guest kubeconfig: **6** `gateway.networking.k8s.io` CRDs present (backendtlspolicies, gatewayclasses, gateways, grpcroutes, httproutes, referencegrants); `httproutes.gateway.networking.k8s.io` carries **`bundle-version: v1.4.0`** — *not* our pinned `GATEWAY_API_VERSION=v1.5.1`; its labels are `kapp.k14s.io/app` + `kapp.k14s.io/association`; and its `managedFields` list **`kapp (Update)`** + `kube-apiserver (Update)` — i.e. **kapp applied them, and there is no `Apply` manager of ours at all**. The carvel packaging API is present (**6** `packaging.carvel.dev` resources), confirming kapp-controller is the owner. Rows 3 and 4 ran a full `install-ingress` on this cluster *before* this measurement, so `istio_ensure_gwapi_crds` either never ran or was reverted by the add-on — either way **the effective version is the add-on's, our SSA-force-apply did not win, and the repo has been asserting a version it does not control.** ⚠️ AND THE KinD STORY DOES NOT TRANSFER: there are **0** gateway-api `ValidatingAdmissionPolicy` matches on this lab, so the `safe-upgrade` VAP that blocks CRD downgrades in the KinD flow is **absent here** — the mechanism we reason about locally is not the mechanism operating on the lab. **Do NOT 'fix' this by bumping our pin to v1.4.0**: that is the version-lock question (istio 1.30 vendors gateway-api v1.5.1 per `istio.md:115-116`), so pinning down to the add-on's version may break the mesh we install. The real decision is the one this row already framed — respect the add-on (stop force-applying) or set `addon.addons.kubernetes.vmware.com/gateway-api: unmanaged` deliberately — and it now has its measurement. Needs an idea round before any code moves. ⚠️ **SEVERITY BOUNDED 2026-08-18 — the mismatch is real and it does NOT break the mesh.** On the SAME lab where this row took its measurement (cut B, 2026-08-17), all four scenario-1 rows ran `install-ingress` and finished with **0 FAILED blocks** — rows 1-4 each walked the full ingress install against the add-on's `v1.4.0` CRDs while the repo pinned `v1.5.1`. So the accurate reading is: **we assert a version we do not control** (a correctness and honesty defect, exactly as stated) **but istio 1.30 installs and routes fine on the add-on's CRDs**. Worth stating because "WE LOSE" and "our pin is NOT in effect" can otherwise be read as "the mesh is broken on the lab", which the row's own evidence base contradicts. This does not change the decision the row frames (respect the add-on vs `gateway-api: unmanaged`), and it does NOT license bumping the pin to v1.4.0 — that remains refuted, since istio 1.30 vendors v1.5.1. Still needs an idea round before any code moves. |
| **B46** | ✅ **CLOSED 2026-07-18 — DECLINED ON EVIDENCE by the owner. Do not re-open without a new reason; the alternatives are measured and two of them do not work.** The finding is real: for identical content, `podman save` emits **623,348,736 B** (docker-archive v1, layers **plain tar**) while `docker save` 29.6.2 emits **286,969,856 B** (**oci-layout + `index.json` + a compat `manifest.json`**, layers **gzip**) — **2.17×**. **The number that decided it:** the saving is **338 MB against a ~12 GB bundle = 2.8% of the carry** (the builder tarball is only 5.2% of the bundle to begin with). Both ways of capturing it add a moving part to the **air-gap path**, which is the one place a failure is most expensive, so the trade was declined. **Options measured, for the record:** (a) `podman save --format oci-archive` → 297,119,232 B but **`crane push` REJECTS it** — `Error: loading …: file manifest.json not found in tar`; docker's archive only works because it is a **hybrid** carrying the compat `manifest.json` alongside the OCI layout. (b) `podman save --compress` → **not supported** for docker-archive (fails). (c) gzip the podman tarball → **285,575,559 B** (i.e. ~97% of docker's win, podman retained) but requires a `gunzip` step + transient disk in `22-builder-push.sh` **on the air-gap box**. (d) prefer docker in `14-builder-build.sh` → 287 MB with no decompress step, but the artifact's shape then depends on which engine the internet box happens to have, and it pushes against the **"docker is opt-in, never required"** invariant that `test-container-engine.sh` check 7 enforces. ⚠️ **The genuinely useful half of this row is NOT the size** — it is that `test-builder-save-crane.sh`'s honesty block claimed both engines default to the SAME format and **that was false**; the claim survived because a successful `crane push` proves crane handles **both** and says nothing about them being the **same**. The discriminating command was `tar tf`. That correction is already in the script's header and in `rules/common/testing.md`. |
| **B20** | ✅ **RESEARCH DONE 2026-07-19 — the question is answered from primary sources; the WIRING stays gated on lab step 3, and the review found a bigger bug than the one I was chasing.** **The answer:** the only supported mechanism is the env var **`VCF_CLI_VSPHERE_PASSWORD`** [`9.0-doc`, verbatim: *"You can also set an environment variable VCF_CLI_VSPHERE_PASSWORD with the password value"*] — it reaches the CLI through `execve` envp, never argv, so it satisfies `security.md`. There is **no `--password` flag** (confirmed by the reference AND by a practitioner) and **no documented stdin form** (NOT STATED — do not invent one). The page 301-redirects from `/latest/` into the `/9-0/` tree, which is what fixes the grade at `9.0-doc`. ⛔ **NEVER `vcf config set env.VCF_CLI_VSPHERE_PASSWORD`** — documented on the same page, it writes the password **plaintext to `$HOME/.config/vcf/config.yaml`** and it *"persists until you unset"* it: outside the repo, invisible to gitleaks and to `check-secrets-untracked` (tracked paths only), surviving every teardown — the `/etc/docker/certs.d` residue class. That warning is the highest-value line of the change and is now in the script. 🔴 **MY DESIGN WAS REFUTED, and the refutation found a CRITICAL I was not looking for.** I intended to wire the env var and add it to `.env.example`. An adversary round killed three things, all verified by me against the tree: **(1)** the premise. `30-vks-login.sh`'s comment claimed the CLI *"prompts interactively for the context name AND the password"* — **REFUTED**: Broadcom's synopsis is `vcf context create CONTEXT_NAME [flags]`, a **required positional**, no `--name` flag, four examples all positional, no documented prompt. `30` passes **none**, so it most likely errors `accepts 1 arg(s), received 0` — while our own `31-fetch-argocd-kubeconfig.sh:77` passes it **and** `--type k8s`. Wiring a password into a call missing a required argument would only make a broken path LOOK automated. **(2)** the new variable. **`VKS_PASSWORD` already exists** (`.env.example:805`, already commented, already documented "never argv") and `check-env-coverage.sh:42` **wildcard-exempts `VCF_[A-Z_]*`**, so a new `VCF_*` var would have had no gate — the second instance of the `PSA_LEVEL_*` trap (B51). **(3)** `export`. The right shape is a **command-scoped prefix**, the precedent for which is on line ~92 of the very file I was editing (`KUBECTL_VSPHERE_PASSWORD="$VKS_PASSWORD" kubectl vsphere login`); a bare `export` would put a vCenter SSO **admin** password in the environment of every `kubectl`/`helm`/`crane`/`git` the script later spawns, readable in each `/proc/<pid>/environ`. The guarded one-liner is recorded in the script, unwired, with the non-empty guard flagged as **not optional** (an unguarded empty assignment CLOBBERS an operator who exported it themselves). **What shipped:** the facts + the forbidden path + the refuted-premise note in `30-vks-login.sh`; an operator-facing warning that now names the **exact recovery command** if the positional error appears; and lab step 3 sharpened from "paste `--help`" to **three named yes/no questions** (positional? `--type k8s`? no `--password`?) so it can be settled in seconds. **Still open, lab-gated:** land the positional fix + `--type k8s` + the guarded env prefix in BOTH scripts, together, from the real `--help`. Also unverified [`community`]: the prompt **recurs at token refresh**, so a long `make install-all` can block mid-run — which is the argument for exporting it for the session deliberately rather than scoping it to `context create` alone. |

| **B65** | ✅ **DONE 2026-08-05 — `fetch-ca.sh` accepted a MITM and printed `VERIFIED`; 4 commits, both adversary rounds run, both REFUTED me.** It took a CA off the wire, `openssl verify`d the served leaf against it, and reported success — self-consistency, which a MITM satisfies BY CONSTRUCTION, and in the single-cert branch (the one we test) it reduces to `verify(X,X)`. MEASURED with a live oracle: an evil self-signed cert was written and reported VERIFIED, rc 0. Blast radius is **credential disclosure**, not image trust — `lib/harbor.sh` submits `admin:password` over this anchored connection and `22-harbor-robot.sh` mints the robot on it. **Now:** out-of-band SHA-256 pin (`HARBOR_CA_SHA256`/`ARGOCD_CA_SHA256`/`VKS_CA_SHA256`); mismatch and malformed both refuse; no-pin+tty discloses and asks; no-pin+no-tty refuses. **Design round killed my escape hatch** — the lab NEVER CALLS this script (`06-install-harbor.sh` mints its own CA), so a `LAB_ONLY` knob would be settable only on the customer path. It also found the plumbing that would have made the whole control INERT: `-include .env` creates MAKE vars, not environment (MEASURED: recipe `[AA:BB:CC]`, script `[]`). **Implementation round found a CRITICAL I introduced:** the copy to `$OUT` ran ~50 lines before the first check and each refusal `rm -f`'d it — so DETECTING AN INTERCEPTOR DELETED THE OPERATOR'S GOOD ANCHOR, in gitignored `./secrets/`, while the die said "refusing to WRITE". Also: an invalid label FAILED OPEN (1135 B of unauthenticated 0644 trust material); the consent block went to stdout so `>log` made it a silent hang; and **the test could not discriminate** — killing the entire interactive branch still passed 7/7. Now staged to a temp candidate, 25/25, RED-proven three ways. |
| **B66** | ✅ **CLOSED 2026-08-17 — the idea round says CLOSE, and it found the strongest fact against this row.** **The Tier-1 CONSUME pattern already exists in this repo — for Istio. Harbor never got it.** `44-install-ingress.sh` has `INGRESS_CONTROLLER=istio\|istio-existing\|traefik`, with `istio-existing` logging *"attaching to an Istio we did NOT install"* and its own e2e (`90-e2e-istio-existing.sh`). MEASURED: `grep -rnE "HARBOR_MODE\|harbor-existing\|HARBOR_INSTALL"` across `scripts/`, `Makefile`, `.env.example` = **ZERO**. Harbor's consume path is IMPLICIT — set `HARBOR_URL` and rely on the right branches not running. ⚠️ **MY OWN JUSTIFICATION FOR THIS ROW IS PARTLY REFUTED, BY ME, BEFORE ANY WORK STARTED.** I first wrote *"nothing structurally prevents a customer run reaching it"* — **MEASURED FALSE**: `make -n install-all` contains **ZERO** references to `06-install-harbor.sh` (the chain is `preflight mirror mirror-verify builder-image vks-login platform gitops`, and `platform` = `install-gitea seed-gitea install-tekton configure-tekton`). `install-harbor` is a standalone operator-invoked target, NOT on the customer path, and there is no flow discriminator in `lib/os.sh` (`is_kind`/`FLOW=` = 0 hits). So the safety argument is gone and what may remain is a DOCUMENTATION problem (two personas in scenario-1, no machine-readable mode) — which is a much smaller thing, and possibly nothing. **A design round is running on exactly the question "is this justified at all, or is it tidiness dressed as safety?" — do not implement before reading its verdict; closing this row is an acceptable outcome.** This is the shape the TLS adversary's Tier-1 recommendation needs, and it is **not new architecture**: copy the ingress precedent, including its clobber workaround (`44-install-ingress.sh` captures the explicit override BEFORE `load_env`, which is why an uncommented `INGRESS_CONTROLLER` in `.env.example` does not defeat a per-run override). Design round required — the ingress analogy is a starting point, not a settled design. 🟢 **CLOSED 2026-08-17 — the idea round says CLOSE, and it found the strongest fact against this row, which my own refutation MISSED.** `06-install-harbor.sh:43` calls **`require_kind_target "install-harbor"`** (`lib/tls.sh:233-257`), a HARD FAIL-CLOSED guard: it compares `kubectl config current-context` against `kind-${KIND_CLUSTER_NAME}` and dies otherwise, refusing on unset/empty/no-context. `07-install-argocd.sh:35` and `91-e2e-tenant-mechanism.sh:47` carry it too. My refutation stopped at *"not on the customer path"* and never mentioned the guard sitting at line 43 of the very script the row is about — **I under-argued my own refutation.** **AND THE ISTIO PARALLEL IS STRUCTURALLY FALSE:** Istio needs a dispatcher because ONE target must produce TWO different mutations of the same cluster (`44-install-ingress.sh:31-44` execs install-or-attach). Harbor's consume path mutates Harbor **not at all** — consuming is `HARBOR_URL` + a CA + a robot, so there is no apply to dispatch. **THE MODE IS ALREADY ENCODED, more strongly than a flag:** two separate targets (`install-harbor-service` for the scenario-1 admin, `install-harbor` for the KinD stand-in, none for the tenant) — a target name cannot be silently mis-set, whereas `HARBOR_MODE=existing` would ADD a way to get it wrong. And ownership is answered by **runtime discovery**, not declaration: `22-harbor-robot.sh:76` asks the live Harbor `GET /users/current` and branches. A flag would be a second, weaker source of the same fact. **THE DOCUMENTATION DEFECT DOES NOT EXIST:** scenario-1 says `install-harbor-service`, kind-local says `install-harbor`, and `docs/scenario-2.md` mentions `install-harbor` **zero** times while carrying an explicit Self-service/Must-request table. Asked to name the misled reader and the misleading line, the round could name neither. ⚠️ **RESIDUAL, and it is the route by which this row could be smuggled back: `require_kind_target` had ZERO committed coverage** — B67 recorded only a BY-HAND proof, which `gates.md` says expires. **NOW FIXED:** `scripts/test-require-kind-target.sh`, 10 cases, RED-proven twice (guard made to fail OPEN on a missing context → 2 FAILED; guard dropped from one call site → 1 FAILED). |
| **B67** | ✅ **CLOSED — ITS ONLY REAL WORK VERIFIED IN CODE 2026-08-18.** The idea round had already struck four of the six cited sites as non-defects and re-scoped the row to one item: `91-e2e-tenant-mechanism.sh` resolving `ARGOCD_CA_FILE` and then sending the ArgoCD admin password with `--insecure` anyway. Re-measured on the current tree, the prefer-the-CA shape is in place: `:231` `export ARGOCD_OPTS="--server-crt ${ARGOCD_CA_FILE}"` (so the variable finally has a CONSUMER — `:218` records that it previously "sat with one producer and ZERO consumers"), `:232` logs *VERIFYING against …*, and `:238-241` falls back to `--insecure` ONLY when no CA exists and says so LOUDLY with the remedy (`make fetch-argocd-ca, then re-run with ARGOCD_CA_FILE set`). The remaining `-k` at `:211` is the pre-trust **healthz reachability poll**, not a credential send — the same class this row already struck as a non-defect at three other sites. `:216`'s comment (*"THIS --insecure IS WHY AN ENTIRE DEFECT CLASS HID"*) stays: it is the reason the class was invisible. 🟡 **RE-SCOPED 2026-08-17 by its own idea round, which REFUTED the row twice over — the row's line numbers are STALE, two of its six sites are NOT defects, and the two sites that matter most were never listed.** Stale cites: 163/222/173 are actually **216/220/239**; line 163 today is the tenant-fixture positive control. **NOT defects** (strike them — carrying non-defects inflates a sweep and hides the real ones): `16-engine-trust-check.sh:148` (its `--insecure` is INSIDE the `HARBOR_INSECURE=1` plain-HTTP branch; the `else` verifies against a system+our-CA bundle), `test-builder-save-crane.sh` (loopback `registry:2`, plain HTTP, no credential), and `06-install-harbor.sh:239` (plain HTTP has no cert to verify, and it IS `require_kind_target`-bounded). **LOW, no action** — `e2e-cross-cluster.sh:95`: the script `kind create`s and deletes BOTH ends itself, so the bearer token belongs to a cluster that exists only for the run. **FIXED 2026-08-17 (this row's only real work):** `91-e2e-tenant-mechanism.sh` resolved `ARGOCD_CA_FILE` at :211-221 and then sent the ArgoCD ADMIN password over `curl -s -k` at :239 — it had the anchor in hand and ignored it; now derives `_e2e_curl_tls` from the SAME branch. And it had **no `require_kind_target`** while `make e2e-kind-tenant` has ZERO prerequisites and reads `KUBECONFIG` from `.env.state` — i.e. `make vks-login` then `make e2e-kind-tenant` would patch a REAL `argocd-cm`, read the REAL admin secret, and ship it to the REAL LB. Guard added and RED-proven (refuses on `kind-cc-guest`). **THE DISCRIMINATOR the row was missing**, and it resolves the B65 question the row flagged as unsettled: a flag that makes an INSECURE default OPT-IN is sound (`ARGOCD_REGISTER_INSECURE`, commented out, defaults 0, *"Never on a real lab"*); a flag that makes a SECURE default OPT-OUT is the shape B65 refuted. These sites had **no knob at all**. **A MECHANICAL GATE IS REFUTED HERE** — measured 46 hits / 33 code lines, of which ~4-6 are genuine: **~82% false-RED on code lines**, in the range `gates.md` already refuted a gate at (89%), and the legitimate `--insecure` (crane vs a plain-HTTP registry) is TEXTUALLY IDENTICAL to the defective one. Honest alternative if ever wanted: a committed exception list with a reason per line (the `.trivyignore` precedent), gated on *every* insecure site being listed and reconciled BOTH directions. **The residual and the real prize moved to B133.** |
| **B133** | 🟢 **SHIPPED 2026-08-18 — the owner decisions are taken, the trust ladder is built, and it is MEASURED ON A REAL LAB with ZERO SSO attempts spent.** **Decision (owner, 2026-08-18):** verify by default; the CA is OPTIONAL and used when provided; `VCENTER_INSECURE=1` is an explicit per-run opt-out. That is the standard polarity (`curl`/`docker`/`kubectl` all verify unless told not to) and it **re-confirms B64** — *"vCenter is pre-existing customer infrastructure, so this repo must fail closed"* — rather than reversing it. ⚠️ The owner's first answer was "continue unverified"; it was surfaced as a conflict with B64 rather than silently built, and the framing that produced it was mine and pessimistic ("refuse breaks every run until you supply a CA" — in fact only Step 2 runs before an anchor can exist). **BUILT — the row's own idea round prescribed this shape; I did not design it:** `vcenter_ca_default()` (pure, defaults to `./secrets/vcenter-ca.pem`), `_vc_tls()` ported from `_harbor_ca_args`, `_vc_tls_args <array> [soft]`, all three hardcoded `-k` gone, `|| true` removed from `vc_login` with curl's rc captured, and explicit **60/77/35/51** arms ABOVE the `000` arm — because rc 60 (anchor does not verify) and rc 77 (anchor unusable) both collapse to `http_code=000`, so without them every TRUST failure told the operator to go debug DNS. `VCENTER_CA_FILE` **and** `VCENTER_INSECURE` in BOTH selector lists. 🔬 **MEASURED ON THE LIVE LAB** (`vcsa.env1.lab.test`), all read-only: correct anchor → `[--cacert <f>]` curl **rc=0**; wrong anchor with a byte-identical subject → **rc=60**; no anchor → **refuses and never dials**; `VCENTER_INSECURE=1` → `[-k]` **rc=0**. Offline oracle **15/15**, including a server that records whether an `Authorization` header ever arrived — so *"the password never left this box"* is asserted, not claimed. 🔴 **THREE DEFECTS THE GATE AND THE IMPLEMENTATION ROUND CAUGHT IN MY OWN CHANGE — the two-round discipline earning its keep:** **(1)** `vc_login --soft` died on a missing anchor, so `make wcp-restart`'s 600s wait would have died on probe 1 (`test-wcp-service.sh` caught it). **(2)** `35|51` was still fatal under `--soft` — measured: the OLD code's `|| true` turned rc 35 into `code=000`, which the `000` arm soft-returns, so making it fatal was a regression on the arm a restart *actually* hits (a proxy that has bound its port but has not finished coming up returns exactly `code=000 rc=35`); this repo already classifies rc 35 as transient in two other places. **(3)** the test's own oracle cert had **no IP SAN**, so curl rejected it at rc 60 and **the 401 case passed by dying at TLS without ever reaching the 401** — a false pass in the one case whose entire purpose is the SSO lockout budget; only the 503 case (which expects a RETURN) exposed it. **(4)** `VCENTER_INSECURE` was in NEITHER selector list — measured with `HARBOR_INSECURE` as a discriminating control: caller `0` + `.env` `1` → **`1` DEFEATED** for vCenter while the control survived; now `0`. All fixed and RED-proven (reverting the soft arm fails exactly 1 case; removing the SAN fails exactly 1). 🟢 **AND THE OUTAGE THE FIX WOULD OTHERWISE HAVE CAUSED IS CLOSED: `make fetch-vcenter-ca` (new).** The round measured that `VCENTER_CA_FILE`/`VCENTER_INSECURE` appeared **0 times** in `docs/` — so scenario-1 rows 1–4 would have died at **Step 2**, before Harbor, on the very next matrix. The new target downloads vCenter's own bundle from `/certs/download.zip`, selects the root by **HANDSHAKE** (`ca_verifies_endpoint`, never a subject compare), and writes `./secrets/vcenter-ca.pem` at 0644. **PROVEN END TO END ON THE LAB:** `candidates in the bundle: 1; verified vCenter by handshake: 1` → file written → `vcenter_ca_default` auto-discovers it → `--cacert` → **rc=0**. It ships with all three of the round's conditions: it is a **separate target** (folding it into `fetch-supervisor-ca.sh` would put the fetch *after* its first consumer at scenario-1 §2 vs §3), it **prints the SHA-256 with the out-of-band confirmation sentence** both siblings carry (without it this is TOFU dressed as a trust ladder), and its selection loop **COUNTS** verifying candidates (a bundle mid-rotation can carry two; a silent first-match installs the one about to expire) and reports the **rc-3 name-mismatch** case separately, because an IP in `VCENTER_HOST` produces exactly that for every candidate. scenario-1 §2 now documents the anchor, the fingerprint warning, and the per-run opt-out **before** the first consumer. ⚠️ **`secrets/vmca-root.pem` is deliberately NOT probed** — measured **rc=60** against the live vCenter, i.e. STALE from an earlier cut, and `-s` cannot tell stale from good; defaulting to it would silently select a dead anchor and blame a file the operator never chose. ⚠️ **STILL OPEN (MED-2, filed not fixed):** there is no PIN. `VKS_CA_SHA256`/`HARBOR_CA_SHA256`/`ARGOCD_CA_SHA256` exist; `VCENTER_CA_SHA256` does not, so `ca_pin_verdict` is not wired into `_vc_tls`. The fingerprint sentence is the interim control. Parity with the sibling ladders is therefore **overstated until that lands** — do not claim it. ⚠️ **Also open (LOW-3, pre-existing):** `vc_api` still keeps `|| true` and captures no rc, so a response truncated after headers yields `code=200` and a success return. ⚠️ **AND `fetch-vcenter-ca.sh` HAS NO COMMITTED TEST — say so rather than implying coverage.** Its happy path is proven end-to-end on the lab, but its two guard branches are **unreachable on this lab**, which returns exactly one candidate that verifies: the **>1 verifying candidate** refusal (a bundle mid-rotation, where a silent first-match installs the CA about to expire) and the **rc-3 all-candidates-name-mismatch** diagnosis (what an IP in `VCENTER_HOST` produces) have both been read but never executed. That is the same MED-3 class the round raised for the refusal arm, one file over, and a hand-run proof would expire at the next commit anyway. **Fix:** a fixture test that mints two throwaway CAs plus a mismatched-name leaf and drives the selection loop offline — the pattern is already in `test-wcp-service.sh`'s TLS oracle and in `/tmp/vcprobe/probe.sh` from this session. ⏸️ Original follows. — 🔴 **CRITICAL, CONFIRMED — and BLOCKED on TWO OPERATOR DECISIONS ONLY. Do not implement before both.** ⚠️ **The lab measurement is ✅ ANSWERED — do NOT spend lab time re-running it.** (2026-08-17, live cut: a VMCA root DOES verify vCenter, `--cacert vmca-root.pem` → rc=0/HTTP 200, while `supervisor-ca.crt` → rc=60 and no `--cacert` → rc=60. The `BLOCKED ON ONE MEASUREMENT` paragraph further down is the PRESERVED ORIGINAL note, kept for the arc — it is not a work order.) `lib/vcenter.sh` sends the vCenter SSO ADMINISTRATOR password to an unverified peer (`curl -sS -k`, `:48`) and the session token on the same channel (`:73`, `:89`). It is the **only credential-bearing TLS client in this repo with no trust ladder** — Harbor has one (`lib/harbor.sh:193-200`), ArgoCD has one (fixed by B67 today), the Supervisor has one (`30-vks-login.sh:139-200`); vCenter has hardcoded `-k`, no knob, no anchor, no pin. **THE IDEA ROUND REFUTED FOUR OF MY PRESCRIPTION'S FIVE SPECIFICS, all measured:** **(a) my "NINE callers" overstates the fix surface ~4x** — there IS one chokepoint; it is **2 files / 4 call sites** (`vcenter.sh:48/:73/:89` + `fetch-supervisor-ca.sh:33`). No consumer builds its own curl; `30-vks-login.sh:249` is inside a printed hint STRING, and the only test reaching `vc_login` stubs it wholesale. **(b) `make install-all` NEVER reaches `vc_login`** (`Makefile:793` — preflight/mirror/mirror-verify/builder-image/vks-login/platform/gitops, none of which source `lib/vcenter.sh`), so the exposure is four standalone targets, not the whole install. **(c) THE REPO HAS TWO LADDERS AND I NAMED THE WRONG ONE** — B65's refuse/consent ladder is the **ACQUISITION** site (`fetch-ca.sh:186-258`); the **USE** site (`30-vks-login.sh:154-180`) is deliberately SILENT on verdict 3 (no pin), with its reasoning written in place: an operator who obtained the CA out of band already has the STRONGER evidence, and demanding a transcribed digest on top *"would nag every correct user for nothing"*. `vc_login` is a USE site, so `VCENTER_CA_SHA256` is OUT of the minimal change. **(d) the snapshot is TWO files, not one** — adding `VCENTER_CA_FILE` to `lib/os.sh:499` ALONE turns `static-check` RED, because `check-env-clobber.sh:99-111` asserts PROTECTED ⊆ SELECTORS and its own comment FORBIDS deriving one from the other. **AND A NEW CRITICAL THE PRESCRIPTION WOULD HAVE INTRODUCED** (measured against a live self-signed oracle): removing `-k` without capturing curl's rc makes every TLS failure print *"vCenter is unreachable — check the FQDN resolves"*, because rc 60 (wrong CA) and rc 77 (unreadable CA file) BOTH collapse to `http_code=000`, which is `vc_login:54`'s existing arm — sending the operator to debug DNS for a TRUST failure. `|| true` currently swallows the rc; it has to go, with explicit 60/77 arms ABOVE the 000 arm. **THE SMALLEST CORRECT CHANGE** is to port `_harbor_ca_args`, which already exists for this exact hazard (*"three functions were each re-deriving this; the moment they drift, one of them sends a password over a connection another one refused to"*): a `_vc_tls` returning `--cacert $VCENTER_CA_FILE` / `-k` only under an explicit `VCENTER_INSECURE=1` / else refuse — the INSECURE-OPT-IN polarity B67 blessed, matching `HARBOR_INSECURE`. Plus `vcenter_ca_default()` mirroring `vks_ca_default()`, `VCENTER_CA_FILE` in BOTH selector lists, and `# VCENTER_INSECURE=0` COMMENTED in `.env.example` (a runtime toggle). ⛔ **WARN-AND-CONTINUE IS REFUTED, and not by me:** `BACKLOG.md` B64 already records the owner-level decision — *"vCenter is pre-existing customer infrastructure, so this repo must **fail closed**"*. And there is no non-breaking secure option: measured, with `-k` removed and no CA, curl falls to the system store and returns rc 60 on a self-signed vCenter. **The breakage IS the fix**; the only real choice is where it lands and how legible it is. **✅ THE BLOCKING MEASUREMENT IS ANSWERED (2026-08-17, on a live cut): a VMCA root DOES verify vCenter.** `--cacert ${LAB_STATE}/vmca-root.pem` → **rc=0, HTTP 200**; `--cacert ./secrets/supervisor-ca.crt` → **rc=60**; no `--cacert` at all → **rc=60** (which is the state every operator is in today, and confirms the round's *the breakage IS the fix*). So the sizing is the SMALL one: only scenario-1 **Step 2** runs before a CA exists. ⚠️ **AND THE MEASUREMENT I QUEUED WOULD HAVE ANSWERED IT WRONGLY.** Step 24 originally compared SUBJECT STRINGS, and all three subjects here are BYTE-IDENTICAL (`CN = CA, DC = vsphere, DC = local, … O = vcsa.env1.lab.test`) while the certs are different — SHA256 `73:79:F1…` vs `B3:93:B2…`, notBefore Aug 14 vs Aug 13. **Every lab cut mints a NEW VMCA with the SAME subject**, so a stale file from any earlier cut reads EQUAL and the step would have concluded *"the anchor already exists"* — wrong, in the direction that ships a broken fix. Step 24 now performs a HANDSHAKE (`curl --cacert`, rc 0 ⇒ verifies) with the subject compare demoted to a diagnostic. This repo's own rule, learned again: **verify by a trust OPERATION, never by the presence of a file — or by a string.** **RESIDUAL:** the working anchor is `${LAB_STATE}/vmca-root.pem`, produced by the OTHER repo's `make trust-vcsa`; THIS repo has no vCenter anchor of its own, and `secrets/supervisor-ca.crt` is measurably not one. So the fix must either consume a caller-supplied `VCENTER_CA_FILE` or stop `fetch-supervisor-ca.sh:110-114` discarding the rest of the bundle it already downloads. Original blocking note follows: **🎯 BLOCKED ON ONE MEASUREMENT — now queued as step 24 of `docs/lab-validation-plan.md`:** does `./secrets/supervisor-ca.crt` also anchor vCenter? EQUAL → only scenario-1 **Step 2** (`make vsphere-namespace`, the one block that runs BEFORE the CA-fetching step) breaks, and the fix is small. NOT equal → all four blocks break and a vCenter root must be extracted first — from a bundle `fetch-supervisor-ca.sh:33` ALREADY downloads and `:110-114` discards. B64 recorded this as its cheap unblock and it was never run. **🎯 AND TWO OPERATOR QUESTIONS THE CODE CANNOT ANSWER:** (1) Step 2 must either HARD-FAIL on a box with no vCenter CA — breaking every current operator and every walk row until they set `VCENTER_CA_FILE` or opt in with `VCENTER_INSECURE=1` — or keep sending the password unverified. Which? And may the walk matrix set `VCENTER_INSECURE=1` for its rows, making the walk explicitly NOT a proof of the secure path? (2) May scenario-1 be REORDERED so a vCenter CA is obtained before Step 2? That converts the break from *permanent, for everyone* into *one doc edit*, and is the only change that makes the secure default free. **Filed separately, NOT part of this row:** `fetch-supervisor-ca.sh` writes an unauthenticated anchor to disk and defers the pin to the next command — a different contract (acquisition, not use) needing its own round; and note `fetch-ca.sh:248-256` already records that a no-tty refusal is reachable from a WALKED block, and `walk-doc.sh` neutralizes only `argocd login`/`port-forward` — so a new refusal in a walked target becomes a walk RED. ⚠️ **B64 AND B78 DEPEND ON THIS ROW (recorded 2026-08-17).** B64's counter-proposal introduces the vCenter credential surface these two decisions govern — its own finding (3) is this row, arrived at independently — and B78 is blocked on B64. So these two operator decisions gate three rows, not one, which is worth knowing when deciding whether they are worth the owner's attention. 🎯 **THE BLOCKING MEASUREMENT IS ANSWERED, 2026-08-17, and the answer is `EQUAL` — take the CHEAP branch.** Measured on the live lab by a HANDSHAKE, not a string compare (the method this row itself prescribes): a FRESHLY fetched `./secrets/supervisor-ca.crt` is **byte-identical** to the vCenter root (`md5 f8857738` == `${LAB_STATE}/vmca-root.pem`) and `curl --cacert ./secrets/supervisor-ca.crt https://<vcenter>/` returns **rc=0**. Control: with **no** `--cacert` the same request is **rc=60**, so the box does not already trust it and the rc=0 is genuinely the anchor doing the work. **So the expensive branch this row feared — "all four blocks break and a vCenter root must be extracted first" — is RULED OUT**; per this row's own text, `EQUAL` means only scenario-1 **Step 2** breaks and the fix is small. Cause: vCenter's bundle at `/certs/download.zip` carries exactly **ONE distinct root** (measured: 2 files, 1 distinct subject, both `f8857738`), so the subject-match at `fetch-supervisor-ca.sh:113-118` cannot pick a different cert than the vCenter anchor. ⚠️ **An inference of mine was REFUTED in passing and is recorded so nobody rebuilds it:** I predicted that match-by-SUBJECT-STRING would be glob-order-dependent, because two certs on this box share an IDENTICAL subject DN and identical size (1789 B) yet differ in bytes. It is not reachable **here** — the bundle has only one distinct root. The two same-subject certs turned out to be the CURRENT root and a STALE one (see B166), not two roots in one bundle. **This also unblocks B64 and B78**, which this row records as depending on it — and B64 named this exact measurement as its cheap unblock and it had never been run. The TWO OPERATOR DECISIONS below are still open and still gate the fix. ⚠️ **ROUND 2026-08-18 (measured, with a real TLS oracle) — THE ROW STANDS, but the LOCKOUT OBJECTION IS REFUTED, and that objection is what made this row feel dangerous.** A fail-closed TLS fix CANNOT contribute to a lockout: measured across three arms against a self-signed server with a server-side auth counter — `curl -k` -> HTTP 401 with the credential **received** (decoded back to cleartext from the Basic header); `--cacert <WRONG>` -> **rc 60, http 000, ZERO credentials sent** because TLS aborts before the HTTP request exists; `--cacert <RIGHT>` -> 401, received. **Verification fails BEFORE authentication, so it costs no lockout budget at all.** Choosing fail-closed is therefore an AVAILABILITY decision (Step 2 breaks until an anchor exists), never a safety one — which is the objection this row's two owner decisions were weighing. ⚠️ **AND A CORRECTION TO THIS ROW'S OWN SIZING:** it says the exposure is "four standalone targets". Measured: **NINE `vc_login` call sites across EIGHT scripts, reachable from ELEVEN make targets** — ~2.75x. (The row's other half is CORRECT and re-verified: `install-all` reaches none of them.) **NEW HIGH, NOT IN THIS ROW — and it is a SEPARATE row, `wcp-service.sh:44-52`:** that loop re-authenticates inside `while :; sleep 10` (up to 60 auths per `make wcp-restart`) and calls it as `if vc_login >/dev/null 2>&1`, so on a failure `die`'s message goes to /dev/null and the script exits at iteration 1 in silence. ⚠️ **Its own round then CORRECTED the framing I first recorded**: this is NOT a lockout path — successful auths do not increment SSO's failure counter and the loop EXITS AT THE FIRST 401 (measured `auths=1`), so it burns **exactly one** attempt, and the `die` being called a bug is the thing PREVENTING a lockout. It follows that **a bounded retry on a 401 must never be added** — it would turn one attempt into 2-60 and *guarantee* the lockout. The real defect is broader and quieter: `vc_login` dies on 401, on `000` AND on any other non-2xx, so one blip during the restart the loop exists to wait through exits at ~10s with no message, while the BENIGN timeout prints a full diagnosis. Fix: hoist `vc_login` OUT of the loop (60 auths -> 0), matching `vc_ss_install`'s already lab-proven shape. **RESIDUAL this row should claim honestly:** there is NO vCenter anchor producer in this repo (`VCENTER_CA_FILE` and `vcenter_ca_default` are ZERO hits), and the one acquisition path (`fetch-supervisor-ca.sh:33`) is `curl -sk` — it downloads the trust bundle over the very channel it anchors (TOFU). So the fix reduces exposure from "every call, forever" to "one unverified first acquisition" — a large win, but the row must claim THAT, not "verified". **Also measured: the repo states TWO DIFFERENT lockout policies** — `.env.example:1326` says 3 failures and permanent, `vcenter.sh:53` (the message an operator reads at the moment they decide whether to retry) says 5-in-3-minutes, which implies waiting resets the counter. Neither cites a source; keep the operator-facing message at the STRICTER reading until a lab read settles it. ✅ **EXPOSURE SIZED FROM A LIVE CUT (2026-08-18): the unverified-peer call fires **36 TIMES PER CUT**, counted in cut B’s own rebuild log.** That is the concrete number behind “only credential-bearing TLS client with no trust ladder” — it is not a rare path, it is the normal one, and every one of those 36 sends the SSO administrator password over a connection whose peer was never verified. Sizing only; it does not change the two operator decisions this row is blocked on. 🔴 **IDEA ROUND 2026-08-18 (13th dispatch; the prior 12 died on API 529) — MY DESIGN IS REFUTED, and its central premise was wrong THREE ways. Read this before writing any code.** My design was: *port the sibling repo's verify-when-anchor-exists + warn-when-absent ladder into `lib/vcenter.sh`'s three `-k` sites, add a `VC_TLS_ENFORCE=1` knob, and hoist `vc_login` out of the `wcp-service.sh` poll loop.* **(F1, CRITICAL — the port is from the WRONG REPO.)** `nested-vsphere-lab/stages/nested-vsphere/vcsa-account.sh:65,72` uses `--cacert` **UNCONDITIONALLY** — no anchor means curl rc 77 and a hard fail, not a warn — so it is not the conditional ladder I described; only `nvl/lib/govc.sh:154-176` has the conditional+warn, and `nvl/docs/EVIDENCE-CUT2.md:103` records **48** `TLS VERIFICATION OFF` warnings in a single cut, i.e. the sibling's *production* state is the unverified arm. Meanwhile **the mature mechanism is already in THIS repo, 140 lines away, and is STRICTLY STRONGER**: `scripts/30-vks-login.sh:230-282` runs `ca_verifies_endpoint` (`lib/tls.sh:104`) for the SAME SSO credential, gets **six typed verdicts** (rc 5 no anchor, rc 3 chain-OK/name-wrong, rc 2 unreachable, rc 4 no cert), then `die "REFUSING to continue: a credential is submitted over this connection"`, plus a separate `die` when `VKS_CA_CERT_FILE` is set-but-missing (*"would silently downgrade a connection you asked to verify"*). ⇒ **Do NOT port the sibling. CALL `ca_verifies_endpoint` from `vc_login` and reuse `30-vks-login.sh`'s three-state policy verbatim** — otherwise the repo carries **two contradictory TLS policies for one credential class, with the WEAKER one newer**, and an operator with a stale anchor gets `die REFUSING` from `vks-login` and `WARNING…proceeding` from `vc_login` ten seconds apart for the same cause. **(F2, HIGH — `--cacert` WITHOUT DELETING `-k` IS A SILENT NO-OP.)** Measured against a local stub (self-signed leaf, random port): `--cacert /nonexistent` → rc **77**; `--cacert <wrong-CA>` → rc **60**; `--cacert <right-CA>` with an **IP** in the URL → rc **60** *no alternative certificate subject name matches target host name*; `--cacert <right-CA>` + `--resolve` by name → **200**; and **`-k --cacert <wrong-CA>` → 200, rc 0** — `-k` wins. ⇒ the change is *delete `-k`* **and** *add `--cacert`*, atomically, at all three sites; a diff that adds `--cacert` and leaves `-k` ships green, logs "verifying", and verifies nothing. **(F3, HIGH — THIS REPO HAS NO vCENTER ANCHOR, and `VKS_CA_CERT_FILE` IS THE WRONG VARIABLE.)** `scripts/fetch-supervisor-ca.sh:104-118` selects a root by matching **`SUPERVISOR_HOST`'s** leaf issuer and verifies it only against the Supervisor (`:134`); `lib/tls.sh:435-436`'s `pairs` list is Harbor + Supervisor with **no `VCENTER_HOST` pair** (B166's own stated residual), and there is no `make trust-vcsa` equivalent here. `/certs/download.zip` legitimately carries several roots, so at any site with a CA-signed vCenter machine cert and a VMCA-signed Supervisor the selected anchor verifies the Supervisor and **not** vCenter — the new verify branch would **fail closed on a CORRECT deployment**. On this single-VMCA lab it happens to work, so **a green lab run cannot discriminate**. ⇒ needs a distinct `VCENTER_CA_FILE` with its own acquisition step and a third `pairs` row. **This is the decision-critical unknown and it is UNMEASURED** — settle it with `openssl s_client -connect "$VCENTER_HOST:443" -CAfile secrets/supervisor-ca.crt -verify_return_error -verify_hostname "$VCENTER_HOST"` on a real lab; if non-zero, the design is dead on arrival. 🔬 **F3'S COMMAND WAS RUN 2026-08-18 ON THE LIVE LAB — AND IT DOES NOT SETTLE F3. Read this before anyone cites the failure as confirmation.** The prescribed `openssl s_client -connect vcsa.env1.lab.test:443 -CAfile secrets/supervisor-ca.crt -verify_return_error -verify_hostname vcsa.env1.lab.test` returned **`verify error:num=20: unable to get local issuer certificate`** — which looks exactly like F3 confirmed, and is not. **The anchor was STALE.** `secrets/supervisor-ca.crt` is dated **Aug 17 18:13**, i.e. it belongs to the lab destroyed at 13:22 today, and the two roots are genuinely different certificates: `secrets/supervisor-ca.crt` fingerprints **`B6:46:E9:C9:A6:EC:C8:48…`** while today's `~/.local/state/nested-lab/vmca-root.pem` is **`43:70:FE:0F:CC:28:AA:0D…`** (same subject `CN=CA, DC=vsphere, DC=local` — so the SUBJECT DN cannot discriminate them, only the fingerprint can). **THE CONTROL SETTLES IT:** the same command against **today's** root returns **`Verify return code: 0 (ok)`**. ⇒ the error-20 was **staleness, not F3's mechanism**, and citing it as evidence for F3 would be wrong. ⇒ **On THIS lab a single VMCA signs both vCenter and the Supervisor, so the Supervisor's root DOES verify vCenter — which is precisely what F3 PREDICTED** (*"on this single-VMCA lab it happens to work, so a green lab run cannot discriminate"*). F3's actual concern — a site where vCenter carries a **CA-signed machine cert** while the Supervisor is **VMCA-signed**, so `/certs/download.zip`'s several roots are not interchangeable — remains **UNMEASURED and structurally unmeasurable on this hardware**. Do not close it here. 🟢 **WHAT THIS DOES CONFIRM, LIVE AND FOR THE SECOND TIME: B166's stale-CA class.** A CA in `secrets/` survived a full `make destroy` + rebuild, still sits on disk, and now anchors a **dead lab's** root — so any vks-side flow that reaches for it fails **closed against a perfectly healthy lab**, with an error (`unable to get local issuer certificate`) that names neither staleness nor the rebuild. That is B166's exact claim, re-confirmed on today's cut, and it is the more actionable of the two findings: the discriminator is a **fingerprint comparison against the live endpoint**, which nothing in the repo currently performs. ✅ **CORRECTION, SAME HOUR — THE REPO DOES PERFORM IT, AND I JUST RED-PROVED IT ON A REAL DEFECT.** The sentence above (*"the discriminator is a fingerprint comparison against the live endpoint, which nothing in the repo currently performs"*) is **WRONG**, and it was wrong when written — I asserted it without running `make ca-status`. Run against the stale anchors and today's live lab it reports, and **exits 2**: *"Harbor CA (./secrets/harbor-ca.crt) does NOT match harbor.env1.lab.test — this is a leftover certificate. A rebuilt lab issues a NEW certificate at the SAME address, so the old file still looks perfectly valid and is not. Get it again — this overwrites in place and cannot lose anything: make fetch-harbor-ca"*, the identical block for the Supervisor against `192.168.101.128`, then *"2 of 2 CA certificate(s) above are wrong. Each one fails LATER, as an error about TLS rather than about the file — and a rebuilt lab reproduces it."* ⇒ the control **names the mechanism (a rebuild reissues at the same address), names the remedy, and gates**. 🟢 **AND THIS IS THE STRONGEST VALIDATION AVAILABLE: a demonstrated RED on a defect nobody planted.** Every other RED-proof in this repo injects its own mutant; this one fired on the *naturally occurring* post-rebuild state, which is exactly the condition the control exists for. 🟢 **It also RETIRES B166's `CHECKED=0` residual.** That residual said the report was measurably BLIND because it builds its Supervisor pair only when `VKS_CA_CERT_FILE` is non-empty while `.env.example` ships that line COMMENTED. Measured today: the Supervisor pair **was** evaluated (it reported on `./secrets/supervisor-ca.crt` by name), so **CHECKED=2, not 0** — i.e. the two-line `vks_ca_default` fix (PR #842, at `29-ca-status.sh:52` and `24-lab-preflight.sh:171`, implemented `lib/tls.sh:288-303`) is doing precisely the job it was landed for. ⚠️ **The stale files were deliberately NOT refreshed.** `make fetch-harbor-ca` cannot succeed in the NOTHING cell (no Harbor exists yet — rows 1-6 install it), and mutating `secrets/` mid-matrix buys nothing while the staleness IS the evidence. Refreshing is an operator action after the run. ⚠️ **What this does NOT do is settle F3**, for the reason given above: this lab's single VMCA verifies both endpoints, so a green ca-status here cannot discriminate the multi-root case. **(F4, HIGH — every TLS failure collapses to `000` and is REPORTED AS DNS.)** `vcenter.sh:48-51` captures only `%{http_code}`, sends stderr to `/dev/null` and ends `|| true`, so **curl's rc is discarded**: missing anchor (77), stale anchor (60) and SAN mismatch (60) all yield `000`, which `:54` maps to *"vCenter … is unreachable … Check the FQDN resolves and is routable from here."* `vc_api` (`:89`) has **no `000` arm at all**. ⇒ capture rc on its own line and branch 60/77/35 separately from 000, **before** the credential goes out — this is precisely the mis-diagnosis class `30-vks-login.sh`'s rc-3 arm exists to delete. **(F5, HIGH — THE HOIST IS REFUTED BY THE FILE'S OWN COMMENT.)** `scripts/wcp-service.sh:39-41` states it outright: during a restart the API disappears *"and the session dies with it, **which is why each probe re-logs-in**"*. The re-login is **load-bearing**. Hoisting hands `wait_for` a token the restart invalidates → `vc_api` non-2xx every poll → `st=""` throughout → the whole `WCP_WAIT_SECONDS` (600) budget burns → `die "wcp did not reach STARTED/HEALTHY within 600s"` **on a lab that recovered normally**: a working restart becomes a 10-minute false FATAL. My arithmetic was wrong too — `:31`'s `vc_login` is unconditional, so the hoist is 60→**1**, never 60→0. The REAL defect is narrower: `if vc_login >/dev/null 2>&1; then` does **not** create a subshell, so `vc_login`'s `die` on the **`000`** arm kills the whole script. ⇒ minimal fix is a **soft mode** — fatal on 401 (preserving the one-attempt lockout budget), non-fatal on `000`/5xx — and stop discarding stderr in the loop. **(F6, MED — NO KNOB.)** `30-vks-login.sh` solves this with **zero** configuration; a fourth policy variable for the same credential is enumerated-config rot. (And any such toggle would have to ship COMMENTED, since `VKS_CA_CERT_FILE` is in `check-env-clobber.sh:91`'s `SELECTORS`.) **(F7, MED — the site count is short.)** `-k` census: `vcenter.sh` ×3 (credential-bearing), `fetch-supervisor-ca.sh:33` (TOFU, deliberate + documented), `harbor.sh:323`, `91-e2e-tenant-mechanism.sh:211`. **UNCOUNTED:** `30-vks-login.sh:473` sends `KUBECTL_VSPHERE_PASSWORD` to `kubectl vsphere login --insecure-skip-tls-verify=…` with **no CA-pin path at all**, unlike the `vcf` arm; same shape at `31-fetch-argocd-kubeconfig.sh:101`. **(F8, LOW — a contradiction this row must resolve before any retry policy is written.)** **`vcenter.sh:53`** (the round said `:56`; **I re-read the file and the round's citation was off by three — corrected here, and it is a reminder that an adversary's citations are claims too**) carries, verbatim in its 401 `die`: *"vCenter SSO locks the account after **5** failures in 3 minutes - do not retry blindly."* This row and CLAUDE.md say **3** attempts. Both numbers are load-bearing for any retry policy and the discrepancy is **not settleable offline** — vSphere SSO's lockout policy is administrator-configurable, so the true value is a property of THIS lab, readable only from vCenter (Administration → Single Sign On → Configuration → Local Accounts → Lockout Policy). ⚠️ **Until it is read, operate on the SMALLER number (3).** The asymmetry is one-sided: assuming 3 when the truth is 5 costs nothing, while assuming 5 when the truth is 3 locks the administrator account. And note both sources agree on the operative instruction regardless — *do not retry blindly* — so the disagreement changes no behaviour today; it only has to be settled before anyone writes a bounded-retry policy against a number. ⚠️ **AND A SCOPE FACT THAT CHANGES HOW ANY RUN IS READ: the walk matrix is STRUCTURALLY BLIND to this entire row.** `.gitignore:15` is `/secrets/` and every row is a fresh clone, so no row can exercise an anchor and none can RED-prove any of the above. **Do not read a 6/6 green as evidence here in either direction.** ⚠️ Every `minimal_fix` above is a SEPARATE CLAIM from its finding — F2 verifies by pointing `vc_login` at a foreign-cert stub and asserting it FAILS (it currently returns 200); F5 by running `make wcp-restart` with the hoist applied and observing the predicted 600 s timeout. **Unaudited residual:** `lib/argocd.sh:377` + `91-e2e-tenant-mechanism.sh:216-242` carry a CA-preferred-else-`--insecure` ladder that looks like a **fourth** independent implementation of this same policy — which would make **consolidation**, not addition, the actual work here. |
| **B68** | ✅ **CLOSED 2026-08-17 as a NEGATIVE RESULT — the row's verdict is DO NOT BUILD, and it stands.** What this row settled is only the ENTRY CONDITION, by MECHANISM rather than a one-off reading: `spec.server.tlsCert` EXISTS on the operator this lab installs (CRD `v1alpha1`, `served=true storage=true`, single version, so no skew ambiguity), which means a supplied value would NOT be pruned. That closes the precondition the row-5 review flagged as unmeasurable when the lab was destroyed. It does **NOT** reopen the build: supplying a cert would break **#745**, which merged the same day. ⚠️ Scope, stated: the measurement holds for THIS operator version — re-measure on a lab whose operator differs, with the `kubectl get crd argocds.argocd-service.vsphere.vmware.com -o json` + `jq` command recorded at the end of this row. **[2026-08-17 — this row's Harbor measurement ANSWERS an open question from the row-5 review, and by MECHANISM rather than by a one-off reading.]** That review could not measure Harbor's SAN (the lab was destroyed) and flagged that if it lacks `DNS:<HARBOR_URL>`, scenario-2's Step-2 CA fix merely relocates the failure to `ca-status` rc=3 — which `lib/tls.sh:495` calls "the MODAL shape here". Settled: `04-install-harbor-service.sh:60` renders `hostname: <HARBOR_URL>` into the Harbor data-values and `:83` ASSERTS it rendered, so the Supervisor Service mints the cert for exactly the name we pass, and the walk passes `harbor.env1.lab.test` (walk-matrix.sh:323). So a FRESH cut reproduces the SAN this row measured — the prediction is derived from the install path, not extrapolated from one lab. And if it were ever wrong, `27-harbor-ca-from-cluster.sh:155` dies on rc=3 in the DRIVER, before any VM is built. **ArgoCD's cert carries NO externally-valid name, which is why `make creds` shows an IP while Harbor shows an FQDN.** MEASURED: Harbor's SAN is `DNS:harbor.env1.lab.test` (the lab minted it for a name it chose via the service's `hostname:`); ArgoCD's SANs are ONLY `localhost, argocd-server, argocd-server.lab, argocd-server.lab.svc, argocd-server.lab.svc.cluster.local`. `argocd.<domain>` resolves and answers 200 **solely because every caller passes `--insecure`**. So the asymmetry is not a display bug: `creds.sh` falls to a discovery branch that reads `.status.loadBalancer.ingress[0].ip`, which is an IP BY DEFINITION — a Service cannot know its DNS name. Both generic auto-name mechanisms are dead here (MEASURED: the LB `hostname` field is empty; reverse DNS returns nothing). **Printing the FQDN would be a display the certificate disclaims.** The fix is the cert. ⚠️ The adversary claimed a replaceable `argocd-tls` secret exists; MEASURED it does NOT in the `lab` namespace (only `lab-gc1-auth-svc-cert`) — the mechanism may still work by CREATING it, but settle that before designing. ⚠️ **RE-MEASURED 2026-08-17 ON AN INDEPENDENT FRESH CUT — the prediction HELD, the residual is SETTLED, and one recorded detail was WRONG.** (1) **Harbor's SAN reproduced exactly**: `subject=CN = harbor`, `X509v3 Subject Alternative Name: DNS:harbor.env1.lab.test`. This row predicted that from the INSTALL PATH (`04-install-harbor-service.sh:60` renders `hostname: <HARBOR_URL>` and `:83` asserts it rendered) rather than extrapolating from one lab, and a second cut confirms it. (2) **ArgoCD's SANs are internal-only, confirmed** — but the list recorded above is namespace-specific and therefore WRONG as written: this cut shows `DNS:localhost, DNS:argocd-server, DNS:argocd-server.`**`cicd`**`, DNS:argocd-server.cicd.svc, DNS:argocd-server.cicd.svc.cluster.local`, i.e. the SANs are DERIVED FROM THE NAMESPACE the instance runs in (`cicd` here, `lab` when it was first measured). That makes the finding STRONGER and more general, not weaker: whichever namespace it lands in, **none of the SANs is externally valid**, so the FQDN would still be a display the certificate disclaims. Do not quote the literal list as a constant. (3) **The residual is SETTLED on a second cut** — the adversary's claimed replaceable `argocd-tls` secret does NOT exist here either. The only TLS secrets in the namespace are `<cluster>-auth-svc-cert` and `<cluster>-extensions-ca`, both prefixed with the GUEST CLUSTER name — per-cluster artifacts, not an ArgoCD server cert. So the mechanism, if it works at all, must CREATE the secret; it cannot replace one. That was the row's own precondition for designing anything, and it is now answered twice. 🟢 **ROUND LANDED 2026-08-17 — verdict DO NOT BUILD, and the row settled the WRONG PRECONDITION: there is no Secret in this interface at all.** Two independent lab cuts were spent answering *"does a replaceable `argocd-tls` secret exist?"* and then *"the mechanism must CREATE it"*. **Both are the wrong object.** Broadcom's ArgoCD Service API documents **`spec.server.tlsCert`** — `cert`/`key`/`ca` **inline base64 in the CR**, no `secretRef` — and the full spec (`paused, version, enableLoadBalancer, url, serverSideDiff, localAccounts, oidc, rbac, resourceConfig, server, repo, controller, applicationSet, redis, notification`) has no hostname, SAN or ingress field. The knob lives in a file this repo already writes (`08-install-argocd-service.sh:194-202`, which emits only `spec.version`), so no lab measurement could ever have answered it. **⛔ AND BUILDING IT WOULD BREAK WHAT MERGED TODAY.** Broadcom is explicit that a supplied `tlsCert` **replaces** the generated cert (*"if not specified, ArgoCD will generate"*), and those generated internal SANs **are the shipping mechanism of #745** — `scenario-2.md:280` now requires *a name the certificate carries*, and B148 measured `argocd-server` + CA → **HTTP 200**. A cert minted for `argocd.<domain>` deletes every name that depends on. The break is **asymmetric and therefore invisible on a green run**: the `--insecure` callers (`scenario-1.md:377`, `09-argocd-address.sh:106`, `91-e2e-tenant-mechanism.sh:238`) keep passing while `argocd_tls_opts` (`lib/os.sh:1269`) makes `configure-argocd` and `make argocd-auth-check` FATAL. **AND IT BUYS NOTHING MEASURABLE:** a verifying path already exists and is lab-proven, with the vendor's own certificate — `make fetch-argocd-ca` is the producer and #753 gave it a consumer. Only the address bar improves. **HALF THIS ROW IS NOT OURS:** in scenario-1 we ARE the platform (we author the instance CR), but in scenario-2 a tenant cannot touch a Supervisor CR at all — so that half is **REQUEST-NOT-CHANGE**, already correctly worded at `scenario-2.md:280`. **THE ONE REAL DEFECT the round found is NOT the certificate** — it is a runbook asymmetry: `scenario-1.md` explains *"an IP cannot verify: argocd-server's default certificate carries DNS SANs only, no IP SAN"* and then, in the same section, prescribes the `--insecure` workaround (`:377`) — a posture `lib/os.sh:1264` itself calls a **fake green**. The fix is ~3 lines mirroring #745 (name + `ARGOCD_CA_FILE`, drop `--insecure`), and `09-argocd-address.sh:96-104` already preserves an operator-set name so it does not regress on re-run. ⏸️ **DELIBERATELY NOT SHIPPED YET, and the reason is the certification run:** rows 1-4 walk `scenario-1.md` and are GREEN on the current instruction; the replacement is **UNWALKED**, and B160 records that run 5b walked the OLD doc for exactly this reason. Landing an unwalked doc change into a matrix in flight risks turning four green rows red and would read as a refutation of a correct change. Ship it AFTER this run certifies the current tree. **Residual for any future cert work (entry condition, not optional):** whether the installed operator's CRD even exposes `server.tlsCert` is **version-UNMATCHED** — the API reference read was `/latest/`, and a field the CRD lacks is pruned silently, which is the obsolete-config-fails-silently class. Settles with `kubectl get crd argocds.argocd-service.vsphere.vmware.com -o json | jq '.spec.versions[].schema.openAPIV3Schema.properties.spec.properties|keys'`. 🟢 **THE ENTRY-CONDITION RESIDUAL IS SETTLED — MEASURED 2026-08-17 against the INSTALLED operator on a live lab, removing its `version-UNMATCHED` grade.** The row required, as an entry condition for any future cert work, that we confirm the installed CRD actually exposes `spec.server.tlsCert` — because the API reference that was read is `/latest/`, and **a field the CRD lacks is PRUNED SILENTLY**, which is the obsolete-config-fails-silently class. Run against the Supervisor of the 2026-08-17 cut (CRD `argocds.argocd-service.vsphere.vmware.com`, created `20:40:05Z` by the certification run's own scenario-1 Step 5): `spec.server` exposes exactly **`replicas`, `resources`, `tlsCert`** — so **`spec.server.tlsCert` EXISTS** and a supplied value would NOT be pruned. CRD is `v1alpha1`, `served=true storage=true` (single version, so no version-skew ambiguity). ⚠️ **This does NOT reopen the build.** The row's verdict stands: DO NOT BUILD — it settled the WRONG precondition, and supplying a cert would break #745, which merged the same day. What is closed here is only the entry condition, and only for the operator version this lab installs; re-measure on a lab whose operator differs. Command, for re-running: `kubectl get crd argocds.argocd-service.vsphere.vmware.com -o json | jq '.spec.versions[].schema.openAPIV3Schema.properties.spec.properties.server.properties|keys'`. |
| **B69** | 🟢 **CLOSED 2026-08-19 — the one action its own verdict prescribed is shipped, and I verified the claim rather than repeating it.** The round's verdict was *"one sentence next to `APP_DOMAIN` stating the RFC 6762 reservation and the escape hatch, not a 54-file rename"*, and it had already measured its OWN minimal_fix insufficient (deriving `GITEA_HOST=gitea.${APP_DOMAIN}` expands AT SOURCE TIME, so an operator's `.env` arrives too late and a command-line `APP_DOMAIN=` is clobbered by `.env.example` itself). `.env.example`'s `APP_DOMAIN` block now carries both halves: **why `.local` is deliberate** (the design is two-tier — `show-dns-records.sh` emits real-DNS records for Harbor and ArgoCD only, the `*.vks.local` names are `/etc/hosts`-only, and the reservation bites exactly one case this repo never asks for), and **what changing it does NOT move** (`GITEA_HOST`, `TEKTON_DASHBOARD_HOST`, `GITEA_ADMIN_EMAIL` are hardcoded, so the knob splits you across two domains with no gate asserting a shared suffix), plus the record that the derivation was tried and REVERTED because a knob that LOOKS functional is worse than an honest literal. **RE-MEASURED before writing it down** rather than trusting the row: sourcing `.env.example` then an override setting `APP_DOMAIN=vks.test` yields `GITEA_HOST=gitea.vks.local` and `TEKTON_DASHBOARD_HOST=tekton.vks.local` — the split is real and current. ⚠️ **STILL OPEN AS A SEPARATE, UNSTARTED DESIGN:** making the knob actually work needs `APP_DOMAIN` in `load_env`'s selector snapshot, the two host keys COMMENTED, and `${GITEA_HOST:-gitea.${APP_DOMAIN}}` applied AFTER the restore. That touches `load_env` and is new design — it needs its own idea round and is deliberately not part of this closure. ⏸️ Original follows. — 🟡 **VERDICT INVERTED 2026-08-17 by its idea round: DOCUMENT THE CONSTRAINT, DO NOT MIGRATE — all four of my premises were refuted, and I then measured the round's own minimal_fix INSUFFICIENT.** **(1) `APP_DOMAIN` CANNOT MOVE THE DOMAIN TODAY.** `.env.example` hardcodes `GITEA_HOST=gitea.vks.local`, `TEKTON_DASHBOARD_HOST=tekton.vks.local` and `GITEA_ADMIN_EMAIL=admin@vks.local`, so setting `APP_DOMAIN=vks.test` moves the two APP hosts and leaves gitea+tekton behind — two domains, two `/etc/hosts` lines, and **0 of 29 `check-*.sh` assert a shared suffix**. The migration executed literally makes the inconsistency this row complains about WORSE. **(2) THIS ROW'S DEFERRAL RATIONALE IS HALF FALSE** — `deploy/` is six files and contains **zero** ingress hostnames; its only `vks.local` hit is a Harbor IMAGE placeholder that `50-seed-gitea-repos.sh:299` overwrites at seed time. No force-push is needed, and the live-HTTPRoute half is name-stable (`lib/istio.sh:339` renders keyed on `APP_NAME`, so `kubectl apply` rewrites `hostnames:` in place). The row has been deferred across cuts on a cost that does not exist. **(3) THE WALK COST IS ZERO** — classified mechanically against `walk-doc.sh`'s own parse rules: scenario-1 has **1** occurrence in a fenced block (a shell COMMENT at :811) and **0** in any `Expect:` line; scenario-2 has **0** and **0**. **0 of 8** doc occurrences are executed or asserted. **(4) THE DECIDING FINDING — the design is deliberately TWO-TIER and the `.local` trap never fires in it.** `show-dns-records.sh` emits real-DNS A records for **Harbor and ArgoCD only** and never for a `*.vks.local` name; scenario-1:48-49 says it outright — *"The `*.vks.local` names are `/etc/hosts`-only"*. Nothing crosses the boundary: `70-configure-argocd.sh:85` unsets the URL and resolves from the live Service, `23-argocd-preflight.sh:274` BLOCKS a cluster-local override, both Gateways are plain HTTP, and Harbor's cert carries an IP SAN. The mDNS refusal bites exactly one case — putting the UI names in REAL DNS — which this repo never asks for. **So: one sentence next to `APP_DOMAIN` stating the RFC 6762 reservation and the escape hatch, not a 54-file rename.** (The row's own "14 files" is stale ~4×: measured **54** repo-wide, **36** excluding docs.) ⚠️ **AND I MEASURED THE ROUND'S OWN minimal_fix INSUFFICIENT.** Its finding-1 fix — derive `GITEA_HOST=gitea.${APP_DOMAIN}` in `.env.example` — does NOT make the knob work, because **the derivation expands AT SOURCE TIME**: an operator's `.env` (sourced AFTER `.env.example`) arrives too late, and a command-line `APP_DOMAIN=` is clobbered by the uncommented value — the repo's own documented `.env.example` clobber rule, the same one behind B14 and B132. MEASURED: with `.env` setting `APP_DOMAIN=vks.test`, `GITEA_HOST` still resolved to `gitea.vks.local`. **I reverted that edit rather than ship a knob that LOOKS functional** — worse than an honest literal. The correct shape uses mechanisms the repo already has: add `APP_DOMAIN` to `load_env`'s SELECTOR SNAPSHOT (so an explicit choice survives sourcing), COMMENT the two host keys, and apply `${GITEA_HOST:-gitea.${APP_DOMAIN}}` AFTER the restore. That touches `load_env` and is a NEW design — it needs its own round. **SHIPPED from this round:** the `98-uninstall-all.sh` rot (finding 6) — it said `sed -i '/vks.local/d'` literally while `creds.sh:285` DERIVES the same line, so after any rename creds-show adds the new names while uninstall deletes the old ones and the stale entry survives teardown; now `/${APP_DOMAIN:-vks.local}/d`, default byte-identical. **STILL OPEN — NARROWED TO ONE ITEM 2026-08-18, by measuring the other two.** *(i) The one-sentence doc note ALREADY EXISTS, and is better than one sentence:* `.env.example:692-697` states the two-tier design outright — ArgoCD is NOT routed through the ingress and neither is Harbor, each keeps its own LoadBalancer, *"Reach ArgoCD at `ARGOCD_SERVER` / `ARGOCD_LB_IP`, not at a `*.vks.local` name"* — and even records that an `ARGOCD_HOST=argocd.vks.local` knob used to sit there with ZERO consumers. Nothing to write. *(ii) The `.local` guard in `show-dns-records.sh` has NO PRODUCER,* so building it would guard a case nothing in the repo generates. MEASURED: `:79-80` take the host from `HARBOR_URL`/`ARGOCD_HOST`/`ARGOCD_SERVER`, and **nothing** in `scripts/`, `docs/`, `.env.example` or the `Makefile` assigns any of them a `.local` value — the real producer is `06-install-harbor.sh:291` `state_set HARBOR_URL "$LB_IP"`, an **IP**; the only `.local` hit anywhere is a TEST fixture (`test-gateway-image.sh:40` `HARBOR_URL=h.local`). It is reachable only by an operator typing a `.local` name, which (i) already warns against. Real as a mechanism, unreachable in practice — grade it LOW/latent, do not build it as a HIGH. ⚠️ **And a stale premise of my own, corrected:** I had it that `.env.example` ships `HARBOR_URL=harbor.vks.local`. It does **not** — it ships `# HARBOR_URL=<SET-IN-.env>`; the old value survives only as a historical note in `lib/os.sh:471`. ⇒ **The only genuinely open item is the `load_env` `APP_DOMAIN` snapshot design**, which this row already says needs its own round. **SEQUENCING CONFIRMED:** stays OUT of the next cut — it buys the matrix nothing (0 executed literals) and `verify-ingress` is DNS-free by construction, while a green run on `vks.local` certifies what an operator actually gets. |
| **B69-orig** | 📎 **ARCHIVAL — the ORIGINAL problem statement for `B69`, which is still OPEN. The work is tracked THERE, not here; this row is kept only as the evidence base.** **`*.vks.local` is on a suffix RFC 6762 reserves for mDNS, and it blocks every resolver-based option.** MEASURED, the discriminating pair: `resolvectl query x.vks.local` -> *"No appropriate name servers"* (**NOT ROUTED**) vs `resolvectl query x.vks.test` -> NXDOMAIN (routed). Also measured here: `avahi-daemon` is **active** and nsswitch is `files mdns4_minimal [NOTFOUND=return] dns` — it works today ONLY because `/etc/hosts` short-circuits first, i.e. the very line we would remove. The repo is already inconsistent: Harbor uses `.test` (RFC 6761, correct), the ingress uses `.local`. **CUT-TIME change:** MEASURED **14 files**, not the 4 an adversary estimated (`.env.example` 14 · `test-creds-show.sh` 13 · `creds.sh` 12 · `Makefile` 10 · `lib/os.sh` 8 · 4 test files · `README`/`CLAUDE`), and `APP_DOMAIN` feeds both the live HTTPRoutes and the hostnames force-pushed into the seeded Gitea deploy repos — so changing it against a running lab means force-pushing the app repos and re-applying routes. ⚠️ **NOT on the critical path** for the recommended default: MEASURED, a proxied client never resolves at all (`curl --proxy` -> exit 7 *couldn't reach proxy*; unproxied -> exit 6 *couldn't resolve*), so a browser proxy sidesteps the mDNS trap entirely. |
| **B70** | ✅ **OWNER DECISION TAKEN 2026-08-18: option C — ADOPT `tkg.tanzu.vmware.com/tkg-registry-additional-ca-cert` AND MAKE IT THE DOCUMENTED VKS PATH, with KinD explicitly second-class.** The reframing this row was waiting on is answered; it is no longer owner-blocked. ⚠️ **What was offered was A / B / C, and B-vs-C was a bad split on my part — both adopt the annotation and they differ only in documentation emphasis. C is B plus: the manual CA wiring is demoted to a KinD-only fallback in the docs rather than presented as the VKS way.** So the buildable content of the decision is: (1) set the annotation on the VKS path so the guest cluster trusts Harbor's CA without the manual step; (2) keep the manual wiring working for KinD, labelled as the stand-in's fallback; (3) rewrite the CA-trust prose in `docs/scenario-1.md` so the annotation is the VKS instruction and the manual path is clearly the KinD one. **NOT STARTED, and it needs BOTH before a line is written:** an idea round (this is a new mechanism on the trust path, and Rule Zero applies most to the changes that feel like follow-through), and a **real lab** to verify — nothing in KinD can exercise a `tkg.tanzu.vmware.com` annotation, so the KinD e2e can only prove the fallback still works. ⚠️ **Accept the cost knowingly:** this deliberately WIDENS the stand-in/lab divergence, which is the thing that has bitten this project hardest. The KinD e2e will no longer exercise the documented VKS trust path at all, so a regression there is invisible until a lab run. Say that in the docs rather than discovering it later. ✅ **The DNS half of this row needs NO decision and is now measured** — see below; it splits per-CLIENT (`curl` does not need DNS, `git` does), so the docs get a correction, not a design. ⏸️ Original follows. — 🔴 **STAYS OPEN — its load-bearing claim RE-CONFIRMED 2026-08-16 (offline half only).** Verified: **nothing in either repo sets `tkg.tanzu.vmware.com/tkg-registry-additional-ca-cert`** — 0 code hits across `vks-airgap-cicd` and `nested-vsphere-lab` (`*.sh`, `*.ya?ml`, `Makefile`). The only occurrences anywhere are this BACKLOG file, B83's reference to it, agent worktree copies of the same file, and — a curiosity worth one line — a **dangling git object** at `nested-vsphere-lab/.git/lost-found/other/c5feb2ee…`, i.e. someone once wrote that annotation and the work was abandoned before it landed. So the row's premise ("VKS sets it, we do not, and we could") holds. ⚠️ **NOT re-verified here:** the DNS-is-not-required half (`curl -H 'Host: …'` → 200 for all four app hostnames, `--resolve` → 200, `git ls-remote` against the Gitea LB with no name). That needs a running ingress, and the certification matrix is mid-walk — touching the lab now would corrupt the run. ⏸️ **Not started, deliberately:** this is an owner REFRAMING of the project's minimum inputs, not a defect fix; it wants its own design + adversary rounds and an owner decision on scope, not a mid-certification start. Original follows. ✅ **THE DNS HALF IS NOW MEASURED (2026-08-18), and it SPLITS — the row's single "DNS is not required" framing is too coarse.** Measured on the live lab from the walkbox: **`curl` does NOT need DNS** (a `Host:` header against the LB IP is sufficient, which is what the row asserted), but **`git` DOES** — a clone/push names the host in the remote URL and there is no `Host:`-header equivalent, so an unresolvable name fails at transport before any HTTP verb. So the honest statement is per-CLIENT, not per-lab: the ingress path is reachable without DNS for header-capable clients and unreachable for URL-addressed ones. That matters because the GitOps loop is a **git** consumer, so "DNS is not required" cannot be carried over to it. The `tkg-registry-additional-ca-cert` half of the row is unaffected and still holds (0 code hits across both repos). | ⛔ **DO NOT SET THE ANNOTATION — the premise is measurably FALSE, and the annotation NAME is UNCORROBORATED. REFRAMED as a docs row, 2026-08-21.** **(1) The problem does not exist.** On a correctly-built lab the guest ALREADY trusts Harbor, and **VKS put the CA there**: `tkg-system/kapp-controller-config` key `caCerts` carries `CN = Harbor CA` with a SHA-256 **byte-identical** to our `secrets/harbor-ca.crt` (`31:B4:DB:16:...:C0:4D`) — verified independently twice. Neither this repo nor `nested-vsphere-lab` writes it (**measured**: `grep -rl additional-ca` → **0 files** in the lab repo, positive control passed on `harbor` → 5+ files). A fresh `imagePullPolicy: Always` pull succeeds in 112ms against `harbor.env1.lab.test/apps/gowebapp` — a real registry round-trip. So adopting the annotation buys **nothing measurable** while paying this row's own stated cost in full (*"deliberately WIDENS the stand-in/lab divergence, the thing that has bitten this project hardest"*) — a pure loss. **(2) The annotation name is UNCORROBORATED.** Broadcom's docs describe a **differently-shaped** mechanism: `spec.topology.variables` → `trust` → `additionalTrustedCAs`, backed by a Secret named `<CLUSTER-NAME>-user-trusted-ca-secret`, **double base64-encoded** for v1beta1 — and the platform **does not monitor changes** to that Secret, so rotating by editing it is a silent no-op. **Zero** sources confirm `tkg.tanzu.vmware.com/tkg-registry-additional-ca-cert` exists. An annotation that does not exist is **silently ignored by the API server** — on a trust path that is the worst failure mode, and B70 itself concedes the KinD e2e could never catch it. **(3) The manual wiring is NOT what this row thinks.** `cm/harbor-ca` in `ci` is a **pod-level mount for Kaniko** — a different consumer from node containerd. It is not redundant with node trust and must stay whatever happens here; demoting it to *"the KinD-only fallback for node trust"* would MISLABEL it. **SHIPPED instead:** the fact is recorded in `docs/vks-services/harbor.md` (lab-verified, with the fingerprint), and `make vks-trust-probe` re-measures it on any lab in ~15s. 🔴 **Residual, and it would change this row again:** node containerd trust vs containerd-configured-INSECURE is **UNDISTINGUISHED** — both produce an identical successful pull. The probe says so in its own output. Settling it needs node access this deliberately does not take. ✅ **OWNER DECISION 2026-08-21: LEAVE THE RESIDUAL, DISCLOSED. ROW CLOSED as a docs row.** The annotation is not adopted and will not be; the lab-verified fact (VKS propagates our Harbor CA - fingerprint match) is recorded in `docs/vks-services/harbor.md`, and `make vks-trust-probe` re-measures it on any lab in ~15s. ⚠️ **THE ACCEPTED RESIDUAL, and what would re-open this row:** node containerd CA-TRUST vs containerd configured INSECURE for this registry is **UNDISTINGUISHED** - both produce an identical successful pull, and the probe says so in its own output rather than letting a green over-read. Nothing depends on the answer today. **If it is ever settled and the answer is INSECURE, this stops being a docs row and becomes a real security finding** - at which point the correct mechanism is Broadcom's documented `spec.topology.variables` -> `trust` -> `additionalTrustedCAs` (Secret `<CLUSTER-NAME>-user-trusted-ca-secret`, **double base64-encoded** for v1beta1, and the platform **does not monitor changes** to it, so rotating by editing the Secret is a silent no-op), **NOT** the uncorroborated annotation this row originally named. Settling it needs guest-node access: `VCF_CLI_VSPHERE_PASSWORD` (absent from `.env`) or the `<cluster>-ssh-password` secret in the Supervisor's vSphere Namespace.
| **B70-orig** | 📎 **ARCHIVAL — the ORIGINAL problem statement for `B70`, which is still OPEN. The work is tracked THERE, not here; this row is kept only as the evidence base.** **THE MINIMAL CUSTOMER BASE — what we must be given, and what we must stop requiring.** Owner reframing 2026-08-05: normally we get only a vCenter IP + login + password for someone else's vSphere. **MEASURED: DNS is NOT required for our tooling** — `curl -H 'Host: …'` against the ingress IP returns 200 for all four app hostnames, `--resolve` returns 200, and `git ls-remote http://<gitea-lb>:3000/…` works with no name at all. The ONE hard name requirement is the **image reference** (containerd cannot set a Host header). **Trust is already solved by the platform:** `tkg.tanzu.vmware.com/tkg-registry-additional-ca-cert` on the Cluster carries `CN = Harbor CA` (notAfter 2036) and MEASURED **nothing in either repo sets it** — VKS does, and it is an annotation on the Cluster WE create, so we can set it for a customer's own registry with only a vCenter credential. **IP-based refs are dead** (Harbor's Supervisor Service `hostname` is an FQDN by contract; the IP-SAN two-phase pattern exists only in the KinD path; `newName` is force-pushed into the Gitea repos and only `newTag` is ever repaired; RFC 6066 forbids IP literals in SNI, so IP-only is incompatible with Contour, which our own runbook offers). ⚠️ One adversary INFERENCE refuted by measurement: the token realm is **request-derived** (`realm="https://<whatever-Host-you-send>/service/token"`), so it does NOT force a name. **RECOMMENDED DEFAULT: ask for ONE A record — Harbor's — not a zone**, and ship a local unprivileged browser proxy + PAC snippet instead of `/etc/hosts`. |
| **B72** | ⛔ **CLOSED 2026-08-05 — DO NOT BUILD, on two structural grounds verified directly.** Do not re-file. |
| **B75** | 🟢 **CLOSED 2026-08-17 by MEASUREMENT — correct AS SCOPED, INCOMPLETE as a class (see B137, filed one hour later when the matrix produced a counter-example at a SIBLING site). The prescribed work was already shipped and then some; the one real residual (an UNPINNED arm) is closed here.** I did not take the row's word for it; I checked each of its five prescriptions against the code and ran the suite. **(1) extend, do not fork** — exactly **1** definition of `classify_kube_failure` (`lib/os.sh`). **(2) anchor `401` AND move it below the network arm** — both done, and the code carries the measurement inline (60 invocations against a refused endpoint with no `401` in the address; **4 of 60** contained `401` from klog microsecond fields like `.380401` and classified UNAUTHORIZED while the other 56 classified UNREACHABLE — a heisenbug a rerun 'fixes'). **(3) kubectl's real expiry phrasing** — present. **(4) PLAINTEXT bucket** — present. **(5) 'all 7 sites across 5 files'** — now **18 sites across 11 files**, so coverage expanded well past what the row asked. The row's first HIGH (`argocd account can-i` discarding the x509-vs-RBAC discriminator, since both are rc=1) is **gone**: `grep -rn 'account can-i' scripts/*.sh` = **ZERO**. The second (a stale kubeconfig CA reported as 'unreachable') is distinguished at 5+ consumer sites via `STALE_CA`. **THE ONE THING THAT WAS ACTUALLY MISSING**, and a grep of the row would never have found it: the arm that catches kubectl's TRANSLATED 401 — its stderr contains neither `Unauthorized` NOR `401`, so it reads *error: You must be logged in to the server (the server has asked for the client to provide credentials)* — had **ZERO test cases**. The arm existed, was correct, and nothing would have gone red if someone deleted it. Two cases added (both phrasings, because the arm matches EITHER and an edit could drop one while the suite stayed green) and **RED-PROVEN in a sandbox: deleting the arm turns exactly those 2 cases red — `want UNAUTHORIZED, got UNKNOWN` — and leaves the other 18 green**, so they pin that arm and nothing else. Suite 18 → **20 passed, 0 failed**. The arm is load-bearing precisely because it is complementary, not redundant: kubectl's translated 401 carries NO numeric token, so the anchored-`401` arm below it cannot catch it — drop either and a real 401 classifies UNKNOWN. **Original row preserved below as the record.** ——— ORIGINAL ——— **The rebuild-staleness class is NOT closed, and the two paths that WERE fixed are the two whose consumers did the checking.** Two HIGH: a TLS-trust failure against argocd-server is reported as a PERMISSION denial (`argocd account can-i … >/dev/null 2>&1` — x509 and RBAC are both rc=1 and the discriminator is discarded), and a stale kubeconfig CA is reported as "the cluster is unreachable" when the cluster is up. Round verdict **ship-with-changes** with three corrections: **`argocd account can-i` EXITS 0 ON A DENIAL** (rc files a refusal under "permitted" — cited from upstream pinned to the installed v3.4.5); `91-e2e-tenant-mechanism.sh`'s RED-1 is a **negative control that passes vacuously** on any transport failure; and **redact-before-classify CHANGES the verdict** (measured: a 1-char secret turned STALE_CA into UNKNOWN). Also: the `401` arm collides with kubectl's **klog microsecond timestamp** — 4/60 = 6.7% non-deterministic on ANY kubectl failure — and, being arm #2, SHADOWS FORBIDDEN. Fix = extend `classify_kube_failure` (do not fork), anchor `401` AND move it below the network arm, add kubectl's real expiry phrasing (its 401 text contains neither "Unauthorized" nor "401"), add a PLAINTEXT bucket, and cover all 7 sites across 5 files with THREE behaviours, not one. |
| **B75-a** | 🔴 **`ARGOCD_CA_FILE` has ONE producer and ZERO consumers — and the e2e that would have caught it passes `--insecure`.** `argocd`'s global `--server-crt` is one line away, so "delete the target" is refuted: it would remove the only fix for B75's HIGH-1. The gate is green precisely because the only test exercising the `api` mechanism disabled the verification the operator path leaves ON. ✅/🔴 **RE-SCOPED 2026-08-17 — HALF of this row is FIXED, and the half that remains is now stated exactly.** **FIXED (measured):** `ARGOCD_CA_FILE` no longer has zero consumers. `argocd_tls_opts()` (`lib/os.sh:1200-1212`) turns it into `--server-crt` and is called at `70-configure-argocd.sh:27` and `23-argocd-preflight.sh:31`; the e2e consumes it at `91-e2e-tenant-mechanism.sh:224-232`. It is also in the loader's SELECTOR snapshot (`os.sh:499`) and the clobber gate (`check-env-clobber.sh:91`), and is correctly COMMENTED in `.env.example:391`. **ALSO FIXED:** the e2e no longer disables verification *silently* — `:224` PREFERS the CA and the `--insecure` fallback at `:234` now emits a loud WARN saying *"this leg therefore does NOT cover the trust-anchor path, which is the one that fails on a real lab"*, and the curl `--cacert`/`-k` pair is derived from the SAME branch so the CLI and the raw session call cannot disagree about whether the run verifies. 🔴 **STILL OPEN, and this is the whole residual: NOTHING MAKES THE E2E TAKE THE VERIFYING BRANCH.** `e2e-kind-tenant` (`Makefile:818-820`) is a bare call to the script and sets no `ARGOCD_CA_FILE`; the repo's own `test-fetch-ca-pin.sh:90` already records the measurement — *"no make target depends on fetch-harbor-ca/fetch-argocd-ca and no CI workflow invokes them"*. So by default the leg still takes `--insecure`; the difference from before is that it now SAYS SO instead of hiding it. That is a real improvement and it is not coverage. **CANDIDATE FIX (one line):** make `fetch-argocd-ca` a prerequisite of `e2e-kind-tenant` — it runs after `e2e-kind`, so a live ArgoCD exists to fetch from. ⚠️ **NOT APPLIED, deliberately:** it strengthens an e2e GATE, so it needs its own round plus a KinD verification that the stand-in ArgoCD's CA is actually fetchable — and it must not be changed mid-certification. Do it after the matrix, not before. 🔴 **REFUTED 2026-08-20 by an idea round (`refuted`); every claim re-verified independently. DO NOT APPLY THE CANDIDATE FIX — it is fatal at FOUR independent layers, any ONE of which aborts it before the verifying branch can run.** **(1) It dies at make PARSE time.** `secrets/.env.state` does not exist and the overlay include is `$(wildcard)`-guarded (`Makefile:108`), evaluated at parse time; make restarts only to remake INCLUDED makefiles. MEASURED — `make -n fetch-argocd-ca` expands to `ep=""; [ -n "$ep" ] || { echo "ERROR: set ARGOCD_LB_IP ..."; exit 1; }`, so even `make e2e-kind e2e-kind-tenant` in ONE invocation still sees it empty and aborts AFTER the whole e2e succeeded. **(2) `fetch-ca.sh` refuses to run unattended, BY DESIGN** — `:248` dies *"refusing to write an unauthenticated trust anchor with no terminal to confirm it on"*; with a TTY it BLOCKS on `read`. And `test-fetch-ca-pin.sh:90-92` states the rule verbatim: *"no make target depends on fetch-harbor-ca/fetch-argocd-ca ... If that ever changes, fix the CALLER — do not weaken this."* The candidate fix IS the change that falsifies that premise, and the obvious follow-on repair is exactly the weakening the comment forbids. A static `ARGOCD_CA_SHA256` is no escape either: argocd-server self-generates its cert per install, so a pinned run dies on the NEXT `kind-up` — a routine rebuild presenting as a suspected MITM. **(3) Even on SUCCESS it writes a FILE and exports no VARIABLE.** `ARGOCD_CA_FILE` ships COMMENTED in `.env.example` (correctly, per the clobber rule), so `load_env` never sets it, `argocd_curl_tls_init` still takes `-k`, and the leg still exports `ARGOCD_OPTS=--insecure`. A Makefile prerequisite is the WRONG LAYER — it cannot publish a variable to a sibling recipe. This is the house fake-green: it looks like coverage, delivers none, and CLOSES the row. **(4) Even with the variable set, the verifying branch CANNOT PASS.** The e2e dials the LB **by IP** (`91-e2e-tenant-mechanism.sh:206-208`) and the cert has **NO IP SAN** (`.env.example:412`, LAB-VERIFIED; B137 carries the live matrix error *`cannot validate certificate for 192.168.101.131 because it doesn't contain any IP SANs`*). Go verifies the HOSTNAME BEFORE the chain, so the name fault **MASKS** the anchor — still unexercised even in failure. **HIGH, also measured:** `e2e-kind-tenant` has **ZERO** prerequisites (`make -n` emits one line; it is at `Makefile:856`), so *"it runs after e2e-kind"* is a help-text CONVENTION, not a dependency — and adding the prerequisite makes the standalone diagnosis WORSE, dying with a message about a VARIABLE that sends the operator to edit `.env` when the real answer is *you have no cluster*. ✅ **THE DIRECTION THAT WORKS (not yet built, not yet reviewed):** port-forward `svc/argocd-server` and dial `localhost:<pick_port>` — `localhost` IS in the SAN set, so chain AND name verify with no `/etc/hosts` and no root (the `pick_port` pattern already runs at `99-verify.sh:87`) — and take the anchor from `argocd-server-tls` over the **authenticated kube API** (`kubectl -n <ns> get secret argocd-server-tls -o jsonpath='{.data.tls\.crt}' | base64 -d`), which has strictly BETTER provenance than fetching it off the connection it is meant to protect — so no TOFU consent is owed and none is prompted. This is B137's own residual (*the `/etc/hosts`→`argocd-server` route*) without needing root. 🔑 **SHIP THE POSITIVE CONTROL FIRST — the WARN IS NOT A GATE.** Nothing FAILS when the leg falls back to `--insecure` (`91-...:238-241` is `log_warn` + continue), so NO fix here is RED-provable and a future regression returns the leg to zero TLS coverage with a green exit. An opt-in `E2E_REQUIRE_TLS_VERIFY=1` strict arm — default OFF so it can never redden a certification — is what makes any later fix provable. **LAB-FREE RED-PROOF (layer 1, seconds, no cluster):** two self-signed certs in a tmpdir — A with `subjectAltName=DNS:localhost` ONLY, B with `IP:127.0.0.1` — served on 127.0.0.1 through the EXISTING `ARGOCD_SESSION_SCHEME` stub in `test-argocd-session.sh`. Four cases: (1) caA + `127.0.0.1` → rc=60 RED (reproduces the IP-SAN wall offline), (2) caA + `localhost` → 200 GREEN (proves the remedy), and the two that make it a measurement rather than a story — **(3) caB + `127.0.0.1` → GREEN**, without which you cannot tell *IP SANs are the problem* from *my stub is broken*, and **(4) an UNRELATED ca + `localhost` → rc=60**, without which a green in case 2 proves only that some file existed. ⚠️ **EVERY LINE CITATION IN THIS ROW HAS ROTTED** (facts hold, coordinates moved): `Makefile:818-820`→**856**, `os.sh:1200-1212`→**1625**, `.env.example:391`→~**429**, `os.sh:499`→**558**, `check-env-clobber.sh:91`→**102**. Third+ row in this file with that shape — read rows for SUBSTANCE, never by coordinate. ✅ **THE PRESCRIBED ROUTE IS NOW MEASURED ON REAL INFRASTRUCTURE (2026-08-20), not inferred.** The adversary's fix direction was graded `inferred` because it could not run anything; it has since been verified against the live lab's argocd-server. **The cert, read off the wire:** `subject=O = Argo CD`, `issuer=O = Argo CD` (self-signed, depth 0, verify error 18), `SAN: DNS:localhost, DNS:argocd-server, DNS:argocd-server.cicd, DNS:argocd-server.cicd.svc, DNS:argocd-server.cicd.svc.cluster.local` — **no IP SAN**, confirming B137 on this cut. **The discriminating pair, same anchor, one command apart:** `kubectl -n cicd port-forward svc/argocd-server <pick_port>:443` then `https://localhost:<p>/` with `--cacert` and **no `-k`** → **HTTP 200, fully verified**, while `https://<LB IP>/` → **HTTP 000**. So the port-forward-to-`localhost` route works and needs no root, no `/etc/hosts`, and no DNS. ⚠️ **AND PUBLISHING A DNS NAME WOULD NOT HAVE HELPED** — `argocd.env1.lab.test` is not in the SAN set either; only those five names verify. That refutes the obvious-looking alternative before someone spends a lab trip on it. 🔑 **A TRAP FOR WHOEVER BUILDS THE RED-PROOF:** the cert is `CA:FALSE`, so NSS/Chrome **reject it as a CA anchor** (`certutil -t "C,,"`) and it must be added as a **trusted peer** (`-t "P,,"`). Measured: with `C,,` Chrome shows `net::ERR_CERT_INVALID` / a Privacy-error page, which reads exactly like *ArgoCD's TLS is broken* and is in fact the instrument; with `P,,` the DOM is 275,793 B titled `Applications Tiles - Argo CD` with zero `ERR_*`. curl does NOT share this trap (`--cacert` accepts a self-signed leaf directly), so a curl-only proof will not surface it. **Auth over that verified channel, with the negative control:** `POST /api/v1/session` → 200 + `argocd.token`; cookie-only `GET /session/userinfo` → 200 `loggedIn: True`; wrong password → **401**. |
| **B75-b** | ✅ **DONE 2026-08-05 — `ca_verifies_endpoint` reported an endpoint that served NO CERTIFICATE as verified.** Plaintext → rc **4** (was 0: s_client prints both `no peer certificate available` AND `Verify return code: 0 (ok)`); missing/empty anchor → rc **5** (was 1 = "stale", the opposite remedy). ⚠️ The FIRST fix was WORSE than the bug — testing `no peer certificate available` alone collapsed THREE verdicts into one, because `-verify_return_error` aborts the handshake on a failed verification too. Only the regression arm caught it. New suite `test-ca-verifies-endpoint.sh` (7 cases, real oracles); the function previously had NO tests at all. |
| **B76** | ✅ **CLOSED 2026-08-17 (verdict DELETE) — was: Operator-UX, both repos: ETA from `TIMINGS.md`, `--debug` provenance, self-explanatory failures.** Not started. 🟢 **CLOSED 2026-08-17 — verdict DELETE, per an idea-round that refuted all three sub-items, and I verified its three load-bearing claims myself.** **(1) ETA from `TIMINGS.md`: DO NOT BUILD.** `docs/TIMINGS.md` **does not exist in this repo** — measured, the ONLY occurrence of the string `TIMINGS` anywhere here is this row. It lives in `nested-vsphere-lab`. Worse, this repo has already CONSIDERED AND REJECTED the design in code, by name: `scripts/lib/progress.sh:8-9` reads *"Progress is a COUNTER + ELAPSED timer, **never a fake ETA** — image/layer sizes vary 100x, so a per-item ETA would lie"*, and the endorsed alternative (`pg_init`/`pg_step`/`pg_done`) is already wired into the mirror scripts. In the repo where TIMINGS.md DOES exist the same estimate was **refuted five times** by measurement (7m45s · ~11m · 11m00s · 13m20s · 21m25s, monotonically upward) and the surviving design is a **range + dominant factor + a slow-vs-wedged discriminator**, not a number — the operator's real question is *is this stuck?*, not *how long?*. **(2) `--debug` provenance: DO NOT BUILD the flag; the SOURCE question is ALREADY DONE.** `creds.sh:250-255` computes DISCOVERED/STORED/DEFAULT and emits `values-provenance:` + `env-populated:`; `make state-show` prints the overlay, whose cluster wrote it, when, redacted. #777 settled the semantics **two commits before this round ran**. A flag would be an argv retrofit across ~175 scripts that today have no argument parsing at all, and per-value provenance would be CONFIDENTLY WRONG for selectors, which resolve caller-env-wins while everything else is last-file-wins. **(3) self-explanatory failures: the specific form is ALREADY BUILT AND GATED** — `classify_kube_failure` emits 8 classes and `check-classifier-consumers.sh` runs green over 7 consumers; the run-5/6 `STALE_CA` reports are that machinery WORKING. The **generic** form has no done-condition (558 `die` sites, ~8% name a next command) so it cannot be a row at all. **AND THE ROW IS STRUCTURALLY UNCLOSEABLE FROM HERE:** CLAUDE.md's own convention forbids tracking the other repo's work in this backlog, sub-item 1's ground truth exists only there, and sub-item 3 is already **nvl B441** — better specified, with a measured denominator and a gate this repo lacks. ➕ **The round's one actionable by-product SHIPPED here**: `state_show`'s redactor keyed on `PASSWORD|TOKEN|SECRET` and printed `GPG_PASSPHRASE`, `GITHUB_PAT` and `TLS_KEY` **in full** — adequate only by coincidence, since our own naming rules mandate `GPG_PASSPHRASE` and keep `GITHUB_PAT`. Widened, and pinned in BOTH directions by a new `test-state-overlay.sh` case (nothing had ever exercised `state_show`), RED-proven. |
| **B77** | 🟢 **CLOSED 2026-08-19 — ALL FIVE FINDINGS ARE SETTLED. The 'browser flow was dropped' gap I raised was MY ERROR, corrected by the owner within minutes.** I read the review's finding-1 Action (*'KEEP the browser flow … the web-UI path is still wanted (owner, 2026-08-04)'*), measured that `scenario-1.md` no longer contains it, and filed that as a gap needing an owner decision. **Wrong.** The browser/vSphere-Client path is an **OPTION, not the default** — and making the REST path the default with the browser as an alternative is precisely what finding 1 ASKED FOR. A runbook that documents the default path is complete without it; its absence is the Action being satisfied, not violated. ⚠️ **The failure mode is the one this file names repeatedly: I manufactured a known-gap out of a stale line and handed it to the owner as a decision.** A 2026-08-04 sentence describing what was wanted BEFORE the capability landed is not evidence about what is wanted after it landed, and I had already written exactly that possibility into the row (*'may simply be stale'*) without acting on my own caveat. **Nothing in B77 is open.** ⏸️ Original follows. — 🟡 **RE-DERIVED FROM THE SOURCE 2026-08-19 — FOUR OF THE FIVE FINDINGS HAVE LANDED.** Since the row's section numbers no longer exist, I walked `docs/reviews/2026-08-04-first-real-lab-measurements.md` (the source B222 says to start with, present here, five numbered findings each with its own Action) against today's `docs/scenario-1.md`, checking each rather than inventing corrections. **Finding 1 — the capability: LANDED.** The review's own measurement was `supervisor-services` → 6 hits ALL in docs, ZERO in code and `namespace-management` → 0 anywhere; re-run today: **23** and **16** hits in code, across `deregister-`/`list-`/`uninstall-`/`unwedge-supervisor-service.sh`, `show-dns-records.sh` and the two install scripts. §2/§4/§5 are scripted-first (`make vsphere-namespace`, `make install-harbor-service`, `make install-argocd-service`). **Finding 2 — LANDED, and STRONGER than the remedy it asked for.** The review warned that `yq '.k = v'` **creates** a missing key, so a renamed expose key appended two dead top-level keys and shipped a Harbor with no reachable ingress and no error. `04-install-harbor-service.sh:60-85` does not use `yq` at all — anchored `awk` patterns, which cannot create a key — followed by a PROPERTY assertion (*'not one vendor placeholder may survive'*) plus explicit `grep -q '^hostname: …'` and `grep -q '^enableNginxLoadBalancer: true$'` checks. That is the failure class closed by construction rather than by a lint. **Findings 3 and 4 — closed in the review itself** (STEP 13 answered, the README claim holds and needs no rewrite; B26's anti-injection defence confirmed working). **Finding 5** is B2's `GATEWAY_API_VERSION` pin, tracked there. 🔴 **THE ONE REAL GAP, and it needs YOU: the browser flow was DROPPED, against an Action that explicitly said keep it and cited you.** The review's finding-1 Action reads *'**KEEP the browser flow** — it stays the documented fallback and the web-UI path is still wanted (owner, 2026-08-04)'*. Measured: `docs/scenario-1.md` now contains **one** incidental use of the word 'browser' (`:889`, about a backend check) and **zero** occurrences of 'vSphere Client', 'Add New Service' or 'Manage Service'. It did **not** relocate — `git log -S'vSphere Client'` shows `72c0530` (*'rewrite scenario-1 as a runbook, and move the rationale to its own file'*) removed it, and the file that commit created, `docs/scenario-1-notes.md`, mentions the vSphere Client exactly once and only about storage-policy naming. ⇒ **Restoring it is a judgement call that is yours:** that same rewrite cut the runbook from 949 lines specifically to be concise, and the REST capability the browser flow was a fallback FOR has since landed — so 'still wanted (2026-08-04)' may simply be stale. **Decide:** (a) restore the browser fallback into `scenario-1-notes.md` and link it from §2/§4/§5, (b) declare it superseded by the REST path and record that in the review file so nobody re-derives this, or (c) leave as-is. **Nothing else in B77 is open.** ⏸️ Original follows. — 🔴 **ITS REMAINING HALF CANNOT BE EXECUTED AS WRITTEN — the section numbers it cites NO LONGER EXIST (measured 2026-08-18).** The row asks for "the §5–§9/§11 corrections" and its source (nvl's B222, `docs/EVIDENCE-OPERATIONS.md:63-68`) lists what landed by the SAME stale numbering: §3 (`--ca-certificate` + the vcf traps), §4 (guest kubeconfig from the secret), a new **§4a** (namespace + guest cluster), §5 (PSA has TWO correct answers), §6 (a Supervisor-Service Harbor serves ONLY its leaf), §8 (`ARGOCD_NAMESPACE` must be DISCOVERED). **`docs/scenario-1.md` today has 13 sections and none of those numbers hold**: §4 is *Harbor*, §5 is *ArgoCD*, §6 is *Guest cluster*, §8 is *Harbor's CA*, and there is no §4a at all. Grepped for the landed corrections' own marker phrases: *"two correct answers"* **0 hits**, *"only its leaf"/"correctly refuses"* **0 hits**, `§4a` **0 hits** — while `ARGOCD_NAMESPACE` and `--ca-certificate` are present (1 each). So some corrections are in and some are not, and **the row's coordinates cannot tell you which**. ⇒ **DO NOT map the old numbers onto the new sections by guessing** — inventing corrections against a restructured document is worse than leaving it. **The only sound way to finish this row is to re-derive from the SOURCE against the CURRENT document**: `docs/reviews/2026-08-04-first-real-lab-measurements.md` (which B222 itself says to START BY READING; it is present here, 7876 bytes, and its findings are numbered 1–5 with their own actions), walked section by section against today's `docs/scenario-1.md`. That is real work, not a mechanical edit, and it wants its own idea round because "correct a runbook against a lab measurement" is a judgement act. ⚠️ **This is a fourth instance of B175's class in one day** (after B145, B166's already-merged part (i), and this) — a row authoritative enough to commission work whose own coordinates are stale. Here the drift is not closure language but SECTION NUMBERS, which no closure-word sweep can detect. ✅ **The blocker the row last recorded is GONE:** it said the doc half waits because the walk harness `scp`s `docs/scenario-1.md` at every row and editing mid-matrix hands different rows different documents. Verified 2026-08-18: the matrix has exited (`/tmp/walk/runwatch.log` → `MATRIX EXITED`), no `walk-matrix.sh` process exists, and **0 walk VMs are running** (one orphan was found and reclaimed — see **B192**). ⏸️ Original follows. — 🔴 **Fold the scenario-1 walk results back into the docs + `.env.example`.** Inputs already exist (the walks against CUT #8 and CUT #9, and B222's §1→§11 pass) — **no new lab needed** for the §5–§9/§11 corrections or the `.env.example` work. Carry this lab's values as working defaults while the prose stays parameterised: respect `check-env-clobber` (per-run knobs stay COMMENTED) and `check-env-coverage`. Storage policy and SSO domain are **parameters, not constants** — two real labs disagree (`wcp-vmfs`/`vsphere.local` here vs `vsan-default-storage-policy`/`wld.sso` there); present both and give the discovery command. Grade Broadcom facts `9.0-doc (inferred for 9.1)` — the 9.1 URLs 301-redirect. ⚠️ The subset of B222's 7 findings that are visible ONLY on a rebuilt lab (5 of them) IS gated on the next cold cut. ⚠️ **THIS ROW CARRIES A MEASURED-FALSE INSTRUCTION — DO NOT FOLLOW IT. Its grading directive would CORRUPT provenance, in a repo whose whole discipline is provenance.** The row says *"Grade Broadcom facts `9.0-doc (inferred for 9.1)` — the 9.1 URLs 301-redirect"*. MEASURED 2026-08-17 against URLs THIS REPO ALREADY CITES (the record holds **19** distinct `/9-1/` URLs and 16 `/9-0/`, and `check-vks-provenance` reports all 79 fact rows carry a resolvable `[src:]` token): three cited `/9-1/` pages return **HTTP 200 with NO redirect** — `9-1/using-argo-cd-service/install-argo-cd-service.html`, `9-1/using-harbor-as-vcf-service/installing-and-configuring-harbor-and-contour.html`, and `9-1/release-notes/vmware-vsphere-supervisor-release-notes.html`. **And the status code alone would not have settled it** — a 200 could serve 9.0 content under a `9-1` path — so the discriminator is the delivered page's content: the three 9-1 pages carry **37 / 43 / 79** references to 9.1 against **3 / 3 / 38** to 9.0, while the `/9-0/` sibling of the first inverts it (**2** vs **45**). The two trees serve genuinely different material. ⇒ **`/9-1/` URLs do NOT redirect**, and grading a fact sourced from one as `9.0-doc (inferred for 9.1)` would be strictly worse than the truth. CLAUDE.md already records this belief as measured FALSE on 2026-07-14 (*only `/latest/` 301s into the `/9-0/` tree*) — so the correct reading is that **CLAUDE.md is current and this row is stale**, not the reverse. The rest of B77 (fold the walk results into the docs + `.env.example`, keep per-run knobs COMMENTED for `check-env-clobber`, treat storage policy and SSO domain as PARAMETERS because two real labs disagree) is unaffected and still stands. ⚠️ A caveat that survives: some **deep** package-reference and `vcf`-CLI pages exist only in the `/9-0/` tree, so a fact taken from THOSE is legitimately `9.0-doc (inferred for 9.1)` — the rule is **grade per URL you actually opened**, never per version-family. 🟢 **RE-SCOPED 2026-08-18 BY VERIFICATION — the `.env.example` HALF IS DONE, so this row's remaining work is the §5–§9/§11 doc corrections ALONE.** The row asks for three things in `.env.example`: carry this lab's values as working defaults, keep the prose parameterised, and respect `check-env-clobber` + `check-env-coverage`. All three are already satisfied, verified against the file rather than assumed. **Storage policy** (`:1219-1228`, `:1350-1356`): BOTH labs' values are presented side by side (`wcp-vmfs` single-host VMFS *this lab* vs `vsan-default-storage-policy` vSAN *the other lab*) with an explicit warning that copying either is a coin flip; the **normalization rule** is spelled out (the vSphere Client shows the policy `vSAN Default Storage Policy`, the class is `vsan-default-storage-policy` — lowercased, spaces to `-`) together with why it matters (the UI string pasted verbatim is not a valid Kubernetes object name, so **no PVC ever binds**); the **discovery command** is given for both phases — the vSphere Client when you have no Supervisor kubeconfig yet (scenario-1 §2 needs the value BEFORE §3 authenticates) and `kubectl get storageclass` to re-check once you do; the downstream consumer is named (Harbor's data-values `insert-storage-class-name-here`); and `VKS_STORAGE_POLICY` carries a `# how:` (vCenter → Policies and Profiles) and stays **COMMENTED**. **SSO domain** (`:1152-1166`): the file states in as many words that **the SSO domain is a PARAMETER, NOT a CONSTANT, and the built-in default is only one lab's answer**, with both MEASURED values (`vsphere.local` on one 9.1 lab, `wld.sso` on the other) and the reason a wrong guess is expensive (a plausible-but-wrong principal takes a real password at an interactive prompt, then fails elsewhere or succeeds as the wrong user); the discovery path is given (vCenter → Administration → Single Sign On → Users and Groups → the *Domain* dropdown) **including the honest note that there is no CLI for it we can run here**; `vks_username()` WARNS every time it applies the default rather than accepting it silently; and both `VKS_USERNAME` and `VKS_SSO_DOMAIN` stay **COMMENTED** so a `make vks-login VKS_USERNAME=…` override is not clobbered. **Gates re-run 2026-08-18, all rc=0 with real denominators:** `check-env` (present), `check-env-coverage` (every operator-settable var documented), `check-env-clobber` (**54** uncommented values, none shadowing a fallback or a per-run override), `check-how-provenance` (**134** acquisition lines, each ours / a real make target / provenance-tagged). ⇒ **Do not redo the `.env.example` work.** ⚠️ **What REMAINS is genuinely blocked, and not by an owner decision:** the §5–§9/§11 corrections edit `docs/scenario-1.md`, which is one of the four files the running walk harness `scp`s **from the local tree at every row** (`walk-matrix.sh:497`), alongside `docs/scenario-2.md`, `scripts/walk-doc.sh` and nvl's own `walk-matrix.sh`. Editing it mid-matrix would hand different rows different documents — the precise failure that killed run 2, when one line of prose changed under a running matrix. So the doc half waits for the run to finish; it is **not** waiting on a lab, and the row's own "no new lab needed" note stands. |
| **B81** | ✅ **SUBSTANTIALLY FIXED — re-measured 2026-08-16. The producer the row says does not exist NOW EXISTS, and the doc gives the reader its command.** `make fetch-supervisor-ca` is `Makefile:536` (*"Fetch the CA that signed the Supervisor's cert FROM VCENTER (scenario-1 §3.3)"*), backed by `scripts/fetch-ca.sh`; **`docs/scenario-1.md:221` gives the command in Step 3**, and `:398` names what it wrote (`VKS_CA_CERT_FILE`). scenario-2 references it once. The row's cited line numbers (`:209`, `:614`) have also drifted and no longer say what it quotes — the same enumerated-line rot as B101. So the reader is no longer sent in a circle. ⚠️ **ONE RESIDUAL, and it is a present-tense claim that is now FALSE:** `scripts/lib/tls.sh:90-91` still states *"Nothing in the repo re-fetches it"*. It sits inside a dated incident narration (`MEASURED 2026-08-05: after a lab rebuild, secrets/supervisor-ca.crt still held the DESTROYED lab's VMCA…`), which per the house rule is history and must NOT be rewritten — but that one clause is **present tense**, so a reader takes it as a current fact and concludes there is no producer. **Minimal fix: tense it** (*"nothing in the repo re-fetched it at the time — `make fetch-supervisor-ca` now does"*), preserving the incident record while correcting the live claim. **MERGE HELD** — it touches `scripts/`, and the certification matrix is walking now and clones the repo per row. Note my own grep for the phrase returned ZERO because it spans two comment lines; the location came from reading, not matching. Original row follows. |
| **B81-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B81`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **§8's VMCA pointer is CIRCULAR — the doc sends you round in a loop and never gives a command.** MEASURED (`adversary-bash-git-cli`, 2026-08-05): `docs/scenario-1.md:209` says the Supervisor's VMCA root *"see §8 for getting it"*; `:614` says *"re-obtain it (Step 8)"*; and §8 itself (`:570-600`) says only *"it is the Supervisor's VMCA root"* — **no command, no producer.** `command grep -rn 'supervisor-ca.crt' Makefile scripts/` returns ONE hit, a comment, and `scripts/lib/tls.sh:87` states verbatim *"Nothing in the repo re-fetches it"*. So a reader following the docs cannot obtain the artifact the docs require. Independent of B64 (found while attacking it) and it does NOT need a lab to fix: either write the producer (a `fetch-supervisor-ca` target, matching the existing `fetch-harbor-ca`/`fetch-argocd-ca`) or state plainly in §8 that it is operator-supplied and how. ⚠️ The obvious producer — fetching the VMCA over the very connection it is meant to anchor — is **TOFU**; decide that deliberately, do not let it appear by default. ⚠️ **AND IT IS WORSE THAN A DOC LOOP — there is a RUNTIME `die` naming a target that DOES NOT EXIST.** MEASURED 2026-08-05: `scripts/30-vks-login.sh:196` tells the operator *"Point VKS_CA_CERT_FILE at the Supervisor's CA (**make vks-ca**, or ask the platform team for ...)"* — and `command grep -nE '^vks-ca:' Makefile` returns **NOTHING**. An operator who hits that error and follows its instruction gets `make: *** No rule to make target vks-ca`. That is strictly worse than the §8 loop, which at least fails silently. **Fix it in the same change**, and note the class: no gate caught a `make <target>` string inside a `die` — `check-doc-make-targets` scans DOCS, not runtime messages; whether to extend it to scripts is its own call. ⚠️ **The B81 adversary round was DISPATCHED and STOPPED mid-run at the handoff, so this finding is the only part of it that survived — the round still needs running before any fix is written.** Two things it must settle: (a) `fetch-ca.sh` is TOFU by its OWN comments (*"reduces to verify(X,X) -> OK, rc=0 for ANY self-signed cert, attacker's included"*), which Harbor and ArgoCD already accept — but this anchor carries the vSphere ADMIN password, so a convenient target nobody fingerprint-checks may make the posture WORSE than the honest gap; (b) whether the Supervisor kube-apiserver cert is issued by the SAME VMCA root `--ca-certificate` wants, or a different intermediate — if different, fetching against `SUPERVISOR_HOST` writes the WRONG anchor and it would still "verify" by construction. |
| **B78** | ✅ **CLOSED BY VERIFICATION 2026-08-18 — SHIPPED, and its blocker B64 is closed too.** `make vsphere-namespace` → `04-vsphere-namespace.sh` (Makefile:466, "Create the vSphere Namespace over the vCenter REST API (scenario-1 §1b). Idempotent: reports and does NOT rewrite an existing one") and `make vks-cluster-create` → `25-vks-cluster-create.sh` (Makefile:536, "Provision the guest VKS cluster from the VKS_* topology keys (scenario-1 §4b); server-side dry-run gates it"). **The strongest evidence available: the six-row certification was EXECUTING BOTH while this row was being read** — row-1 log lines 598 (`$ make vsphere-namespace`) and 876 (`$ make vks-cluster-create`). Original text: §4a/§4b CONSUMER — the script that creates the namespace + guest cluster. |
| **B82-a** | 🔵 **The install half IS automatable — `nested-vsphere-lab` already installs Supervisor Services over the vCenter REST API, no web UI.** MEASURED 2026-08-09 (`grep` of that repo's Makefile): `services` (*"Register + install Supervisor Services over the vCenter REST API (no web UI)"*), `services-remove SERVICE=<id> CONFIRM=yes` (uninstall + deregister), `services-uninstall`, `services-status`, `services-deactivate/activate/deregister`, plus `istio-uninstall WL_NS= WL_GC= CONFIRM=yes` and `namespace-delete`. So scenario-1 Steps 2/3's INSTALL half — which every walk to date has skipped because the lab already had Harbor and ArgoCD (both Supervisor Service namespaces measured **19 h old** and carrying **no** `vks-airgap-cicd.local/owned-by` label) — can be exercised end to end: remove the services, install them from this repo, verify. ⚠️ Two things to settle first: (a) whether this repo should DEPEND on the lab repo (it must not — borrow the REST call shape, do not shell out to another repo's Makefile); (b) the data-values files carry credentials (`supervisor-service-harbor-data-values-*.yml` sets the admin password), so whatever we build inherits the secret contract (`umask 077`, never argv, never `.env`). Owner 2026-08-09. |
| **B82** | ✅ **ITS CENTRAL CLAIM IS NOW FALSIFIED BY EXECUTION — 2026-08-16.** The row says the Supervisor-Service INSTALL half *"has never been executed by anything"* because every walk to date found Harbor and ArgoCD already installed. The certification matrix now cuts a lab **and does not install the services** (`make lab` ≠ `make services`), so row 1's NOTHING cell FORCES that half. MEASURED live in `MATRIX-row1-ubuntu.log`: block **[12]** `make install-harbor-service` → `service: harbor.tanzu.vmware.com version 2.14.3+vmware.2-vks.1 (CARVEL_A…)` → `registered` → `install issued for harbor.tanzu.vmware.com` → `published HARBOR_USERNAME/HARBOR_PASSWORD to the state overlay`, **rc=0**, both Expects SEEN (`7 secrets generated, 0 placeholders left`; `install issued for harbor.tanzu.vmware.com`). Block **[15]** `make install-argocd-service` → `registered argocd-service.vsphere.vmware.com` → `install issued` → `ArgoCD CRD is present` → `version from the published Carvel Package: 3.0.19+vmware.1-vks.1` → `argocd.argocd-service.vsphere.vmware.com/argocd-1 created`, **rc=0 (42s)**, Expect SEEN. It consumed exactly the files the row lists as never-used — `supervisor-service-harbor-legacy-v2.14.3+vmware.2-vks.1…yml`, `supervisor-service-harbor-data-values-v2.14.3.yml`, `supervisor-service-argocd-legacy-1.1.0-25100889.yml`. So the capability exists (they are repo `make` targets) AND is now exercised end-to-end on a real Supervisor. ⚠️ **Scope of the claim:** these two blocks are definitively green; the ROW they sit in was still walking when this was written, so this certifies the install half, not the whole row. Original follows. |
| **B82-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B82`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **Install the Supervisor Services (Harbor, ArgoCD) from this repo — Steps 2 and 3's install half, which NOTHING here exercises.** MEASURED 2026-08-09 walking scenario-1 on a bare photon jump box: of the 37 files in the operator's Broadcom folder, the jump box consumes **5** (the VCF CLI tarball, the plugin bundle, argocd-cli) and the other **~30 are vCenter-side Supervisor Service definitions** (`supervisor-service-harbor-*.yml`, `supervisor-service-argocd-*.yml`, contour, extdns, vks, plus their `*-data-values-*.yml`). Every walk to date has SKIPPED that half because the lab already had Harbor and ArgoCD installed, so the runbook's Steps 2/3 install path has **never been executed by anything in this repo**. Owner 2026-08-09: this is the likely next step, and the mechanism can be borrowed from `AndriyKalashnykov/nested-vsphere-lab` — but **minimally relevant to this repo**: take the service-install call shape, not that repo's lab-provisioning scope. ⚠️ Before designing it, settle two things that decide whether it belongs here at all: (a) installing a Supervisor Service is a **vCenter-admin** action, and scenario-1's own Step 1b already forks on the reader NOT being an admin — so this may be an admin-only sub-path, not a step every reader runs; (b) the data-values files carry **credentials** (`supervisor-service-harbor-data-values-*.yml` sets the admin password), so a target that applies them inherits the secret-handling contract (`umask 077`, never argv, never `.env`). |
| **B83** | 🟠 **ITS BLOCKING QUESTION IS NOW SETTLED — measured on a REAL VKS Harbor 2026-08-16 (ns `svc-harbor-0gmiw`), and the answer FAVOURS the swap.** The row gates finding (a) on *"⚠️ Settle first: if `harbor-tls`'s `tls.crt` == its `ca.crt`, that secret is ALSO a CA keypair and the swap is cosmetic — one `openssl x509 -noout -subject -issuer` decides it."* Decided, read-only, public certs only: <br>• `harbor-tls` → `ca.crt` = `CN = Harbor CA`, self-issued, **CA:TRUE**, sha256 fp `1C:68:41:F0:95:89:CA:2F…`; `tls.crt` = **`CN = harbor`, issued by Harbor CA, CA:FALSE** — a LEAF, fp `3B:51:5D:6F…`. <br>• `harbor-ca-key-pair` → `ca.crt` **and** `tls.crt` are BOTH `CN = Harbor CA`, CA:TRUE, and carry the **SAME fingerprint `1C:68:41:F0…`**. <br>⇒ `harbor-ca-key-pair` **is** a CA keypair, so its `tls.key` really is the **CA's private signing key**, and finding (a) is CORRECT about what that Secret contains. And `harbor-tls` carries a **byte-identical `ca.crt`** (same fp) while its `tls.key` is only the LEAF's. ⚠️ **CORRECTED 2026-08-16, AND THIS ROW HAD ALREADY DELETED THE CORRECTION ONCE — do not delete it again.** An earlier version of this row called the swap *"a real least-privilege win"*. **That is wrong, and B83-orig BELOW already said so** — verbatim: *"the exposure is the **RBAC GRANT**, not the read — k8s has no field-level authz on Secrets, so anyone who can run the script can already read `tls.key` themselves; the jsonpath constrains what the SCRIPT writes, never what the OPERATOR can read."* Writing this row on 2026-08-16 dropped that sentence, so the newer entry read as current truth and was the *less* accurate one — the `configuration.md` §"PRUNING a doc DELETES THE CORRECTION" shape, at row granularity. **The honest claim: the swap reduces EXPOSURE (on the happy path the CA signing key never crosses the wire and never enters kubectl's memory), which is a PRECONDITION for a least-privilege grant — it is not the grant.** A privilege win would require a `Role` with `resourceNames: ["harbor-tls"]` plus the doc line telling a platform admin to grant that; no such Role exists anywhere in the repo. ⚠️ **Finding (b) is HALF WRONG, and the half that is right still matters:** the row says `harbor-tls` *"does NOT reliably carry `ca.crt`"* — on this **VKS-installed** Harbor it DOES (keys measured: `['ca.crt','tls.crt','tls.key']` on both secrets). But the row is right about **this repo's own KinD Harbor**, which `06-install-harbor.sh:202` creates with `kubectl create secret tls` (tls.crt/tls.key only). ⚠️ **BUT my justification for the ladder was ALSO wrong, and B83-orig had said this too:** I wrote *"a hardcoded `harbor-tls` would break KinD"*. **Measured FALSE, independently:** `06-install-harbor.sh` mentions `harbor-ca-key-pair` **0 times** (KinD never creates it, it is a cert-manager-shaped name only the VKS install produces), and sets the discovery label `appplatform.vmware.com/serviceId` **0 times** — that label appears ONLY in the two READERS (`27`, `28`), so discovery cannot reach KinD's namespace at all. KinD is therefore **already dead at rung 3**; hardcoding `harbor-tls` would move it from *"dies at rung 3: extracted ZERO bytes"* to *"dies at rung 2: no `ca.crt`"*. **Both die — there is no KinD regression for the ladder to prevent, so that argument must not be used to justify it.** 🔴 **WRONG RIGHT NOW, INDEPENDENT OF THE LADDER, and the cheapest thing here — 4 sites point the operator at the WRONG SECTION.** Verified: `## 6.` is **Guest cluster**, `## 8.` is **Harbor's CA**; yet `27-harbor-ca-from-cluster.sh:8`, `:13`, **`fetch-ca.sh:116` (an operator-facing `die`)** and **`Makefile:556` (`make help`, a copyable artifact)** all say **§6** for the Harbor CA. A one-token `§6→§8` fix in four places; ship it separately from any ladder. (Keep `fetch-ca.sh`'s die GENERIC — it is shared with ArgoCD and `:96-99` explains why — fix only its section number.) ⚠️ **And the ladder itself is REFUTED AS SCOPED — the round split it into three:** **(A) do now:** the real defect on *every* path through `27` today is that it never checks the candidate **VOUCHES for Harbor** — `ca_anchor_reject_reason` is source-blind and was MEASURED to ACCEPT both an unrelated `CN=Acme Corporate Root CA` and a stale `CN=Harbor CA` from a dead lab. `ca_verifies_endpoint` already exists (`lib/tls.sh:116`, typed rcs) and `fetch-supervisor-ca.sh:131-148` is a ready template; `27` simply skips it, while scenario-1 §8's primary route guards exactly this (*"same name, wrong file"*, `:571`). **(B) separate:** rung 2 by CONTENT-based discovery, not by name — `28-harbor-admin-password.sh:125-138` records that this namespace carries kapp-VERSIONED secrets (`-ver-N`), so a hardcoded `harbor-tls` is a name-stability bet that fails silently around a cert rotation, exactly when the ladder is meant to help. **(C) a row, not code:** rung 1 is unbuildable today — `27` reads neither `VKS_NAMESPACE` nor `VKS_CLUSTER_NAME` (both commented placeholders), so it cannot NAME the Cluster CR, and B70's annotation claim is `UNVERIFIED-for-this-use` until someone prints one from a lab. **Also: the shipped `test-ca-anchor-validation` covers the DECISION and says so itself — it does NOT cover SELECTION, which is the entire ladder.** **Still open: (A), then (e)'s remaining sites.** That is a code change to `27-harbor-ca-from-cluster.sh`, so it needs its own adversary round and the merge is held while the certification matrix walks. (c)/(d) already shipped 2026-08-13. Original follows. ⚠️ **THE SWAP IS REFUTED AS SCOPED — do NOT ship it standalone (idea round, `adversary-security-secrets`, 2026-08-17).** My reading of the measurement SURVIVED: `harbor-ca-key-pair` really is a CA keypair and `harbor-tls`'s `ca.crt` really is the same anchor — the round could not break that, and the *"the swap is cosmetic"* branch of this row's settle-first is closed. What it refuted is the STEP FROM THERE TO "so swap it": **(1) the equality is ONE LAB, ONE CUT, and NOTHING IN THIS REPO establishes what PRODUCES either Secret** — zero cert-manager `Certificate`/`Issuer` CRs, zero VKS package manifests; the only support is that `harbor-ca-key-pair` matches cert-manager's canonical example NAME, which is an inference from a naming convention (the *grep-found-the-string* shape). Two things narrow it: `kubectl create secret tls` emits exactly `tls.crt`/`tls.key` (**measured**), so a `harbor-tls` carrying `ca.crt` was written by a CONTROLLER; and the plain goharbor chart with `certSource=auto` produces no separate `harbor-ca-key-pair` at all. Honest grade: **UNMEASURED**. **(2) The benefit is conditional on an artifact that does not exist** — it becomes a privilege win only with a `Role` carrying `resourceNames: ["harbor-tls"]`, and `git grep` finds none. **(3) It retargets the read from the object LEAST likely to churn to the one MOST likely to** — the CA is valid to 2036, leaves rotate — while `28-harbor-admin-password.sh:125-138` already handles `-ver-N` kapp versioning for a sibling secret and `27-` does not. **(4) The real cost: it invites softening the sentence *"route B costs an admin-level grant"*, which REMAINS TRUE after the swap** (the grant is namespace-wide `get secrets`) and is the only thing steering operators to the cheap UI route. **THE ONE COMMAND THAT SETTLES IT** (read-only, no key material): `kubectl -n <ns> get secret harbor-tls harbor-ca-key-pair -o jsonpath='{range .items[*]}{.metadata.name}{"\t"}{.metadata.annotations}{"\n"}{end}'` — `cert-manager.io/*` ⇒ mechanism, swap is sound; `kapp.k14s.io/versioned` ⇒ finding (3) fires; neither ⇒ the equality is a coincidence and the swap is a bet. **Queued for the lab.** ⚠️ **AND TWO ITEMS ON THIS ROW ARE STALE:** *(A) do now: 27 never checks the candidate VOUCHES* has SHIPPED (`27-harbor-ca-from-cluster.sh:119-163`), and the §6→§8 fix has shipped at `Makefile:556`. Close them before someone re-implements them. Also correct this row's reason for *"KinD is dead at rung 3"*: **measured**, `06-install-harbor.sh` sets `appplatform.vmware.com/serviceId` ZERO times so label discovery cannot reach KinD — TRUE — but `:55` is an escape hatch that bypasses discovery, so what actually makes KinD unreachable is the **Supervisor-kubeconfig requirement at `:49-51`**, not the label. ➕ **NEW, FOUND BY THE B66 ROUND (2026-08-17) — filed here because it is Harbor-install, not B66's install-vs-consume: `04-install-harbor-service.sh:110-117` guards REGISTER for idempotency but calls `vc_ss_install` UNCONDITIONALLY.** `vc_ss_register` is wrapped in `if vc_ss_is_registered ...; else ... fi`, but the `vc_ss_install` on the next line has no already-installed check and POSTs with `$VALUES` — which on a wiped `.env.state` (lab rebuilds are routine here) carries **freshly generated secrets**. ⚠️ **The CONSEQUENCE is UNVERIFIED** — what vCenter does with a repeat install against a live Harbor was not measured, and it may well be a benign upsert. **Do not write code before measuring it**; it is a vendor-behaviour question, which is exactly the class this repo requires to be measured rather than inferred. If a fix is needed it is an already-installed check in `04`, not a mode flag (an admin re-running their own installer would never set one). ⚠️ **ITS SETTLING COMMAND IS ANSWERED 2026-08-17, on a real lab — BOTH halves, and both favour the swap.** Namespace `svc-harbor-rk6ra` (note: VKS names it `svc-harbor-<id>`, NOT `harbor` — my own measurement script's `^harbor` grep missed it on the first pass and reported *no harbor namespace*). **(1) MECHANISM:** BOTH secrets carry `cert-manager.io/*` annotations — `harbor-tls` from issuer `harbor-ca-issuer` (`cert-manager.io/certificate-name: harbor-tls-cert`, alt-name `harbor.env1.lab.test`) and `harbor-ca-key-pair` from issuer `harbor-self-signed-ca-issuer` (`certificate-name: harbor-ca`, alt-name `harborca`). Per the row's OWN verdict key, `cert-manager.io/*` ⇒ **a MECHANISM produces the equality; the swap is sound**. NO `kapp.k14s.io/versioned` on either, so the name-churn finding does NOT fire. **(2) ROOT vs INTERMEDIATE:** `harbor-tls`'s `ca.crt` is `subject=CN = Harbor CA`, `issuer=CN = Harbor CA`, `X509v3 Basic Constraints: critical CA:TRUE` — a **self-signed ROOT**, so `ca_anchor_reject_reason` will not refuse it and the swap is NOT an availability regression. Both conditions the row named as blocking are therefore cleared. 🔴 **DO NOT BUILD — finding (a) CLOSED 2026-08-17 by an idea-round, and the round REFUTED MY BRIEF AND THIS ROW'S OWN NEWEST CORRECTION.** ⚠️ **THIS ROW'S `:49-51` CLAIM IS MEASURED FALSE — that is the THIRD time this row has made itself less accurate by editing, and it is live misinformation until read.** The row asserts KinD is unreachable because of the Supervisor-kubeconfig requirement; measured, `supervisor_kubeconfig()` (`lib/os.sh:713-714`) ends its candidate list with **`"${KUBECONFIG:-}"`** and `05-kind-up.sh:104` does `state_set KUBECONFIG`, so that guard **PASSES** on KinD. The row's EARLIER statement — the discovery **label**, which `06-install-harbor.sh` sets **0 times** — was the right one. **The swap itself: NO WIN LEFT.** The two `ca.crt` blobs are byte-identical, and `27:107-108` extracts only `{.data.ca\.crt}` — so the written file, its digest, its 0644 mode, `ca_anchor_reject_reason` and `ca_verifies_endpoint` are ALL unchanged. The only delta is which bytes transit into kubectl's memory and are discarded, and the operator who can run the script can already `kubectl get secret harbor-ca-key-pair -o jsonpath='{.data.tls\.key}'` in one command. **AND THERE IS NO KinD ASYMMETRY** (my brief's premise, refuted): KinD dies at the SAME line with the SAME message before and after. **The code is a FALLBACK inside a collapsed `<details>`** (`scenario-1.md:618-624`), which `walk-doc.sh:552` excludes — so no walk row, no e2e and no gate executes it, and `test-env-file-mode.sh:93` only greps for a chmod. **A change nothing runs is a change nothing can certify.** The privilege win remains unbuildable as scoped: `git grep resourceNames` over `k8s/ docs/ scripts/` = **0 hits**, so without a `Role{resourceNames:["harbor-tls"]}` plus the doc line telling a platform admin to grant it, capability is unchanged — that is a DIFFERENT, larger row needing its own round and an owner decision. **(e) is CLOSED as stale:** all four §6→§8 pointers are already correct (`27:8`, `27:13`, `fetch-ca.sh:116` all say §8; `Makefile:556` carries no section; `scenario-1.md:532` is `## 8. Harbor's CA`) — verified. Residual, explicitly NOT covered: whether cert-manager guarantees `ca.crt` on `harbor-tls` across VKS releases is `inferred`, and settles with `kubectl -n <ns> get certificate harbor-tls-cert -o yaml` on a second, differently-versioned cut. |
| **B83-orig** | 📎 **ARCHIVAL — the ORIGINAL problem statement for `B83`, which is still OPEN. The work is tracked THERE, not here; this row is kept only as the evidence base.** **`27-harbor-ca-from-cluster.sh` reads the wrong Secret, and accepts a LEAF as a trust anchor.** Now the FALLBACK (scenario-1 §6 leads with `curl .../api/v2.0/systeminfo/getcert`, MEASURED 2026-08-09: http 200, 1155 B, `CN = Harbor CA`, sha256 identical to the trusted CA — no login, no Secret read). Findings from `adversary-security-secrets`, all measured: **(a)** the script reads `harbor-ca-key-pair`, whose response also carries `tls.key` — **the CA's signing key**, which mints a valid cert for ANY name every consumer trusts, offline, until 2036. `harbor-tls`'s `ca.crt` is byte-identical (measured, `cmp` identical) and its `tls.key` is only the leaf's. ⚠️ **Settle first**: if `harbor-tls`'s `tls.crt` == its `ca.crt`, that secret is ALSO a CA keypair and the swap is cosmetic — one `openssl x509 -noout -subject -issuer` decides it. **(b)** `harbor-tls` does NOT reliably carry `ca.crt`: `kubectl create secret tls` produces only `tls.crt`/`tls.key` (measured), and **`06-install-harbor.sh:202` creates this repo's own `harbor-tls` that way** — so prefer a LADDER (the `tkg.tanzu.vmware.com/tkg-registry-additional-ca-cert` Cluster annotation from B70, which carries NO key material at all → `harbor-tls` → `harbor-ca-key-pair` with the 🔴 warning printed at point of use), never a hardcoded name. **(c)** line 64 only checks the blob PARSES as a certificate — it accepts a **leaf**. Harmless today only because `ca.crt == tls.crt` in the keypair secret; reading `harbor-tls` makes a one-token jsonpath slip install a leaf as the anchor and exit 0. Add `subject == issuer` + `basicConstraints CA:TRUE`. **(d)** `scripts/test-*.sh` has **ZERO** coverage of this script. **(e)** three other sites name the secret, incl. `fetch-ca.sh`'s operator-facing `die` (shared with ArgoCD — keep it generic). | ✅ **(c) and (d) SHIPPED 2026-08-13.** The anchor check is now a pure `ca_anchor_reject_reason` in `lib/tls.sh` requiring self-issued **and** `basicConstraints CA:TRUE`, with a 10-case offline test (`test-ca-anchor-validation`, wired into `test-scripts`) — closing (d)'s ZERO coverage. ⚠️ **My first fix was itself defective and an `adversary-security-secrets` round MEASURED it:** it matched `*CA:TRUE*` against the whole `openssl -text` dump, so three self-signed `basicConstraints=critical,CA:FALSE` certs carrying the literal in a CN, an O and a **subjectAltName URI** were all ACCEPTED — the SAN vector being the one that matters, since every real cert has a SAN. It now reads `-ext basicConstraints` (a closed output set; already relied on at `02-env.sh:444` and `30-vks-login.sh:193`, so zero portability delta). Same round refuted `openssl verify -CAfile <f> <f>` as an alternative — MEASURED **OK** for a self-signed CA:FALSE cert — and found the function validated only PEM block #1 of a file the caller installs whole (now refuses a multi-cert file). **(a)/(b) REMAIN, and (a) is not deferred so much as ORDERED SECOND:** a ladder that can fall back to `harbor-tls` is only safe once a leaf is rejected, which is what (c) added. The settle-first command needs the lab: `kubectl -n <harbor-ns> get secret harbor-tls -o jsonpath='{.data.tls\.crt}' | base64 -d | openssl x509 -noout -subject -issuer -ext basicConstraints`. ⚠️ **My stated reason for deferring (a) was FACTUALLY WRONG** — I claimed a naive swap would break the KinD path; measured, `27-...sh` cannot reach KinD at all (it needs a Supervisor kubeconfig and selects by a label `06-install-harbor.sh` never sets). Right conclusion, wrong evidence. The real mechanism: the exposure is the **RBAC GRANT**, not the read — k8s has no field-level authz on Secrets, so anyone who can run the script can already read `tls.key` themselves; the jsonpath constrains what the SCRIPT writes, never what the OPERATOR can read. **CLASS: 1 of 5 anchor-installing sites adopted.** Next is `fetch-ca.sh:121`'s MULTI-CERT branch (takes 'the last (the issuer)' with no self-issued and no CA:TRUE test); NOT changed here because refusing an intermediate is a real behaviour change for a server that legitimately presents leaf+intermediate, and that needs its own decision. |

| **B84** | ✅ **CLOSED — see the B84 section below, which has said so since #657; this table row simply never caught up.** ⚠️ **ID COLLISION — this row is the BROADCOM PORTAL LINKS. There is a SECOND, unrelated `## B84` section below (`export VAR=x; make <target>` was silently defeated).** They are different subjects filed under the same id. Do NOT renumber either: `scripts/test-env-precedence.sh` cites `B84` in three places and means **the section**, not this row. ✅ **FIXED 2026-08-16 — the prescribed sentence shipped, in the table's PREAMBLE (before the reader clicks), not in the gotchas list below the table (which they reach only after failing).** The pre-existing gotcha *"a `release=` in the URL is ignored — use the on-page selector"* is a DIFFERENT claim: it tells you a URL parameter is ignored, not that the page reads **"No data found"** and looks like the artifact does not exist. ⚠️ **NEW MEASUREMENT 2026-08-16:** navigating the ArgoCD link (`servicePk=538499`) in a browser that is NOT signed in redirects to `access.broadcom.com/default/ui/v1/signin/` — so the reader's real sequence is sign-in → blank Release list → pick a release → files. That is now stated. **The "other three links UNVERIFIED" half remains UNVERIFIED and I did not close it by inference**: verifying the empty-list behaviour needs an authenticated session, and entering credentials is not something I will do. The doc states the behaviour plainly as a property of the portal's productfiles page (all four links are that same page type); the provenance caveat stays here, not in the end-user document. Original row follows. |
| **B84-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B84`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **The four Broadcom portal links in `docs/scenario-1.md` land on an EMPTY file list — MEASURED in a logged-in browser 2026-08-09.** Navigating the Harbor link (`servicePk=542081`) while authenticated renders the page fine and shows **"No data found"**: there is a `Select Release List` combobox that starts **blank**, and no files appear until a release is chosen. So the download table merged in #498 gives the reader the *page* but not the step that reveals the *files* — a reader who clicks it concludes the artifact does not exist. ⚠️ Only the Harbor link was opened; the other three (`538499` ArgoCD, `542815` VCF CLI/Plugins) are **UNVERIFIED** and presumed identical, which is an inference, not a measurement. Note the irony: the notes directly beneath that table already say *"a `release=` in the URL is ignored — use the on-page selector"*, so the trap is documented three lines from the table it breaks. **Fix:** state the selector step in the table's preamble (one sentence: "each link opens a page whose Release list starts empty — pick your release before the files appear"), and open the remaining three to confirm the behaviour is uniform. Downloading was blocked on a separate issue: the download icons require ticking "I agree to the Terms and Conditions", which is accepting an agreement on the operator's behalf. |
| **B85** | ✅ **FIXED — re-measured 2026-08-16. The branch it asks for now exists at every step it names.** ⚠️ Note the row's step numbers drifted (it says "Steps 2 and 3"; Harbor is **Step 4** and ArgoCD **Step 5** in the current numbering) — the same enumerated-reference rot as B101/B81, so match on CONTENT, not the numbers. Measured: **Step 2** `:170` — *"**Already have one?** Put its name in `VKS_NAMESPACE` and go to Step 3 — nothing else here"*; **Step 4** `:275` — *"**Already have Harbor?** Many labs ship it — check vCenter → Workload Management → Supervisor Services, or `kubectl get ns | grep harbor`. If it is there, skip the install command and open *Harbor already exists* at the end of this step"*, plus a dedicated `### If Harbor already exists` section and an inline *"Harbor already installed on this Supervisor? Skip that one command — but still run the next block"*; **Step 5** — *"**Already have ArgoCD?** `kubectl get argocd -A`. If it is there, skip the install command and go…"*. That is exactly the Step-1b pattern the row says the doc "already knows" and should copy. The specific collision it raises — a Supervisor built by the sibling `nested-vsphere-lab`'s `make services`, which registers the same service at the same version — is answered by those branches: the reader is told to check, and what to do when it is already there. Original follows. |
| **B85-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B85`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **`docs/scenario-1.md` Steps 2 and 3 have no "already installed → skip" branch, and collide with any Supervisor built by the sibling `nested-vsphere-lab`.** That repo's `make services` installs Harbor **and** ArgoCD as Supervisor Services — its `supervisor-service.sh` registers `argocd-service.vsphere.vmware.com 1.1.0-25100889`, the **same service at the same version** as the YAML our download table tells the reader to fetch. A reader on such a lab opens vCenter → Supervisor Services → Add New Service and finds it already there, with no guidance on what to do. **This is the COMMON case, not an edge one:** the runbook's own premise is *"you have a Supervisor endpoint, a login and a password"* — a Supervisor someone else built, where these services are frequently pre-installed. **The doc already knows the pattern** — Step 1b does exactly this (*"If you already have one — put its name in `VKS_NAMESPACE`, then go to Step 2"*); Steps 2 and 3 simply never got it. **Fix:** an "already installed" branch on each, telling the reader to take the FQDN/storage-class from the running instance and set the same `.env` values without installing. That also demotes **three of the six** download-table rows from *required* to *required only if you are installing*, which is what makes the table honest. ⚠️ **What happens if you install over an existing service is UNMEASURED** — Add New Service with a duplicate could no-op, error, or offer an upgrade. Uninstall targets exist for a clean test: lab `make services-uninstall SERVICE=<id> CONFIRM=yes` (destroys data; `services-deactivate` only *blocks* installs, it does not remove) and this repo's `make uninstall-all`. |
| **B92** | ✅ **CLOSED 2026-08-17 — the fix this row PRESCRIBED is shipped, at the line it named.** `scripts/26-vks-cluster-status.sh:269` opens the `WAIT_SECONDS > 0` branch with `# READ THE ENDPOINT ONCE, BEFORE BURNING THE BUDGET (B92).` and does exactly what the Fix paragraph below asks: reads the endpoint *before* the budget is spent; is gated at the **call site**, not inside the function (so `endpoint_report` stays print-only by contract, which `test-endpoint-report` asserts); and **only the DIVERGENT verdict stops** — every DECLINE path (either value empty / NOT YET KNOWABLE, a NAME-shaped endpoint not comparable to an IP, AGREE, no candidate Service) falls through unchanged, which is the no-false-RED requirement the row demanded. The optional 25 skip-path gate was correctly NOT added, as this row's own analysis argued. Verified by reading the code against the prescription, not by trusting the row. ORIGINAL FINDING: **`_cp_endpoint_check` never runs on the SKIP-CREATE path, and the 30-minute burn it prevents lives in a DIFFERENT script.** `25-vks-cluster-create.sh:78` (`ALREADY EXISTS — not re-applying`, exit 0) returns before the gate at :182, so a reader whose cluster already exists is not warned about a stale `spec.controlPlaneEndpoint`. ⚠️ **The obvious fix — gate 25's skip path — is REFUTED as the primary fix** (`vks-adversary`, 2026-08-12): `docs/scenario-1.md:402` tells that exact persona to SKIP `make vks-cluster-create`, so a gate there reaches nobody in that flow, and the walk cannot trigger it either (`walk-matrix.sh:68` rotates the cluster name per invocation, so `adv == lb` on the first read). **The burn is in `26-vks-cluster-status.sh`:** `endpoint_report` is PRINTS-ONLY by its own comment (:113) and the `WAIT_SECONDS > 0` branch enters its loop **without consulting the endpoint at all** — it only reports after the full budget is spent. **Fix:** ~6 lines in 26's wait branch, immediately before `end=$((SECONDS + WAIT_SECONDS))` — read `adv` and `lb`; both readable and different → print the existing remedy and exit 1; either empty → fall through unchanged (no false-RED path). Covers create, skip-create, and never-ran-create. The 25 skip-path gate is optional afterwards; if added, expect +90 s on rows 2/4 whenever the VirtualMachineService name does not resolve. |
| **B93** | ✅ **CLOSED (#675, then SUBSUMED by B122/#684).** Verified 2026-08-17: the key is commented at `.env.example:1394`, an override of `99999` now SURVIVES `load_env` (measured through the real loader with `SKIP_DOTENV=1` and the state overlay pinned away), and it is covered by `scripts/test-env-timing-overrides.sh`, which probes every commented timing key by a DERIVED predicate rather than a list — so this cannot regress unnoticed. The row's own framing was right that it is not a one-liner, and B122 is why: it was 1 of **14**. Original text: **`READY_TIMEOUT_SECONDS` is INERT from the command line, and commenting it in `.env.example` is NOT a one-liner because the code-side defaults DISAGREE.** MEASURED (`vks-adversary`, 2026-08-12): `READY_TIMEOUT_SECONDS=600 make …` → `load_env` → **300**. It ships uncommented at `.env.example:1292` and is absent from `load_env`'s snapshot list (`lib/os.sh:492`) and from `check-env-clobber.sh:91`'s `SELECTORS` — this repo's own clobber class, uncovered by its own gate. **But the obvious fix changes behaviour**: MEASURED, the scripts read three different defaults — `${READY_TIMEOUT_SECONDS:-300}`, `:-600`, `:-90` (plus bare `$READY_TIMEOUT_SECONDS` refs) — so commenting the line makes each script fall back to its OWN number instead of the 300 they all get today. Order: unify the code-side defaults (or make every site explicit), THEN comment it, THEN add it to the clobber gate's recognizer. Same question for its siblings `POLL_INTERVAL_SECONDS` (2 defaults, 9 bare refs) and `CURL_MAX_TIME_SECONDS` (3 defaults, 4 bare refs) — fix the class, not the instance. Documented meanwhile in `docs/scenario-1.md` Step 12 as ".env only". |
| **B95** | ✅ **CLOSED — detect-only SHIPPED (#645, F2); the reconcile half is a DELIBERATE non-goal, not an omission.** The adversary round's reasoning is the value: re-minting a Gitea CI token costs a token, but re-registering a guest MINTS A CLUSTER-ADMIN ClusterRoleBinding on a cluster the operator may not own — triggered by a probe failure that can equally mean the network blinked. **DETECT-ONLY — the prescription below was SUPERSEDED by measurement; the detection half SHIPPED in #645, the reconcile half is a DELIBERATE non-goal.** The original row said: validate the stored Secret's token against the guest and re-mint on failure, the pattern `50-seed-gitea-repos.sh` uses for the CI token. An adversary round returned **detect-only**, and the reason is that the two cases are not analogous: re-minting a Gitea CI token costs a token; re-registering a guest **mints a cluster-admin ClusterRoleBinding on the guest cluster** (`71-argocd-register-guest.sh` says so at its registration branch), so a silent auto-re-mint would escalate privilege on a cluster the operator may not own, triggered by a probe failure that can equally mean "the network blinked". **What shipped (#645, F2):** the already-registered path now prints the matching Secret's NAME and `creationTimestamp` plus the remedy — so an operator can see at a glance that the registration predates the guest's rebuild. It could not before: the message printed only the server URL the operator had just supplied, which is identical in both cases. Deliberately a SEPARATE `kubectl get`, NOT a third column on `ARGOCD_CLUSTER_LIST_TEMPLATE` — the round MEASURED that a third column makes `IFS=$'\t' read -r n s` in `argocd_pick_dest_server` swallow the tab into `$s`, so `70-configure-argocd.sh` would die *"AMBIGUOUS deploy destination"* for every operator, invisible to `test-argocd-topology.sh` (its fixtures are hand-written two-column strings). **RESIDUAL, named not hidden:** a same-address rebuild is now *visible* but still not *reconciled* — the operator must read the timestamp and run `ARGOCD_REGISTER=force`. An automatic probe is filed as **B95c** and is NOT scheduled: it needs a live persistent-Supervisor lab to be worth anything, and this lab gives each cut its own ArgoCD, so the failure mode is unreachable here and any test would be asserting a mock. ⚠️ Still not reachable in the walk — a green matrix says nothing about it. |
| **B96** | ✅ **CLOSED 2026-08-17 — every member of the class accounted for, and the substance VERIFIED on `main` rather than inferred.** **THE CLASS: 16 sites skip-on-existence; 2 were confirmed defects and 1 is suspect (B95).** Enumerated by `vks-adversary` 2026-08-12 under the new mandatory class-denominator clause — the first finding it produced was a sibling I had not asked about. Both confirmed members are fixed (the Gitea webhook secret + the Gitea admin password, PR #582). **The other 13 are CORRECT**, and four are the patterns to copy when fixing the rest: `50-seed-gitea-repos.sh` ~185 (CI token — validate against the live system, re-mint on failure), `22-harbor-robot.sh:130` (explicit two-case die), `50-seed`'s `push_repo` (`git push -f` reconciles content rather than skipping), `25-vks-cluster-create.sh:75` (loud refuse-to-clobber). **The discriminator that makes a skip a DEFECT:** the thing being skipped carries a LOCALLY-GENERATED value (a token, a password) that the other side regenerates independently — then "already present" means "diverged", not "done". A skip over content the operator owns is fine. 🟢 **CLOSED 2026-08-17 — every member of the class is now accounted for, and the substance was VERIFIED on main rather than inferred from the row.** The two confirmed defects are fixed and the fix is the RECONCILE pattern, not a louder skip: `50-seed-gitea-repos.sh:354` **deletes** the existing webhook (`could not delete the existing webhook …`) before re-creating it, and :364 logs `secret in force`; the admin password is likewise reconciled at :140 (`--must-change-password=false`) instead of being skipped when the user already exists. PR **#582** is MERGED and touched exactly `scripts/50-seed-gitea-repos.sh` + `scripts/99-verify.sh`. The other **13** sites were enumerated and judged CORRECT by the round that produced this class. The one remaining suspect, **B95**, is resolved as **detect-only** — the detection half shipped in #645 and the reconcile half is a DELIBERATE non-goal, with the automatic probe filed as B95c and deliberately NOT scheduled (it needs a persistent-Supervisor lab; this lab gives each cut its own ArgoCD, so any test here would be asserting a mock). ⇒ 2 fixed + 13 correct + 1 resolved = the whole denominator, nothing open. ⚠️ Closed by READING both rows end-to-end and checking the code on main — not by a keyword scan, which **B136** records as able to NOMINATE a row and never to close one. The class DISCRIMINATOR this row established is the durable part and is worth carrying forward: a skip is a DEFECT when the thing skipped carries a LOCALLY-GENERATED value (a token, a password) that the other side regenerates independently, because then *already present* means **diverged**, not *done*; a skip over content the operator owns is fine. |
| **B97** | ✅ **CLOSED BY LIVE MEASUREMENT 2026-08-18 — the PREMISE holds, the CONSEQUENCE is FALSE.** The six-row certification confirms `vcf context use` still exits non-zero (row-1 log lines 678 and 1293, twice in one row) — but Step 10 is NOT a no-op: the code WARNs and then verifies the END RESULT, which is the correct handling and is already self-documenting in its own message. Measured this run: `WARN 'vcf context use' exited non-zero — verifying the end result before judging it` → `Supervisor context verified via …/secrets/supervisor.kubeconfig` → `published ARGOCD_KUBECONFIG=…` → blocks **[30] rc=0** and **[31] rc=0**, with the block's own `expect: Supervisor context verified via` **SEEN**. So the context IS selected and IS verified; the upstream oddity is tolerated deliberately rather than ignored. The row is now redundant with the code, which explains the tolerance at the point a reader would ask about it. Original text: `31-fetch-argocd-kubeconfig.sh`'s `vcf context use` FAILS on every observed run — so Step 10's context selection is a measured no-op. LAB-MEASURED across today's rows and 2026-08-11: `selecting the vSphere-Namespace context 'argocd-supervisor:<ns>'` is immediately followed by `could not select … — list them with: vcf context list` (a warn, not a die). What still works is the final `kubectl get deploy argocd-server`, against the file **Step 3 already wrote and verified**. So the open question is what `make fetch-argocd-kubeconfig` still accomplishes — diagnose the `use` failure, or drop the create/use cycle and keep the verification. ⚠️ Silver lining, and it is LATENT not fixed: because `use` fails, the collision it would otherwise cause (31 repointing `supervisor.kubeconfig`'s current-context away from `vks-cicd:<ns>`) is not realised. Also UNVERIFIED: whether `vcf context create` MERGES or TRUNCATES an existing kubeconfig — if it truncates, a working `use` would destroy the `vks-cicd` context. Settle with `vcf context list` before/after on a real box. ⏸️ **LAB-BLOCKED, and its UNVERIFIED question is now QUEUED (2026-08-17).** Both of this row's open moves need a real `vcf` CLI against a live Supervisor — diagnosing the `use` failure, and settling whether `vcf context create` MERGES or TRUNCATES. Neither is answerable offline, so this row is not startable at a desk. ⚠️ **AND THE ORDER MATTERS, which this row states but is easy to miss when picking work:** the `use` failure is currently MASKING a collision, so if `create` TRUNCATES then *fixing* `use` would DESTROY the `vks-cicd` context. The truncate question must therefore be settled **BEFORE** anyone touches the `use` failure — not alongside it. Queued as item 6 of the lab-measurement list, flagged NOT read-only (it writes the kubeconfig), to be taken on a throwaway lab and only AFTER a matrix run. |
| **B101** | ✅ **CLOSED 2026-08-17 — REFUTED by an idea round, three independent ways, and its own seven items were already done.** **(1) STALE-OPEN, not under-scoped:** all **7/7** cited items are already resolved, graded, or tracked elsewhere — two of them closed by this row's OWN prescription on 2026-08-12 (`scripts/test-gitea-hook-ids.sh`, whose header says verbatim it IS that pattern); the `vcf .kubeconfigpath` one is already MEASURED with a do-not-revert; the Harbor one is already a graded row inside the Confidence table `check-vks-provenance` gates. **(2) It falls INSIDE the `hooks.md` refutation, a fortiori:** a claim falsified by a THIRD PARTY shipping a release has **no signal in our commit stream at all**. Measured — a *must-carry-a-token* gate fires on **20 of the last 200 commits (10%)**, every one a healthy bug-fix whose comment was TRUE when written; true-positive rate at commit time is **structurally zero**. Four gates for this class were already built, measured and refuted; a PRINTER shipped instead. **(3) Both prescriptions are refuted by this repo's own corpus:** *graded provenance* **LAUNDERS** (a third-party fact can never take a `code:FILE:LINE` token — `bundle/` is gitignored, `git ls-files bundle/` = 0 — so all ~33 facts could only take a SHAPE-checked token, and **0 of 97 sites carry one today**, i.e. day-one RED on 97 sites); *assert the CONSEQUENCE* makes rot **HARMLESS but never DETECTS it** — two of its three exemplars cannot be commit-time gates at all, and `96-verify-gateway-image.sh:19` forbids the offline form IN CAPITALS. **(4) It aims at the SAFE half:** *"X cannot do Y, so we do Y"* yields DEFENSIVE code that degrades to redundancy (`istio_ensure_gwapi_crds` is install-if-absent — rot-safe by construction); the dangerous inverse (affirmative reliance) returned **ZERO** instances in `scripts/`, and the one real one is already graded `community` and tracked. **(5) Any such gate's cheapest remedy is DELETING the `so/because` clause** — the only record of why non-obvious code exists. ⚠️ **Its numbers were also unreproducible**: the stated shape yields 730/157 (or 97 tightened), not 150/21, and the row carried TWO contradictory denominators at once — the enumerated-rot it diagnosed, in itself. **RESIDUAL SETTLED HERE (the one networked one-liner):** `00-install-prereqs.sh:5` says *"the CLIs mise does not cover (tkn, argocd)"* — MEASURED against `mise registry`: **`tkn` absent (claim correct), `argocd` PRESENT (claim FALSE)**, same shape as the already-refuted kubeconform/hadolint/yamllint claim in the same file. Note the DECISION may still be right for a DIFFERENT reason (CLAUDE.md's air-gap exception: mise fetches from the internet) — so the fix is to state the real reason, not to switch to mise. Filed as **B144**. |
| **B101-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B101`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **SEVEN merged code paths rest on an UNTESTED claim about a third party ("we do X because Y cannot do Z"); 4 of 11 are guarded.** Enumerated by `vks-adversary` 2026-08-12 (phrase-based grep, so a LOWER BOUND). Unguarded: `50-seed:327` (Gitea PATCH cannot set config.secret) · `50-seed:332` (Gitea never returns the secret) · `24-vks-k8s-version.sh:72` (the VKS selector is a PREFIX matcher) · `30-vks-login.sh:310` (the `vcf` CLI leaves `.kubeconfigpath` null) · `71-argocd-register-guest.sh:70` (B95) · `harbor.md:23` (VKS does not watch the user-trusted-ca-secret) · `22-harbor-robot.sh:163` (partial). **The repo already has the right pattern and it is used 3×** — `verify-gateway-image`, `mirror-verify`, `check-pod-inject-label` all assert the CONSEQUENCE over OUR OWN artifacts, and **no test here runs a third-party service to assert what it does**. Guard the rest that way, or record the fact as graded provenance and accept it; do not start running third parties in unit tests. |
| **B102** | ✅ **CLOSED 2026-08-19 — all four actionable lines are settled; see the `B102-fix` row above for the per-line verdicts and evidence.** Two shipped a literal quoted from the producing code (`L507`, `L899`), two are marked HUMAN-ONLY with the reason stated in the document itself (`L523` — no `>=6`-char token in `kubectl get nodes` output carries the readiness meaning; `L928` — the block is SKIPPED on every row, so no literal there can ever be checked). Post-fix state, re-derived: **33 `Expect:` lines in scenario-1, 2 with zero literal — and both of those are deliberate and documented, not gaps.** The walk's own dry run reports **34 parsed of 34** (34 because `scenario-1.md:56` walk-includes `common-bootstrap.md`), so the slack-zero `EXPECT_LINES_PARSED >= INDEP_E` floor holds. ⚠️ **Do not re-open this row for the two human-only lines** — they are not unfinished work, and "add a literal" was refuted for each on its own measured grounds. ⏸️ Original follows. — 🟠 ⚠️ **SCOPE MEASURED 2026-08-19 — THIS IS A SCENARIO-1-ONLY DEFECT, and nobody had ever checked the sibling.** Applying the extractor's own block-independent rules (`walk-doc.sh:724-742`: `len<6`; contains `<`/`>`/`...`/`…`; a bare `make <target>`) to BOTH documents: **`scenario-1.md` — 33 Expect lines, 4 with zero surviving literal**; **`scenario-2.md` — 15 Expect lines, ZERO**. So every claim the tenant document makes is machine-checkable, which means **rows 5 and 6's first-ever green (run 6) is better-evidenced than rows 1-4's**, and no scenario-2 work is implied by this row. ⚠️ **The line-number rot is now THREE-for-three** — the row has said 26, then 31, then 32 Expect lines; it is **33** today. Re-derive with the snippet, never quote the figure: `python3` over `**Expect:**` lines applying the three rules above. **The four current dead lines and my measured verdict on each:** `L507` (`0`/`Ready`, both len<6 — its producer builds output inside a `jq` program at `26-vks-cluster-status.sh:161`, and the `endpoint     :` strings at `:204`/`:212` are ERROR branches, so no happy-path literal is proven yet); `L523` (`Ready` len<6 — `INTERNAL-IP` is a `kubectl -o wide` header and would also evidence that `-o wide` ran); `L899` (no literal at all — `values below :` is printed by **every** branch of `creds.sh`'s provenance `case` at `:386`+, whereas **`DISCOVERED` would be WRONG: it is one branch of three**); `L928` (no literal at all — `thing(s) alone.` comes from the unconditional `98-uninstall-all.sh:301`, whereas **`LEFT ALONE` would be WRONG — ⚠️ **but MY STATED REASON FOR THAT WAS FACTUALLY FALSE and is corrected here (2026-08-19), because a wrong reason sends the next reader to re-check a branch that does not exist.** I wrote that `:306` *"sits inside `if [ "${#LEFT[@]}" -gt 0 ]`, so it never prints on a clean teardown"*. **Measured: `left()` is called UNCONDITIONALLY.** `98-uninstall-all.sh:236` and `:293` are both at **column 0** (verified with `cat -A`), so `${#LEFT[@]} >= 2` on every run and `:306`'s conditional is **never false** — `DELIBERATELY LEFT ALONE` always prints. **The real reason to reject it is far stronger: the whole BLOCK is SKIPPED on every row.** `walk-doc.sh:122` matches `*"make uninstall-all"*` and returns *"teardown - would destroy the lab mid-walk"*, and `:621-624` `continue`s **before** the Expect check, which is where `EXPECT_TOTAL` is incremented. So **no literal on L928 can ever be checked** — adding one would be a permanent silent no-op that reads in the diff as closing a gap**). ⚠️ **That is the governing risk for this row: a literal the command does not RELIABLY print converts a silent coverage gap into a FALSE UNMET on every matrix row — strictly worse than the gap, against a standing goal of six rows green on the first pass.** Two such traps were found by reading branches rather than grepping for the string; assume a third. Every candidate must be quoted from the PRODUCING CODE and proven unconditional on the walked path. ⏸️ Original follows. — 🟠 **11 of 26 `Expect:` lines in scenario-1 are NOT CHECKABLE by the walk, and that reservoir hides at least one real defect.** MEASURED (`vks-adversary`, 2026-08-12, with a simulator validated against the live run — it reproduced `15 blocks CHECKABLE` exactly): the PYX matcher drops a literal carrying a `<placeholder>` or an ellipsis, so Step 2's `created vSphere Namespace '<name>' …` was **structurally unreportable** — the walk could never have told us it names only one of two success branches. Fixed for the human this pass; the other 10 are unaudited. Audit them one by one and either (a) reword to a literal the walk can match, or (b) record explicitly that the claim is human-only, so `15 of 26 CHECKABLE` stops reading as coverage. ⚠️ Two kinds hide in there and only one is fixable: a placeholder-bearing literal can be reworded; a FILE-EXISTENCE claim (`./secrets/supervisor.kubeconfig`) can never appear in stdout and is permanently unmatchable — say which is which. ⚠️ **RE-MEASURED 2026-08-17 — the row's numbers are stale in BOTH directions and it CONFLATES three different kinds.** scenario-1 now carries **31** `**Expect:**` lines (the walk reports 32 because `DOC_SET` includes an include), not 26. Applying the extractor's OWN rule (`walk-doc.sh:614-628`: drop a literal if `len<6`, if it contains `<`/`>`/`...`/`…`, if it appears in the block, or if it is a bare `make <target>`), **27 of 31** Expect lines keep at least one literal that survives the block-INDEPENDENT rules — so the structurally-unmatchable set is **4, not 11**: `L451` (`0`, len<6) · `L467` (`Ready`, len<6) · `L828` (creds-show — **no backticked literal at all**) · `L857` (teardown "prints what it left alone" — no literal). The other three kinds the row lumps in are NOT coverage gaps: a literal dropped because it NAMES THE COMMAND in its own block is a **correct** drop (finding it would prove nothing), and an Expect on a block the row legitimately SKIPS is not a gap either. Run 3's row 1 measured **22 CHECKABLE of 32 parsed**; the gap from my 27 is exactly those two kinds, which I did not decompose further (rule 3 needs each Expect paired to its block text). **So the actionable work is 4 lines, split 2/2:** `L451`+`L467` are short-literal and **fixable by rewording** to a longer literal the walk can match; `L828`+`L857` carry no literal and need a judgement — add one, or mark the claim human-only explicitly. The row's own "file-existence claims are permanently unmatchable" caveat still holds and is why (b) exists. NOT DONE HERE: the docs are gate-guarded and this pass was read-only. ⚠️ **RE-DERIVED 2026-08-18 — the ANALYSIS HOLDS, the LINE NUMBERS HAD ROTTED.** scenario-1 now carries **32** `**Expect:**` lines (was 31), and re-applying the extractor's block-INDEPENDENT rules (`len<6`; contains `<`/`>`/`...`/`…`; a bare `make <target>`) still yields **exactly 4** with zero surviving literals — the 2/2 split is unchanged, so the row's reasoning survives. But all four quoted line numbers were WRONG when checked: L451 is prose, L467 and L857 are blank, L828 is a different Expect. **Current:** `L465` (`0`, len<6) · `L481` (`Ready`, len<6) · `L855` (creds-show, NO backticked literal) · `L884` (teardown, NO literal). **DO NOT hand-copy those either — RE-DERIVE:** for each `**Expect:**` line keep backticked literals of len>=6 that carry no `<`/`>`/`...`/`…` and are not a bare `make X`; an Expect with ZERO survivors is structurally unmatchable. That one rule reproduces the list in seconds and cannot rot. ⏸️ **NOT FIXED 2026-08-18, deliberately:** all four live in `docs/scenario-1.md`, which the certifying matrix WALKS, and rewording an `Expect` to add a matchable literal is EXACTLY how run 2 died — one such edit (#829) produced an UNMET, which failed row 2, which made the harness refuse the cut-B rebuild, so rows 3/4/6 never walked. A literal that the command does not actually print costs three rows and a lab cut. Land after a run certifies the tree, and check each new literal against that command's REAL output first (Expect is ANY-of-the-literals, so an added literal can only help — provided it appears). ⚠️ **ROUND 2026-08-18 — RE-STATED AGAINST THE CORRECT DENOMINATOR. Every count previously in this row, and in my own re-derivation of it, was measured with ONE FILTER OMITTED.** `walk-doc.sh:710-722` drops a literal when `len<6`, when it carries `<>`/`...`/`…`, when it fullmatches `make <target>`, **and when `lit in blk`** — i.e. when the doc quotes a string its own command block already contains. That last filter is block-DEPENDENT, and omitting it is exactly what produces the smaller numbers. Replicating the REAL filter set: **scenario-1 has 9 zero-literal Expects (blocks 7,15,16,21,28,29,36,37,38), scenario-2 has 3 (blocks 8,9,10)** — versus 4 and 0 with the filter omitted, which reproduces the old figures EXACTLY. So **"scenario-2 is clean" is FALSE** and **"this is a scenario-1-only problem" is refuted**; the gap is **12, not 4**, and closing this row as "2 of 4 fixed" would have shipped a false all-clear over ten remaining gaps. **Also: the harness walks a DOCUMENT SET, not a file.** Both runbooks carry `<!-- walk-include: common-bootstrap.md -->`, so any per-file denominator disagrees with the harness's own (33 vs 32 for scenario-1), and a gap living in the shared include belongs to BOTH runbooks. Any future count must analyse `$DOC` + every include, and must anchor on `^[[:space:]]*\*\*Expect` — an unanchored grep also counts PROSE mentions of `**Expect:**` inside backticks. **SIX of the ten remaining are `lit in blk` self-echo cases** (L266, L600, L760, L771, L872 in scenario-1; scenario-2's three are shell blocks whose Expect quotes a string the block itself `echo`s) — those are fixable by quoting a DIFFERENT string, and are the larger half of this row. Only TWO are structurally impossible: L872 (`port-forward`, neutralized at `walk-doc.sh:239`) and L884 (`uninstall-all`, deliberately skipped — it would destroy the lab mid-walk). **TWO EDITS ARE READY BUT DELIBERATELY NOT LANDED**: L465 -> `every expected node is Ready` and L855 -> `Access the UIs:`. Both are MEASURED present exactly ONCE PER ROW in run 6 (4/4 scenario-1 rows and 6/6 rows respectively) and both survive the filters. They are held because adding an Expect literal converts a silent non-check into a **hard `exit 1`**, so it lands only after a run certifies the tree — the same discipline B145 carries. ⚠️ **AND THEY NEED A GATE FIRST:** both strings are SINGLE occurrences in the code (`26-vks-cluster-status.sh:322`, `creds.sh:317`). Reword either and `static-check` stays GREEN while the next matrix goes EXPECT UNMET and exits 1 — the "one line of prose killed a matrix" shape. Two lines in `static-check-fast` asserting each added literal is `grep -qF`-present under `scripts/` is the only thing that converts that latent matrix-killer into a local RED. **L481 is NOT covered by its neighbour** (an earlier framing of mine, refuted): L465's block re-extracts the kubeconfig from the Supervisor Secret into a temp file, while L481 reads the one ON DISK — a different artifact — and `nodes_ready` asserts `>= want` Ready while the doc says **all** Ready, so a cluster with an extra NotReady node satisfies one and violates the other. 🔴 **IDEA ROUND 2026-08-18 (`adversary-bash-git-cli`, both instruments PERMITTED): the prescribed gate is `build-a-different-thing`. DO NOT build the two hardcoded lines.** The MECHANISM survived — both literals ARE `grep -qF`-able today (ran-it: `every expected node is Ready` contiguous at `26-vks-cluster-status.sh:322`; `Access the UIs:` contiguous at `scripts/creds.sh:317`) — so a reword would turn it red. What is refuted is the *"only thing"* claim, the WIRING, and literal 2 itself. **(1) HIGH — THE WIRING IS BLIND ON THE SIDE THE LITERALS ARE WRITTEN.** `static-check-fast` is gated on `code`, and `classify-changes.sh:36-42` maps `*.md`/`docs/*` to `docs=true, code=false` — so an Expect-line edit in a DOCS-ONLY PR never runs the gate, which is exactly the *"one line of prose killed a matrix"* shape in the direction the design does not cover. The repo ALREADY solved this class: `check-doc-target-coverage`, `check-doc-make-targets`, `check-vks-terminology` and `check-vks-provenance` are dual-wired into BOTH `docs-lint` (`Makefile:1457`) and `static-check-fast` (`:1491`). Dual-wire it too. **(2) HIGH — LITERAL 2 IS REFUTED ON THE MERITS, gate or no gate.** `Access the UIs:` is an UNCONDITIONAL top-level `echo` at `creds.sh:317` with no branch, so under walk-doc's ANY-match rule (`:789`) it can essentially never fail — and worse, once it is on that Expect line it PERMANENTLY MASKS every stronger literal added later (`DISCOVERED`, `STORED`), which land in the MASKED bucket forever. It converts a silent non-check into a check that CANNOT fail, which reads as coverage. Use `DISCOVERED` and `STORED` — they test the sentence the Expect actually makes, both are grep-able, and they must NOT be added alongside the header literal. **(3) MED — hardcoding makes a THIRD copy**, so a CORRECT coordinated reword of code + doc goes RED and the remedy is to edit a third file; DERIVE the set from the docs' own Expect lines instead. **(4) MED — the fully general form is 13% false-RED and irreducibly so:** measured, replicating walk-doc's four filters, **45 literals are checked and 6 are not grep-able under `scripts/`** — four are third-party output this repo will never contain (`-rw-------` from coreutils, `PROVISIONER` a kubectl column header, and two `install issued for …` lines from the `vcf` CLI) and two are ours but ASSEMBLED (`robot account 'robot$vks-cicd' created.` prints `'$rname'` at `22-harbor-robot.sh:181`; `harbor.crt: OK`). So: derived gate + a SIX-entry allowlist carrying a reason each. **(5) MED — two vacuity paths, and the first is delicious:** BOTH literals appear in `BACKLOG.md` **because this row quotes them**, so a gate later "improved" from `scripts/` to a repo-root `grep -rF` goes PERMANENTLY GREEN off its own specification. Scope to `scripts/`, say why in the header, print the denominator and enforce a floor. Second path: a derived gate whose doc parser breaks yields 0 literals and passes having checked nothing. **(6) LOW — this row's own citation is wrong: `scripts/lib/creds.sh:317` does not exist; it is `scripts/creds.sh:317`.** ⚠️ **RED-proof must run in BOTH directions** — reword the code site (expect RED) AND add a fabricated Expect literal to a scratch copy of the doc (expect RED), the latter being the direction the prescribed design cannot fail on. **Residual the round states itself:** its block↔Expect pairing approximates walk-doc's extractor (immediately-preceding fence, no statement splitting), so 45/6 could shift by one or two; settle it by running the real extractor offline after the matrix. NOTHING BUILT — `Makefile`, `scripts/` and the docs are all WALKED files and a certifying matrix is in flight. |
| **B104** | ✅ **CLOSED — REFUTED by its own idea round (`adversary-bash-git-cli`, 2026-08-19). Both remaining items are DROPPED; neither ships.** **Item (2) `-o Acquire::Retries=3` is VACUOUS — apt's default is ALREADY 3.** Measured with an instrumented server counting requests, and with a WORKING CONTROL that proves the flag is honoured rather than the harness broken: `Retries=0` → **2 conns / 6.04s**; `Retries=3` → **8 / 31.06s**; **default, no flag → 8 / 31.06s**. Nothing in `/etc/apt` sets it (`apt-config dump` 0 hits) — the default is compiled in. ⇒ the commit would have shipped a flag with **zero** behaviour change while the row closed believing apt retry had been added. **Item (3) `--retry-all-errors` is a NET REGRESSION that manufactures a fake-green.** Under the real Makefile shell (`Makefile:166-167`, `.SHELLFLAGS := -eu -o pipefail -c`), a truncated `mise.run` today gives curl **56** → pipefail → the `||` fires → *"mise install failed — install it manually"*. With the flag the retry succeeds, **curl exits 0, the guard never fires**, and `sh` has already executed a garbled seam command (measured: it ran `MARKecho MARKER_A`). It destroys the very message the plan said it wanted to keep. It also retries permanent 4xx (404 → 4 attempts, **15.02s** wasted) and lets `--retry-delay 5` override any server `Retry-After`. **And today's failure mode is BENIGN + LOUD by construction:** `mise.run` is 370 lines that define functions and call `install_mise` **once, at EOF**, so a truncated prefix has **0** top-level calls and is a syntax error (`sh -n` rc 2) — it executes nothing. The change trades benign+loud for unpredictable+silent. **Three further measured findings, all of which independently sink the plan:** the motivating class is **uncovered** — a **503 is not retried at all** (1 request at default *and* at `Retries=3`; what apt retries is timeout and connection-refused); the **denominator is understated** — `mise.run | sh` has **6** sites (`Makefile:256`, `bootstrap-jumpbox.sh:89`, 4 jumpbox Dockerfiles), and the apt arms number **4**, because `bootstrap-jumpbox.sh:51,58` is a **second, independent** `pkg_refresh`/`pkg_install` implementation the plan never mentions; and the subset verdict **reproduces the defect this row warns about** — siblings `06-install-harbor.sh:79-80` and `46-install-istio.sh:85,89` (`helm repo add/update`) are still bare. Air-gap premise (c) **holds** (`00-install-prereqs.sh:9` is internet-side only and the sole caller of `os.sh`'s `pkg_*`). ⚠️ **The round's named residual is now SETTLED and it does not change the verdict:** it scoped its apt result to one operating point (apt 2.8.3 / Ubuntu 24.04) and flagged that this repo also targets **26.04**. Measured here on both, connection-refused class, no hang-server needed — `ubuntu:24.04` **apt 2.8.3**: 31 ms at `Retries=0` vs **7020 ms** default; `ubuntu:26.04` **apt 3.2.0**: 26 ms vs **7023 ms**. **Both releases retry by default**, and the 24.04 figure reproduces the round's independent 7040 ms. ⇒ item (2) is a no-op on every OS this repo targets. **What would reopen this row:** an actually-observed `mise.run` transient (none found; it is a once-per-fresh-box fetch with a working manual fallback), and if retry is ever wanted there the shape is **download → `sh -n` verify → execute**, never `--retry-all-errors` on a pipe into `sh`. ⏸️ Original follows. — ⚠️ **CITATIONS RE-MEASURED 2026-08-19 — every line number below is STALE, and B175's claim that they were 'corrected in the same pass' is FALSE (the correction never landed; measured, this row still said `Makefile:250`, `Makefile:212` and `:88`/`:115`/`:137`).** The FACTS hold; only the coordinates rotted. **Correct, measured today:** the apt sites are **`scripts/lib/os.sh:131`** (`$SUDO apt-get update -y`) and **`:355`** (`DEBIAN_FRONTEND=noninteractive $SUDO apt-get install -y --no-install-recommends "$@"`) — **TWO sites, not three**, and neither is at `:88`/`:115`/`:137`. The mise curl is **`Makefile:256`** — ONE site, which this row double-counted as `:250` AND `:212`. `Acquire::Retries` is genuinely unset (`apt-config dump` → **0** hits; `lib/os.sh` → **0**), so the premise that apt never retries STANDS. ⚠️ **Read the step list below for its SUBSTANCE, never for its line numbers** — this is the third row today whose numbers rotted while its argument survived (see B77, B189). ⏸️ Original follows. — 🟡 **VERDICT: BUILD-A-SUBSET — the row as written is REFUTED, and the round found a CRITICAL that has nothing to do with this row's proposal.** Idea-round `adversary-bash-git-cli` 2026-08-17, both Step-0 instruments PERMITTED, so everything below is measured. **THE HEADLINE COUNT IS WRONG, in this row's favour.** Re-enumerated at `180ddea`: **4 of the 12 cited line numbers now point at COMMENTS** (`Makefile:212`, `46-install-istio.sh:104`, `10-mirror-pull.sh:141`/`:144`); the whole `make mirror` group **was already fixed** — `10-mirror-pull.sh:160,161,163` carry `mirror_retry "${MIRROR_RETRIES:-5}"`, landed by **PR #699 on 2026-08-17**, so the row was stale by one day when it was read; and its ⚠️ trailing constraint (*"any use of `http_get_retry` for a LARGE artifact must override `HTTP_GET_MAX_TIME_SECONDS`"*) **already shipped** as `prereq_dl_max_time()` (`00-install-prereqs.sh:343`, default `PREREQ_DOWNLOAD_MAX_TIME_SECONDS`=900, applied `:369/:381/:393`, documented `:341-368` with the 238 MiB/33 Mbit arithmetic, commented in `.env.example:1500` per the clobber rule). Independent sweep: **19 external-capable fetch sites on operator paths, 8 retried, 11 not** — and of the 11, only **4** are sites where retry is the right answer and is absent. Not 9. 🟢 **THAT CRITICAL IS STALE — MEASURED FIXED 2026-08-18. Do NOT act on the paragraph below; it is kept only so nobody re-derives it.** Every load-bearing element of it has since changed. **(1) THE COMPOUNDING MECHANISM NO LONGER EXISTS:** the curl invocation is now `curl -fsSL --remove-on-error -w '%{http_code}' --connect-timeout --max-time -o` — **zero occurrences of `retry`** (measured on the call itself), so there is no inner `--retry 3 --retry-delay $delay` to compound with the outer loop. **(2) THERE IS NOW A TOTAL WALL-CLOCK BUDGET,** `HTTP_GET_TOTAL_BUDGET_SECONDS` (default `maxtime*5` = 300 s), checked BEFORE each attempt *and* before each sleep (*"Do not start a sleep we cannot finish inside the budget"*), with a die-on-non-numeric guard so it cannot silently vanish. **(3) THE BACKOFF IS CAPPED** at `HTTP_GET_RETRY_MAX_DELAY_SECONDS` (default 30). **MEASURED, both arms:** the row's own refused-connection probe (`http://127.0.0.1:9/x`) now lands in **65 s**, not 540 — an **8.3x** discrepancy — with backoff `5s 10s 20s 30s` and a die after 5 attempts; and the budget guard is live, not decorative — `HTTP_GET_TOTAL_BUDGET_SECONDS=12` dies at **5 s / 2 attempts** on the pre-sleep check. The blackhole arm is bounded by the same budget at ~275 s by arithmetic (4x60 s transfer + 5+10+20 sleep; the 4th sleep would cross 300) — **stated as computed, not measured**, because the refused arm already refutes the mechanism. ⚠️ **The row's line citation had also rotted:** it says `lib/os.sh:1114-1135`, which is now `ensure_secret_token`/`gen_password`; `http_get_retry` is at **`:1328`**. ⇒ The row's instruction *"must be fixed BEFORE any new call site is added"* is **retired**, and its step (1) *"FIRST, own PR: fix the compounding delay"* is **already done**. Steps (2) and (3) are unaffected by this correction and stand on their own. Original CRITICAL follows. — 🔴 **(SUPERSEDED — SEE ABOVE) CRITICAL, FOUND BY THE ROUND AND CONFIRMED BY READING THE CODE: `http_get_retry` burns 540 s against a connection refused in 0 ms, and it is SHIPPED TODAY at 5 live call sites.** `lib/os.sh:1114-1135` hands the **doubling OUTER** `delay` to the **INNER** `curl --retry 3 --retry-delay "$delay"`, so they compound: `3x(5+10+20+40+80) + (5+10+20+40)` = **540 s of pure sleep**, none of it transfer. Measured: `http://127.0.0.1:9/x` -> **9 m 00.5 s**; blackhole `https://10.255.255.1/x` -> **12 m 20.5 s**. Live callers: `10-mirror-pull.sh:75`, `00-install-prereqs.sh:369/:381/:393`, `07-install-argocd.sh:111` — and `scenario-1.md:140` WALKS `make deps`, which uses three of them, with **no per-block timeout in `walk-doc.sh`** (grep: 0 hits) to bound it. This is independent of whether B104 is ever built, and must be fixed BEFORE any new call site is added. Minimal fix: a FIXED inner `--retry-delay 2`, or drop `--retry` and let the outer loop own backoff; cap total wall time. RED-proof: re-run the refused-connection probe — corrected, it lands in tens of seconds, not 540. ✅ **BUILD EXACTLY THREE THINGS, IN THIS ORDER.** (1) **FIRST, own PR:** fix the compounding delay. (2) `pkg_install`'s **apt arm** -> `-o Acquire::Retries=3`, **NOT a wrapper**: measured `apt-config dump | grep Acquire::Retries` is unset (default 0) so apt genuinely never retries, but a `http_get_retry`-shaped wrapper would re-run a MULTI-MINUTE package transaction 5x and inherit the CRITICAL. One flag covers `:88`, `:115`, `:137`. (3) `Makefile:250` `curl -fsSL https://mise.run | sh` -> `--retry 3 --retry-delay 5 --retry-all-errors`, keeping its actionable `||` message. `00-install-prereqs.sh:323` `mise install` is **already non-fatal** (`|| log_warn`) — leave it. ⛔ **EXCLUDED, each with its reason.** `46-install-istio.sh:85,89` is **not the default ingress path** — it is the `else` of an air-gap-first ladder (`:69` carried charts, `:72` dies actionably when a bundle carries none, `:83-89` fetches only when there is NO bundle), and after `make mirror` the charts exist so the walk never reaches it; the one way an air-gap box gets there is a wrong `BUNDLE_DIR`, exactly where 12 min of retry is a regression over a fast named failure. `06-install-harbor.sh:79-80` is **KinD-only** — `install-all` (`Makefile:808`) is `preflight mirror mirror-verify builder-image vks-login platform gitops`, no `install-harbor`. `walkbox.sh:152` (`--fail -sSL --max-time 1800 || die`, harness not operator path) and `fetch-supervisor-ca.sh:33` (`--max-time ${VCENTER_CA_FETCH_TIMEOUT_SECONDS:-30} || die`, actionable, sibling probe already retries for a documented floating-IP reason) are **already guarded** — remove both from the count. 🔴 **THE BOUNDARY THIS ROW NEVER STATED, and which a blanket sweep WOULD cross: GET / idempotent / INTERNET only.** `22-harbor-robot.sh:132` says in its own message *"Harbor shows a robot secret ONCE and cannot re-issue it"* — a retried mint is unrecoverable; also `50-seed-gitea-repos.sh:240,252,362` (`-X POST`), `:353` (`-X DELETE`), `lib/harbor.sh:114` (`POST projects`). All LAN, so B104 does not propose them — but nothing in the row said so, and "fix the class not the instance" is exactly how a sweep reaches them. ⛔ **NO GATE IN THIS PASS** — none exists, so the enumerated-list-rot argument is correct, but a gate is a NEW CONTROL needing its own idea round. ⏸️ **HELD.** All three surviving sites are on WALKED blocks (`make deps` `scenario-1.md:140`, `make install-all` `:754`); the contract is *all six rows green FROM THE FIRST GO, every fix resets that clock*. Land after a run certifies the tree — same sequencing as B145 and B75-a. **RESIDUALS:** whether **tdnf** has a retry knob is UNTESTED (this box is apt; settles with `podman run --rm photon:5.0 tdnf install --help 2>&1 | grep -i retr`) — say so rather than fake symmetry; the **corrected** helper's latency is unmeasured; the real transient DISTRIBUTION is unmeasured (the one datum is the `curl: (52) Empty reply from server` on tkn, walk row 3 2026-08-13, already fixed; whether `pkg_install` ever failed transiently here settles with `grep -rn "apt-get install" /tmp/walk/*/*.log`); and `46-install-istio.sh`'s else-branch reachability on a genuinely air-gapped box is **source-read, not measured**. **Original row follows, kept for its enumeration — but 4 of its line numbers are comments and its headline count is refuted above.** 🟠 **NINE more operator-path network fetches have NO retry — this pass fixed 3 of 12 (25%), and the largest untreated group is on the DEFAULT ingress path and inside `make mirror`.** Enumerated by `adversary-bash-git-cli` 2026-08-13 after a `curl: (52) Empty reply from server` on tkn killed `deps-prereqs`: `00-install-prereqs.sh:88` (the OS floor `pkg_install`, BARE under `set -e` — a tdnf/apt blip kills deps outright) · `Makefile:212` (`curl https://mise.run \| sh` — the mise BOOTSTRAP itself) · `06-install-harbor.sh:79-80` (helm repo add+update) · `46-install-istio.sh:85,89,104` (helm repo add+update+pull — the DEFAULT ingress) · `10-mirror-pull.sh:141-144` (helm repo add+update+pull, inside `make mirror`) · `walkbox.sh:152` (cloud image) · `fetch-supervisor-ca.sh:33` (vmca.zip — the probe at :62 retries, the DOWNLOAD does not). Already retried: `http_get_retry` at `10-mirror-pull.sh:75` + `07-install-argocd.sh:111`, `mirror_retry` at `:108`, and deps-mise's one manual retry. **Fixing 3 and stopping is the enumerated-list rot** — the next transient lands on `helm repo add` and reads as a brand-new class. ⚠️ `helm` has no `--retry`, so those six need a wrapper, not a flag; and any use of `http_get_retry` for a LARGE artifact must override `HTTP_GET_MAX_TIME_SECONDS` (its 60s default demands 33 Mbit/s sustained for the 238 MiB argocd — measured). ⚠️ **CITATIONS CORRECTED 2026-08-18, and this row DOUBLE-COUNTED one site as two.** MEASURED on both `origin/main` and the walked tree `021c6a3`: there is exactly **ONE** `curl -fsSL https://mise.run \| sh` in the Makefile and it is at **`:256`** (`:255` is only its `echo`). This row cites it as **`Makefile:250`** in its BUILD list AND separately as **`Makefile:212`** *("the mise BOOTSTRAP itself")* in its site list — but `:212` is `##@ Prerequisites`, a section header, and `:250` is a comment line. The row's own earlier correction already flags `:212` as pointing at a comment, so it contradicts itself: **one site, two citations, both stale.** ⚠️ **AND IT ABSORBS TWO ROWS I FILED IN ERROR.** An adversary round (2026-08-18) independently re-found this class and I filed it as B183 (`pkg_install` has no retry) and B184 (`mise.run` piped to `sh`) **without checking whether it was already owned** — it was, by this row, which already names `pkg_install`, the tdnf/apt blip (*"ARE under `set -e` — a tdnf/apt blip kills deps outright"*), the exact three sites `:88`/`:115`/`:137`, and the mise.run curl with a prescribed `--retry 3 --retry-delay 5 --retry-all-errors`. B183/B184 are DELETED rather than left standing — three rows for one defect is precisely the ill-defined-row-count problem B175 documents. **The one genuinely new fact, kept because it corrects ME:** I had reported *"ZERO un-retried network calls in the walk path"*. That is FALSE. The audit behind it read only `10-mirror-pull.sh` — the calls that had ALREADY been hardened — and never enumerated the ones upstream of them, which is the same class that killed run 6's row 3. Nothing here is fixed: `os.sh` and `Makefile` are WALKED files and a certifying matrix is in flight. |
| **B106** | ✅ **CLOSED — RESIDUAL TRACED 2026-08-18.** The row's own per-category notes already mark every rotted statement fixed in-pass (4+2+2+1 = 9). The 10th — *apps named by hand, 1 of 2* — deferred to **B105**, which has NO ROW: it is in the **"Pruned 2026-08-13: 21 rows whose work had SHIPPED"** list (:195), so the residual is DONE, not lost (full text in `git log -p -- BACKLOG.md`). Spot-checked rather than trusting the prune note: `apps/registry.tsv` carries exactly two apps and scenario-1.md names each exactly once, so the doc agrees with the registry. **The gate REFUTATION in this row stays — do not delete it:** a `claim == table-row-count` gate scores the WORST row GREEN while pushing rows 9/10 toward "set 0 values" instead of deletion, i.e. a gate whose only remedy DEGRADES the artifact. 🟠 **~130 statements in scenario-1.md are fixed by content elsewhere; 10 are MEASURED rotted, and the densest site is not the most consequential one.** Enumerated by `vks-adversary` 2026-08-13: summary-table counts 7 (4 rotted — fixed this pass) · inline durations vs the doc's OWN Timings table 7 (2 rotted — fixed) · doc↔code behavioural claims 3 (2 rotted — fixed) · which-file-a-value-lands-in 5 (1 rotted — fixed) · make-target names 39 (0) · in-document anchors 16 (0) · `Step N` cross-refs 48 (0 dangling, semantics spot-checked only) · port-forward object names 4 (0) · apps named by hand 1 of 2 (B105). ⚠️ **A GATE FOR THE COUNTS WAS DESIGNED AND REFUTED — do not rebuild it:** a `claim == table-row-count` gate is mechanically sound and RED-provable, and it scores the WORST row (6, where the operator types zero) **GREEN** while pushing rows 9/10 toward "set 0 values" instead of deletion — a gate whose only remedy degrades the artifact. The right invariant ("keys the operator TYPES") needs a classifier that was measured wrong 5 of 6, including conditionals like "VKS_SSO_DOMAIN is required iff VKS_USERNAME lacks an @". Discipline, labelled as discipline. **Unique to this document**: scenario-2.md and README.md carry ZERO counted claims. |
| **B107** | ✅ **MEASURED 2026-08-16 — the "~42 unchecked" figure counted the wrong thing, and the real exposure is ONE line.** Enumerated all step headings (16) and all `Step N` references (48) mechanically. **0 dangle** (confirms the row). Then split the 48 by whether they make a CLAIM about the target (`tells/says/sets/creates/reads/installs/publishes/…`) or are BARE POINTERS: only **4 are claim-bearing**; the other 44 are pointers, which cannot be *semantically* wrong — so "~42 unchecked" counted unverified *pointers*, not unverified *claims*. Of the 4, **3 CONFIRMED** against their target's line range: `:102`→Step 12 (installs Istio from your own Harbor, or attaches — `:768`), `:129`→Step 6 (creates the guest cluster — `:408`), `:538`→Step 4 (says `HARBOR_URL` is a bare host — `:283`). **The 4th is the only finding:** `:690` tells the reader *"Step 3 published `ARGOCD_KUBECONFIG` when you logged in"*, and that string appears **nowhere in Step 3's body (204-269)**. The claim is **TRUE about behaviour** — Step 3 runs `make vks-login` → `30-vks-login.sh:447 state_set ARGOCD_KUBECONFIG "$SUP_KUBECONFIG"` — but Step 3's own `**Expect:**` names only `Supervisor context verified via` + `./secrets/supervisor.kubeconfig`, so a reader who follows the pointer back to check finds nothing. **Fix (HELD until the certification matrix finishes — it walks scenario-1, and merging mid-run makes later rows walk a different tree): one clause in Step 3's Expect naming what it publishes.** **Step 14's teardown claims: CONFIRMED at source** — `98-uninstall-all.sh:17` states it deletes ONLY label-carrying objects, never by name, never `--all`, and refuses an unlabelled object; `:133-134` implement the "left alone" print that the Expect promises. ⚠️ **Residual, named not hidden:** that is a SOURCE-level confirmation, not a live teardown run — the script's logic matches its documented claim, which is not the same as having watched it decline a real unlabelled object. Original row follows. |
| **B132** | 🟢 **FIXED 2026-08-17 — `env_publish`: a step that SUPERSEDES a value must write to the sink that currently HOLDS it, or clear that sink.** Found by matrix row 1 on a live lab (scenario-1 §9, `make harbor-robot`, rc=1, block [28] — the ONLY failure in a row that scored 34 ran / 0 UNMET). `04-install-harbor-service.sh:144-145` publishes HARBOR_USERNAME/HARBOR_PASSWORD to `.env.state`; Step 9 wrote the ROBOT identity to `.env`; `load_env` sources the overlay LAST, so the robot identity could never take effect — the run died having already created the robot in Harbor. **THE IDEA ROUND REFUTED ALL THREE OF MY CANDIDATES**, and the scoping error was the real lesson: I had scoped the fix to the ONE key that happens to carry an `assert_env_effective` guard, when the class is **5 keys / 2 writers** — HARBOR_USERNAME + HARBOR_PASSWORD (guarded) and GITEA_ADMIN_PASSWORD, ARGOCD_ADMIN_PASSWORD, HARBOR_URL (**silent, no detector at all**). I was seeing this one only because someone added the guard yesterday. **Fix A (stop publishing) breaks the very step it repairs**: `.env.example:201` ships HARBOR_USERNAME COMMENTED (B14) and scenario-1 never asks the operator to type it, so `04:144` is the ONLY supplier on the documented path — delete it and `22:26`'s `:?` kills Step 9 before it reaches Harbor. **And on one key alone it manufactures a mismatched credential pair** (measured: `USER=robot$vks-cicd PASS=adminpw` → a 401 that reads like a wrong password). **Fix C** puts a Harbor credential in the sink `state_claim_kind` ARCHIVES on the next `kind-up`. **`05-kind-up.sh:129` is NOT the same bug** — `E2E_SKIP_DOTENV=1` makes the overlay the only sink there, so touching it would delete the KinD path's credentials for a bug it cannot reach. **SHIPPED:** `state_unset` (removes ONE superseded key, preserves 0600, no-op on absent key/file — not a breach of state.sh's never-`rm` promise, which is about the FILE) + `env_publish KEY VALUE why` = write `.env` → clear the overlay → assert; applied at BOTH writers and BOTH keys (`22:196-197`, `28:187-188`). 7/7 RED-proven, including case 1 (the defect still reproduces with bare `set_env_var`, so the suite is not vacuous) and case 3 (the mismatched pair, pinned so nobody ships half of it). **Also corrected:** the guard's advice — *"remove that line"* — told the operator to hand-edit `.env.state`, which `docs/scenario-1.md:43-45` says not to touch, and *"re-run"* could not succeed (a robot cannot mint robots → 409 → a hand-copy the design had eliminated). And `28`'s guard was **vacuous on this box**: it compared `admin == admin`, so you could always get back to admin and never forward to robot — that asymmetry WAS the bug. **Residual → B134, B135.** |
| **B134** | ✅ **CLOSED 2026-08-17 — an idea round returned `verdict: neither`, and the PREMISE was REFUTED at all six sites.** Measured in both directions against the real `02-env.sh` + real `lib/os.sh`/`lib/state.sh` on a throwaway root: every `env_set` in `env_populate` is **write-only-if-unset**, and "unset" is computed AFTER `load_env` (`02-env.sh:71`) has already sourced the overlay — so the overlay's presence turns the write **off**; it never gets a chance to lose. With the overlay holding all four keys, only `GITEA_CI_PASSWORD` (the one key with no overlay entry) was written; with it empty, all four were. `02-env.sh:153` even logs *"already set … not overwriting"*. Neither option A (uniform `env_publish`) nor B (a key list) fixes anything at these sites, and A buys three new `set -e` abort paths. **The proposed gate was also refuted by RUNNING it: true-RED 0, false-RED 2** — it cannot see `02-env.sh` (the sink is `"$ENV_FILE"`, not a `.env` literal) nor `27` (hand-rolled `_upsert`), and its only hits are two keys that are not `state_set`; its two REDs are a gate's own fixture, whose only remedy is mangling it. **Do not build it.** The round's real products are B138–B141 below. |
| **B135** | ✅ **CLOSED 2026-08-17 — ALREADY FIXED IN #770, and the scoping this row proposed was REFUTED by that PR's own round.** **`.env` is 0664 while the credential it holds is 0600 everywhere else.** Independent of B132, pre-existing. `22-harbor-robot.sh:171-179` is meticulous about `secrets/harbor-robot.env` — `rm -f` first, `umask 077`, 0600, seven lines of comment — and twenty lines later writes the SAME secret to `.env`, whose mode `set_env_var` PRESERVES (measured: **664**). `.env.state` is 600. Fix separately so the two stay independently revertable. ⚠️ **RE-MEASURED 2026-08-17 — the defect is REAL, but the row's '664' is a SYMPTOM OF ONE BOX and states the cause too narrowly.** On this box `.env` is **600**, not 664 — so the row's headline does not reproduce, and someone re-checking it would wrongly conclude it was already fixed. The actual defect is that **`set_env_var` never ENFORCES a mode**; it does `touch "$file"` and inherits whatever the ambient umask gives. Measured by driving the real function into a temp dir: **umask 022 → 644**, **umask 002 → 664** (the row's number — and this box's umask IS 002), and a **pre-existing 0644 file written under umask 077 stays 644**. That last case is the one that matters most and no umask can fix it — the corpus rule is *"umask protects a file only at CREATION; a pre-existing world-readable file KEEPS its mode"*. So the honest statement is: **the protection of the file holding the Harbor robot credential depends on the ambient umask of whoever first created it, and a loose mode is never repaired.** `.env` being 600 here is luck, not a guarantee. **The fix must chmod explicitly, not rely on umask** — and it should be decided together with which sinks `set_env_var` legitimately writes, since it takes the file as a required argument. ⚠️ **MEASURED by an idea round commissioned FROM THIS STALE ROW** (its brief was a verbatim copy of the row). `scripts/lib/os.sh:971` already reads `chmod 600 "$file" 2>/dev/null \|\| true`, placed exactly where this row's Q3 asks — before any content is written — via `83425e4` / **#770**. It ships with a gate, `scripts/test-env-file-mode.sh`, measured **6/6**. **The fix I was about to propose is the exact scoping #770's OWN round refuted:** *"`set_env_var` is not where the mode is decided. `env_init`'s `cp` births the file at 0664, and scenario-1 tells the operator to HAND-EDIT `VCENTER_PASSWORD` into it at Step 2 while the first hardening writer does not run until Step ~5."* The shipped fix touches **three** sites. **Two answers worth keeping:** the container path does not merely tolerate 0600, it **REQUIRES** it — `jumpbox-run.sh:17-37` excludes every `.env*` from the `/src` copy *because it cannot read them*, and that exclude is now DERIVED (`find`), not enumerated, after the rename-rot incident; and the cost is nil — **2000 `chmod` calls = 1038 ms**, no TOCTOU because the window between `touch` and `chmod` holds a **zero-byte** file. |
| **B136** | 🟢 **MEASURED 2026-08-17 — a keyword audit of the backlog produced 3 candidates for closure and reading REJECTED ALL 3. Second data point for the same lesson.** Grepping the 14 open 🔴 rows for `REFUTED` flagged B64, B66 and B26 as possibly closable. Reading each in full: **B64** — the ~1,700-line PORT was refuted, but the underlying doc claim (§2's *"browser work — not scriptable"*) is still FALSE and its F5 (*a candidate consumer at `docs/scenario-1.md:259`*) is explicitly UNMEASURED, so the row is open; **B66** — my own justification was partly refuted by me before work started, and the row still ends *"Design round required"*; **B26** — fixes (1)(2)(4) shipped and fix (3) is refuted, but the tail says in as many words *"Do not close this row on the inference alone"*, because the residual (`seccompProfile` on a package-installed VKS mesh) is INFERRED and step 13 of `docs/lab-validation-plan.md` exists to measure it. **0 of 3 closable; 3 of 3 false positives.** This repeats task #53's result exactly (an automated shared-key check produced 6 false positives that only reading could reject), so it is now two independent measurements of the same thing: **a keyword grep can NOMINATE backlog rows for closure and must never CLOSE one.** The one true positive was B37, re-graded 🔴→🟡 here — its verdict is DO NOT BUILD, its incident is closed (#295/#296/#297/#304), and even a printer was refuted, so nothing about it is open. ➕ **THIRD DATA POINT, 2026-08-17 — and this one is about COUNTING the rows, not closing them.** I tried to census this file automatically, twice, and got two different wrong answers from the same file within minutes: a HEAD-scan (does the row *start* with a closure marker?) said **28 open**, and a LAST-MARKER scan (these rows are append-only, so the last verdict wins) said **34 open** — MORE, after a session that closed four rows. Both are artifacts of the question. The head-scan misses closures appended at the TAIL, which is exactly where every closure in this file lands; the last-marker scan picks up a 🔴 belonging to QUOTED ORIGINAL TEXT further down the same cell. **There is no regex that reads these rows**, because a row is a long append-only prose history with historical verdict markers embedded throughout, and the current verdict is identified by MEANING, not position. ⚠️ **So do not report a backlog count as if it were measured, and do not build a better classifier** — that is the trap this row exists to name. Read the rows. If a number is genuinely needed, say how it was derived and that it is approximate. |
| **B137** | ✅ **CLOSED 2026-08-17 — see the end of this row for the full arc.** **A TLS-TRUST failure against argocd-server is reported as an RBAC DENIAL — and it points the operator at a tool that checks the wrong thing. MEASURED LIVE, matrix row 5, 07:17:43Z.** The argocd CLI's own error: `tls: failed to verify certificate: x509: cannot validate certificate for 192.168.101.131 because it doesn't contain any IP SANs`. What we then tell the operator: *"argocd-server refused to create Application 'javawebapp'. Does your AppProject 'default' permit this destination and repo? ('make argocd-preflight' checks exactly that.)"* — an AppProject/RBAC question for a transport failure, plus a recommendation to run a tool that measures RBAC and can never surface the cause. `70-configure-argocd.sh` (the Application-creation path, `Makefile:636 configure-argocd`). ⚠️ **THIS CORRECTS MY OWN CLOSURE OF B75, MADE ONE HOUR EARLIER.** B75's first HIGH was *"a TLS-trust failure against argocd-server is reported as a PERMISSION denial"*, cited at the `argocd account can-i … >/dev/null 2>&1` site. I measured that site GONE (`grep -rn 'account can-i' scripts/*.sh` = 0) and closed the row. That measurement was right and the closure was **correct-as-scoped and incomplete-as-a-class**: the INSTANCE was removed, the CLASS recurred at a sibling site, and the matrix produced the counter-example within the hour. Fixing one instance is not fixing the class — and I have the receipt. **ROOT CAUSE IS B68, now doubly measured:** ArgoCD's cert carries NO externally-valid name and NO IP SAN — on this cut its SANs are `localhost, argocd-server, argocd-server.cicd, argocd-server.cicd.svc, argocd-server.cicd.svc.cluster.local` — and this path dials it by **IP**. B68 records that `argocd.<domain>` answers 200 *"solely because every caller passes `--insecure`"*; this caller does not, so it hits the wall B68 describes. **AND THE LADDER CONTRADICTED ITSELF ONE LINE EARLIER:** the probe correctly said *"the argocd API probe FAILED TO ANSWER (STALE_CA) — this is NOT a denial. argocd-server never received the request, so no AppProject role will change it. Not selecting the api mechanism on a capability nobody measured"* — and the very next line reads `write mechanism: api  (kubectl=no, argocd-api=unknown)`. Either the ladder falls through to `api` as a last resort despite that sentence (then the sentence is misleading) or it is a genuine bug; **settle which before designing a fix.** **THE FIX IS NOT A BETTER MESSAGE ALONE** — `classify_kube_failure` already distinguishes STALE_CA and the probe already used it correctly; the defect is that the FAILURE PATH of the Application create discards the CLI's own x509 text and substitutes a guess. Capture the CLI's stderr, classify it, and say TLS when it is TLS — and do not recommend `argocd-preflight` for a transport fault. Evidence: `/tmp/walk/run-20260817T061515Z-1427190/MATRIX-row5-photon.log:2586-2601`. ✅ **THE BLOCKING QUESTION IS SETTLED, AND THE ANSWER IS NEITHER HYPOTHESIS THIS ROW OFFERED.** The row said: *either the ladder falls through to `api` as a last resort (then the sentence is misleading) or it is a genuine bug; settle which before designing a fix.* Measured in the source: the `unknown`-guard that dies (`70-configure-argocd.sh:289-294`) lives **only in the `auto` arm**, and `api) MECH=api ;;` (`:278`) bypasses the whole `case`. `ARGOCD_MECHANISM=api` is set **EXPLICITLY** by `docs/scenario-2.md:264` and by the harness at `nested-vsphere-lab/scripts/walk-matrix.sh:393`, and the `auto` die text appears nowhere in the row-5 log. So the ladder is not buggy and the sentence is not misleading *on the `auto` path* — the tenant path simply never reaches either. ⚠️ **AND MY OWN DIAGNOSIS OF THE REMEDY WAS FALSE, refuted by an idea round and then by my own measurement.** I claimed a name mismatch means *the anchor is irrelevant; fetching the CA cannot help*. Measured with go1.26.5 and a real TLS handshake (`/tmp/x509probe`): a leaf carrying DNS SANs only, dialled by IP, emits `x509: ... doesn't contain any IP SANs` **IDENTICALLY whether the CA is trusted or not** — Go verifies the HOSTNAME BEFORE the chain, so a name fault **MASKS** the anchor. The anchor is not irrelevant, it is **UNMEASURED**, and a message saying *do not re-fetch the CA* would strand the operator on a second round trip. **SHIPPED (commit `14b3712`):** (1) four measured x509 shapes added to `test-classify-kube-failure.sh` — two of the arm's three tokens had **ZERO** coverage (deleting `*"x509"*` AND `*"certificate is valid for"*` previously left **20 passed / 0 failed**; RED-proven in a sandbox, the same mutation now gives **20 passed / 4 FAILED**); (2) a guard refusing up front when `MECH=api` and the probe answered `unknown`, naming the reason already held 160 lines earlier — the run used to create a namespace, apply PSA labels and mint an image-pull secret **before** dying with a guess; (3) `argocd app create` **and** `argocd app wait` now CAPTURE and CLASSIFY the CLI's stderr. `app wait` was **worse** than the site this row found: it sent stderr to `/dev/null` outright and then blamed repo-server/Gitea, a different component. ⚠️ **NO NEW CLASSIFIER VERDICT, deliberately.** The round measured **7** case-form consumers, not the 15 my file-grep suggested — and **all 7 are kubeconfig sites**, where the anchor (`certificate-authority-data`) and the address (`cluster.server`) travel in ONE file so both faults have the identical remedy. A split would turn 7/7 RED for a remedy delta of **0/7**, while `70-configure-argocd.sh` — the one place the anchor and the address are independent knobs — does not call the classifier at all. **RESIDUAL:** `argocd repo add` (`~:551`) is still unclassified, and the walkbox `/etc/hosts`→`argocd-server` route (a bare SAN that measurably gives FULL verification with no `--insecure`) is not built. ✅ **CLOSED 2026-08-17 — VERIFIED ON `main`, not taken from the task list.** The fix landed in **#719** (`fix(argocd): a TLS transport fault is no longer reported as an RBAC denial (B137)`), and `main` now carries **7** transport-aware lines. Read at the die site (`70-configure-argocd.sh` api arm, ~:500-507): the CLI's own stderr is captured to a temp file, classified by `classify_argocd_failure`, and **`argocd_transport_die "$_ac_cls" …` fires FIRST** — the AppProject/RBAC message is only reached when the class is NOT a transport fault, and it now APPENDS `the CLI said: ${_ac_txt}` so even the RBAC path shows the evidence rather than substituting a cause. ⚠️ Note the RBAC sentence is still present at that line and that is CORRECT — the defect was never the sentence, it was the sentence firing for a transport fault; a grep for the old wording will therefore still hit and must not be read as a regression. |
| **B138** | ✅ **CLOSED (#729) — and the OBVIOUS fix was a REGRESSION, measured by an implementation round before it shipped.** One `env_publish` per key aborts under `set -e` after key #1, so #2/#3 never run: `VKS_AUTH_METHOD` stays `vcf` — the *stop talking to the Supervisor* switch the ORIGINAL `_upsert` **did** flip — while `.env` already shows `KUBECONFIG=<guest>`, so an operator reading the file believes the switch happened. **Strictly worse than the no-op it replaced.** Shipped instead: TWO PHASES — write all three + clear all three overlay pins, THEN assert all three, collect, die ONCE. Keeps `_upsert`'s all-or-nothing write and adds the gate. Also: the *"bare KEY=VALUE — no quotes"* comment was FALSE (`set_env_var` quotes on space/`$`) and is rewritten — verified safe, zero make-expansions of the three; `NOW RE-SOURCE IT` KEPT and strengthened (`assert_env_effective` unsets the key first, so it is structurally blind to an exported `KUBECONFIG` — a green assert is NOT "effective"); the log still says `wrote to`, so `scenario-1.md:476`'s Expect still matches (replayed through walk-doc's own extractor — no doc churn, no false `NOT SEEN` in run 4). **New test `test-use-guest-kubeconfig.sh`, RED-proven both ways: 4/4 on the shipped form, 2 FAILED on the refuted one.** I also caught a VACUOUS assertion in my own test first — it COUNTED the keys, which returns 3/3 on both forms because `setup()` writes two of them; replaced with a value assertion, which is why the refuted form fails 2 cases and not 1. Liveness of the original defect remains UNMEASURED (unchanged); the fix is right either way. |
| **B139** | 🟢 **CLOSED 2026-08-18 — the re-specified fix SHIPPED, both arms RED-proven, and the defect was a MISLEADING ERROR rather than the rc it was filed under.** `assert_env_effective` now discriminates before accusing: under `SKIP_DOTENV=1` it re-reads with `.env` ENABLED, and if the value is there it WARNS (naming why the write is unverifiable in-process) and returns **0**; a genuine shadow still FAILS and still NAMES the file. Measured across the row's own three states: `SKIP_DOTENV=1` + nothing shadowing → **rc=0 + WARN** (was rc=1 telling the operator to remove a line that does not exist); `SKIP_DOTENV=0` → rc=0, unchanged; `SKIP_DOTENV=1` + a real `.env.kind` shadow → **rc=1** naming `.env.kind`. Full discriminating power retained, which is what the row's FIRST prescription ("special-case the message") would have destroyed. `test-env-publish.sh` gained case 8 — **4 assertions, both arms** — taking the suite 11 → 15. RED-proven: reverting the fix fails EXACTLY the 2 arm-1 cases while arm 2 keeps passing, because the shadow path is unchanged by design. ⚠️ **And my first version of that case hit the trap the file documents ONE CASE ABOVE:** `_out="$(cmd)"; rc=$?` ABORTS under `set -e` when the command fails, so arm 2 never ran and the suite exited 1 having printed no summary — case 7's own `|| true` comment records the same thing. Fixed to `&& rc=0 || rc=$?`. Original follows. — 🟡 **RE-SPECIFIED 2026-08-17 — the row cites the WRONG FUNCTION, and its prescribed fix throws away a real discrimination.** ⚠️ **`env_populate` contains ZERO `env_publish` calls** (`grep -c env_publish scripts/02-env.sh` → **0**; `02-env.sh:318` is a fragment of an error-message string), so the row's whole latency argument describes a function that cannot reach the defect. The real callers are **22-harbor-robot.sh, 28-harbor-admin-password.sh and 27-use-guest-kubeconfig.sh** — all operator steps of the scenario-1 walk. The mechanism is confirmed: with `SKIP_DOTENV=1`, no overlay and no legacy file, `env_publish` returns rc=1 saying *"a higher-precedence file already sets HARBOR_PASSWORD"* and lists **zero files**, because none does; the identical call with `SKIP_DOTENV` unset returns rc=0. **But "special-case the message" is the wrong fix**: a write to `.env` under `SKIP_DOTENV=1` is not useless, it is *unverifiable in-process* and WILL take effect on a normal run — and warning blindly throws that away. **Better fix, measured across three states:** re-read with `SKIP_DOTENV=0` inside `assert_env_effective`; nothing shadows → WARN (*"`.env` is ignored in this process; a normal run WILL see this"*) and succeed; `.env.kind` shadows → genuine FAIL with the correct message; overlay shadows → FAIL. Full discriminating power retained. Also add the missing `SKIP_DOTENV=1` case to `test-env-publish.sh` — the `.env.kind` case landed with #731. 🔬 **RE-MEASURED 2026-08-18, all three premises CONFIRMED, and the defect is worse than "rc=1" — it is a MISLEADING ERROR.** (1) `grep -c env_publish scripts/02-env.sh` → **0**, so the row's original latency argument really did describe a function that cannot reach the defect. (2) The live callers are exactly `22-harbor-robot.sh`, `27-use-guest-kubeconfig.sh`, `28-harbor-admin-password.sh` (plus two tests) — all scenario-1 operator steps. (3) The same `env_publish HARBOR_PASSWORD` call in a throwaway `REPO_ROOT`: `SKIP_DOTENV=1` → **rc=1**, `SKIP_DOTENV=0` → **rc=0**. **The message is the real harm:** with NOTHING shadowing it says *"a higher-precedence file already sets HARBOR_PASSWORD … remove that line (or fix the writer that put it there) and re-run"* and **names no line at all** — an operator is sent to delete a line that does not exist. With a genuine `.env.kind` shadow the identical code path DOES name it (`…/.env.kind:1  <- this one wins`). ⇒ **the presence of a named file IS the discriminator**, which is precisely what the prescribed re-read-with-`SKIP_DOTENV=0` fix restores. ⚠️ My first probe reported "zero files" for BOTH states — its extractor was broken and discriminated nothing; the numbers above come from reading the real messages. **Not yet implemented:** the fix lands in `scripts/lib/os.sh`, which was under adversary review when this was measured, and editing a file mid-review is the moving-tree mistake this session already made twice. |
| **B140** | ✅ **CLOSED 2026-08-17 — option 1 REFUTED TWICE, option 2 ALREADY IMPLEMENTED. Do not reopen.** An idea round measured both. **Option 1 ("clear `.env.kind` too") is refuted at BOTH placements:** patched AFTER `state_unset`'s early returns it is **dead code** — in B140's own scenario the legacy file holds the key and the overlay does not, so `grep -qE … || return 0` exits first, and `test-use-guest-kubeconfig.sh` passed **4/4 over a fix that did nothing** (a vacuous green). Patched BEFORE them it makes `assert_env_effective` **structurally unfailable**: precedence is `.env.example` → `.env` → `.env.state` → `.env.kind`, so once the publish clears both higher sinks nothing can shadow `.env` — measured, B138's RED-proof goes **3 passed / 1 FAILED**, i.e. the fix removes the only shadow its RED depends on. It would also make a file the repo documents as *read-only back-compat, nothing writes it* mutable again from a write path with no stamp, no ownership check and no archive. **Option 2 is already shipped:** with `.env.kind` pinning the key, `env_publish` returns rc=1 and prints `…/.env.kind:1  <- this one wins` plus the remedy — the row itself conceded this and prescribed a fix anyway. Only residual, optional: that diagnostic does not name `make state-migrate`, which is the sanctioned remedy. |
| **B141** | 🟢 **LOW — `scripts/test-env-lifecycle.sh` passes `ENV_FILE=.env` at five sites believing it matters; it is INERT.** `02-env.sh:31` is an UNCONDITIONAL `ENV_FILE="${REPO_ROOT}/.env"`, so it is byte-identical to `env_publish`'s hardcoded sink and cannot be overridden — the test's `:32/:37/:42/:57/:91` assignments do nothing. Harmless today; the hazard is that the obvious "make it overridable" edit (`${ENV_FILE:-…}`) would silently split writes between `$PWD/.env` and `$REPO_ROOT/.env`. **Fix:** leave `:31` unconditional and drop the inert `ENV_FILE=` from the test, so nobody trusts it. Round's Q6, recorded as the better long-term shape: gate the **SINK**, not the key — make `env_publish` the only path allowed to target `${REPO_ROOT}/.env` (a literal, therefore greppable, with an always-available RED), which subsumes B138 and needs no enumerated key list. That is a control DESIGN and needs its own RULE ZERO round. ✅ **RE-VERIFIED 2026-08-18 — holds exactly, with one sharpening.** `02-env.sh:31` is `ENV_FILE="${REPO_ROOT}/.env"`, unconditional (no `${ENV_FILE:-…}`), and `test-env-lifecycle.sh` carries exactly **5** `ENV_FILE=` assignments (`:32 :37 :42 :57 :91`), every one inert. **The sharpening the row does not state:** those same call sites ALSO set `REPO_ROOT="$WORK"`, and since the sink is `${REPO_ROOT}/.env`, **`REPO_ROOT` is what actually redirects the writes** — so the tests do work, just not for the reason the `ENV_FILE=` implies. That makes the inert assignment actively MISLEADING rather than merely redundant, which strengthens both the row's fix (drop it) and its warning that the obvious "make it overridable" edit would split writes between `$PWD/.env` and `$REPO_ROOT/.env`. |
| **B142** | 🟢 **CLOSED 2026-08-18 — the round's re-specified predicate SHIPPED, all three states measured, RED-proven.** `load_env` now publishes **`_VKS_STATE_SOURCED`** from INSIDE its own `if state_check` branch (`lib/os.sh`), and `state_unset` refuses to edit a sink this process never sourced. That is the write-path predicate this row argued for: `state_check` is a READ-path concession (it PERMITS on an unstamped sink and returns 0 early when `_VKS_EXPLICIT_KUBECONFIG` is empty), which is why three keys could be stripped from a sink stamped for ANOTHER cluster. MEASURED, all three states: `=1` → key REMOVED (the ordinary path is untouched); `=0` → sink **LEFT ALONE**, rc **0**; **unset** → PROCEEDS, because `load_env` legitimately never ran for many callers and a fail-closed default would break every one of them. **The refusal returns 0, not 1** — the caller's intent ("this key must not be pinned") is already satisfied by an unsourced pin, and rc=1 would abort `env_publish`'s pair under `set -e`, which is the half-written-credential defect `test-env-publish.sh` case 7 exists for. Case 9 pins all three; suite 15 → 18. RED-proven: removing the guard fails EXACTLY the `=0` case while `=1` and unset keep passing. ⚠️ **Knock-on caught by the gate, and fixed at the right end:** writing `export SKIP_DOTENV=0` inside B139's subshell made shellcheck raise **SC2031 at `creds.sh:378`** — a file that legitimately reads the AMBIENT value. A prefix assignment (`SKIP_DOTENV=0 load_env`) reaches the function just as well and cannot escape the command substitution; disabling the warning in creds.sh would have been the wrong end. Original follows. — 🟡 **RE-SPECIFIED 2026-08-17 by an idea round — the original prescription was under-specified and the stated conflict was aimed at the wrong line.** Three corrections. **(a) The gate MUST return 0, not refuse.** `env_publish` had `command -v state_unset … && state_unset "$key"`, and `state_unset` is the LAST command of an `&&` list — so under `set -e` a non-zero return aborted the function BEFORE `assert_env_effective`, silently skipping the whole control (measured: a probe prints `wrote` and never `ASSERT-RAN`). **Already hardened to `|| log_warn` in #731**, which also closes the latent copy via `state_unset`'s own `>=2` (ENOSPC) arm. **(b) My stated worry — that 27's `|| true` would half-succeed — is REFUTED**: measured, 27 returns rc=0 with the correct effective triple even when the overlay is refused, because a refused sink is not sourced and its pins are inert, so clearing them was never load-bearing. **(c) A `state_check`-EQUIVALENT gate is BLIND in the majority case** and is the wrong predicate: `state_check` permits on an unstamped sink AND returns 0 early when `_VKS_EXPLICIT_KUBECONFIG` is empty — a read-path concession with no meaning on a write path. Measured with no explicit `KUBECONFIG`, three keys were stripped from a sink stamped for another cluster and `state_check` never refused. **Right predicate: "was this sink SOURCED in this process"** — have `load_env` export `_VKS_STATE_SOURCED=1` inside its `if state_check` branch and `0` otherwise; `state_unset` warns and returns **0** when it is `0`; **unset ⇒ proceed** (no change for callers that never ran `load_env`). If the sink was not sourced its pin cannot be shadowing, so skipping is not merely safe, it is correct. ⚠️ **And do NOT archive**: the row cited `state.sh:177-179` as support, but that reasoning does not transfer — `state_claim_kind` archives because it is about to overwrite the WHOLE file; `make use-guest-kubeconfig` is not. Archiving here re-creates the read-path hazard `state_check` was written to avoid. **Skip-with-warn, never archive.** |
| **B143** | ✅ **CLOSED BY VERIFICATION 2026-08-18 — the WARN noise is GONE and both residuals are resolved.** Measured on the current tree: `check-how-provenance` emits **0 WARN lines** (was 24) and reports `OK — 134 acquisition line(s): 66 command-checked, 25 provenance-graded, 43 non-command (answers + wrapped prose, NOT command-checked)`, rc=0. The 43 lines that CORRECTLY carry no command are now CLASSIFIED rather than warned about — exactly what the row asked for, and without the tagging it forbade. **The row's own residual was honoured, not assumed:** it said fixes 1/2/6 would GROW the corpus and to *re-run the denominator before trusting the green* — done: the corpus grew from 86 `# *how` matches to 134 acquisition lines, and the three categories sum exactly (66+25+43=134). Its second residual (the header citing 921-924, which is unrelated KinD prose) is also gone — the gate's header no longer cites those lines. Original text follows. — **`check-how-provenance` WARNs 24 times on every `make ci`, and 22 of the 24 are lines that CORRECTLY have no command — measured 2026-08-17.** The gate exists because a FABRICATED `vcf` command once shipped, so its target is *a command that looks runnable and was never run*. Its matcher is `grep -n '# *how'` (`check-how-provenance.sh:69`) — no colon required, and no distinction between a COMMAND and an ANSWER. Full denominator over `.env.example`: **86** `# *how` matches — **6 are not `# how:` lines at all** (L365/400/478/510/780/896: wrapped PROSE whose sentence merely continues with *"how a …"*), **6 carry a real command**, **74 are guidance**. Of the **24 WARNed**: **1** is genuinely a command with no provenance (L401, `` run `argocd --help` ``), **1** is the prose false positive (L400), and **22** are answers like *"you choose it"* / *"leave unset"* / *"request it from the platform team"*. ⚠️ **DO NOT silence them by tagging.** A provenance grade on a line that makes no factual claim is a gate whose only remedy DEGRADES the artifact — the pattern `gates.md` says refutes a gate on sight. The discriminator should be *"does the answer contain a COMMAND?"*, not *"does every `# how` carry a tag"*, and the matcher should require the colon. **NOT changed here:** altering a control is exactly the act CLAUDE.md says most needs an idea round, and the gate is WARN-only (`make ci` rc=0), so this is noise, not a blocker — but 24 WARNs per run is how a gate stops being read. Cheapest honest first step is the colon (kills 6 FPs, zero judgement); the command-vs-guidance split needs the round. ⚠️ **RE-MEASURED 2026-08-17 — this row's own characterisation of the 6 no-colon lines is WRONG, and wrong in the direction that would WEAKEN the gate.** The row says all six are *"wrapped PROSE whose sentence merely continues with 'how a …'"*. I read all six: **FIVE are legitimate provenance lines** — `# how (verified command shape; …)` at L365, `# how (INFERRED — Broadcom 9.0-via-redirect docs; we have never run it on a lab):` at L478 and L896, a similar INFERRED at L510, and **L780 `# how (verified): kubectl get deploy -A -l app=istiod …` which carries a REAL COMMAND**. Only **L400** (*"how a trust-anchor failure came to be reported to operators as a permissions denial"*) is genuine prose. ⇒ **The obvious fix — require a colon immediately after `how` — would silently STOP CHECKING five real provenance lines, including one with a live command.** Anyone acting on the row's wording would weaken the gate while believing they were sharpening it. **Numbers re-confirmed:** `# *how` matches **86**, with-colon **80**, without **6**, WARN lines **24**, and the gate exits **OK** — so all 24 warnings gate nothing. **AN UNRECONCILED DENOMINATOR, named rather than hand-waved:** the gate reports *"all **85**"* while `grep -c '# *how'` finds **86**. One line is consumed differently and I have NOT explained which — a gate whose denominator cannot be reproduced should not be tuned until it can. **And my own 'command vs answer' split is NOT trustworthy either:** counting WARNed lines that contain any of `kubectl|helm|crane|openssl|curl|argocd|vcf|tkn|make|docker|podman|git|jq|yq|awk|sed|grep|base64` gave **3 command-shaped / 21 pure answers**, against the row's *1 genuine + 1 prose FP + 22 answers* — but that token list certainly over-matches prose containing the words *make* or *git*, so the 3 is an upper bound, not a finding. **The real question is whether COMMAND-vs-ANSWER is decidable by a matcher at all**, and if it is not, the honest options are a one-line summary (*21 guidance lines not checked*) or dropping the per-line WARN entirely — the hard-fail arm already exists. Idea round dispatched with these corrected inputs; do NOT implement from the row's original prose claim. **Whatever ships must still catch the FABRICATED-command class the gate was built for** — that is the positive control, and a fix that loses it is a regression however quiet the output becomes. 🔴 **BOTH ROUNDS ARE IN, and the IMPLEMENTATION round (2026-08-17, `adversary-bash-git-cli`, instruments PERMITTED) returned `ship-with-changes` with 10 findings — 4 HIGH, and one of them RE-OPENS THE ORIGINAL HOLE. DO NOT SHIP THE DRAFT.** **THE PREMISE HOLDS, independently reproduced:** 21 vendor-CLI **lines** (24 occurrences), **0** on a `# how` line, old gate `OK — all 85`, rc=0, 24 WARNs. **TWO CORRECTIONS TO MY OWN HEADLINE:** (a) my *"overstates coverage by 39%"* is **not reproducible** — the three defensible figures are answers-mislabelled-as-commands **24.7%** (21 of 85), old-85-vs-new-67 **+26.9%**, corpus growth **+37.6%**; direction right, number invented. (b) the rewrite scans **2 of the 21** vendor lines (543, 927), not 21 — *"the gate now sees vendor commands"* is true and far narrower than it reads, and the real `vcf context create/use` block at **950-954** stays unscanned because it carries no `# how` token at all. **THE FOUR HIGHS, each measured by executing the proposed script in an isolated `REPO_ROOT`:** **(F3) grade inheritance LAUNDERS anything appended under a graded header** — a fabricated `vcf … --export-file /tmp/fabricated` added under the real graded header at :540 took `graded` 16→17 and **rc=0**. That is the ORIGINAL INCIDENT'S EXACT SHAPE, re-admitted by my own fix. Run the VENDOR check even when inherited, unless the line ITSELF is graded. **(F4) the grading regex REJECTS `Lab-verified` — this repo's STRONGEST grade — while accepting `(inferred)`, the weakest** (`# how (Lab-verified 2026-07-22): vcf …` → rc=1). Both spellings are in `.env.example` today. Inverted incentive, and the precise class that gets a gate loosened. Restore the old `-i` semantics: `[[ ${1,,} =~ \((lab-)?(verified|inferred) ]]`. **(F1) the bullet-reset `^#[[:space:]]*-` also matches a `--flag` CONTINUATION** and kills the block — the wrapped shape exists in the real file at 928-929 and 952-953. Use `^#[[:space:]]{0,3}-[[:space:]]`. **(F2) the block dies on ordinary 3-space prose**, and 3-space is this file's explanatory style — a fabricated `vcf context create --fabricated-flag` at 6-space indent left the corpus **unchanged at 117**, completely invisible. Use `{3,}`. **THREE MEDIUMS worth the same care:** `OURS` matches the RAW line not the payload, so an incidental `kubectl` mention in a trailing parenthetical launders a fabrication (rc=0, measured) — match the first token of `$pay`; `CMD_SHAPE` demands a `--flag`, so a FLAGLESS fabrication scores as an *answer*; and `make[[:space:]]+([a-z0-9-]+)` captures a flag, so `make -C apps/…` fails with *names 'make -'*. ⚠️ **F4 and F5 would BLOCK EVERY PR** the first time anyone writes `make -C` or `(Lab-verified)` in a how block — the gate is in `static-check-fast`, which runs per-PR. That raises them above their intrinsic severity. **CLEARED, do not re-litigate:** every `[[ =~ ]]` unquoted RHS is correct, no `((x++))`, shellcheck 0.11.0 rc=0, `_payload`'s whitespace idiom has no reachable edge case (0 `##` lines, 0 tabs), `grep -qE "^${tgt}:"` is NOT injectable (`tgt` is `[a-z0-9-]+`, 0 lowercase `name:=` in the Makefile), the vacuity contract still scores `ok`, and the rewrite deletes all **5** `printf \| grep -q` pipes the old gate carried. **RESIDUAL the round names:** fixes 1/2/6 will GROW the corpus and may surface new REDs on existing content — land them and **re-run the denominator before trusting the green**; and the header's own disclosed residual cites the wrong lines (921-924 is unrelated KinD prose). |
| **B144** | 🟢 **`00-install-prereqs.sh:5` states a FALSE capability as the reason for a decision that may be right for another reason.** It says *"the CLIs mise does not cover (tkn, argocd)"*. MEASURED 2026-08-17 against `mise registry`: **`tkn` is absent** (correct) but **`argocd` IS present** (false) — the same shape as the kubeconform/hadolint/yamllint claim in the SAME file that `.mise.toml:55` already records as *assumed, never checked. It does*. ⚠️ **Do NOT 'fix' this by moving `argocd` to mise**: CLAUDE.md's air-gap exception says `tkn`/`argocd` deliberately come from OS packages / pinned releases because mise fetches from the internet. The defect is that the comment gives a CAPABILITY reason for what is really an AIR-GAP reason — so a reader who checks the capability finds it false and may draw the wrong conclusion. Fix: state the air-gap reason, and keep `tkn`'s genuine absence as the separate fact it is. ✅ **RE-VERIFIED 2026-08-18 — the load-bearing fact re-measured, not recalled.** `00-install-prereqs.sh:3-7` still reads *"then ensures the CLIs mise does not cover (tkn, argocd) are present at pinned versions"*, and a `mise registry` lookup returns a row for **argocd** (`aqua:argoproj/argo-cd`, `asdf:beardix/asdf-argocd`) and **no row for tkn**. So the claim is FALSE for argocd and TRUE for tkn, exactly as the row says. Fix unchanged: state the AIR-GAP reason (CLAUDE.md's exception — mise fetches from the internet) and keep tkn's genuine absence as the separate fact. Do NOT move argocd to mise. |
| **B145** | 🟢 **REFUTED AGAIN 2026-08-18 — DO NOT BUILD. And the headline is that I RE-LITIGATED A SETTLED REFUTATION: I read this row's HEAD, quoted its ORIGINAL fix text and its ORIGINAL verification criterion VERBATIM into a fresh idea round, and never reached the `IDEA ROUND 2026-08-17` block further down that already refutes both with F1–F7.** The second round reproduced F2/F3/F4/F5 independently and agreed in direction and magnitude — i.e. the whole round was spent rediscovering what the row already said. Same failure class as B83. **READ A ROW TO ITS END BEFORE DISPATCHING ANYTHING AGAINST IT.** 🔴 **AND MY OWN 'CORRECTION' TO THIS ROW WAS WRONG, in the most instructive way.** I measured the evaluation semantics (`_seen -eq 0` is the ONLY UNMET condition; unmatched siblings become MASKED, printed and not ratcheted) and concluded the row's *"it will redden healthy rows"* was false. The semantics reading is CORRECT and the conclusion I drew from it is NOT: reddening arrives through a **different door** — a block going `_tot 0 → >0`, which was never counted at all, so it is not "an existing passing block". `docs/scenario-2.md:209-217` says so in writing: the literals there are *"DELIBERATELY on continuation lines … A line-1 literal therefore guarantees a FALSE UNMET"* on the branch that document calls the normal tenant case — which **both scenario-2 rows take**. So the fix would redden rows 5 and 6, which went green for the first time only in run 6. **I discarded a CORRECT conclusion because its stated mechanism was imprecise** — the documented "after a refutation, do NOT invert, RE-DERIVE" trap. ⚠️ **Three more reasons it stays unbuilt, each measured:** (a) it would take the `EXPECT_LINES_PARSED >= INDEP_E` floor from **slack 0 (46 vs 46, EXACT)** to slack ~61, after which ~26 of 46 paragraphs could be silently dropped and still pass; (b) it desyncs `check-expect-literals` (head-anchored, and its own header says it deliberately matches walk-doc's capture) — walk-doc would check ~20 literals that gate never sees, reopening B102's defect for them; (c) the prescribed verification is unsound in BOTH directions — the CHECKABLE denominator moves by exactly +1 and **that +1 IS the regression**, while removing the regression makes it move by 0 despite attaching 61 lines. And ~14–16 of the 20 new literals are permanently unmatchable (a version EXAMPLE prefaced "such as", an interpolation-broken failure string, `/etc/hosts` inside a sentence saying it is not needed). ✅ **TAKEN FROM THE ROUND INSTEAD:** the slack-0 floor is now DOCUMENTED at the counter (`walk-doc.sh:563`) with the heads-only restoration recipe, so nobody widens it casually; and `scenario-2.md`'s load-bearing comment cited `walk-doc.sh:346` when the real line is **452** — a reader following it lands ~100 lines away inside the include resolver. Both fixed. ⚠️ **A NEW BLINDNESS I CREATED:** scoping the round to my BRANCH SHA (the fix for briefing a branch NAME) made it blind to `check-expect-literals`, which landed on `main` AFTER the branch was cut — the branch was 1 commit behind. Rebased. **The rule is not "use a SHA": it is review at `origin/main`, or state the divergence in the brief.** Original follows. — 🔴 **HIGH — the walk's Expect checker TRUNCATES a multi-line `**Expect:**` paragraph to its FIRST LINE, so 26 of 43 paragraphs are only partly checked and the headline CHECKABLE count is over an undisclosed subset.** Found by the run-4 root-cause round, `ran-it`. `walk-doc.sh`'s parser attaches a claim only on `line.startswith('**Expect')`, so everything after the first line break is dropped. MEASURED: scenario-1 **31 paragraphs, 19 multi-line, 13 literals dropped**; scenario-2 **11, 6, 9**; common-bootstrap 1/1/0 — **26 of 43 truncated, up to 22 literals never checked**. The concrete cost in run 4: block 16's Expect printed **SEEN** on the very block whose `rc=1` killed the next 8, because the only checked literal was the label `VKS_K8S_VERSION:` that the script prints unconditionally — while the dropped continuation contained **`NOT overwriting it`**, the literal naming that exact failure mode. ⚠️ **Do not oversell it:** a block passes if ANY literal matches, so checking that string would have printed SEEN too — it would have been a **visible tell in the transcript, not a gate**. **Fix:** treat the Expect PARAGRAPH as the unit — keep appending to `out[-1]['e']` until a blank line, a fence, or a new block-level construct — then RE-MEASURE the CHECKABLE denominator across all six rows; a denominator that does NOT move means the change did nothing. ⚠️ **Land it AFTER a run certifies the tree**: it will surface previously-invisible UNMETs and could redden healthy rows for document defects that were always there. Needs its own idea round. ⚠️ **INDEPENDENTLY RE-MEASURED 2026-08-17 with a second counter, and the core claim is CONFIRMED EXACTLY**: scenario-1 **31 paragraphs / 19 multi-line**, scenario-2 **11 / 6**, common-bootstrap **1 / 1** — identical to the round's figures, so **26 of 43 truncated** stands on two independent measurements. The dropped-LITERAL count differs: my counter (literals appearing ONLY in the continuation, deduped per paragraph) gives **16** for scenario-1 where the round gave 13; scenario-2 agrees at **9**. So the honest figure is **at least 22 and measured 25 by a second counter**, not "up to 22" — the row's original wording understated it. The two counters differ only in how they dedupe/segment, not in whether the truncation happens; do NOT spend time reconciling them before the fix, because the fix's own success criterion is that the CHECKABLE denominator MOVES, which neither counter predicts. 🔴➡️🟡 **IDEA ROUND 2026-08-17 (`adversary-bash-git-cli`, both instruments PERMITTED, parser port validated byte-identical against the real extractor before use): THE FIX AS SPECIFIED IS REFUTED. Do NOT build it on the gating path.** My own pre-brief suspicion — that ANY-match makes ADDING literals weaken the checker — is **correct and UNDERSTATED**. `walk-doc.sh:686`: `_seen -eq 0` is the ONLY UNMET condition, so a block with >=1 matching literal can never be made to fail by adding more. **THE DECIDING MEASUREMENT** (both docs, faithful port of the PYX filter): Expect LINES attached 33->69 / 14->35, but blocks **CHECKABLE 24->24 and 10->11**. The fix doubles the attached text and changes the checkable count by **+1 across both documents** — and that +1 IS the regression. 🔴 **F1 CRITICAL — it does not fix its own motivating incident.** The run-4 block is `scenario-1 [18] "6. Guest cluster"`, old literal set **exactly `['VKS_K8S_VERSION:']`**. In run 5b row 1 that string appears **2x** in the step output; `NOT overwriting it` appears **0x**. Today `_seen=1` -> SEEN. After the fix `_seen>=1` -> **still SEEN, still exit 0**; the only change is one extra `unseen-literal:` line, which `:690-700` deliberately PRINTS AND DOES NOT GATE. 🔴 **F2 CRITICAL — the fix's ONLY gating effect is a FALSE RED, on the block the document deliberately designed.** `scenario-2 [13]` is the sole block going 0->checkable; its line-1 `DISCOVERY:` is filtered by `lit in blk`, and the fix adds `harbor-nginx`/`HARBOR_URL`/`ARGOCD_SERVER` from continuation lines. Branch 2 — which `scenario-2.md:216` calls *"the normal case for a tenant handed only a workload kubeconfig"* — emits ONE line containing **none** of them -> `_tot=3 _seen=0` -> `exit 1`. **Both** scenario-2 rows took branch 2 in run 5b (verified from `MATRIX-row5-photon.log:574-628` and `row6:537-539`). So the deliberate design at `scenario-2.md:209-217` is exactly right and the fix breaks it. (The `HARBOR_URL`/`ARGOCD_SERVER` that DO appear in the row logs are the walker's own `:543` diagnostics, never appended to `STEP_OUT_FILE` — they cannot rescue the match.) 🔴 **F3 HIGH — it destroys the tightness of the only floor guarding claim attachment.** `:723` asserts `EXPECT_LINES_PARSED >= INDEP_E`, where INDEP_E is a **grep over the sources**, deliberately independent of the parser. Today it holds at EXACT equality (33/33, 14/14) so dropping ONE Expect line trips it; after the fix it is 69>=33 and 35>=14 — **36 and 21 lines of slack**, enough to silently drop an entire paragraph. Restoring tightness means re-implementing the paragraph rule in BASH so two languages must agree — an unbudgeted cost the row never mentions. 🔴 **F4 HIGH — it re-opens the negation CRITICAL the repo already closed.** The negation guard (`:742`) scans ONLY `^**Expect` lines, and its own documented remedy is *"move the 'if it says X instead' sentence to a following line"*. After the fix, "a following line" IS the checked set, so the prescribed remediation becomes the vector for the false-PASS the guard exists to prevent, and the guard is structurally blind to it. Latent today (0 tight-pattern negations on continuation lines) but **4 continuation lines already carry conditional/alternative literals** (`scenario-1:443,444,502`, `scenario-2:746`), including a FAILURE-MODE literal the healthy path can never print. 🔴 **F5 HIGH — the row's success criterion is unsound in BOTH readings.** "The CHECKABLE denominator MUST MOVE": scenario-1 goes **24->24, it does not move at all**, while the change really did attach 36 lines — the criterion would call a real change a no-op; scenario-2's +1 is satisfied EXCLUSIVELY by the regression. Read as lines-parsed it moves 33->69 and is satisfiable by attaching prose yielding ZERO new checkable literals, which is what happens. Honest criterion: a **verdict-transition table over the six real rows**, not any denominator. 🟡 **F7 — ALL-match, the obvious alternative, is ALSO refuted, by measurement.** Run 5b rows 1-4 are GREEN yet carry **4 `NOT SEEN` literals each** (`already exists` = a claim about the other branch; `still waiting` = a transient; `./secrets/supervisor.kubeconfig` = a file-existence claim that can never appear on stdout). ALL-match turns **4 of the 4 green scenario-1 rows RED**. `:690-698` already says why: *"Some of these are also permanently unmatchable."* ⚠️ **THIS ROW'S OWN DENOMINATOR IS STALE:** the current tree has **46 Expect paragraphs / 27 multi-line**, not 43/26 — and scenario-1's 33 and scenario-2's 14 each INCLUDE common-bootstrap via `walk-include`, so the two do not sum. Also **all 46 paragraphs terminate on a BLANK line, 46/46** — zero are followed by a table, HTML comment, list, `<details>`, blockquote or a second `**Expect:**`, so the elaborate terminator set the row proposes is not the risk. ✅ **SALVAGE — what IS worth building:** paragraph-unit parsing feeding **ONLY the reporting path** (`EXPECT_UNSEEN` / `unseen-literal:`), never the gate. 7 blocks gain diagnostics, zero gating risk under ANY-match. Ship with (a) the F2 block exempted by an explicit marker, (b) the negation guard extended to whole paragraphs, (c) the floor recomputed paragraph-aware in bash, (d) dedupe (`scenario-2 [25]` would add `HARBOR_CA_FILE` **three times**, inflating `_tot`). **DO NOT BUILD:** any expectation that this strengthens the checker, or the "denominator must move" criterion. **SEPARATE, LARGER PROJECT:** a *designated must-appear literal* (a doc convention marking THE literal that must match, keeping ANY-match for the rest) — that is the only thing that would have caught run-4, and it is a 46-paragraph doc change plus a parser change. **RESIDUALS the round names:** the implementation does not exist, so it tested *a* reasonable paragraph rule — a different terminator set changes F2/F3 magnitudes but not their direction, and F1 is terminator-independent; it did not machine-reconstruct `STEP_OUT_FILE` for all six rows (F1/F2 verified by targeted extraction from the real logs); run 5b predates the current docs (41/25 blocks then vs 42/27 now), so blocks were matched by **heading + literal text, not by number**. ⚠️ **RE-SCOPED 2026-08-18 by run 6 — the harness ALREADY SURFACES this class, as MASKED, so the fix moves a number rather than revealing a hidden one.** `walk-doc.sh:745-753` reports `N block(s) with a MASKED literal`: a block passes when ANY literal on the Expect line matches, so a sibling that did NOT appear is silently discarded. That is the same claims this row is about, already counted. MEASURED across run 6's four scenario-1 rows: **rows 1+3 identical at 22 CHECKABLE / 4 MASKED; rows 2+4 identical at 18 / 5 — across DIFFERENT operating systems**, and scenario-2 at 8 / 0. So the OS contributes NOTHING to Expect coverage; all variation is which CELL ran. That independently confirms the walker's own argument for NOT ratcheting the count ("varies by CELL, not by document quality" — a ratchet would RED a healthy row and would be measuring which cell ran). The masked claims decompose **3 STRUCTURAL + 3 cell-specific**, which reconciles 4-vs-5 exactly: `[09] Log in to the Supervisor`, `[14] Harbor` and `[28] Harbor's admin credential` are masked in EVERY row; `[07] vSphere Namespace`, `[23] Preflight` and `[31] Supervisor kubeconfig` in two each. The three STRUCTURAL ones are the honest targets — unlike the cell-specific three they cannot be explained away by "the wrong cell ran". ⚠️ **LAND ONLY AFTER a run certifies the tree**: this change surfaces previously-invisible claims, so it can turn MASKED into UNMET, and an EXPECT UNMET exits 1 — one line of prose has already killed a whole matrix run once. |
| **B146** | ✅ **CLOSED 2026-08-17 — IMPLEMENTED IN CODE, verified by READING THE CODE rather than the comment beside it.** `24-vks-k8s-version.sh` does `_osi_rc=0` … `|| _osi_rc=$?` with stderr to `$_TKR_OSI_ERR`; on non-zero it FAILS CLOSED (truncates, sets `_OSI_UNUSABLE=1`) and emits a **latched** warning — one copy, not 60 — routed through `classify_kube_failure`, with FORBIDDEN-specific guidance naming `kubectl auth can-i list osimages` and stating that osimages is a DIFFERENT resource from kubernetesreleases so the kubeconfig must NOT be re-fetched. That is the row's prescribed fix, present and executing. Its comment reads in the PAST tense about the old `2>/dev/null`, which is exactly why I read the code: a comment describing a fix is not the fix, and that trap fired twice elsewhere today. **MED — `24-vks-k8s-version.sh`'s OSImage read is FAIL-SILENT, and it is the same class the script's own comment says it fixed for the sibling read.** `:54-56` ends `|| : >"$_TKR_OSI"`, so an RBAC `Forbidden` or a transport failure on `get osimages` is **indistinguishable from "no image exists"**. Consequence: `have[]` is empty → `_newest_ready` returns nothing → a **900 s wait** → `die` offering two hypotheses, neither of which is *"I could not read the OSImages"*. That is the same 900 s misdiagnosis shape B131 just fixed in the harness, in the OTHER arm of the same function — while `:84-88` is a proud comment about no longer blaming the content library on a probe that never completed, for the **TKr** read three lines later. **Fix:** capture the osimages rc and route it through the existing `classify_kube_failure` ladder; on `FORBIDDEN`, say so and fall back to TKr-only filtering with a warning rather than waiting 900 s. **Also here, the run-4 round's candidate (d) as a WARNING (not a refusal):** when `VKS_K8S_VERSION` is pinned and no OSImage exists for the requested os-name, print the cause and name the newest release that HAS one — ~10 lines against data already in `$_TKR_OSI`, **zero false-refuse cost**, and it converts an opaque admission denial into a named cause one command earlier. ⚠️ It must stay a WARNING: `k8s/vks/cluster.yaml:21-28` records that `spec.topology.version` is a PREFIX selector and *"the value in .env is admissible and is NOT in that list"*; the osimages read is fail-silent (above); and `scenario-1.md:437-440` tells the reader a fresh Supervisor's images land minutes later. A refusal would false-refuse all three. This row is the prerequisite if (d) is ever promoted to a refusal. |
| **B147** | 🟢 **`VKS_OS_NAME` is a DECOUPLED knob — setting it changes what the RESOLVER picks but not what the WEBHOOK asks for, so a non-default value is a guaranteed denial.** MEASURED by the run-4 round: `grep -rn VKS_OS_NAME` over the whole tree returns **one reader**, the awk filter at `24-vks-k8s-version.sh:73`. `k8s/vks/cluster.yaml` sets **no** `os-name` and no `osImageSelector` — the `os-name=photon` in the admission denial is the `builtin-generic-v3.6.0` ClusterClass DEFAULT (confirmed in the run-4 log: `class: builtin-generic-v3.6.0`). So `VKS_OS_NAME=ubuntu` makes the resolver pick a release with an ubuntu image while the webhook still demands photon → identical opaque denial. `.env.example:1118-1124` warns in prose (*"Change this only if your ClusterClass's osImageSelector asks for a different OS"*) but nothing enforces the coupling. **Low priority.** If B146's warning is built, have it report the OS it filtered on AND state that it does not control what the webhook asks for. ⚠️ Related and settled: row 1's *"ubuntu"* is the **JUMP BOX** distro (`WALK_OS` selects apt vs tdnf), NOT the node OS — the guest nodes are photon on every row, from the ClusterClass. Asking for `os-name=photon` on the ubuntu row is **correct**; a "fix" that made it request ubuntu would break every row. ✅ **RE-VERIFIED 2026-08-18 — substance HOLDS, the LINE NUMBER ROTTED (`:73` → `:110`).** A raw `grep -rn VKS_OS_NAME` now returns **12** hits, which reads as a refutation until discriminated: **6** are inside `log_warn`/`log_error`/`log_info` STRINGS (mentions, not reads), **0** are comments, and exactly **1** is a genuine behavioural read — the awk filter, now at `24-vks-k8s-version.sh:110`. `k8s/vks/cluster.yaml` still sets **0** `os-name`/`osImageSelector`, confirming the denial's `os-name=photon` is the builtin-generic ClusterClass default, not ours. ⚠️ **A MEASUREMENT HAZARD worth carrying:** the 12th hit was `.claude/worktrees/agent-<id>/scripts/24-vks-k8s-version.sh:110` — a COPY of the same file inside a LIVE adversary's worktree. An agent worktree silently INFLATES every tree-walking grep with duplicates, so a count taken while an agent runs is not the repo's count. The repo already knows this class (`.claude` is excluded from `trivy-config` for the same reason) but a hand-run `grep -rn` has no such exclusion — exclude `.claude/worktrees` whenever the NUMBER is the finding. |
| **B148** | ✅ **CLOSED 2026-08-18 by verification — all four items shipped, and (3) shipped MORE than it asked.** I came here expecting live work that would fail rows 5+6. Measured against the code instead. **(1) doc** — `scenario-2.md:280` now says `ARGOCD_SERVER` is *"a **name** the argocd-server certificate carries — **not** the bare LB IP"*, `:223` explains that what you discovered is an IP and the name is what you map it to, and `:287` adds the whole *"The ArgoCD address, in the ONE order that works"* section (hosts entry -> `make fetch-argocd-ca ARGOCD_SERVER=argocd-server` -> then the NAME in `.env`). **(1b) `.env.example:373`** prescribes `make gitops ARGOCD_SERVER=<a-name-the-cert-carries>` and additionally documents the env-prefix trap it used to recommend — measured: with `.env` carrying an IP, `ARGOCD_SERVER=1.2.3.4 make <target>` still yields the `.env` value, because `-include .env` makes a FILE-defined make variable that beats an environment one. **(2) the asymmetry is closed** — `70-configure-argocd.sh` routes through `ca_verifies_endpoint` (3 call sites) and branches on its rc, so ArgoCD is no longer the only one of the three (Harbor, Supervisor, ArgoCD) that did not. **(3) the SAN diagnostic** is there AND carries a trap the row did not anticipate: `:431` — *DERIVE it from the SANs, never hardcode, and NEVER print it on rc 0* — because an earlier version asserted "It carries NO IP SAN" directly beneath a printed IP SAN. **The RECONCILE item is guarded, not live:** `09-argocd-address.sh:96-97` overwrites `ARGOCD_SERVER` only when it is a placeholder or already equals the discovered IP; a name-based answer makes it WARN and leave the value alone, so it does not regress on the next run. **What remains is NOT this row:** the walk HARNESS still stages the IP (`nested-vsphere-lab/scripts/walk-matrix.sh`), which is B160 and tracked separately — the repo-side document and code are correct. | — |
| **B163** | ✅ **CLOSED 2026-08-18 by verification — all three findings are shipped, and F1 shipped MORE than it prescribed.** Checked line by line against the code rather than taken on trust (the B171 lesson: a row's status is a CLAIM). **F1** — `argocd_session_token` (`lib/argocd.sh:424-433`) already captures `-w '\n%{http_code}'` AND curl's rc and writes `rc=%s http=%s` to a diag file; the consumer reads both (`argocd-auth-check.sh:167-170`) and prints `NO token (curl rc=…, HTTP …)`; `argocd_session_explain` is tried FIRST and the two-item ADDRESS/ANCHOR list is reached only when the cause is genuinely undetermined **AND** `ARGOCD_TLS_MODE = verified`. That last conjunct is the incident's own lesson encoded: under `-k` the certificate's names are never consulted, so the list cannot be honest, and it is SUPPRESSED rather than annotated — the code comment says so verbatim. **F2** — the `/healthz` readiness poll IS in `argocd-auth-check.sh:128-140`, bounded by `ARGOCD_READY_ATTEMPTS` (default 30) × `ARGOCD_READY_INTERVAL_SECONDS` (default 2), i.e. the `*_WAIT_SECONDS`-style knob the row asked for; and it goes further, testing the status so a 404 or an Envoy `503 no healthy upstream` cannot break the loop during the exact K1.5 race the poll exists to wait out. **F3** — `require_cmd jq curl kubectl` at `:50`, variadic, with the row's own reasoning in its comment. The DO-NOT-BUILD list stands unchanged and is still the valuable half: every candidate that changes the ADDRESS or the ANCHOR is an input to a code path that did not execute. | — |
| **B165** | ✅ **CLOSED 2026-08-17 — see the end of this row for the full arc.** **HIGH — `configure-argocd` FATALs "ArgoCD cannot clone the deploy repo" after 180s and names a cause that is MEASURABLY FALSE. It is the SAME CLASS as B163: a bounded wait expiring, reported as a definitive diagnosis.** Certification run 2026-08-17, row 5 (photon, scenario-2), block `[26] rc=1 (930s)`. Verbatim: *"Application 'javawebapp' never fetched a revision from `http://<gitea-lb>:3000/demo/javawebapp-deploy.git` in 180s … **Most likely GITEA_ARGOCD_URL is not reachable from the ArgoCD cluster** … FATAL: ArgoCD cannot clone the deploy repo — refusing to report success."* 🔴 **THE NAMED CAUSE IS REFUTED, measured from inside the repo-server pod on the still-live lab** (`kubectl -n cicd exec argocd-repo-server-… -c repo-server -- curl`): the Gitea homepage returns **http=200**, the repo path returns **200**, and — decisively — the actual clone handshake `/demo/javawebapp-deploy.git/info/refs?service=git-upload-pack` returns **200**. Reachability was never the problem. The Application's `spec.source.repoURL` is exactly that URL on `main`, with **zero** error conditions. 🔴 **AND "NEVER FETCHED" IS FALSE.** The same Application now reads `sync=Synced health=Healthy operationPhase=Succeeded`, carries a deploy HISTORY (`21:14:42, 21:15:50, 21:38:41, 21:39:46`), and **reconciled again at 22:02:47Z — about EIGHT MINUTES after our 21:54:20Z FATAL**. `gowebapp`, the control on the same path, is also Synced/Healthy. So the run gave up on a convergence that then happened on its own. ⚠️ **THE ARITHMETIC IS THE TELL, and it makes this a race BY CONSTRUCTION rather than bad luck:** our wait is **180s** and ArgoCD's default reconcile poll is **~3 minutes**. We wait almost exactly ONE poll interval, so a single missed poll is a guaranteed FATAL. **Graded `measured` for the refutation, `inferred` for the poll-interval mechanism** — I did not read this instance's `timeout.reconciliation` from the argocd-cm. **FIX (design, needs its own round):** (a) wait a MULTIPLE of the reconcile interval, and READ that interval from `argocd-cm` rather than hardcoding a number that happens to equal it; (b) FORCE a refresh instead of waiting for the poll — `kubectl annotate application <app> argocd.argoproj.io/refresh=hard` is the mechanism `git-workflow.md` already records for exactly this ("wait for the deployed IMAGE to change, NOT a generic Synced/Healthy"); (c) **stop naming a cause the check has not measured** — if it is going to say "most likely X", it must first PROBE X (the clone handshake from the repo-server is one exec away and returns a status code). That last point is B163's finding arriving a second time in one run, in a different file. ⛔ **Do NOT "fix" this by raising 180s to a bigger number** without (a) or (b): a longer arbitrary wait is the same race with a wider window, and it makes every genuine failure slower to report. | 🔴 **MY DIAGNOSIS IS REFUTED BY ITS OWN IDEA ROUND — 2026-08-17, and the refutation came from the code I had not read.** **(1) MY FIX (b) IS ALREADY IN THE TREE**: `scripts/70-configure-argocd.sh:770` annotates `argocd.argoproj.io/refresh=hard` **immediately before** the wait loop. Its presence refutes my claim 3 from the code alone — the run did NOT wait for a poll, it FORCED a refresh and still saw an empty field for 180 iterations, and no poll-interval story can explain a failure that happened after a forced refresh. **(2) CLAIM 3'S MECHANISM WAS WRONG ANYWAY**: a NEW Application is enqueued for refresh on ADD by the controller's informer, not on the next tick; `timeout.reconciliation` governs RE-reconciliation of an app that already has `ReconciledAt`. That my 180s equalled the 180 default is a **coincidence of two independently-chosen numbers** — precisely the kind that manufactures a confident wrong cause. **(3) MY CLAIM 2 WAS OVERSTATED BY ITS OWN EVIDENCE**: the deploys I cited (21:38:41 / 21:39:46) are BEFORE the wait window, which was **21:51:12Z-21:54:20Z** and is itself **UNOBSERVED**. The claim survives only on the stronger argument — `.status.sync.revision` is a PERSISTENT field that should have been readable regardless — not on the history. ✅ **THE ACTUAL DEFECT, CONFIRMED FROM THE RUN'S OWN LOG:** both reads are silenced — `:773` `rev="$(ka … get application … 2>/dev/null || true)"` and `:780` the conditions dump, also `|| true`. So NotFound, Forbidden, a wrong namespace, a wrong kubeconfig and "no resource type application" all yield the IDENTICAL empty string, after which `:778-783` names `GITEA_ARGOCD_URL` — a variable that branch never probes. **PROVEN, not inferred:** in `MATRIX-row5-photon.log`, line **2626** prints *"could not clone the repo. Its own words:"* and line **2627** is IMMEDIATELY the `Most likely GITEA_ARGOCD_URL…` guess. **There is no conditions dump between them** — the jsonpath read produced ZERO bytes and the script promised evidence it did not have. This is the B163 class at a SECOND site. 📐 **THREE MEASUREMENTS THAT NARROW IT (live lab, same kubeconfig/namespace the script resolves):** (i) the wait ran **188s for 180 iterations** of `read + sleep 1`, i.e. ~0.04s per read — and `ka` carries `--request-timeout=15s`, so timing-out reads would have stretched the loop toward 45 minutes. **The reads returned INSTANTLY**, consistent with empty/NotFound and INCONSISTENT with an unreachable API server. (ii) the same read TODAY returns **rc=0** with a populated revision, from `https://192.168.101.128:443`, with both Applications listable — so finding 3's PERMANENT variants (wrong ns / wrong kubeconfig / missing CRD / no RBAC) are **RULED OUT**. (iii) I ran the round's live experiment for finding 4 — annotate a HEALTHY app `refresh=hard`, poll the field 90 times over 45s: **0 blank observations**, health stayed `Healthy`. ⚠️ **THAT REFUTES ONLY THE UNCONDITIONAL FORM.** Finding 4's mechanism is *"a comparison that ERRORS can leave Revision empty"*, and a healthy app's comparison does not error — so this experiment CANNOT discriminate the conditional form, and I am not claiming it does. 🎯 **WHAT REMAINS OPEN:** a TRANSIENT read failure, or the conditional blank. **The run's own evidence for which was DESTROYED by `2>/dev/null || true`** — which is the whole point. ✅ **SO THE FIX IS THE DIAGNOSTIC, NOT THE WAIT, and it is correct regardless of which cause was live:** capture stderr and rc separately; on a non-zero read die with a DIFFERENT message naming the READ (namespace / kubeconfig / CRD / RBAC), never the repo URL; log the resolved `ARGOCD_NAMESPACE` + API server on the failure path; and read the revision BEFORE annotating, returning early when it is already non-empty (which also removes any chance of self-sabotage). ⚠️ `ARGOCD_NAMESPACE` resolves via `os.sh:613` as `${ARGOCD_NAMESPACE:-${VKS_NAMESPACE:-argocd}}`, and `os.sh:683-690` records a prior incident where two targets resolved it to DIFFERENT values in one run — so logging it is not decoration. **PRECEDENT AT THE SIBLING SITE:** `99-verify.sh:160-165` records, MEASURED 2026-08-08, the same swallowed-`| true` annotate printing *"the server doesn't have a resource type \"application\""*, with its own note that *"on KinD the two are the same file, which is why this survived: the bug is invisible in the only topology the local e2e exercises"* — and scenario-2 IS that topology. ⛔ **Design (a) — read `timeout.reconciliation` from `argocd-cm` — is DROPPED**: `:745` already handles a tenant whose kubectl cannot GET Applications, so reading a ConfigMap is strictly less likely; and on a Supervisor-hosted operator-managed ArgoCD the value may live in the CR, so a CM read can return the default while the real interval differs. ⛔ My "do not just raise 180s" stands, but for a DIFFERENT reason than I gave: not because 180s is one poll interval, but because the loop was never waiting on a poll at all. **A dead end, closed so nobody re-walks it:** `Makefile:172` sets `ARGOCD_NAMESPACE ?= argocd`, which WOULD clobber the resolution if make exported it — measured, it does not (no `.EXPORT_ALL_VARIABLES`, not in any `export`). **LOW:** the message says "in 180s" but the loop is 180 ITERATIONS of read+sleep; say "180 attempts" or track wall clock. ✅ **CLOSED 2026-08-17 — fix MERGED in #802, after THREE refutations, and the arc is the value.** v1 short-circuited on a pre-existing revision (exited 0, ZERO repo contact). v2 removed the short-circuit and KEPT THE OUTCOME — still testing PRESENCE, and `.status.sync.revision` PERSISTS because both apply paths are UPSERT. v3 required `reconciledAt` to ADVANCE — refuted from argo-cd v3.5.1 source: a FORCED refresh selects `CompareWithLatestForceResolve` (Level 3), Level 3 sets `noRevisionCache=true`, and `state.go:700-712` gates BOTH `ErrCompareStateRepo` short-circuits on `&& !noRevisionCache` — **the check's own forced refresh disabled the protection it relied on**, so `reconciledAt` advances on an unreachable repo. **What shipped requires BOTH halves:** it reconciled (reconciledAt advanced) AND the fetch succeeded (no `ComparisonError` per `state.go:699`, `sync.status != Unknown` per `state.go:1034`). ⚠️ **THE SUITE SCORED A CLEAN GREEN OVER EVERY BROKEN VERSION — 13/13, then 16/16, then 24/0** — its cases asserted WORDING and MECHANISM, never the VERDICT, and each round had to say so separately. The suite now leads with that history and its stubs model ARGO-CD rather than the implementation. RED-proven: dropping the fetch conjunct fails exactly the 4 decisive assertions (24/4); restored 28/0. |
| **B166** | 🟢 **CONFIRMED ON THE LIVE LAB 2026-08-18, hours after shipping — the remedy the fix points at genuinely works, and it works in exactly the state that motivated the row.** The fix made both stale-CA dies name `make ca-status`, on the reasoning that it needs no cluster and therefore works when everything else is dying. That reasoning is now measured rather than argued: on a lab whose workload cluster was **unreachable** (`kubectl get ns` → `x509: certificate signed by unknown authority`) and where `make creds-show` could only report *"cluster: not reachable"*, `make ca-status` still returned a **definitive verdict** — `Harbor CA (./secrets/harbor-ca.crt) matches harbor.env1.lab.test` / `Supervisor CA (./secrets/supervisor-ca.crt) does NOT match 192.168.101.128`, `1 of 2 CA certificate(s) above are wrong`, rc non-zero, naming `make fetch-supervisor-ca` as the remedy. ⇒ **The circular loop the row describes is genuinely broken**: an operator whose Supervisor is unreachable because of a stale anchor now has a command that answers, and the messages that strand them now name it. Note the shape of the failure it caught is the row's own: a rebuilt lab minted a new certificate at the SAME address, so the stored file *looks* perfectly valid. ⏸️ Original follows. — 🟢 **CLOSED 2026-08-18 — BOTH PARTS OF MY FIX WERE REFUTED, AND PART (i) WAS ALREADY MERGED. I re-litigated a solved problem for the SECOND time today** (B145 was the first, hours earlier). An idea round measured it: `24-lab-preflight.sh:192` **is** `vks_ca_default`, on the line **directly above** the `ca_status_report` at `:193` that my brief quoted — landed in `0258cd0` (PR #842), and `test-ca-staleness-check.sh:202-223` already pins it with a 12-line-window gate plus a behavioural RED-proof. My brief's line numbers were exact for the tree; I read the file and missed the line above the one I cited. Verified independently before acting: `grep -n 'vks_ca_default\|ca_status_report'` shows `:192`/`:193` and `29-ca-status.sh:52`/`:53`. ⇒ **Part (ii)'s blocker does not exist either.** Hermetic bare fixture, `CA_STATUS_STRICT=1` → **rc=0, CHECKED=0, MATCHED=0**: the strict arm is INSIDE the per-pair loop (`tls.sh:473`) and a bare box builds ZERO pairs (`:434-435`), returning at `:447`. The `CHECKED=0 → exit 1` rule lives ONLY in `29-ca-status.sh:54-57`, a wrapper `24-lab-preflight.sh` does not use. So *"WARN at that position, never a die"* is **already the behaviour** and (ii)'s new code was unnecessary. ⚠️ **And my `make ca-status` → rc=2 measurement was reading the wrong rc**: the SCRIPT returns **1**; **2** is GNU make's exit code for a failed recipe. That is this repo's own *"read the gate's own rc, not the pipeline's"* trap, committed in a row about rigour. **Three further reasons the hoist was wrong, all measured:** (a) it is **unreachable on the path that matters** — `Makefile:624` runs `argocd-preflight` BEFORE `lab-preflight`, that dies rc=1 on a bare box, and make aborts at the first failing prerequisite, so inside `make preflight` (the only place `CA_STATUS_STRICT=1` is set, and the first prereq of `install-all`) `lab-preflight` never runs at all; `docs/scenario-2.md` contains **zero** `make lab-preflight`, so the hoist's entire benefit was ONE case — a scenario-1 operator running it standalone at §7. (b) A **+30s latency regression** on the exact case the gate exists to fail fast on (two unreachable pairs at the default `CA_VERIFY_TIMEOUT=15`; the kubectl gate dies in <1s). (c) Splitting into two calls doubles the report and the network probes and trips the 12-line-window gate twice. 🟢 **SHIPPED INSTEAD — the round's own minimal fix, because the real defect is DISCOVERABILITY, not ordering.** Measured: `24-lab-preflight.sh` mentions `make ca-status` **3 times, all in comments, 0 in any die**; `23-argocd-preflight.sh` **0 times**; `docs/scenario-1.md` once, at `:609`, AFTER §7's lab-preflight. The `STALE_CA` die said only `make vks-login` — **which cannot clear it**, because scenario-1 §6 sets `VKS_AUTH_METHOD=kubeconfig` and that arm (`30-vks-login.sh:42-45`) checks only `[ -s "$KUBECONFIG" ]`; every CA check is inside the `vcf)` arm. **That is the loop.** Both dies now name `make ca-status` and say why (it needs no cluster, so it works when everything else is dying). Verified by RENDERING the message with a stub `die` — 8 lines, `${_lp_srv}` interpolated, `make ca-status` present. ⚠️ **I introduced a live defect doing it and the gate caught it:** I wrote `` `vcf` `` and `` `make vks-login` `` in **backticks inside a double-quoted die string** — command substitution, so firing the message would have tried to EXECUTE `vcf`. `make lint` flagged SC2006 (rc=2) on lines 88 and 90; removed, re-run rc=0, 0 findings. The documented backtick trap, walked into while shipping a fix. **Also corrected:** `docs/scenario-1.md:614-616` claimed lab-preflight catches a rebuilt lab *"in the first seconds"* — false, since the CA report sits 112 lines and one `die` past the kubectl gate; the paragraph now says so and points at `make ca-status`. **Residuals split out:** finding 5 → **B189**, finding 9 → **B190**. ⏸️ Original follows. — 🟡 **THE CIRCULAR GATE IS STILL LIVE (re-verified 2026-08-18) — AND THE ROW'S OWN "30-line MOVE" IS REFUTED AS STATED; a naive hoist would BLOCK `make preflight` on a bare box.** Re-verified in current code: `24-lab-preflight.sh` has `require_cmd kubectl` at **:56**, `kubectl version … || die` at **:81**, and `ca_status_report` at **:193** — 112 lines and one `die` downstream, exactly the circularity this row describes. So the defect is real and open. 🔬 **BOTH FEASIBILITY ARMS MEASURED, and they disagree.** *Can it RUN there?* **YES** — `ca_status_report` (`lib/tls.sh:389`) and `ca_verifies_endpoint` invoke **`openssl` ONLY** (4 and 3 occurrences; zero `kubectl`, and zero `curl` — this row says "openssl/curl", which overstates it). *Can its ASSERTIONS PASS there?* **NO** — measured with no kubeconfig and the shipped `.env.example`, `make ca-status` returns **rc=2** and prints *"checked nothing — see above. This is NOT a pass."* Since `Makefile:623` does `preflight: export CA_STATUS_STRICT = 1`, hoisting the call above the kubectl gate would make a **bare first-run box DIE in preflight** on a CA that does not exist yet — the documented "a PRE-install gate must not assert a POST-install fact" trap, and precisely the CHECKED=0 blindness a separate round already measured (it builds its Supervisor pair only when `VKS_CA_CERT_FILE` is set, and `.env.example` ships that commented). ⇒ **The fix is TWO parts, not one:** (i) call `vks_ca_default` BEFORE `ca_status_report` so the check is not blind, and (ii) hoist it above the kubectl gate with the CHECKED=0 case as a **WARN at that position**, never a die — the strict die stays where the inputs are guaranteed to exist. Shipping (ii) alone converts a circular gate into a hard block on every fresh install. ⏸️ **Not implemented here:** `24-lab-preflight.sh` was under adversary review when this was measured. Original follows. — 🟡 **RE-SCOPED — MY DESIGN WAS REFUTED THREE WAYS, and the headline is that I REDISCOVERED A SOLVED PROBLEM.** The staleness *observation* stands (measured: `secrets/supervisor-ca.crt` md5 `96611af5` dated Aug 16 against an Aug 17 lab; `curl --cacert` -> **rc=60**; a fresh fetch -> **rc=0**; control with no `--cacert` -> rc=60). Everything I proposed to do about it was wrong. **(1) CANDIDATE (c) IS ALREADY BUILT, WIRED AND RED-PROVEN** — `ca_verifies_endpoint` (`lib/tls.sh:104`, 6 typed verdicts), `ca_status_report` (`lib/tls.sh:389`), `scripts/29-ca-status.sh` + `make ca-status` (`Makefile:549-551`), consumed by `30-vks-login.sh:181-262` across all six rc arms, with its own RED-proof in `scripts/test-ca-staleness-check.sh`. And `29-ca-status.sh:9-16` states this row's defect **VERBATIM, dated 2026-08-16**: *"NOTHING re-fetches it when the lab is rebuilt, and a rebuilt lab mints a new CA at the SAME address — so the stale file still looks valid… AGE IS NOT THE SIGNAL."* Building (a) or (b) would ship a SECOND staleness mechanism beside it and make `29-ca-status.sh:57`'s `ALL-MATCH` token ambiguous. **(2) SO WHY DIDN'T IT SAVE ME? — THE REAL DEFECT, and it is a 30-line MOVE, not new code.** (c) is gated behind the very thing a stale CA prevents: `ca_status_report` sits at `24-lab-preflight.sh:166`, **104 lines and one `die` downstream** of `require_cmd kubectl` (`:35`) and `kubectl version || die` (`:61`). A fresh cut needs a kubeconfig to clear that gate, `vks-login` needs the CA, and the CA check lives past it — **circular**. The one pre-login entry point, `make ca-status`, is documented at `docs/scenario-1.md:609` **Step 8**, five steps AFTER Step 3's `fetch-supervisor-ca`/`vks-login` — and `ca_status_report` needs only `openssl`/`curl`, never kubectl (measured). **(3) CANDIDATE (b) IS NOT IMPLEMENTABLE — measured at two operating points, two different cuts:** subject DN is **byte-identical** across cuts (`CN=CA, DC=vsphere, DC=local, … O=vcsa.env1.lab.test`), and the host is *inside* that DN, so both are REFUTED as discriminators; fingerprint / serial / notBefore are properties **of the file**, so a stamp detects file MUTATION and never WORLD CHANGE — the stamp and the file move together, so it can never fire on the defect; cluster UID is circular. **The general result: staleness is a relation between the file and the WORLD, and any stamp is state about the file.** (b) therefore has a non-zero false-block rate (it fires on every legitimate re-fetch and on the operator who supplies their own out-of-band CA — which `30-vks-login.sh:161` calls *"strictly better than any digest"*) and a **structurally ZERO true-positive rate**. Its "or refetch" half is also self-refuting: that arm is `fetch-supervisor-ca.sh:33`'s `curl -sk`, the acquisition-over-unverified-channel this row explicitly excluded, now fired automatically. **(4) CANDIDATE (a) REVERSES A DECISION THIS REPO ALREADY TOOK, IN WRITING** — `98-uninstall-all.sh:281`: *"NOT removing secrets/ automatically — it may hold a credential this repo did not create"*, i.e. `patterns.md`'s provenance rule already applied, with the scoped command handed to the operator. It is also **in the wrong repo**: `grep -rn "walk-reset"` here returns **zero**; the lab cut lives in `nested-vsphere-lab`, so (a) means a foreign repo deleting files inside this working tree. (Measured: there are **zero** `rm -f secrets/` deleters in this repo today, so the widening risk to `harbor-robot.env` / `gitea-ci-token` / `webhook-token` is not present — but `./secrets/` is a single flat dir, so any glob-shaped deleter is one careless edit away.) ⚠️ **(5) CORRECTION TO MY OWN MECHANISM CLAIM — it was PROSPECTIVE, not current, and I stated it as current.** I wrote that a stale anchor lands on `vc_login`'s *"vCenter is unreachable — check the FQDN resolves"* arm. **`vc_login` never sees this CA**: all three of its vCenter curls (`lib/vcenter.sh:48/:73/:89`) are `-k`, and `grep -- "--cacert\|VKS_CA_CERT_FILE" scripts/lib/vcenter.sh` returns **nothing**. So my `curl --cacert` rc=60 measured a command **the repo does not run**. The misleading-diagnosis chain becomes real only once B133 gives `vc_login` a `--cacert` — so it is a **design constraint ON B133**, not a present defect, and reporting it as current would send a reader to `vcenter.sh:54` to fix something that cannot fire. **(6) THE WALK MATRIX IS STRUCTURALLY IMMUNE AND THEREFORE STRUCTURALLY BLIND** — `.gitignore:15` is `/secrets/` and every row is a fresh VM + fresh clone, so no row can hit staleness AND no row can ever RED-prove a fix for it. **Do not read a green matrix as evidence here in either direction.** **(7) THE 11 DEAD GUEST KUBECONFIGS ARE A DIFFERENT CLASS — do not fold them in.** They carry unique per-cluster names and ACCUMULATE (N dead files); the CA is a fixed path that a re-fetch OVERWRITES (1 stale file max), and only the CA is auto-selected by `vks_ca_default`. `98-uninstall-all.sh:178-179` already reasons about the kubeconfig scoping deliberately. Worth naming separately though: fixed-path `secrets/supervisor.kubeconfig` and `secrets/argocd.kubeconfig` ARE the CA's class, plus a zero-byte `secrets/testcluster.kubeconfig`. ✅ **WHAT TO ACTUALLY DO — three moves and one note, no new mechanism:** **(i)** hoist the `ca_status_report` block from `24-lab-preflight.sh:166` to BEFORE the `require_cmd kubectl` / `kubectl version` gate at `:35`/`:61`; **(ii)** give `31-fetch-argocd-kubeconfig.sh:94` the same pin+handshake gate `30-vks-login.sh:154/181` uses — better, fold it into the shared `vks_ca_default` (`lib/tls.sh:299`), which BOTH already call (`30-vks-login.sh:138`, `31-fetch-argocd-kubeconfig.sh:93`), so a third consumer inherits it; **(iii)** move `make ca-status` in `docs/scenario-1.md` from §8 to §3 beside `fetch-supervisor-ca`; **(iv)** note on B133 that its CA must be probed by `ca_verifies_endpoint` BEFORE the credential goes out, and that the `000` arm must not swallow rc 60/77. **RED-PROOF, available offline:** `cp secrets/vmca-root.pem secrets/supervisor-ca.crt` -> `make ca-status` must exit non-zero -> restore from a **fresh `cp` backup, NOT `git checkout --`** (the file is gitignored, so git cannot restore it). **RESIDUAL:** `lib/tls.sh:435` pairs the CA with `SUPERVISOR_HOST` only, never `VCENTER_HOST` — harmless today because a stale root fails both, but the denominator is one short the moment B133 makes them diverge. |
| **B167** | ✅ **CLOSED — the row’s own text says "is CLOSED and PROVEN closed by a real run"; what it lists as remaining belongs to B165/B148, not here.** **MED — SCOPE NARROWED 2026-08-18 by live observation: on the DEFAULT scenario-1 path this file is not in play at all.** Measured in the running certification (row-1 log :680, :1490, :1496): `27-use-guest-kubeconfig.sh` publishes `ARGOCD_KUBECONFIG=…/secrets/supervisor.kubeconfig` and `make fetch-argocd-kubeconfig` then writes THAT path — `SUPERVISOR kubeconfig -> …/secrets/supervisor.kubeconfig (override with ARGOCD_KUBECONFIG in .env)`. So the separate `secrets/argocd.kubeconfig` with its own embedded CA only exists when an operator OVERRIDES the default, i.e. the cross-cluster-registration shape. The row's staleness argument is unchanged FOR THAT CASE; it simply does not apply to the path every row walks, which is covered by the `supervisor-ca` story instead. NOT closed — the override case is real and unmeasured. Original text follows. — **MED — `secrets/argocd.kubeconfig` carries its OWN embedded CA, and it goes stale INDEPENDENTLY of `secrets/supervisor-ca.crt`. This is a TWO-COMMAND DISCRIMINATOR for the F9 `STALE_CA` failures on matrix rows 5+6.** Found 2026-08-17 as a free aside by the B54 idea round, which measured it while establishing a precondition. **MEASURED:** `secrets/argocd.kubeconfig` (Aug 11) is stale in BOTH its CA and its token. Used with its OWN embedded CA the Supervisor answers **`x509: certificate signed by unknown authority`**; substituting the FRESH `secrets/supervisor-ca.crt` (Aug 17) the TLS **succeeds** and the same request becomes **`Unauthorized`**. **So the error text alone discriminates the two faults:** an `x509` error means the ANCHOR is stale; an `Unauthorized` means the anchor is fine and the CREDENTIAL is stale. The Supervisor at `192.168.101.128:443` was alive throughout — only the file was dead. **WHY IT MATTERS BEYOND F9:** it is a THIRD instance of B166's class (state outliving the lab it describes) at a THIRD fixed-path file, and unlike the 11 per-cluster guest kubeconfigs (which accumulate under unique names — see B166 finding 7) this one is a **fixed path that a re-fetch overwrites**, i.e. exactly the shape `ca_status_report` was built for. ⚠️ **AND `ca_status_report` DOES NOT COVER IT:** `lib/tls.sh:434-435` derives its anchor/endpoint pairs and pairs `VKS_CA_CERT_FILE` with `SUPERVISOR_HOST`; the CA **embedded inside** a kubeconfig is not a file in that list, so no existing check probes it. **DO NOT FIX BEFORE AN IDEA ROUND** — the obvious "re-fetch the kubeconfig when its CA fails" re-opens the acquisition-over-unverified-channel question B166 finding 5 already refuted for the CA itself, and `31-fetch-argocd-kubeconfig.sh` is the same script B166 finding 3 flags as the ungated second consumer, so the two rows must be fixed TOGETHER or the fix will be written twice. | ⚠️ **CORRECTION 2026-08-17, same day — THE LINK TO ROWS 5+6 IS WRONG AND I AM RETRACTING IT.** This row was filed claiming to be *"a TWO-COMMAND DISCRIMINATOR for the F9 `STALE_CA` failures on matrix rows 5+6"*. **It cannot be**, and B166's own idea round had already said why: **every walk row is a FRESH VM + a FRESH `git clone`, and `/secrets/` is gitignored — so no row can carry a file that survived a re-cut.** I repeated an error a round I had already read had corrected. **What rows 5+6 actually hit** (verbatim, `MATRIX-row5-photon.log:893`): *"argocd API: COULD NOT DETERMINE (STALE_CA) — argocd-server never received the request, so this is NOT a permissions answer … If the reason is a trust anchor: `make fetch-argocd-ca`, then set `ARGOCD_CA_FILE`."* **AND THE EXACT MECHANISM IS ALREADY RECORDED, MEASURED, IN `nested-vsphere-lab/scripts/walk-matrix.sh`'s own comments — it is neither a stale file NOR vaguely 'a wrong anchor':** scenario-2 says `argocd login <ARGOCD_SERVER> --sso` establishes **both** the token **and** the CLI's TLS trust for the self-signed argocd-server, because *"there is no `ARGOCD_CA_FILE` wired into the CLI path"* — the repo env var is **not read** by the CLI. **The harness pastes a token and never logs in**, so trust is never established, and the probe reports the *"opaque TLS failure"* the document itself predicts. Its consequence, in the harness's words: ***"The tenant's ONLY write mechanism therefore went unproven in every scenario-2 row."*** The FIX is known and MEASURED against argocd `v3.0.19` — the CLI's own localconfig supports `certificate-authority-data` (confirmed in argoproj/argo-cd release-3.5 `util/localconfig/localconfig.go`): no CA -> `x509: certificate signed by unknown authority` (control) · `insecure: true` -> trust passed but UNVERIFIED · `certificate-authority-data: <b64 PEM>` -> trust passed **and verified**. ✅ **CORRECTION (mine, third pass on this thread): IT IS IMPLEMENTED, AND IT WORKED.** I wrote *"NOT IMPLEMENTED … rows 5+6 will fail identically"* — **false twice over**, and the git timeline settles it: the CA-pinning arm landed **2026-08-17 00:28** (nvl #64) and the IP-SAN guard — which pins the chain ONLY when the cert actually covers the address dialled, and otherwise takes the disclosed `insecure: true` arm — landed **15:26** (nvl #77). The **20:33Z** matrix run executed on `7f8b19b`, i.e. the IP-SAN-guard commit, so it **had both**. **And the measurement agrees:** the 11:10Z run (pinning arm, NO IP-SAN guard) shows `STALE_CA` **×3**; the 20:33Z run shows `STALE_CA` **0**. So this failure class is CLOSED and PROVEN closed by a real run. nvl #75 was indeed docs-only, but it was not the fix — #77 was, and I mistook the absence of one for the absence of the other. What remains on row 5 is the DIFFERENT failure (an empty revision — B165, fixed in this repo's #798), not this one. Different mechanism, different repo, and it belongs with the F9/B148 work rather than in this row. ✅ **WHAT SURVIVES, and it is still worth having:** the measured DISCRIMINATOR itself — used with its OWN embedded CA the Supervisor answers **`x509: certificate signed by unknown authority`**; substituting a FRESH `secrets/supervisor-ca.crt` the TLS succeeds and the same request becomes **`Unauthorized`**. So the error TEXT separates an anchor fault from a credential fault in one command, which is useful for F9 **and** for an operator box. The **scope** is corrected: this row is about an OPERATOR'S box across cuts, where `secrets/` and `.env` persist — NOT about walk rows, which cannot reach the state. **AND THE SAME SCOPE CORRECTION APPLIES TO B166**: its own finding 8 already says the matrix is *"structurally IMMUNE, therefore structurally BLIND"* — do not read a green matrix as evidence for either row, in either direction. **ROOT-CAUSE NOTE, also corrected:** I claimed the lifecycle has no bridge that clears stale state. **False.** `make env-init` (`02-env.sh init`) creates a FRESH `.env` from `.env.example` and backs the old one up, and **BOTH scenarios call it at Step 0** via `common-bootstrap.md:35`; `.env.example:1138` ships `VKS_CLUSTER_NAME` **commented**, so a fresh copy carries no cluster name at all. The bridge exists and the runbook uses it. A stale operator `.env` means Step 0 was not re-run — operator state, not a design gap. |
| **B168** | ✅ **CLOSED 2026-08-17 — see the end of this row for the full arc.** **HIGH x2 — `ARGOCD_LB_IP`'s mere PRESENCE outranks an operator's explicit `ARGOCD_SERVER`, and the SAME sentinel picks which ArgoCD's CA gets written to disk. The second one is a CANDIDATE ROOT CAUSE FOR THE F9 `STALE_CA` FAILURES ON ROWS 5+6.** Found 2026-08-17 by the B154 idea round, which was asked to refute B154 and instead found that B154's finding (3) understates a defect that is LIVE and needs no hypothetical fix. **HIGH-1 (MEASURED, hermetic render):** `ARGOCD_SERVER=argocd-server make creds-show` printed `https://192.168.101.131 (self-signed; --insecure)` — the bare IP plus the literal `--insecure` that #745 tells the operator never to use. Mechanism: `creds.sh:69` tests `[ -n "${ARGOCD_LB_IP:-}" ]` FIRST and only `elif`s to `ARGOCD_SERVER`. **The inversion is the point:** `ARGOCD_SERVER` IS in `load_env`'s snapshot-protected list (`lib/os.sh:499`) and in `check-env-clobber.sh:91` SELECTORS; **`ARGOCD_LB_IP` is in NEITHER** — so an *unprotected, file-sourced* value silently beats a *protected, command-line* one, inverting the exact precedence machinery this repo built to stop selector clobbering. It also dead-codes `creds.sh:71`'s real-lab branch whenever the sentinel is present. **HIGH-2 — UPGRADED TO MEASURED 2026-08-17 for the half that matters (the SELECTION); only the REACHABILITY half remains inferred:** the same sentinel selects the **CA-FETCH ENDPOINT** at the `fetch-argocd-ca` recipe (`Makefile`, `ep="$(if $(ARGOCD_LB_IP),…)"`) — and that writes a **TRUST ANCHOR**, not a display string. On a box with BOTH a live KinD and a live lab, `make fetch-argocd-ca` fetches the **KinD** ArgoCD's CA into `ARGOCD_CA_FILE` while the operator meant the lab's. `fetch-ca.sh` dies loudly only if the KinD IP is UNREACHABLE; **if it answers, the WRONG anchor is written silently** — which is precisely the `STALE_CA` shape rows 5+6 exhibited. ✅ **PROBED, A/B/control, via the round's own settling command `make -n fetch-argocd-ca`:** with the sentinel PRESENT alongside an explicit `ARGOCD_SERVER=argocd-server.real.lab` the recipe resolves to **`10.99.99.99`** (the sentinel WINS); with the sentinel ABSENT and the same `ARGOCD_SERVER` it resolves to **`argocd-server.real.lab`** (correct); with neither, `ep=""`. **So the SELECTION defect is proven, and selection IS the defect.** ⚠️ **STILL INFERRED, and named rather than hidden:** that a live KinD at that address would ANSWER and thereby cause the wrong anchor to be written *silently* — this box has **no kind clusters** (`kind get clusters` -> none), so the dual-live case could not be reproduced. Settling it needs a box with BOTH stacks up. **Worth pursuing as a cause before assuming staleness-by-age.** ✅ **THE FIX IS ALREADY IN THE FILE, TRUSTED TWICE, AND NOT USED AT THE DEFECT:** `creds.sh:196` and `:251` both do `grep -q '^VKS_STATE_KIND=1' "$_sink"` with the comment *"Whose state is it? The KinD flow STAMPS the sink; a real lab's does not."* So the purpose-built discriminator is in hand and the defect keys on an IP's presence instead. ⚠️ **The `[ -n "$ARGOCD_LB_IP" ]` conjunct is LOAD-BEARING in the fix**: `05-kind-up.sh:156` stamps `--kind` BEFORE `07-install-argocd.sh:152` publishes the IP, so a KIND-only test would print `https://` with an EMPTY host in that window. **FALSE-POSITIVE RATE, per state:** KinD **0** · real lab **0** (nothing on the lab path calls `state_stamp`) · **stale sink UNCHANGED — the fix does NOT close it** (a lingering KinD sink still stamps `KIND=1`); that residual is B166's class and needs `creds.sh:41-51`'s liveness-probe pattern, so do NOT claim the fix covers it. **AND THE IRONY IS INSTRUCTIVE:** `creds.sh:41-51`, EIGHTEEN LINES ABOVE the defect, carries a header reading *"⚠️ PRESENCE IS NOT LIVENESS, and this variable is the worst place for that confusion because it is published to the state overlay by an install and SURVIVES A REBUILD"* — and adds a 2s TCP probe, for a DIFFERENT variable. The identical hazard sits untreated 18 lines later. **TEST COVERAGE IS WORSE THAN B154 CLAIMED (measured):** `test-creds-show.sh` has **6** render fixtures; `ARGOCD_LB_IP` appears in **4** (not "every"), and **`ARGOCD_SERVER` appears ZERO times** — so `creds.sh:71-79`, the whole real-lab ArgoCD branch, has **no coverage at all**, and the fixture captioned *"THE REAL-LAB STATE"* sets the KinD sentinel. Any fix ships with a fixture that sets `ARGOCD_SERVER`, omits `ARGOCD_LB_IP`, and asserts the URL contains **no** `--insecure` and no bare IPv4. **MEASURED ON THIS BOX 2026-08-17:** `grep -c '^ARGOCD_LB_IP=' .env.state` -> **0**, so the precedence bug is NOT firing here right now — but the KinD flow writes that key, so any box that has run `make kind-up` carries it. **ALSO NOTED:** `creds.sh:150`'s `mktemp` is not trap-guarded (LOW — mode is 0600, so untidy rather than a disclosure), and `creds.sh:151` is the concrete file:line for **B153**'s printer-side half — it prints the ArgoCD admin password read FRESH from the Secret, which is exactly the printer B153 says shape-keyed redaction cannot see. ✅ **CLOSED 2026-08-17 — MERGED in #801, verified on main by content:** `creds.sh:83` tests `ARGOCD_SERVER` first with `ARGOCD_LB_IP` in the `elif`, the `fetch-argocd-ca` recipe is inverted the same way, harness green. The KIND-stamp design I first proposed was REFUTED by a measured 5-state table (repairs 1 of 3, REGRESSES one), and `ARGOCD_LB_IP` was deliberately NOT dropped — its writer is Supervisor-only, so removing it breaks `make fetch-argocd-ca` on KinD. Four cases added where `ARGOCD_SERVER` had ZERO coverage across all six render fixtures, including a CONTROL proving KinD still works. **RESIDUAL, filed not hidden:** a THIRD pre-existing site — `creds.sh:251` trusts the same KinD stamp and thereby LAUNDERS PROVENANCE (measured: a mismatched overlay upgraded to `DISCOVERED`) — plus a NEW defect at `creds.sh:90-94`, which renders `https://Please enter Username:` when kubectl prompts on a credential-less kubeconfig (the same kubectl-prompt-to-stdout class as #800). Neither is fixed; both need their own round. |
| **B169** | ✅ **REFUTED 2026-08-17 — see the end of this row for the full arc.** **UNVERIFIED QUESTION, not a confirmed defect: in `INGRESS_CONTROLLER=istio` (INSTALL) mode with gateway-api routing, does `46-install-istio.sh` publish the CLASSIC gateway's address while the routes live on the AUTO-PROVISIONED one?** Found 2026-08-17 closing B38's residual. **Measured:** `46-install-istio.sh:216` sets `LB_IP="$(istio_wait_lb_ip)"` and `:220` publishes it; `istio_wait_lb_ip` (`lib/istio.sh:499,509`) reads `ISTIO_GATEWAY_NAMESPACE/ISTIO_GATEWAY_SERVICE`, which `46:37,53` set to `istio-ingress`/`istio-ingressgateway` — the **classic helm-installed** Service. Meanwhile the gateway-api branch of `istio_apply_routes` creates its Gateway in `ISTIO_GWAPI_NAMESPACE` (default `vks-ingress`), discovers ITS address from the Gateway status (`lib/istio.sh:393`), and Istio auto-provisions a SEPARATE Service (`:402,404`). **On the live lab these are different addresses and only one serves:** `istio-ingress/istio-ingressgateway` = `192.168.101.134` -> **curl 56, connection reset**; `vks-ingress/vks-uis-istio` = `192.168.101.135` -> **HTTP 200** on all four hosts. ⚠️ **WHY A QUESTION AND NOT A FINDING:** the only row that exercised gateway-api routing (row 2) took the **ATTACH** path (`istio-existing` -> `47-attach-istio.sh:118`), which publishes the auto-provisioned address correctly and went GREEN. **No row exercised install-mode + gateway-api**, so the combination is untested in either direction. **SETTLING IT COSTS ONE RUN:** `make install-ingress INGRESS_CONTROLLER=istio` on a cluster carrying the Gateway API CRDs, then compare the published `INGRESS_LB_IP` against `kubectl -n vks-ingress get svc`. ⛔ Do NOT fix before settling: `istio_wait_lb_ip` is ALSO the classic path's correct source, so a blind change breaks the mode that works. ✅ **REFUTED 2026-08-17 — ANSWERED OFFLINE BY CODE READING, and the answer is the OPPOSITE of what this row feared: install mode never uses gateway-api routing AT ALL, so there is no auto-provisioned gateway for the published address to disagree with.** The decisive line is which routes function each mode calls. `47-attach-istio.sh` BRANCHES — `:83 istio_apply_routes_gwapi` (gateway-api) or `:103 istio_apply_routes` (classic), with the matching `:88 istio_wait_gwapi_address` / `:108 istio_wait_lb_ip`. `46-install-istio.sh:213` calls **`istio_apply_routes`** — the CLASSIC one — **unconditionally**, and `istio_apply_routes` (`lib/istio.sh:592`) contains **no `ISTIO_ROUTE_API` reference whatsoever**; `grep ISTIO_ROUTE_API scripts/46-install-istio.sh scripts/44-install-ingress.sh` returns **nothing**. So install mode applies classic VirtualServices and publishes the classic gateway's address — **internally consistent**, both classic. The row's premise (routes on the auto-provisioned gateway, address from the classic one) cannot arise because `istio_apply_routes_gwapi` is never reached on that path. **The live measurement in this row stands and does not contradict it:** `istio-ingress/istio-ingressgateway`=`192.168.101.134` → curl 56 vs `vks-ingress/vks-uis-istio`=`192.168.101.135` → 200 was observed in **ATTACH** mode (row 2 took `istio-existing`), which is exactly the mode that branches correctly. **Residual, checked and NOT a defect:** `ISTIO_ROUTE_API` is silently ignored in install mode — but `.env.example:735` documents it under the heading *"--- Istio: SOMEONE ELSE installed it (INGRESS_CONTROLLER=istio-existing) ---"*, so its scope is stated where an operator sets it. Nothing to fix; the combination this row wanted tested (install + gateway-api) **does not exist as a code path**, which is why no row exercised it. |
| **B164** | ✅ **CLOSED BY VERIFICATION 2026-08-18 — SHIPPED, and the shipped fix REFUTES this row's own prescription.** The gate no longer false-positives: verified both directions today — `--selftest` **16 passed, 0 failed**; a genuinely restated credential still MATCHES (1); the jq form MATCHES 0. ⚠️ **The row prescribed "add one narrow line to the exclusion chain". The code REFUTES that, with a measured exploit recorded at `check-prose-secrets.sh:28-32`:** the chain is LINE-level and BACKLOG.md lines run to 12,845 characters, so excluding a line containing a jq expression would suppress the WHOLE ROW, credential included — proven both ways, and that case is now pinned in the selftest. What shipped instead is narrower and better: the four value-bearing arms of `$PATTERN` were changed to require a SINGLE separator, so a doubled slash (jq's alternative operator) is no longer read as one. **Narrow the MATCH, never exclude the LINE.** The measured benign residual is recorded in the code rather than hidden (the credential-named arms lose the case where the separator ENDS the line — by construction such a line carries no value after it). Original text follows. — **`check-prose-secrets` FALSE-POSITIVES on a jq expression — jq's ALTERNATIVE OPERATOR (two slashes) after a field named for a credential matches the gate's pattern. Third construct in a known class; it BLOCKED a merge on 2026-08-17.** ⚠️ **THIS ROW IS DELIBERATELY WRITTEN WITHOUT QUOTING ANY OF THE TRIGGERING STRINGS, and that is the finding's own strongest evidence:** the first draft quoted them, and `check-prose-secrets` flagged the row **six times**, so the write-up of the false positive could not be committed. That is the documenting-a-trap-makes-it-unwritable class. Read the live pattern at `scripts/check-prose-secrets.sh:23` rather than reproducing it here. **THE TRIGGER:** a backlog row quoting the repo's OWN code from `lib/argocd.sh:343` — a jq filter selecting a `.token` field with an empty-string fallback — reddens the `secrets` CI job, and on a docs-only PR that job IS the whole gate. **The two already-known members of this class:** the shell working-directory variable followed by a colon (as in a `docker run -v` mount), and any camelCase key ending in the word Password followed by a colon (as in a documented default). This adds **jq's alternative operator after a credential-named field**. **It is a REAL false positive, not a near-miss:** the matched text is a JSON path plus an operator, and the gate's own failure message says a value-less hit should get a NARROW allowlist. **WORKED AROUND** by rewording (docs-only; `scripts/` was frozen for the certification run) — so **the gate still fires on the next person who quotes jq**, and quoting jq is normal in a repo whose scripts use it. **FIX:** add one narrow line to the existing exclusion chain at `check-prose-secrets.sh:32-49`, matching a jq alternative applied to a credential-named field, with a comment naming exactly why — and **RED-prove it still catches a genuinely restated credential** (plant one in a scratch `.md`, confirm rc=1, remove). ⛔ **Do NOT widen the pattern instead.** The gate exists because gitleaks misses natural-language credentials, and this repo has already shipped three live plaintext passwords in a runbook that gitleaks reported clean. **The allowlist must stay narrower than the thing it protects.** |
| **B149** | ✅ **CLOSED 2026-08-17 — the fix is ON `main`, and I VERIFIED IT BY RUNNING IT, not by reading its comment.** 🟡 **`make deps`'s failure message PROMISES something `check-tools` does not do, and the promise is false on every WARM box.** FOUND 2026-08-17 by reading scenario-1 §1 as an end user and RUNNING it (not by a grep). `make deps` failed on my box (`apt-get` needs a tty for sudo; `deps-prereqs` Error 1), and printed verbatim: *the sentinel .deps-failed is set, so 'make check-tools' will tell you the toolchain is INCOMPLETE BECAUSE THIS FAILED - instead of sending you back here.* I then ran `make check-tools` exactly as §1 instructs. It printed **`all REQUIRED tools present.`** and said nothing about the failure. **MECHANISM:** `03-check-tools.sh:176` reads the sentinel, but that branch sits inside the **`else` of a missing-tools test** — so it is reachable ONLY when something is absent. On a box where every tool already happens to be present (a re-run, a warm dev box, a second walk on the same VM) `deps` can fail and `check-tools` reports a clean toolchain in total silence. **WHY IT MATTERS even though 'all present' is literally TRUE:** the operator was just told, by name, what the next command would say. It says the opposite, so they reasonably conclude `deps` succeeded. Same class as B119's misattributed §8 message and B120's false cause-list — an error message asserting something the code does not do. **NOT the same as 'the toolchain is broken'**: in this state it genuinely is not, which is exactly why the silence is the defect rather than the verdict. **MINIMAL FIX (needs an idea round — it touches a control's messaging, and inventing/altering a control is the act that most needs one):** either (a) surface the sentinel in the ALL-PRESENT path too, as a WARNING — *a previous `make deps` failed; the toolchain nevertheless looks complete, so the failure was in a part you do not need* — which keeps the promise honest without inventing a false red; or (b) stop `deps` promising what another target will print, and have it say what IT knows. Prefer (a): it is the only one that tells the operator the two facts they actually hold. ⚠️ Do NOT make the sentinel FAIL `check-tools` — the tools are present, and a red there would be a false negative on a working box, plus it would break every warm-box walk. **Housekeeping observed in passing:** the sentinel is gitignored (`.gitignore:74`) and `make deps` removes it on a clean run, so it is operator-local state, not repo state. 🔬 **MECHANISM RE-VERIFIED on the current tree 2026-08-17, with exact anchors so the fix lands in the right branch.** The row was written from a live run; this is the structural confirmation an implementer needs. `scripts/03-check-tools.sh`: the sentinel is read at **:176** (`if [ -f "${REPO_ROOT}/.deps-failed" ]`), and it sits inside the branch reached when tools ARE missing — that branch's inner `if/else` closes at **:186**, and **:187 `else`** opens the ALL-PRESENT path, which runs **:188-:199** and reads the sentinel **nowhere**. The two all-present messages are **:189** (`PRE-CARRY OK …`, when `$pending_carried` is non-empty) and **:195** (`all REQUIRED tools present.`) — and note there are **TWO** of them, so fix (a) must surface the warning in BOTH or it will be silent on the pre-carry box, which is precisely the air-gap operator who can least afford a false all-clear. `exit "$rc"` at **:201** is unaffected either way, which is what keeps fix (a) from inventing a false red (the ⚠️ in this row). So the row's diagnosis is accurate as written; only its single-message assumption needed widening to two. Still needs its idea round before implementing — this note changes nothing about that, it just means the round can start from verified anchors instead of re-deriving them. **Live A/B on this box, both directions:** with `.deps-failed` present and EVERY tool present — the exact warm-box state the row says was silent — `make check-tools` now emits **7 WARN lines** naming the sentinel, what `deps` installs that this gate never checks (`java maven kustomize go node`) and CONFIGURES (container-engine runtime, registry search paths), the `rm -f .deps-failed` remedy, and the air-gap caveat that the sentinel may have TRAVELLED with the repo copy. **Control:** with no sentinel, **0** such lines. And in BOTH runs the literal `all REQUIRED tools present.` is still printed exactly once — which is load-bearing and nearly got broken: `docs/scenario-1.md` carries that string as an **Expect:** literal, substring-matched, so rewording the message instead of ADDING lines would have turned every walk row UNMET. The mechanism the row diagnosed was right (`03-check-tools.sh:176`'s sentinel branch sits inside the `else` of a missing-tools test, unreachable when nothing is missing); the fix surfaces it a second time in the all-present branch rather than moving it. Also worth keeping from the fix's own comment: it deliberately does NOT say *"the failure was in a part you do not need"*, because under `set -euo pipefail` a failure in `deps`'s FIRST `pkg_install` aborts before crun and podman's unqualified-search-registries — so a reassured operator walks into a `podman build` that cannot resolve a short name. |
| **B150** | ✅ **CLOSED — ALL THREE VERIFIED FIXED 2026-08-18, each against the current tree.** **D1** scenario-1.md:813 now reads *"Attaching installs nothing — one read-only `helm status`"*, verbatim the prescribed wording. **D2** scenario-2.md:583 now reads *"the first one's ERROR MESSAGE, so what you paste is well-formed base64 that DECODES TO …"*, the prescribed rewording. **D3** BOTH documents now name BOTH destination variables (scenario-1: `ARGOCD_DEST_SERVER` + `ARGOCD_DEST_CLUSTER_NAME`; scenario-2: both), so neither reader is left unaware of the alternative. D2 and D3 landed as part of the run-2 remediation; this re-check confirms they ACTUALLY landed rather than assuming the edits took. 🟠 **THREE defects found by READING scenario-1 and scenario-2 end to end as an END USER and verifying every checkable fact — 2026-08-17. 42 claims verified, 3 wrong.** METHOD, stated because it is the point: both documents read whole, commands run where possible, and every `**Expect:**` literal and every code citation checked against the tree. **29 of 29** scenario-1 Expect strings present in code; **7 of 7** code citations accurate (incl. `04-vsphere-namespace.sh:52` and the Steps-4/5 `${VCF_CLI_SRC_DIR:-$HOME/Downloads/vcf}` fallback at `04:22`+`08:17`); **6 of 6** structural claims accurate (six Step-0 downloads all present; robot file 0600 via `umask 077` plus the rm-first mode trap; `403 IS A PASS` at `lib/harbor.sh:173`; registry/Service/namespace/port-80). scenario-2's two MEASURABLE claims reproduced EXACTLY: a `<placeholder>` in `.env` yields `syntax error near unexpected token` with the first key set, the last UNSET and the file untouched; and the three base64 forms behave as documented (`--from-file` ONE LAYER SHORT, single-encoded `--from-literal` CORRECT, double ONE TOO MANY). **DEFECT 1 (LOW, scenario-1 §12:786) — 'Attaching runs no helm at all' is FALSE as written.** `47-attach-istio.sh:99` runs `helm status istiod` — read-only, inside an `if ... >/dev/null`, so a missing helm just takes the else branch: NO functional impact. The sibling claim 'the attach path installs nothing' IS true (1 read-only call vs 7 in `46-install-istio.sh`). Fix: 'installs nothing (one read-only `helm status`)'. ⚠️ My first count said TWO — line 100 matched because its PROSE contains 'helm release'. **DEFECT 2 (LOW, scenario-2:519) — 'you would paste an EMPTY value into your Cluster spec' UNDERSTATES its own trap.** MEASURED: the one-pipeline double-base64 on a missing file exits **0** and emits the base64 OF THE ERROR MESSAGE, so the reader pastes encoded error text that LOOKS LIKE DATA. The two-step form the doc prescribes correctly exits 1. Fix: 'an encoded ERROR MESSAGE that looks like data'. **DEFECT 3 (MED) — EACH document offers exactly ONE of the two interchangeable destination variables, a DIFFERENT one, so neither reader learns the other exists; and :792's file citation is wrong.** MEASURED mention counts: scenario-1 `ARGOCD_DEST_SERVER` **0** / `ARGOCD_DEST_CLUSTER_NAME` **1** (:716); scenario-2 **4** / **0**. ⚠️ **I FIRST WROTE THIS UP AS 'the tenant doc names the option a tenant CANNOT use' AND THAT IS WRONG — the owner's question forced the correction.** A tenant reads their guest cluster's API URL straight out of their OWN kubeconfig, so `ARGOCD_DEST_SERVER` is perfectly usable for them; what they cannot do is ENUMERATE registered names. Symmetrically the scenario-1 admin CAN enumerate — `argocd-preflight` prints them — so NAME is the natural offer there. **Each doc's PRIMARY choice is defensible; neither is a bug.** The real defects are narrower: (a) neither doc says the alternative exists though `70:490/492` accepts either, so a reader who cannot supply their doc's variable gets no hint — most likely a tenant whose guest API URL is an address the Supervisor-hosted ArgoCD cannot reach and whose admin knows the registered NAME; and (b) :792 cites `k8s/argocd/application.yaml` for `ARGOCD_DEST_SERVER`, which is NOT in that file — it renders `${ARGOCD_DEST_KEY}: ${ARGOCD_DEST_VALUE}` (`:37`). Fix: add ONE clause to each doc naming the other variable ('the install accepts either') and correct the citation. Both are real settables (`.env.example:527,567`) and `70:164` prints the name form as its remedy. ⚠️ **MY INSTRUMENT PRODUCED TWO FALSE FINDINGS out of 45 checks, both mine not the docs':** a `cut -c1-170` viewer truncated a line mid-code-span and made a healthy file look corrupted (B61), and a case-SENSITIVE `grep -qF` reported scenario-2:408's 'not valid for this address' missing when `lib/tls.sh` prints it UPPERCASE. Re-running all 30 literals case-insensitively: **0 missing**. **A verification helper is an instrument; grade its output before the product's.** ⚠️ **THIS ROW WAS WRITTEN ONCE AND LOST, and the mechanism is worth more than the row:** the `make docs-lint && git commit` chain was BLOCKED by the no-gate-in-commit-chain hook, so the commit never ran; I did not notice, and a later `git reset --hard origin/main` wiped the uncommitted edit. Row and commit message both gone; rebuilt from the transcript. **A blocked chain means NOTHING in it ran — verify the commit exists before switching branches.** ⚠️ **DEFECT 1 FIXED, DEFECT 2 REFUTED BY MEASUREMENT — 2026-08-17.** **(1) FIXED:** `docs/scenario-1.md:800` said *"Attaching runs no helm at all"* while `47-attach-istio.sh:99` runs `helm status istiod`; it now reads *"Attaching installs nothing — one read-only `helm status`"*. The `**Expect:**` literal at `:815` is deliberately untouched (the row confirms it is TRUE, and the walk substring-matches it). **(2) REFUTED — and this row's proposed wording would have made a CORRECT document WRONG.** The row says the one-pipeline form *"emits the base64 OF THE ERROR MESSAGE, so the reader pastes encoded error text that LOOKS LIKE DATA"*, and prescribes changing *"an EMPTY value"* to *"an encoded ERROR MESSAGE"*. **MEASURED, exactly as the doc writes the command** (`base64 -w 0 $CA \| base64 -w 0`, no redirection): **rc=0, stdout EMPTY** — `base64` writes its diagnostic to **stderr**, so the second stage receives nothing and encodes nothing. The doc's *"you would paste an EMPTY value"* is **accurate as written**. The row's observation only reproduces with **`2>&1`** merged (measured: yields `YmFzZTY0OiAv…` decoding to `base64: …: No such file or directory`), which the doc's command does not do. The two-step form the doc prescribes correctly exits **1** — also confirmed. **Defect 3 is unresolved here** and is narrower than its heading: the row's own text says *"Each doc's PRIMARY choice is defensible; neither is a bug"*, leaving (a) neither doc mentioning the other variable and (b) a wrong file citation at `:792`. |
| **B151** | ✅ **CLOSED 2026-08-18 — this row’s own tail says "Nothing further is open here"; the marker had not caught up.** **RE-SPECIFIED 2026-08-18 by its idea round — MY PROPOSED FIX WAS REFUTED AS A FAKE-GREEN, all four candidates ALREADY EXIST, and the real defect is none of them.** ⚠️ **(1) CRITICAL — the 'local schema dir' candidate creates exactly the fake-green this row exists to prevent.** MEASURED A/B, kubeconform 0.8.0, same Deployment: a URL location that is unreachable gives `errors:1, skipped:0`, **exit 1 → loud RED**; a local dir with the file absent gives `errors:0, skipped:1`, **exit 0 → `validate: OK`**. Both `kc` call sites pass `-ignore-missing-schemas` (`validate.sh:206,307`), so a PARTIAL local dir is indistinguishable from "no schema exists for this kind", and `validate.sh:176-177` states outright that REQUIRE_SCHEMAS does NOT cover the skipped path while `:135-142` folds `skipped` into `total`, so the zero-examined guard cannot fire either. The design would have turned the #745 red into a green. A local dir may only ever be an ADDITIONAL location with the URL retained. **(2) The four candidates are already built** — `validate.sh:18` (`-cache`), `:51-59` (three `--schema-location` sets), `ci.yml:227-243` (`actions/cache`, key `kubeconform-v2-${{ hashFiles('k8s/**','deploy/**') }}` + restore-keys), `ci.yml:301` (`KUBECONFORM_REQUIRE_SCHEMAS: '1'`). Candidate (c)'s own constant-key hazard was already found and fixed (v1→v2). This is a BUG-FIX to an existing mechanism, not a new control. **(3) THE ACTUAL DEFECT, which none of the four address:** `actions/cache@55cc834` (v6) declares **`post-if: "success()"`** in its own action.yml at the pinned SHA — so when `make static-check` FAILS, which is precisely what a throttle causes under REQUIRE_SCHEMAS=1, the freshly-fetched schemas are **DISCARDED**. The next run starts from the same stale entry, so **a throttle failure is SELF-SUSTAINING**, and any unrelated red (lint, app-test) also throws the fetch away. **FIX:** split into `actions/cache/restore` + `actions/cache/save` with `if: always()` — safe here specifically because the cache is content-addressed, the same argument `ci.yml` already makes for the Go build cache. **(4) My ordering was wrong:** attempt 1's 429 was at *Set up job* fetching the `jdx/mise-action` tarball (`ci.yml:212`), which PRECEDES every step including the cache restore — untouchable by any schema work. The two attempts failed for two different reasons; scope this row to attempt 2 and do candidate (c) first. **(5)** The cache key is fine and a filed KNOWN GAP against it (`ci.yml:234-240`) is a PESSIMIZATION: measured, the inner cache is version-discriminating (same dir, `-kubernetes-version 1.31.0` then `1.30.0` → entries 1→2), so adding `KUBERNETES_VERSION` to the actions/cache key buys ZERO correctness and costs a full re-fetch on every bump. **(6)** Vendoring is cheap in bytes (31 files / 4.6 MB, measured) but buys nothing for the air gap — `03-check-tools.sh:79` classes kubeconform **ci-only**, so `make validate` never runs there; and it would create a second source of truth for `KUBERNETES_VERSION` (a hardcoded literal at `validate.sh:14`, absent from `.env.example`) that nothing polices. **(7)** A local dir cannot be a drop-in anyway: CRD schemas come from a different upstream with INTENTIONAL 403s — `validate.sh:44-49` records that jsDelivr answers a nonexistent yannh CRD path with **403, not 404**, which kubeconform treats as a hard error rather than a skippable miss; a local mirror collapses that distinction into "file absent" = skipped, i.e. finding (1) again. Original text follows. — `make validate`'s kubeconform step DEPENDS ON A NETWORK FETCH PER RUN, so a GitHub throttling episode reddens a PR whose diff cannot possibly have caused it — MEASURED 2026-08-17 on PR #745.** Two different downloaders throttled in one episode: attempt 1 died at *Set up job* with `Response status code does not indicate success: 429 (Too Many Requests)` fetching the `jdx/mise-action` tarball from codeload.github.com; the re-run reached **3m15s** and died with `kubeconform: 1 of 8` then `16 of 16 resource(s) could not have their schema downloaded`. **PROVEN NOT THE DIFF:** #745 touches `.env.example`, `docs/scenario-2.md` and two `scripts/` files — `git diff --name-only` gives **0** files under `k8s/` or `deploy/`, i.e. ZERO of kubeconform's inputs — and `make validate` is **rc=0 locally** on the same branch. **THE GATE IS CORRECT AND MUST NOT BE LOOSENED:** `KUBECONFORM_REQUIRE_SCHEMAS=1` refusing to pass over resources whose schema it could not download is precisely the anti-fake-green this repo exists for — its own message says *passing here would report OK over work the gate did not do*. Do NOT 'fix' this by dropping that variable or adding `--ignore-missing-schemas` in CI. **THE DEFECT IS THE PER-RUN FETCH, not the strictness.** Candidates, in order, each needing an idea round because a schema cache is a new control: (a) **cache the schema set** — kubeconform accepts a local schema location, so CI restores a keyed cache and fetches only on a miss, REMOVING the dependency rather than tolerating it; (b) **retry with backoff**, which only narrows the window; (c) pin + cache the action tarball (it is already SHA-pinned — the 429 was codeload throttling, not resolution). ⚠️ Prefer (a): (b) still fails when the throttle outlasts the backoff, and **a gate that goes red for reasons no diff can fix trains people to re-run until green, which is how a REAL red gets waved through.** **SAME FAMILY AS B104, OTHER SIDE:** B104 is operator-path fetches with no retry (measured today: **7 of 10** in `00-install-prereqs.sh` + `10-mirror-pull.sh` have none — `00:422` and `10:63,64,65,68,71,138`); this is the CI-path twin. They want DIFFERENT remedies: the operator needs a retry plus an actionable message, CI needs a cache. **HANDLING UNTIL THEN, on the record:** read the log before re-running (a 429 at *Set up job* and a schema failure at 3m are different events); prove the diff does not touch the gate's inputs; `gh run rerun --failed` ONCE (it replays the SAME merge SHA, so it is valid only for a genuinely transient cause); and **never merge over the red** — `ci-pass` failed correctly here. 🔴 **ESCALATED 2026-08-17 — this is now BLOCKING a real fix (PR #745, the F9 correction) from landing, and the cause is MEASURED: the schema CDN is down, not throttled.** `validate.sh:38-39` fetches from **`cdn.jsdelivr.net`** (`/gh/yannh/kubernetes-json-schema@master/...` and `/gh/datreeio/CRDs-catalog@main/...`). Measured during GitHub's *Partial System Outage*: every CRD schema via jsdelivr returned **403 or 000** (re-probed twice, still 000 then 403), the core k8s schema via jsdelivr **000**, while jsdelivr's own root answers **301** (so the CDN is up and its GitHub origin fetch is failing). The **same files on `raw.githubusercontent.com` returned 200** — 6 of 8 CRD schemas and both core schemas — but one returned **429**, which is exactly the rate limit jsdelivr exists to avoid. So *switching host* is NOT a clean fix: raw is rate-limited, jsdelivr is outage-prone, and `KUBECONFORM_REQUIRE_SCHEMAS=1` correctly refuses to pass over unvalidated resources (it exists because a lenient default once produced a green gate over ZERO validated resources — do NOT weaken it). ⇒ the fix is a **keyed cache of the schema set**, so the gate stops depending on a per-run network fetch at all; `validate.sh:40-41` already honours `KUBECONFORM_SCHEMA_K8S`/`KUBECONFORM_SCHEMA_CRD`, so a cached local dir needs no code change, only wiring. Affected dirs, measured from the run: `k8s/istio/` (8 of 8 resources) and `k8s/vks/` (1 of 1); `k8s/tekton/` (16) and `k8s/traefik/` (9) validated fine, which is why the failure looks selective. Needs its own idea round (cache key, staleness, and what a cache MISS must do — a miss must not silently become a skip). 🔴 **ROUND LANDED 2026-08-17 — verdict REFUTED. The design re-implements a mechanism that is ALREADY DEPLOYED, and the failure MECHANISM in this row is wrong in a way that misdirects the fix.** (a) **The cache already exists**: `validate.sh:18,85,89` pass `-cache "$KC_CACHE"`, and `ci.yml:179-185` persists it with `actions/cache` keyed on `hashFiles('k8s/**','deploy/**','tekton/**')` + `restore-keys`. Design items 1–4 rebuild a shipped mechanism **that did not prevent this outage**. (b) **The mechanism is a LOCATION-SEARCH ABORT, not a cache miss.** The round reproduced the CI failure locally — `k8s/istio` 8 of 8, `k8s/vks` 1 of 1, matching the CI log exactly — **with a warm 24-entry cache that CONTAINED the istio Gateway (10,630 B) and VirtualService (60,959 B) schemas**. The cache was warm and useless, because kubeconform's `-schema-location` list behaves differently per status: a **404 FALLS THROUGH** to the next location, a **403 ABORTS the whole search** (later locations never consulted), and so does a refusal/timeout. jsdelivr currently returns **403 for paths that exist**, and `KC_LOCS_K8S` puts **yannh FIRST** — an istio CRD has no yannh path, so a healthy day is *404 → fall through → datreeio 200* and the outage is *403 → abort*. ⇒ **No cache or vendored set placed AFTER a network location can fix this; ORDERING is the fix** (local FIRST, asserted by a test so a future reordering cannot silently re-open it). (c) **This row's own reading of its CI log is wrong**: it says *"k8s/tekton (16) and k8s/traefik (9) validated fine, which is why the failure looks selective"* — MEASURED **tekton = 16 of 16 could not download (RED)**; only traefik validated. The selectivity explanation was false. (d) **`raw.githubusercontent` as a fallback is REFUTED and the row's evidence is stale**: measured **000 on every probe** against a 200 control (`example.com` and `github.com` both 200 at the same moment), and per (b) a later location is unreachable behind the 403 abort anyway. It also doubles latency — each location retries 3× with backoff; the warm-cache run took **105 s just to fail**. (e) **Vendoring, if pursued, must NOT ship while `-ignore-missing-schemas` is unconditional** — that combination measured **5 of 8 dirs printing "all schemas resolvable" over 14 SKIPPED resources**. Sizes measured: warm cache **3.12 MiB / 24 entries**; tracked repo content **6.19 MiB**, so a full vendored set is a **~50% increase** (Deployment alone **673,804 B** × 3 variants), while the **CRD-only subset is 892,396 B (0.85 MiB)** — Application 415 KB, HTTPRoute 215 KB, Cluster 107 KB, gwapi Gateway 82 KB, istio VirtualService 61 KB, istio Gateway 10 KB. **Every failing dir failed on a CRD kind and every passing dir passed on cached CORE kinds**, so CRD-only targets 100% of observed failures at ~1 MiB, and its decisive edge over caching is that **it works on a cold clone**. (f) **The key is DERIVABLE** — one awk pass over `k8s/ deploy/` yields **44 apiVersion/kind pairs, 23 distinct GVKs** (22 validated; `Kustomization` is consumed by kustomize), so no hand-maintained list is needed — but today's cache key covers **neither** `KUBERNETES_VERSION` nor the location templates. (g) **`file://` is the one part of the design that survives intact**: both a bare template and a `file://` prefix returned `Valid: 1` on a good manifest **and `Invalid: 1` / rc=1 on a bad one**, so the positive control passes and no new code is needed. **THREE SEPARATE DEFECTS the round found while here, all live:** (1) the **skipped-counts-as-validated** fake-green — **FIXED, PR #755**, RED/GREEN-proven (old code 8 dirs printed the false claim, new code 0); (2) `check-gwapi-istio-alignment.sh:31` uses `curl -sSL … || true` **with no `--fail`**, so on an HTTP error it gets **rc=0 plus a 305,204-byte HTML body**, the `[ -z "$gomod" ]` guard never fires, the `GWAPI_REQUIRE_FETCH` honesty branch is **dead code**, and it exits 1 blaming *"istio release-X go.mod has no sigs.k8s.io/gateway-api line"* — fails CLOSED but names the WRONG cause; fix is `curl -fsSL`; (3) `KUBERNETES_VERSION` is a **hardcoded literal `1.31.0`** at `validate.sh:14`, absent from `.env.example`, and `validate.sh` **never calls `load_env`** — invisible to Renovate, `check-env-coverage` and `check-toolchain-alignment`, and load-bearing the moment schemas are vendored. Plus a LOW: the cache key hashes `'tekton/**'`, which matches **nothing** (`git ls-files tekton` = 0; the manifests are at `k8s/tekton/`) — the same dead-path rot already corrected once in the adversary hook's `GUARDED_PREFIXES`. **DENOMINATOR for the "what else has this shape" question:** 46 of 316 tracked files contain a URL, but exactly **2** gates make a live per-run fetch (`validate.sh` and `check-gwapi-istio-alignment.sh`) — too few to justify a shared mechanism, and they need DIFFERENT fixes. They also run in different CI jobs (`validate` inside `static-check`, gated `code == 'true'`; `check-gwapi` inside `static-check-fast`, asserted by `ci-pass`). **RESIDUALS the round names rather than hides:** jsdelivr's HEALTHY-day status for a nonexistent yannh CRD path (404 vs 403) is UNVERIFIED — if it is 403 on a healthy day too, then yannh-first has been aborting on EVERY istio/vks CRD always and those dirs have only ever passed via `-cache`, which enlarges the blast radius; and whether datreeio carries the 6 Tekton GVKs is UNVERIFIED (decides 16-of-22 vs 22-of-22 vendorable). Both settle with one `curl -w '%{http_code}'` post-outage. ✅ **CLOSED 2026-08-18 by verification.** Its central claim was CONFIRMED at the pinned SHA, its fix SHIPPED, and the implementation improved on the row's own prescription — the row proposed a local schema dir, and its idea round measured that exact candidate to be the fake-green the row exists to prevent (a URL location that is unreachable gives `errors:1, exit 1` = loud RED; a local dir with the file absent gives `errors:0, skipped:1, exit 0` = `validate: OK`). What shipped keeps the loud-RED behaviour. Nothing further is open here. |
| **B152** | ✅ **CLOSED 2026-08-17 — the prescribed work SHIPPED in two PRs, and the residual is now BY DESIGN rather than unnoticed.** The row's measurement stands and is worth keeping: 6 lines neutralized per scenario-1 row, identical across rows 1-4, and 0 real-login lines in all 6 — so `0 FAILED` was being reported over instructions no row had ever executed. `make argocd-auth-check` is the answer: the doc keeps its honest interactive instruction and the PROOF lives in a target a reader can run. **The matrix NEUTRALIZES 6 lines per scenario-1 row — and those 6 are EVERY "see it with your own eyes" instruction the document gives — so for two weeks every row has reported `0 FAILED` over a set of instructions NO ROW HAS EVER EXECUTED, on any OS, in any cell.** MEASURED 2026-08-17 over run 5b's six logs: rows 1,2,3,4 each report `6 line(s) neutralized` and the six are IDENTICAL in all four — `argocd login "$ARGOCD_SERVER" --username admin --insecure`, `argocd account update-password`, and four `kubectl port-forward` lines (gitea-http 3000, javawebapp 18080, gowebapp 18081, tekton-dashboard 9097). Real-login lines (`'admin:login' logged in successfully`, `context … updated`) appear **0 times in all 6 rows**. Mechanism: `scripts/walk-doc.sh:152-166` `neutralize()` matches `*"argocd login"*`, `*"argocd account update-password"*` and `*"port-forward"*` and comments the line out, because each needs a TTY or blocks forever. The mechanism is legitimate; the CONSEQUENCE is a coverage hole exactly where an end user's eyes go. **RANK THE SIX — they are NOT equally serious, and overstating this is as wrong as missing it:** (a) `argocd login` proves a **CREDENTIAL** and **nothing else in the matrix proves it** — this is the whole reason F9 (row 5's `ARGOCD_MECHANISM=api` transport failure) could sit undetected behind four green scenario-1 rows; (b) `argocd account update-password` is a **MUTATION the doc instructs the operator to perform**, also with zero other coverage — if that line is wrong the operator locks themselves out of a fresh ArgoCD, and we would never know; (c) the four port-forwards are a **REDUNDANT access path**: `make verify-ingress` independently proves all four `*.vks.local` hosts return HTTP 200 through the ingress LB *and serve their own body marker* (rows 3+4, e.g. row 4 at 14:12:55), so the BACKENDS are proven and only the port-forward instruction itself is unexercised. So (a) and (b) are the real defect; (c) is a documentation-fidelity gap. **WHY IT SURVIVED TWO WEEKS, which is the more transferable finding:** the count is REPORTED IN EVERY VERDICT LINE (`… 6 line(s) neutralized`) and is **never gated** — it was quoted verbatim in status tables all day and never interrogated. Three compounding causes: a neutralized line **cannot fail**, so it never enters the FAILED count and `0 FAILED` is compatible with never having run the instruction; there is **no coverage denominator** (the verdict counts blocks ran/skipped, and the earlier Expect-coverage work measured *Expect paragraphs* — a different denominator, so the gap fell between two metrics and no number moved); and it **looks like a deliberate documented decision**, because the code carries a careful correct-sounding comment explaining the TTY. A correct rationale is the best camouflage there is. **The generalisable fix is therefore a DENOMINATOR, not just an arm:** the verdict should state neutralized lines as *lost coverage* against the doc's total command count, and a neutralized line whose claim nothing else proves should be distinguishable from one that is merely a redundant path. **FIX (idea round dispatched 2026-08-17; do NOT implement before it returns):** rewrite `argocd login` into its non-interactive form (`--password-stdin`, if the pinned CLI has it — UNVERIFIED, measure both upstream `argocd` and the lab's `argocd-vcf` v3.0.19) and assert a token was issued; degrade to a SKIP when no password is obtainable (a tenant may legitimately not be able to read `argocd-initial-admin-secret`). Open questions the round must settle: whether the assertion belongs in `walk-doc.sh` at all or in a `make argocd-auth-check` the doc tells the reader to run; whether exercising **admin** login is even right for a TENANT row or a false positive about a capability the tenant lacks; whether the rewrite mangles a `\`-continuation (the existing `UNSAFE_FILE` mechanism at :157-158 exists for exactly that and a naive arm drops it); and whether the printed block would leak the password value rather than the variable name (B153 records that 2 of 6 logs already captured a live Harbor secret). ⚠️ Do NOT close this by deleting the `Log in:` line from the docs — an operator does need it. ⚠️ **ROUND RETURNED 2026-08-17: my fix was REFUTED, and part of it SHIPPED anyway as the correction the round named 'the cheapest real fix in this whole review' (PR #752).** **(1) `--password-stdin` DOES NOT EXIST** — MEASURED on `argocd v3.0.19+d67e6eb90-vcf`: `argocd login --password-stdin` errors `unknown flag`, and it is absent from upstream's docs too. The only non-interactive form is `--password <string>` = **argv** = forbidden by `security.md`. Worse, the rewrite would have made the block exit non-zero and taken run 5b's `0 FAILED` to `1 FAILED` **in four rows**, resetting the all-green clock. Root cause of my error: `--password-stdin` is the **docker** idiom this portfolio prescribes for `docker login`, transplanted to a tool that never had it — the *a fix that works in a sibling is a HYPOTHESIS* failure. **(2) The mechanism ALREADY EXISTS HERE:** `scripts/91-e2e-tenant-mechanism.sh:241-244` POSTs the session API with the body on stdin (`curl --data @-`) and then drives the CLI via `ARGOCD_AUTH_TOKEN` from the ENVIRONMENT — and its own comment says *"There is no --password-stdin"* verbatim. I re-invented a mechanism this repo's source documents as impossible. **(3) My assertion could NEVER match:** `get-user-info` prints `Logged In: %v` (capitalised, two words), not `logged-in: true`; and `|| echo` returns 0 — so it would have printed `ARGOCD LOGIN DID NOT AUTHENTICATE` on **every run including every success** while counting `RAN, 0 FAILED`. Strictly worse than no assertion. **(4) The right layer is a TARGET, not the walk:** `scenario-1:384` *deliberately* documents an interactive step (*"prompts … paste what `make argocd-password` printed"*), so rewriting it would test a command no reader types. Prescribed shape: a `make argocd-auth-check` lifting the working session-API POST + **one** doc line with an `Expect:`; the walk then runs the doc's own line natively, needing no neutralize arm and no two-repo change. **(5) My RANKING was wrong on (b):** `account update-password` is NOT unwalkable-by-nature (`--current-password`/`--new-password` exist) — it must never be walked because it **MUTATES the only ArgoCD admin credential on a shared lab**, poisoning every later step in the row and every later row on the same cut. **(6) And (c) is not as redundant as I said:** `verify-ingress` covers the *ingress* path only; the port-forward **fallback** path has zero coverage, and covering it needs a background-and-kill harness (its own idea round). **SHIPPED (PR #752):** the per-arm reason strings (the old single `needs a TTY / blocks forever` was FALSE for two of three arms) + a `covered-by:` field per neutralized line. **STILL OPEN:** the `make argocd-auth-check` target + its one doc line. ⚠️ If any password is ever added to `.walk-env`, note the round MEASURED that `walk-matrix.sh:502` uses the **bare** `printf 'K=%s\n'` form and this repo's own `test-walkbox-secret-quoting.sh` proves it **5/6 BROKEN** (backtick → command substitution, spaces/quotes → parse failure, `P@ssw0rd$1` → empty); use the `printf "%s='%s'\n"` + `esc_sq` form at `walkbox.sh:301`. ✅ **THE PRESCRIBED WORK IS SHIPPED 2026-08-17 — in two PRs, and the residual is now BY DESIGN rather than unnoticed.** #752 corrected `neutralize()`'s reason strings (the single *needs a TTY / blocks forever* was FALSE for two of the three arms) and added a `covered-by:` field per neutralized line, so *unwalkable-and-covered* no longer looks like *unwalkable-and-blind*. #753 productized the missing proof as **`make argocd-auth-check`** — argv-safe (password to `jq` via the ENVIRONMENT, body to `curl` on STDIN), 14 offline tests against a local stub, RED-proven (sabotaging `jq`→`printf` turns the four escaping cases and both source-guards red; restoring gives 14/14), the reusable half LIFTED into `lib/argocd.sh` with `91-e2e-tenant-mechanism.sh` now CALLING it so the e2e and the target cannot drift, plus the one scenario-1 line telling the reader to run it. **Proven on the real lab, both TLS arms:** bare IP + correct CA warns about the address shape up front then issues NO token (it predicts and reproduces F9); the SAN name + the same anchor returns HTTP 200 with no `-k`. ⚠️ **The walk still neutralizes `argocd login`, and that is CORRECT, not a remaining gap:** `scenario-1:384` deliberately documents an interactive step, and rewriting it would test a command no reader ever types — the round refuted my rewrite on exactly that ground. So the matrix still does not authenticate to ArgoCD, deliberately, with the proof living in a target the document points at. Still open as its own question, unchanged in kind: the four port-forwards' FALLBACK path has no coverage, and covering it needs a background-and-kill harness. |
| **B153** | 🟢 **CLOSED 2026-08-18 (PR #868, 745259b/e6ea5c1) — BUT NOT BY THIS ROW'S PREFERRED REMEDY, WHICH WAS MEASURED AND REFUTED.** Its idea round inventoried the leak across ALL SIX of run 6's row logs: **16** credential occurrences (Gitea 6, Harbor 6 incl. two 32-char robot secrets, ArgoCD 4). The arithmetic decides it: the `creds-show` guard alone closes **14/16**; the value-keyed sink redactor this row prefers closes **12/16**; **BOTH TOGETHER still close 14/16** — the redactor adds **ZERO**. The two that survive both are the BARE `make argocd-password` prints at `docs/scenario-1.md:376`, whose value is read from the k8s Secret `argocd-initial-admin-secret` and never lands in `.env` or `.walk-env`, so NO downstream redactor can key on it. Only the PRINTER can mask it. **So the sink redactor was NOT built**, and the closure is `argocd-password.sh` gaining the same pre-`load_env` `SHOW_SECRETS` snapshot plus a single `_emit` for all three exit-0 paths, with `--raw` on its EXISTING argv parser for `creds.sh` (which already passed `--wait 0` there) → **16/16**. ⚠️ **The round also measured what the redactor would have COST had it been built:** `openssl` and `robot$vks-cicd` each destroy an Expect literal → an UNMET → a RED row → the six-green-from-the-first-go clock resets at ~3.5 h per re-cut; it needed a ≥12-char floor and a values-not-usernames rule to be safe at all. ⚠️ **And my first RED-proof of the fix was VACUOUS**: asserting the value appears under `SHOW_SECRETS=1` passes on the BROKEN build too, because the child inherits the flag and reveals as well — the discriminating state is a TTY with `SHOW_SECRETS` unset (`script -qec`), where `creds.sh` reveals and its `$( )`-captured child masks. **RESIDUAL, not hidden:** the round separately found `OUT_FILE`/`STEP_OUT_FILE`/`EXPECT_LOG` are `mktemp`'d at `walk-doc.sh:94` but absent from the EXIT trap at `:96` (six of eight are there), leaving a second copy of every step's output — credentials included — on every walk box. One line; blocked only because the live matrix is executing that file. The original report follows. 🟡 **CONFIRMED LIVE 2026-08-18, mid-certification — this is not hypothetical. A walk log can capture a LIVE credential in cleartext, because `creds-show` legitimately prints one and the walk records everything the doc tells the reader to run.** MEASURED 2026-08-17 across run 5b: **2 of 6** row logs (rows 1 and 3) contain a line pairing a Harbor robot login with its high-entropy secret (`robot$<name>  <32 chars>`); all six rows contain **2–3** `creds-show`-shaped blocks. Mitigations already in place, which is why this is 🟡 and not 🔴: the per-run evidence dir is **0700 andriy** (the fix from the run-id work), the lab is throwaway and cut A's credentials are already dead, and nothing is committed — `/tmp/walk/**` is outside the repo and cannot reach a PR. But the portfolio convention is that a secret never reaches a log at all, and these logs OUTLIVE the lab that issued the credential: they are the artifact I quote in handoffs and paste into rows, so the next accidental `cat` of a row log in a session transcript publishes a credential that was live at capture time. **Fix candidates, in preference order:** (1) have the walk REDACT on capture — a `sed -E 's/(robot\$[a-z0-9-]+)[[:space:]]+[A-Za-z0-9]{16,}/\1 <REDACTED>/'` style filter on the log writer, which is cheap and needs no doc change; (2) have `creds-show` itself gain a `CREDS_REDACT=1` mode the walk sets, keeping the human path unchanged — better, because the redaction then lives with the thing that knows which fields are secret; (3) do nothing and rely on 0700 + throwaway labs, which is the status quo and is defensible but leaves the outlives-the-lab property. ⚠️ Whichever is chosen, the redaction must be RED-proven against a real captured line and must NOT redact the login NAME (`robot$vks-cicd` is load-bearing evidence that the tenant path used a robot rather than admin — row 3's log is the only proof of that). Do not solve it by dropping `creds-show` from the walk: it is one of the few blocks that proves the endpoints and logins exist. 🔴 **ROUND LANDED 2026-08-17 — verdict REFUTED: the premise is WORSE than measured and the RANKING is wrong in BOTH directions.** (a) **The row's own `robot$`-anchored regex is refuted as a mechanism.** It catches the Harbor robot secret and MISSES the Gitea admin password, the ArgoCD admin password (whose `-` breaks `[A-Za-z0-9]{16,}`) and the bare `make argocd-password` token — **2 of 4 leaking rows in run 5b, and 0 of the 13 exposed historical logs**. Its coverage is load-dependent on which doc step happened to run, so it is not a mechanism; **key on the VALUE, not the SHAPE**. (b) **The row's containment premise ("0700 … nothing is committed") described only the NEWEST run dir and was falsely reassuring** — this was the round's CRITICAL and the largest blast radius. VERIFIED INDEPENDENTLY: `/tmp/walk` was mode **0775** (world-listable AND traversable) with **123 of 144** `.log` files world-readable, **13** of them carrying a live-at-capture Gitea/ArgoCD admin password, spanning **2026-08-10 → 08-17**. Root-caused precisely: `walk-matrix.sh:156` `mkdir -p "$OUT_DIR"` creates the PARENT too, and the `chmod 0700` beside it only ever reached the per-run dir — so the axis is **DEPTH** (99 logs written directly under `/tmp/walk` inherited 0664; 40 inside a `run-<id>` dir were 0600), **not row index** as the round guessed. **FIXED in nvl #76** (parent chmod, RED-proven both ways: with=700/700, without=**775**/700) and the corpus tightened by hand (**123 world-readable → 0**, 226 files preserved, **nothing deleted**). ⚠️ **The comment above that chmod asserted "the files inside are 0600, so the NAMES leaked rather than the CONTENTS" — measurably FALSE, and false in the REASSURING direction, which is why the parent went eight days unfixed.** (c) **The SINK is `walk-doc.sh:587`, NOT `walk-matrix.sh:552`** — VERIFIED: `:552` is `</dev/null > "$log" 2>&1`, a **REDIRECT** whose rc is read on the next line, and the comment at `:553-556` records the MEASURED 2026-08-13 fake-green a pipe would reintroduce (*"rows 1 and 3 exited rc=1, and the matrix still printed MATRIX_RC=0"*). `:587`'s `*)` arm feeds stdout + `OUT_FILE` + `STEP_OUT_FILE` from inside a loop body, so filtering there is rc-safe. (d) **Candidate 2 covers 1 of 3 printers** the DOCS tell the reader to run — `creds-show`, `argocd-password` (a **BARE token on its own line**, unbuildable by shape: nothing distinguishes it from a digest or an image tag), and `harbor-admin-password`. If it ships, ONE `redact_secret()` in `lib/os.sh` routed by all three, not a per-printer flag. (e) **Candidate 2's env toggle walks into this repo's own documented clobber class** — `creds.sh:22` calls `load_env`, and its `:140` comment already names it; an UNCOMMENTED `CREDS_REDACT=0` in `.env.example` would silently defeat the walk's `CREDS_REDACT=1`, invisibly (absence of redaction produces no output). (f) Redaction does NOT break Expect checking — **0 of 63** literals mutated — but that is luck, not design: `robot account 'robot$vks-cicd' created.` survives only because the next char is `'`. Keep the 63-literal extractor as the redactor's regression test. **RESIDUAL that changes the design:** value-keyed replacement CANNOT catch the ArgoCD **initial** admin password (4/6 rows) because it is read fresh from a k8s Secret each call and never lands in `.at`/`.hp`/`.env` — so the answer is **BOTH** candidates (sink-side value replacement + printer-side), not one. **Evidence for the row from the round itself: it pasted two REAL credential values into a shell command while RED-proving, putting them in a session transcript — the exact quoting path the row describes, committed by the reviewer, within ten minutes of reading the log.** Honest negatives recorded so nobody re-derives them: `.walk-env` echoes **0**, JWTs **0**, the command echo at `:558` prints the UNSUBSTITUTED text (safe by design), VERDICT file clean. | **LIVE OBSERVATION 2026-08-18 01:07 EDT:** while the six-row certifying matrix was walking row 1, its log was tailed and caught mid-block at the step whose own comment says it prints the initial admin password — i.e. the capture this row describes was happening AS the row was being read. **Exposure measured and partly closed the same minute:** `/tmp/walk` and every run dir are **700** (so no other user can traverse in — that half was fixed by nested-vsphere-lab #76), but the log FILES are **664**, protected only by the parent. 25 such files across 11 runs. The 21 files in COMPLETED runs were tightened to 600. **The 4 files in the LIVE run were deliberately LEFT ALONE** — marginal defence-in-depth is not worth any risk of disturbing a certifying run, and the 700 parent already blocks traversal. Tighten them post-matrix. Same shape as the `secrets/vmca` child-dir finding: correct mode on the parent, permissive mode on the contents.
| **B154** | ✅ **REFUTED 2026-08-17 — MY PREMISE WAS MEASURABLY FALSE, and the candidate fix would have caused a NEW operator-facing regression. Do NOT implement it; the row is kept so nobody re-derives it.** **(1) Neither writer clobbers an operator-set NAME.** `is_placeholder` (`lib/os.sh:741`) matches ONLY empty and `*<SET-*`, so `argocd-server` is not a placeholder and BOTH writers take their preserve branch — MEASURED by executing the real predicates: with `ARGOCD_SERVER=argocd-server`, `09-argocd-address.sh:96` warns *NOT overwriting* and `02-env.sh:155` says *already set*; only an empty or `<SET-…>` value is written over, which is the intended design. PR #745 ships the key COMMENTED, so a tenant who sets a name is safe from both. ⚠️ B148's sibling note (*a name-based answer REGRESSES on the next run*) is false for the same reason and should be corrected rather than acted on. **(2) THE REAL WRITER IS IN ANOTHER REPO, and it is the only one that fired in row 5:** `nested-vsphere-lab/scripts/walk-matrix.sh:700,712,530` reads argocd-server's LoadBalancer **IP** and writes `ARGOCD_SERVER=<IP>` into the walk env, then the SAME function pins the server's real chain as `certificate-authority-data` (`:733-741`) *preferring* it over `insecure: true` — i.e. it constructs exactly the IP + correct-CA combination that can NEVER verify, which is row 5's `STALE_CA` precisely. And neither vks-airgap-cicd writer can even fire in scenario-2: `argocd-address` appears **0 times** in `scenario-2.md`, and `env-populate` queries `ARGOCD_NAMESPACE` on the **GUEST** kubeconfig while ArgoCD is a Supervisor Service (`scenario-2.md:663` says so verbatim), so it prints *not discovered*. ⇒ **PR #745 landing will NOT green row 5** — filed as its own row in the nested-vsphere-lab backlog (independent `B<N>` numbering per CLAUDE.md). **(3) `ARGOCD_LB_IP` IS A KinD SENTINEL, not an IP-shaped synonym — my symmetry argument was wrong.** `creds.sh:69` and `Makefile:561` use its PRESENCE to mean *this is KinD*, and it **outranks** `ARGOCD_SERVER` in both. Publishing it on a real lab would make `make creds-show` print the bare IP plus the literal `--insecure` — the exact value and posture #745 tells the operator never to use — in the credentials summary, and would dead-code `creds.sh:71`'s *A REAL LAB* branch. `test-creds-show.sh` pins `ARGOCD_LB_IP` in EVERY fixture, so the tests would not have caught the flip. If a lab-side IP ever must be published it needs a DISTINCT key, plus entries in `load_env`'s snapshot list and `check-env-clobber.sh`'s SELECTORS, where `ARGOCD_LB_IP` is absent today. **(4) Leaving `ARGOCD_SERVER` unset trades a loud die for a SILENT NO-OP** — MEASURED over all six states of `70-configure-argocd.sh:280-296`: with it unset, `argocd_api_ready=no` so `can_api` stays at its INITIALISER `no` rather than `unknown`, and the die at `:289` fires only on `unknown` — so `auto` selects `request` and exits **0 having applied nothing**, the outcome the code's own comment calls *the worst possible default*. Explicit `api` (what scenario-2 sets) does die loudly, so scenario-2 is protected and the DEFAULT is not. **(5) `09-argocd-address.sh` writing the IP is CORRECT for scenario-1** — `ARGOCD_AUTH_TOKEN` appears 0 times there, so the mechanism resolves to `kubectl`; `scenario-1.md:399` documents the value as *display and probes only* and `:377` runs `argocd login "$ARGOCD_SERVER" --insecure`. The fix would have broken a correct scenario. **What the round DID keep:** the instinct that an unreconciled writer exists was sound — I just found the wrong one. And it caught a live bug in PR #745 itself (its new CA recipe used the env-prefix form that the same PR measures as silently ignored on a retry), fixed in e3ae192. ⚠️ **CANDIDATE (1) RED-PROVEN, AND ITS COVERAGE MEASURED 2026-08-18 — it catches 1 of 2 real credential lines, which is a MEASURED argument for candidate (2).** Against a real captured line in run 5b's row-1 log, the proposed filter yields `… robot$vks-cicd <REDACTED>`: the login NAME is preserved (the row's explicit constraint — it is the only proof the tenant path used a robot rather than admin) and **0** residual 16+ char tokens. **But** the same log holds a SECOND credential line — `ArgoCD  https://…  admin  <TOKEN>  <- INITIAL secret` — which a `robot$`-shaped pattern cannot see. Discriminating labels from values: **12** argocd/gitea credential-LABEL lines, of which only **1** carries an actual token, so row 1's true exposure is **2 lines** and the sed covers **1**. A regex filter must ENUMERATE credential shapes and that list rots — it already misses one of the two shapes in the first log checked. **So prefer candidate (2)** (`creds-show` gains `CREDS_REDACT=1` that the walk sets): the redaction then lives with the thing that KNOWS which fields are secret, and a newly added credential is redacted by construction rather than by someone remembering to extend a pattern. Row-level counts re-confirmed exactly as stated: 2 of 6 row logs carry a robot secret; all six carry 2-3 `creds-show` blocks. |
| **B155** | 🟢 **The 15021 AMBIGUITY CASE DOES NOT FIRE ON A REAL LAB — measured, and it settles the one item `docs/lab-validation-plan.md` step 13 calls *the only item in this plan that changes shipping-code behaviour*.** MEASURED 2026-08-17 on cut B's guest: **TWO** Services expose port 15021 — `istio-ingress/istio-ingressgateway` (`selector.istio=ingressgateway`, ours) and **`vks-ingress/vks-uis-istio` (`selector.istio=null`)**, which is a VMware-shipped istio-based ingress for the VKS UIs. But `scripts/lib/istio.sh:70` filters `select(.spec.selector.istio != null)`, so the VMware one is **EXCLUDED** and discovery sees exactly **1** candidate. So `make attach-istio` does NOT hit its ambiguity refusal here, and `ISTIO_GATEWAY_NAMESPACE`/`ISTIO_GATEWAY_SERVICE` are NOT required on this lab. ⚠️ **Do NOT 'improve' the filter to include Services without an `istio` selector key** — `vks-uis-istio` is not a mesh gateway for tenant traffic, and including it would manufacture exactly the ambiguity this measurement just ruled out. COROLLARY worth knowing: on a guest where our gateway is NOT installed, the count is **0** and discovery correctly reports *no Istio ingress-gateway Service found* even though VMware's istio-based ingress is running — that is the right answer, not a defect. Two more step-13 answers from the same run: **`istio-cni-node` is ABSENT** (the plan expected it present, since `istioCNI.enabled` defaults to true — our install does not create it), and **GatewayClass `istio` is `Accepted=True`**. And B26 (the SHIPPED injector's selectors) **cannot be answered on cut B**: `istiod`'s image is `harbor.env1.lab.test/cicd/istio/pilot:1.30.3`, i.e. OURS — measuring the injector there would report our own selectors as VMware's, which is the wrong-mesh trap. |
| **B107-orig** | ✅ **ARCHIVAL — this is the ORIGINAL problem statement for `B107`, whose verdict is CLOSED. Kept verbatim as the evidence the fix was measured against; it is NOT open work.** **~42 of 48 `Step N` cross-references in scenario-1.md are semantically UNCHECKED, and Step 14's teardown claims are UNVERIFIED.** None DANGLE (all resolve to a real step), and ~6 were spot-checked correct — but "Step 7 tells you X" is only true if Step 7 still says X, and step numbering shifted when 8.5 was inserted. Separately, `98-uninstall-all.sh`'s label-scoping and its "prints what it left alone" claim were never read against the doc. Both are cheap to settle and neither is walk-checkable: the teardown block is SKIPPED by every row by design ("would destroy the lab mid-walk"), so it has NEVER been executed by any run. |
| **B157** | ✅ **FIXED + MERGED (#776) — this row is the WORKED EXAMPLE of why a closure belongs at the HEAD: its own marker sat at character 8709 while the head read 🔴, so on 2026-08-17 it commissioned a full adversary round against already-shipped code. That round found TWO NEW defects in the SHIPPED fix (#820), so the waste was not total — but the premise it was given was false.** **`ci-pass` — the SOLE required status check — went GREEN while a gate job in its own `needs:` list FAILED. MEASURED, not inferred, on run `32043952216` (PR #754).** Job conclusions from the API: `secrets` = **failure**, `ci-pass` = **success**. And `ci-pass`'s own step log shows the interpolated expression rendered as literally **`if false; then`** — so `contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')` evaluated **FALSE with a needed job at conclusion `failure`**, the step printed `ci-pass: OK`, and branch protection (`required: ["ci-pass"]` — the only entry) was satisfied. `needs:` is `[changes, static-check, static-check-fast, docs-lint, diagrams-check, secrets]`, so `secrets` IS in the list; this is not a missing dependency. **WHY IT MATTERS:** the ruleset requires exactly ONE check, and that check is demonstrably capable of passing over a failed gate — so the repo's entire merge gate can be green while a security scan job failed. The `secrets` failure in this instance was benign (the runner could not download `jdx/mise-action` — codeload 429 then 503 during the GitHub outage, so gitleaks never ran), which is *worse* for diagnosis, not better: the gate that exists to notice "the scan did not run" is the one that passed. **HYPOTHESES, none confirmed — do NOT implement before an idea round:** (a) a job that fails during **Set up job** (before any step runs) may report a `needs.<job>.result` that is not `failure`, while the REST API still reports `conclusion: failure` — i.e. the two surfaces disagree and the workflow expression sees the weaker one; (b) `needs.*.result` with `contains()` may behave differently from a per-job `needs.<job>.result` read in this case. **The `if: always()` + `contains(needs.*.result, …)` idiom is used because a blanket skip-test would redden `ci-pass` on essentially every PR** (measured and recorded in the job's own comment: a docs-only PR legitimately skips `static-check` + `diagrams-check`; a code-only PR skips `docs-lint` + `diagrams-check`), so the fix is NOT "assert every job succeeded". **The shape that is correct under both hypotheses** — and the one this portfolio already prescribes for the analogous `!failure()`-vs-skipped question — is an EXPLICIT per-job assertion that each needed job's result is `success` **or** `skipped`, and never `failure`/`cancelled`, exactly as the job already does for `static-check-fast`. **RED-PROOF that must go red and today does not:** run `32043952216` itself — any fix must report FAILURE for that combination of results. Re-run it against the fix before believing it. **BLOCKED on an idea round:** this is the sole required check, so a wrong change either reddens every PR or leaves the hole open, and CLAUDE.md's RULE ZERO names inventing-a-control as the act that most needs the round. | Found while checking whether #754's `secrets` failure was real or the outage — it was the outage, and looking exposed this instead. 🔴 **ROUND LANDED 2026-08-17 — verdict `ship-with-changes`. The premise is CONFIRMED and WORSE than filed; MY PROPOSED FIX IS UNPROVEN against the very incident it targets, and one candidate value of the unmeasured variable DEFEATS it.** (a) **Confirmed, with the confounds excluded**: `run_attempt: 1` on every job (no re-run confound); `secrets` completed 15:59:23Z and `ci-pass` STARTED 15:59:27Z (4 s later, so no race); `ci.yml` at the run's head sha `ea600bd` is byte-identical to the tree, so `secrets` really was in `needs`. (b) **BLAST RADIUS: 3 of 400 `ci.yml` runs, and ONE IS ON `main`** (`32044803928`, 16:15:15Z) — not one bad run. All three are the identical shape: `secrets`, `conclusion: failure`, **`steps: 1`**. (c) **THE DISCRIMINATOR IS THE STEP COUNT, and it is available where the `needs` context is not**: a job that fails at a USER step (`steps > 1`) renders the guard as `if true` and the gate WORKS (~33 runs, e.g. `32044827165` logs `Run if true` + `result: failure`); a job that fails inside **`Set up job`** (`steps == 1`) renders `if false` and the gate LIES. A *skipped* job is `steps: 0` / `conclusion: skipped`. The jobs API and the check-runs API report the truth in ALL cases; only the expression context lies. (d) 🔴 **THE FINDING THAT REFUTES MY FIX: `needs.secrets.result` for a set-up failure is UNMEASURED and NOT source-readable** — the `needs` context is computed server-side by the closed-source orchestrator, and nothing in those 3 runs rendered it. All that is proven is *neither `failure` NOR `cancelled`*. If the value is `''`/absent my fix closes the hole; **if it is `skipped` or `success` my fix does NOT** — and for the three CONDITIONAL jobs a set-up failure would be indistinguishable from a paths-filter skip. So landing it and reporting the hole closed would be manufactured confidence. (e) **THE HIGHEST-VALUE CHANGE IS A RULESET EDIT WITH NO CODE**: each job ALREADY publishes its own check run, and for head sha `ea600bd` `gh api …/check-runs` returns `secrets → conclusion: failure` — so had `secrets` been directly required, PR #754 would have been BLOCKED. Direct-require the three UNCONDITIONAL, dependency-free jobs (`changes`, `static-check-fast`, `secrets`): measured present and **never skipped in 60 of 60** completed PR runs, so it cannot false-block, and it is correct under EVERY candidate value of the unmeasured variable. ⚠️ **Do NOT direct-require the conditional trio** — GitHub's own docs: *a job skipped by a conditional reports **Success***, and *jobs depending on a failed job are skipped and do not report a failure* — so a legitimate `if:`-skip and a my-dependency-died skip are indistinguishable. That is precisely why the aggregator exists. **Keep `ci-pass` for the trio; require the three unconditional jobs directly.** (f) **`changes` is the worst node in the graph**: `static-check`/`docs-lint`/`diagrams-check` all `needs: changes`, so if it fails they are all `skipped` — accepting `skipped` blindly means green with zero gates run. A step-failure of `changes` is already RED (measured `32043751152`); only its set-up variant is open. Assert `needs.changes.result == 'success'` (it is unconditional). (g) **THE CONSTRAINT IS REAL AND THE JOB'S OWN COMMENT IS ACCURATE**: measured over 60 PR runs, **59/60 (98%) skip at least one job** — 33 docs-only (`diagrams-check,static-check`), 11 code-only (`diagrams-check,docs-lint`), matching the comment's two named sets exactly. A blanket every-job-must-succeed would redden 98% of PRs. Do not weaken it. (h) **`ci-pass` has ZERO test coverage** — `grep -rl ci-pass scripts/ Makefile` finds only the Makefile. The one job that decides every merge is the least-verified code in the repo. Prescribed: extract the verdict to `scripts/ci-pass-verdict.sh` reading `name<TAB>conclusion<TAB>nsteps` on stdin, commit the captured `…/jobs` JSON of the three real false-green runs as fixtures, and add 7 cases — A1 (the real fixture) and A7 (`changes` success but `steps:1`) are the RED-proofs that fail today; A4/A5 (docs-only, code-only) are the false-RED controls that stop the fix reddening 98% of PRs. **The `nsteps` discriminator is exactly the signal the `needs` context lacks, and it is available offline.** (i) **Two more measured findings:** a **cancelled** run can still publish `ci-pass: success` (`31671455091`); and `if: always()` does NOT guarantee `ci-pass` reports at all — in 2 of 13 cancelled runs the `ci-pass` entry is **entirely absent**, which is fail-CLOSED on a PR (acceptable) and silent non-verification on `main` (document, do not 'fix' by loosening). (j) **A correction to CLAUDE.md**: its *"`static-check` still does not run per-PR"* is stale — `ci.yml` restores it via `needs.changes.outputs.code == 'true'`; 33/60 PR runs skip it only because they are docs-only. **BLOCKED ON AN OWNER DECISION for the ruleset half**: adding required checks is a repo-settings mutation that can wedge merging (a renamed job leaves its required check `Pending` forever), so it is recommended, measured, and NOT applied unilaterally. **RESIDUALS the round names rather than hides:** the mystery value (settle with a `workflow_dispatch` fixture whose arm 1 uses an unresolvable action — ⚠️ and it flags that its arm fails at *Getting action download info* while the incident failed at *Download action repository*, so whether the orchestrator classifies them identically is UNVERIFIED); splat-vs-per-job for this case (untested); whether a 1-step CANCELLATION reports truthfully (its candidate evidence `32044827165` is confounded); and whether any of the 3 head shas actually contained a secret (the failures were all codeload infrastructure, so the exposure is *unscanned*, not *known leak*). 🟢 **FIXED + MERGED 2026-08-17 (PR #776, main `97e1da1`), and the wired control is LIVE-PROVEN.** `ci-pass` now decides from the **JOBS API** instead of the `needs` context: the API reported the truth in every case examined, while the context rendered `if false` over a `failure`. An implementation round measured the fix against **all 400 real runs / 2,698 jobs** and found a **THIRD** false green nobody had identified (`32041902265`, alongside `32043952216` and `32044803928`), plus the root cause of all three — `Prepare all required actions` hitting codeload 429/429/503, i.e. transient GitHub infra that RECURS, which confirms refuse-is-correct. Ten findings applied, two HIGH: the hand-typed job lists rot into a **hard refuse of every run** (66/400 refused on `static-check-fast ABSENT`; the job set changed twice in six days) — now gated by `scripts/test-ci-pass-verdict.sh`, which asserts them against ci.yml's job list AND `ci-pass`'s `needs:`; and `filter=all` would make a `rerun --failed` that FIXES ci permanently unmergeable (measured on runs `32044956290`/`32044785511`). Also corrected a FALSE FACT in my own header — it called the step count "the discriminator" when across 2,698 jobs it has **never fired**; the conclusion arm does the work. Measured on the corrected script: **3/3 false greens caught, 0 false reds, 0 false allows** over 328 current-workflow runs. ⚠️ The round's residual ("the decisive path has never executed") is **CLOSED**: it ran on #776's own CI — `6 job(s) judged — 4 ok, 2 legitimately skipped, 0 bad`, `ci-pass: OK` — and then **correctly REFUSED** #777's first run over a genuinely-failed `secrets` job. ➕ **THE ROUND'S LAST RESIDUAL IS CLOSED, as a NEGATIVE RESULT (2026-08-17).** It ended *"I did not review `scripts/classify-changes.sh`, whose correctness the `changes` outputs depend on"* — a real gap, because a wrong classifier silently SKIPS gates and every job then reports green having run nothing. Audited; **no defect found**, and the four checks are recorded so nobody re-derives them: **(1)** the `docs/diagrams/*` arm is complete — **15** tracked `.puml`, **0** outside that prefix, and **0** committed PNGs outside it, so no diagram source can fall through to `*)` and lose `diagrams=true`. **(2)** the `Makefile` arm is complete — `PLANTUML_VERSION ?= 1.2026.6` lives at `Makefile:179` and there are **0** `*.mk` includes for it to migrate into; `C4_PLANTUML_VERSION` (`Makefile:1430`) is a second renderer-affecting pin and is covered by the same arm. **(3)** the `ci.yml` occurrence that looked like a second pin is a **COMMENT** (`:325`, *"the renderer pin (PLANTUML_VERSION, in the Makefile)"*), so a Renovate bump cannot land there and classify as `code` with `diagrams=false` — which WOULD have been the exact hazard the `Makefile` arm exists to prevent. **(4)** its own suite runs **19 cases** and passes, including the two that matter here (`committed PNG changed -> diagrams=true`, `Makefile -> code+diagrams`) and the empty-input arm that turns everything on. ⚠️ **The coupling worth knowing:** Renovate's customManager is scoped to `/^Makefile$/` and the classifier's arm is the literal `Makefile` — they agree **by construction today**, and nothing asserts it. If the PlantUML pin is ever moved (into an include, into `.mise.toml`, into a workflow `env:`), BOTH must move with it or the drift gate stops firing on a renderer bump and every committed PNG goes silently stale. |
| **B158** | ✅ **CLOSED 2026-08-18 by verification — every cause is fixed in the shipped code.** I came to this row expecting to fix it before the certifying run, because its headline predicts a guaranteed row-6 failure. MEASURED first, and it is stale. **Headline (scenario-2 missing the PATH step):** FIXED. `grep -c shell-init` is **2** in BOTH documents; scenario-2:46-70 carries the section AND the measured OS explanation (Photon finds kubectl in `/usr/bin`, Ubuntu downloads it to `~/.local/bin` which no shell searches, so a bare `kubectl` exits 127 on Ubuntu only). **Cause 2(i) (`kind-down.sh` gating a docker-FREE operation):** FIXED — `:20` now reads *"NO `require_cmd docker` HERE, DELIBERATELY"*, with the reason: the file half (overlay, kubeconfig, secrets/) needs no engine, and scenario-2 tells every tenant to run `make kind-down` at Step 0c. **Cause 2(ii) (a FATAL prescribing an impossible remedy):** gone with the guard. **The FALSE GREEN (absence inferred from INABILITY):** FIXED, and thoroughly — `_dps` is now a THREE-state read returning **2 for "could not ask"** against 0 for "answered, possibly empty", branching on rc rather than stdout, because permission-denied(1), socket-absent(1) and not-on-PATH(127) all yield EMPTY stdout. Its comments also record two traps worth keeping: do NOT match the stderr TEXT (two different strings for one condition across docker versions), and do NOT generalise to `$(container_engine)` — `podman ps --filter name=kindccm-` returns rc=0 + EMPTY, an honest-looking success that would defeat the entire fix. **Cause 3 (F9 / STALE_CA)** was never OS-specific and belongs to B148/B160, which remain open on their own merits. **Residual, unchanged and still `inferred`:** whether a real Ubuntu operator in a LOGIN shell has `~/.local/bin` on PATH (stock `~/.profile` adds it only if the directory exists at login). | — |
| **B160** | 🟡 **ITS ONE IN-REPO ITEM IS SCOPED AND DELIBERATELY NOT LANDED BEFORE THIS CUT (2026-08-19).** The row's remaining action here is item (3): teach `walk-doc.sh`'s `substitute()` the `<argocd-lb-ip>` placeholder so scenario-2's ArgoCD-address block stops being skipped. **Measured:** `substitute()` (`walk-doc.sh:205-210`) knows exactly ONE placeholder — `<your SSO password>`, guarded by `[ -n "${VCF_CLI_VSPHERE_PASSWORD:-}" ]` — so the change is a single symmetric line, and it is **fail-safe in isolation**: with the value unset, `has_live_placeholder` still fires and the block still SKIPS exactly as today. It cannot make things worse by itself. ⚠️ **But it is not landing before this cut, and the reason is the first-go rule, not the code.** Substituting the value converts a SKIPPED block into three EXECUTED commands on rows 5 and 6 — an `/etc/hosts` entry, `make fetch-argocd-ca ARGOCD_SERVER=argocd-server`, and a `.env` edit — and rows 5/6 are the two that went green **for the first time** in run 8. Adding three never-before-executed steps to them immediately before the run that is meant to re-earn first-go trades a certification for coverage, and that is the owner's trade to make, not mine. **It also needs the other repo:** `walk-matrix.sh` must export the value (it already holds it at `OUT_DIR/.as`), so this is cross-repo and cannot be verified from here alone. ⇒ **Land it AFTER a green cut**, then re-run the matrix to see the three steps actually walk. The row's own framing that this is *"strictly stronger"* is true about COVERAGE and silent about RISK; both are recorded here so the next session does not have to re-derive either. ⏸️ Original follows. — 🟡 **RE-SCOPED 2026-08-18 by a SECOND idea round — THE RED ROW IT EXISTS TO FIX NO LONGER EXISTS, and two of my steps were structurally impossible.** ⚠️ **(1) CRITICAL — already fixed, ~10 hours before I designed against it.** nvl **#77** ("pin the argocd CA only when the cert covers the address we dial", 2026-08-17 15:26) landed the IP-SAN guard, and a POST-FIX run exists: `run-20260817T203305Z-314064/MATRIX-row5-photon.log` shows `argocd CLI trust: insecure:true — the cert has NO IP SAN … (weaker test, disclosed)`, then `write mechanism: api (kubectl=no, argocd-api=yes)` — the probe ANSWERED YES — then Applications created `via api` and `End-to-end verified for EVERY app (javawebapp gowebapp)`. **STALE_CA is GONE.** The block's residual rc=1 is *ArgoCD cannot clone the deploy repo* — Gitea reachability, NOT TLS, NOT the CA, NOT the SAN. ⚠️ The CLAUDE.md handoff line *"walk-matrix.sh is untouched, so rows 5+6 remain blocked on a fix nobody has written yet"* is now **FALSE** and is what my design inherited. **(2) CRITICAL — my DNS step is structurally impossible.** `nested-vsphere-lab/scripts/dns-record.sh:44-47` refuses any name outside the lab domain (`*.${DOMAIN}` or `_die`), so `argocd-server` and `argocd-server.cicd` both hard-die, while the one publishable form `argocd-server.env1.lab.test` is **not in the SAN list** — steps 2 and 3 are mutually unsatisfiable through the dns-attendant. The doc already prescribes `/etc/hosts` (scenario-2.md:295), and dns-record.sh's own "hosts is insufficient" argument does NOT apply here because it is about IN-CLUSTER workloads resolving through dnsmasq — the only consumer of `ARGOCD_SERVER` is the jump box, never a pod. **(3) HIGH — WRONG LAYER, and the better fix is smaller.** The document's own ArgoCD-address block is **NEVER WALKED**: `walk-doc.sh:563-564` skips any block carrying an unsubstituted `<placeholder>`, scenario-2.md:291-309 carries `<argocd-lb-ip>`, and `substitute()` knows only `<your SSO password>`. Measured: the post-fix row-5 log contains **ZERO** occurrences of `etc/hosts` or `fetch-argocd-ca`. So the harness would prove a path the tenant is never given. **Teach `substitute()` the `<argocd-lb-ip>` placeholder** — the harness ALREADY holds that IP in `OUT_DIR/.as` — and the document's own three-step block walks: hosts entry → `make fetch-argocd-ca ARGOCD_SERVER=argocd-server` → name in `.env`. Strictly stronger: it converts a substitution into a walk, needs no lab-DNS change, and invents no name. **(4) HIGH — the doc already names the NAME, and it is not mine.** scenario-2.md:280 already requires "a name the argocd-server certificate carries — not the bare LB IP"; :285 already makes `ARGOCD_CA_FILE` required for a verifying path; :317 already warns `argocd-server` is single-label and search-suffix fragile. The doc says `argocd-server`; my design picked `argocd-server.cicd`, which no reader is told to use. The gap is not the doc — it is that `supply_s2_contract` still writes the IP (`walk-matrix.sh:723` → `:541`). **(5) HIGH — the harness's SAN disclosure is TRUNCATED MID-NAME** (`cut -c1-120`): the emitted string is exactly 120 bytes ending `DNS:argocd-server.cicd.sv`, almost certainly `…svc` truncated. Reading a name off it invites an x509 failure that looks like a CA problem. Print the parsed DNS names one per line and DERIVE the choice. **(6) MED — (a) CONFIRMED, and here is the measured contract** (tier-B against the REAL pinned `v3.0.19+d67e6eb90-vcf`, self-signed DNS-SAN-only cert, pasted token, NO `argocd login`): `IP + ca-data` → x509 cannot validate for IP · `IP + insecure:true` → TLS OK · `NAME(in SAN) + ca-data` → **TLS OK and VERIFIED** · `NAME + nothing` → unknown authority · `--grpc-web` does NOT change trust · a 2-cert PEM bundle is accepted · a `servers[]` entry keyed on the WRONG server **fails CLOSED** ("Server '…' undefined"). So `certificate-authority-data` is honoured, is **PER-SERVER** (keyed by the `server:` string), and works with a pasted token. Adopt these in place of the comment. **(7) MED — blast radius:** `ARGOCD_SERVER` has more consumers than the CLI config — `lib/argocd.sh:415-435` curls `/api/v1/session` via `argocd_curl_tls_init` (`--cacert $ARGOCD_CA_FILE` or `-k`), a SEPARATE trust path that `insecure: true` does nothing for; plus `make fetch-argocd-ca`, `argocd-auth-check.sh`, `creds-show`, `.env.state`. Change `ARGOCD_SERVER` and `ARGOCD_CA_FILE` in the SAME change and assert `ARGOCD_TLS_MODE=verified`. **(8) MED — what `insecure: true` actually costs:** blindness to an argocd-server cert regression (wrong SAN, expired, wrong issuer) — and NOTHING in correctness of the tenant mechanism, which is measured green. ⇒ **This is a POST-CERTIFICATION improvement, not a blocker. Land nothing until the verdict file exists.** Original text follows. — **The rows-5+6 harness fix: supply a NAME, not the LB IP — idea round 2026-08-17 (`vks-adversary`, instruments PERMITTED) returned `ship-with-changes` and REFUTED three of my specifics, one of which would have swapped one TLS failure for another.** **MY PREMISE HELD AND WAS INCOMPLETE.** Confirmed from the harness's own on-disk artifacts (`measured`): `/tmp/walk/.as` = `192.168.101.131`; `/tmp/walk/.acfg` carries that IP in **all six** argocd-CLI keys with `certificate-authority-data` present and `insecure` absent — the un-verifiable CA-pinned-to-an-IP combination, sourced at `walk-matrix.sh:711-712`. **INCOMPLETE THREE WAYS:** (1) my *"rows 5 and 6 both fail with STALE_CA"* is true of row 5 (**[24] only**) and MISLEADING about row 6, which has **FOUR** failed blocks — `[13]` docker not found, `[21]`/`[22]` kubectl rc=127, `[24]` STALE_CA — so **this design greens row 5 and leaves row 6 with three failures**; do not ship it as "the fix for rows 5 and 6" or the next run reads as a refutation of a correct change (the ubuntu-PATH half is **B158**/B159). (2) vks PR #745 is **MERGED** (`4bf0fd6`) — the product already reads the SANs off the wire, prints them, separates DOES-NOT-RESOLVE from every TLS verdict, and prints the exact `sudo tee -a /etc/hosts` remedy at `70-configure-argocd.sh:376-425`. (3) **run 5b walked the OLD doc** — `MATRIX-row6-ubuntu.log:551` still quotes *"ARGOCD_SERVER = the discovered argocd-server LB IP"*, so today's `scenario-2.md:280`/`:295` **has never been walked**. **FIVE DEFECTS IN MY DESIGN:** **(F1 CRITICAL)** it does not rekey `.acfg` — `walk-matrix.sh:774-778` writes all six CLI-config keys from `$asrv`, so setting `ARGOCD_SERVER=argocd-server` while the config still says `server: 192.168.101.131` leaves **no matching `servers[]` entry**, the CLI falls back to the system roots, and you get `x509: signed by unknown authority` — same STALE_CA class, new cause, reading as *"the fix didn't work"*. This is the **exact** failure `lib/govc.sh:182-190` was written to prevent (an FQDN rewrite applied to one call path and not its siblings). Fix: one variable — `local aname="argocd-server"` for `.as` and every `.acfg` key; keep `$asrv` ONLY as the hosts-mapping address and the dial target. **(F3 HIGH)** *"read the SANs off the wire"* is **refuted twice**: `localhost` is **FIRST** in the real SAN list, so a naive first-DNS-SAN pick writes `192.168.101.131 localhost` into the walkbox's `/etc/hosts` and breaks loopback for the rest of the row; and structurally the harness ALREADY reads the CA off the wire (`:766-770`), so a wire-read name verifies the server **against itself** — it can never fail on a wrong CA, wrong SAN, expired cert or wrong issuer. Use the **constant `argocd-server`** (the value the document prints at `:304`), overridable via `WALK_ARGOCD_NAME`, and use the wire read as a **discriminator**: if the cert does not carry it, the DOCUMENT is wrong and the row must say so loudly. **(F4 HIGH)** the doc's own new block writes a LITERAL `<argocd-lb-ip>` — `walk-doc.sh:138-141` substitutes only `<your SSO password>` and `should_skip` has no arm for it, while `walkbox-vm.sh:143` grants NOPASSWD sudo, so it **executes**. Either the name never resolves (a NEW row-5 failure one block earlier) or the harness's own entry makes it succeed and the row **goes green having never tested the document's instruction** — and `tee` echoes its input, so the block's Expect passes either way and **cannot catch it**. Fix, strictly better than my design: extend `substitute()` with `<argocd-lb-ip>` → `$asrv` and let the DOCUMENT create the hosts entry, guarded the way `substitute` already guards (empty ⇒ leave the placeholder). **(F5 HIGH)** `ARGOCD_CA_FILE` is **never supplied** — `grep -c` = 2, both in comments, and `walk_env`'s heredoc does not emit it — so `ca_verifies_endpoint` returns 5 and `70-configure-argocd.sh:401-403` prints *"NOT MEASURED: ARGOCD_CA_FILE is unset or unusable, so the two knobs cannot be told apart."* Fixing the address alone leaves the row failing with a vague message. Stage the CA as a file mirroring `.hca` at `:833`. **(F6 MED)** the injection uses the exported-env-var form that `.env.example:373-386` and `scenario-2.md:299-303` both say in bold is **silently ignored** once `.env` carries the key; it works today only because `.env.example:413` ships `ARGOCD_SERVER` commented — i.e. **by luck**, and it stops the day `09-argocd-address.sh:100` writes the key. Assert instead of hoping: `70-configure-argocd.sh:303` already logs `argocd-server (effective): X`; require it to equal what was supplied. **(F7) my REASON for preferring the CA over `insecure: true` was WRONG** — a wire-read CA is equally blind; what earns an anchor its keep is coming from an INDEPENDENT source, which is exactly what `walk-matrix.sh:822-831` already does for Harbor (`make harbor-ca-from-cluster`, read from the Supervisor by label, refuses a leaf, and verifies against the live endpoint — its own comment calls it a *"FREE FAIL-FAST … instead of surfacing 40 minutes later"*). That is the 1133-second block-[24] failure, already solved one arm over. **ANSWERED:** `ARGOCD_SERVER` is the only member of this class (Harbor is a real FQDN with a cluster-sourced, endpoint-verified CA; `.ds` is self-consistent by construction) — but see the residual. `/etc/hosts` is the right vehicle, not lab DNS: every `ARGOCD_SERVER` consumer is a jump-box script, **nothing in-cluster dials it**, `argocd-server` is single-label so `nsswitch` files-before-dns applies, and `dns-record.sh:40-45` would refuse to publish it anyway. **An IP SAN is REFUTED three ways:** the LB IP changes every reinstall (`.130→.133→.134→.135→.136→.139` observed), a supplied `tls.crt` REPLACES every name, and minting a cert for a shared service is an admin act. **RESIDUAL:** the three-arm curl measurement is mine and was NOT reproduced by the round (no lab access) — the arm most worth re-running is the middle one **with `.acfg` rekeyed**, which is the combination F1 says is untested; whether glibc skips a malformed `/etc/hosts` line is `inferred` and load-bearing for F4; and rows 5/6 currently inherit Harbor's DNS record from rows 1-4 on the same cut, so `WALK_ROWS="5 6"` on a FRESH cut has never been exercised. | Fix lives in nested-vsphere-lab, not here. ⚠️ **2026-08-17 — nvl PR #77 SHIPPED A DIFFERENT FIX FOR THE SAME FAILURE, AND MY CLAIM ABOUT IT WAS OVERSTATED. Read this before reading the next matrix result.** What #77 does: pin the argocd CLI's `certificate-authority-data` **only when the leaf actually carries an IP SAN for the address being dialed**, and otherwise take the `insecure: true` arm, with both arms logging which they took and why. What it does **NOT** do: any part of THIS row's design. It does not supply a NAME, does not rekey `.acfg` (F1), does not touch `substitute()` for `<argocd-lb-ip>` (F4), does not stage `ARGOCD_CA_FILE` (F5), and does not assert the effective address (F6). **So this row stays 🔴 and its five defects are all still unaddressed.** ⚠️ **And note the interaction with this row's own finding:** it records that *an IP SAN is REFUTED three ways*, which means #77's discriminator will take the **insecure** arm for ArgoCD every time — by construction, not by accident. That is the weaker outcome this row argued against, and it is defensible only because **F7 already conceded the point**: a wire-read CA verifies the server against itself, so `insecure: true` and a wire-read anchor are equally blind. A real anchor still requires a NAME plus an INDEPENDENT source, which is what this row exists to build. **THE CLAIM TO NOT REPEAT:** I wrote that #77 *unblocks rows 5 and 6*. It removes the **STALE_CA** arm only — this row's own point (1) says row 6 had **FOUR** failed blocks (`[13]` docker not found, `[21]`/`[22]` kubectl rc=127, `[24]` STALE_CA), and warns verbatim *"do not ship it as 'the fix for rows 5 and 6' or the next run reads as a refutation of a correct change"*. Verified since: the PATH causes have their own fixes — `make shell-init` exists (`Makefile:690`) and **#762 is MERGED** — so rows 5 and 6 may well both pass now, but if row 6 fails on a PATH block that is **B158/B159**, not #77, and if it fails on `[24]` again that is a REAL refutation of #77. Attribute the next result per BLOCK, never per row. ⚠️ **ITEM (3) SUPERSEDED 2026-08-18 — its prescribed fix was BUILT, MEASURED INERT and REVERTED the same day, and the row as written would send the next reader to rebuild it.** Item (3) says the ArgoCD-address block is never walked because `substitute()` knows only `<your SSO password>`, implying the fix is to teach it `<argocd-lb-ip>`. That substitution was added to `walk-doc.sh` on 2026-08-18 and reverted the same day; the reasons are recorded IN THE FILE at `:141-165` so nobody rebuilds it, and they are measured, not reasoned. **(a) It was INERT:** its own comment claimed the harness "exports it as `ARGOCD_LB_IP`", but that name occurs ZERO times in the whole `nested-vsphere-lab` repo — `walk-matrix.sh:541` emits `ARGOCD_SERVER`, and `ARGOCD_LB_IP` is published only by `07-install-argocd.sh` (the KinD flow) into `.env.state`, which row 5's own log reports absent. Run with the exact harness env, the placeholder survived and the block still SKIPPED. **(b) Re-pointing it at `ARGOCD_SERVER` would not help either** — the block cannot complete its own purpose, because step 3 of that 3-step procedure is COMMENTS ONLY and `fetch-ca.sh` merely PRINTS "set it in .env" without publishing the var; afterwards `ARGOCD_SERVER` is still a bare IP, i.e. the exact IP-endpoint + CA-present combination B148 lab-verified can NEVER verify. **(c) It would ADD a failure mode to a certifying row** — `fetch-ca.sh` is `set -euo pipefail`, so it converts a counted SKIP into a block that can FAIL on rows 5/6, and `tee -a` APPENDS, so a retried row duplicates the `/etc/hosts` line. **The honest state:** that block is deliberately NOT walked because its third step is unwalkable, and making it walkable is a DOCUMENT change (a real `state_set`/`sed` line that actually publishes the value), not a substitution. Items (1) and (2) of this row are unaffected and still stand. ✅ **ITEM (1) IS NOW CONFIRMED BY A CERTIFYING RUN, not just by a post-fix log.** Run 8 (2026-08-18) walked **row 5 (photon, scenario-2)** to `WALK DONE — 27 blocks: 18 ran, **0 FAILED**, 9 skipped` with **0 EXPECT UNMET**, on the same lab and the same tree. That is the tenant path completing end-to-end, which is a stronger statement than the single post-fix row-5 log this item was originally argued from: STALE_CA is gone AND nothing downstream of it blocks. (Run 6 had already taken rows 5+6 green once; run 8 makes it reproducible rather than a one-off.) ⏸️ **SEQUENCING for the one remaining item — the DOCUMENT change that would make the unwalkable third step walkable: it stays OUT until run 8 is certified.** Editing a walked scenario block RESETS the first-go clock by construction — run 8 cannot certify a block it never saw — so landing it now would cost a whole six-row run to re-earn what is currently in hand. Land it as the FIRST change of the next cycle instead, so the following run measures it. Same reasoning B69 records for its own deferral. |
| **B161** | ✅ **FIXED + MERGED (#777, main `b4fcd8f`) — but NOT as this row proposed: the round REFUTED the row's own fix.** **`creds-show` calls a STALE REAL password a "placeholder" — and that is worse than being merely inaccurate.** FOUND 2026-08-17 by DOING the standing post-cut credential verification against the live cut-B lab, with both controls. **MEASURED:** endpoints are up (`harbor.env1.lab.test` -> **200**, argocd `192.168.101.131` -> **200**); `.env.state` is **ABSENT** on this box (the walk cuts the lab on VMs, not here); `make creds-show` is rc=0 and its provenance line reads *"values below : DEFAULTS from .env / .env.example — NOTHING IS INSTALLED YET, these are placeholders"*; and `GET /api/v2.0/users/current` with the `.env` credential returns **401**. ⚠️ **THE NEGATIVE CONTROL ALSO RETURNED 401** (a deliberately wrong password), so that pair ALONE cannot discriminate *bad credential* from *the API rejects everything* — which is why the **POSITIVE control** matters: `/api/v2.0/ping` and `/api/v2.0/systeminfo` both **200**, so the API is serving and the 401 is a genuine CREDENTIAL verdict. **THE DEFECT:** `HARBOR_PASSWORD` in this box's `.env` is **NOT a placeholder** — it is a REAL 16-character generated password from an EARLIER cut. creds-show says "placeholder" because `.env.state` is absent, which is the right INFERENCE from the state it can see and the wrong DESCRIPTION of the value it is printing. **WHY WORSE THAN INACCURATE:** a reader who is told "placeholder" discards it; a reader who TRIES it gets 401 and reasonably concludes THE LAB is broken rather than that their copy is stale — the wrong-cause error class this repo keeps hitting — and a real-looking credential labelled fake is exactly what someone pastes into a ticket. **CANDIDATE FIX (needs its own idea round — it changes what a credential-printing command ASSERTS):** three states, not two. Today it is DISCOVERED vs DEFAULTS; the missing one is **STALE** — a value that is neither the committed sentinel nor discovered from live state. The sentinel comparison already exists single-sourced (`harbor_url_is_placeholder`-style), so the split is offline: *equals the committed example value* = a true placeholder; *a real value with no `.env.state`* = STALE, vouched for by nobody. ⚠️ **DO NOT** make creds-show probe endpoints to decide — it must stay read-only and offline, or it acquires a network dependency and a whole new failure mode. **CHROME half, stated not skipped:** item 5 says an HTTP probe is not proof a human can log in. The UI loads and the endpoint serves, but NO valid credential exists on this box for this cut, and typing a password into a web login form is forbidden by standing policy — so the human-login half is **UNPROVEN for this lab state**, which is a fact about the state rather than a step I skipped. 🟢 **FIXED + MERGED 2026-08-17 (PR #777, main `b4fcd8f`) — but NOT as this row proposed; the round REFUTED the row's own fix and found the defect UNDERSTATED.** The real defect is a **positive, checkable, FALSE claim**: with a populated `.env` and no overlay, `creds.sh:351` asserted *"EVERY value above is a default from .env.example — including Harbor's and Gitea's. None of them exists."* `HARBOR_PASSWORD` is **commented** in `.env.example` (measured `uncommented=0`), so a value on screen cannot have come from there — and on a real lab it is a live credential the operator typed, per the runbooks. **REFUTED:** "equals the committed example value = a true placeholder" is **dead code** — all three you-choose secrets are commented, so nothing exists to compare against and every password, live ones included, would fall to the stale arm. **AND FRESHNESS IS NOT COMPUTABLE OFFLINE AT ALL**: `.env` carries no cluster stamp, so a leftover from a destroyed lab and a just-typed value render **byte-identically**; a STALE label would be FALSE on the repo's own documented real-lab flow and would send an operator to rotate a working credential. **SHIPPED instead:** answer **SOURCE** — a value uncommented in `.env` is named as coming from `.env`, unstamped and unconfirmable here, pointing at `make env-validate`, which authenticates against Harbor for real; the genuinely-empty case keeps its original wording untouched. **The gate was structurally blind**: every case held `.env` empty via `SKIP_DOTENV=1`, so this state was unreachable by construction — which is why three prior sessions on the file never saw it. RED-proven (neuter the discriminator → 4 of 7 new cases fail, each naming the right defect). CI then caught a real defect in the fix itself: the first fixture used a realistic 16-char password and gitleaks flagged it; it survived a working-tree fix until **force-push**, because the old commit stayed reachable via the remote-tracking ref. |
| **B162** | 🟢 **CLOSED as a NEGATIVE RESULT — the blind source-assertion was a class of ONE, not the 11-wide hole its round warned it might be. Audited 2026-08-17; recorded so nobody re-derives it.** The implementation round that found `test-argocd-session.sh`'s `--data @-` assertion matching **only a comment** correctly flagged **11 sibling source-assertion gates as UNAUDITED — "not proven healthy"** — and gave the discriminator: *does the pattern match any line whose first non-space char is `#`, and does it match any line that is not?* **RAN IT over all 16 such gates** (`grep -q` against a `${SCRIPT_DIR}`/`${REPO_ROOT}` file). **RESULT: 15 sound, 1 blind, and the blind one is the one already fixed.** Per-gate, `tot`/`comment`/`code`: `ca-staleness CA_STATUS_STRICT` 1/0/1 · `argocd-addr _cv -ne 0` 1/0/1 · `argocd-addr IP Address` 1/0/1 · `state-overlay VKS_STATE_KIND` 2/**1**/1 · `state-overlay file=${3:?}` 1/0/1 · `secret-quoting rm -f OUT_FILE` 1/0/1 · `kind-down KIND_KUBECONFIG` 1/0/1 · `kind-down gitea-ci-token` 2/**1**/1 · `harbor harbor_last_code` 4/1/3 · `harbor HARBOR_CODE_FILE` 4/0/4 · `argocd-topo go-template` 2/0/2 · `argocd-topo gitea_clone_url` 1/0/1. **The four `test-kubeconfig-ready.sh` loop gates are STRUCTURALLY IMMUNE** — they anchor `^[[:space:]]*kubeconfig_ready[[:space:]]*$`, a whole-line match, and a line beginning `#` can never satisfy it. ⚠️ **THE RESIDUAL WORTH NAMING, since "sound" is weaker than it sounds:** two gates (`VKS_STATE_KIND`, `gitea-ci-token`) match **one comment AND one code line**. They are sound TODAY because the code line exists — but delete the code and leave the comment and they keep passing, which is the identical failure mode one step removed. They are not urgent (a deletion is a louder change than the continuation-split that caused the original bug) and they are not clean either. ⚠️ **AND "matches code today" is not "would go red if the code changed SHAPE"** — the original defect was not a deletion, it was a `\` continuation splitting `curl` from `--data @-` across two lines. This audit measures the first property, not the second; a gate whose pattern spans tokens that could be line-split is still at risk. The general fix is the one now in `test-argocd-session.sh`: fold continuations and drop comments before matching, and carry a positive control so a broken normaliser cannot make every assertion pass vacuously. | Closes the round's residual. |
| **B156** | 🟢 **RECORD-ONLY, AND ITS RE-OPEN CONDITION IS RE-CHECKED 2026-08-19 AND NOT MET.** The row's own verdict is *"record, do not build"*: the fake-green mechanism is real (a post-1.31 core API under `-ignore-missing-schemas` is `Skipped: 1, rc=0` at 1.31.0 and `Valid: 1` at 1.36.0) but the consequence for THIS repo measured **zero**, and aligning the pin is a NEW CONTROL on a gate that a zero consequence does not earn. It names the re-open observable precisely — *"the `skipped (NO SCHEMA)` count DIVERGING between the two versions, a two-command check"* — so I ran it rather than assuming it still held: **`make validate` at `KUBERNETES_VERSION=1.31.0` and at `1.36.0` both report `10 skipped`.** No divergence ⇒ **the condition is not met and the row stays closed.** Re-run those two commands after any change that adds a manifest using a core API newer than the pinned schema set; the 10 skips are Tekton CRDs, not core kinds. ⚠️ **Do NOT add `KUBERNETES_VERSION` to `.env.example`** — `check-env-coverage.sh:95` excludes `validate.sh` in the same case-arm as every other CI gate, deliberately and with the reason in its header, and `validate.sh` never calls `load_env` so the `${…:-1.31.0}` default would win anyway, making the entry documentation with no effect. ⏸️ Original follows. — 🟡 **`KUBERNETES_VERSION` — the single input that decides the WHOLE core schema set — is a hardcoded literal outside every mechanism this repo uses to keep versions honest.** MEASURED 2026-08-17: it appears at `scripts/validate.sh:14` as `KUBERNETES_VERSION="${KUBERNETES_VERSION:-1.31.0}"` and at `:85`/`:89` as `-kubernetes-version`, and **nowhere else in `scripts/` or `.env.example`** (3 sites, all in that one file). `validate.sh` **never calls `load_env`**, so it is invisible to `check-env-coverage`, to `check-toolchain-alignment` and to Renovate — a violation of this repo's own single-source-of-truth rule, in the gate that validates every manifest. ⚠️ **THE `check-env-coverage` HALF OF THAT SENTENCE IS ALSO REFUTED, 2026-08-18 — it mis-reads a DELIBERATE class exclusion as a defect.** `check-env-coverage.sh:95` skips `validate.sh` in the SAME case-arm as `lint.sh`, `check-*`, `test-*` and the e2e/jumpbox/walk harnesses, and its header states the reason verbatim: *"Still excluded (genuinely not operator flow …): the e2e/test harnesses, the jump-box/bootstrap harnesses, the lab-walk harness, and **the CI gates themselves**"*. `validate.sh` IS a CI gate. So `.env.example` is the WRONG HOME twice over — the exclusion is by design, AND (the row's own order trap) with no `load_env` call the `${…:-1.31.0}` default still wins, making the entry documentation with no effect. ⇒ **Do NOT add it to `.env.example`.** 🔬 **WHAT IS ACTUALLY TRUE, MEASURED 2026-08-18 — and it is a LATENT hazard with ZERO current consequence, which is a different row from the one written here.** The literal pins manifest validation to schema **1.31.0** while the repo's kubectl pin is **`KUBECTL_VERSION=v1.36.3`** (`.env.example:1657`) — a **5-minor gap**, tracked by nothing (`grep -c KUBERNETES_VERSION renovate.json` → **0**). The fake-green MECHANISM is real and measured: a genuine post-1.31 core API (`resource.k8s.io/v1 DeviceClass`) under the `-ignore-missing-schemas` this gate uses is `Skipped: 1, rc=0` at 1.31.0 and `Valid: 1` at 1.36.0 — i.e. a newer core kind would be silently unvalidated. **BUT the consequence for THIS repo today is ZERO:** running the real `make validate` at both versions gives **identical** totals — **32 validated, 10 skipped(NO SCHEMA)** either way; the 10 skips are Tekton CRDs, not core kinds, and every core resource here resolves in both schema sets. ⚠️ **My first attempt to measure that returned `Valid=0 Skipped=0` for BOTH versions** — a zero denominator, an instrument that discriminates nothing, and it would have read as "no difference" for the right-sounding wrong reason. The numbers above come from the fixed extractor. **AND THE GATE ALREADY SHOUTS ABOUT SKIPS** — `validate.sh` prints *"a skipped resource is UNVERIFIED, not clean"* plus an explicit warning that `KUBECONFORM_REQUIRE_SCHEMAS=1` does **not** cover this path, so a skip is not silent today. ⇒ **VERDICT: record, do not build.** Aligning `KUBERNETES_VERSION` with the kubectl pin is the only fix shape that is not refuted, but it is a NEW CONTROL on a gate (Rule Zero: it needs its own idea round), and with a measured consequence of zero it does not earn one yet. **Re-open when** a manifest here starts using a core API newer than the pinned schema set — the observable is the `skipped (NO SCHEMA)` count DIVERGING between the two versions, which is a two-command check. ⚠️ **THE 'LIVE CONSEQUENCE' BELOW IS REFUTED BY MEASUREMENT 2026-08-18** (B151's second idea round): the inner kubeconform `-cache` is **version-discriminating** — measured, same cache dir, `-kubernetes-version 1.31.0` then `1.30.0`, entries went **1 → 2** — so a version bump **MISSES and refetches** and CANNOT serve a 1.30 schema for a 1.31 request. There is no stale-schema correctness bug. Adding `KUBERNETES_VERSION` to the `actions/cache` key therefore buys **ZERO** correctness and **costs a full re-fetch of the whole set on every bump** — a pessimization. ⇒ **Do NOT do step (2).** The row's FIRST half still stands on its own terms (the literal is outside `check-env-coverage`, `check-toolchain-alignment` and Renovate, and `validate.sh` never calls `load_env`), and so does its ORDER trap — adding the `.env.example` line ALONE does nothing, because without a `load_env` call the `${…:-1.31.0}` default still wins and the entry is documentation with no effect. Original claim follows, kept so nobody re-derives it. — **AND IT HAS A LIVE CONSEQUENCE ALREADY, separate from the tidiness argument:** the CI kubeconform schema cache is keyed on `hashFiles('k8s/**','deploy/**')` (PR #757 drops a third pattern that matched nothing) and **does NOT cover this version** — so bumping the k8s version reuses schemas fetched for the OLD version until some manifest happens to change too. That is a stale-cache CORRECTNESS bug, not a performance one. **THE FIX IS TWO FILES AND ORDER MATTERS:** adding `KUBERNETES_VERSION` to `.env.example` **alone does nothing** — without a `load_env` call the `${…:-1.31.0}` default still wins and the entry is documentation with no effect, which is the failure mode that *looks* fixed. So: (1) add the `load_env` call (checking it does not clobber a caller's per-run override — this repo's documented `.env.example` clobber class applies, and a var read with a fallback must be left COMMENTED there); (2) then add the file to the cache key. **DO NOT** close this by adding the `.env.example` line on its own. ⚠️ Also note `validate.sh` now carries a second reason to touch it carefully: its zero-examined guard cannot see an ALL-SKIPPED directory (`total` folds `skipped`), disclosed at that guard and pinned by `test-validate-counts.sh` case 4 — closing THAT changes pass/fail and wants this row's `-skip <GVK>` groundwork first, or every dir of uncatalogued CRDs reds at once. | Found by the B151 round (finding #5); the cache-key consequence and the load_env-ordering trap are mine. ⚠️ **RE-VERIFIED 2026-08-18 — the first half STANDS and THE IMPLIED FIX IS WRONG.** Re-measured: 3 sites, all in `validate.sh` (:14, :85, :89); 0 hits in `.env.example`; 0 `load_env` calls in that file. But `check-env-coverage.sh:95` EXCLUDES `validate.sh` **explicitly**, in the same list as `check-*`, `test-*` and `lint.sh` — so this is not an accidental blind spot to close by adding an `.env.example` line; it is a deliberate boundary between GATE scripts and OPERATOR-FLOW scripts. Routing the pin through `.env.example` + `load_env` would make a GATE read operator config, i.e. a gate whose result depends on the box it runs on — the wrong direction. Combined with the row's own ORDER TRAP (the `.env.example` line alone does nothing without a `load_env` call), BOTH halves of the obvious fix are wrong for the same reason. **The real question, which needs its own idea round:** where does a GATE's version pin live so it is Renovate-visible and alignment-checkable WITHOUT being operator-overridable? Candidate: pin it where the other toolchain versions live and have `check-toolchain-alignment` assert `validate.sh` agrees — leaving `.env.example` out entirely. |
| **B170** | 🟢 **LOW (downgraded 2026-08-18 — the sharp residual is REFUTED; see the measurement inline)** — `secrets/*.kubeconfig` ACCUMULATES one file per destroyed guest cluster, and they all name the SAME address — so a stale one is a credential for an endpoint that now belongs to something else. **MEASURED 2026-08-17:** 12 files present; `cicd-gc0816023508`, `cicd-gc0817163305` and `cicd-gc1` — three DIFFERENT clusters, all destroyed — every one carries `server: https://192.168.101.132:6443`, because the Supervisor reuses the guest VIP. `.env` meanwhile still pins `KUBECONFIG=./secrets/cicd-gc1.kubeconfig`, a cluster gone ~11 re-cuts ago. This is the stale-trust-anchor class from `patterns.md` (a CA trusted for an address that no longer belongs to what minted it), with one MITIGATION that is why this is MEDIUM and not HIGH: **it fails LOUDLY** — each guest cluster mints its own CA and client cert, so a stale file against a live re-used address gives x509/401, which task #124 measured directly (`creds-show` and `env-validate` BOTH called it correctly). The sharp residual is a NAME collision: if a future cut is named `cicd-gc1` again, `.env` silently points at a stale kubeconfig for a LIVE address with a different CA, and the x509 error then blames TRUST rather than STALENESS. **Not yet designed:** pruning is legitimate under the teardown rule (we created these), but only for a cluster PROVABLY gone — a blanket `rm secrets/*.kubeconfig` is the blanket-delete this repo forbids for `harbor-robot.env`/`gitea-ci-token`/`webhook-token`, and the same reasoning applies here. Needs an idea round: what proves a cluster is gone, and does the lab-cut flow already repoint `.env` (it `state_set`s `KUBECONFIG`) making the stale FILES the only real issue? **MEASURED 2026-08-18 — the SHARP RESIDUAL IS REFUTED for the walked path, and both open questions are answered.** (a) ⚠️ **CORRECTED same day — my first version of this said the name CAN collide because `walk-matrix.sh:102-103` uses FIXED names `cicd-gc1`/`cicd-gc2`. That is FALSE: those two lines are COMMENT PROSE** describing the controlled experiment that JUSTIFIES the rotation, not code. The code is `:111` — `WALK_CLUSTER_NAME="${WALK_CLUSTER_NAME:-cicd-gc$(date +%m%d%H%M%S)}"` — **one fresh timestamped name per invocation**, so a collision needs an explicit `WALK_CLUSTER_NAME` override. I matched a string and reported it as code (`agents.md`: a grep that FINDS the string can still be about a DIFFERENT KIND OF THING). And the reason for the rotation is a SEPARATE, sharper hazard than this row's: measured on VCF 9.1, the platform writes `Cluster.spec.controlPlaneEndpoint` ~4s BEFORE the VirtualMachineService that owns the control-plane LB exists, so the value is a PREDICTION; on a reused name it predicts the PREVIOUS incarnation's LB IP and CAPI never revisits it — controlled experiment, same lab same minute: reused `cicd-gc1` advertised .138 vs service .140 -> 53 min, CP 0/1, NEVER converged, while a never-used `cicd-gc2` matched .142/.142 -> Available in ~11 min. (b) But `26-vks-cluster-status.sh:51-52` OVERWRITES the file unconditionally — `get secret <name>-kubeconfig -o jsonpath='{.data.value}' | base64 -d > "$KC"` — a fresh decode of the NEW cluster's OWN Secret, with `[ -s "$KC" ] || return 1` (:53) catching the zero-byte truncation that `>` creates before the pipeline runs. (c) `27-use-guest-kubeconfig.sh` never mints; it REFUSES unless the file is non-empty and reads the context FROM it. (d) Ordering holds in the document: scenario-1 runs `make vks-cluster-status` at :457/:458 and `make use-guest-kubeconfig` at :486. So a stale CA cannot survive onto a live reused address in the walk — the file is replaced ~29 lines BEFORE it is published. **Q2 answered NO-then-YES:** `state_set KUBECONFIG` exists ONLY in `05-kind-up.sh:104` (the KinD path), so the guest path does not use the overlay — but `27-` writes all THREE selector keys to `./.env` directly and DELIBERATELY (its header records why the `state_set` design was refuted: `load_env` snapshots selectors and would silently overwrite it). **Q1 still open** — what proves a cluster is gone, for pruning. ⇒ DOWNGRADED to 🟢 LOW/cosmetic: the 12 files are disk accumulation, and the stale `.env` pin fails LOUDLY (task #124 measured exactly that: 401/x509, both gates correct). No pre-matrix action. |
| **B171** | ✅ **CLOSED 2026-08-17 by measurement (#828 + a forced-update run).** The literal is aligned (`scripts/e2e-cross-cluster.sh:28` now defaults to the repo pin) and the root cause — a DEAD renovate annotation that made that site invisible to the bot — is fixed by a 5th customManager over `scripts/*.sh`. The grouping residual is settled EMPIRICALLY: `LOG_LEVEL=debug make renovate-validate` extracts THREE sites under one `depName: argoproj/argo-cd` (both `.env.example` keys + the script), and because all three already sit at the latest version that run computes NO branch — so it cannot answer grouping and its silence is not evidence. Forcing the question in a throwaway worktree (all three pinned back to v3.4.5, main untouched) yields `renovate/argoproj-argo-cd-3.x` **3 times** against one occurrence each of `renovate/mise-tools` and `renovate/renovate-44.x`: three upgrades, ONE branch, one PR. The version-locked-train split does not apply. No alignment gate was added — option (b) in the original row — because the bot now moves all three as a unit, so the gate would guard a class that can no longer occur. ORIGINAL: **MEDIUM — the cross-cluster e2e validates the two-cluster mechanic against an ArgoCD the repo does NOT ship, and nothing asserts they agree.** MEASURED 2026-08-17 (mine, after a round flagged it — I re-derived every line rather than take it on trust): `scripts/e2e-cross-cluster.sh:28` is `ARGOCD_MANIFEST_VERSION="${ARGOCD_VERSION:-v3.4.5}"`; that script calls `load_env` **zero** times; the `e2e-kind-cross-cluster` Makefile target does not export it; so `bash -c 'echo ${ARGOCD_VERSION:-v3.4.5}'` in a clean shell yields **v3.4.5**. Meanwhile `.env.example:1658` declares `ARGOCD_VERSION=v3.5.1` **uncommented**, and `scripts/07-install-argocd.sh:36` makes it **mandatory** (`:?`), so the real install path runs **3.5.1**. Two ArgoCD versions in one repo, from the same upstream manifest, a **minor** apart — and `grep ARGOCD_VERSION scripts/check-*.sh` returns **nothing**, so no alignment gate exists (contrast `check-image-alignment`, which exists precisely for this class). **Why it is not merely untidy:** the cross-cluster registration mechanic — the one thing that e2e exists to prove for the real Supervisor→guest topology — is being proven against a binary we do not deploy, and argo-cd moved the relevant `state.go` internals between these two minors (v3.5.1 lines 653-655/1042-1046 are v3.4.5 lines 581/978-980), which is how the version drift was noticed at all. **Fix options, neither yet rounded:** (a) have the script take the repo pin — safe on the port axis here, MEASURED: `pick_port` and `SKIP_DOTENV` both appear **0** times, so the `.env.example`-clobber trap that forbids `load_env` in ephemeral-port harnesses does not apply; or (b) align the literal and add a gate asserting the script's default equals `.env.example`'s value. (b) introduces a new control, so it needs its own idea round per RULE ZERO. |
| **B172** | ✅ **CLOSED 2026-08-18 — all three sites accounted for, verified against the SHIPPED code on main.** The row said "0 of 3 sites fixed; one of them is MERGED and live"; that is now stale. **Site 1, the kubectl path** (`_await_fetch_state`) — fixed in #832 and hardened in #834: classification is on `sync.status` with a POSITIVE `Synced|OutOfSync` enum (`lib/argocd.sh:614,617`), the message prefix only NAMES the cause, and the repo prefix is fatal regardless of sync.status as forward-defence across an ARGOCD_VERSION bump. **Site 3, the api/TENANT path** (`argocd_app_fetch_verdict`) — fixed in #834 (`:905,908,916`). It carried the SAME defect one level worse, because its own `jq -r '.status.sync.status // ""'` MANUFACTURES the empty string: `{"status":{}}` classified `ok` and the caller logged `"<app>: synced (argocd-server)"` for an Application nothing had compared, on the branch that has never executed in any recorded walk run. **Site 2** (`e2e-cross-cluster.sh`) — re-measured: **0** occurrences of `ComparisonError`; the row's own "closed refuted" stands. **The asymmetry this row exists for is preserved in both fixed sites:** the ABSENCE of a ComparisonError is a sound repo-SUCCESS signal; its PRESENCE is NOT a sound repo-FAILURE signal, because v3.5.1 appends one at 749/1053/1060 without setting `failedToLoadObjs`, and :1035 is the only app-level forcing — so an Application can be Synced/Healthy while carrying one. Evidence: `test-argocd-await-revision.sh` 34 → 49 and `test-argocd-app-fetch.sh` 12 → 15, both 0 failed, every group RED-proven by reverting its own defect. B177 (the cross-mechanism divergence this row spawned) is CLOSED too. | — |
| **B173** | 🟢 **CLOSED 2026-08-18 BY VERIFICATION — mechanism-checked in live code, not just call-swapped, and the row's OWN prescription was deliberately NOT taken.** `70-configure-argocd.sh` no longer calls `argocd app wait --sync` anywhere: the api/tenant path now calls `argocd_app_fetch_verdict` (`lib/argocd.sh:863`), which issues `argocd app get "$app" --refresh -o json` and reads `.status.conditions[] | select(.type=="ComparisonError")` — i.e. it demands evidence of a FETCH, which is exactly the property `--sync` alone cannot prove. Grepped: zero live `app wait --sync` call sites remain (the only hits are comments recording its removal). ⚠️ **DO NOT "fix" the `--refresh` to `--hard-refresh`, which is what this row's prescription literally asked for.** A separate round measured (against argo-cd source) that a FORCED refresh DISABLES the repo-error short-circuit, so `reconciledAt` advances even on an unreachable repo — the soft refresh is the deliberate, measured choice (#817, RED-proven 12/0 vs 11/1). The row's prescription is superseded by that measurement; leaving it unmarked is how it gets re-applied. Original follows. — 🟡 **RE-SPECIFIED 2026-08-17 BY ITS IDEA ROUND — the MECHANISM is confirmed 3/3, but BOTH consequences this row led with are MEASURABLY FALSE, and the severity drops HIGH → MEDIUM.** 🔴 **HIGH — the api/TENANT path has NO freshness gate at all, while the admin path has a rigorous one — so a stale `Synced` reports green in under a second with ZERO repo contact.** Found by the same round, outside its brief and (its words) *"worth more than the message wording"*. `70-configure-argocd.sh:791` runs `argocd app wait "$app" --sync` with **no prior `refresh=hard`**, and with `--sync` alone the CLI's predicate reduces to `sync.status == Synced` (`cmd/argocd/commands/app.go:2470-2503` @ v3.0.19 — `healthCheckPassed` initialises **true** and no case matches; `operational := !watch.operation` is true). Both apply paths are **UPSERT**, so a `Synced` state **PERSISTS across runs** — which is exactly why `argocd_await_revision` annotates `refresh=hard` and demands `reconciledAt` ADVANCE. Consequence: an Application synced yesterday against a Gitea that is **down today** returns in <1s and the run logs `"synced (argocd-server)"` having made **no repo contact whatsoever** — precisely the contract `.env.example` says this gate exists to enforce (*"a wrong URL used to sync green forever"*). The branch then `exit 0`s at `:802`, **bypassing `argocd_await_revision` entirely**. So the path that will ACTUALLY run on the lab is the unguarded one. **Fix:** give the api path the same conjunction the kubectl path has — hard-refresh first (`argocd app get --hard-refresh`, or the annotate where kubectl may patch), then require the observed reconcile to be **NEW**. ⚠️ **Not RED-provable against a recorded run:** `grep -rc 'verifying via argocd-server'` over all 256 `/tmp/walk` files returns **0** — the branch has never executed, because scenario-2 dies ~500 lines earlier at the mechanism gate (`STALE_CA`). It goes live the moment the nested-vsphere-lab `walk-matrix.sh` `ARGOCD_SERVER` fix lands, and there will be no prior run to compare against. ⚠️ **WHAT THE ROUND REFUTED, from argo-cd source at BOTH the pinned server versions (v3.5.1 and the lab's v2.14.3):** (1) *"a wrong URL used to sync green forever"* **cannot happen on this path** — a changed `repoURL` makes `needRefreshAppStatus` take the `!currentSourceEqualsSyncedSource(app)` branch and select `CompareWithLatestForceResolve` (Level 3), which sets `noRevisionCache=true`, which is exactly the flag that DISABLES the repo-error short-circuit (`time.Since(firstSeen) <= repoErrorGracePeriod && !noRevisionCache`). The fetch error falls through to `failedToLoadObjs` → `Unknown` + a `ComparisonError`, and `argocd app wait --sync` **blocks and correctly fails**. The gate this row says is missing is, for its own headline case, already enforced by the controller. (2) *"synced yesterday, Gitea down today returns in <1s"* — `defaultRepoErrorGracePeriod` = `defaultAppResyncPeriod + jitter` = 120+60 = **180s**; once it expires the periodic Level-2 refresh stops short-circuiting and persists `Unknown`. A day-old outage is `Unknown`, so the wait blocks and fails. Correct RED, just slow. (3) *"the path that will ACTUALLY run on the lab is the unguarded one"* — **false, and the row conflated two independent gates**: MECH selection keys on `create`/`patch` (`:232-246`) while the api-WAIT branch keys on `auth can-i **get** == no` (`:781`). MEASURED from a real lab log (`run-20260817T203305Z-314064/MATRIX-row5-photon.log:2606`): the scenario-2 tenant took **api write + kubectl verify**, i.e. it got the RIGOROUS gate, and that gate correctly failed at 180s. **WHAT SURVIVES — the real gap, and it is narrow:** a spec-IDENTICAL re-run within ~180s of the FIRST repo error the controller saw (`repoErrorCache` is an in-memory per-app `sync.Map`). Then `appcontroller.go:1927-1930` does `if errors.Is(err, ErrCompareStateRepo) { return processNext }` — **nothing is persisted** — so `sync.status` stays `Synced`, `reconciledAt` stays frozen, the wait returns <1s and `:803` exits 0. `make gitops` runs squarely inside that window. **THE PRESCRIBED FIX IS ONE CALL, and it is SIMPLER than what it replaces** — not the `--hard-refresh` + reconciledAt conjunction this row proposed: `timeout "$ARGOCD_REPO_TIMEOUT_SECONDS" argocd app get "$app" --refresh -o json`, then assert (a) no condition whose message begins `Failed to load target state:` (colon-space included) and (b) `.status.sync.status != Unknown`. Why each part: **`--refresh`, NOT `--hard-refresh`** — ANY refresh selects Level 3, so soft already kills the short-circuit; hard additionally issues a per-app `GetAppDetails{NoCache:true}` whose failure is only `log.Warnf`-ed server-side, so it costs more and proves nothing extra. **No `reconciledAt` at all** — `Get` with `Refresh` BLOCKS until the controller clears the annotation, and that annotation is deleted in the SAME patch that persists the new status (`persistReconciliationStatus`), so the returned JSON is structurally post-reconcile; there is nothing to poll and no pre-reading to compare. **`timeout` is mandatory** — `argocd app get` has NO `--timeout` flag (v3.0.19 offers only `--output/--show-operation/--refresh/--hard-refresh`) and the server loop exits only on `ctx.Done()`, so a missing controller HANGS it. **Message PREFIX, not condition type** — only the repo arm emits `"Failed to load target state: "`; matching the bare `ComparisonError` type reproduces the B137/B172 class. ⚠️ **MY Q2 WORRY WAS INVERTED:** `argocd app get --refresh` enforces `applications, get` ONLY (no `action/`, no `override`, no `update`), and `RefreshApp` patches the annotation using **argocd-server's own ServiceAccount** — so it needs ZERO tenant Kubernetes RBAC and works precisely where `argocd_await_revision`'s `ka annotate` is Forbidden. It is the STRONGEST primitive a locked-down tenant has, not a permission risk. ⚠️ **AND THIS ROW'S 'not RED-provable' CLAIM IS FALSE:** the current defect is RED-provable offline TODAY — stub `argocd app wait` to return 0 instantly and assert the present code exits 0. Extraction into `scripts/lib/argocd.sh` is required first (the logic is inline at `:783-803`, so nothing can drive it), which is the precedent `lib/argocd.sh:490-493` already states verbatim. **BONUS DEFECT, outside the brief:** a tenant with `get=yes, create=no, patch=no` selects `MECH=api`, then falls PAST `:781` (because `get=yes`) into `argocd_await_revision`, which `ka annotate`s and dies *"refusing to report a reachability result we did not obtain"*. The comment at `:228-231` describes exactly this failure for the create-path and fixes it only there. **CORRECTION to this row's own evidence:** `/tmp/walk` holds **158** `.log` files, not the 256 I cited twice. |
| **B174** | 🟢 **THE "1 OF 3 KUBECONFIGS" RESIDUAL IS REFUTED BY MEASUREMENT ON A REAL LAB (2026-08-18) — the scope was 1 of 1, and it is done.** The round prescribed a `harden_secret_file` sibling because *"three kubeconfigs land in `secrets/` and NOTHING sets any of their FILE modes"*, with the `vcf` CLI's actual mode graded UNVERIFIED pending a lab. Measured directly on this box's real `secrets/`, which 15 lab cuts have written: **every single `.kubeconfig` is already `600`** — `supervisor.kubeconfig` (600), `argocd.kubeconfig` (600) and all 13 `cicd-gc*.kubeconfig` (600) — while the CAs are `644` (`harbor-ca.crt`, `supervisor-ca.crt`, `vmca-root.pem`, `ca-lab-gc1.pem`), which is correct and deliberate: a CA is public trust material and a 0600 one is unreadable to a container uid. The directory itself is `700`. ⇒ **The `vcf` CLI writes 600 on its own**, so the two code-written kubeconfigs were never exposed, and the ONLY one at risk was `vks.kubeconfig` — the file the OPERATOR copies by hand in scenario-2 — which is exactly what shipped. **Do not build `harden_secret_file`**: it would iterate three sites to enforce a mode two of them already have, and the third is a doc instruction no code path touches. The `env-check` mode-check alternative is likewise unnecessary for the same reason. ⚠️ **What this does NOT refute:** the doc fix remains un-provable by the walk (the harness pre-hardens under `umask 077`), and **B191** is untouched — `.jumpbox/kubeconfig` is a different directory, outside `secrets/`, measured at 644/664. ⏸️ Original follows. — 🟡 **THE DOC GAP IS SHIPPED 2026-08-18 — but the row's framing of it was wrong TWICE, and its scope is 1 of 3. DO NOT read this as closed.** An idea round returned **ship-with-changes** and refuted both halves of the premise. **(1) The walker does NOT execute that block** — `walk-doc.sh:637`'s `has_live_placeholder` fires on the unsubstituted `<path-to-the-kubeconfig-you-were-given>` and the step is recorded SKIPPED (verified: the fence count is unchanged at 23 and the placeholder is still present after the edit, so it still skips). The row said the walker executes it. **(2) And that is NOT why it was never caught.** The reason is worse: `nested-vsphere-lab/scripts/walk-matrix.sh:483` seeds `secrets/vks.kubeconfig` under **`umask 077`**, yielding dir **700** / file **600** — precisely what the fix prescribes. So the document could say `chmod 000` and rows 5/6 would still be GREEN, and the `head -1 ./secrets/vks.kubeconfig` **Expect** at `:97` passes **because of the harness**, not the `cp`. This is *"would this still pass if I deleted the feature?"* with the answer **yes**. ⇒ **This fix is UN-PROVABLE by the walk. Nothing here is matrix-proven, and no run can ever RED on it.** 🔴 **THE ACTUAL EXPOSURE, measured:** at umask 002 a 644 source `cp`s to a **644 file in a 775 dir**, and **nothing in the repo EVER chmods a kubeconfig** — a full `chmod|umask` sweep of `scripts/` shows explicit modes for `.env`/`.env.bak`, mktemps, tokens and CAs (deliberately 0644) and **zero** for any kubeconfig. The DIRECTORY self-heals later (`50-seed-gitea-repos.sh:159` during `make platform`); the FILE never does. The round also **refuted its own hypothesis that a tool would warn**: two-arm control on the pinned helm **4.2.3**, output byte-identical at 644 and 600 — **no warning**. So a 644 cluster-admin credential is silent forever, which removes any *'we would notice'* argument and strengthens the case for a code-side fix. ⚠️ **Reach is narrower than the mode suggests on THIS box** (`/home/andriy` is 0750 `other::---`, so generic `other` cannot traverse; the reachable principal is `libvirt-qemu` via its ACL) — it is fully real on the SHARED jump box the runbooks describe. 🟢 **SHIPPED:** `docs/scenario-2.md:87-90` now hardens the directory **before** the copy and the file after it, with prose saying why. **The ordering is load-bearing and now documented:** the 700 directory is what closes the window between `cp` and `chmod`; move either line and the window reopens. (`install -m 600` closes it atomically and matches `jumpbox-launch.sh:115`'s idiom, but `install` on bare Photon/toybox is UNVERIFIED — this row has already shipped two fixes that were silent no-ops there — so `cp` + `chmod` stays.) **`chmod 600` is safe HERE and the round proved it**: `secrets/` is excluded from the jumpbox tar (`jumpbox-run.sh:34`) and from `test-airgap-toolchain.sh:85`, the only thing copied into a container's `secrets/` is `harbor-ca.crt` at explicit 0644, and **no container consumer reads `vks.kubeconfig`**. The recorded 0600-unreadable incident was about a **CA** — public trust material — a different class; that concern lands on **B191**, not here. **Gate blast radius measured ZERO** with the real walker on both fixtures: `blocks: 2 extracted, 2 counted independently`, same SKIP reason, `1 Expect: … 1 parsed, 0 UNMET`; `check-expect-literals` 56/9/0 MISSING and `check-doc-command-count` OK — 2 claims, both unchanged after the edit. Trap recorded: `check-doc-command-count` fires on a number-word + *'command(s)'* + a `make` invocation, so keep new prose clear of that shape (verified 0 in the added paragraph). ⚠️ **STILL OPEN — the scope is 1 of 3, and the doc was the WEAKEST lever.** THREE kubeconfigs land in `secrets/` — `vks.kubeconfig` (43 refs), `supervisor.kubeconfig` (38), `argocd.kubeconfig` (20) — `ensure_secret_dir` fixes the DIRECTORY for all three and **nothing sets any of their FILE modes**; the latter two are written by the `vcf` CLI via `KUBECONFIG=…` (`30-vks-login.sh:379`, `31-fetch-argocd-kubeconfig.sh`) at whatever mode it chooses, which is **UNVERIFIED and needs one `stat -c %a ./secrets/*.kubeconfig` after `make vks-login` on a real lab**. **Prescribed, NOT built:** a `harden_secret_file` sibling to `ensure_secret_dir` in `lib/os.sh` (same containment `case`, `chmod 600`, tolerant of absence) called at the 3 write sites, with `test-secret-dir-mode.sh` as the ready-made pattern — **and/or** a mode check at `env-check`'s existing `[ -f ]` (`02-env.sh`), which is the real chokepoint: `load_env` defaults `KUBECONFIG` to that path (`lib/os.sh:614`) and `env-check` is already on the tenant's path (`scenario-2.md:741`). The doc edit is still worth having because it is the **only** thing covering the reader between `:89` and that check. ⏸️ Original follows. — 🟡 **PARTIALLY FIXED 2026-08-18 — 3 of ~13 creation sites; DO NOT read this as closed. `secrets/` is born 0775 at umask 002, and `secrets/.env.make` is make `-include`d: a group-writable directory holding an included file is make-variable injection.** #770's commit message deferred this to *"its own row"* and **that row was never filed** — found 2026-08-17 by the round it commissioned. MEASURED: a fresh `mkdir -p secrets` gives **775** at umask 002 (this box's umask IS 002), 755 at umask 022; `Makefile:132` does `-include $(if $(wildcard .env),secrets/.env.make)` and `:133` generates it. A writable DIRECTORY lets any group member unlink-and-recreate the included file regardless of the file's own 0600. ⚠️ **On this box `secrets` happens to be 700 — luck, not a guarantee, structurally the SAME defect B135 was re-measured to be.** Group-write is empty here (`getent group andriy` → no other members), but the world-READ chain is live: `/home/andriy` is 0750 with ACL `user:libvirt-qemu:--x`, and BOTH `/home/andriy/projects` and the repo root are **0775** — so the uid the nested-lab VMs run as can traverse to and read any world-readable file in the repo. **FIXED 2026-08-18 — and the row's own prescription was wrong twice, both measured.** (1) It says **two** Makefile recipes; there are **THREE** — `:87`, `:104`, `:134` (enumerated-list rot inside the fix itself). (2) The obvious `mkdir -p -m 700` **DOES NOT WORK**: measured, `-m` is IGNORED when the directory already exists, so an existing 775 dir stays **775** — i.e. the naive fix is a silent no-op on precisely the boxes that have the defect. ⚠️ **AND MY OWN FIRST FIX WAS A SILENT NO-OP ON THE ONE OS THAT MATTERS.** I shipped `install -d -m 700 secrets`; an implementation round refuted it and my own probe confirmed it the same minute: **toybox IGNORES `-m` on the `-d` path**, rc=0, no message. MEASURED on bare `photon:5.0` (toybox 0.8.9 — the repo's PRIMARY air-gap jump-box OS): fresh **775** and existing **775** at umask 002. It works on GNU coreutils 9.4 and uutils 0.8.0, so it passes on every dev box and does nothing on the air-gap box. **Shipped instead: `mkdir -p secrets && chmod 700 secrets`** — measured 700/700 on toybox AND GNU, fresh AND existing. **THE SURFACE IS 3 OF ~13, NOT COMPLETE.** My grep (`\t@mkdir -p secrets`) is blind to `mkdir -p "$(dirname "$OUT")"`, which is how NINE scripts create the dir: `22-harbor-robot.sh:171`, `30-vks-login.sh:35`/`:376`, `31-fetch-argocd-kubeconfig.sh:62`/`:89`, `50-seed-gitea-repos.sh:159`, `fetch-ca.sh:263`, `fetch-supervisor-ca.sh:149`, `lib/tls.sh:71`, `05-kind-up.sh:88` — each at the operator's umask. PLUS **`docs/scenario-2.md:88` is an OPERATOR INSTRUCTION** (`mkdir -p ./secrets`) on a real tenant box, and the next line copies a kubeconfig into it. **AND THE THREE FIXED SITES CANNOT REPAIR THE OTHER TEN:** they are timestamp-gated make rules — measured, a dir regressed to 775 with `.env` UNCHANGED stays **775**, because `secrets/.env.make: .env` only re-runs when the prerequisite is newer. **Real fix (not done):** a `secrets_dir()` helper in `lib/os.sh` doing `mkdir -p "$1" && chmod 700 "$1"`, called by the nine sites, + the doc line. **THREAT MODEL HALF-REFUTED, measured:** `/home/andriy` is 0750 with `other::---`, so generic `other` CANNOT traverse and the row's claimed "live world-READ chain" through the 0775 `projects`/repo root is **REFUTED**; `getent group andriy` has an EMPTY member list, so the unlink+recreate injection has **NO principal** here. It is real for exactly ONE principal — `libvirt-qemu`, which holds ACL `--x` on `/home/andriy` — and there it closes an **ENUMERATION** leak, not a credential leak (every credential is already 0600). Honest framing: defence-in-depth for the SHARED jump box the runbooks describe, not a live local exploit. **Also found:** `secrets/vmca/` is **775** (a child dir, masked only by the 700 parent); and `Makefile:48` carries a STALE fact — it asserts photon ships no coreutils and `00-install-prereqs.sh` does not install it, but `:88` DOES. **COMMITTED RED-PROOF** (not a hand-run one): `test-env-precedence.sh` case 9b, 14 -> **15 passed, 0 failed**, and RED-proven — reverting to bare `mkdir -p` gives `14 passed, 1 failed` with the mode named. It pins the mode AND the timestamp-gating in one case. **Both of my earlier declines were independently verified CORRECT:** `jumpbox-run.sh:92` runs after `cd "$WORK"` INSIDE the ephemeral container, and `test-env-precedence.sh:124`/`:43` are fixtures inside a `mktemp -d`. Also verified: NO bind-mount of `secrets/` into any container exists, and the live dir is ALREADY 700 with everything working — so 700 empirically breaks nothing. **Residuals, unmeasured:** SELinux context differences under Enforcing; ownership under a sudo-invoked make; whether any field Photon box genuinely lacks coreutils (`03-check-tools.sh`'s TOOLS list contains NO coreutils-only binary, so it cannot detect that); and whether the nested-vsphere-lab walk harness reads host `secrets/` as another uid. **RED-proven both directions in a worktree** (never the real tree — see the trap below): fix at `umask 002` -> **700**; the old `mkdir -p` form at the same umask -> **775**; `.env.make` itself 600 via the recipe's own `umask 077`. Gates: `test-env-precedence` 14/0, `static-check-fast` rc=0. ⚠️ **THE ROW'S OWN RED RECIPE IS A TRAP — do NOT run it in the repo.** `rm -rf secrets` would delete `harbor-robot.env`, `gitea-ci-token` and `webhook-token`, the three files this repo explicitly forbids blanket-deleting; all three are PRESENT in the real tree. RED-prove in a git worktree, which has no `secrets/`. **Residual, NOT fixed:** `scripts/jumpbox-run.sh:92` also `mkdir -p secrets` — but that runs INSIDE the throwaway jumpbox container to place a deliberately world-readable CA (`chmod 0644`, public trust material), so the directory mode there is not the same hazard; and `scripts/test-env-precedence.sh:124` GENERATES a mini-Makefile carrying the old form, which is a fixture for env precedence, not for permissions. **RED:** `rm -rf secrets && umask 002 && make secrets/.env.make && stat -c %a secrets` must print 700. (The exploit is `inferred` — the round could not become libvirt-qemu.) ⚠️ **RE-COUNTED 2026-08-18 — "~13 creation sites" CONFLATES TEST FIXTURES WITH REAL ONES, and the remaining surface is ONE.** Measured: 12 `mkdir …secrets` sites, 3 carrying `chmod 700`. Split by what they are — **3** Makefile:93/:110/:140 (operator-facing) **FIXED**; **1** `docs/scenario-2.md:88` `mkdir -p ./secrets`, run BY THE READER at the default umask — **THE REAL GAP**; **1** `scripts/jumpbox-run.sh:92`, which creates it INSIDE the container and then `chmod 0644` the CA — **DELIBERATE and correct**, a CA is PUBLIC trust material and a 0600 one is unreadable to the container user (the uid-1000-vs-1001 rule); **7** `scripts/test-*.sh` `mktemp -d` fixtures — **NOT the defect**, they are throwaway dirs, not the operator's `secrets/`. So an operator who follows the DOC ends up with a WEAKER directory than one who runs a make target, and that asymmetry is the whole remaining item — a one-line change (`&& chmod 700 ./secrets`), but in a fenced block the walker EXECUTES, so it lands with the same care as any other walked step. 🔴 **THIS ROW'S OWN "Real fix (not done)" IS A HAZARD AS WRITTEN — do NOT build `secrets_dir()` and call it at the nine sites until this is resolved.** The prescription is *"a `secrets_dir()` helper in `lib/os.sh` doing `mkdir -p "$1" && chmod 700 "$1"`, called by the nine sites"*. But **every one of those nine sites passes `$(dirname "$SOME_PATH")`, and four of those paths are OPERATOR-CONTROLLED** — so the helper would `chmod 700` a directory the operator chose, anywhere on their filesystem, outside this repo. Read against the code 2026-08-18: `22-harbor-robot.sh:30` takes `${HARBOR_ROBOT_OUT:-secrets/harbor-robot.env}`; `31-fetch-argocd-kubeconfig.sh:43` takes `${ARGOCD_KUBECONFIG:-${REPO_ROOT}/secrets/argocd.kubeconfig}`; `30-vks-login.sh:375` takes `${VKS_SUPERVISOR_KUBECONFIG:-…}`; and the sharpest one, **`30-vks-login.sh:35`, takes `$(dirname "$KUBECONFIG")` — `KUBECONFIG` being THE selector**, documented in `.env.example`, snapshot-protected by `load_env`, and explicitly overridable per run. ⇒ **`make vks-login KUBECONFIG=~/.kube/config` would `chmod 700 ~/.kube`**, and a path like `/etc/…` would be worse. That is a destructive surprise on the operator's own machine, from a change whose entire purpose is hardening. ⚠️ **Not all nine are exposed, and the difference is instructive:** `05-kind-up.sh:35` is `KUBECONFIG_PATH="${REPO_ROOT}/secrets/kind.kubeconfig"` — **hardcoded**, and its `:24` comment records that it *used to* be `${KUBECONFIG:?}` and was deliberately changed, i.e. the "a flow must WRITE ITS OUTPUT to a path IT OWNS" rule was already applied there. That is the shape the other four want. **Candidate fixes, none built, all needing an idea round because this is a NEW CONTROL on a destructive verb:** (a) chmod only when the resolved dir is under `$REPO_ROOT` (needs `realpath` canonicalisation, or `..` defeats it); (b) chmod only when the helper itself CREATED the dir (test existence before `mkdir`, which also sidesteps the measured `mkdir -p -m` no-op); (c) follow `05-kind-up.sh` and make each site own its output path, leaving the operator's selector as an INPUT only. (c) is the smallest conceptually and the largest in diff. ⚠️ **Do NOT resolve this by widening the helper to "chmod whatever it is handed"** — that is the version this note exists to stop. 🟢 **CANDIDATE (a) SHIPPED 2026-08-18 (PR #868, `42cb68e`) — containment-scoped, 8 sites, 11 RED-proven cases.** `ensure_secret_dir()` in `lib/os.sh` creates the dir, resolves it, and `chmod 700`s it **only** when the resolved path is `${REPO_ROOT}/secrets/` or `${REPO_ROOT}/*/secrets/`. Everything else is created and left alone. **The 4 NON-conversions are the point, not an omission:** `shell-init.sh:45` (`$HOME`), `os.sh:973` (`REPO_ROOT`), `30-vks-login.sh:35` (`~/.kube` via the operator's `KUBECONFIG`) and `lib/tls.sh:71` (a `mktemp` dir) are all directories we do not own, and converting them would make `$HOME`'s mode depend on a string predicate. `test-secret-dir-mode.sh` is therefore mostly SKIP cases; loosening the predicate to `*)` fails **4 of them** (measured), which is what stops a future "simplification". ⚠️ **The round prescribed `realpath -m` for the resolution and it was REJECTED** — `31-fetch-argocd-kubeconfig.sh:59` already records that toybox's `realpath` is not guaranteed on Photon, and this row has now shipped **two** fixes that were silent no-ops there. `cd … && pwd -P` is the repo idiom and it is asserted by a grep in the test, because the cases themselves run on a dev box and cannot catch that class. ⚠️ **And the portability guard shipped as a `grep -q` PIPE** — `grep -A25 … | grep -q realpath`, whose SIGPIPE direction turns "someone reintroduced realpath" into a CLEAN pass. `check-grep-q-pipe` caught it ~20 minutes later; fixed to a herestring in `d0daa88`. A guard written to prevent one fake-green committed another. **STILL OPEN:** the remaining creation sites outside `secrets/` are untouched by design, and B135's structural question is unchanged. |
| **B178** | ✅ **CLOSED 2026-08-20 — FIXED BY THE OPERATOR AND VERIFIED END TO END. The agent's part was to PROVE the outcome first, which it did on a COPY, then hand over the command.** Verification, all read-only against the operator's own store: the stored `nested-vsphere-lab` fingerprint now EQUALS the live VMCA (`30:85:E0:EE:3B:98:D4:D9…`, trust `C,,`); `curl --cacert` with it returns **HTTP 200** where the stale one returned **rc=60**; and Chrome renders **`Welcome to VMware vSphere`** instead of a Privacy error — confirmed BOTH on a COPY of the store under a throwaway `HOME` **and, decisively, in the OPERATOR'S OWN RUNNING CHROME** after a restart (the restart matters: Chrome caches trust decisions, so a freshly-added NSS entry is not picked up by a running instance). **The proof-before-the-ask, measured:** three throwaway HOMEs — EMPTY store → `ERR_CERT_AUTHORITY_INVALID`; a COPY of the operator's store (stale CA) → `ERR_CERT_AUTHORITY_INVALID`, i.e. the store's CONTENT is the cause; that same copy with the anchor swapped → the UI renders. 🔑 **THREE FACTS WORTH KEEPING.** **(1) There is NO headless workaround, and the question WILL be asked again.** A throwaway `HOME` can VERIFY but can never FIX, because the defect IS the operator's store: Chrome 151's `--help` carries **zero** cert/NSS flags, its binary contains `.pki/nssdb` exactly once, and `$HOME` is the only lever. **(2) The trust flag is `C,,`** — a real CA (`CA:TRUE`) — **NOT** the `P,,` trusted-peer flag that ArgoCD's `CA:FALSE` self-signed leaf requires (B75-a); using the wrong one produces `net::ERR_CERT_INVALID`, which reads like a product defect. **(3) The anchor moves MID-MATRIX, not just per cut** — `vmca-root.pem` mtime `2026-08-20 00:24:52`, exactly when `REBUILD2` finished, because the harness re-runs `trust-vcsa` after its rebuild. A fingerprint published at 22:24 was dead by 00:24. ⚠️ **AND THE TOFU CONFIRMATION THIS ROW USED TO ASK FOR IS NOT AVAILABLE HERE** — vCenter's `/certs/download.zip` is the SAME channel `trust-vcsa` fetches from (circular), and the VCSA deploy logs contain **zero** occurrences of the VMCA thumbprint. Do not carry it forward as an operator chore. **It still recurs on every cut** (a new VMCA with a byte-identical subject), so the remedy is the two `certutil` commands, now with a demonstrated outcome behind them. — 🟡 **RE-CONFIRMED LIVE 2026-08-19 — it is stale RIGHT NOW, on this box, and the check is read-only.** Measured without mutating anything: `certutil -d sql:$HOME/.pki/nssdb -L` lists one entry, `nested-vsphere-lab` (trust `C,,`); exporting it with `-a` and testing it as an anchor gives **rc=60** against the live vCenter, while the root freshly fetched from vCenter's own bundle gives rc=0. Stored fingerprint `C8:AC:D6:5E:C9:D6:D9:BD:A6:84…` vs live `84:80:66:7C:3F:65:3C:9F:09:88…` — **different certificates**, and (as the row says) the subject DN is byte-identical, so nothing looks wrong. ⇒ **an operator opening the vCenter UI in Chrome today gets an error page**, and every `curl --cacert` against the correct anchor succeeds — the exact split this row was filed for, still true. ⚠️ **STILL NOT FIXED, and still deliberately so:** adding or removing a CA in a browser trust store is a security setting that belongs to the operator, not to an agent, and the standing constraint for this session is explicit that the operator's NSS store must not be mutated. The read-only diagnosis above is the whole of what an agent should do here. **Operator remedy, unchanged and printed by `make creds`:** `certutil -D -d sql:$HOME/.pki/nssdb -n nested-vsphere-lab` then re-add the current anchor. ⚠️ **It recurs on EVERY cut** (`make destroy` + `make lab` mints a new VMCA with the same subject), so it now sits in the handoff's post-cut checklist rather than being rediscovered each time. ⏸️ Original follows. — 🟡 **MEDIUM — the lab CA in the OPERATOR'S BROWSER goes stale on every re-cut, and the subject DN is byte-identical so nothing looks wrong.** MEASURED 2026-08-17 by doing what an HTTP probe cannot do — loading the vCenter UI in Chrome after a fresh cut. `curl --cacert <the anchor this cut pinned>` returns **http=200**; Chrome returns an **error page**. The lab CA IS in the NSS store (`certutil -d sql:$HOME/.pki/nssdb -L` -> 1 entry named `nested-vsphere-lab`) — it is simply a DIFFERENT certificate: live `DF:C7:27:71:DD:1A…` notBefore Aug 15 02:30:28 vs stored `B6:46:E9:C9:A6:EC…` notBefore Aug 14 20:03:41, with **subject DN byte-identical** (`CN = CA, DC = vsphere, DC = local, C = US, ST = California, O = vcsa.env1.lab.test, OU = VMware Engineering`) — exactly the discriminator B166 measured and REFUTED across cuts. So the stale entry is indistinguishable from the right one by anything except the fingerprint. **This is B166's class in a THIRD location:** that row covers `secrets/*.crt` on the jump box and names the browser only in passing, via `make creds`'s own note that *"teardown leaves it behind — remove it with certutil -D"*. It is real, it is recurring (every `make destroy` + `make lab`), and it is invisible to every gate — `.gitignore:15` keeps `/secrets/` out of the repo and the NSS DB is not in the repo at all. **NOT FIXED HERE, deliberately:** adding or removing a CA in a browser trust store is a security setting and belongs to the operator, not to an agent. The remedy `make creds` already prints is `certutil -d sql:$HOME/.pki/nssdb -D -n "nested-vsphere-lab"` then re-add from the current anchor. **Done when:** either `make trust-vcsa` detects a stale NSS entry and PRINTS the two-command remedy with both fingerprints (it must not run certutil itself), or `make creds`'s browser note is upgraded from a static caveat to a measured one. **Do NOT close this by having tooling mutate the NSS store.** | — ✅ **RE-MEASURED 2026-08-18 on the NEW cut, and the row's remedy is INCOMPLETE — there is a SECOND CA.** The row is about the *lab* CA going stale in the operator's NSS store. Measured today: Harbor is signed by its **own `CN=Harbor CA`** (leaf `CN=harbor`, SAN `DNS:harbor.env1.lab.test`, notBefore Aug 18 13:16Z — minted by this cut), and that CA is in **no store on the host at all** — not NSS, and not the system OpenSSL store either. So trusting the lab CA does not make Harbor work, and the row's remedy covers one of two anchors. ⚠️ **AND THE ROW'S OWN FRAMING WAS IMPRECISE IN MY RETELLING**: I had been repeating it as "Chrome errors where curl returns 200". Measured: `curl` with **system trust** ALSO fails (`000`, `ssl_verify_result=20` = unable to get local issuer); only `curl -k` returns 200. So this is not browser-specific — the anchor is simply absent everywhere. A **name mismatch** was a live competing hypothesis (the leaf's CN is `harbor`, not the name browsed) and is RULED OUT: the SAN covers the exact name, and the failure code is 20, not 62. **Two-arm headless proof that the stack itself is correct**, run under a throwaway `$HOME` so the operator's trust store was never opened: control (no CA) -> `ERR_CERT_AUTHORITY_INVALID` / `<title>Privacy error</title>`; treatment (CA imported into the throwaway NSS db) -> `<title>Harbor</title>`, the real UI. `curl --cacert ./secrets/harbor-ca.crt` -> 200, verify=0. So endpoint, certificate and the CA we ship are all CORRECT and the sole defect is an untrusted anchor. Importing it into the real stores is an **operator action** and was deliberately not taken. |
| **B177** | ✅ **CLOSED 2026-08-18 — both mechanisms now classify the SAME Application the SAME way.** ORIGINAL: the two write mechanisms disagreed on identical state, selectable with one variable. The row then claimed the belt-and-braces repo arm made them "agree exactly on the repo arm"; **an implementation round MEASURED that FALSE** — same Application, `sync.status=Unknown`, two ComparisonErrors with the repo message SECOND: the kubectl path said `repo` and offered the probe, the api path said `notrepo` and died "do NOT start at Gitea". Cause, confirmed by reading both sites: `argocd_app_fetch_verdict` read its conditions with `jq … join("; ")` — **no LEADING separator** — and tested the prefix **first-position only**, while the sibling matched both forms. So the divergence was never about `sync.status` ordering at all; it was the separator. **FIXED:** the api read is now `[""] + [ … ] | join("; ")` (an empty first element yields a leading `"; "` while an EMPTY list still yields `""`), and its prefix test carries both alternatives, matching the sibling exactly. The same round also found the api path had the sibling's **empty-`sync.status` fail-open** — `jq -r '.status.sync.status // ""'` MANUFACTURES the empty string, so `{"status":{}}` classified `ok` and the caller logged `"<app>: synced (argocd-server)"` for an Application nothing had compared, on the tenant path that has never executed in any recorded walk. Fixed with the same positive enum, plus the message now interpolates `${_sst:-<none>}` instead of hardcoding "sync.status=Unknown" for a value that is empty. All four are RED-proven by reverting each in turn; `test-argocd-app-fetch.sh` 12 → 14 cases. | — |
| **B176** | 🟢 **The six-row matrix's standing rules exist only in chat and in this row — they belong in `docs/`.** The owner has asked for them twice, and both times they were delivered as a message that scrolls away. They are 24 rules in eight groups: (A) scope — a NEW lab, all six rows, first-go over the FINAL tree, every fix resets the clock, report EST/EDT; (B) reading — read both scenario documents WHOLE as an end user, navigate only by what they say, verify every fact including ones a document asserts about itself; (C) executing — line by line, observe full and real output (no exit codes, no grepping a fragment), no verdict before the falsifying command, `.env` FRESH from `.env.example` via the document's own lifecycle, and after every cut verify `creds-show`/`creds` endpoints and credentials by authenticating and driving Chrome where an HTTP probe is not proof; (D) reading the result — the per-invocation `VERDICT-<runid>.txt` never a fixed path, each row's `WALK DONE`/`DOCUMENT` lines AND the `N of 6` denominator, never the exit code or the notification, and `MATRIX FAILED` means failed even when every row printed `WALK DONE`; (E) implications — a script/`.env` fix obliges a document check and vice versa, both directions every time; (F) security — secrets never in argv, the vCenter SSO account locks out permanently after 3 failures, no passwords typed into web forms, `harbor-robot.env`/`gitea-ci-token`/`webhook-token` never blanket-deleted; (G) process — Rule Zero on the DESIGN with `isolation: "worktree"` and schema-forced or synchronous dispatch, commit every ~30 min, `env -u GOROOT make static-check` locally before every merge because a PR runs only `static-check-pr` (no `sec`, no wall-clock tests); (H) during a run — never edit a script the matrix is executing, one matrix at a time, kill by process group, shell blocks under `bash`. **Why it is not written yet:** `docs/` is guarded by `adversary-first-gate`, which correctly re-armed after the last commit; a docs transcription did not seem worth consuming an adversary round while two were already in flight on matrix-blocking code. **Done when:** the eight groups live in `docs/walk-matrix-standing-rules.md` with the four rules I have already measurably broken this session marked as such — read-fragments-and-call-it-reading, prove-a-parser-claim-with-my-own-reimplementation, report-a-verdict-from-a-summary-instead-of-the-VERDICT-file, and fix-one-direction-only. | — ✅ **SHIPPED 2026-08-18 — `docs/matrix-standing-rules.md`, linked from CLAUDE.md.** Written out as **34** numbered items in eight groups (the row said 24; several of its groups bundle more than one obligation into a sentence, so the content is the same and the count is not). FOUR rules were added from matrix run 6's own lessons, each with a recorded incident: (C) **verify credentials AFTER the matrix completes, not during it** — a run reinstalls Harbor between cells, and run 6 printed FIVE distinct Harbor credentials, so during a run the authoritative evidence is in-walk (a 24-image mirror push cannot happen without a successful auth); (C) **drive the browser under a throwaway `HOME`**, because Chrome reads NSS from `$HOME/.pki/nssdb` and the operator's trust store is their security setting, not the agent's; (D) **"it has not advanced" is measured on the log's TAIL and mtime, never on a marker whose cadence you have not read** — a `[N/24]` marker logs only on pull completion, so a healthy row pushing blobs for 13 minutes looked stalled and a correct diagnosis was nearly revised; (G) **a batch prepared during a freeze goes STALE** — run 6's own 14-row batch carried two rows whose conclusions adversary rounds had refuted hours later, and the "validated" label is exactly what stops the next reader from checking. Numbering verified monotonic 1..34 with no duplicates. |
| **B175** | ⚠️ **ONE OF THIS ROW'S OWN CLAIMS IS FALSE, MEASURED 2026-08-19 — which is the row's own class, in the row.** It states that *'B104's stale citations were corrected in the same pass'*. They were not: measured today, B104 still carried `Makefile:250`, `Makefile:212` and `:88`/`:115`/`:137`, all wrong (the real sites are `lib/os.sh:131`/`:355` and `Makefile:256`). The correction was asserted, not applied. **That is exactly what this row exists to catch** — a backlog row authoritative enough to be believed, carrying a claim nobody re-checked — and it demonstrates the row's own denominator warning: **54 of 57 open rows remain UNAUDITED, not proven current.** ⚠️ **That `57` DOES NOT REPRODUCE, and the way it fails is this row's own point (2026-08-19).** Three ways of asking "how many are open" give three answers over the same 124 rows: **21** are not head-marked `✅`/`🟢`; **43** do not contain the word `CLOSED` (81 do); and the row's own `57` matches neither. The marker vocabulary is not controlled — `✅`(73), **four byte-distinct `🟢` sequences**(11+6+5+4), `🟡`(5), `⚠️`(5), `🔴`(4), `🟠`(3) — so every colour-derived census silently measures a subset it chose. The **argument stands** (most rows are unaudited); the **number is an artifact**, making this the FOURTH row today whose substance survived while its figures rotted (B77, B189, B104, and now B175 itself). Do not "fix" it by picking one of the three — there is no controlled status field to count, and inventing a number would repeat the defect. Cite the denominator you used, or say "unaudited" without one. B104's citations are now genuinely corrected, in a note at its HEAD, with the measurements. **The cheap prevention this row already prescribes stands and is what would have caught it: grep for the SYMBOL (`pkg_install`, `mise.run`), not for closure language.** ⏸️ Original follows. — 🟡 **MEDIUM — a merged fix that leaves its backlog row OPEN is authoritative enough to commission work against already-shipped code, and it did so THREE times on 2026-08-17** (B157 → a round against #776's live control; B135 → a round whose brief was a verbatim copy of the stale row; B149 → caught by reading, fix already on `main`). **The instrument I reached for is WEAK IN BOTH DIRECTIONS and I am recording that rather than its output:** a sweep for closure language inside open-at-head rows returns **25 of 57**, but it FALSE-POSITIVES on legitimately partial rows (B26: *"fixes (1)(2)(4) SHIPPED; this row is now fix (3) ONLY"* is correctly open) and FALSE-NEGATIVES on the row that motivated it — **B135 is not in the 25**, because it was never updated at all and contains no closure words. **The accurate check is per-row and costs real time:** `git log --oneline -S'<key identifier>' -- <the file it names>`. **Denominator, honestly:** 3 of 57 open-at-head rows VERIFIED against the code today; the other **54 are UNAUDITED for this drift — not proven current**. Two structural fixes worth considering first: put the closure at the HEAD (the convention #814 restored, B54 recorded long before), and have whatever closes a row CITE the PR, so `-S` has something to find. ✅ **SECOND MEASUREMENT 2026-08-18, and it CONFIRMS this row in BOTH directions it predicted.** I ran the closure-language sweep again over the whole file: **113 rows scanned, 8 candidates, and exactly 4 were real** — B179, B151, B180 and B167 flipped to ✅ (each row's own body already said done); the other 4 were REJECTED on reading. **So the instrument measured 50% precision on a second, independent run**, and its false positives are precisely the class this row named: B181's *"CLOSED — do not re-sweep"* refers to its CLASS DENOMINATOR, not the row (its Stage 2 is genuinely open), while B90, B83 and B145 say *"DO NOT BUILD"* as a **decision**, not a closure — B90's next step is a logging addition, B83's residual settles on a second differently-versioned cut, B145 is blocked on a certifying run. **AND THE FALSE-NEGATIVE DIRECTION WAS CONFIRMED THE SAME DAY, BY ME, AT COST:** B104 had never been updated, contains no closure words, and is therefore invisible to this sweep — so I filed **B183 and B184** against a class B104 already owned (`pkg_install`, the tdnf/apt blip, the sites `:88`/`:115`/`:137`, and the mise.run curl with a prescribed retry) and had to delete both. That is the **B135 shape exactly**: a never-updated row commissioning duplicate work. It also cost a second error — B184 asserted that B181's *"1 of 1 SWEPT AND CLOSED"* was a subset claim, when B181 states its own scope verbatim (*"`-f` is wrong only when the question is REACHABILITY; it is RIGHT … when you are fetching CONTENT"*), so I nearly shipped a row accusing a sibling of an error it did not make. ⚠️ **THE CHEAP PREVENTION IS NOT THE SWEEP — IT IS ONE GREP BEFORE FILING:** search the file for the SYMBOL the new row is about (`pkg_install`, `mise.run`) rather than for closure language. That finds a never-updated owner, which is the case the sweep structurally cannot see. **Structural fix (1) of this row is now applied to 4 more rows** — the closure is at the HEAD, with the marker flipped — and B104's stale citations were corrected in the same pass (it cited `Makefile:250` AND `:212` for a single site that is actually at `:256`, i.e. one site double-counted as two, with both numbers wrong). **Denominator, still honest:** 7 of ~113 rows verified against the code across the two audits; the rest remain UNAUDITED for this drift, not proven current. 📈 **DENOMINATOR MOVED 2026-08-18 — NINE more rows verified against the code in one pass, and the per-row check this row calls "real time" turned out to be the cheap part; the expensive part was that FIVE of the nine were WRONG.** Verified today: **B104** (its 🔴 CRITICAL measured **65 s**, not the 540 s it claims — the `--retry` it blames no longer exists, and its line citation had rotted to a different function), **B156** (mechanism real, consequence for this repo **zero**, and its `.env.example` prescription wrong twice over), **B166** (defect still live at the cited lines, but its own "30-line MOVE" refuted — the hoist would die on a bare box), **B173** (**CLOSED** by mechanism-level check; its own prescription marked do-not-apply), **B139** (all three premises confirmed; the defect is a MISLEADING ERROR, worse than the rc it is filed under), **B69** (narrowed 3 open items → 1: the doc note already exists and the guard has no producer), **B160** (item 1 upgraded from a post-fix log to a certifying run), **B174** and **B188** (both shipped with RED-proofs). **So the running total is ~12 of ~57 verified, and ~45 remain UNAUDITED — not proven current.** ⚠️ **The finding that matters for this row's thesis:** the drift is not mostly "merged but left open" (the shape this row was filed for). Five of today's nine carried a conclusion that MEASUREMENT REFUTES — a stale CRITICAL, a refuted prescription, an overstated dependency, a superseded fix, an already-existing doc note. A row can be simultaneously OPEN, ACCURATE-LOOKING and WRONG, and no closure-language sweep can see that class at all. The only instrument that works is running the thing the row describes. |
| **B189** | ✅ **CLOSED 2026-08-19 — the residual is ALREADY ADDRESSED, and implementing the candidate would have made things WORSE.** The row's remaining suggestion was *"have the `kubeconfig` arm say what it did and did not verify"*. **Measured: `24-lab-preflight.sh:84-90` already says exactly that**, verbatim, inside its `STALE_CA` die — *"your SAVED Supervisor CA is stale too — and `vks-login` cannot tell you, because under `VKS_AUTH_METHOD=kubeconfig` (what scenario-1 section 6 sets) its arm checks only that the file is non-empty; every CA check lives in the `vcf` arm."* And the ordering claim holds: **`docs/scenario-1.md:565-566`** are `make vks-login` and `make lab-preflight` on **adjacent lines of the same code block** in §7 Preflight, so the diagnosis arrives at the very next command. ⇒ **The disclosure exists, and it is placed better than the candidate would have placed it**: it prints on the **failure** path, when it is actionable, rather than on every successful `vks-login`. Adding the proactive note would have (a) printed noise on every green run, (b) created a **second copy** of a paragraph that must stay true — the drift shape this backlog keeps getting bitten by, and (c) said less, because the preflight message also names the remedy. **Do not re-open to add it.** ⚠️ **Instrument note, because I nearly mis-closed this the other way:** my first check for the ordering grepped `^make (vks-login|lab-preflight)` over a line RANGE that excluded §7, found the two commands ~292 lines apart at `:274`/`:566`, and made the row look wrong. The row was right; **my range was**. A negative result from a bounded search is a statement about the bound. ⏸️ Original follows. — 🟡 **RE-SCOPED 2026-08-19 — MY CANDIDATE FIX IS REFUTED (it would check the WRONG ANCHOR), and the gap is much narrower than I filed.** The row prescribed *"move the CA block ahead of the `case`, so both arms get it"*, and I had already measured the dependency question as clear (the block reads only `SUPERVISOR_HOST`/`VKS_CA_CERT_FILE`/`VKS_CA_SHA256`, none of which the `vcf` arm sets above it). That measurement was correct and **the conclusion did not follow.** ⇒ **The two arms are looking at DIFFERENT CLUSTERS.** The `vcf` arm authenticates to the **Supervisor**, so a Supervisor anchor is the right thing to check. The `kubeconfig` arm's `$KUBECONFIG` is a **GUEST workload-cluster** kubeconfig — `scenario-1.md:565` says so in its own inline comment (*"now checks the GUEST kubeconfig"*) and the arm's `die` says *"Place your VKS workload-cluster kubeconfig there"*. Hoisting the Supervisor CA block above the `case` would run a **Supervisor** anchor check on an arm holding a **guest** kubeconfig: the wrong anchor for the wrong endpoint, and on the tenant path (scenario-2) the operator may legitimately have no Supervisor CA at all. 🟢 **AND THE CHECK THE OPERATOR ACTUALLY NEEDS ALREADY RUNS, ONE COMMAND LATER.** `scenario-1.md:565-566` is `make vks-login` immediately followed by `make lab-preflight`, and `24-lab-preflight.sh:83-84` classifies the kubeconfig's own failure and emits the `STALE_CA` die — against the server **that kubeconfig points at**, which is the correct anchor for this arm. So an operator following the document does get the diagnosis; they get it from the next step rather than from `vks-login`. ⇒ **the honest residual is one sentence, not a code move:** `make vks-login` reports success on a guest kubeconfig whose CA is stale — it does not LIE about the CA, it simply does not check one, and the failure surfaces at the very next documented command. **Candidate, much smaller:** have the `kubeconfig` arm say what it did and did not verify ("the file is present; its CA is checked by `make lab-preflight`, next"), or call `classify_kube_failure` there directly against `$KUBECONFIG`'s own server. **Do NOT hoist the Supervisor block.** ⏸️ Original follows. — 🟡 **`make vks-login` performs NO Supervisor-CA check on the arm scenario-1 actually uses — so the remedy every stale-CA message recommends cannot clear a stale anchor.** Found by B166's idea round (2026-08-18) and verified independently against the code before filing. `30-vks-login.sh:38` defaults `METHOD` to `kubeconfig`, and `docs/scenario-1.md:523` (§6) sets it explicitly — so that is the arm every scenario-1 operator takes from §7 onward. That arm (`:42-45`) checks **only** `[ -s "$KUBECONFIG" ]`. Every CA check — `vks_ca_default` (`:138`), `ca_pin_verdict` (`:154`), `ca_verifies_endpoint` with all six rc arms (`:181`), and the stale-case `die` at `:262` whose message is *better* than `ca_status_report`'s (it prints the stored CA's SUBJECT beside the live cert's ISSUER, with `:267-269` explaining why those are the comparable objects) — sits inside `case "$METHOD" in vcf)` at `:47`. ⇒ **A scenario-1 operator on a rebuilt lab is told `make vks-login`, runs it, it passes, and the anchor is still stale.** B166 shipped the mitigation (both stale-CA dies now also name `make ca-status`, which needs no cluster), but the underlying asymmetry is untouched: the `kubeconfig` arm silently skips a check the `vcf` arm treats as fatal. **Candidate, NOT built and needing its own idea round:** move the CA block ahead of the `case`, so both arms get it. ⚠️ Not obvious that it is safe — the `vcf` arm's block may depend on state that arm establishes, and the `kubeconfig` arm's whole premise is a kubeconfig handed over by someone else, whose Supervisor CA the operator may legitimately not have. Measure what the block reads before moving it. |
| **B190** | 🟢 **CLOSED BY CONSTRUCTION 2026-08-19 — the row's own suspicion was right, and it is now measured rather than suspected.** The row asked whether the class *"may already be closed"* because the repo's established answer is a SEPARATE ENTRY POINT rather than a reorder. All three checks have one: `harbor_reachable_report` → **`make harbor-reachable`** (`Makefile`, 1 target); `ca_status_report` → **`make ca-status`** (1 target, and B166 made both stale-CA dies name it); and `harbor_auth_report` → **`make env-validate`**, which is the one I nearly missed because there is no `make harbor-auth`. **Proven by running it against the live lab:** `env-validate` printed `Harbor reachable (https://harbor.env1.lab.test)` and then `Harbor rejected HARBOR_USERNAME/HARBOR_PASSWORD (HTTP 401)` — i.e. it performs the reachability probe AND the auth probe, standalone, and reports a definitive verdict on a box whose guest cluster is unreachable. ⇒ **an operator whose cluster is down is NOT stranded on any of the three**; the `kubectl` gate at `24-lab-preflight.sh:81` costs them the convenience of getting all three from one command, not the diagnosis. **Do NOT reorder `24-lab-preflight.sh`** — B166's hoist was refuted on exactly this ground (unreachable on the path that matters, +30s on the fail-fast case, doubled probes), and this row would have re-derived it. ⚠️ **Residual, deliberately not built:** there is no `make harbor-auth` alias, so the auth entry point is discoverable only if you know `env-validate` does more than validate format. That is a one-line `make help` wording question, not a defect. ⏸️ Original follows. — 🟡 **The "a check gated behind a `kubectl` die it does not need" class is THREE instances, not one — B166 addressed the third and left the two that already have a written precedent.** Measured by B166's round (2026-08-18): in `24-lab-preflight.sh`, `harbor_reachable_report` (`:152`) and `harbor_auth_report` (`:159`) both use **zero** `kubectl`, exactly like `ca_status_report` (`:193`) — yet all three sit after the `kubectl version … || die` at `:81`, so on a lab whose guest cluster is down none of them runs and the operator gets a kubectl failure instead of the Harbor diagnosis. **The repo already answered this class, and the answer is NOT a reorder:** `:148-151`'s own comment records that the Harbor checks were factored into `lib/harbor.sh` and given a **separate entry point** (`make harbor-reachable`, `Makefile:600`, cited at `docs/scenario-1.md:318`) *precisely so* scenario-1 §4 can run them before the guest cluster exists. `make ca-status` is the same pattern for the third. ⇒ **So the class may already be closed by construction and this row may be a no-op** — the separate entry points exist for all three. **What is NOT established:** whether an operator who runs only `make lab-preflight` (not the separate targets) is meaningfully worse off, and whether the three dies name their standalone entry point the way B166 just made the stale-CA ones do. **Do not reorder anything before settling that** — B166's hoist was refuted on exactly this ground (unreachable on the path that matters, +30s on the fail-fast case, doubled probes). |
| **B191** | 🟢 **FIXED 2026-08-18, and the residual the round left open is now MEASURED rather than inferred.** `jumpbox-launch.sh:77` now does `mkdir -p "$WORK" && chmod 700 "$WORK"`. **The round's own caveat was the whole question — "measure it against the Ubuntu cell (uid 1001) before landing; prescribing a mode change on that path without it is exactly the trap the repo has already hit once" — so I measured it, three ways:** (1) **the uid really does differ by base image** — running the Dockerfiles' own `useradd` inside each: `photon:5.0` → **uid 1000**, `ubuntu:24.04` → **uid 1001**; (2) **so a `chmod 600` on the FILE would break the ubuntu cell** — the host file is owned by uid 1000, at `664` uid 1001 reads it through the `other` bit and at `600` it cannot, which is precisely this repo's recorded 0600-unreadable class and why the CA 27 lines below is deliberately `install -m 0644`; (3) **hardening the DIRECTORY is safe because every mount here is a FILE bind-mount** — `:180` mounts `${WORK}/kubeconfig` directly, so the container never traverses this directory. ⇒ **directory 700, file mode untouched.** **The naive form is a silent no-op and that is measured too:** on an existing `775` dir, `mkdir -p -m 700` leaves it **775** while `mkdir -p && chmod 700` gives **700** (and `install -d -m` is additionally ignored by toybox, the primary air-gap OS — B174 shipped two fixes that were no-ops there before this was understood). Confirmed on the live box before the fix: `.jumpbox` was `drwxrwxr-x` (**775**) with `kubeconfig` at **664**. ⚠️ **NOT closed by this:** the exposure was an ENUMERATION leak rather than a credential leak on THIS box (`/home/andriy` is 0750 `other::---`, so generic `other` cannot traverse; the one principal that can is `libvirt-qemu` via its ACL) — it is a real credential exposure on the SHARED jump box the runbooks describe, which is the case the fix is for. ⏸️ Original follows. — 🔴 **HIGH — a SUPERVISOR kubeconfig is written to `.jumpbox/kubeconfig` at 644/664, OUTSIDE `secrets/` where the hardening helper provably cannot reach it, and then bind-mounted into a container.** Found by B174's idea round (2026-08-18) as an out-of-scope finding and **verified independently before filing**. `scripts/jumpbox-launch.sh:77` sets `WORK="${REPO_ROOT}/.jumpbox"` with a bare `mkdir -p`; `:88` does `cp "$_sup" "${WORK}/kubeconfig"` (the lab branch — inherits the SOURCE's mode) and `:90` writes it by redirect (`kind get kubeconfig … > "${WORK}/kubeconfig"`). MEASURED at umask 002: dir **775**, file **644** from a 644 source, **664** from the redirect. MEASURED that `ensure_secret_dir`'s containment `case` does **NOT** match `${REPO_ROOT}/.jumpbox` — it matches only `${REPO_ROOT}/secrets` and `${REPO_ROOT}/*/secrets` (and their subtrees) — so the 8-site fix that closed the `secrets/` half cannot touch this by construction. **The damning detail is 27 lines apart in one file:** `:115` writes the **CA** with `install -m 0644` AND a written reason (*'a CA is PUBLIC trust material, and a 0600 one is unreadable by the container's uid'*), while `:88` writes a **cluster-admin credential** with no mode at all. Mitigating, not exculpating: `.jumpbox` is gitignored (no commit risk) and the mount is `:ro` (`:180`). ⚠️ **The fix is NOT `chmod 600` on the file.** The container runs `USER vks` whose **uid differs by base image** — `jumpbox/Dockerfile.airgap:59` and `.photon:34` say so — and that is this repo's recorded 0600-unreadable class, which is exactly why the CA beside it is deliberately 0644. **Candidate:** `install -d -m 700 "$WORK"` (measured → 700), hardening the DIRECTORY and leaving the file's own mode alone so the container's `open()` is unaffected. **NOT BUILT, and it must be measured against the UBUNTU jumpbox cell (uid 1001) before landing** — a mode change on that path without that measurement is precisely the trap this repo has already hit once. `make jumpbox JUMPBOX_OS=ubuntu` settles it. |
| **B192** | ✅ **CLOSED HERE, FILED THERE (2026-08-19) — it is `nested-vsphere-lab` **B453**, PR #83.** This repo owns neither `walk-matrix.sh` nor the fix, and the diagnosis was sitting only in THIS backlog, where the owning repo's next session would never have seen it — the same stranding that CLAUDE.md's cross-repo note exists to prevent (its siblings there are **B436** and **B428**; this is the third). **I re-verified the corrected diagnosis in the sibling tree rather than trusting this row:** `grep -n destroy_stale_walkboxes scripts/walk-matrix.sh` returns **exactly two** lines — the definition at `:616` and a single call at `:642` — `:642` sits inside `row()` **before** the `walkbox-vm.sh` invocation, and `grep -n 'trap '` returns **one** hit, at `:59`, which is the word "trap" inside a comment. So the file has **no `trap` at all**, the last row's box is never swept, and a mid-run kill strands one for the same reason. ⇒ **one VM per run, not one per row**, exactly matching the single 8 GiB stray observed 58 minutes after a 6-row matrix. The B453 section carries the fix (call it once more after the final row, ideally from a `trap … EXIT`) **and the two constraints that can make that trap worse than the leak** — it must not fire before the VERDICT is written (the failing row's VM is the primary evidence), and a run is FROZEN by standing rule, so sweeping on EXIT is fine while sweeping on a pause signal is not. ⚠️ **Do not re-file it here when it is still open there** — a row open in two backlogs under two numbering schemes is how a fix gets done twice or not at all. ⏸️ Original follows. — 🟡 **CORRECTED 2026-08-19 — MY ROOT CAUSE AND MY BLAST-RADIUS NUMBER WERE BOTH WRONG, and the fix is smaller than the row prescribes.** I filed this as *"nothing tears that name down"* and warned that *"a 6-row matrix can strand up to 6 VMs"*. Reading `walk-matrix.sh` refutes both. **The matrix ALREADY HAS the marker-guarded sweep** this row proposes as candidate (b): `destroy_stale_walkboxes()` at `:616` enumerates every domain, keeps only those whose XML carries `vks-walkbox-managed`, and `destroy`+`undefine --nvram --remove-all-storage` each one — with a comment recording that it was verified 2026-08-11 that all three walkboxes carry the marker and the lab's own `esxi01` does not, so it cannot reach a lab VM. **So candidate (b) is already built and does not need a new destructive verb or its own round.** ⇒ **The real defect is WHERE it is called: `:642`, INSIDE `row()`, BEFORE the VM is built** — each row sweeps the PREVIOUS row's box on its way in. That is a deliberate and correct design (it also reclaims a box left by a crashed earlier run), and it has exactly one consequence: **the LAST row's VM is never swept, because no row follows it.** ⇒ **the leak is ONE VM per matrix run, not up to six** — which is precisely what was observed (row 6's `vks-walkbox-ubuntu`, alone, 58 minutes after the matrix exited). My "up to 6" was inferred from a wrong root cause, and the observation that motivated the row (exactly one stray) should have refuted it on the spot. **Fix, now a one-liner:** call `destroy_stale_walkboxes` once more after the final row — ideally from a `trap … EXIT` in `walk-matrix.sh` so it also fires when the matrix is killed mid-run, which is the case that strands a box today. ⚠️ **It must NOT run before the VERDICT is written**, or a reader loses the box they may want to inspect; and a `trap` that destroys VMs needs to be reasoned about against the rule that a matrix run is FROZEN — sweeping on exit is fine, sweeping on a signal that the operator sent to pause is not. ⚠️ **The fix lives in `nested-vsphere-lab`, not here** — same repo as **B436**/**B428** — so it is a cross-repo item and this repo's `walkbox.sh down` remains unable to see the matrix's names, which is a separate (and now cosmetic) mismatch. ⏸️ Original follows. — 🟡 **EVERY MATRIX RUN LEAKS A WALK VM — the matrix creates `vks-walkbox-<os>` and NOTHING tears that name down.** Found 2026-08-18 by an end-of-session artifact sweep, not by any gate: `vks-walkbox-ubuntu` was still **running 58 minutes after the matrix exited**, holding **8 GiB** of RAM, with no driver process and no ssh session against it. **Root cause, measured:** `nested-vsphere-lab/scripts/walkbox-vm.sh:31` names its domain `${WALKBOX_NAME:-vks-walkbox-${OS}}` and that script has **no down/destroy verb at all** (grepped: no `down)`, no `destroy)`, no ACTION dispatch); this repo's `make walkbox-down` → `scripts/walkbox.sh down` defaults `WALKBOX_NAME=vks-walkbox` (`:39`) — **no OS suffix** — so it reports `no domain named vks-walkbox` and exits **0**, having done nothing. A teardown that cannot see the thing it is meant to remove, reporting success. Reclaimed by hand with `WALKBOX_NAME=vks-walkbox-ubuntu ./scripts/walkbox.sh down`, which worked immediately AND passed the `vks-walkbox-managed` marker guard — so the VM was genuinely ours and the only missing piece is the NAME. **Two candidate fixes, neither built:** (a) have the matrix call this repo's `walkbox.sh down` with `WALKBOX_NAME` set, per row, in a `trap … EXIT` — cheapest, and it reuses the marker guard rather than adding a second destructive path; (b) give `walkbox.sh down` a sweep mode that enumerates every domain carrying the `vks-walkbox-managed` marker and removes those — stronger (it also catches a row that died before its trap), but it is a NEW destructive verb on a libvirt connection that also hosts the lab's nested ESXi, so it needs its own idea round. ⚠️ **Do NOT 'fix' this by matching on the name prefix** — the marker is the guard this repo deliberately chose over a name convention, and a prefix match would destroy a hand-made `vks-walkbox-experiment`. ⚠️ **The leak is per-ROW, so a 6-row matrix can strand up to 6 VMs** — this session found one because five rows reused two OS images and libvirt names collide, but a run that interleaved differently could hold far more RAM than the box has. **RESIDUAL:** `/var/tmp/walkbox-andriy/base.img` (596M) is KEPT by design as a cache and is not part of this defect; nvl's **B436** separately tracks that the walkbox base image downloads into `$LAB_STATE` instead of the artifacts dir. |
| **B193** | ⛔ **THE PRESCRIBED DESIGN IS REFUTED (2026-08-19): wiring `ca_pin_verdict` into `_vc_tls` would print a FALSE fingerprint-mismatch on a CORRECT anchor.** Verified by EXECUTION, not by reading: `lib/vcenter.sh` does not source `lib/tls.sh`, so `command -v ca_pin_verdict` in that context returns NOT AVAILABLE (11 of 12 `vc_*` consumers do not source it). My own first grep said the opposite because its only hit is a COMMENT mentioning the file — the documented trap where a grep finds the string but not the kind of thing. Under `set -uo pipefail` the missing command returns 127, which falls into the catch-all arm, so the operator is told they are being intercepted, on a good CA, and then told the file is missing. Neither is true. Three more, all measured by the round: `_vc_tls` is called at 2 of 2 sites inside a command substitution, so a `die` there kills only the subshell and emits a second contradictory FATAL (it fails SAFE — zero requests reached the oracle — but diagnoses the wrong cause); one of those sites is `vc_logout`, an EXIT trap whose own comment one line above says a `die` there would replace the caller's real exit status; and a per-call check emits 120 identical log lines per `make wcp-restart`, nagging precisely the operators who set a pin. The arm count is FIVE, not six — `ca_pin_verdict` returns 0/1/3/4/5, and the sixth line a naive count picks up is the opening arm of the NEXT case. 🔴 THE REFUTATION THAT MATTERS: a pin at CONSUMPTION buys nothing. It ships commented, so unset is normal; `make fetch-vcenter-ca` downloads over `curl -sk` from the very host being authenticated and selects by handshake, and a MITM's root verifies a MITM's leaf perfectly — so with no pin the check returns verdict 3, is silent by design, and the SSO administrator password goes to the MITM. A pin is only a control if its VALUE PREDATES THE FETCH. ⇒ Build it in the PRODUCER instead, with `fetch-ca.sh:186-258`'s arms: a NOT-AUTHENTICATED banner on stderr so a redirect cannot hide it, an out-of-band confirmation prompt on a TTY, and a die when there is no TTY. `fetch-vcenter-ca.sh` already sources `lib/tls.sh` and calls `load_env`, so it is ~15 lines. The precedent the row cited is the wrong one: `30-vks-login.sh` is silent because the operator obtained the CA themselves, out of band — false for a `make fetch-vcenter-ca` product. 🔴 OWNER DECISION REQUIRED BEFORE IT CAN LAND, which is why this is red: `make fetch-vcenter-ca` sits in an executable bash fence at `docs/scenario-1.md:200` and `walk-doc.sh` runs fences non-TTY, so such a gate would die there and redden matrix rows 1-4 unless the harness supplies `VCENTER_CA_SHA256`. `fetch-ca.sh` already reasoned about this and concluded the walk SHOULD stop, since a green walk that pinned a stranger's certificate would be worse than a red one. Choose: supply the digest to the harness, accept red rows, or ship the gate TTY-only. When it lands, add `-u VCENTER_CA_SHA256` to `test-wcp-service.sh`'s `probe()` in the SAME commit — it does not scrub that variable today, unlike the refusal probes which do and say why, so an exported pin would make the oracle's throwaway CA mismatch and collapse cases 6-7 into refused-at-TLS. Sibling filed as B195. ⏸️ Original follows. — **MED — the vCenter trust ladder has no PIN, so it is one rung short of the three siblings it claims parity with.** Filed by B133's implementation round (2026-08-18) and NOT fixed there. `lib/tls.sh:193` states the two questions are different and neither substitutes: *ca_verifies_endpoint* answers "is this anchor for the lab that is RUNNING?" (staleness) while *ca_pin_verdict* answers "is this anchor the one they TOLD me about?" (authenticity) — and **a MITM's CA verifies a MITM's leaf perfectly**, so the staleness check returns 0 and we would submit the vCenter credential over an intercepted connection. `30-vks-login.sh:154` enforces `ca_pin_verdict` with `VKS_CA_SHA256`; Harbor has `HARBOR_CA_SHA256`; ArgoCD has `ARGOCD_CA_SHA256`. **vCenter has none.** ⚠️ **This matters MORE now than before B133**, because `make fetch-vcenter-ca` fetches the anchor from the very host being authenticated, over an unauthenticated channel — exactly the trust-on-first-use shape the pin exists to redeem. The interim control shipped: the fetch prints the SHA-256 and both it and scenario-1 §2 carry the out-of-band confirmation sentence verbatim from `fetch-supervisor-ca.sh:152`. **Fix:** add `VCENTER_CA_SHA256` to `.env.example` (COMMENTED — it is a selector), wire `ca_pin_verdict` into `_vc_tls` with the same six-arm handling `30-vks-login.sh:154-180` uses, and follow that file's deliberate choice to be **SILENT on verdict 3 (no pin)** rather than nagging — an operator who obtained the CA out of band already has the stronger evidence. **Do NOT make the pin mandatory**; that would nag every correct user and is the reason the sibling is silent. ⚠️ **Until this lands, do not describe the vCenter path as having parity with the other three ladders** — B133's row says so explicitly. | ⛔ **DROPPED 2026-08-21 — OWNER DECISION.** Its prescribed design was refuted by execution (above) and nothing replaced it, so the row had become a standing invitation to re-derive a fix that was already proven wrong. The measurements it produced are KEPT because they are reusable and were expensive: `lib/vcenter.sh` does NOT source `lib/tls.sh`, so `ca_pin_verdict` is unavailable in that context and returns **127** under `set -uo pipefail`, landing in the catch-all arm and telling the operator they are being intercepted **on a good CA**; `_vc_tls` is called at 2 of 2 sites inside a command substitution, so a `die` there kills only the subshell; and the grep that first said otherwise matched a **comment** naming the file — the find-the-string-not-the-kind-of-thing trap. If vCenter anchor pinning is ever wanted, it is a **fresh design**, not this row: start from the sourcing question and from whether `_vc_tls`'s call sites can carry a fatal at all.
| **B197** | ✅ ⚠️ **MY PREMISE WAS FALSE AND I MERGED IT — THE GUARD ALREADY EXISTS. Do NOT re-derive this row's original claim (corrected 2026-08-19 by its idea round, verified by me).** `70-configure-argocd.sh:338-340` reads, verbatim: `if [ "$MECH" = api ] && [ "$argocd_api_ready" != yes ]; then die "ARGOCD_MECHANISM=api needs the argocd CLI plus ARGOCD_SERVER and ARGOCD_AUTH_TOKEN (see .env.example)."` — and `argocd_api_ready=yes` requires `have argocd` at `:282`. **I read `:305-327` and stopped ELEVEN LINES SHORT of the guard**, then filed and merged "there is no precondition". The round measured four arms against the real script: CLI absent + no vars, CLI present + no vars, **CLI absent + BOTH vars set**, and `auto` + CLI absent — all die at `:339` or degrade correctly, and **`:727` is never reached from a missing CLI.** ⇒ **the fix I proposed is a STRICT SUBSET of a guard that already fires 31 lines earlier**: it could catch nothing new, could not redden a row, and **its RED could not be demonstrated without first deleting the existing guard** — a guard whose RED-proof requires disabling another guard is dead code by construction. **CLOSED as a premise error.** ✅ **What the round found INSTEAD — three real defects, in priority order, none of which I was looking at:** **(1) HIGH, and measurably wrong TODAY:** `:333` appends `[api not probed: set ARGOCD_SERVER + ARGOCD_AUTH_TOKEN]` whenever `argocd_api_ready=no` — **including when BOTH are already set and only the CLI is missing** — so the operator is told to set the two variables they just set; and `:339` then names all three causes without saying which one is missing (arms 1 and 2 produce a **byte-identical** message). Fix: discriminate at `:282` with three booleans instead of one, then name the specific missing item at `:333`/`:339`. **Message-only, no new branch, zero risk to a green row, RED-provable offline.** **(2) MED:** `:727` `argocd repo add … || die` is the **only one of THREE** argocd failure sites in the file that does **not** route through `classify_argocd_failure`/`argocd_transport_die` — `:628` and `:837` both do. Its stderr is not captured, so an expired token or a STALE_CA at repo-add time reports *"argocd repo add failed for &lt;url&gt;"*: **the misattribution I was hunting, with a different cause than I assigned it.** Fix: capture stderr and route `:729` through `argocd_transport_die`, mirroring `:628-629`. **(3) MED:** the gate that should pin (2) cannot see it — `test-argocd-classify.sh:86` asserts `n_die >= 2` (*"both write sites…"*) over **three** sites, so it is green while one is raw and stays green if a fourth lands raw. Fix: assert **equality** between argocd invocations that can die and `argocd_transport_die` calls, not a floor. ⚠️ **Reachability facts worth keeping:** `nested-vsphere-lab/scripts/walk-matrix.sh:412` emits `ARGOCD_MECHANISM=api` for **every scenario-2 row (5 and 6)** — so this path IS walked, and those rows are green today *with the guard live*, which is itself evidence the CLI is present (`docs/scenario-2.md:41` runs `make deps`, and `:342-346` requires `argocd login` + `argocd account generate-token` two steps before `make gitops`). And `:727` sits behind `if [ "$GITEA_DEPLOY_PRIVATE" = "true" ]` (`:699`) while repos are seeded **PUBLIC** — an off-by-default branch. **Residual:** `:727`'s runtime behaviour is source-read only; settle with `make e2e-kind` then `GITEA_DEPLOY_PRIVATE=true ARGOCD_MECHANISM=api ARGOCD_AUTH_TOKEN=<expired> bash scripts/70-configure-argocd.sh`, expecting the unclassified die. ⏸️ Original follows, WRONG. — 🟠 **MEDIUM — an explicit `ARGOCD_MECHANISM=api` has NO `have argocd` precondition, so on a box without the CLI it fails LATE with a message that names the WRONG CAUSE — and the air-gap box is exactly where the CLI is unobtainable.** Found 2026-08-19 by the RULE ZERO-A contract audit (derive the far side's binary contract by GREP, never by recall), not by any gate. **Four measurements, all confirmed:** (1) `70-configure-argocd.sh:307` is `api)     MECH=api ;;` — the explicit arm sets the mechanism with **no capability check**, while the `auto` arm is safe because `can_api=yes` requires `have argocd` at `:282`; (2) the arm's only real invocation is `:727` `printf '%s' "$TOKEN" | argocd repo add …` guarded by `|| die "argocd repo add failed for <url>"` — so a missing binary is reported as **a failed repo-add against a URL**, sending the operator to debug Gitea or RBAC; (3) `11-bundle.sh` mentions `argocd` **0 times** — it stages `crane helm jq kubectl yq` and **the CLI is not carried**; (4) the only installer is `00-install-prereqs.sh`, whose own header says **"INTERNET side only"**, and `require_cmd argocd` appears **0 times** in `70-configure-argocd.sh`. ⇒ on a sneakernet air-gap box the remedy the message implies (install the CLI) is impossible, and the real remedy (`ARGOCD_MECHANISM=kubectl`, or carry the binary) is never named. ⚠️ **SEVERITY DELIBERATELY NOT INFLATED — the reachability is UNPROVEN.** The default is `auto` (`:179`), which degrades correctly without the CLI, so this bites only when an operator *explicitly* sets `api`. `:108` calls `ARGOCD_MECHANISM=api` *"the tenant's ONLY path"*, but the tenant scenario (scenario-2) runs against a real lab, not the sneakernet box — I did **not** find a documented flow that sets `api` ON the air-gap box, and I am not claiming one exists. What is certain is the **misleading error**, which costs debugging time on any box lacking the CLI. **Candidate fix (needs an idea round — a new precondition is a new control):** give the explicit `api)` arm a `have argocd` check that `die`s at SELECTION time naming both remedies, rather than letting `:727` blame the repo. **Do NOT "fix" it by adding argocd to the bundle without measuring** — that is a second static binary across the air gap for a path whose reachability is unproven; establish the flow first. **Also check the sibling:** `request)` needs no CLI (it only `log_warn`s the command for a human), so it is correctly unguarded — do not add a check there. ✅ **THE AUDIT IS COMPLETE, NOT STOPPED AT THE FIRST HIT — `argocd` is the ONLY gap.** Censused all **11** far-side scripts (derived from `make -n` over `bundle-load mirror-push mirror-verify platform gitops install-ingress verify`), comment-stripped, command-position, with shell keywords/builtins/functions filtered by `type -t` = `file`. Eight candidates fell out and seven are accounted for: `seq` `sha256sum` `tee` `timeout` `which` are coreutils/util-linux and covered by the floor's "coreutils"; `argocd` is this row; and **`sudo` and `cluster` are FALSE POSITIVES** — `70-configure-argocd.sh:413` (`… | sudo tee -a /etc/hosts`) is inside an `_addr_verdict="…"` **advice string printed to the operator**, and `71-argocd-register-guest.sh:144` (`cluster (${_rs`) is a fragment of a `die "…"` message. Nothing on the far side executes either. ⚠️ **THE AUDIT METHOD COST THREE WRONG CENSUSES, EACH A DIFFERENT FALSE-POSITIVE CLASS — budget for all three next time:** (1) **comments** — the first pass counted `file`×3, `docker`×2, `podman`×1 on the far side and every one was comment prose; (2) **shell keywords** — the second pass reported `if`, `for`, `done`, `case`, `else` as "binaries that exist here", because `command -v if` **succeeds**, so an existence test cannot filter them (`type -t` = `file` can); (3) **multi-line string literals** — `#`-stripping cannot reach prose inside a `die "…"` or an advice string spanning lines, which is what produced `sudo` and `cluster` here. Only the third census, with all three filters, produced a real candidate. **A binary census that does not name which of these three it filters is not a census.** | ✅ **ALL THREE SHIPPED 2026-08-21.** (1) `70-configure-argocd.sh` now tracks the three causes SEPARATELY in `_api_missing` and names the specific missing item at both the log line and the `die` — measured across all four arms, including the one that was wrong: **CLI absent + BOTH vars set** now reports *the argocd CLI*, not *set ARGOCD_SERVER + ARGOCD_AUTH_TOKEN*, and the healthy arm is SILENT. (2) the `argocd repo add` site now captures stderr and routes through `argocd_transport_die`, mirroring `:628-631` — all **3** argocd failure sites classify, **0** die raw. (3) `test-argocd-classify.sh` replaced the `-ge 2` FLOOR with **equality** (`-eq 3`) plus a **non-rotting** companion asserting zero raw dies on an argocd invocation. **BOTH RED-proven, each on its OWN assertion:** un-classifying the repo-add site fires the equality arm (*2 calls, expected 3*); landing a fourth raw site fires the raw-die arm (*1 raw*) — and the restore returns 11/11. The count is enumerated ON PURPOSE so a new failure site must be reviewed; the raw-die arm is what catches it regardless of the count.
| **B198** | ✅ **`check-toolchain-alignment`'s GO arm can pass VACUOUSLY; its kubectl arm cannot. Found 2026-08-19 by the RULE ZERO round, source-read at `Makefile:1058-1064`, NOT yet fixed.** The recipe reads `if [ -n "$$go_mise" ] && [ -n "$$go_img" ] && [ "$$go_mise" != "$$go_img" ]` — so if **either** extraction returns empty the comparison is **skipped**, the recipe prints `check-toolchain-alignment: go aligned ()` and **exits 0**. The extractor is `grep -E '^go '`, which needs a literal space after `go`: a perfectly valid TOML reformat to `go="1.27.0"` yields empty and silently blinds the one gate protecting this invariant. Contrast the kubectl arm directly above, which compares **unconditionally** and therefore has no such hole. **Consequence:** the next Go drift ships green and surfaces far away — a Kaniko `MANIFEST_UNKNOWN`, or a test-vs-build toolchain mismatch at the end of a pipeline. **Minimal fix:** an empty-guard before the comparison that names the file it read (`[ -n "$$go_mise" ] \|\| { echo 'ERROR: could not parse a go pin from .mise.toml'; exit 1; }`, same for `go_img`). This is the `gates.md` extract-then-compare vacuity class: `"" != ""` is false, so a gate that fails to read **both** its inputs certifies agreement. **DEFERRED, not forgotten:** `Makefile` is walked, so fixing it before a certification would reset the first-go clock. Do it immediately after the tag. | ✅ **SHIPPED 2026-08-21 — BUT THE ROW ABOVE IS WRONG ON ITS CENTRAL CLAIM; read this before citing it.** Its idea round MEASURED the opposite: the recipe did **not** print `go aligned ()` and exit 0. `Makefile:167` sets `.SHELLFLAGS := -eu -o pipefail -c`, so a non-matching `grep` exits 1, `pipefail` propagates, and `set -e` killed the recipe **at the assignment** — a **SILENT RED naming nothing**, not a fake green. Two further refutations: **(a)** the empty-guard this row prescribed is **DEAD CODE**, because `set -e` kills the line above it (the `coding-style.md` guard-below-a-fallible-assignment class) — measured, `REACHED-LINE-2` never printed; **(b)** adding `|| true` WITHOUT the guard is *worse than either*, because it makes `"" != ""` reachable and produces **exactly** the `aligned () / rc=0` this row feared — so the row describes a state that only a PARTIAL application of its own fix can create. They land together or not at all. The round also found two defects **neither this row nor the brief named**: `^kubectl` had no `=` anchor and neither arm had `head -1`, so a real mise tool (`kubectl-convert = "1.31.0"`) yields a **two-line** value and a false DRIFT quoting a garbled pin. **The fix copies `scripts/check-java-alignment.sh` verbatim** — `|| true` on every extraction, `head -1`, `=`-anchored regexes, a hard guard on each REFERENCE value naming the file it read, then an **unconditional** compare rendering `<none>`. **RED-proven on the MESSAGE, not the rc** (the old code exited 1 too, so rc proves nothing): a `go="x"` reformat now PARSES; a vanished `golang:` pin prints *the extractor is blind, not the pin absent*; `kubectl-convert` does not garble; genuine drift names both values. ⚠️ One RED-test of mine **silently missed** — the real line is `go       = "1.26.7"` with padding, so a `'^go = '` sed never landed and reported a false green; the mutation must be regex-tolerant.
| **B199** | 🟡 **DECISION RECORD 2026-08-19 — Renovate #892 is THREE bumps, not one, and the golang hop was HELD (owner decision), not rejected. Do NOT close #892.** The PR reads as "update Docker images" and was described in the handoff as a golang bump; it is actually **(a)** golang `1.26.6→1.27.0` across `.env.example` + `apps/go/gowebapp/Dockerfile` + `images/images.txt`, **(b)** `mikefarah/yq 4.53.3→4.53.4`, which lands in `k8s/tekton/tasks/update-deploy.yaml` — the write-back task on **every** app pipeline's critical path, which both scenarios exercise via `make verify` — and **(c)** `traefik v3.7.10→v3.7.11`, which the matrix **never installs** (scenario-1 runs `INGRESS_CONTROLLER=istio-existing`, `docs/scenario-1.md:869`), so it is mirrored but not runtime-exercised. **Why held:** `golang:1.27.0-bookworm` was pushed **~3h** before the freeze (a brand-new Go MINOR), `golang:1.26.7-bookworm` exists as a same-minor patch (both digests resolved via `crane digest`), and nothing is CVE-forced — `main` was CI-green on 1.26.6 and `trivy-fs` scans the built Go binary (`Makefile:1377`). Taking an hours-old toolchain into the tree being frozen for a **first-go** certification is the inverted risk trade: a failing row could not be told from a document defect, and the clock resets either way. **It also fails `check-toolchain-alignment` as filed** (`.mise.toml=1.26.6` vs `images/images.txt golang:1.27.0`) — the gate is CORRECT; Renovate split a coupled set. **⚠️ Do NOT "defer" it by closing the PR** — Renovate states in its own PR body that closing adds the update to the ignore list, which would suppress the golang bump permanently. Leave it open; re-evaluate after the tag, and prefer `1.26.7` over `1.27.0` unless 1.27 has soaked. **OWNER DECISION 2026-08-22 — the hop is TAKEN. ROW CLOSED.** Owner: *"take the hop, one PR bumping all four together"*. Landed as one hand-written change because SPLIT it is structurally unmergeable: `check-toolchain-alignment` compares `.mise.toml` against `images/images.txt`, and the hop had fragmented across **#892** (closed — which per Renovate's own PR body ignore-lists the update, so the Dockerfile/`images.txt`/`.env.example` half stopped being proposed at all) and **#934** (open, `.mise.toml` ONLY). Measured before editing: no open PR proposed golang for `apps/go/gowebapp/Dockerfile` or `images/images.txt`. Four sites moved together — `.mise.toml:41`, `.env.example:1756` (`GOLANG_BUILD_TAG`), `apps/go/gowebapp/Dockerfile:13` (`ARG BUILDER_IMAGE`), `images/images.txt:28`. **`apps/go/gowebapp/go.mod`'s `go 1.26.5` deliberately NOT moved** — that is the LANGUAGE directive (a minimum), not the toolchain pin, and neither alignment gate compares it. `go.dev` lists **go1.27.0 as current stable** ahead of 1.26.7. `docs/sizing.md` updated from MEASURED sizes (`crane` — build image 283 → **287 MB**, Go total 290 → **293 MB**), not assumed. **#934 must NOT be closed by hand** — once `main` carries the version there is no update left to propose and Renovate closes it itself; closing it manually is the ignore-forever action that caused this split in the first place. |
| **B200** | 🔵 **LOW — a stale GENERATED include outlives its source, and one `$(wildcard)` guard is all that keeps it inert.** Found 2026-08-20 by the B75-a idea round. `secrets/.env.state.make` exists (233 B, dated 2026-08-16) while its source `secrets/.env.state` does **not** — it is a 4-day-old snapshot of a DESTROYED cluster's `KUBECONFIG` / `HARBOR_URL` / `ARGOCD_LB_IP`. It is not included today only because `Makefile:108` guards on `$(wildcard $(STATE_SRC))`. Any future "simplification" of that guard to a bare `-include secrets/.env.state.make` makes a dead lab's endpoints LIVE make variables — the same class the Makefile's own comment at `:95-97` records (*"a KinD-named file that carried REAL-LAB state, which is how `make kind-down` came to destroy a lab's kubeconfig"*). **Minimal fix:** have `kind-down` remove `secrets/.env.state.make` alongside `.env.state`, so the derived file cannot outlive its source. Deferred: `Makefile` is walked. |
| **B201** | ⛔ **EVERY lab cut leaves a DEAD cluster-admin kubeconfig in `secrets/`, and they had accumulated to 16 before anyone looked — one of which was the LIVE cluster's, so the obvious cleanup command is a footgun.** Measured 2026-08-20: `secrets/` held 12 `cicd-gc08*.kubeconfig` plus `argocd`, `cicd-gc1`, `cicd-gc4`, `supervisor` and a **0-byte** `testcluster.kubeconfig`. All gitignored, so no gate sees them and `git status` is blind. Each is a cluster-admin credential for a destroyed cluster. 🔴 **THE TRAP:** the natural `rm secrets/cicd-gc08*.kubeconfig` would have deleted **`cicd-gc0819222721.kubeconfig` — the LIVE certified cluster's**, written by that night's walk (`walk-matrix.sh` publishes it to `${WALK_REPO}/secrets/${WALK_CLUSTER_NAME}.kubeconfig`). *Location is not ownership*: the live one and the dead ones share a prefix, a directory and a mode. ⚠️ **AND REACHABILITY DOES NOT DISCRIMINATE** — the lab reuses the same LB IPs on every cut, so four dead kubeconfigs "reached" a live endpoint (`.128` → 200, `.132` → 403) purely because today's Supervisor sits where the destroyed one did. Only `kubectl auth whoami` separated them: all four failed x509 against a destroyed lab's VMCA. A prune keyed on *does it answer* would keep dead credentials and could delete good ones. **The safe shape, used 2026-08-20 to remove 16 and keep 1:** resolve the live cluster name from the CLUSTER (`kubectl -n <ns> get cluster -o name`), **refuse to proceed if that lookup fails** (a failed query must never read as "nothing is live"), skip that name, and **verify by RE-LISTING, never by the delete counter**. **Open:** nothing prunes these automatically, so the count grows by one per cut. Candidate fix — have the teardown retire the guest kubeconfig the way `nested-vsphere-lab` already retires its Supervisor one (rename to `*.destroyed-<ts>` rather than delete), which keeps the evidence trail and makes the dead ones self-labelling. Needs an idea round: `secrets/` is operator-local state, and a repo that deletes an operator's credentials unprompted is its own hazard. | ⛔ **DO NOT BUILD THE PRUNE — refuted 2026-08-21 on its core premise.** The design was “delete only kubeconfigs that fail x509 against a destroyed VMCA”. **`kubectl auth whoami`'s EXIT CODE cannot express that** (measured, kubectl v1.36.3): **rc=1** for x509-against-a-dead-CA, **rc=1** for unreachable, and **rc=1** for a 0-byte file — which silently retargets **`localhost:8080`**, so a stray `kubectl proxy` would make `whoami` **succeed** and KEEP the junk while deleting a real-but-down cluster's credential. Only stderr TEXT differs, and that is unstable across versions. Worse: the **LIVE** certified cluster's config returns **rc=1** at `--request-timeout 1ms` — rc is a function of TIMING, not validity — and B181 has just finished proving a 5-second blip happens on these very boxes, so **B181's evidence is B201's disqualifier**. The glob is also not homogeneous: it catches `supervisor.kubeconfig`, whose deletion silently DEMOTES `supervisor_kubeconfig()`'s resolution (`lib/os.sh:783-790`) so Supervisor-only operations start targeting the guest cluster. **And the repo already decided this, the other way, citing the same rule:** `98-uninstall-all.sh:280-283` prints *“NOT removing secrets/ automatically — it may hold a credential this repo did not create”* and emits the **name-specific** command — provenance, the only discriminator that works. Scale re-measured: **2** files, not 16 (cleaned 2026-08-20), ~1 per cut. **If anything is built:** retire-by-name in the teardown (`mv secrets/${VKS_CLUSTER_NAME}.kubeconfig ...destroyed-<ts>`), or a REPORT-only target that deletes nothing. ⛔ **AND THE RETIRE-BY-NAME ALTERNATIVE IS ALSO REFUTED 2026-08-21 — the row is CLOSED, build nothing.** The mechanism is sound (3 of 4 attacks on it cleared, below) but it is **structurally unable to fire on the path that accumulates the files**: `walk-doc.sh:120-122` SKIPS any fenced `make uninstall-all` (*"teardown - would destroy the lab mid-walk"*, pinned by `test-walk-doc.sh:86`), `98-uninstall-all.sh:51` additionally demands a **typed** `CONFIRM=<VKS_CLUSTER_NAME>`, and **measured**: `grep -n uninstall-all` in `nested-vsphere-lab`'s `walk-matrix.sh` and `walkbox-vm.sh` → **zero hits**. Cuts destroy the lab without ever running the teardown, so a correct, tested `mv` there would fix nothing while the row read closed. **Attacks that CLEARED — recorded so nobody re-derives them:** a rename does NOT violate delete-only-what-you-created (reversible, and this repo demonstrably creates that path at `27-use-guest-kubeconfig.sh:42`); the `.destroyed-<ts>` suffix does **not** un-ignore the file (**measured both ways**: `git check-ignore -v` returns `.gitignore:15 /secrets/` for BOTH names — the whole directory is ignored, so the suffix is irrelevant — and **zero** consumers glob `*.kubeconfig`); `VKS_CLUSTER_NAME` cannot be stale-or-empty at teardown (`98-uninstall-all.sh:42-48` dies on empty-or-placeholder); and timestamp determinism is a non-concern since `secrets/` is wholly gitignored. **Live with the scale (2 files, ~1 per cut)** or fix it in the repo that owns the producer.
| **B202** | ✅ **`make creds` shows Harbor/ArgoCD/Gitea from `.env` only; reading them live is NOT a matter of "prefer the cluster", which an adversary round REFUTED as a PRIVILEGE INVERSION.** After scenario-1 Step 9, `.env` holds the least-privilege **robot** (`22-harbor-robot.sh` publishes `robot$<project>+<name>`) while the cluster secret holds **admin** — so preferring the cluster promotes admin over the account the pipeline actually runs as, contradicting `28-harbor-admin-password.sh:37-42` (*a credential that already authenticates wins*). Worse, taking only the **password** rebuilds the mixed pair whose 401 `22-harbor-robot.sh:200-206` already records as MEASURED. Four states where `.env` is authoritative and the cluster is stale: the Step 9 robot; a **rotated** admin password (goharbor applies `HARBOR_ADMIN_PASSWORD` only at first bootstrap); a re-install over a surviving DB; and values the cluster does not carry at all (vCenter). **Shape if built:** show BOTH sources, labelled, username and secret moving as ONE ATOMIC PAIR from ONE source, `.env` winning whenever it holds a `robot$` name — never a silent winner. Do NOT re-implement the secret read: `28-harbor-admin-password.sh:135-152` already sorts `-ver-N` numerically and documents that `harbor-exporter-ver-1` carries the same key, so *the first secret with the key* is a coin flip. Call `make harbor-admin-password`. ⚠️ **And correct `22-builder-push.sh:100` in the same change** — it claims Harbor *supports failed-login lockout* and that retrying is *the same shape as retrying a vCenter 401*. Per goharbor source that penalty is a **1.5s per-principal sleep** (`frozenTime`), not a lockout; the false equivalence is currently the stated reason nothing verifies Harbor. Cited-not-measured by me: settle offline once mirrored with `crane export <harbor-core> \| strings \| grep -i frozenTime`. **✅ CLOSED 2026-08-20.** Its idea round returned `refuted-in-part` and killed the design's central move: I intended to RETRACT the vCenter equivalence outright on the strength of `frozenTime`, and that inference is wrong. **MEASURED** at goharbor **v2.15.2** (appVersion of chart 1.19.2): `frozenTime = 1500ms`, `IsLocked = time.Since(...) <= d` (self-expiring), no counter, no threshold — the FACT holds. But `mirror_retry` backs off **2/4/6/8s** (`lib/mirror.sh:120`), every interval EXCEEDING the 1.5s window, so ALL attempts reach `Authenticate()`; and `auth.Login` dispatches per `auth_mode`, so under `ldap_auth` each attempt is a bind at a directory we do not control — possibly the SSO that locks PERMANENTLY after 3. So the equivalence is *not identical and not retractable*, and the B187 predicate is **necessary and NOT sufficient**. Our install is `db_auth` (repo sets `auth_mode` 0 times; live lab `/api/v2.0/systeminfo` returned `"auth_mode":"db_auth"` unauthenticated, 2026-08-20) — but a scenario-2 tenant's Harbor is the platform team's (RULE ZERO-B). **Shipped:** the corrected comment; a `_settle_note()` helper naming DIAGNOSE vs FIX in **all three** stale-value arms (the STORED arm, which a post-matrix box actually lands in, had named NO remedy); a refusal in `28-harbor-admin-password.sh`'s `rejected` arm when `HARBOR_USERNAME` is a `robot$` account — it used to fall through to `env_publish_all ... HARBOR_USERNAME admin`, **silently promoting a revoked robot to admin**, the exact downgrade its own header claimed to prevent; the privilege-inversion rationale (NOT "a printer must not probe" — it does, PR #901); and a gate ASSERTION for the atomic-pair hazard rather than a comment. **⚠️ The prescription in this row to settle `frozenTime` via `crane export <harbor-core> | strings` is SUPERSEDED** — reading upstream source at the pinned tag is strictly better evidence and is what was done. **Not taken:** a live Harbor/ArgoCD/Gitea read, because "prefer the cluster" is a privilege inversion after Step 9. |
| **B203** | 🔵 **LOW today, HIGH if anything ever un-masks — `check-prose-secrets.sh`'s PATTERN cannot see the `make creds` TABLE shape.** The gate (`:37`) requires `password`/`token`/`secret` followed by `:` or `/`, or a literal `admin /`; a whitespace-column table row (`Harbor   https://...   admin   <value>`) satisfies none. An adversary measured **0 of 3** realistic creds-show rows matching. **Why it is LOW and not HIGH:** every secret in that report goes through `_mask`, and `walk-doc.sh` runs each statement through a PIPE, so a row log carries `<hidden: ...>` and not a credential — the `0 occurrences in the row logs` control in CLAUDE.md still holds (re-verified 2026-08-20 after the B198 work added the live SSH read). It becomes HIGH the moment anything renders under a **pty** (`script -qec`, `ssh -t`, `unbuffer`), which `[ -t 1 ]` treats as a terminal and reveals — already recorded at `creds.sh:60`. **Fix if taken:** extend PATTERN with the table shape and RED-prove it on the 3 rows that measure no-match today, keeping the single-PATTERN discipline the gate's header mandates. **⚠️ PARTIALLY ADDRESSED 2026-08-20 — DELIBERATELY NOT CLOSED.** Its idea round returned `refuted` and killed one of my two proposed arms outright. **SHIPPED:** the existing `admin` arm now also accepts **2+ spaces** as the separator (`\badmin ?(/|  +) ?[A-Za-z0-9]`), taking the catch rate from **0/3 to 2/3** at **0** measured false positives over 11,129 lines of `*.md`; RED-proven (reverting the arm misses exactly the 2 new catch cases and nothing else); selftest 16→21. `\b` is load-bearing and measured both ways — it kills the `gitea_admin  account` prose class and loses NO real row, because the rendered usernames are bare `admin` (`.env.example` GITEA_ADMIN_USER=admin). `robot$<project>+admin` still matches and that is CORRECT — such a line IS a robot credential row. **⛔ TWO ARMS REFUTED BY MEASUREMENT, recorded in the file so nobody re-adds them:** `admin +[A-Za-z0-9]{6}` → **61** false positives; `Username +Password` (pasted-header detection) → **ZERO DISCRIMINATION**, measured firing **twice** on `SKIP_DOTENV=1 creds.sh` output where every secret is a `<hidden…>` sentinel and no credential exists — it fires identically on the safe paste and the leaking one, and its cheapest remedy is to reword the HEADER, leaving every credential row unflagged. **🔴 STILL OPEN, and the uncaught shape is the WORST one:** `guest node SSH  <secret>  vmware-system-user  <live password>` — added in PR #901, read live from the Supervisor, and carrying no `admin`, so **no widening of that arm can reach it**. Catching it needs an enumerated username list (rot) or a STATEFUL table-context gate keyed on the real discriminator (every SAFE cell renders `<…>`/`(…)`; every dangerous cell is bare) — which cannot live in this file, whose header mandates ONE line-oriented PATTERN. That is a NEW CONTROL and needs its own idea round. The gap is PINNED as a `_must_pass` selftest case, so if a future arm ever catches it that case FAILS and whoever did it must come here and close this row. **Threat still UNWITNESSED:** 0 creds tables have ever been pasted into a tracked doc in this repo's history.
| **B204** | 🔵 **The machine token and the prose CONTRADICT each other, and `docs/scenario-1.md:899` promises a per-value labelling that no row carries.** Measured 2026-08-20 in the real-lab shape (`.env` populated, no overlay — what the walkboxes produce): one run emits `values-provenance: DEFAULT` **and** *Treat every credential here as LIVE until you know otherwise.* `test-creds-show.sh:131` asserts DEFAULT means *placeholders, not credentials*, so the token labels live lab credentials DEFAULT. Separately, **0** table rows carry DISCOVERED/STORED/DEFAULT although scenario-1 §Step promises *Each value is labelled DISCOVERED (read live), STORED (remembered), or a DEFAULT* — and that Expect line is **not machine-checked** (the certified run recorded `[41] rc=0 (1s)` only). **Fix:** make the per-value labels real and demote the report-wide token to a per-value one, or the scenario doc stays false and the gate's DEFAULT semantics stay violated. Cheap now that the `Lab access` section already distinguishes `.env`-supplied from live-read values. **✅ CLOSED 2026-08-20 — and its own PREMISE was REFUTED, so the fix is ZERO product change.** (1) `test-creds-show.sh:131` is **not** violated: `render()` sets `SKIP_DOTENV=1`, so `env-populated=0` and the report says *these ARE PLACEHOLDERS* — token and prose are CONSISTENT there. What was stale is the **gloss in the `ok()` string**, which read *(placeholders, not credentials)* as if DEFAULT meant that unconditionally. Corrected. (2) The proposed 4th enum value `SUPPLIED` is **REFUTED**: `creds.sh` already emits **`env-populated: {0,1}`** on the next line, so the pair (DEFAULT, env-populated=1) **already identifies the state uniquely** — a 4th value is either exactly that pair (redundant, two orthogonal axes collapsed into one enum) or it wins whenever `.env` is populated and **DESTROYS the DISCOVERED/STORED split**, the branch carrying *may be from a lab that no longer exists*. Instead the PAIR is now PINNED in STATE 6, which nothing did before. (3) *Make the per-value labels real* re-enters **B76**, already CLOSED verdict DELETE: per-value provenance is **not computable** — `load_env` sources with `set -a`, so the surviving value carries no record of which file supplied it, and selectors resolve caller-env-wins while everything else is last-file-wins. So the doc is corrected, and NOT on table-width grounds. (4) The round found a **SECOND false claim** on that same line that this row never mentioned: `docs/scenario-1.md:900` ended *or a DEFAULT (**nothing installed yet**)* — the ONLY live occurrence of the world-claim PR #901 deliberately removed from the product hours earlier. Both claims rewritten. Gate 37→38.
| **B205** | 🟠 **THE HEADLINE DEFECT IS SHIPPED (`f925e21`, #911) — READ THIS ROW TO ITS END.** The OFFLINE gate reads continuations (77 → 94 literals) while the runtime parser stays head-only, asymmetry RED-proven both ways. The residual at the end is the head-anchored `INDEP_E` / `check-doc-expect-leak.sh` / negation guard. — 🔴 **HIGH — `walk-doc.sh` DROPS EVERY WRAPPED `Expect` CLAIM, and all three guards share the blindness, so the reconciliation guard reports 46 vs 46 EXACT over a corpus where 28 claims are half-read.** Found 2026-08-20 by the B203/B204 idea round (out of scope, its strongest finding); I re-measured every number. The parser is `line.startswith('**Expect')` (`walk-doc.sh:452`) — **single line only**; a continuation falls to `else: body.append(line)` and vanishes. MEASURED over `docs/scenario-1.md` + `scenario-2.md`: **46** Expect claims, **28 WRAP** onto a non-blank continuation, and **12** of those carry backtick literals the checker would otherwise have verified. The reconciliation guard `INDEP_E` (`:518`) greps `^[[:space:]]*\*\*Expect` — **heads only** — so it counts the SAME unit as the parser and is structurally incapable of reporting the loss it exists to prevent; `:564` records *MEASURED 2026-08-18: 46 vs 46, EXACT*, which is exactly what a shared-blindness pair looks like from the inside. Its own comment (`:512-514`) states the right principle — *a column-0-only grep would share its blindness* — and applies it to the INDENT axis while missing the WRAP axis. `check-expect-literals.sh` then adopts the same head-only anchor **deliberately** (`:134` *deliberately MATCHING walk-doc.sh's startswith*), propagating it to the one gate that had a chance to be independent. **🔴 AND THE NEGATION GUARD PRESCRIBES WRITING INTO THE BLIND SPOT:** `:823` tells authors to move a clause *to a following line*. **Fix:** attach non-blank continuation lines until a blank/fence, **AND** widen `INDEP_E` to count the same unit — changing only one re-creates the shared-blindness bug in the other direction. RED-prove by planting a backticked literal on a continuation line and confirming it becomes CHECKABLE. **Settles it:** an awk pass over both scenario docs that, for each line beginning with the Expect marker, reads the NEXT line and counts it when it is non-blank, is not a fence opener, and contains a backtick-quoted literal → currently **12**; must reach 0 or be printed as a disclosed exclusion. (Written out rather than inlined: the one-liner contains backticks, and a code span carrying them breaks MD038 and cascades into the following row.) ⚠️ This is certification machinery: it decides what the six-row matrix actually verifies. **⚠️ PARTIALLY ADDRESSED 2026-08-20 — and THE ROW'S OWN DESIGN WAS REFUTED. Read this before touching the parser.** Its idea round returned `refuted` on the CRITICAL axis: **widening `walk-doc.sh`'s parser is CERTIFICATION-BLOCKING.** Measured by reimplementing the checker exactly — `docs/scenario-2.md`'s discovery block flips from `_tot=0` (not checkable) to `_tot=3` carrying `harbor-nginx` / `HARBOR_URL` / `ARGOCD_SERVER`, **none of which can appear on the `else` branch EVERY scenario-2 matrix row takes** (the branch is forced: the only block creating the Supervisor kubeconfig sits inside a `<details>` the parser skips, and the harness ships only the GUEST kubeconfig). That is `EXPECT UNMET` → `exit 1`, ~25 minutes into a live lab. **AND IT IS NOT AN OVERSIGHT:** `docs/scenario-2.md:216-224` is a dated comment stating those literals are DELIBERATELY on continuation lines, naming **PR #696** which got it wrong once, and predicting this exact failure — the design decision lives in the DOCUMENT, not the script, which is why a script-side review misses it. **⚠️ THE ROW'S NUMBERS WERE STALE** (my own #909 merge added claims after filing): re-measured **52 heads / 27 wrapped / 11 with a checkable literal**, not 46/28/12; `walk-doc.sh:564`'s *46 vs 46* is stale by the same amount. **SHIPPED — the ASYMMETRIC fix:** the OFFLINE gate (`check-expect-literals.sh`) now reads continuations while the RUNTIME parser stays head-only. `walk-doc.sh` is **not in the diff**, which PROVES the runtime verdict is unchanged. 77 → **94** literals checked. An offline MISSING costs a red gate on a laptop; a runtime UNMET costs a certification row. The asymmetry is pinned by `test-expect-extraction.sh`, RED-proven in BOTH directions (widen the parser → FAIL; narrow the gate → FAIL). **THE 2 'DEFECTS' THIS ROW IMPLIED WERE NOT DEFECTS:** the VM-class literal is a **pairing drop** (named by its own fenced command, so `lit in blk` drops it and `_tot` stays 0) — ALLOWed with a reason; the Harbor TLS one was a **DOC** error, since the printer interpolates the URL mid-string, so the doc now quotes only the invariant prefix. `FLOOR` 40 → **85**, because at 40 a silent break of the new extraction drops 94 → 77 and still passes at 1.9x. **🔴 STILL OPEN:** `INDEP_E` and `check-doc-expect-leak.sh` remain head-anchored — correct under this fix, and a coupled change if the unit is EVER widened; the negation GUARD at `walk-doc.sh:816` is head-anchored too (latent: 0 negated literals on continuations today); and over-attach drags illustrative examples (`such as <version>`) into the offline contract, which is why the terminator stops at a blank line and is tested for it.
| **B206** | 🟡 **MEDIUM — an `Expect` line's NON-backticked prose is UNVERIFIABLE BY CONSTRUCTION, and `0 UNMET` reads as though it were checked. DISCIPLINE, not a gate — do NOT build one.** Found 2026-08-21 by walking my own defect. `docs/scenario-2.md:779` (shipped in **v1.2.0**, commit `61731af`) asserted *"The vCenter row stays unset: a tenant is not given vCenter credentials"*. Rows 5 and 6 rendered that row **SET** (`<hidden: not a terminal>` — the harness writes `VCENTER_PASSWORD` into every walkbox at `walk-matrix.sh:520-522`, unguarded, while the sibling Harbor injection IS scenario-2-guarded). The matrix still reported **0 UNMET**, because the checker extracts only backtick-quoted literals — that line carries **five** (`make creds-show`, `Lab access`, `VKS_USERNAME`, `SUPERVISOR_HOST`, `.env`), all present and all true, while the false sentence between them is invisible. **This is NOT B205's axis** (B205 = wrapped continuation lines): the sentence was on the HEAD line, fully in scope, and still unchecked. MEASURED: only **1 of 52** Expect heads is backtick-free, so counting backtick-free lines does not find this class — the shape is *true literals coexisting with a false clause on one line*, which needs a judgement no parser makes. ⚠️ **Two gate designs are REFUTED in advance, recorded so nobody rebuilds them:** (a) *flag an Expect line whose prose contains an assertion verb with no nearby literal* — the corpus already measures this family at ~89% false-RED, and its only remedy is deleting explanatory prose, i.e. a gate whose fix degrades the artifact; (b) *require every Expect line to be literal-only* — it deletes the reader-facing sentence that makes the claim meaningful. **The discipline instead:** when an `Expect` line asserts an OBSERVABLE, express that observable as a backticked literal (`` `<not set>` ``) so the checker can see it — and if it cannot be expressed that way, do not phrase it as a guarantee. **Fixed in place 2026-08-21:** the sentence now reads *"This document never asks you for vCenter credentials, so unless you set them yourself the vCenter row reads `<not set>`"* — conditional, and its observable is now a checkable literal. |
| **B208** | 🟠 **MEDIUM — a SECOND walked consent-gated call exists, and the certification's greenness over it rests on a TYPOGRAPHIC ACCIDENT.** Found 2026-08-21 by B195's second idea round, out of scope, as its strongest finding. `fetch-ca.sh`'s no-TTY die text names *"ONE automated path"*. There are **two**: `docs/scenario-2.md:311` runs `make fetch-argocd-ca ARGOCD_SERVER=argocd-server` with **no presence pre-check and no staged `argocd-ca.crt`**. It survives only because a sibling line in the SAME fence (`:302`) contains the live placeholder `<argocd-lb-ip>`, and `has_live_placeholder` (`walk-doc.sh:277`) is applied to the **whole block** (`:635`), so the fence is skipped wholesale. **Measured**: argocd fence = **1** live placeholder → skipped; the harbor fence (`:403-410`) = **0** → executes, and is genuinely protected by its own `[ -s "$CA" ]` branch **plus** driver staging. 🔴 **The hazard:** substituting that placeholder — an obvious future improvement, and the kind of thing a docs pass does without thinking — **un-skips the block, the die fires, and rows 5/6 go red.** **Fix:** give `:311`'s fence the same `[ -s "$CA" ]` pre-check the harbor fence has, and correct the die text's *"ONE"* to name both paths. ⚠️ **A near-miss worth keeping:** the round first concluded the die text was a FALSE FACT INSIDE A CONTROL, then checked the step headings and found it **accurate** — pre-check present, CA staged. It is **incomplete, not wrong**. Verify the negative before retracting. ⚠️ **Unverified, and the whole no-TTY argument rests on it:** `walk-doc.sh` runs one statement per `bash -i -c` (`:680`); whether `[ -t 0 ]` is FALSE under the driver is **inferred**, not executed. One `[ -t 0 ]; echo $?` inside a walked fence settles it, and it is worth doing before building anything here. | ⚠️ **PARTIALLY ADDRESSED 2026-08-21 — the pre-check SHIPPED, and its implementation round proved it does NOT close the hazard. Read this before assuming rows 5/6 are safe.** The argocd fence now carries the harbor fence's have-it-already branch, which helps a HUMAN who was sent the CA. It does **not** help a walk: **nothing stages `argocd-ca.crt`** (measured: 0 hits in both `walk-matrix.sh` and `walkbox-vm.sh`), so `[ -s "$CA" ]` can never be true there — the `else` fires, the no-TTY die fires, rows 5/6 redden. **Harbor is safe because of BOTH halves**, the pre-check AND the staging; I shipped one half of a two-half invariant. The repo had already settled this shape: `fetch-vcenter-ca.sh:108-113` records that a pre-check on an UNSTAGED file *"reddens certification rows 1-4 with no rescue branch"* — and that round removed the prompt rather than adding a branch. **Two corrections to my own filing, both measured:** the live-placeholder count protecting the block is **1, not 3** (`has_live_placeholder` strips comments first at `walk-doc.sh:278`, so the ones in my new comment are invisible to it) — the margin is 3x smaller than I claimed and rests on a single `sudo tee` line; and the row's *Unverified* flag on the TTY question is now **CLOSED** — `[ -t 0 ]` returns 1 under `walk-doc.sh`'s interactive `bash -c` with stdin from /dev/null, a pipe, or inherited. 🔴 **STILL OPEN, and it is an owner/cross-repo call:** either the driver stages `argocd-ca.crt` (mirroring `walk-matrix.sh:500` for harbor — that repo, not this one), or the argocd label follows the `fetch-vcenter-ca` precedent and drops the prompt. **Also found, unfixed:** `scenario-2.md:455` is a THIRD, prose instruction to run `make fetch-argocd-ca` that contradicts `:292`'s name-not-IP procedure; and `has_live_structure`'s own guard `has_live_placeholder` is the `printf | sed | grep -q` SIGPIPE shape (0/400 at two operating points here, positive control 200/200 - so the zeros are real, not untested).
| **B209** | ✅ **FIXED 2026-08-22 — FOUR sites reported a Harbor 401; ONE named the fix, and TWO named a remedy that CANNOT WORK in that state.** Found by the `lab-sync` idea round, which **refuted its own subject** (see B210 for what it refuted). The incident: a post-matrix box has a stale `HARBOR_PASSWORD` (the matrix installs Harbor from throwaway walkboxes and destroys them, so the credential it generated does not exist here — this recurs after EVERY run). `creds.sh` was fixed on 2026-08-20 with a `_settle_note` helper whose own comment says *"one helper, called from every arm, so the copies cannot drift"* — **but it was never called from the GATES**, which is why the operator hit it a second time. Measured: `02-env.sh` `env_validate`'s 401 arm named **no remedy at all** (8 of its 22 error arms name a `make` target; the 401 was not one); `lib/harbor.sh` `harbor_auth_report` named `make harbor-robot`, which **mints using the admin credential** (`22-harbor-robot.sh:26-27`) — i.e. the exact thing that is broken; `lib/harbor.sh` `harbor_api` named **`make kind-down`**, a KinD remedy printed on the **real-lab push path**, whose whole diagnosis (a Harbor DB surviving an earlier local install) is a statement about KinD. **Fix:** one `harbor_settle_note()` in `lib/os.sh` — **not** `lib/harbor.sh`, because `creds.sh` sources `os.sh`+`apps.sh` and `02-env.sh` sources `os.sh`+`tls.sh` and **neither sources `harbor.sh`** (measured: their `harbor.sh` grep hits are COMMENTS), so defining it there is `command not found` under `set -euo pipefail` on the 401 path only — the trap `os.sh:1699` already records for `harbor_scheme`. All 9 scripts that DO source `harbor.sh` also source `os.sh` (measured 9/9). The `kind-down` advice is now conditioned on `VKS_STATE_KIND` (stamped only by `05-kind-up.sh`, so a real lab defaults to 0) while the real-lab remedy prints **unconditionally** — a stale KinD overlay then costs one extra paragraph rather than withholding the only fix that applies. The note also states that `harbor-admin-password` needs the **Supervisor** kubeconfig, so a TENANT must REQUEST instead (RULE ZERO-B). **Also fixed the 20-minute gap:** `install-all` was already gated (`preflight`→`lab-preflight`→`harbor_auth_report`, `24-lab-preflight.sh:163`) but `make mirror` was not, so a rejected credential surfaced at `mirror-push` AFTER the pull; new `make harbor-auth-check` is a prereq of `mirror` and **deliberately NOT of `mirror-pull`**, which the sneakernet internet box runs with no Harbor at all. RED-proven: 9 scope/behaviour assertions + 6 in `test-harbor-auth-gate.sh` (auto-discovered by the `test-*.sh` wildcard); `test-creds-show` 42, `test-harbor-auth-report` 17, `test-env-validate` 5 all still green. |
| **B210** | 🔴 **HIGH, UNFIXED — `supervisor_kubeconfig()` silently accepts a GUEST kubeconfig, so an empty result reads as "Harbor is not installed" when Harbor is running.** Found 2026-08-22 by the `lab-sync` idea round and **measured on the live lab**, not inferred. `lib/os.sh` falls through to `${KUBECONFIG:-}`, which from scenario-1 Step 6 onward is the **guest** cluster; there is **no Supervisor-vs-guest discriminator anywhere in the repo** (grepped: 0 hits). Ran the exact probe `28-harbor-admin-password.sh:132-133` makes, against the live guest: `kubectl get ns -l appplatform.vmware.com/serviceId=harbor -o name` → **rc=0, EMPTY**. Because rc=0, the whole `classify_kube_failure` ladder at `:134-158` is **bypassed** and execution reaches `:159` — *"ONLY NOW is an empty result a fact about the world"* — so it dies with **"no Harbor Supervisor Service on this Supervisor — install it (Step 4) or ask your platform admin"**: a confidently wrong error telling the operator to install a Harbor that is running. That is the exact failure class the file's own comment at `:120-127` says it exists to prevent. **Denominator: 10 production scripts share the resolver** (`08`,`09`,`24`,`25`,`26`,`27`,`28`,`98-uninstall-all.sh`,`creds.sh`,`argocd-password.sh`). Currently masked on this box only because `KUBECONFIG=./secrets/vks.kubeconfig` does not exist, so the chain falls through to the right `die`; it fires the moment `KUBECONFIG` points at a live guest. **Fix needs its OWN idea round** — asserting a Supervisor before trusting an empty result is a new control, and the cheapest discriminator is a Supervisor-only API group/CRD, not a namespace count. Measure the false-block rate first. **PARTIALLY ADDRESSED 2026-08-22 — the row stays 🔴 UNFIXED, deliberately.** Three CLASSIFY-not-GATE changes shipped: (a) `supervisor_kubeconfig_candidates()` moved `${KUBECONFIG:-}` from position 5 of 6 to genuinely LAST, making its own header true — a GUEST kubeconfig had outranked a real SUPERVISOR one at `${VKS_LAB_STATE_DIR}/kubeconfig`; (b) `09-argocd-address.sh` stopped merging streams (`2>&1`, which `classify_kube_failure`'s header forbids outright) and now refuses to burn 900s printing "the ArgoCD instance is still reconciling" about a cluster it is not pointed at; (c) `28-harbor-admin-password.sh` appends a HINT (never a block) when ZERO serviceId-labelled namespaces exist, distinguishing "a real Supervisor with no Harbor yet" from "you handed me a guest kubeconfig" — the pinned `install it (Step 4)` message is preserved verbatim so `test-harbor-admin-ns-classify` stays green. **WHY IT IS STILL UNFIXED:** the implementation round measured that with ONLY `$KUBECONFIG` set and no other candidate present — **the DEFAULT tenant posture per RULE ZERO-B** — the resolver still returns the guest as "the Supervisor". The reorder only changes which of TWO EXISTING files wins. Of ~11 production consumers, only `09` gained the diagnosis and only `28` gained the hint; the other nine are untouched. The ORIGINAL fix (assert the kubeconfig IS a Supervisor) remains **REFUTED** — no Supervisor-only CRD is reliably readable (`supervisorservices` returns "No resources found" WITH services serving; `serviceinstallconfigs` is Forbidden even to sso:Administrator), and an assert would false-block a fresh Supervisor before Step 4, which is the state scenario-1 puts every new operator in. |
| **B211** | ⛔ **REFUTED 2026-08-22 — do NOT build `make lab-sync`.** Proposed as a one-shot "re-derive every lab-derived value in `.env` after a re-cut". Killed on four independent grounds by its own idea round. (1) **Its premise is false for the path that matters**: `install-all` ALREADY fails in seconds via `preflight`→`lab-preflight`→`harbor_auth_report`, and says so in as many words. What was missing was the NAME OF THE FIX at the three gates that detect it — that is B209, and it is done. (2) **The CA half is refuted in writing, twice, by the code**: `29-ca-status.sh:18-23` — *"IT DETECTS; IT NEVER FETCHES. Deliberate… a CA taken over the connection it is meant to protect is not authenticated by the act of downloading it. Automating the fetch would remove the only step that makes it trustworthy"* — and `fetch-ca.sh:240-249` **prompts**, dying with no TTY. (3) **It fails on its SECOND run by construction**: `22-harbor-robot.sh:210-212` publishes the robot pair over `HARBOR_USERNAME`/`HARBOR_PASSWORD`, so the next run's `28-harbor-admin-password.sh:78-89` sees a `robot$` user and **dies** refusing to downgrade a least-privilege setup, while `22:153` **dies on 409** because Harbor shows a robot secret once. A recovery target that works exactly once is not a recovery target. (4) **`env-populate` cannot be reused for it**: it writes only over placeholders (`is_placeholder`, `02-env.sh:90,161-166`) and is therefore **structurally incapable of re-deriving a STALE value** — by design. Any sync would need the opposite, more dangerous, overwrite-stale predicate. Two corollaries worth keeping: `secrets/harbor-robot.env` is **read by nothing** (grepped every consumer — it is archival; the live credential is in `.env`), so stamping or deleting it fixes nothing; and **age is not the signal** — `29-ca-status.sh:12-16` measured a 6-hour-old CA already dead beside a 12-hour-old kubeconfig perfectly current. |
| **B212** | ⚠️ **CORRECTED 2026-08-22 — MY CONSEQUENCE CLAIM WAS FALSE; READ THIS ROW TO ITS END BEFORE ACTING.** The measurement below is real but was taken on an operation `make mirror` NEVER PERFORMS. **MEASURED 2026-08-22 — a MULTI-PLATFORM `crane copy` of a large image into the lab Harbor WEDGES UNBOUNDED; a SINGLE-PLATFORM copy of the same image succeeds.** Found while pre-flighting the six-app plan's new base images. `rust:1.97-alpine` failed **4 of 4** attempts (one ad-hoc + three under a 900s cap, two of which the cap had to KILL); `node:24-alpine`, `python:3.14-slim`, `dotnet/sdk:10.0-alpine` and `dotnet/aspnet:10.0-noble-chiseled` all mirrored INTACT. **Root cause is NOT size, quota or platform count** — three hypotheses measured and killed: Harbor's `cicd` project quota is **UNLIMITED** at 4.75 GB used; `python:3.14-slim` has **16** platforms to rust's 6 and passed; and the discriminating run settled it — `crane copy --platform linux/amd64 rust:1.97-alpine` returned **rc=0** with **0** closed-connection errors and validates **INTACT**, while the multi-arch run logged **3× `use of closed network connection` on `PATCH /v2/cicd/rust/blobs/uploads/<uuid>`**. So a sustained multi-platform upload session gets its connection SEVERED somewhere in Harbor's ingress path, and `crane` then retries and HANGS rather than failing — measured at **0 bytes of I/O per 12s for 30 minutes**, which neither the log tail nor its mtime could reveal (only `/proc/<pid>/io`, sampled twice). Nothing in the stack ever ends it: `mirror_retry` (`scripts/lib/mirror.sh:89`) retries a command that RETURNS. Also visible and BENIGN: 16× `UNAUTHORIZED ... library/rust, action: pull` — a cross-repo blob MOUNT from the Docker Hub source path that Harbor refuses, after which crane correctly falls back to a full upload. **Consequences:** (a) the Rust app's builder base hits this in Stage 2 of the six-app plan, so it is not a preflight curiosity; (b) `make mirror` has no wall-clock cap at all, so the same wedge is unbounded there. **Not fixed.** Options, none yet chosen: cap each `crane copy` with `timeout` inside `mirror_retry` (turns an unbounded hang into a retryable failure — measured to work); mirror large images per-platform; or raise the ingress/proxy timeout on Harbor's side, which is a platform-team ask, not ours. **THE CORRECTION (idea round, 2026-08-22, re-verified by me):** the wedge was measured with **`crane copy`**, which appears **0 times in any script** (`grep -rn 'crane copy' --include='*.sh' scripts/` → 0). `make mirror` runs **`crane pull --format=oci`** then **`crane push`**. Worse for my claim: `mirror_platform_arg` (`lib/mirror.sh`) returns **`--platform linux/amd64`** for a TAG-based entry — executed and confirmed: `rust:1.97-alpine` → `[--platform linux/amd64]`, `python:3.14-slim` → same, a digest-pinned ref → `[]` (all-arch). So a rust base added to `images.txt` the way 10 of 11 entries already are would mirror **single-platform — exactly the condition I measured as rc=0, 0 severed connections, INTACT.** **Consequence (a) — "the Rust builder base hits this in Stage 2" — is therefore UNPROVEN and probably FALSE, and I had reported it to the owner as a blocker. It is not one.** What SURVIVES: (i) there is still **no wall-clock bound anywhere** in the mirror path, so a hang is unbounded wherever it can occur; (ii) ~39 of ~51 refs are digest-pinned and therefore take the **all-arch** path, so reachability is not zero — but those are small Tekton controller images, and **whether the wedge is driven by upload VOLUME (small ⇒ safe) or SESSION DURATION (small ⇒ not safe) is UNMEASURED.** That one experiment decides whether any bound is needed, and until it runs, building one is premature. (iii) It is a **LIVENESS** bug, not a correctness one: crane pushes the manifest LAST and `23-mirror-verify.sh` runs `crane validate --remote`, which fetches every layer blob — so no false green is reachable. **Design notes if a bound is ever built:** use **`-s KILL`**, never `--signal=KILL` (busybox rejects the long form with rc=1 and never runs the command, which would burn all 5 `mirror_retry` attempts instantly); it must be `mirror_retry N run timeout -s KILL <N> crane …` because `run` is a shell FUNCTION and `timeout run …` fails rc=127; and `rm -rf "$dst_dir"` at `10-mirror-pull.sh:112` sits **OUTSIDE** `mirror_retry`, so killed attempts leave orphaned temp blobs that survive the successful retry AND ride across the air gap inside `bundle/` — that `rm` must move INSIDE the retried command FIRST, or a bound trades a hang for bundle bloat. |
| **B207** | ✅ **MEDIUM — `make creds` reports a FLOW it has already proved cannot authenticate, and then prescribes a remedy that cannot run.** Reported by the owner 2026-08-21 on the post-matrix host; every FACT in the render is correct, the GUIDANCE is not. MEASURED on that box: the Context block prints `flow : real VKS lab (VKS_AUTH_METHOD=vcf)` while `VCF_CLI_VSPHERE_PASSWORD` — the one credential the `vcf` method requires — is **NOT SET**, and the `Lab access` table two sections below prints exactly that (`<not set>`). The report holds both halves and never joins them. Compounding: the SSH trailer emits `Guest-node SSH password NOT read: no Supervisor kubeconfig — run: make vks-login`, but `vks-login` under `VKS_AUTH_METHOD=vcf` needs that same unset credential, so the prescribed next step **cannot succeed** — the *advice-attached-to-an-impossible-state* class (`gates.md`: advice must be guarded on the FINDING, and you must ask what happens if the operator DOES what you told them). Also measured on that box, and NOT a defect: the Harbor password rendered from `.env` is **dead** (`make env-validate` -> `Harbor reachable` then `Harbor rejected HARBOR_USERNAME/HARBOR_PASSWORD (HTTP 401)`); `creds.sh` never authenticates by design and says so in its own trailer, so that is correct behaviour, not a bug. **Design sketch (NOT implemented — Rule Zero: this changes what the tool ADVISES, so it needs an idea round first):** when the selected `VKS_AUTH_METHOD` is missing its required credential, say so ON the flow line, and make the SSH remedy name the prerequisite (`run: make vks-login — needs VCF_CLI_VSPHERE_PASSWORD, currently unset`). ⚠️ Two traps for whoever builds it: the required-credential map is per-method (`vcf` -> `VCF_CLI_VSPHERE_PASSWORD`, `vsphere` -> `VKS_PASSWORD`, `kubeconfig` -> none) and an enumerated map ROTS — derive it from `30-vks-login.sh`'s own dispatch or gate that they agree; and the note must stay SILENT when the credential IS set, pinned by a healthy fixture, or it becomes the false-advice class it exists to remove. | ✅ **SHIPPED 2026-08-21 — and its idea round refuted the design in four places, so read this, not the row above.** **(1) My requirement map was WRONG on all three arms.** `VCF_CLI_VSPHERE_PASSWORD` only **WARNS** (`30-vks-login.sh:350`); the `vcf` arm **dies** on `SUPERVISOR_HOST` (:59) and `VKS_CONTEXT_NAME` (:60), `kubeconfig` dies on a non-empty `KUBECONFIG` FILE (:43) — not “none” — and `vsphere` has **five** `:?` (:462-466). Naming the password would have sent the operator to the **third** blocker while vks-login dies on the **first**. **(2) “Derive the map from the dispatch” is NOT TRACTABLE** — 6 of 11 mentions in that file are COMMENTS, one a NOT-WIRED design note (:309-311) a deriver reads as the wiring (the docstring-matching class). So the map is **hand-typed** and `make check-vks-login-requires` asserts it agrees, keyed on the **usage form** `${VAR:?`, never the bare name, with a vacuity guard on both sides. RED-proven 3 ways (new requirement / stale entry / blind extractor). It discloses in its own header that it compares the **union**, NOT the per-arm assignment. **(3) NOT on the flow line** — that line carries a dated in-file directive (`creds.sh:347-362`, *“IT MUST NOT CLAIM WHAT IS INSTALLED”*) and is pinned by STATE 8; it also consults the method in only **1 of its 4 branches**, so a real lab *with* an overlay would never have been covered. The note is a trailer under the Lab-access table instead. **(4) Phrased as an OBSERVATION** (*“not set in .env or in this environment”*), not the prediction *“cannot authenticate”* — `VAR=… make vks-login` authenticates fine. **STATE 10** pins all three properties, including **SILENCE on a healthy box**, RED-proven by making the note fire unconditionally. **Bonus (F6), a real bug the round found while checking my counter-case:** `30-vks-login.sh:351` warned *“this run will PROMPT”* while `:380` applies `</dev/null` **unconditionally** — it cannot prompt; the two lines contradicted each other. Corrected. ⚠️ **TWO BUGS INSIDE THE FIX ITSELF, both caught only by RED-proving — record them, they are the reusable part.** **(a) The second draft went SILENT on the operator's exact box.** Having correctly learned that `VCF_CLI_VSPHERE_PASSWORD` only WARNS, I left it out of the map entirely — so on a box where every `:?` requirement IS met and only the password is missing (which is precisely what was reported), `_first_unmet` returned nothing and the note never printed. **The whole defect survived its own fix.** Re-measured: there is **no short-circuit** between the warn (`30-vks-login.sh:351`) and `vcf context create … </dev/null` (:380), so the password IS fatal — at a different PLACE, not to a lesser degree. It is now last in the `vcf` arm with its own reason string, and STATE 10's assertion (b), which had been written as *must NOT name it*, is **inverted** and pinned in both directions. **(b) My own gate shipped this repo's `check-grep-q-pipe` defect.** `sed … | grep -q` under `set -uo pipefail`: `grep -q` exits at the first match, `sed` takes SIGPIPE, `pipefail` propagates, and the `if !` takes the WRONG branch — **non-deterministically**, which is why the same unchanged tree passed once and failed once and briefly read as a corrupted restore. Fixed with a herestring; **20/20 clean**. The same round found the `</dev/null` assertion was **BLIND**: the alternation `'vcf context create .*</dev/null|</dev/null'` matched any of the **4** other occurrences in the file, so deleting the one that matters passed. Now joins continuations and requires both on the same LOGICAL line — RED-proven with the other four still present.
| **B102-fix** | ✅ **CLOSED 2026-08-19 — all four lines settled, 2 SHIPPED and 2 marked HUMAN-ONLY, on its round's verdict (`refuted`: 2 of my 4 candidates were unsafe, 1 was a silent no-op, and it found the literal I could not).** **SHIPPED `L507`** — `conditions hold AND every expected node is Ready`, from `26-vks-cluster-status.sh:322`, which fires **only on the wait loop's success break**, so it carries the claim's own semantics and discriminates in both directions (round measured it with a stub kubectl: 4 conditions True + 3/3 Ready → rc=0 literal PRESENT; `Available=False` → rc=1 literal ABSENT). **SHIPPED `L899`** — `Harbor`, `ArgoCD`, `Tekton`, all printed by unconditional top-level `add_row` calls (`creds.sh:434-443`); **`Gitea` is deliberately NOT backticked — 5 characters, it dies in the `len<6` filter**, and counting it would be self-deception. The same edit **fixed the claim itself**, which was false in 2 of 4 provenance states: there is a third label (`DEFAULT`) the sentence denied. **MARKED HUMAN-ONLY `L523`** — `INTERNAL-IP` was refuted twice over: it is a **fake-green** (the header prints whenever the node list is non-empty, so it passes on a cluster where every node is NotReady, while *newly looking measured*), and it would have **reddened the existing `check-expect-literals` gate**, which requires every Expect literal to be greppable under `scripts/` — kubectl's table printer is server-side, so `grep -rlF INTERNAL-IP scripts/` is **empty**. No `>=6`-char token in that output means "ready", so no literal can carry the claim. **MARKED HUMAN-ONLY `L928`** — see the correction above: the block is skipped on every row. ⚠️ **MY FIRST ATTEMPT AT THE TWO HUMAN-ONLY NOTES RE-CREATED THE EXACT DEFECT THEY DESCRIBE.** I wrote the notes with backticks — `` `INTERNAL-IP` ``, `` `walk-doc.sh:122` `` — and the parser reads a backticked token on the head line as **a literal to match**, so the explanation *became* two new unmatchable claims. `check-expect-literals` went **rc=2, "2 MISSING"** and named `INTERNAL-IP`. ⇒ **a "this cannot be checked" note must contain NO backticked token of 6+ characters**, or the note is itself a claim. De-backticked; gate back to rc=0. **Verified after the edit, per the round's slack-zero warning** (`walk-doc.sh:565-570` asserts `EXPECT_LINES_PARSED >= INDEP_E` with **zero** slack, so a dropped claim REFUSES the walk before it starts): a dry walk reports **"34 Expect: lines in the doc, 34 parsed"** — 34, not 33, because `scenario-1.md:56` walk-includes `common-bootstrap.md`, which contributes one. **Every added literal is on the `**Expect:**` HEAD line**, because `walk-doc.sh:440` captures only the head and a literal on a wrapped continuation is invisible (B145) — which would have been a second, quieter silent no-op for both L899 and L507. |
| **B196** | ✅ **THE RATCHET IS FIXED (2026-08-19); WHAT REMAINS IS AN OWNER POLICY DECISION, NOT CODE.** Its idea round returned `refuted-in-part` and **my premise was wrong in three ways.** ✅ **SHIPPED — the round's own cheaper alternative, which needs no policy call:** `VKS_INSECURE_SKIP_TLS_VERIFY` added to `load_env`'s EXISTING snapshot list **and** `check-env-clobber.sh`'s `SELECTORS` **in one commit** (the B188-F1 trap: snapshot-alone ⇒ `FATAL: SELECTORS has FALLEN BEHIND`). This kills a **measured ratchet** — before it, caller `=0` with `.env=true` resolved **`true`** with no per-run way back — and it makes a **documented promise true again**: `.env.example:1209-1210` says verbatim *"Commented so the code default (false) applies and a `make vks-login VKS_INSECURE_SKIP_TLS_VERIFY=1` / `.env` override is not clobbered"*, and that was **false**. `make check-env-clobber` **rc=0 after the change** — settling the round's residual #2 by execution rather than the source-read it offered. Pinned by `test-vks-insecure-snapshot.sh` (5 arms). 🔴 **THE BLOCKER, AND IT IS YOURS: is `.env` a legitimate channel for this variable?** I proposed the prefix-only treatment (as shipped for `ARGOCD_REGISTER_INSECURE`). **The round refuted it as HIGH/blocking**, because unlike the precedent, `.env` is the **documented, invited** channel here in **three** places — `.env.example:1209`, `docs/vks-authentication.md:23` (a table column literally headed *"Inputs (`.env`)"*) + `:35`, and `docs/lab-validation-plan.md:268`. Measured: with the documented `.env` line and no `secrets/supervisor-ca.crt`, prefix-only makes `31-fetch-argocd-kubeconfig.sh` fall to its `else` and `die` **demanding the value the operator had just set** — byte-for-byte the bug `scripts/lib/os.sh:139-146` records as **ALREADY FIXED**, arriving by a new route. ⇒ reclassifying is legitimate ONLY as an explicit decision that moves **all four** documents *and* `31:113`'s die text in the same commit. ⚠️ **Three corrections to my own filing, all measured:** (1) **THREE read sites, not two** — I missed `30-vks-login.sh:473` (the `vsphere` arm, `--insecure-skip-tls-verify="$(bool_word …)"`, executed alongside `KUBECTL_VSPHERE_PASSWORD`); the snapshot covers it automatically, a per-call-site fix would not. (2) **The shared-helper dilemma was a FALSE DILEMMA** — the snapshot never needed to precede *sourcing* `lib/os.sh`, only the *call to* `load_env`, and both consumers have that window; the list-based fix sidesteps the question entirely. (3) **Snapshot with `${V:-}`, never `${V:-0}`** — a `0` makes the value SET where it is currently UNSET, changing any future `[ -n … ]` read at all three sites. 🟢 **The SSO-lockout worry is answered and is NOT a stopper:** the repo's own measurement (`30-vks-login.sh:283-287`, 2026-08-04) is that with neither TLS option set the CLI fails closed on `x509: certificate signed by unknown authority` and **costs NO auth attempt — it fails before authenticating**; this change can only make TLS fail earlier. The `vsphere` arm is `inferred`, not measured. 🟢 **The six-row matrix and both walkthroughs are unaffected** — `make fetch-supervisor-ca` (scenario-1:265, scenario-2:129) runs BEFORE `make vks-login` (:274), so `vks_ca_default` populates `VKS_CA_CERT_FILE` and the CA branch wins; breakage would be scoped to `docs/vks-authentication.md`'s `vcf` recipe and `lab-validation-plan.md` step 9. ⚠️ **Unflagged fragility that ships either way:** `check-env-clobber`'s coverage of this variable hangs on the **wording of an error message** — arm (b2) fires only because `31-fetch-argocd-kubeconfig.sh:113` happens to contain the literal `VKS_INSECURE_SKIP_TLS_VERIFY=true` **inside a `die` string** (`check-env-clobber.sh:250` says so itself). Reword that message and the gate goes silently blind. **Class:** measured repo-wide, this is **1 of 1** for the `is_true "${V:-}"` shape — no sibling waits. **Do NOT read this as closing B196's wider 12-toggle table:** `ALLOW_PUBLIC_CHARTS`, `ENGINE_SKIP_CA_RED`, `BUNDLE_SKIP_STATIC_CHECK`, `BUNDLE_TOOLS_FORCE`, `SKIP_DOTENV` remain unhandled. **Residual:** whether `kubectl vsphere login --insecure-skip-tls-verify=false` costs an SSO attempt on a TLS failure is unmeasured — settle on a lab with a deliberately wrong anchor, reading the failed-attempt counter before/after, and **run it once**. ⏸️ Original follows. — 🔴 **HIGH — `VKS_INSECURE_SKIP_TLS_VERIFY` is armable from a personal `.env`, disables TLS verification on a connection that SUBMITS A CREDENTIAL, and the clobber gate is STRUCTURALLY BLIND to it.** Surfaced by the B188 idea round (`adversary-bash-git-cli`, 2026-08-19) as the class denominator I had not swept: of **12** weakening toggles, 3 take the `load_env` snapshot (closes threat A only), 2 take the pre-`load_env` pattern (closes A **and** B), and **7 are unhandled** — `ARGOCD_REGISTER_INSECURE` (now fixed, B188), `ALLOW_PUBLIC_CHARTS`, `ENGINE_SKIP_CA_RED`, `BUNDLE_SKIP_STATIC_CHECK`, `BUNDLE_TOOLS_FORCE`, `SKIP_DOTENV`, and **this one**. It is the only one of the seven that is not bookkeeping. **Why it is worse-covered than the one I just fixed:** it reads **`${VKS_INSECURE_SKIP_TLS_VERIFY:-}`** (empty default, via `is_true`), and the clobber gate's arm (d) keys on a boolean default — so arm (d) **cannot see it at all**; `check-env-clobber.sh:248-250` records in its own words that it is caught only *"INCIDENTALLY"*, by arm (b2), and only because `31-fetch-argocd-kubeconfig.sh` happens to have the right shape. **Two consumers, verified:** `30-vks-login.sh:275` and `31-fetch-argocd-kubeconfig.sh:96`. **The severity is the repo's own words, not mine:** `30-vks-login.sh:271` tells the operator *"Do NOT reach for `VKS_INSECURE_SKIP_TLS_VERIFY`: a credential is submitted over this connection"*, and `:278` repeats it — so the code already classifies this as the never-legitimate category, while leaving the `.env` channel wide open. ⚠️ **NOT a copy-paste of B188's fix, which is why it is filed rather than shipped:** two consumers means the snapshot must be **hoisted into a shared helper** (duplicating it in both scripts is the enumerated-list rot that guarantees one gets fixed and the other does not), and one of those consumers is `30-vks-login.sh` — **the VKS authentication path**, which the standing SSO-lockout constraint says must not be touched casually. It needs its own idea round and must not land the night before a lab cut. ⚠️ **Do NOT "fix" it by hardcoding a secure default:** both call sites are legitimate escape hatches for a lab whose Supervisor cert has no usable anchor yet; the goal is that the choice is per-run and loud, exactly as B188 made `ARGOCD_REGISTER_INSECURE`. **Also unswept:** `${V:-}`-shaped toggles beyond this one — `grep -rnE '\$\{[A-Z_]+:-\}' scripts/` cross-referenced against the 12-row table settles it. | ✅ **F9 SHIPPED 2026-08-21 — and it was decision-INDEPENDENT and had to land FIRST.** The gate's coverage of this variable hung on **the wording of an error message**: `check-env-clobber.sh` says so itself — arm (d) could not see `is_true "${V:-}"` (an EMPTY default, so the boolean-literal alternation cannot match), so it was caught *INCIDENTALLY* by arm (b2), and only because `31-fetch-argocd-kubeconfig.sh` **happens** to contain an env-prefix line. Rewording that die — which the prefix-only migration **requires** — would have made the gate go **silently blind on the repo's single most dangerous toggle**, i.e. the migration commit would have disabled its own guard. Arm (d) now also matches the `is_true` shape (**measured 1 of 1 repo-wide**, so no sibling churn and no return of the naive form's 97% false-RED). **RED-proven in the decisive configuration**: armed uncommented **with arm (b2)'s env-prefix line REMOVED**, it still fires. ⚠️ **AND MY OWN READING OF THIS ROW WAS OVERSTATED.** I told the owner *"(b) is refuted by the row's own measurement, so fix = (a)"*. It is not. That measurement refutes **prefix-only done HALFWAY**; if all five sites move in one commit the prefix form **works** — which is exactly what the shipped snapshot fix guarantees. The honest framing is **persistent vs per-run**: (a) is defensible and much cheaper; (b) is coherent and unrefuted, and its strongest argument — which this row's framing suppressed — is that a `.env` value **silently disables TLS verification on a credential-bearing connection on every future run**, while a prefix is per-run and visible in the command. `30-vks-login.sh:271` already says *"Do NOT reach for VKS_INSECURE_SKIP_TLS_VERIFY: a credential is submitted over this connection"*, and `ARGOCD_REGISTER_INSECURE` set the prefix-only precedent. 🔴 **THE POLICY CALL REMAINS THE OWNER'S**, and it is now correctly stated. ✅ **OWNER DECISION 2026-08-21: OPTION (a) - `.env` REMAINS a legitimate channel for `VKS_INSECURE_SKIP_TLS_VERIFY`. ROW CLOSED, no further code.** The four documents that invite it (`.env.example:1209`, `docs/vks-authentication.md:23` + `:35`, `docs/lab-validation-plan.md:268`) and `31-fetch-argocd-kubeconfig.sh:113`'s die text all STAY as they are - and that consistency is the point: the measured failure mode was prefix-only done HALFWAY, where the script dies demanding the value the operator had just set. ⚠️ **THE ACCEPTED COST, recorded so it is a decision and not an oversight:** a `.env` value is **PERSISTENT** - it silently disables TLS verification on a credential-bearing connection on **every future run**, where a prefix would be per-run and visible in the command. `30-vks-login.sh:271` still says *"Do NOT reach for VKS_INSECURE_SKIP_TLS_VERIFY: a credential is submitted over this connection"*, and that advice stands - the decision is about which CHANNEL is legitimate, not about the toggle being safe. **Option (b) was never refuted** (my earlier claim that it was is corrected above); it is available unchanged if the persistence ever becomes unacceptable, and F9's gate fix means the migration would no longer disable its own guard.
| **B195** | ✅ **HIGH — only 2 of the 4 `fetch-*-ca` producers have a consent gate, and the two WITHOUT it are exactly the pair on the SSO administrator path.** Found by B193's idea round (2026-08-19) as an out-of-scope finding and re-verified here by counting `ca_pin_verdict` call sites: `fetch-ca.sh` **3** (it backs `make fetch-harbor-ca` and `make fetch-argocd-ca`), `fetch-supervisor-ca.sh` **0**, `fetch-vcenter-ca.sh` **0**. ⇒ the two producers with the gate anchor the Harbor and ArgoCD credentials; the two without anchor the **vCenter SSO administrator** path. `fetch-ca.sh:186-258` does what the missing pair do not: a NOT-AUTHENTICATED banner to **stderr** (deliberately, so a `>log` redirect cannot hide it), a statement of what a wrong anchor costs in credentials, an out-of-band confirmation prompt on a TTY, and a **die** with no TTY — *"refusing to write an unauthenticated trust anchor with no terminal to confirm it on"*. Both weaker siblings print a fingerprint and a `CONFIRM this out of band` line and then write the anchor regardless, which is advice rather than a gate. ⚠️ **Do NOT fix vCenter alone.** They are the same ~15 lines (both already source `lib/tls.sh`), and fixing one leaves the other looking protected by association — the exact half-a-class shape this file warns about. 🔴 **It carries the SAME owner decision as B193 and must not land without it:** `make fetch-supervisor-ca` is a walked step in scenario-1 (§3) just as `fetch-vcenter-ca` is in §1b, and `walk-doc.sh` runs fences **non-TTY**, so a `fetch-ca.sh`-style gate dies there and reddens matrix rows 1-4 unless the harness supplies the digests. `fetch-ca.sh` already reasoned about precisely this and concluded the walk SHOULD stop, because *a green walk that pinned a stranger's certificate would be worse than a red one*. **Not started.** | ⚠️ **TWO OF THIS ROW'S OWN FACTS CORRECTED 2026-08-21, measured before implementing.** **(1) The SSO-lockout framing is MISATTRIBUTED.** Neither ungated producer authenticates: `fetch-vcenter-ca.sh` downloads `https://<host>/certs/download.zip` ANONYMOUSLY with `curl -sk` (:43), and its only `password` hit is a **comment** (:5) saying it deliberately does not send the admin password to a peer whose identity it has not checked — the find-the-string-not-the-kind-of-thing trap, again. `fetch-supervisor-ca.sh` has **zero** auth references. So *"the account that locks after 3 binds"* is not the mechanism here. **The severity SURVIVES by a different and better-stated route:** these scripts write the trust anchor that LATER steps use, and those steps DO submit the vCenter administrator credential — which is exactly what `fetch-ca.sh`'s own banner says (*"Anything trusting this file will submit <LABEL> credentials over it"*). A bad anchor here means the admin password is later sent over a connection verified by a certificate an attacker supplied. **(2) A no-TTY `die` is SAFE — measured, and this was the blocking unknown.** Both producers are reachable ONLY through their own `make` target (`Makefile:555-560`); **no** workflow, e2e, `install-all` or walk path invokes either, and **no** make target lists either as a prerequisite. So adding the gate cannot redden a green automated flow. Both already compute the fingerprint (5 and 6 references) and already print a confirm-out-of-band line — they simply do not GATE on it, which is what makes the fix an extraction rather than new behaviour. ⚠️ **PARTIALLY SHIPPED 2026-08-21, and the row's OWN SHAPE was refuted — read this before building the rest.** **(1) SHIPPED — F1.5, the real defect, and neither the row nor I was looking at it.** `30-vks-login.sh:157-165` is DELIBERATELY SILENT on "no pin", and its reason is written out: *"the operator has already pointed VKS_CA_CERT_FILE at a file they obtained THEMSELVES — which is the out-of-band path"*. **That premise is FALSE whenever `make fetch-supervisor-ca` wrote the file**: `fetch-supervisor-ca.sh:33` pulls it off the wire with `curl -sk`, which is *exactly* the trust-on-first-use case the same comment says this is NOT — and the consumer could not tell the two apart, so the anchor most likely to be an interceptor's inherited the silence meant for the anchor least likely to be. The producer now records the **digest** it wrote (`<ca>.fetched`) and the consumer warns ONLY while the file still IS those bytes. Digest, not a flag, so an operator who replaces the file by hand does not inherit a stale verdict. `scripts/test-ca-provenance-marker.sh` pins all four states (fetched-unchanged → WARNS; no marker → SILENT; stale marker → SILENT; empty marker → SILENT) plus both wiring directions, each RED-proven. **(2) NOT SHIPPED — extracting fetch-ca.sh's consent block, and it is REFUTED as specified.** **F1.2 (CRITICAL to the plan):** my own "no automated caller" measurement was INCOMPLETE — I scanned make targets, workflows and e2e, and missed the **walked doc fences**. `docs/scenario-1.md:201` (§1b) and `:265` (§3) run both commands UNCONDITIONALLY, and unlike scenario-2 **neither has a presence pre-check**, so the rescue branch that makes `fetch-ca.sh`'s no-TTY die harmless does not exist here. A no-TTY die would redden matrix rows **1-4** and make the 6/6 certification unreproducible. **F1.3:** the extracted die would tell the operator to pass `VCENTER_CA_SHA256=<digest>` — a variable with **2 occurrences repo-wide, both in this backlog's prose**: zero in `.env.example`, `scripts/`, `load_env`'s selector list or `check-env-clobber.sh`. That is the fabricated-`# how:` class, reintroduced inside a security gate. **F1.4:** the two are NOT twins, so *"do not fix vCenter alone"* is backwards — the supervisor anchor HAS a gate, deferred to use time (`30-vks-login.sh:154`, `VKS_CA_SHA256`); the vCenter anchor is checked **nowhere in its entire life**. **F1.6:** the stderr-banner property is NOT missing — `_log` already writes to stderr (`lib/os.sh:81`). **Order for whoever continues:** `VCENTER_CA_SHA256` must EXIST first (`.env.example` + a `ca_pin_verdict` call + both selector lists), and the consent prompt needs an **owner decision** because of F1.2. ✅ **THE SAFE INCREMENT SHIPPED 2026-08-21, and my proposed fix was REFUTED before a line of it was written.** 🔴 **F1 (CRITICAL): the presence pre-check I proposed is a NO-OP.** It assumed the walk driver stages `secrets/vcenter-ca.pem` the way it stages scenario-2's CA. **It does not** — **measured**: `grep -c 'vcenter-ca\|supervisor-ca'` = **0** in both `walk-matrix.sh` and `walkbox-vm.sh`; the only staged secrets are `vks.kubeconfig` and `harbor-ca.crt`. On a fresh walkbox the file genuinely does not exist, so a pre-check **always takes the fetch branch**, hits the die, and rows 1-4 go red anyway — it would have added the *appearance* of protection while changing nothing. **SHIPPED (F4+F5+F6+F7), one commit:** `VCENTER_CA_SHA256` now EXISTS — `.env.example` (**COMMENTED**, matching all three sibling pins, so `set -a` cannot clobber a per-run override) + **both** selector lists (`lib/os.sh` snapshot and `check-env-clobber.sh` SELECTORS, in the SAME commit — RED-proven that updating one alone dies *SELECTORS has FALLEN BEHIND*) + a `ca_pin_verdict` call in `fetch-vcenter-ca.sh` placed **BEFORE the install and against `$match`, not after and against `$OUT`** (fetch-ca.sh's deliberate order: *"ONLY NOW does the operator's filesystem change"*). **Deliberately NO consent prompt**, which is what makes it walk-neutral: verdict **3** (no pin) returns silently — measured across all four arms (unset→3 silent, match→0, malformed→4 die, mismatch→1 die). The vCenter anchor now has **the first check in its entire life**. 🔴 **STILL OWNER-BLOCKED — the consent prompt**, and the choice is now narrow and clear: either the walk driver supplies `VCENTER_CA_SHA256=<digest>` (**cross-repo**, in `nested-vsphere-lab`, which keeps the walk EXERCISING the command *and* adds pin coverage), or accept that the walk stops, per the die's own reasoning (*"a green walk that pinned a stranger's certificate would be worse than a red one"*), at the cost of the 6/6 certification. **Staging the file instead is the wrong answer** — it would delete the walk's coverage of `fetch-vcenter-ca` entirely. ✅ **OWNER DECISION 2026-08-21: NO CONSENT PROMPT. ROW CLOSED.** The pin check shipped above is the whole of this row: `make fetch-vcenter-ca` now refuses an anchor that does not match `VCENTER_CA_SHA256` when an operator sets one, and is silent (trust-on-first-use, unchanged) when they do not. The vCenter anchor went from **checked nowhere in its entire life** to checked at write time, at zero cost to the certification. ⚠️ **THE ACCEPTED RESIDUAL:** with no pin set, the only thing between an operator and an interceptor is the CONFIRM-out-of-band warning - i.e. advice, not a gate. That is deliberate: `docs/scenario-1.md:201` and `:265` are UNCONDITIONAL walked fences whose files the matrix driver does not stage (measured), so a no-TTY die reddens rows 1-4 with no rescue branch. **If this is ever revisited, the route is cross-repo** - `nested-vsphere-lab` supplies `VCENTER_CA_SHA256=<digest>`, which keeps the walk EXERCISING the command while adding pin coverage. **Staging the CA file instead remains the wrong answer**: it would delete the walk's coverage of `fetch-vcenter-ca` entirely.
| **B194** | 🟢 **FIXED AND RED-PROVEN 2026-08-19 — and the bug is real, measured, not theoretical.** `vc_api` decided success from `%{http_code}` alone, which is what the server SAID rather than what ARRIVED. MEASURED against a raw-socket TLS fixture that sends a complete `200 OK` + `Content-Length: 5000`, delivers **9 bytes**, then closes hard: **curl prints `200`, rc `18`** (*transfer closed with 4991 bytes remaining to read*). Both arms, through the real `vc_api`: **WITHOUT the fix `rc=0 code=200`** — a SUCCESS return over incomplete JSON — and **WITH it `rc=1 code=000`**. The fix captures curl's rc and folds a non-zero into `code=000`, keeping the return-non-zero-without-dying contract several callers depend on (`deregister-supervisor-service.sh:57` branches on a 404). ⚠️ **MY FIRST FIXTURE WAS WRONG AND ITS RED-PROOF PROVED NOTHING — recorded because it looked convincing.** It used python's `http.server`, whose handler kept the connection open after the short write, so **curl HUNG and then errored**: both arms measured an identical `rc=1 code=000`, and **the control was the only thing that revealed the proof discriminated nothing**. I reported it as unproven rather than banking it; the owner said build the real fixture. The instrument check that settled it was running **bare curl** against the fixture and reading what it actually prints — `http.server` → hang, raw socket → `200` + rc 18. **Shipped as `scripts/test-vc-api-truncation.sh`** (auto-discovered by the `TEST_ALL` wildcard, `ci-tier: fast`, so no enumerated list to rot), which asserts the fixture DELIVERED a short body before judging `vc_api` — a case that cannot reach its own subject measures nothing — and carries the failed-fixture story so nobody re-simplifies it back to `http.server`. Its own RED-proof: reverting the fix fails it with the exact message. shellcheck 0 findings; `static-check` rc=0. | `vc_api` now captures curl's rc alongside `%{http_code}` and forces `code=000` when the rc is non-zero, leaving the return-non-zero-without-dying contract intact (several callers legitimately expect a 404 — `deregister-supervisor-service.sh:57` branches on it). ⚠️ **MY RED-PROOF DID NOT DISCRIMINATE AND I AM NOT BANKING IT.** I built a truncating HTTPS oracle (200 headers promising `Content-Length: 5000`, then 9 bytes and close) and measured **`rc=1 code=000` WITH the fix and the IDENTICAL `rc=1 code=000` in the CONTROL** with the fix reverted. So that fixture never produced the case this exists for — curl reporting 200 while the body is short — the proof discriminated nothing, and **only the control revealed that**. Still unmeasured: what curl actually prints for `%{http_code}` on a mid-body close; if it prints 000 there the fixture is wrong and a real one needs `--max-time` expiring mid-transfer or a chunked reply with a bad terminator, and if it prints 200 then something else here collapses it and this change may be redundant. **Shipped anyway because the RULE is independent of my fixture** — `coding-style.md`: a wrapper that NORMALISES a tool's output must preserve its EXIT STATUS too, which `vc_login` already does and `vc_api` did not — and because it is two lines that cannot make any current caller worse. **The gap is recorded at the code, not only here**, so the next reader meets it where they would rely on it. **Do not describe this as proven; build the fixture before anyone does.** Verified: `bash -n` ok, shellcheck 0 findings, `test-wcp-service` 8/8, the offline TLS oracle 15/15. ⏸️ Original follows. — 🟡 **LOW, pre-existing — `vc_api` cannot tell "HTTP 200" from "truncated after 200 headers", while `vc_login` now can.** Filed by B133's implementation round (2026-08-18) as explicitly out of scope. `vc_api` keeps `|| true` and captures no curl rc, so a response cut short after its headers yields `code=200` → `case 2*) return 0` → **the caller receives a truncated body and a success return**. This is `coding-style.md`'s *"a wrapper that NORMALISES a tool's output must preserve its EXIT STATUS too"* rule; B133 applied it to `vc_login` (with explicit 60/77/35/51 arms) and deliberately did not touch `vc_api`, whose documented contract is to return non-zero on any non-2xx **without dying** because some callers expect a 404. **Fix when it is next touched:** capture the rc alongside the code and force `code=000` when the rc is non-zero, leaving the return-not-die contract intact. **Not urgent:** it predates B133 and no caller has been observed hitting it. ⚠️ **Related residual worth reading first:** `vc_api` is called inside `$( )` almost everywhere, so its `die` exits only the SUBSHELL and the caller silently receives `""` — `04-vsphere-namespace.sh:105` is the shape, where a mid-run anchor loss would burn the full `VKS_NAMESPACE_WAIT_SECONDS` and then die naming the NAMESPACE rather than the anchor. Bounded by that budget, so it cannot hang. |
| **B213** | 🔴 **OPEN — `--security-opt seccomp=unconfined` would dissolve the whole /etc/shadow failure class.** MEASURED by `adversary-docker` 2026-08-23: `--privileged` is a BUNDLE — only its **AppArmor-unconfined** half causes the shadow bug (the HOST `unix_chkpwd` profile attaches by path with no `dac_override`), and only its **seccomp** half is needed for the userns rootless podman/dockerd requires on a host with `kernel.apparmor_restrict_unprivileged_userns=1`. With shadow forced to 0000: `--privileged` → sudo rc=1; `--privileged --security-opt apparmor=docker-default` → rc=0; `--security-opt seccomp=unconfined` alone → **sudo rc=0 AND unshare rc=0**. NOT shipped with the 0400 pin because a non-privileged container has **no `/dev/fuse`**, which `fuse-overlayfs` needs — the round proved the two blockers dissolve, not that a whole leg passes. Needs its own change plus a full `make jumpbox-matrix`. **Keep the 0400 pin regardless.** See the detail section below. |
**The rest of the real-lab unknowns** — the Supervisor topology, the `vcf` auth flow, tenant RBAC into
the ArgoCD namespace, and whether the Supervisor can route to a guest LoadBalancer VIP — are tracked in
[`docs/lab-validation-plan.md`](docs/lab-validation-plan.md), in a better form than a backlog line: each
is a numbered step with its command, its expected observable, and what to send back.

> ⚠️ **ID COLLISION — this section is the `export`/`make` PRECEDENCE fix, and it is the one
> `scripts/test-env-precedence.sh` cites.** An unrelated `| **B84** |` table row above covers the
> Broadcom portal links. Different subjects, same id; do not renumber either.

## B84 — ✅ CLOSED. `export VAR=x; make <target>` was silently defeated, and the FIX inverted `.env` vs `.env.state`

MEASURED 2026-08-09, closed 2026-08-16 (#657). GNU make lets a **makefile assignment beat the
environment**, and a plain `-include` is exactly that.

`.env` was rewritten to `?=` in 8db2da1. **The state overlay was not** — and it is the include that
matters most, because `state_set` writes exactly the SELECTOR class there: `KUBECONFIG`,
`HARBOR_URL`, `HARBOR_CA_FILE`, `VKS_CONTEXT`, `INGRESS_CONTROLLER`, `ARGOCD_KUBECONFIG`.

    export HARBOR_URL=harbor.OPERATOR-CHOSE-THIS   ->   HARBOR_URL=[harbor.from-STATE]

Not theoretical: `fetch-harbor-ca` passes `$(HARBOR_URL)`/`$(HARBOR_CA_FILE)` as **argv** to
`fetch-ca.sh`, which deliberately does not call `load_env` — so the selector snapshot cannot rescue
it, and the operator fetches a CA from the overlay's Harbor while believing they named another.

**The fix introduced its own regression, and that is the part worth remembering.** `?=` inverts
file-vs-file order: `load_env` SOURCES the files so the LAST wins, while under `?=` the FIRST wins.
Rewriting the overlay in place made a stale `.env` beat the just-discovered state. The includes are
now in REVERSE order (kind, state, `.env`), each `-include` guarded on its SOURCE existing so a
deleted `.env` cannot keep applying through its orphaned generated file.

`make test-env-precedence` — 14 cases, and it LIFTS the include blocks out of the real Makefile
rather than retyping them, so it cannot keep passing after someone reorders the real ones. Case 10
is a positive control that rebuilds the pre-fix shape and requires it to STILL defeat `export`.

## B86 — ⚠️ REFUTED AS WRITTEN. Do NOT make the real-lab path call `state_stamp`

**The row below asked for something MEASURED to destroy values.** An idea-round (2026-08-16) refuted
three of its four candidate fixes on one fact the row does not contain: **the sink is written under
TWO different API servers**, so a scalar `VKS_STATE_SERVER` cannot describe it.

| written by | under which kubeconfig |
|---|---|
| `HARBOR_USERNAME` / `HARBOR_PASSWORD`, `ARGOCD_LB_IP` | **Supervisor** |
| `INGRESS_LB_IP`, `GITEA_LB_IP` (after `make use-guest-kubeconfig`) | **guest** |

Stamping ARMS `state_check`'s mismatch refusal, which is dead code today only because the sink is
unstamped. MEASURED A/B on the same sink, only the stamp differing:

| sink state | `HARBOR_PASSWORD` | `HARBOR_URL` | `INGRESS_LB_IP` |
|---|---|---|---|
| unstamped (today) | `SupervisorHarborSecret` | `10.0.0.9` | `10.0.0.20` |
| **stamped for guest** | **LOST** | **LOST** | **LOST** |

And from a Supervisor shell — which scenario-1 says is where everything runs — `creds-show` after
stamping **keeps the banner AND loses the values**: `values-provenance: STORED`, `Harbor
https://harbor.vks.local admin <not published>`. It trades a cosmetic over-alarm for silent wrong
values on half the walk. A mirror push aimed at `harbor.vks.local` instead of the real LB is the
sharp end.

Two further findings, both measured:

- **`state_stamp` on a box with NO sink CREATES one**, producing a false `DISCOVERED` over pure
  `.env.example` placeholders — *strictly worse* than the bug this row reports: today's failure
  over-alarms, that one under-alarms.
- **The row's stated hazard is FALSE.** `state_stamp` never archives; it is a pure upsert. The real
  hazard is silent misattribution, not deletion. A wrong hazard misdirects the next session.

**THE DESIGN THAT SURVIVES — no new writes.** Split the ladder and let per-value liveness carry the
alarm: `STORED` becomes `UNSTAMPED` (expected on a lab; neutral wording) vs `MISMATCHED` (stamped
AND contradicted — the only state that PROVES staleness, and the only one that keeps the loud
banner); stop prescribing `make state-stamp` in `creds.sh` and `lib/state.sh:95` (measured: 3 warn
lines per `load_env`, i.e. per script invocation — a bigger alarm surface than the banner this row
is about, prescribing the refuted action); and extend the existing bounded TCP probe from
`INGRESS_LB_IP` to `HARBOR_URL` and the resolved ArgoCD address. No writes means `state_check`'s
refusal stays disarmed, so the CRITICAL above cannot occur.

**Settle first, in one command on a live lab** (the round could not, and if the answer is "same
endpoint" the CRITICAL collapses):

    kubectl --kubeconfig secrets/supervisor.kubeconfig config view --minify \
      -o jsonpath='{.clusters[0].cluster.server}'      # vs the same against the guest kubeconfig

Stale in the original row below: it cites `Makefile:264`; the caller is `Makefile:305`.

---

### B86, as originally written (kept for its evidence — the DESIGN in it is refuted above)

**HIGH.** `state_stamp` is called from exactly two places: `scripts/05-kind-up.sh:156` (KinD
only) and `Makefile:264` (a manual `make state-stamp`). **No real-lab install script calls
it**, and neither `scenario-1.md` nor `scenario-2.md` mentions `state-stamp`. So every
real-lab `make creds-show` — including one run minutes after a clean install — prints
`STORED` plus the five-line *"an overlay SURVIVES A REBUILD, so a password or an IP here may
be from a lab that no longer exists"* banner.

Reproduced on this box 2026-08-10: `make creds-show` printed exactly that banner, and
`scenario-1.md:628` promises the reader a state (`DISCOVERED`) the documented path cannot
reach.

**Why it matters, and it is not cosmetic:** it is alarm fatigue on the one command whose job
is to tell the operator what is true. A banner that fires on every correct install trains
them to ignore it — and then the case it exists for (a genuinely stale overlay after a lab
rebuild) is ignored too, because **the two are byte-identical**.

⚠️ **This paragraph is the part that was MEASURED FALSE.** `state_stamp` does NOT archive — it is
a pure upsert (`lib/state.sh:55-64` -> `state_set` -> `set_env_var:826-831`); a re-stamp over a
populated, mismatched sink left `HARBOR_PASSWORD`/`GITEA_CI_TOKEN` intact and created no `.stale-*`
file. `state_archive` is reachable only from `state_claim_kind`, the KinD WRITE path. And "put it
where KUBECONFIG points at the guest cluster" is precisely the stamp that loses the Supervisor half.
See the refutation above before touching this.

## B87 — ✅ CLOSED. `test-creds-show.sh` now covers the real-lab provenance path

**CLOSED 2026-08-16 — verified by RUNNING the suite, not by reading its section headers.**
States 4 and 5 exist (`test-creds-show.sh:161,183`) and one run prints:

    ok  unstamped overlay, non-KinD -> declares values-provenance: STORED
    ok  stamp MATCHES the live kubeconfig -> DISCOVERED (no KinD shortcut involved)
    ok  fully installed -> declares values-provenance: DISCOVERED (the token FLIPS)

So the branch a real lab actually reaches IS rendered, and the DISCOVERED assertion no longer
passes through the KinD shortcut. **B86 remains OPEN** and is the reason state 4 matters: nothing
on the real-lab path calls `state_stamp`, so STORED is the only branch a real lab reaches.
The original diagnosis is kept below — it is what makes state 4 worth keeping.

**Was HIGH.** Its three states are: no overlay, and two overlays that **both** set
`VKS_STATE_KIND=1` (`scripts/test-creds-show.sh:67,87`). That flag short-circuits at
`scripts/creds.sh:217`, *before* the stamp comparison at `:218` — so the `STORED` branch,
the only one a real lab ever reaches, is never rendered, and the `DISCOVERED` assertion at
`:105` passes through the KinD shortcut rather than the logic under test.

The gate written to keep `creds-show` honest *"in every state"* is blind to the state the
operator is actually in. Fix: two more states — unstamped non-KinD (expect `STORED`), and a
matching `VKS_STATE_SERVER` (expect `DISCOVERED`). Fully offline: `state_kubeconfig_server`
parses the file and never dials (`scripts/lib/state.sh:38-39`).

Both found by an idea-round `vks-adversary` on 2026-08-10 that **refuted** the proposed
`make creds-check` (measured 67% false-RED — nothing in this repo writes `/etc/hosts`, so
`*.vks.local` is NXDOMAIN by design, and §11 prints two port-forward URLs live only while a
port-forward runs). The reachability question it was meant to answer is already answered by
`verify-ingress` (Host header, no DNS, per-host body marker), now cited in §10.

## B109 — ⚠️ REFUTED. Both halves of its measurement are stale or misread; the REAL defect is fixed (#661)

**Do not build the probe.** An idea-round measured all three proposals down.

**(a) The Harbor 401 was B111**, which is fixed: the repair now verifies against live Harbor before
writing and then ASSERTS the write survived `load_env` precedence (`assert_env_effective`).

**(b) The ArgoCD 401 is DOCUMENTED CORRECT BEHAVIOUR, not a defect.** `scenario-1.md` §5 tells the
operator to run `argocd account update-password`, and says in as many words that `make
argocd-password` / `make creds-show` can then only show the initial one — *"that is them working,
not failing."* A gate on it reds on **every correctly-followed scenario-1 run, forever**.

### Why each proposal is refuted

1. **"Let the Harbor auth probe run even when the CA check failed"** is not a sequencing change —
   it is unimplementable without `-k`, and **`-k` TRANSMITS the admin password to an endpoint whose
   identity was never verified.** MEASURED against a local TLS oracle logging the Authorization
   header: with a WRONG CA the server received **nothing** (rc 60, code 000 — there is no auth
   verdict to obtain, so the short-circuit is the only reachable order); with `-k` the server logged
   `authorization: Basic YWRtaW46...` — the password, in the clear, to whatever now answers that IP.
   The CA arm exists precisely for a REBUILT lab reusing the same address. It would also break a
   RED-proven contract test (the `-k` silent-skip regression) and falsify a published promise
   (`scenario-2.md`: *"there is no -k skip-verify"*).
2. **The ArgoCD probe cannot make the claim it exists to make.** argo-cd release-2.14 returns the
   IDENTICAL string — `Invalid username or password` — for a wrong password and for a THROTTLE
   (`ARGOCD_SESSION_FAILURE_MAX_FAIL_COUNT` 5 per `ARGOCD_SESSION_FAILURE_WINDOW_SECONDS` 300,
   per-username). B109's own evidence quotes that exact string, so it cannot tell the two apart —
   and the budget is SHARED with the human's browser logins. (On the lockout question: ArgoCD
   `admin` is a LOCAL bcrypt account, so the vCenter SSO 3-strike lockout does not apply — but the
   throttle is worse for a probe, not better.)
   FALSE-RED over six real operator states: **2 of 6 are structural**, and one of them is the
   SUCCESS state of the primary runbook (post-`update-password`). A tenant has no admin password at
   all — their credential is `ARGOCD_AUTH_TOKEN`.
3. **UNREACHABLE != REJECTED is the only survivor and IT IS ALREADY BUILT.** `harbor_auth_verdict`
   in `scripts/lib/harbor.sh` returns exactly `accepted | rejected | unchecked:<reason>`, is
   RED/GREEN-tested, and already runs inside `make preflight` via `24-lab-preflight.sh` — which is
   the first prereq of `install-all`. The credential probe RUNS today; it is just not in
   `env-validate`.

### The real defect — FIXED in #661

`creds.sh` ran `argocd-password.sh` with `2>/dev/null`, discarding its own warning: *"this is the
INITIAL admin password — if you have run 'argocd account update-password' it no longer works."* So
the operator got a bare password with no sign it was the pre-rotation one, on a runbook that TOLD
them to rotate it. The probe would have reported a true 401 about a value the repo already knew was
superseded. The stderr is now kept and the ArgoCD row renders the provenance.

The same round found a live secret leak while reading the only `/api/v1/session` call in the repo:
the ArgoCD admin password was in **jq's argv**, inside a 60-iteration retry loop
(`/proc/<pid>/cmdline` is world-readable; `environ` is not). Also fixed in #661.

### Residual worth acting on later, NOT a probe

- `creds-show` is DISQUALIFIED as a home for any network probe: `test-creds-show.sh` carries no
  `# ci-tier: manual` marker and the test set is DISCOVERED, so it runs inside `make static-check`.
  A live probe there puts outbound auth in the offline gate — and `creds.sh`'s own header records a
  45-minute CI hang from exactly that class.
- `lib/harbor.sh`'s `_harbor_auth_code` hardcodes `https://` while `02-env.sh` treats
  `HARBOR_INSECURE=1` as `http` — so in the KinD insecure leg the productized probe is
  PERMANENTLY `unchecked` (measured: `curl -k https://` against a plain-HTTP listener -> rc 35).
  Single-source the scheme. Small, real, and independent of everything above.
- The class denominator is 3, not 2: `creds-show` prints Gitea as well, and Gitea has an auth API —
  it is out of scope only because it is an ingress `*.vks.local` host, i.e. the NXDOMAIN class the
  earlier `creds-check` refutation already killed. Say that rather than "the only two".

<details><summary>the original row, kept for its measurement</summary>

**HIGH.** MEASURED against the running lab 2026-08-16, from this box, with the values
`make creds-show` had just printed:

| what it printed | live reality |
|---|---|
| Harbor `https://harbor.env1.lab.test` | ✅ HTTP 200, genuine Harbor API (`/api/v2.0/systeminfo` answers) |
| Harbor admin password | ❌ **HTTP 401** on `/api/v2.0/users/current` |
| ArgoCD URL `<not set>` | ❌ but `192.168.101.131` **is** live — `{"Version":"v3.0.19+d67e6eb-vcf"}` |
| ArgoCD admin password (`ARGOCD_ADMIN_PASSWORD`) | ❌ **HTTP 401** `Invalid username or password` |

This is **not** B86 (the banner always reads `STORED`) or B87 (the test never renders that
branch). Those say the warning is noisy. This says that on the one occasion the warning was
checked, **every testable value behind it was wrong**, and the repo has no way to tell the
operator so — the owner's standing instruction is that these be verified after each lab cut,
and today that is a manual curl session.

⚠️ **The broad fix is ALREADY REFUTED — do not re-propose it.** The idea-round on 2026-08-10
(above, B86/B87) measured `make creds-check` at **67% false-RED**: nothing here writes
`/etc/hosts`, so `*.vks.local` is NXDOMAIN *by design*, and §11's port-forward URLs are live
only while a port-forward runs. My measurement deliberately touched **neither** class.

**The narrow proposal that survives that refutation** — Harbor and ArgoCD are the only two
services with their OWN LoadBalancer and a real auth API, so they are reachable exactly when
the lab is up, and a 401 from either is a true finding by construction (it is the credential
`make install-all` would use):

1. `env-validate` **already** probes Harbor auth (`scripts/02-env.sh:369`, and it handles 401
   explicitly at :373). Today it never gets there: the CA check fails first and it returns.
   Measured output — it correctly reported *"the CA at ./secrets/harbor-ca.crt does NOT verify
   the certificate harbor.env1.lab.test presents"*, named both as `CN = Harbor CA` (so a name
   comparison would have said MATCH), and told the operator `make fetch-harbor-ca`. Excellent
   gate; it just short-circuits the sibling probe.
2. Add the **ArgoCD** equivalent (`POST /api/v1/session`, 200 = valid, 401 = rejected) —
   currently nothing anywhere probes an ArgoCD credential.
3. Report **UNREACHABLE ≠ REJECTED**. Only a reachable endpoint that refuses the stored
   credential is RED; anything else is a skip with a reason.

**Scope note:** ArgoCD's address is discoverable (its own LB answered) yet `creds-show` prints
`<not set>`, so there is a recording gap as well as a testing gap. Settle which script should
publish `ARGOCD_SERVER` on the real-lab path before writing the probe.

## B111 — ✅ CLOSED (#658). The writers now ASSERT their write took effect — and the round found a worse, SILENT case

All THREE fixes this row proposed were REFUTED, each by measurement, and a fourth survived.

- **repair writes the overlay** — re-creates the exact harm #641 fixed. (The row's objection to it
  was itself false: `kind-down.sh:59` refuses to prune an unstamped overlay, so on a lab box it is
  never pruned.)
- **`load_env` sources `.env` last** — a stale `.env` `HARBOR_URL` then beats the discovered LB IP
  (measured: `harbor.OLD-LAB.example` vs `172.18.0.5`), and it contradicts
  `test-env-precedence.sh:76-78`, written the same day. That test is right; the candidate is wrong.
- **repair deletes the overlay key** — a per-value fix on a 4-of-6-site class, and no `state_unset`
  exists.

**THE CRITICAL THE ROW WOULD HAVE MISSED, and it does not 401.** Both Harbor keys ship COMMENTED in
`.env.example`, so at Step 4 `HARBOR_PASSWORD` is unset, `04-install-harbor-service.sh`'s
`[ -n "${HARBOR_PASSWORD:-}" ] ||` guard is false, and it `state_set`s ADMIN's credential into the
overlay — on the DEFAULT scenario-1 admin path, every time. Step 9's robot then writes `.env` and is
shadowed, so `make harbor-robot` prints *"the pipeline now runs as the ROBOT, not as admin"* while
the pipeline runs as Harbor **admin**. Admin works, so nothing surfaces it; it also defeats
`22-harbor-robot.sh`'s own `robot$*` re-run guard, minting a second robot unnoticed.

**Shipped:** `assert_env_effective` in `lib/os.sh`, called by both writers. It changes no
precedence, writes no overlay, deletes nothing, and generalises to every future `set_env_var`
caller — which had no post-write check at all. ⚠️ The `unset` inside it is the whole test: both keys
are in `load_env`'s SELECTOR SNAPSHOT, which restores the caller's exported value, so a re-resolve
without it passes unconditionally on exactly the box where the bug is live. ABLATION-PROVEN —
deleting the `unset` flips the vacuity case from rc=1 to rc=0 (`make test-env-effective`, 3 cases).

**Deliberately NOT built:** a static gate (it would flag the 4 sites that are correct by design, and
its only remedy is the refuted candidate 1) and a runtime shadow-warning (semantic — the overlay
winning is CORRECT for `HARBOR_URL` and WRONG for `HARBOR_PASSWORD`, and no matcher separates them).

<details><summary>the original row, kept for its measurement</summary>

**HIGH — measured end-to-end on the live lab 2026-08-16, and this is why B109's credentials were 401.**

Two commands, seconds apart, gave opposite verdicts on the same credential against the same Harbor:

    make harbor-admin-password  ->  ok  Harbor accepts admin (http 200)
                                    wrote HARBOR_USERNAME and HARBOR_PASSWORD to ./.env
                                    (verified against harbor.env1.lab.test first)
    make env-validate           ->  ERROR Harbor rejected HARBOR_USERNAME/HARBOR_PASSWORD (HTTP 401)

Both were telling the truth. They are reading **different values**:

| sink | sha256 (first 12) | length |
|---|---|---|
| `.env` — what Step 8.5 wrote and verified | `d2c07f3b2a3c` | 16 |
| `.env.state` — the overlay | `484845762367` | 32 |
| **what `load_env` actually resolves** | **`484845762367`** | **the overlay wins** |

**The mechanism, from the code:** `load_env` sources `.env.example` (`lib/os.sh` :88) → `.env` (:102)
→ **`.env.state` (:130, last)**, with `set -a`, and its own comment at :9 says *"every file below
outranks"*. Meanwhile:

- `04-install-harbor-service.sh:124-125` — the **install** — writes the password to the **overlay**
  (`state_set HARBOR_PASSWORD`);
- `28-harbor-admin-password.sh:129-130` — the **repair** — writes it to **`.env`**
  (`set_env_var … "${REPO_ROOT}/.env"`).

Same logical value, two sinks, different precedence. So on any box where Harbor was ever installed
by this repo, the overlay holds that install's password and **Step 8.5 cannot override it** — the
command whose entire purpose is to replace a wrong password with the installed one writes to the
file that loses. Its success message is true and useless.

**Why it matters beyond a stale box:** `docs/scenario-1.md` §4 says *"Do not skip Step 8.5"* for the
already-exists persona precisely because `make install-all` must authenticate to Harbor. On a lab
rebuilt under the same repo checkout, the overlay's password is the PREVIOUS lab's, Step 8.5 reports
success, and `install-all` then fails to authenticate with no indication that the repair was
discarded.

**Same family as `check-env-clobber`, one layer up.** That gate polices `.env.example` shadowing a
fallback or a per-run override. Nothing polices **`.env.state` shadowing `.env`** — which is the
same defect with a higher-precedence file.

⚠️ **No fix is proposed here on purpose.** The obvious ones each have a plausible failure mode and
none has been reviewed: making the repair write the overlay instead (the overlay is also the thing
`kind-down` prunes, and `state_set` on an UNSTAMPED overlay is what B86/B98 are about); making
`load_env` source `.env` last (inverts a precedence other values rely on); or having the repair
delete the overlay's key (destructive, and the overlay may hold the only copy of other generated
secrets — the warning `load_env` already prints). Run an idea-round before touching it.

**Verified as part of the same session:** with a valid Supervisor kubeconfig the whole documented
chain otherwise works — `make harbor-ca-from-cluster` rc=0 (route B, the `<details>` alternative the
walker skips) and `make harbor-admin-password` rc=0 with a live `http 200`. The failure is purely
the sink precedence.

</details>

## B114 — ⚠️ CAUSAL CLAIM REFUTED (measured). The wedge is REAL; the unwedge does not cause it

**Do not implement the fix as this row originally described it — it would search the wrong
namespace, find nothing, ship green, and never fire.**

**MEASURED ON THE LIVE LAB 2026-08-16**, which is what settles it:

    kubectl get sts,deploy -A -o wide | grep -i argocd
      ns=cicd                      statefulset.apps/argocd-application-controller   <- the finalizer's controller
      ns=cicd                      deployment.apps/argocd-server
      ns=svc-argocd-service-wpypi  deployment.apps/argocd-service-controller-manager <- the OPERATOR
    kubectl get ns | grep '^svc-argocd'
      svc-argocd-service-wpypi                                                       <- all the unwedge can touch

`unwedge-supervisor-service.sh:70` derives `prefix="svc-$(cut -d. -f1 <<<"$SERVICE")-"` and every
delete is `kubectl -n "$NS"`. The stranded CRs are `application/cicd/*`. **The two namespaces are
disjoint and the script has no code path into `cicd`** — it never had the ArgoCD application
controller in scope, so it cannot have stranded anything.

**What actually happens:** the UNINSTALL deletes both `applications.argoproj.io` and the ArgoCD
instance in the same kapp app, so the package self-deadlocks on its own finalizers. The unwedge
cleared the FIRST wedge, which let kapp advance far enough to EXPOSE a deadlock that was already
armed.

**What SURVIVES from the original row:** the observation (two 15-minute rounds, no progress;
clearing the two CR finalizers made the CRD disappear in under 8 seconds) and the conclusion that
the closing *"re-issue the uninstall"* advice is WRONG. Only the attribution was wrong — and it is
the attribution that decides where the fix looks.

### The corrected design

- **Key on TERMINATING CRDs, cluster-wide — never on `$NS`, and never on the service's API group.**
  There is no derivable relation between them: `SERVICE=argocd-service.vsphere.vmware.com` (group
  `vsphere.vmware.com`) vs the stranded `applications.argoproj.io` / finalizer
  `resources-finalizer.argocd.argoproj.io` — **zero token overlap**. Terminating CRDs are normally
  ZERO, so the per-CRD fan-out is bounded by the wedge, not by the cluster.
- **Call it from ALL THREE terminal paths**, not just after the delete loop. The path a SECOND
  invocation takes is `:93` (namespace already gone -> `exit 0`), which prints the same false
  reassurance — and by then `$NS` does not exist while the stranded CRs in `cicd` very much do.
  (B112's lesson recurring: fix the CLASS, not the cited line.)
- **Emit CR patches ONLY. Do NOT patch the CRD.** Removing `customresourcecleanup.apiextensions.k8s.io`
  while CRs remain deletes the CRD object and leaves the CRs as unreachable garbage in etcd with no
  API left to serve them — and it is unnecessary: the row's own <8s measurement IS the apiextensions
  controller clearing it once the last CR goes. Emit `-n <ns>` only for namespaced CRs (from the
  CRD's `spec.scope`); a stray `-n` on a cluster-scoped CR is a silent no-op-looking error.
- **The found branch must CONTRADICT the 15-minute line, print LAST, and stay short.** Merely
  omitting it lets the operator's prior ("re-issue and wait") win by default, and `:165-167` dumps
  `Remaining in NS` afterwards, which would scroll the finding away.
- **Read failures must not read as "none found."** A naive `crds="$(kubectl get crd -o json | jq …)"`
  under this script's `set -euo pipefail` exits AT the kubectl, and a `2>/dev/null` variant renders a
  transport failure as "no stranded CRs" followed by the reassuring advice — the FOURTH instance of
  the class this script has been fixed for three times. Use `|| rc=$?` + `classify_kube_failure`.
- **Second home:** a read-only `make` target as well as inline, because path `:93` cannot run the
  inline form usefully. Mid-incident the operator reads the inline output; the next day they re-run
  the target.
- **`do NOT auto-patch` stands, but NOT on the precedent the row cited.** "It dies on an AMBIGUOUS
  namespace" is about not knowing WHICH object, not about destructiveness — and this script is
  emphatically not read-only (it deletes platform-managed workload after `CONFIRM=yes`). The
  load-bearing argument is ORPHANING: `resources-finalizer.argocd.argoproj.io` exists to
  cascade-delete the Application's deployed workloads, so dropping it leaves a tenant's real
  workloads running with no owner and nothing recording that they were abandoned.

### Also worth checking, and NOT in the original row

The delete loop deletes kind `service` (`:142`), and the script's own header names
`argocd-service-webhook-service-*` in that namespace. A `ValidatingWebhookConfiguration` is
**cluster-scoped** and survives; with `failurePolicy: Fail` and its backing Service gone, every
matching create/update/delete fails cluster-wide with *"no endpoints available for service"* —
potentially including the finalizer patches this design tells the operator to run. Same shape for an
`APIService` whose `spec.service` points into `$NS`. Report them (never delete them); it is two
cluster-scoped list calls, cheaper than the CRD scan. PVs are NOT in scope
(`kubernetes.io/pv-protection` is kube-controller-manager's, which this script never touches).

### Unmeasured, stated rather than hidden

The MECHANISM (why the apiextensions controller requeues forever rather than force-clearing a CR
finalizer) is upstream-source reasoning, not something anyone ran; the observation and the <8s clear
are consistent with it and no competing mechanism survives inspection. And the COST of
`kubectl get crd -o json` on a Supervisor is unweighed — it returns full OpenAPI schemas for every
CRD. `time kubectl get crd -o json | wc -c` settles it on the next lab trip; kapp's own
`kapp.k14s.io/app`-labelled ConfigMaps may name the CRD directly for one small list, UNVERIFIED.

<details><summary>the original row, kept for its observation</summary>

**MEDIUM. Observed end-to-end on the lab 2026-08-16, twice in one reset.**

`unwedge-supervisor-service.sh` deletes the namespaced workload kapp is waiting on — including the
service's **controller**. That is correct for the first wedge and it worked (measured: it cleared
`endpointslice/argocd-service-webhook-service-r9ksq` and reported `deleted 3 object(s)`). But
deleting the controller **strands every CR whose finalizer needs that controller**, and the strand
is CLUSTER-SCOPED, so nothing the script deletes can reach it:

    crd/applications.argoproj.io   Terminating since 11:07:26Z
      finalizer: customresourcecleanup.apiextensions.k8s.io   <- waits for all CRs of the kind
    application/cicd/gowebapp      Deleting since 11:07:27Z
    application/cicd/javawebapp    Deleting since 11:07:27Z
      finalizer: resources-finalizer.argocd.argoproj.io       <- needs the controller we just deleted

kapp then moves from the endpointslice to `customresourcedefinition/applications.argoproj.io` and
waits there **forever** — and the script's closing line says *"re-issue the uninstall. kapp retries
in 15-minute rounds, so give it that long."* That advice is now WRONG: no number of rounds resolves
a finalizer whose controller is gone. Measured: two full 15-minute rounds, no progress; clearing the
two finalizers (`kubectl patch --type=merge -p '{"metadata":{"finalizers":null}}'`) made the CRD
disappear in **under 8 seconds**.

**Fix, in the order I would do it:** (1) after deleting workload, LOOK for CRs stuck `Deleting` with
finalizers in the service's own API groups and say so explicitly, naming them and the patch — the
script already has the operator's attention at exactly that moment; (2) only then print the
"re-issue the uninstall" line, and make it conditional on having found none. Do NOT auto-patch
finalizers: dropping a finalizer abandons whatever cleanup it represented, which on a REAL cluster
can orphan real resources, and this script already refuses to guess elsewhere (it dies on an
AMBIGUOUS namespace rather than picking one).

⚠️ The generic form is worth stating because it is not specific to ArgoCD: **a break-glass tool that
deletes a controller must account for the CRs that controller was the only thing able to finalize.**

## B113 — ✅ CLOSED. An "offline" suite DIALLED the live lab and hung `make ci` for 22 minutes

**The class is MEASURED at 1 of 48, and that one is fixed.**

`test-creds-show.sh` carefully sandboxes `VKS_STATE_FILE` and sets `SKIP_DOTENV=1`, and its own
comment explains why (*"a test that reads its own environment"*). It did not sandbox
**`KUBECONFIG`** — and `creds.sh` dials (`kubectl … version`, plus an ArgoCD probe). So the suite's
runtime, and in principle its verdict, depended on the state of whatever cluster the box happened
to be pointed at. On 2026-08-16, with a Supervisor Service uninstall in flight, the lab accepted TCP
and then stalled, and `make ci` hung **22+ minutes** inside a target whose help says *"Offline
script-logic unit tests"*.

**`--request-timeout` was NOT at fault, and a `timeout` wrapper would have been the wrong fix.**
Measured at BOTH failure modes — a packet-drop black hole (192.0.2.1) and an accept-then-stall
listener — kubectl exits at **3s** in both. The 22 minutes is arithmetic: ~2–3 kubectl calls per
case × 11 cases, each paying the full 3s, against a slow link that retried.

**RED-proven with the confounder held constant** (the lab had also stopped stalling, so a plain
re-run would have proved nothing): against the same accept-then-stall listener the suite takes
**0s with the sandbox and 78s without**. Both exit 0 — this was never a failure, it was a silent
dependency on a live cluster.

**THE CLASS IS CLOSED, BY MEASUREMENT, AND IT WAS A CLASS OF ONE.** My grep ("runs a product
script and does not pin KUBECONFIG") returned **31 of 48** — a **31× over-approximation**, because
most of those stub `kubectl` on `PATH` in forms the pattern cannot see. **Never quote a grep as a
class size.** The experiment that settles it ran instead: every one of the 48 suites, twice — once
with `KUBECONFIG` unset, once pointed at an accept-then-stall listener — comparing wall time.

    0 of 48 suites are materially slower against a stalling endpoint (>2000ms)
    largest delta: test-walk-doc, 222ms (noise)

**And the zero is not vacuous, because the harness has a POSITIVE CONTROL.** Removing the sandbox
from the one suite that did dial makes it **77,183ms slower** on the same listener. So the harness
can see a dialler at 350× the noise floor; "0 of 48" means the remaining 47 genuinely do not dial.
(This matters: the measurement ran AFTER the fix, so `test-creds-show`'s own 0ms delta is the fix
working, not evidence it never dialled — the control is what separates those.)

## B112 — ✅ CLOSED, and the row named 2 of 3 sites

**The two it named were already fixed** (the confirm listing and the delete loop, both carrying
`B112:` comments). Auditing the whole script for the CLASS rather than the two cited lines found a
**third** swallowed read — the namespace discovery — and its failure is the most reassuring of the
three: `kubectl get ns -o name 2>/dev/null` makes a transport failure produce an EMPTY array, which
lands on `case 0` and **EXITS 0** with

    no namespace matching 'svc-harbor-*' — the workload is already gone.
    next: re-issue the uninstall; the platform's next reconcile should complete it.

So an operator whose Supervisor token has just expired is told the wedge cleared, and sent to
re-issue an uninstall that will stay stuck. Fixed with the same `classify_kube_failure` pattern;
`make test-unwedge-ns-discovery` RED-proves both directions (a failing kubectl must refuse; a
WORKING kubectl with no match must still say "already gone" and exit 0 — the second matters as much
as the first, or the fix has broken the legitimate path).

**The lesson for the next row like this: fix the CLASS, not the cited line numbers.** A row that
names sites will be read as an inventory, and this one was two-thirds of the truth.

## B110 — ✅ CLOSED for the enumerated set (4 of 4) — an EXPIRED Supervisor token read as "Harbor is not installed"

**Fixed 2026-08-16, each with a test the script did not have and a demonstrated RED:**

| | script | what it used to claim |
|---|---|---|
| #635 | `28-harbor-admin-password.sh` | "no Harbor Supervisor Service — install it (Step 4)" — on the documented Step 8.5 path |
| #636 | `24-vks-k8s-version.sh` | burned up to **900s**, then "a Supervisor/content-library problem" |
| #637 | `27-harbor-ca-from-cluster.sh` | same as #635; **proven live before/after** on the lab with the same expired kubeconfig |
| #638 | `26-vks-cluster-status.sh` | "cluster \<ns\>/\<name\>: NOT FOUND" |

Each now classifies the probe it actually ran and names the specific cause; `--request-timeout` was
added throughout (an unbounded kubectl hangs >2 min on a black-hole endpoint); and 27/28 carry a
`HARBOR_SERVICE_NAMESPACE` escape hatch so a tenant who legitimately cannot list namespaces has a way
forward rather than a correct message and a dead end. `check-classifier-consumers` now counts **7**
case-form consumers, all classes handled — the gate-visible `case "$(classify_kube_failure …)"` form
was used deliberately, since a `case` on a variable would have been invisible to it.

⚠️ **CLOSED for the ENUMERATED set, not for the class.** The reviewer's denominator was an explicit
FLOOR: it hand-classified the Supervisor-touching subset of **65** swallowed `kubectl … 2>/dev/null`
reads across **27** files and did not walk all 65. See **B112** for the one it named and excluded.

**Two bugs of my own, both caught by the new tests, both from this repo's own rules:** I set a global
inside `v="$(_newest_ready)"` — a command substitution is a SUBSHELL, so the rc was discarded and
every classification silently fell through to the 900s wait (only the ELAPSED assertion caught it;
rc alone could not, because the timeout path also exits non-zero); and a `trap … EXIT` I nearly added
to `27` would have been REPLACED by the one already at :61 and leaked a temp file every run.

<details><summary>The original write-up and the refutation of my first design (kept — do not rebuild it)</summary>

## B110 (original) — an EXPIRED Supervisor token reads as "Harbor is not installed", and the script says so

**HIGH — measured on the live lab 2026-08-16, both directions, same command and same cluster.**

`scripts/28-harbor-admin-password.sh:67` and `scripts/27-harbor-ca-from-cluster.sh:55` find the
Harbor service namespace with the **correct** selector — `appplatform.vmware.com/serviceId=harbor`
is exactly what the namespace carries (verified against `svc-harbor-fz4kh`). The defect is one
level deeper: the probe is `kubectl … 2>/dev/null || true`, so an **expired or invalid Supervisor
kubeconfig yields zero matches**, which is indistinguishable from "the service is absent".

| kubeconfig | `auth whoami` | namespaces the script sees |
|---|---|---|
| `~/.local/state/nested-lab/kubeconfig` | OK | **1** |
| `secrets/supervisor.kubeconfig` (expired) | FAILS | **0** |

The script then prints, verbatim:

    no Harbor Supervisor Service on this Supervisor - install it (Step 4) or ask your platform admin.
    refusing to guess - an empty value would silently target the 'default' namespace

Harbor was running the whole time. **An error that names the wrong cause is worse than a crash** —
this one sends a tenant to ask their platform admin to install a service that is already there, and
sends a Scenario-1 operator back to Step 4 to re-install over a healthy Harbor.

**It is on the documented path for the persona who needs it most.** `docs/scenario-1.md` §4 says, of
the already-exists branch, *"Do not skip Step 8.5"* — and Step 8.5 is `make harbor-admin-password`,
the command that fails this way.

**Why 4/4 green never caught it:** on a walk the row either installs Harbor itself or is handed a
credential that authenticates, so the "read the installed one" branch is never taken; and
`27-harbor-ca-from-cluster.sh` lives inside a `<details>` alternative, which the walker skips by
design (row 2 log: `skipped: [25] 8. Harbor's CA - inside a <details> alternative`). Both consumers
sit on paths the matrix does not execute.

### ⚠️ My first design was REFUTED in 3 of 5 parts — do NOT rebuild it

`vks-adversary` idea-round, 2026-08-16, shell PERMITTED; every counter-claim below is `ran-it`.

I proposed copying `nested-vsphere-lab/walk-matrix.sh`'s *"AUTH BEFORE OBSERVING"* fix — a
`kubectl auth whoami` precheck that dies naming the **token**. Four measured refutations:

1. **The cause is not an expired token — it is a NEW CA.** The stale kubeconfig fails with
   `x509: certificate signed by unknown authority … candidate authority certificate "CA"`, i.e.
   `STALE_CA`: the lab was rebuilt and minted a new CA at the same address. A die naming the token
   would name the wrong cause — the very sin this row is about, one level down.
2. **`auth whoami` is a STRICTLY WEAKER precondition than the probe it guards, so it is blind to
   the persona this row exists for.** I feared it would be *Forbidden* for a tenant and false-RED.
   Measured, the opposite and worse: it **succeeds** for an identity that is Forbidden on a
   cluster-scoped list. `auth whoami` needs `create selfsubjectreviews` (broadly granted);
   `get ns -l …` needs cluster-wide `list namespaces`. So for the low-privilege reader of §4's
   *"If Harbor already exists"* branch, the precheck passes, the probe still returns 0, and the
   message is still wrong. `n=0` has **five** causes and the precheck catches two.
3. **This repo already has a better mechanism, and the design would BYPASS ITS CI GATE.**
   `lib/os.sh:1195` `classify_kube_failure` returns
   `STALE_CA|UNAUTHORIZED|FORBIDDEN|UNREACHABLE|PLAINTEXT|NO_KUBE_TARGET|KUBECONFIG_UNUSABLE|UNKNOWN`,
   each with its own remedy; `lib/os.sh:1142` `k_can_i` is the three-state probe; and
   `check-classifier-consumers.sh` **fails any `case`-form consumer missing an arm**. It already
   classifies our exact stderr as `STALE_CA`. `08-install-argocd-service.sh:93-103` is this same
   fix, for this same `$SUP` kubeconfig. A flat `auth whoami || die "<one message>"` gets zero
   coverage from that gate, and `24-lab-preflight.sh:52` records why one-guess messaging was
   already deleted once: *"the wrong remedy for four of the five ways it fails."*
4. **STALE_CA and FORBIDDEN have OPPOSITE remedies.** `make vks-login` fixes the first and is
   exactly wrong for the second (`24-lab-preflight.sh:66`: *"an RBAC GRANT, not a broken
   kubeconfig — do NOT re-fetch it"*).

**What survived:** the defect (reproduced both directions on the live lab), its HIGH severity, and
*"auth before observing"* as a **principle**. Only the mechanism was wrong.

### The design to build instead

Capture the probe's stderr to its **own** file (never `2>&1` — `lib/os.sh:1086` records that
merging inverts downstream emptiness tests), branch on its rc, and `case` on
`classify_kube_failure`, with a per-class remedy. Only after that is `n=0` a fact about the world,
and today's *"install it (Step 4)"* message becomes correct for the case where it is true. Add
`--request-timeout=15s` regardless of which fix is taken: neither script passes it, eleven other
call sites do, and `23-argocd-preflight.sh:47` records that an unbounded call **hangs >2 min** on
a black-hole endpoint — a precheck would have *doubled* that.

**Denominator — 4 broken of ~11 in the class, not 2.** The reviewer scanned **166** shell files,
found **65** `kubectl … 2>/dev/null` reads across **27**, and hand-classified the
Supervisor-touching subset: **~7 already carry the fix**, and **4 are broken** —
`27-harbor-ca-from-cluster.sh:53`, `28-harbor-admin-password.sh:66`,
**`24-vks-k8s-version.sh:37,43`**, and `26-vks-cluster-status.sh:57` (a report, returns 2 — lower
stakes). ⚠️ Sampled, not exhaustive: treat 4 as a **floor**.

**If only two get fixed, `24-vks-k8s-version.sh` must be one of them** — it burns up to **900s**
(`VKS_TKR_WAIT_SECONDS`) and then makes a confident platform diagnosis (*"no Ready AND Compatible
TKr … a Supervisor/content-library problem"*) from a transport failure. It is the most expensive
instance in the class.

**Two things the fix must carry or it trades one dead end for another:**

- a **FORBIDDEN escape hatch** (`HARBOR_SERVICE_NAMESPACE`), or a tenant who legitimately cannot
  list namespaces gets a correct message and still has no path;
- **tests** — neither script has any. `test-argocd-preflight-ns.sh` is the pattern, and note its
  kubectl stub already skips `--request-timeout` positionally; a new stub that omits that will
  mis-parse the moment the flag is added, and it will look like a product bug.

**Related, not counted:** `unwedge-supervisor-service.sh:91` is the same shape in a **mutating**
script — it would delete nothing and report "deleted". Different blast radius; own row.

</details>

## B88 — ✅ CLOSED, no action: the measurement stands and the conclusion it argued for is now much weaker

**What changed, 2026-08-16.** `static-check` was restored per-PR — but on a `static-check-pr`
target that deliberately EXCLUDES trivy (measured: `make -n static-check-pr` -> 0 trivy
invocations; `make -n static-check` -> 3). So the ~25s this row correctly re-measured is now paid
**once a week**, not once per PR. That is roughly a 20x reduction in what the cache would buy,
against a staleness bound on a security gate — and the row's own closing line already said a bigger
saving is not by itself a reason to overturn a correctness call.

The re-measurement is still worth keeping (the recorded decision's "~10s" was 2.5x too small, and
the DATE-KEYED cache option genuinely never appeared in it), but nothing should be done about it.
If per-PR trivy is ever reconsidered, this row is the number to weigh — not before.

Stale citation corrected while here: the decision is at `.github/workflows/ci.yml:194`, not :147.

<details><summary>the original row, kept for the measurement</summary>

`.github/workflows/ci.yml:194` (was :147) records the 2026-07-14 decision to DROP a trivy-DB cache,
reviewed by java-adversary + docker-adversary, on the grounds that it *"would silently serve
a DB up to trivy's 24h client interval, missing same-day CVEs, **for ~10s**"*.

The correctness objection stands and this row does NOT propose overturning it. What is wrong
is the number it was weighed against. MEASURED on run 31451956799, job `static-check`:

    02:20:04  trivy-fs: scanning javawebapp -> .../javawebapp-0.1.0.jar
    02:20:32  (first Report Summary)                                    <- 27.5s
    02:20:32  trivy-fs: scanning gowebapp -> /tmp/.../gowebapp
    02:20:32  trivy-fs: OK - 2 app artifact(s) scanned                  <-  0.07s

Same process family, same machine, same run: the FIRST scan is 27.5s and the SECOND is 0.07s.
The delta is the vulnerability-DB download. So the trade is **~25s, not ~10s** — 15% of the
164s `make static-check` step, against a staleness bound trivy's own 24h client interval
already permits by default.

Whoever revisits it should also weigh a DATE-KEYED cache (`trivy-db-<YYYY-MM-DD>` with a
`trivy-db-` restore-key), which bounds staleness to <24h explicitly rather than relying on
trivy's internal interval — that option does not appear in the recorded decision.

⚠️ Do NOT re-add it without a fresh adversary round: the original rejection was a CORRECTNESS
call about a security gate, and a bigger saving is not by itself a reason to overturn one.

</details>

</details>

</details>

## B206 — the builder probe's `mvn` hardcode ✅ CLOSED (fixed 2026-08-23, one day after filing) — and this row was WRONG about reachability

**CLOSED 2026-08-24 by measurement, not by doing the work: the work was already done.**

    lib/apps.sh:503   app_builder_probe() dispatches java|go|nodejs|python|rust|dotnet,
                      and die()s on an unknown lang (:511)
    24-builder-probe.sh:67   probe_cmd="$(app_builder_probe "$app")"
    grep 'mvn -o -v' 24-builder-probe.sh -> 2 hits, BOTH in COMMENTS (:57, :68)

The code says so itself at `:56-58`: *"it was a hardcoded `mvn -o -v` until **2026-08-23**, which was
right while java was the only app with a builder and would have failed 5 of the 6 that ship one
today."*

### ⚠️ This row's SECOND claim was FALSE, and that half matters more

It said *"no walkthrough row and no `make install-all` reaches it"*, resting on
`grep -c builder-probe docs/scenario-1.md docs/scenario-2.md` -> `0 / 0`. Both true, and the
conclusion does not follow — **`scripts/jumpbox-run.sh:198` runs `make builder-probe` as Step 4c**,
so every `make jumpbox` / `jumpbox-matrix` hits it. The scenario docs were the wrong place to look.

That is also the evidence the fix is real: `make jumpbox-matrix` went green closing [[B208]] on
2026-08-23/24 with all six apps shipping a `Dockerfile.builder`. A probe still hardcoding `mvn`
would have failed 5 of 6 in that run.

**Why the false half is the dangerous one.** "Nothing reaches it" is the sentence that justifies
leaving a future break unfixed. Left standing, it would have licensed exactly that for a script the
jumpbox harness runs on every invocation.

### The lesson, recorded because it cost a wrong proposal

This row was offered to the operator as the top next task — a fix for a bug fixed the day before —
on the strength of its one-line index entry, never opened. `gates.md` already says a backlog row's
STATUS is a claim; so is its REACHABILITY. The check was 30 seconds:

    grep -rn 'builder-probe' Makefile scripts/*.sh .github/workflows/*.yml docs/*.md

Do not propose work from an index line. Open the row, then verify the row.

## B207 — MOVED to nested-vsphere-lab as B459 (the walkbox rootfs growth) ✅ closed

Mis-filed here. The walkbox is `nested-vsphere-lab`'s: that repo's `scripts/walkbox-vm.sh` builds it,
and CLAUDE.md's own rule already says walkbox defects are filed **there** and deliberately not
tracked in this backlog, because the fixes edit files this repo does not contain and the two
backlogs number `B<N>` independently.

It is now **B459** in `nested-vsphere-lab/docs/BACKLOG-EVIDENCE.md` (that repo's index is GENERATED —
`make backlog-index` is its drift gate; regenerated and re-checked green).

Kept here as a pointer only, because the SYMPTOM lands in this repo and is thoroughly misleading: the
walkbox runs out of disk building this repo's SIXTH builder image, and what you actually see is
`missing secrets/gitea-ci-token — run 'make seed-gitea' first`, because `install-all` died before
`platform → seed-gitea`. **If a walk reports that token error, run `df -h /` before touching Gitea.**
The sizing that drives it is in [docs/sizing.md](docs/sizing.md).

## B208 — `jumpbox-matrix` photon×docker: `--privileged` breaks sudo, so that cell has never passed ✅ closed

**PRE-EXISTING** (`jumpbox/Dockerfile.photon-docker` last changed 2026-08-08) and **off the six-row
critical path** (`grep -c jumpbox docs/scenario-{1,2}.md` -> **0 / 0**), so it is filed rather than
fixed mid-certification.

MEASURED 2026-08-23, `make jumpbox-matrix` -> **`JUMPBOX MATRIX: 3/4 passed — FAILED`**, the failing
cell being `LEG FAILED: photon x docker`. Inside it:

    sudo: PAM account management error: Authentication service cannot retrieve authentication info
    sudo: a password is required
    make[1]: *** [Makefile:329: deps-prereqs] Error 1

`--privileged` is the discriminator, and `scripts/jumpbox-launch.sh:195` passes it on every leg:

    docker run --rm                vks-jumpbox:photon-docker  -> sudo rc=0
    docker run --rm --privileged   vks-jumpbox:photon-docker  -> sudo rc=1     <-- the launcher's form
    docker run --rm --privileged   vks-jumpbox:ubuntu-docker  -> sudo rc=0

So it is Photon + docker-image + `--privileged` specifically. Ruled OUT by measurement:

- the shadow entry is present -- as root, `getent shadow vks` -> `vks:!:20689:0:90:7:::` (the `!` marks the account as having no
  usable login credential, which is normal and correct for a passwordless sudoers entry);
- `/etc/nsswitch.conf` resolves both the account and shadow databases from local files;
- `/etc/pam.d/sudo` is the stock Photon include stack;
- the sudoers drop-in is identical to the PODMAN photon image, which passes: both do
  `groupadd vks && useradd -g vks -m -s /bin/bash vks` plus a passwordless-sudo line for `vks` in
  `/etc/sudoers.d/vks` at 0440, then `USER vks`.

The two images differ only in packages -- podman/fuse-overlayfs/slirp4netns versus
docker/procps-ng/iproute2 -- so the next step is to find what Photon's `docker` package pulls in that
changes PAM behaviour under `--privileged`. Not yet established.

Note the image runs non-root **deliberately**: its own header records that at uid 0 the sudo column,
which is the entire deliverable of the engine matrix, is not merely wrong but UNMEASURABLE. So
"run it as root" is not the fix.

Worth stating plainly: this cell of a documented target has presumably never passed, and nothing
surfaced that until the target was actually run. The other three cells are green --
photon×podman, ubuntu×podman and ubuntu×docker all reported `engine-trust-check PASSED`, the last of
them via `docker-rootless` with the CA at `~/.config/docker/certs.d/`.

**✅ CLOSED 2026-08-23 — measured, with a live push to the real Harbor.** Root cause was NOT the base
image date (two earlier diagnoses, both refuted): `/etc/shadow` is owned by no package, and
`shadow`'s `pwconv` (0400) races `systemd`'s `systemd-sysusers` (0000) — rpm's transaction order
decides, and the docker set orders systemd first. Fixed in PR #968 by pinning 0400 at build time in
every hazard-scoped Photon image, with a drift NOTE that fired on its first real build
(`photon-docker: arrived 0 (expected 400)`; `photon-podman`: silent — exactly the predicted split).
`crun` was also missing from the podman image's install list although its comment claimed it.

**Evidence — `make jumpbox JUMPBOX_OS=photon JUMPBOX_ENGINE=docker` -> `JUMPBOX_OK`:**
rootless docker build+run smoke OK; the CA **negative control** fired (*"without the CA, docker
CANNOT reach 172.18.0.3"*), so the trust result is not vacuous; login+pull+build+**push** to the KinD
Harbor succeeded; and **0** broken-PAM warnings from the new `os.sh` capability probe, which is the
correct silent behaviour on a healthy box.

## B213 — `--security-opt seccomp=unconfined` would dissolve the whole /etc/shadow failure class 🔴 open

**Found by `adversary-docker`, 2026-08-23, MEASURED.** `jumpbox-launch.sh:195` runs the jump-box
containers with `--privileged`. That flag is a **bundle**, and the two halves pull in opposite
directions:

| flags (same image, /etc/shadow forced to 0000) | `sudo -n true` | `unshare -Ur true` |
|---|---|---|
| `--privileged` | **rc=1** (PAM account management error) | rc=0 |
| `--privileged --security-opt apparmor=docker-default` | rc=0 | rc=0 |
| `--security-opt seccomp=unconfined` (no `--privileged`) | **rc=0** | **rc=0** |
| none | — | rc=1 (`Operation not permitted`) |

Only `--privileged`'s **AppArmor-unconfined** component causes the shadow bug (it lets the HOST
`unix_chkpwd` profile attach by path, granting `audit_write` + shadow-read but **no dac_override**).
Only its **seccomp** component is needed for the userns that rootless podman/dockerd requires — the
host runs `kernel.apparmor_restrict_unprivileged_userns=1`. So `seccomp=unconfined` buys the userns
**and keeps `docker-default` AppArmor**, and the shadow mode stops mattering at all.

**Why this is NOT shipped in the same change as the 0400 pin:** measured, a non-privileged container
has **no `/dev/fuse`**, which `fuse-overlayfs` needs. So the swap is `seccomp=unconfined` +
`--device /dev/fuse` + whatever else a full leg needs — and the round proved only that the *two
blockers* dissolve, **not** that a whole run passes. It needs its own change and a full
`make jumpbox-matrix` run to validate.

**Keep the 0400 pin regardless** — defence in depth, and it is the thing that makes the images
correct under the flag they use *today*.

**Done when:** `jumpbox-launch.sh` runs without `--privileged`, `make jumpbox-matrix` is 4/4, and the
`/etc/shadow` contract is documented as belt-and-braces rather than load-bearing.

## 🟡 B474 — `8.5` is a decimal insertion point, not a numbering scheme; renumbering costs ~30 refs across TWO repos

`docs/scenario-1.md` has a step numbered **8.5**. There is no reader-facing logic behind it — it
was inserted between 8 and 9 and nothing was renumbered. `8.1` would be **worse**, not better: it
reads as a *sub-step of 8*, and it is not one. Step 8 fetches Harbor's **CA** and always runs; 8.5
fetches an **admin credential** and only runs if Harbor pre-existed. Independent things.

**Why it is not just a heading edit.** Measured 2026-08-25:

| | |
|---|---|
| `8.5` references | **6** real (4 in `scenario-1.md`, 2 in script comments) |
| references to Steps 9–14 by number | **~30**, across `BACKLOG.md`, 6 scripts, and `README.md` |
| headings + anchor links that move | 6 + 6 |
| **`scripts/walk-doc.sh`** | **9** step-number references |
| **`nested-vsphere-lab/scripts/walk-matrix.sh`** | **11** step-number references — **a different repo** |

Both walkers key on step numbers, so a renumber can change which blocks the matrix skips. That
makes this a change to run **between** matrix campaigns, never before one, and it needs a full
6-row run to validate — the same bar as any walker change.

**Done when:** 8.5 → 9 and 9–14 → 10–15, every reference in both repos updated, `walk-doc.sh` and
`walk-matrix.sh` still classify the same blocks, and a 6-row matrix is green on the renumbered doc.

## 🟡 B475 — the walkthrough harness invents 21 env variables, so it certifies the COMMANDS, not the DOCUMENT

`nested-vsphere-lab/scripts/walk-matrix.sh`'s `walk_env()` pre-supplies ~21 variables into
`$HOME/.walk-env` before a single documented command runs. The list is hand-typed and has nothing
linking it to the documents it claims to walk, so it drifts in both directions:

- a key a document requires but the harness never supplies → the row dies on a real lab for a
  reason a **reader** would also hit, and the harness scores it as a product bug;
- a key the harness supplies that no document names → the row is green on a value no reader could
  ever have, i.e. it certifies a path that does not exist.

A **third** hand-typed copy lived in `make env-populate` and was missing 4 of the 9 keys
scenario-1 Step 1 lists — for six weeks, silently (fixed in #983).

**Two designs were built and REFUTED. Do not rebuild either.**

1. *A flat union of doc keys + an EXEMPT table in the harness.* **Scenario-blind.**
   `HARBOR_PASSWORD` is in the doc set **and** the emitted set, so moving its emission out of the
   `if [ "$scen" = scenario-2 ]` guard leaves the gate **GREEN** while re-short-circuiting the very
   §8.5 regression `walk-matrix.sh:553-577` records. It also **could not run** where it was placed:
   `walk_env` there dies `CELL_ROBOT: unbound variable`, rc=1, **zero stdout** — and walk-matrix is
   `set -uo pipefail` **without `-e`**, so it fails **open**.
2. *Grepping the harness's own `printf 'KEY=%s\n'` block to derive what it emits.* As fragile as the
   list it replaces — refactor those printfs to a heredoc and the emitted set silently shrinks, the
   gate reports false drift on a key that IS supplied, and the cheapest fix is a wrong exemption.

**The design that survives review:** a manifest **in this repo**, `docs/walk-env-manifest.tsv`,
one row per *(scenario, key)* with one of three dispositions and a reason —

    scenario-1  HARBOR_PASSWORD  FORBID  §8.5 must be walked; injecting it skips the branch that reads it
    scenario-2  HARBOR_PASSWORD  EMIT    a tenant IS handed one — that is scenario-2's premise
    scenario-1  VKS_K8S_VERSION  EXEMPT  the walk writes it; a pinned value goes stale within one cut

reconciled **offline, here, in both directions** against the documents, with the harness *reading*
the file rather than hand-typing anything. That puts the refusal in the repo whose edit caused it,
kills the second parse, and makes the per-scenario dimension expressible — which is the only shape
that can hold the §8.5 constraint.

**Prerequisite, measured:** the exporter's parse must first stop dropping **indented table rows**
(12 across the two docs) and **keys inside longer code spans** — `INGRESS_CONTROLLER` and
`HARBOR_INSECURE` are named in scenario-2 tables and were absent from the derived set.

**Done when:** the manifest exists, a gate here fails on a doc key with no row AND on a manifest row
for a key no document names, a `FORBID` row is RED-proven by attempting the emission, and
`walk-matrix.sh` reads the file instead of `walk_env`'s hand-typed heredoc.

## 🟡 B476 — Istio is the only one of our three components installed by HELM when a VKS-native path already exists and is measured

Measured 2026-08-25. The three components install three genuinely different ways:

| | mechanism | client |
|---|---|---|
| Harbor | Supervisor Service — `POST /api/vcenter/namespace-management/supervisor-services` with an entitled `.yml` | `curl` to vCenter |
| ArgoCD | Supervisor Service — same route, same shape | `curl` to vCenter |
| **Istio** | **`helm install`** istio/base + istiod + gateway, images from our Harbor (`46-install-istio.sh`) | `helm` |

But the VKS-native path for Istio **already exists in this repo and is lab-measured**:

    make install-vks-package   PACKAGE=istio.kubernetes.vmware.com
    make uninstall-vks-package PACKAGE=istio.kubernetes.vmware.com CONFIRM=yes

`scripts/vks-package.sh` uses **`kubectl` and `jq` only** — 16 and 4 invocations, **zero** `vcf`,
`helm`, `imgpkg` or `kctrl`. It applies a Carvel **`PackageInstall`** CR (plus a ServiceAccount and
cluster-admin binding named after the package) and the guest cluster's existing kapp-controller
reconciles it. So it needs neither the licensed CLI nor a vCenter API call — a lighter dependency
than either of the other two paths. `vcf package install istio …` is recorded as the **LEGACY**
path (`docs/vks-services/istio.md:21`); the CR is the current one.

MEASURED on a 9.1 guest cluster: **install 20 s, uninstall 11 s**, `istio-system` and every workload
gone afterwards.

**TWO BLOCKERS, both already measured — this is not a one-line switch:**

1. ~~**The package does not pin istiod's memory.**~~ **BOTH BLOCKERS ARE SOLVED, MEASURED
   2026-08-25**, and the values file is committed at `k8s/istio/vks-package-values.yaml`. The
   package's schema **does** pin `requests: {cpu: 500m, memory: 2048Mi}` (this row previously said
   `null` / "inherits upstream" — wrong; see the correction in `docs/vks-services/istio.md`), and
   `istiod-overlay.yaml:350` sets `requests:` from the data-values **unconditionally**
   (`#@overlay/match missing_ok=True`), so an override lands. Rendering the public bundle offline —
   `ytt -f config/ --data-values-file k8s/istio/vks-package-values.yaml`, no cluster touched — gives
   **`istiod requests: {cpu: 100m, memory: 768Mi}`, `replicas: 1`**, and the **ingress gateway ON**
   as a `LoadBalancer` in `istio-ingress` (the same namespace the helm gateway uses).
2. **A third knob nobody had measured: `pilot.replicas` defaults to `2`** in the package, where helm
   defaults to 1. So a naive package install wants TWO istiods at 2048Mi each — the package path is
   not merely *equal* to the helm path's problem, it is **twice as bad**. Pinned to 1 in the values
   file.
3. ~~**THE REAL REMAINING COST — a VERSION DOWNGRADE.**~~ **ACCEPTED BY THE OWNER 2026-08-25.**
   Measured on a live 9.1 guest cluster: the package offers **1.28.5**`+vmware.1-vks.1`; our helm
   path runs **1.30.3**, so switching is a **two-minor downgrade**. Surfaced as a decision because
   it is a real tradeoff (VMware-certified build vs newer upstream) rather than a free swap; the
   owner accepted it. **No open objections remain — this row is approved work, not a proposal.**

**NOT the same question as `istio-existing`.** CLAUDE.md's "attach, never install" is about a mesh
the PLATFORM TEAM already runs — `INGRESS_CONTROLLER=istio-existing` + `47-attach-istio.sh`, which
installs nothing. That stays. This row is about which mechanism WE use when we are the ones
installing on our own guest cluster.

**Why it is worth doing:** it removes helm from the guest-cluster install path entirely, it removes
the `global.hub` override (and with it `make verify-gateway-image`, which exists solely because
helm accepts an unknown `--set` key with rc=0 and silently falls back to the public registry), and
it makes our Istio install the same shape a VKS operator would use.

**AN ADVERSARY ROUND ON THE IMPLEMENTATION (2026-08-25) REFUTED THE DEFAULT FLIP AND FOUND FIVE
MORE THINGS. Three were fixed; these five are design-level and remain open:**

| # | finding | why it is not a code fix |
|---|---|---|
| **F3** | **THE BLOCKER.** `images/images.txt` mirrors istio **1.30.3** — the HELM versions. The package pulls **1.28.5** from its own bundle repo, digest-addressed, which `make mirror` cannot stage. kbld resolves images RELATIVE TO THE BUNDLE, and there is no `global.hub` to redirect them. | Relocating the standard-packages repository is `imgpkg copy` by the **platform team** — outside this repo and outside `make mirror` entirely. |
| **F2** | KinD has **no kapp-controller** (0 hits for `kapp\|carvel\|packageinstall`), so `make e2e-kind` can never exercise the package path — the only local verification that would have caught the F1 crash. | Adding Carvel to the KinD stand-in is its own project. |
| ~~**F6**~~ | ~~teardown leaves a cluster-admin binding~~ **REFUTED 2026-08-25 — non-finding.** The adversary grepped `98-uninstall-all.sh` for `pkgi\|PackageInstall\|vks-package`, found 0, and stopped. But `:242-245` says the guest-cluster half is **deliberately skipped** because deleting the Cluster (`:258`) removes everything inside it — and all three package objects (PackageInstall, `<pkg>-pkg-sa`, `<pkg>-pkg-sa-cluster-admin`) are **guest-cluster** objects, since `vks-package.sh` runs against the guest kubeconfig. `vks-package.sh uninstall` also removes all three itself, including the values Secret. **RESIDUAL:** if the Cluster is not labelled as ours, teardown skips it and the objects survive — but so does everything else, by design. | — |

### 🔴 B478 — the documented KinD quickstart FAILS on a clean checkout, and the repo calls the claim "ENFORCED"

MEASURED 2026-08-25 in a fresh `git worktree` of `origin/main` with no `.env`:

    $ make e2e-kind E2E_FRESH=1
    no .env yet — run 'make env-init' first
    make[1]: *** [Makefile:366: env-check] Error 1
    make: *** [Makefile:845: e2e-kind] Error 2

Chain: `e2e-kind` (Makefile:845) runs `install-all`, which needs `preflight` (Makefile:704), whose
prereqs include `env-check` (Makefile:365 → `scripts/02-env.sh check`). It fails on the **absence of
the `.env` FILE**, not on any value.

**Four claims say otherwise, and one of them calls it enforced:**

| where | claim |
|---|---|
| `README.md:68` | *"**KinD** — see it work. No VKS cluster, **zero `.env`**."* |
| `docs/kind-local.md:6` | *"`make deps` → `make e2e-kind` → `make creds-show` (three commands)."* |
| `docs/kind-local.md:77` | *"`make env-init` — **optional** — KinD needs no `.env`"* |
| `Makefile:201` | *"Ignoring it by default keeps 'make e2e-kind needs zero `.env`' an **ENFORCED** property"* |

And `make deps` — the documented FIRST command — does **not** create `.env`: the only `.env` strings
in `scripts/00-install-prereqs.sh` are comments about `.env.example`. So the three-command
quickstart a new operator is told to run **cannot work on a clean clone**.

⚠️ **Why it went unnoticed for so long, and this is the transferable part:** every run of the KinD
e2e in this repo's history has been on a box that already had a `.env`. The property is only false
in the one state nobody develops in — and the repo's own rule already says a *"zero-config"* claim
must be **VERIFIED ON A CLEAN CHECKOUT**, because *"a local run passes on values only your box
has"*. That rule exists here, is cited in `configuration.md`, and was not applied to this claim.
`scripts/test-env-check.sh` exists and does not catch it: it tests `env-check`'s own logic, not
whether the documented sequence runs.

**Found by accident**, which is worth recording honestly: the e2e was being re-run in a throwaway
worktree to prove an unrelated library change, and the worktree happened to be the clean state the
claim had never been tested in.

**NOT FIXED — an idea round is out.** Three candidates, each with a real cost: (a) make `env-check`
presence-exempt when `SKIP_DOTENV=1` (the run is deliberately ignoring `.env`, so gating on its
presence is incoherent — but does `SKIP_DOTENV` even reach that process through
`$(MAKE) install-all` → `preflight`?); (b) have `e2e-kind` run `env-init` itself (which **renames a
real operator's `.env` to `.env.bak`** — possibly worse than a hard failure); (c) change the docs and
drop the claim (but `Makefile:201` argues the property is deliberate and valuable, so weakening it
may be the wrong direction). Do not implement before the round returns.

### 🔴 B477 — an orphaned kapp-owned `istiod` Service made the mesh unreachable while EVERY object looked healthy (MEASURED on the lab, 2026-08-25)

The walkthrough matrix's row 2 failed on exactly one block — `make verify-ingress`, all 8 hosts
`HTTP 000` after the full 300s-per-host budget (2404s). The chain, measured through the guest
kubeconfig, not inferred:

| # | observation |
|---|---|
| 1 | `vks-uis-istio` gateway pod: **CrashLoopBackOff, 17 restarts**. Log: `failed to sign CSR: … dial tcp 172.21.4.110:15012: connect: connection refused` — it cannot reach istiod's CA, so it never gets a workload cert and aborts. |
| 2 | `istiod` pod: **Running 1/1, Ready=True, 0 restarts**, IP `172.20.2.51`. |
| 3 | `kubectl -n istio-system get endpoints istiod` → **`<none>`**. The EndpointSlice exists with an EMPTY endpoint list. |
| 4 | Service selector: `app=istiod`, `istio=pilot`, **`kapp.k14s.io/app=1787633405480953199`**. Pod labels carry `app=istiod` and `istio=pilot` but **no `kapp.k14s.io/app`** — they say `app.kubernetes.io/managed-by: Helm`, `helm.sh/chart: istiod-1.30.3`. |
| 5 | The **Service** says `helm.sh/chart: istiod-`**`1.0.0`** and carries `kapp.k14s.io/identity` + `kapp.k14s.io/original`. The **Deployment** says `istiod-`**`1.30.3`**, no kapp labels. `sh.helm.release.v1.istiod.v1` exists. ⚠️ **I ALSO WROTE that `kubectl get pkgi -A` shows no istio PackageInstall. THAT WAS FALSE** — see the correction below. |

### 🔴 CORRECTED 2026-08-25 — not an orphan, and not a misreading either. THIS REPO'S OWN TWO ISTIO INSTALL PATHS COLLIDED

Everything below the next heading (`**Reading:**` onward) is the ORIGINAL diagnosis. ⚠️ **It is
REFUTED** — read this first.

**What was actually true**, measured on the live guest cluster (`https://192.168.101.132:6443`):

| when | what | how it is known |
|---|---|---|
| `02:20:41Z` | **helm** installs Istio **1.30.3**, including istiod's Service | helm release `sh.helm.release.v1.istiod.v1`, `first_deployed == last_deployed`; `svc/istiod` managedFields `manager: helm, op: Apply` |
| `04:50:03Z` | **`43-install-istio-package.sh`** installs the VKS **package** into the SAME namespace. kapp **ADOPTS** helm's objects | `istio-pkg-sa`, `istio-pkg-sa-cluster-admin`, `istio-pkg-values` all created at that instant; **30 of the 50** kapp-labelled objects have helm's `02:20:41Z` creationTimestamp |
| then, forever | kapp retries every **10 min** and can never succeed | five `istio.app-change-*` ConfigMaps at `12:00/12:10/12:20/12:30/12:40Z`, all `successful = False` |

So the order is **helm first, package second** — the exact opposite of the original reading — and the
package path did not *install alongside* helm, it **took ownership of helm's control plane**.

**The deadlock:** kapp's label-scoping wants `kapp.k14s.io/app` inside istiod's **Deployment**
`spec.selector`, which Kubernetes forbids as **immutable**. The reconcile can never succeed. But the
retry still re-applies the **Service** (mutable selector), re-inserting the label and zeroing its
endpoints — so proxies lose `:15012` and their XDS config freezes.

**⚠️ THE REAL DEFECT, and it is in this repo, not the lab:** neither install path checks whether the
other already owns the namespace. Measured — `43-install-istio-package.sh` greps for a helm release
**0 times**; `46-install-istio.sh` greps for `kapp`/`pkgi` **0 times**; `44-install-ingress.sh:45`
dispatches on `ISTIO_INSTALL_METHOD` (default `helm`) with **no state check at all**. Running the
documented default and then the documented package variant is therefore enough to silently and
**permanently** hand a helm-installed mesh to a reconciler that can never converge. See [[B480]].

**Two presentations, one cause** — which you get depends on *when* istiod goes away relative to the
proxy's own lifecycle:

| | 2026-08-24 (the original table below) | 2026-08-25 |
|---|---|---|
| proxy pod | **CrashLoopBackOff**, 17 restarts — never got a cert | **Running** — had a cert, lost istiod later |
| ingress | **8 of 8 → `HTTP 000`** (2404s) | **5 → `HTTP 200`, 3 → `HTTP 000`** |

The second is the nastier one: a proxy holding a cert keeps serving its last-known config, so it
looks healthy and mostly works. **CrashLoopBackOff is NOT this bug's signature.**

**The fix, and the trap in it.** A plain `kubectl delete pkgi istio` makes kapp garbage-collect
everything it owns — measured **50 objects, including 14 CustomResourceDefinitions**, so it would
have cascade-deleted every `Gateway`, `VirtualService` and `HTTPRoute` in the cluster, not merely
"the mesh". `spec.noopDelete: true` first, then delete, keeps them (confirmed against the live CRD:
*"delete PackageInstall/App CR but preserve App's associated resources"*).

    kubectl -n vmware-system-tkg patch pkgi istio --type=merge -p '{"spec":{"noopDelete":true}}'
    kubectl -n vmware-system-tkg delete pkgi istio
    kubectl -n istio-system patch svc istiod --type=json \
      -p='[{"op":"remove","path":"/spec/selector/kapp.k14s.io~1app"}]'
    kubectl -n vks-ingress delete pod -l gateway.networking.k8s.io/gateway-name=vks-uis

`spec.paused` and `spec.canceled` also exist and would have stopped the re-breaking while keeping the
reconciler. They were **not** used, deliberately: the immutable-selector deadlock is permanent, so a
paused pkgi is a paused-forever pkgi.

**⚠️ RESIDUAL STATE — the cluster is repaired but NOT clean.** All measured, all still true:

| left behind | why it matters |
|---|---|
| **5 of 7** running Istio containers pull from **`projects.packages.broadcom.com`** — `istio-cni-node` (×3), `istio-support`, and the `vks-uis-istio` gateway proxy | ⚠️ **NOT a defect — this is a deliberate, warned override, and the gate WORKED.** See the correction below. The cluster is genuinely not air-gapped for those five, so `make verify-gateway-image` exits **2** and names them (measured 2026-08-25) — but that is the *recorded consequence of a choice*, not damage. [[B482]] is why the gate never ran here on its own |
| `istio-cni` **1.28.5** under istiod **1.30.3** | a two-minor data-plane skew |
| ClusterRoleBinding `istio-pkg-sa-cluster-admin` → **cluster-admin** | a dangling cluster-admin grant to a ServiceAccount nothing manages (plus `istio-pkg-sa`, `istio-pkg-values`) |
| all **50** objects still carry `kapp.k14s.io/app` in `metadata.labels` | the patch removed it only from `spec.selector`. A future package install mints a new app id → ownership conflict on 50 objects |

**⚠️ ROOT-CAUSED 2026-08-25 — the public-registry images are NOT a defect, and this row said they were.**
The operator asked for the root cause rather than accepting the finding, and the answer is in two log
lines one minute apart:

    00:50  ag1.log  the preflight REFUSED the install:
             "On a lab WITH internet you can proceed deliberately with
              ISTIO_PACKAGE_ALLOW_REMOTE=1 -- knowing ..."
    00:51  ag2.log  re-run WITH the override (04:50:02Z):
             WARN  package images resolve from projects.packages.broadcom.com,
                   which is NOT harbor.env1.lab.test
             WARN  ISTIO_PACKAGE_ALLOW_REMOTE=1 — proceeding. This install is NOT air-gapped

`43-install-istio-package.sh:90-106` reads the Package CR's `imgpkgBundle.image` host and **dies**
unless it resolves from Harbor or localhost. The Supervisor's Package CRs genuinely resolve from
`projects.packages.broadcom.com` (measured — that is how VMware ships them), so it refused, named the
consequence, and offered an explicit escape. The escape was taken, knowingly.

**So the control worked perfectly, and this row originally credited its output as damage.** Two
lessons, and the second is the one that matters:

1. **The registry question and the ownership question are INDEPENDENT.** `ISTIO_PACKAGE_ALLOW_REMOTE`
   governs *where images come from* and is fully guarded. [[B480]] — kapp adopting helm's control
   plane — governs *who owns the objects* and is guarded by **nothing**. Conflating them inflated a
   real finding with a non-finding.
2. **An adversary's finding is not a root cause.** The round graded this HIGH as an unfiled defect and
   was right about the observation; it had no way to reach the cause, which lived in a `/tmp` log from
   a prior session. Writing it into the backlog as a defect without root-causing it is the same
   failure this row already records one paragraph down — accepting a plausible account instead of
   measuring — committed while correcting exactly that.

**On the lesson.** A first draft of this correction claimed the cause was mine — that I had read only
"the first page" of `kubectl get pkgi -A`. **That is false and was itself unverified**: measured,
that command emits **9 lines**, `kubectl get` does not paginate, and `istio` sorts LAST. Inventing a
plausible-sounding explanation for my own error, and writing it down as the transferable lesson, is
the more embarrassing failure — it would have sent the next reader to improve their reading habits
instead of to the ungated collision in [[B480]].

**Reading:** a VKS **Standard Package** Istio was installed via Carvel/kapp — kapp injects
`kapp.k14s.io/app` into selectors — its PackageInstall was later removed, and its **Service
survived**. A later **helm** Istio install created a Deployment whose pods can never match that
orphaned selector. Every object reports healthy; the control plane is unreachable.

**Why this is the DANGEROUS shape, and what it costs:** `make istio-preflight` said the mesh was
fine and named the ATTACH command; `make attach-istio` then reported **success** and applied 8
HTTPRoutes and a programmed Gateway with an LB IP — against a dead control plane. Nothing was red
until a 40-minute poll. The repo's own SYMPTOM GUIDE for `HTTP 000` names the **wrong** cause here
(*"the Gateway's selector matches NO gateway workload"*) — the *gateway's* selector was correct;
**istiod's** was not.

⚠️ **This sharpens the F6 residual above, which I dismissed.** I refuted F6 on the grounds that
deleting the Cluster subsumes teardown, and named the residual — objects surviving when the Cluster
is not deleted — as benign *"but so does everything else, by design."* **It is not benign.** The
survivor here is not one of the three objects F6 names (PackageInstall, `<pkg>-pkg-sa`,
`<pkg>-pkg-sa-cluster-admin`) — it is a **Service**, i.e. the leftover class is **wider than F6
enumerated**, and a leftover does not merely sit there: it **shadows and breaks a later install of
the same component**. An enumerated list of what teardown must remove is the defect.

⚠️ **Adversary verdict: do NOT reopen F6 verbatim.** F6 names PackageInstall + SA +
ClusterRoleBinding, which are exactly the three `vks-package.sh:193-197` **does** delete — filing
this against F6 would record the wrong defect. What is genuinely unclosed is: (a) the **premise**
*"cluster deletion subsumes it"*, which is FALSE for the walkthrough matrix because it **reuses a
cut across rows**; (b) `98-uninstall-all.sh` has **zero** hits for `pkgi|packageinstall`, so
`make uninstall-all` cannot remove a package-installed Istio at all; and (c) **label-scoped
Services are invisible to every teardown this repo has**. That is this row.

**Repair (needs a human — the agent's mutating `kubectl` against the lab is classifier-blocked):**

    kubectl -n istio-system patch svc istiod --type=json \
      -p='[{"op":"remove","path":"/spec/selector/kapp.k14s.io~1app"}]'
    kubectl -n istio-system get endpoints istiod     # must now list the istiod pod IP
    kubectl -n vks-ingress delete pod -l gateway.networking.k8s.io/gateway-name=vks-uis

**Product change — the first design was REFUTED; shipped as PR #995.** I proposed asserting that
*istiod* has ready endpoints. The idea round refuted it: that needs `istio-system`, a namespace a
TENANT does not own (RULE ZERO-B), and it false-REDs on canary/multi-revision and has to reason
about ambient. The prescribed guard instead asserts the **provisioned proxy's pods are Ready**, in
`istio_wait_gwapi_address` — the proxy lives in a namespace **we create** (no foreign RBAC), it
never names istiod, and it is the END RESULT rather than a proxy for it, so it also catches
ImagePullBackOff on an air-gapped pull, PSA rejection and OOM. Three states: Ready / not Ready /
COULD-NOT-ASK, the last degrading to a loud warning rather than blocking a tenant.

⚠️ **One step of the original narrative here was WRONG and is corrected:** I wrote that our helm
installer created the crashing gateway. It cannot have — `istio_apply_routes_gwapi` is called ONLY
by `47-attach-istio.sh`. Measured: Gateway `vks-uis` was created `05:43:18Z`, exactly when row 2's
ATTACH ran, so it is not a leftover and no stale-Gateway sweep is needed. Also measured: the kapp
app ConfigMap `istio` still EXISTS (so this is a partial delete, not a full orphan), and the Service
carries **no** helm ownership annotation — kapp created it and helm never owned it.

**Still open, 6 of 7:** the adversary counted **7** sites asserting a component is PRESENT where
SERVING is what matters, and **0** uses of endpoints/endpointslices as an assertion anywhere in
`scripts/` — against 18 presence-booleans, 9 `rollout status`, 8 `wait --for=condition`, none of
which can see this bug. #995 closes site 1 (`lib/istio.sh:403`). The rest: `47-attach-istio.sh:65`,
`48-istio-preflight.sh:38`, `lib/istio.sh:52`, `lib/istio.sh:65` (the same bug one object over --
it finds a Service with :15021 but never checks its selector matches a pod), `lib/istio.sh:135`,
`lib/istio.sh:513`.
| **F5** | Istio release-1.28 vendors **gateway-api v1.4.1**; we install **v1.5.1** (`GATEWAY_API_VERSION`), the CRDs-newer-than-client direction `check-gwapi-istio-alignment.sh` names as crash-looping. That gate reads `ISTIO_VERSION`, which the package path never sets — **so it stays green while its invariant is false**. | The gate needs to learn the second path. |
| **F7** | The version default **floats**: `PKG_VERSION=""` → `jq sort \| tail -1`, a LEXICOGRAPHIC sort, in a repo built on pinning. Today's six versions happen to sort correctly; a 1.9/1.100 would not. | Pinning it is easy; deciding whether the default should float at all is not. |

### ✅ F5 and F7 are CLOSED — and the FIRST fix for F7 was itself REFUTED (2026-08-25)

Both went out, an adversary attacked them, and it **refuted the F7 fix**. Recording the arc, because
the refuted version is the one a future session would otherwise rebuild.

| | |
|---|---|
| **F7, attempt 1** (#989, merged) | Replaced **one** of the two selection sites with `sort -V` and left the other in jq. |
| **why that is worse** | `sort -V` is **implementation-dependent**. MEASURED over `[1.28.5, 1.28.5+vmware.1-vks.1, 1.28.5+vmware.2-vks.1]` — GNU coreutils 9.4 → `+vmware.2`; **toybox 0.8.9 (Photon 5) → `1.28.5`**; jq (the other site) → `+vmware.2`. So on a Photon jump box `make list-vks-packages` reports one LATEST and `make install-vks-package` installs another. **Before #989 both sites were pure jq and byte-identical on every OS** — the "fix" traded *consistently wrong* for *right-on-GNU / different-on-Photon*, in a repo whose `jumpbox-both` matrix exists to prove the two OSes equivalent. |
| **why my own Photon check missed it** | I tested `1.9.0` vs `1.100.0`, which **agrees** on both implementations. The divergence needs the `+vmware` build suffix. A single operating point that happens to agree is not a portability proof. |
| **F7, attempt 2** (#992) | `sort -V` deleted; **one shared jq key** (`_VKEY`) used by both sites, so divergence is structurally impossible on any OS. The key is **total** (raw string as final tiebreak at a fixed position) and **GA outranks its own prereleases**, so the floating default can never land on an rc. `.spec.version // ""` so one schema-surprise row no longer aborts the listing under a message blaming KUBECONFIG. |
| **F5** (#991) | The gate learned the package path — and the arm shipped **fail-OPEN**: `release-1.8/go.mod` is a real HTTP 200 with **zero** gateway-api lines, so a successful fetch could still extract nothing and return 0 **silently, even under `GWAPI_REQUIRE_FETCH=1`**, while the sibling helm arm exits 1 on the identical condition. Now fails closed. Also: the FLOATS disclosure is the **default** path (`ISTIO_PACKAGE_VERSION` is commented in `.env.example`) and was a quiet `log_info` followed by an unqualified `OK`; now a WARN, and the OK line says **"HELM PATH ONLY — the package path is UNCHECKED"**. |

**Both tests were vacuous or absent, which is the more useful finding:**

- `test-vks-package-version-sort.sh` passed **7/7 with the fix surgically removed** — its `grep -q 'sort -V'` matched the product's own **comment** (the *"greps a SYMBOL NAME, also matches the DOCSTRING"* defect) and its other cases executed a **pasted copy** of the expression rather than the product. Rewritten to sed-extract `_VKEY` from `vks-package.sh` and execute it, with a fatal guard if the extraction stops matching. 7 → 13 cases, 4 RED-proofs.
- `check-gwapi-istio-alignment.sh` had **no test at all** and **could not be RED-proved**: both halves of its comparison are uncommented in `.env.example`, so `load_env`'s `set -a` clobbers any override (measured — `GATEWAY_API_VERSION=v1.4.1` returns rc=0, and so does `SKIP_DOTENV=1`, which ignores `.env` but not `.env.example`). That clobber is **correct** — both are pins coupled to `images/images.txt` — so the seam is a **hermetic tree**, not a production test-only branch. New `test-gwapi-alignment.sh`: 9 cases, 5 RED-proofs. The gate whose header opens *"WHY THIS EXISTS: I GUESSED"* has now been seen to fail.

⚠️ **Three defects were found only on the MERGED tree, never by CI**, because `static-check` is paths-filtered and **SKIPPED on PRs**: a `sed | grep -q` SIGPIPE trap (caught by this repo's own `check-grep-q-pipe`, in the very test written to prove a fix), and **fifteen** `cond && ok || bad` sites across the two tests — the A && B || C fake-green this repo's rules forbid, invisible at `-S warning` because `make lint` runs `shellcheck -x` with no `-S`. Run `env -u GOROOT make static-check` on a merged worktree before merging; the PR green does not cover it.

**Class denominator.** `sort -V | tail -1` production sites: **2**. The other is `scripts/24-vks-k8s-version.sh:115`, which selects the **TKr** — the Kubernetes version of the cluster you are about to create. Measured: GNU and toybox **agree** on realistic TKr strings (`v1.34.1---vmware.1-vkr.4`), because TKrs carry no `+build` metadata, which is what diverges. Same unguarded class, **no live defect**; deliberately left alone rather than churned.

⚠️ **AND THIS LAB CANNOT DETECT F3**, which is why the preflight exists. `PackageRepository
standard-packages` reports `Reconcile succeeded` — it pulled from Broadcom, so **the lab has
internet**. `make install-vks-package` would go **green here and prove nothing**: the same
dual-homed false-green this repo already documents for the builder base image. The new
`43-install-istio-package.sh` therefore REFUSES when the bundle host is not local/mirrored
(RED-proven against this lab: rc=1), with `ISTIO_PACKAGE_ALLOW_REMOTE=1` as a deliberate,
documented opt-out that says out loud the run proves nothing.

**FIXED in the same round, each re-proven:** the path died at `istio_apply_routes` because it never
set `ISTIO_GATEWAY_LABEL`/`ISTIO_GATEWAY_SERVICE` (F1 — it died AFTER creating 8 namespaces and
installing the package); the `helm` escape hatch was itself CLOBBERED because the new key was not in
`load_env`'s snapshot (F4 — verbatim the incident at `lib/os.sh:507`, proven fixed with a control);
the `envsubst` render was a NO-OP because the values file had no `${}` tokens at all (F8); the
values file still carried my superseded "leaves it NULL" comment (F9); and `48` collided with
`48-istio-preflight.sh` (F11 → renamed 43).

**The 768Mi claim SURVIVED both rounds and is the best-evidenced thing here:** exactly ONE istiod
Deployment in the rendered output, carrying the unresolved image AND `requests: {cpu: 100m, memory:
768Mi}` and `replicas: 1` in the SAME object — and kbld, the only remaining pipeline stage, rewrites
image fields only.

**What is left is ONLY the live proof.** Rendering settles that the values reach istiod; it cannot
show that kapp-controller reconciles, that the LoadBalancer gets an IP, or that traffic routes. A
live test needs `helm uninstall` of the three releases first (they would fight: two istiods, two
mutating webhooks), and the gateway gets a **NEW** LB IP, so the `*.vks.local` `/etc/hosts` entries
must be repointed before `make verify-ingress` means anything.

**Done when:** `INGRESS_CONTROLLER=istio` installs via `install-vks-package` with
`k8s/istio/vks-package-values.yaml`; `make verify-ingress` is green on a real lab; the 1.28.5-vs-1.30.3
downgrade is an accepted decision rather than an unnoticed side effect; and `docs/vks-services/istio.md`
records which path is the default with its measurement.

## ✅ B490 — we LOGGED a ClusterClass the Supervisor silently overrides — FIXED 2026-08-26 (the "hardcoded default" framing was REFUTED)

MEASURED on the live lab 2026-08-26, VKS `3.6.3-embedded+v1.35`:

    scripts/25-vks-cluster-create.sh:39  export VKS_CLUSTERCLASS="${VKS_CLUSTERCLASS:-builtin-generic-v3.6.0}"
    .env.example:1327                    # VKS_CLUSTERCLASS=builtin-generic-v3.6.0     <- COMMENTED, so the code default applies
    scripts/24-vks-k8s-version.sh        picks the NEWEST Ready+Compatible TKr and publishes VKS_K8S_VERSION

Two axes of the same cluster spec, two opposite policies. `spec.topology.version` self-updates with
the platform; `spec.topology.classRef.name` does not — so the day VKS moves, every cluster this repo
creates is pinned to the class of the release BEFORE the one that is installed, and nothing goes red.

Live evidence that the axis really moves:

    vmware-system-vks-public: builtin-generic-v3.1.0 … v3.6.0    <- tops out exactly at the installed VKS 3.6
    guest cicd-gc0826070115:  classRef builtin-generic-v3.6.0, version v1.35.6+vmware.2
    TKrs:                     v1.36.1 and v1.36.2 present but Ready=False Compatible=False

The `+v1.35` suffix in `3.6.3-embedded+v1.35` is the max k8s minor the SERVICE supports — which is
why both 1.36 TKrs are `Compatible=False` today, and it is the reason the class list stops at v3.6.0.

**Why this is a re-certification blocker, not a nit.** Certifying the matrix on VKS 3.7 means guest
clusters must actually land on 3.7's class. With the hardcode they land on v3.6.0, the run goes
green, and the certification says "3.7" about a tree that exercised 3.6's ClusterClass.

**Direction (NOT yet designed, NOT adversary-reviewed — do not implement from this paragraph).**
The obvious move is a `24`-sibling that discovers the newest `builtin-generic-v*` in
`VKS_CLUSTERCLASS_NAMESPACE` and publishes `VKS_CLUSTERCLASS`. It is NOT obviously right:

- **newest-class and newest-TKr are two independent picks that must be COMPATIBLE with each other.**
  Picking each independently is exactly the "version-locked release train" trap — nothing here
  asserts the pair is supported, and a class/TKr mismatch is an admission rejection at create time.

- a ClusterClass has no `Ready`/`Compatible` condition to gate on the way a TKr does, so the
  discovery has no equivalent readiness signal — "newest by name" is a semver sort over a name, i.e.
  the enumerated-list rot in a different costume.

- **an EXISTING cluster is not migrated by changing the default.** Rebasing a live guest from
  v3.6.0 to v3.7.0 is a separate, mutating operation with its own blast radius.

### MEASURED 2026-08-26 after the lab reached VKS 3.7.1 — the platform DECLARES the pairing

The pairing is not a guess and does not need an enumerated table: **each ClusterClass carries the
Kubernetes range it supports as LABELS the platform sets.**

    builtin-generic-v3.6.0   kubernetes.vmware.com/min-version-supported=v1.32   max-version-supported=v1.35
    builtin-generic-v3.7.0   kubernetes.vmware.com/min-version-supported=v1.33   max-version-supported=v1.36

So the two axes are **not** independent, and "pick the newest class" is the WRONG rule — it is right
by luck today and wrong the moment a class ships that the chosen release predates (v3.7.0's floor is
already v1.33, so a 1.32 cluster must NOT take it).

**The rule that follows from the data:** `24-vks-k8s-version.sh` picks the Kubernetes version first
(newest Ready+Compatible); the class is then the **newest class whose declared
`[min,max]` range CONTAINS that version**. One direction, no circularity, and the constraint is read
from the object rather than typed here — so it cannot rot as classes are added.

Concretely, with the lab now on VKS 3.7.1 the newest Ready+Compatible release is a **1.36**, and
v3.6.0's `max-version-supported=v1.35` **excludes it** — the hardcoded default is now actively
wrong, not merely stale.

⚠️ Two things still NOT established, and the second is the one that decides how loud the failure is:

- whether admission REJECTS an out-of-range pair (loud, fine) or silently resolves something else
  (quiet, and then a green matrix certifies the wrong class — the outcome this row exists to
  prevent). `k8s/vks/cluster.yaml:21-28` already records that `spec.topology.version` is a PREFIX
  selector that admission REWRITES, so a quiet resolution is not far-fetched.

- the v3.7.0 variable SCHEMA differs from v3.6.0's (`k8s/vks/cluster.yaml:116-120` warns the
  variable names are ClusterClass-version-specific; the lab doc records
  *"bootstrapAddons variable cannot be set when KR version < 1.35.0"*). Changing the class without
  re-checking the schema swaps one silent breakage for another.

### 🔴 THE IDEA ROUND REFUTED ALL OF THIS — the platform already does the selection (MEASURED 2026-08-26)

I was about to build a script to compute `VKS_CLUSTERCLASS`. **The Supervisor's mutating webhook
already implements exactly that algorithm.** Six server-side dry-runs against non-existent cluster
names (nothing created; `kubectl get cluster -A` unchanged before and after):

    asked v3.6.0 + v1.36.2  (OUT of range)  -> rc=0, STORED builtin-generic-v3.7.0
    asked v3.6.0 + v1.35.6  (IN range)      -> rc=0, STORED builtin-generic-v3.7.0
    asked v3.5.0 + v1.33.13-fips (IN range) -> rc=0, STORED builtin-generic-v3.7.0
    asked v3.1.0 + anything                 -> rc=1, REJECTED (schema: vsphereOptions undefined)
    stderr: "Warning: ClusterClass builtin-generic-v3.6.0 updated to the newest compatible
             ClusterClass builtin-generic-v3.7.0"

The second row is decisive: **it bumps even when the asked class is already in range.** So
`VKS_CLUSTERCLASS` is not *stale*, it is **inert** — a computed value admission would discard.

Three further premises of mine were measured FALSE, and one of them would have CREATED a bug:

- **The manifest sends FOUR variables, not six.** `bootstrapAddons` appears exactly once in this
  repo — in this backlog, quoting the lab doc. The extra names I "saw" are what the webhook
  DEFAULTS into the result. "Fixing" the manifest to carry `bootstrapAddons` would have ADDED the
  variable the lab doc says cannot be set below KR 1.35.
- **v3.6.0 and v3.7.0 have IDENTICAL variable schemas.** The schema cliff is at **v3.1.0** (it
  carries `nodePoolVolumes` and lacks `vsphereOptions`/`volumes`) — the opposite end of the range
  from where I aimed the risk.
- **A `sort -V` range check matches NOTHING.** The labels are 2-component (`v1.36`), the chosen
  version is 3-component with `+vmware.N-vkr.N`, so `sort -V` puts the chosen one on top and every
  class is excluded → a fail-closed die on a healthy lab. (Naive string compare is separately
  broken: `v1.9.1 > v1.35` lexically.)

### The REAL defect, and it is live TODAY with no new script

It resolves **silently**: rc=0, a `Warning:` on stderr, and `25-vks-cluster-create.sh:74` logs
`class: builtin-generic-v3.6.0` for a cluster that will be **v3.7.0**. Nothing ever reads the stored
value back — `grep -rn 'classRef' scripts/` returns zero hits outside the template. Worse, the one
path built to catch things throws the evidence away: the pre-apply dry-run sends stderr to
`${RENDERED}.err`, prints it only in the failure arm, and `rm -f`s it **unread** on success.

**So a green six-row certification would record "v3.6.0" for clusters that are v3.7.0** — precisely
the outcome this row exists to prevent, reachable with no code change at all.

**Done-when (the corrected fix, ~5 lines in `25`, no new target and no new scenario-1 step):** after
the apply, read `-o jsonpath='{.spec.topology.classRef.name}'`, log THAT, and warn when it differs
from what was asked; and stop discarding the dry-run stderr on success. This is the same discipline
`k8s/vks/cluster.yaml:21-28` already mandates for `spec.topology.version` (*"the ground truth is the
LABEL on the created object, never spec.topology.version"*) — the class has the identical shape and
was simply never covered. It is also correct under **both** behaviours: if a future VKS stops
auto-bumping, a read-back is still right, whereas the discovery script would be right under only one.

⚠️ Residual, named: the auto-bump is a measured fact about **VKS 3.7.1+v1.36 on one lab via one
webhook** (`capi.mutating.tanzukubernetescluster.run.tanzu.vmware.com`). It is not a promise.
Whether it also fires on UPDATE was deliberately NOT tested (that dry-run would target the live
cluster). And `docs/scenario-1.md:579` tells the operator to `kubectl get clusterclass -A` and pick
"the newest Ready one" — `-A` spans three namespaces whose tenant copies are the v3.1.0–v3.3.0 ones
that would be REJECTED, and the answer is overridden anyway.

### SHIPPED 2026-08-26 — the read-back, not the discovery script

`scripts/25-vks-cluster-create.sh`: the request line now says **REQUESTED; admission may rewrite
it**, the dry-run's stderr is PRINTED on a rc=0 run instead of `rm -f`'d unread, and the class is
read back off the created object and reported — naming both values and which one to cite.

`scripts/test-vks-class-readback.sh`, **7 cases, 0 failed**; against the unfixed script the same
test scored **2 passed / 5 failed**, and its RED output is the defect verbatim:
`msg=class:        builtin-generic-v3.6.0` logged as fact for a cluster that would be v3.7.0.

One more finding from the round, applied: the ALREADY-EXISTS refusal also stops a re-apply letting
the webhook bump a **live** cluster's class and roll its nodes — a rebase, not a create. Its comment
now says so, and points at creating under a new name rather than at the lab repo (RULE ZERO-B).

Still open from that round, deliberately NOT bundled: `docs/scenario-1.md:579` tells the operator to
`kubectl get clusterclass -A` and pick "the newest Ready one" — `-A` spans three namespaces whose
tenant copies are the v3.1.0–v3.3.0 ones that would be REJECTED, and the value is overridden anyway.
That is a docs change with its own blast radius (it is a scenario-1 step) and wants its own review.

## ✅ B491 — VKS 3.7.1 blocked by a stale SUPERVISOR Package — RESOLVED 2026-08-26; the lab is now on 3.7.1

MEASURED on the live lab 2026-08-26. Recorded because the diagnosis is counter-intuitive: vCenter's
catalogue and the Supervisor's Carvel Package are **two different stores**, and re-registering the
YAML fixes only the first.

### The failure

`make -C nested-vsphere-lab vks-upgrade VKS_VERSION=3.7.1 CONFIRM=3.7.1` -> HTTP 500
*"Supervisor Service (tkg.vsphere.vmware.com) version (3.7.1+v1.36) … is not compatible."*
The vSphere UI wizard is more specific — Signature Verification **ERROR**:

    Connectivity to image depot.kube-system.svc/vcf/supervisor-service-vks/ga/3.7.1/…:3.7.1 cannot be
    verified: Get "https://depot.kube-system.svc/v2/": lookup depot.kube-system.svc … no such host

### Root cause

The operator first registered the **depot**-flavoured YAML, then replaced it with the **legacy** one.
vCenter's catalogue took the swap; the Supervisor's Package did not:

| store | what it holds |
|---|---|
| vCenter catalogue, version `3.7.1+v1.36` | the LEGACY yaml — 9108 b64 chars -> **6830 bytes**, image `projects.packages.broadcom.com/…` (byte-identical in size to the legacy file on disk) |
| Supervisor `Package` in `vmware-system-supervisor-services` | still `depot.kube-system.svc/vcf/supervisor-service-vks/ga/3.7.1/…` |

There is no `depot` Service in `kube-system` (VCF Operations / Software Depot is out of scope for
this lab), so the Supervisor's lookup can never resolve.

### Facts that CLOSE the obvious competing explanations

- **It is not a version incompatibility.** Compatibility Check is a *Warning*, and its text is
  *"…on a single-node control plane Supervisor can cause downtime…consider scaling out to a 3-node
  control plane first."* Nothing about ClusterClass, TKr, or Supervisor k8s.

- **`trust_verified: false` is NOT the blocker.** It is `false` on **BOTH** versions — including
  `3.6.3-embedded+v1.35`, which is installed and running right now. A field that reads the same on
  the broken and the working version discriminates nothing. (This was a real hypothesis until the
  control was measured.)

- **Supervisor egress to `projects.packages.broadcom.com` is PROVEN**, so the legacy image is
  reachable: `harbor.tanzu.vmware.com 2.14.3+vmware.2-vks.1` and
  `argocd-service.vsphere.vmware.com 1.1.0-25100889` both fetch from that host and both report
  `Reconcile succeeded`. 79 Packages reference it.

- **A `wcp` restart does NOT clear it** (already tried; `RunState: STARTED`, 500 unchanged).

### Route facts (probed with a BOGUS version id, so nothing real could be touched)

A semantic 404 that echoes the id means the route EXISTS; a bare `Not found.` means it does not.

| route | verb | verdict |
|---|---|---|
| `…/supervisor-services/{svc}/versions/{ver}` | DELETE | **exists** |
| `…/versions/{ver}?action=deactivate` / `?action=activate` | PATCH | **exists** |
| `…/versions/{ver}?action=deactivate` | POST | absent |
| `…/versions/{ver}?action=setDefault` | PATCH | absent |
| `…/versions/{ver}` (update content in place) | PUT / PATCH | **absent** |

So **there is no in-place content update** — delete + re-register is the only path — and a
**per-VERSION** deactivate exists, so the whole service never has to be deactivated.

⚠️ `scripts/deregister-supervisor-service.sh` is NOT usable here: it deactivates the whole service
and loops DELETE over **every** version, which would take out the running 3.6.3 VKS.

### The END-RESULT criterion (not a proxy)

An HTTP 200 from the upgrade proves nothing. The observables are:

    v1.36.2---vmware.2-vkr.3   Compatible=False -> True      (today both 1.36 TKrs are False/False)
    builtin-generic-v3.7.0     absent -> present in vmware-system-vks-public   (today it stops at v3.6.0)

`3.6.3-embedded+**v1.35**` — the `+v1.35` suffix IS the max k8s minor the service supports, which is
exactly why both 1.36 TKrs are `Compatible=False` today.

### RESOLVED — what actually fixed it, and the order that made it safe

The first plan (delete the 3.7.1 version, re-register, upgrade) was **REFUTED by an adversary**: it
would have run an irreversible delete to test a hypothesis that was testable read-only, and
registration of a Core service over the public API is a measured HTTP 403 (UI only), so a failed
re-register would have left the lab with no 3.7.1 and a manual-only restore.

**The additive proof came first, and it destroyed nothing.** Registering **3.7.0** — a version that
did not yet exist — built its Package from the uploaded bytes:

    3.7.0+v1.36  created 17:05:30Z  projects.packages.broadcom.com/...   <- from the LEGACY file
    3.7.1+v1.36  created 16:22:48Z  depot.kube-system.svc/...            <- fossil of the depot upload

That single observation established **both** facts at once: registration DOES build the Package from
content, and the 3.7.1 Package was therefore a leftover the legacy re-upload never replaced.
Registering a version that ALREADY EXISTS does not rebuild its Package — which is why the operator's
remove-and-re-upload had not worked.

Then, in order:

| step | result |
|---|---|
| `make vks-upgrade VKS_VERSION=3.7.0` | **CONFIGURED, 2m31s** — no HTTP 500 |
| UI Manage Versions -> DEACTIVATE 3.7.1 -> DELETE | *"successfully deleted"*; `Supervisors: 0`, 3.7.0 untouched |
| **checkpoint:** is the stale Package gone? | **yes** — the delete cascaded to the Supervisor |
| UI Add New Version -> upload `-legacy-3.7.1` | Package REBUILT at 17:14:02Z with the broadcom image |
| `make vks-upgrade VKS_VERSION=3.7.1` | **CONFIGURED, 2m02s**, `Reconcile succeeded` |

End state, verified against the criteria above rather than the exit code:

    svc-tkg.vsphere.vmware.com   3.7.1+v1.36   Reconcile succeeded
    v1.36.1 / v1.36.2 TKr        Ready=True    Compatible=True     (were False/False)
    builtin-generic-v3.7.0       present                            (list stopped at v3.6.0)
    guest cicd-gc0826070115      Available=True AddonsReconciled=True, untouched

Two predictions from the lab doc did NOT hold here and are recorded so they are not re-trusted:
`Ready` stayed False was expected (the content library, not VKS, supplies it) — **both 1.36 TKrs
went Ready=True anyway**; and the compatible catalogue was expected to SHRINK 12 -> 9 — this lab
reads **13**.

The recovery procedure is now documented in the lab repo at `docs/VKS-UPGRADE.md` §2
(*"If you already registered the NON-legacy file…"*), because the doc warned against the non-legacy
file but never said what to do once it had been registered.

⚠️ **STILL OPEN and unchanged:** `registered_by_default` is TRUE only for `3.6.3-embedded+v1.35`, so
**every lab cut still reverts VKS to 3.6.3** and the register+upgrade must be re-applied per cut.
That belongs in `docs/matrix-standing-rules.md` §A — not yet written, because the re-certification
run plan is not settled (see B490, which blocks it).

### Consequence for the matrix (measured, and it changes the run plan)

    3.6.3-embedded+v1.35   registered_by_default: TRUE     <- ships with the Supervisor image
    3.7.1+v1.36            registered_by_default: FALSE    <- registered by hand

The lab pins no VKS version (`nested-vsphere-lab` has no such key), and 3.6.3's Package image is
`docker-registry.kube-system.svc:5000/tkg-svs/…` — the Supervisor's own embedded registry. So **every
lab cut reverts VKS to 3.6.3**, and certifying the matrix "on 3.7" requires the register+upgrade to
be re-applied after **each** cut (the six-row matrix cuts twice). That belongs in
`docs/matrix-standing-rules.md` §A once the upgrade is proven to work.

## ✅ B494 — a BACKLOG ID is a magic word in `check-walk-env-manifest`'s escape list; 5 rows hang on the literal `B471`

MEASURED 2026-08-26 (an adversary round on B492 found it; re-measured independently here).

`check-walk-env-manifest.sh:110` lets an ORPHAN row escape when its free-text `reason` matches:

    *"never names it"* | *"no document names"* | *B471*

The third is a **backlog row ID**, substring-matched against prose. Ten rows in
`docs/walk-env-manifest.tsv` mention `B471`; **five escape on that literal ALONE** —
they carry neither of the other phrases:

    VKS_STORAGE_POLICY   VCENTER_HOST   VCENTER_USERNAME   VCENTER_PASSWORD   HARBOR_STORAGE_CLASS

Two failure directions, both live:

- **A copy-edit flips the gate RED for no semantic change.** All five reasons read perfectly well
  without the `B471:` prefix, so anyone tidying the prose silently turns five rows into ORPHANs and
  reddens `static-check` having altered no decision.
- **Any FUTURE reason that mentions B471 for an unrelated purpose gets a free pass**, because the
  match is a bare substring with no structure behind it.

**CLOSED 2026-08-26.** Measurements re-derived first, with one correction to this row: **8** rows
mention `B471`, not ten — the five-escaping-on-the-literal-alone figure is exact, and the five names
match. Before touching anything I checked every reader, because adding a column silently breaks any
consumer that takes the LAST field: `check-walk-env-manifest.sh:57` validates `[ "$n" -lt 4 ]`
(**less than**, so a 5th field is already legal) and the harness reads
`read -r m_scen key disp _` (the `_` absorbs it). Verified `disp` stays `EMIT`, unpolluted, on a
5-column row. 4- and 5-column rows now coexist in the file.

RED-proven three ways, and the third is the one that matters:

    copy-edit `B471:` out of a reason  -> rc=0   (was RED; the defect this row is about)
    remove the flag from a row         -> rc=1   ORPHAN, by name
    flag removed, OLD PROSE left in    -> rc=1   ORPHAN -- prose no longer votes

Two cases added to `test-walk-env-manifest.sh` pinning both halves (12 -> 14 passing, so the corpus
grew rather than going green at the same count).

**Fix:** replace prose-substring matching with an explicit 5th TSV column (e.g. `undocumented=yes`),
so the escape is structural rather than a magic string. Same-repo, gate-local, no cross-repo change.

Do NOT "fix" it by adding more escape phrases — that is the enumerated-list rot one level down.

## 🟢 B495 — REFUTED, recorded so nobody rebuilds it: do NOT make `walk-env-keys.sh` honour `<details>`

B492 records that the two machine readers of the scenario docs disagree about `<details>`, and the
obvious tidy-up is to align them. **An adversary round refuted that, measured.**

- Narrowing the gate yields **45 doc pairs vs 68 manifest rows → 13 ORPHANs, rc=1** (from a current
  58 pairs / 0 orphans). Not a no-op.
- **Four of the thirteen are scenario-2 `EMIT` rows** — `SUPERVISOR_HOST`, `VCF_CLI_VSPHERE_PASSWORD`,
  `VKS_CONTEXT_NAME`, `VKS_USERNAME`. Deleting them to go green removes the assertion that the
  harness supplies the tenant's SSO identity.
- **The readers do not disagree** — they answer different questions. The manifest's own contract is
  *"what the harness MAY PRE-SUPPLY"*, and `check-walk-env-manifest.sh:101-104` says outright that
  *"forbidding a key the document does NOT name is the strongest form of the row, not a defect."*
  Doc-naming is a TRIGGER for requiring a decision, not the decision.
- **`<details>` is one of THREE divergence axes**, so aligning on it is a half-fix: `walk-doc.sh:423`
  also requires the key in the FIRST CELL (measured: 5 more scenario-2 keys differ on that alone),
  and `:440` splits rows by value shape.
- **Broader-guards-narrower is the intended architecture here, twice** — `walk-doc.sh:518` computes
  `INDEP_E` with a deliberately broader regex so `:798` can REFUSE when the parser attached fewer,
  and `check-expect-literals.sh:297-312` records that widening the parser was designed and
  **refuted as certification-blocking**.

If the asymmetry bothers you, spend it on **B494** instead — that is a real rot with two live
failure directions.

## ✅ B492 — the two machine readers of `scenario-1.md` DISAGREE about `<details>`, so a harmless-looking docs edit turns a gate RED

MEASURED 2026-08-26 while fixing B490's docs half. Recorded because the surprise is structural, not
a one-off, and it will bite the next person editing a scenario document.

| reader | honours `<details>`? | consequence |
|---|---|---|
| `scripts/walk-doc.sh` (the WALK) | **yes** — table-row env extraction is gated on `details is None` | rows inside an `<details>` block are invisible to the walk |
| `scripts/walk-env-keys.sh` (the GATE) | **no** — it greps every `^\s*\|` table row for ALL-CAPS backtick tokens | every such row creates a `walk-env-manifest.tsv` obligation |

So the gate's ground truth (*the document NAMES this key*) is strictly broader than the walker's
(*the document INSTRUCTS this key*). A row in an optional `<details>` table therefore owes a manifest
entry for a key the walk can never use — and writing `$VKS_CLUSTERCLASS_NAMESPACE` into that row
turns `check-walk-env-manifest` RED (measured: `rc=1`, *"A scenario document names these, and
docs/walk-env-manifest.tsv has no row for them"*). Writing the LITERAL namespace is green.

Deleting such a row is also a TWO-file change: measured, removing it alone yields
`ORPHAN 1 VKS_CLUSTERCLASS ... rc=1`, because `walk-env-manifest.tsv`'s EXEMPT reason does not match
the escape phrases at `check-walk-env-manifest.sh:112`.

Measured, for anyone who needs the numbers: the walk extracts **44 blocks / 23 env instructions**
from `scenario-1.md`, and `VKS_CLUSTERCLASS` reaches `env`: **False** — as do `VKS_VM_CLASS`,
`VKS_STORAGE_CLASS`, `VKS_NODE_COUNT` and `VKS_CONTROL_PLANE_COUNT`. So wording in that table cannot
change what a certification exercises.

### Second, smaller: `check-expect-literals`'s rationale is broader than its mechanism

Its failure text says *"Fix one of the two copies — **the log line** in scripts/, or the Expect line
in the document"*, and *"The next matrix row asserting it will go EXPECT UNMET and exit 1"*. Both
overstate what it proves:

- The literal `v1.36.2+vmware.2-vkr.3` is satisfied by a **COMMENT** (`24-vks-k8s-version.sh:9`), not
  by any log line. The gate proves the string exists under `scripts/`, not that code can emit it.
- The "will go EXPECT UNMET" claim assumes an extraction broader than `walk-doc.sh:452`, which
  appends only lines **starting with** `**Expect`. Measured on the real file: 36 Expect lines, and
  the version literal — which sits on the CONTINUATION line — is on **none** of them, so the walk
  would not have asserted it at all.

Both directions fail SAFE, so this is not a defect to rush. It is worth knowing before someone reads
that message as a statement about runtime behaviour and goes looking for a log line that was never
supposed to exist.

**CLOSED 2026-08-26 (second half only; the `<details>` asymmetry above is RECORDED, not changed).**
All four measurements re-derived independently before acting: the message did claim *"the log line"*
(`:354`); the version literal is matched by exactly one occurrence under `scripts/` and it is a
**COMMENT** (`24-vks-k8s-version.sh:9`); `walk-doc.sh:452` appends only `line.startswith('**Expect')`;
and of **36** Expect lines in `scenario-1.md`, **0** contain that literal — it sits in prose at `:518`.

The failure text now says what it MEASURES (a copy of the string exists under `scripts/`, which a
comment satisfies) and no longer promises a consequence that does not follow for every literal it
flags. The header states that the extraction is **deliberately broader** than `walk-doc.sh:452` and
why: narrowing it to the column-0 rule would stop catching a literal that a future continuation-line
or table-cell extraction WOULD assert, and every direction here fails safe — a flagged literal is a
string to reconcile, a missed one is a live-lab failure 25 minutes in. Not narrowed, per this row.

RED-proven: planting an unmatchable `**Expect:**` literal fires the corrected message; removing it
returns `94 literal(s) checked, 9 allowlisted, 0 MISSING`.

**Done-when:** the message says what it measures (*"a copy under scripts/"*, not *"the log line"*),
and either the gate's extraction matches `walk-doc.sh:452` or it states plainly that it is
deliberately broader. Do NOT narrow it to match without checking what that stops catching.

## ✅ B497 — `make verify`'s port-forward can bind a doomed pod, and the harness blames the page

**It cost row 4 of the VKS 3.7.1 certification on 2026-08-26.** `31 ran, 1 FAILED, 13 skipped` —
the ran/skipped/neutralized counts matched their pair exactly; one block failed.

`kubectl port-forward svc/X` selects ONE pod and does not follow endpoints. kubectl's own help
(v1.36.4, verbatim): *"The forwarding session ends when the selected pod terminates, and a rerun of
the command is needed to resume forwarding."* Every app here is `replicas: 2` with **no `strategy:`
block** (6/6, `deploy/*/deployment.yaml:9`), so the default RollingUpdate is
`maxSurge=1/maxUnavailable=0` and old pods **are** terminated — during the very rollout `verify` has
just triggered. So the tunnel dies by construction; only luck decides which app is hit.

**MEASURED across the whole walk archive: 237 app-verify attempts over 49 rows, exactly ONE failure
of this shape (0.42%), first seen 2026-08-26** — so the forward binds a survivor ~236 times out of
237. Over a 36-app-verify matrix that is **≈14% of runs losing a row**. A lower bound: a tunnel that
died *after* the marker was seen leaves no trace.

**MEASURED IN A THROWAWAY CLUSTER 2026-08-26 — the mechanism, not an inference.** A 2-replica
Deployment (`terminationGracePeriodSeconds: 30`), a `port-forward svc/web` bound exactly as
`99-verify.sh:251` does, then a rollout:

    t+33s  curl_rc=0   http=200  pf_alive=yes  pods=4    <- 2 old + 2 new
    t+36s  curl_rc=52  http=000  pf_alive=yes  pods=2    <- BROKEN, and the rc is 52, not 7
    t+39s  curl_rc=7   http=000  pf_alive=NO   pods=2    <- dead

Two things this settles that reading logs could not: the tunnel dies at the **graceful-termination
tail** (~36s, matching the 30s grace period), which is precisely the window between `rollout status`
returning and the old pods going away; and **rc=52 arrives BEFORE rc=7**. The original design keyed
on rc=7 alone and would have read that first failure as "marker absent" — the adversary refuted that
on paper, and this is the measurement.

⚠️ **It is a NARROW RACE, not a certainty — an earlier draft of this row said "dies by construction"
without the condition, and that is wrong.** It dies by construction *only when it binds a pod the
rollout is about to remove*. And the `rollout complete → app HTTP up` gap was ≤1s in **94 of those
237** attempts (40%), so an "instant" rollout status is the NORMAL case and is **not** by itself
evidence of anything.

The harness then reported the wrong thing. From row 4:

    00:46:53  FATAL [rustwebapp] end result not observed   ("the page does NOT show the marker")
    00:47:05  OK    rustwebapp.vks.local served the greeting page through the ingress   <- ALIVE, 12s later

Six defects, all in one function:

| | |
|---|---|
| `port-forward svc/` binds one doomed pod | now binds a **named pod** already running the new image |
| `rollout status`'s rc was **DISCARDED** | now checked, **and** we settle until every pod reports `$img` |
| curl's `rc=7` used as the tunnel-death test | **insufficient** — accept-then-RST gives 56, accept-then-hang 28, and *those leave no trace at all*. Classification now asks **kubectl**, which needs no tunnel |
| all three curls **unbounded** | `wait_for` only tests its deadline *between* attempts, so one hanging attempt made `READY_TIMEOUT_SECONDS` not a bound. `--max-time`/`--connect-timeout` added |
| the failure diagnostic re-fetched down the **same dead tunnel** | so it printed nothing, and the archive cannot separate *tunnel died* from *marker absent*. The last non-empty body is now **cached** and printed |
| all six apps shared ONE local port | `:69`'s `pick_port` fallback was dead — `:28` sets `APP_LOCAL_PORT` unconditionally |

Still **FATAL** either way — a certification cannot pass on an unobserved end result, and an
"INCONCLUSIVE ⇒ don't fail the row" bucket is the false-GREEN that lets a crash-looping app certify.
Only the token changes: `PRODUCT` vs `HARNESS-TUNNEL`, so the operator knows whether to retry the row
or debug the app.

**Refuted alternatives, recorded so nobody rebuilds them:** curl through the **ingress** — it does not
exist yet (`verify` is walk step [35], the ingress installs at [36]+), `creds.sh:128` says verify uses
a port-forward *"precisely so it needs none"*, and a tenant cannot get a hostname on a shared Gateway;
`kubectl exec` — the apps are **distroless, no shell, no curl**; an in-cluster probe pod — needs a
mirrored image (`curlimages/curl` is not in `images/images.txt`) plus `restricted`-PSA overrides.

**Residual, named:** which of two readings caused row 4 specifically — probed a stale pod (the
discarded `rollout status` rc; it returned in **<1s**) vs a pure tunnel death — **cannot be recovered
from the archive**, because the harness captured no page content and no pod state. The cached-body
instrumentation is what settles it on the next occurrence. Not verified against a live cluster.

## 🔴 B500 — `harbor_credential_settle --may-reconcile` is a NO-OP, and two of its premises are unmeasured

**Shipped 2026-08-27 in #1055.** The verify half is real and RED-proven; the reconcile half is a
deliberately-unimplemented placeholder, disclosed at the call site and in `lib/harbor.sh`. This row
exists so that disclosure does not quietly become permanent.

### What is real today

`06-install-harbor.sh` step 8b AUTHENTICATES once at the end of the install. Harbor honours
`HARBOR_ADMIN_PASSWORD` only at ITS OWN first bootstrap (goharbor `updateInitPasswordWithMgr()`,
v2.15.2 lines 76-92 — cite the SYMBOL, the line numbers move), so a re-install silently keeps the
old password while every readiness probe passes, because `/api/v2.0/health` is UNAUTHENTICATED. A
401 now dies at the install instead of ~20 minutes later in `21-mirror-push.sh`.

### 1. `--may-reconcile` does nothing

It behaves exactly like `--verify-only`; the only difference is one extra `log_error` line. ArgoCD
(`07-install-argocd.sh:163-181`) and Gitea (`50-seed-gitea-repos.sh:139`) both RECONCILE their admin
password, and a KinD-only Harbor equivalent — reset `harbor_user.salt`, restart `harbor-core` — is
the obvious generalisation. It is NOT implemented because **the mechanism is unverified on the
pinned chart**, and this will not mutate a database on an unmeasured hypothesis.

**Settles it:** on a throwaway KinD Harbor, confirm the admin row can be re-seeded by clearing the
salt and restarting core, and that the restart does not take the registry blob store with it (see
the emptyDir/Redis-descriptor incident in `CLAUDE.md` — the blob store now has a PVC, but the
interaction is exactly the kind that has bitten here before).

### 2. An un-bootstrapped Harbor's status code is UNMEASURED, and it decides the retry design

The retry path assumes a FRESH Harbor may answer **401** on `/api/v2.0/users/current` before its
admin row is seeded, and retries that. If it actually answers **5xx**, the probe returns
`unchecked:the probe did not complete` — which also retries on fresh, so the shipped behaviour is
correct either way — but the *rationale* in the code names 401 specifically and would be wrong.

**Settles it, in one command during the first 60s of a fresh install:**
`curl -o /dev/null -w '%{http_code}' -u admin:x https://<harbor>/api/v2.0/users/current`

### 3. `HARBOR_FIRST_INSTALL` keys on "no helm release", NOT "the DB is fresh"

`helm uninstall` that leaves the PVC yields `FIRST_INSTALL=1` against a DB that already has a salted
admin → ~25s of pointless retries before the (correct) die. The converse (release present, PVC
deleted) yields `FIRST_INSTALL=0` and no retry where retrying would have helped. Both are cosmetic;
measuring them needs a live KinD Harbor.

### 4. ✅ EXECUTED LIVE 2026-08-27 — including the RED

`make kind-up install-harbor` ran step 8b against a genuinely fresh KinD Harbor, exit 0:

    Harbor accepted admin — the credential we hold is the one in effect
    Harbor installed: https://172.18.0.3 (admin user: admin)

So the source line resolves (no rc 127), the probe RAN (not `unchecked`), and the credential was
really verified. A green alone would prove none of that, so the three arms were then measured
read-only against that same live Harbor:

| input | verdict |
|---|---|
| the real password | `accepted` |
| a WRONG password | **`rejected`** — it discriminates |
| `HARBOR_URL` unset | `unchecked:no HARBOR_URL` — never a password verdict |

⚠️ This does NOT settle (2): the 401-vs-5xx question needs a probe inside the first ~60s of a fresh
install, and this run was already past that window when it was measured. (1) and (3) are unchanged.

## 🔴 B498 — Kaniko is ARCHIVED by Google; a MAINTAINED FORK exists but publishes NO image

**MEASURED 2026-08-27** against the GitHub API and the registries. I filed an earlier version of
this row that listed BuildKit / Buildah / buildpacks as the candidates and **missed the obvious
one** — the operator pointed it out. This is the corrected row.

| | ours today | chainguard-forks/kaniko |
|---|---|---|
| repo | `GoogleContainerTools/kaniko` — **`archived=true`**, last push **2025-06-03** | **`archived=false`**, last push **2026-08-26** |
| newest release | `v1.24.0` (2025-05-23) — also the newest tag gcr.io serves | **`v1.25.18`** (2026-08-10) |
| activity since Google archived it | none | **≥100 commits**, incl. `fix(util): close each layer reader before opening the next` |

So our pin is not stale, it is **terminal** — and a live successor is **14 patch releases** ahead.

### The decisive fact: the fork publishes NOTHING

`.github/workflows/images.yaml` builds all four targets (`kaniko-executor`, `kaniko-debug`,
`kaniko-slim`, `kaniko-warmer`) and line 96 is **`push: false`**. I probed the obvious registries —
`ghcr.io/chainguard-forks/{kaniko,kaniko-executor,executor}`, `cgr.dev/chainguard/kaniko`,
`chainguard/kaniko` — **none serves it**. There is no image to re-point `images/images.txt` at.

**So this is not a pin bump. It is a self-build:**

    git clone https://github.com/chainguard-forks/kaniko && cd kaniko
    docker build --target kaniko-debug -t <registry>/kaniko:debug --file deploy/Dockerfile .
    docker push <registry>/kaniko:debug

`deploy/Dockerfile:102` does carry the `kaniko-debug` target, and `-debug` is the variant we need
(`check-tekton-scripts.sh:44` allows the busybox shebang for kaniko **only**).

### Why that is cheaper here than it sounds — the pattern already exists

This repo ALREADY builds an image on the internet side and pushes it to Harbor: the offline Maven
builder, `14-builder-build.sh` (INTERNET box, no Harbor) plus `22-builder-push.sh` (air-gap box, no
internet, carried crane). A kaniko self-build is the same shape and should reuse it rather than
invent a second mechanism — including `builders.tsv`, the `io.vks.builder.inputs` freshness stamp
and the sneakernet split.

The fork's build pulls three digest-pinned bases (`golang:1.26`, `debian:bookworm-slim`,
`busybox:musl`). Fine — the build happens on the **internet** side, same as the Maven builder — but
they are new inputs and belong in whatever freshness stamp the kaniko build gets.

### What to settle BEFORE building it

1. **Does the fork fix anything we actually hit?** 14 releases is not automatically a reason. Read
   the changelog for the classes that bite us: layer handling, registry auth, TLS to a self-signed
   Harbor.
2. **Reproducibility.** We would ship a builder WE built. Pin a tag (`v1.25.18`), never `main`, and
   record the resulting digest — otherwise the bundle's provenance is weaker than the gcr.io image
   it replaces.
3. **Does it change the PSA story?** Kaniko runs as **root** (`runAsUser=0`), which is why the `ci`
   namespace needs PSA `baseline` rather than `restricted` (`60-configure-tekton.sh:55`,
   `lib/psa.sh:55`). If the fork has not changed that, the exception stays — and that is the biggest
   reason to care about an unmaintained builder, since it also holds a **Harbor push credential**
   (`22-harbor-robot.sh:110`).
4. **`check-image-alignment` keeps passing either way.** RED-proven again 2026-08-27 (drifting the
   manifest tag gives `DRIFT kaniko-project/executor: manifest=v1.23.9-debug vs
   images/images.txt=v1.24.0-debug`, rc=1). It is a **consistency** gate, not a **freshness** one:
   both copies can agree forever on an archived image. Nothing here would ever have told us.
5. **The Renovate annotation is dead weight that LOOKS live.** `# renovate: datasource=docker
   depName=gcr.io/kaniko-project/executor` can never fire again, so its silence reads as "up to
   date". A self-built image needs `datasource=github-releases depName=chainguard-forks/kaniko`.

**Done-when:** either the fork is built, pinned by tag AND digest, pushed to Harbor through the
existing builder pattern, and proven by `make e2e-kind` plus a real `make verify` on the lab — **or**
the decision to stay on the archived v1.24.0 is written down explicitly and the Renovate annotation
retired, so nobody reads its silence as maintenance.

⚠️ Do NOT swap the builder while a certification is in flight — it is on the walked path.

### 2026-08-27 — the branch was reviewed and its wiring REFUTED; the row's own warning is why it stays draft

`feat/selfbuilt-kaniko` (#1051) was rebased onto current `main` and given a cold `make e2e-kind`. It
FAILED, as it should: `TaskRunImagePullFailed` /
`ErrImagePull ... NotFound` on `${HARBOR_URL}/cicd/kaniko-project/executor:v1.25.18-gcr0.21.9-debug`.

**MEASURED root cause:** `selfbuilt-build` / `selfbuilt-push` exist as targets and **nothing calls
them** — `mirror` is `harbor-auth-check mirror-pull mirror-push mirror-verify`, `install-all` is
`preflight mirror mirror-verify builder-image vks-login platform gitops`, and
`grep -rn selfbuilt scripts/ Makefile .github` finds zero invocations. The branch ALSO removed the
kaniko row from `images/images.txt`, so there is no mirror fallback. The consumer was switched, the
producer was not wired, and the manifest lost its old answer. **`main` is unaffected** — its
`images.txt` still carries `v1.24.0-debug`.

**The obvious fix — add a `selfbuilt-image` one-shot to `install-all` beside `builder-image` — was
REFUTED by an idea-round before it was written.** Recorded so nobody re-derives it:

1. **CRITICAL — it would make `install-all` hard-depend on a Go toolchain nothing provisions.**
   `selfbuilt.tsv`'s kaniko row has a `go_get`, so `14-selfbuilt-build.sh` requires `go` on PATH.
   MEASURED on this tree: **0** active `go` pins in `.mise.toml`, **0** installs in
   `00-install-prereqs.sh`, **0** checks in `03-check-tools.sh`. So every walk row would reach step
   11, run the ~10-minute mirror, and *then* die on a missing toolchain — the exact shape `preflight`
   exists to prevent. ⚠️ Validating this on the dev box is a FALSE GREEN: it has a stale
   `mise/installs/go/1.27.0`. Validate on a walkbox or `photon:5.0`.
2. **CRITICAL — it is VACUOUS for sneakernet.** `e2e-sneakernet.sh` runs
   `mirror-pull → builder-build → bundle` and `jumpbox-run.sh` runs
   `bundle-load → mirror-push → builder-push → mirror-verify`; neither mentions selfbuilt. Both paths
   need wiring in the same change, or the air-gap box gets a bundle with no executor.
3. **HIGH — `11-bundle.sh`'s new guard tests a DIRECTORY, not the tarballs.**
   `14-selfbuilt-build.sh` `mkdir -p`s the output dir before it can fail, so a FAILED build leaves an
   empty dir, the guard passes, and the bundle ships without kaniko — failing "after the carry, on
   the machine that cannot re-cut", which is precisely what that guard's own comment promises to
   prevent. It must iterate `selfbuilt_names` and require each `<name>.tar`.
4. **HIGH — the targets are misfiled under `##@ Quality gates`, which `check-doc-target-coverage`
   EXEMPTS.** They are not gates: they clone from GitHub, build from source and mutate a registry.
   Consequence: `grep -rn selfbuilt docs/ README.md CLAUDE.md` → **zero**, while the coverage gate
   reports OK. `builder-probe` is a pre-existing third instance of the same misfiling.
5. **MED** — no idempotence: every `install-all` would pay a full from-source rebuild (no sentinel,
   unlike the mirror's `.mirror-ok`), and `scenario-1.md` tells operators to re-run `install-all`.
6. **MED** — both scenario docs print the `install-all` chain verbatim and record its timing; adding
   a prereq drifts all three, and no gate covers a comment line.

**Cleared by the same round** (so these are settled, not open): `e2e-kind` DOES call `install-all`;
`builder-image` IS the right analogue (`15-build-push-builder.sh` is a thin orchestrator — prefer a
script over a make-prereq chain so `-j` cannot reorder it); the dual-homed build→push needs no
`bundle`/`bundle-load`; `check-image-alignment` already reads `selfbuilt.tsv` as a second inventory
and passes; and the ORDERING is right, though it belongs *before* `mirror` so a toolchain death costs
seconds rather than twenty minutes.

**Sequencing stands, and this row already said so.** The warning above is the reason: a certification
was in flight when this was reviewed, so nothing was wired. Land the `v1.0.0` re-cut first, then wire
BOTH paths in one change with 1–4 fixed, then run kaniko its own six-row matrix.

## 🟡 B496 — a NEWER VCF plugin bundle (0500) exists, and I could not confirm whether a matching CLI does

Asked to check Broadcom for newer VCF CLI / plugin / service artifacts and update the pins, I found a
newer **plugin bundle** on the portal:

| artifact | pinned in `.env.example` | found on the portal |
|---|---|---|
| `VCF-Consumption-CLI-Linux_AMD64` | `9.1.0.0400.25509669` | 0400 (no newer seen) |
| `VCF-Consumption-CLI-PluginBundle-Linux_AMD64` | `9.1.0.0400.25509793` | **`9.1.0.0500.25653839`** |
| `argocd-cli` | `v3.0.19-vcf` | unchanged |

**NOT APPLIED, deliberately.** Bumping the plugin bundle alone would ship a **0500 plugin bundle
against a 0400 CLI**, and nothing in this repo or in the vendor docs states whether those two are a
matched pair — `grep -iE 'match|pair|same|must'` across `docs/` and `01-install-vcf-clis.sh` returns
**zero** hits, so the repo has no opinion either. These artifacts are licensed, real-lab-only, and
`VCF_CLI_SRC_DIR` is empty on the dev box, so there is no way to test the pairing here. An untested
version skew in the one component a real-lab operator installs first is not worth the bump.

**What settles it, and it is one page:** sign in to the Broadcom portal and read the *VCF CLI*
product-file group (`groupId=540529`) — if a `9.1.0.0500.*` CLI archive exists, bump BOTH pins
together; if the CLI genuinely stops at 0400, then 0500-plugins-on-0400-CLI is the vendor's own
shipped combination and the plugin bump alone is correct. My session could not read it: the portal
had signed out, and I do not enter credentials into login forms.

Note the failure mode if someone downloads 0500 and does *not* bump the pin: resolution is
**arch- and version-bound** (`01-install-vcf-clis.sh:103` builds the glob from `VCF_PLUGINS_VERSION`),
so the installer refuses with a message naming the pin rather than silently installing the wrong
bundle. That is fail-closed, which is why this is 🟡 and not 🔴.

**Done-when:** both pins reflect one confirmed vendor combination, `docs/scenario-1.md`'s download
table matches them, and `make test-scripts` stays green. (The test that guards this was itself broken
until PR #1040 — it read `.env` while the installer read `.env.example`, so it produced three
unrelated-looking failures on exactly this kind of pin bump. Bump on top of that fix, not before it.)

## 🟡 B493 — one test is ~half of `static-check`, and the cause is a hardcoded `sleep 15` that the repo's own rule forbids

MEASURED 2026-08-26, three consecutive runs: `test-argocd-address-classify.sh` reports **201s** —
against a whole-gate wall time of roughly 400s. It is the single largest item in the run, and the
gate runs many times in a working session (five times today, so ~17 minutes in that one test).

It is not doing 201s of work. `09-argocd-address.sh` polls with a **hardcoded** interval, twice:

    145:      sleep 15; _w=$((_w + 15))
    186:      sleep 15; _rw=$((_rw + 15))

The *budget* is externalized (`ARGOCD_ADDRESS_WAIT_SECONDS`, default 900) but the *interval* is not,
so a test can only reduce the NUMBER of polls, never the cost of one. Any case that must exercise a
timeout still pays 15s per iteration.

⚠️ This is also a straight violation of `rules/common/configuration.md`: *"Timeouts, intervals,
retries"* are named explicitly as operator-tunable, and the only acceptable slots are an env-fallback
default, a canonical declaration, or `.env.example`. A bare `sleep 15` is none of them — and the file
right beside it already externalizes its budget, so the omission is inconsistent within one script.

**Direction (NOT designed, NOT reviewed):** externalize the interval the same way the budget already
is, and have the test pass a small one. Two things to settle first rather than assume:

- whether any case depends on the 15s cadence for its *meaning* (a re-resolve race needs at least
  one interval to elapse between two stubbed answers — shrinking it could make a real race untestable
  rather than merely faster);
- whether shrinking it in tests would hide a defect that only appears at the production cadence,
  which is the usual objection to making a timing knob smaller for the benefit of the harness.

Do NOT "fix" this by deleting cases. The 19 cases exist because the re-resolve behaviour they pin
was written after a live incident where the LoadBalancer address moved under an unchanged Service.

## 🔴 B501 — the seed fires a build per app on a WARM cluster (THE FIX SHIPPED IN #1063 IS REFUTED)

⚠️ **CORRECTED 2026-08-27 BY THE LIVE TWO-ARM RUN. Read this before touching the seed again.**

PR #1063 hoisted the webhook DELETE above the push, on the reasoning that a deleted hook cannot
deliver. **Measured on a warm cluster, it does not work**, and it cannot:

| | javawebapp |
|---|---|
| seed pushed | 19:17:**22** |
| hook `created_at` | 19:17:**23** |
| hook `updated_at` (a delivery attempt) | 19:17:**25** |
| PipelineRun created | 19:17:**25** |

**Gitea delivered at :25 for a push at :22 — one second before the hook existed.** Push events are
QUEUED and hooks are resolved at DELIVERY time, not at push time, so removing the hook beforehand
changes nothing: it only has to exist when the queue drains. Result: **3 of 6 apps still fired a
seed build** (run 8 was 4 of 6 — a 4→3 move that is within the noise of a delivery race, not a fix).

The idea-round that cleared the hoist reasoned about ORDERING and did not know delivery is
asynchronous. Its option **(d) "do nothing"** — which was argued and dismissed — was the closer
answer: **#1062's marker-SHA assertion is the real fix**, because it makes verify immune to a stray
build rather than trying to prevent one.

**What the hoist DID achieve, and why it was not reverted:** the refuse-rather-than-guess gate now
fires BEFORE two destructive force-pushes, so a 401/502 on the hook listing aborts before the repos
are rewritten. ARM 2 passed — exactly one hook per repo, correct EventListener URL, active — so it
is harmless. Only its stated purpose is unmet.

**Do NOT re-attempt this by re-ordering.** Any variant that deletes/creates the hook around the push
loses to the same queue. Options that could actually work, none of them yet reviewed: create the
hooks only AFTER every app is seeded AND the delivery queue has drained (unbounded wait — the
adversary already refuted quiescence-waiting); seed with the EventListener scaled to zero; or accept
the builds and rely on #1062, which is what happens today.

**Original row follows, for the measurements that are still valid.**

## B501 (original) — the seed fires a build per app on a WARM cluster, racing verify

**Measured 2026-08-27 (e2e run 8), adversary-confirmed against the live cluster.**

`scripts/50-seed-gitea-repos.sh:293` `push_repo`s the app repo, and only reconciles the
webhook at 304+. On a **warm** cluster the pre-existing hook is already live and its HMAC
comes from the persisted `secrets/webhook-token`, so the seed push **fires a PipelineRun per
app** that nothing waits for.

Measured: **4 of 6 apps** got one, in a 6-second burst at 14:06:1x —
`javawebapp-ci-pvsqv` 14:06:16, `nodejswebapp-ci-k95b6` 14:06:17,
`pythonwebapp-ci-v8lx9` 14:06:20, `rustwebapp-ci-pqfbw` 14:06:22.

**It is deterministic in REGISTRY ORDER, not intermittent.** verify walks apps sequentially,
so the gap between the seed build's write-back and the marker's grows monotonically with
position, against ArgoCD's **180 s** default reconciliation (this repo overrides it nowhere):

| app | seed -> marker gap | result |
|---|---|---|
| javawebapp | 36 s | PASS |
| nodejswebapp | 165 s | PASS |
| pythonwebapp | **214 s** | **FAIL** |

180 falls exactly between 165 and 214. rustwebapp and dotnetwebapp sit further out still and
were simply never reached, so reading run 8 as "a pythonwebapp problem" under-scopes it.

**Invisible on a cold cluster** (`E2E_FRESH=1`), where no stale hook exists — which is why it
reads as random.

**What is already fixed, and what is not.** `be4b48f` made verify *correct* in the face of
this: it now asserts the deployed tag is a prefix of the marker commit's sha, so a stray
build's write-back can no longer satisfy the wait. That is a fix to the SYMPTOM. This row is
the CAUSE: the extra build, the extra rollout churn and the wasted CI time all remain.

**Prescribed fix (adversary):** move the existing-hook **DELETE** above `push_repo` and leave
the **create** after it. Zero seed builds, bounded, no waiting. Do NOT wait for quiescence —
unbounded.

⚠️ The webhook block is a carefully reasoned control (delete+POST because PATCH cannot set the
secret; a documented Gitea non-dedupe hazard; a SIGPIPE trap in its own hook-listing). Changing
its ordering is a control change and needs its own idea-round before implementation — it is not
a one-line move.

**Related, same review, not yet filed separately:**

- `$img` is a one-shot snapshot that `_all_pods_on_img` and every `_pick_pod` call closes over.
  In run 8 this turned the mis-timed wait into `no Ready pod on ...:5981b0e` -> svc/ fallback ->
  **113 tunnel deaths across 5 generations**, then a `PRODUCT ... does not show marker` verdict
  rendered from a cached pre-roll body. `be4b48f` narrows the window but does not close the class.
  Fix: have `_pick_pod` re-read the Deployment's current image.
- `update-deploy` pushes with no rebase/retry, so two concurrent write-backs to one deploy repo
  fail non-fast-forward. Not reached in run 8, but the seed burst makes it likelier.
- `test-verify-marker-build.sh` reconstructs the predicate with no drift gate (disclosed in its
  own header). Fix: extract the predicate to `lib/` and source it from both.

## ✅ B502 — a tunnel REBUILD's bind was invisible, so #1062 was unverifiable from any log

**DONE** (this branch). `_start_pf` reported its bind with `log_info` only. Every rebuild site sits
inside `_pf_classify`, which runs as a `wait_for` predicate — and `wait_for` invokes predicates as
`"$@" >/dev/null 2>&1` while `_log` writes to **stderr**, so the line is discarded. Only the FIRST
bind (called outside the predicate) ever reached a terminal.

Measured on two live e2e runs, 2026-08-27:

| run | apps | rebuilds announced | `tunnel target` lines | of which generation 2 |
|---|---|---|---|---|
| mid-run peek | 2 | 2 | 2 | **0** |
| full organic e2e | 6 | 6 | 6 | **0** |

Faithful reproduction (stderr-writing `_log`, as `lib/os.sh`): 3 generations → **1** log-visible
bind → **0** recoverable; with `_pf_ev` → **3** recoverable.

Consequences it had while open:

- #1062's rebuild-site port fix could not be verified from any log — the handoff's "all six bound a
  pod on 8080" describes the **initial** bind only.
- The round-4 comment asserting "every generation reports its own target" was **false**, and it is
  the third time this file's `wait_for`-discards-output trap has been hit. `_resolve_pf_port` already
  carried a `_pf_ev` companion for exactly this reason; the `log_info` two functions later did not.

Fix: a `_pf_ev` bind report beside the `log_info`. Pinned by two cases in
`test-verify-pf-readiness.sh`, RED-proven (removing it → 1 FAILED).

## ✅ B503 — the classifier's verdict LATCHED (and my FIRST fix for it was refuted)

**DONE** (this branch), but **not by the fix this row originally described** — read the arc, because
two plausible fixes were measured and both are wrong.

The defect: `PF_RESTARTS_BLOCKED` and `PF_UNKNOWN` are set and never cleared. The rebuild *decision*
re-derived per poll; the *verdict* did not. So one transient not-Ready poll permanently disabled the
`HARNESS-TUNNEL` arm. The transient is **ordinary** — readiness `periodSeconds: 5`, `replicas: 2`,
no `strategy:`, so any re-sync rolls a pod.

Three candidate rules, measured across four scenarios:

| scenario | latch (the bug) | clear per poll (my fix) | ever-unstable guard (adversary's fix) | **counts (shipped)** | want |
|---|---|---|---|---|---|
| flap ×3, then 1 death | PRODUCT ✓ | HARNESS ✗ | wrong reason ✗ | **PRODUCT ✓** | PRODUCT |
| 1 transient, then ×3 deaths | PRODUCT ✗ | HARNESS ✓ | PRODUCT ✗ | **HARNESS ✓** | HARNESS |
| pure tunnel deaths | HARNESS ✓ | HARNESS ✓ | HARNESS ✓ | **HARNESS ✓** | HARNESS |
| pure not-Ready | PRODUCT ✓ | PRODUCT ✓ | PRODUCT ✓ | **PRODUCT ✓** | PRODUCT |
| **score** | 3/4 | 3/4 | **2/4** | **4/4** | |

- **My first fix (clear per poll) inverts the error** — it decides on the LAST poll instead of the
  first, so a flapping app that takes one tunnel death is told to "retry the row". An
  implementation-round adversary caught it.
- **The adversary's prescribed fix is worse than the bug** (2/4): a binary "was it ever unstable"
  cannot separate a flapping app from a single transient, and it re-breaks the very case the change
  exists for. A prescription is a claim; this one was refuted by running it.

**A FOURTH design was then refuted, by INTERACTION rather than by logic.** Comparing `PF_DEATHS`
against `PF_NOTREADY_POLLS` looked right and scored 4/4 on the table above — but B506, in the same
change, caps `PF_DEATHS` at the generation cap (5) while `PF_NOTREADY_POLLS` is capped by the poll
count (120). So `deaths > not-Ready` becomes **unwinnable after ~25s of any pod being not-Ready**,
and a rolling update makes that the ordinary case. Measured against the shipped classifier:

| polls | shipped-then | with B506 reverted |
|---|---|---|
| notReady=4, tunnel-dead=116 | HARNESS ✓ | HARNESS ✓ |
| notReady=5, tunnel-dead=115 | **PRODUCT ✗** | HARNESS ✓ |
| notReady=30, tunnel-dead=90 | **PRODUCT ✗** | HARNESS ✓ |

Two individually-correct changes destroying each other — neither reviewable in isolation.

Shipped: **three uncapped POLL-CLASS counters on the same scale** — `PF_NOTREADY_POLLS`,
`PF_TUNNEL_POLLS`, `PF_UNKNOWN_POLLS`, one increment per poll — with `PF_DEATHS` kept purely as the
rebuild count for messages. Every arm decides on which **class dominates**; ties favour the app, so
an operator is never told to retry a row when their workload is flapping. The latching strings
survive as message detail only. Verified 8/8 across every scenario in this row plus the two that
refuted its predecessor.

Pinned by cases asserting the class counters, that no arm compares the capped `PF_DEATHS` against a
poll class, and that the refuted per-poll clear has **not** come back — the absence check
deliberately **unanchored**, because anchoring an absence assertion *softens* it (the natural way to
re-add the clear is to append it to the existing `local rc=…` line, which an anchored grep misses).

## ✅ B508 — the readiness arm could not say UNKNOWN, so an RBAC-limited tenant was told a falsehood

**DONE** (this branch). Pre-existing, found out-of-scope by an adversary. `_health_up`'s failure arm
had **no** `PF_UNKNOWN` branch, so a tenant whose RBAC denies `pods/list` was told
`PRODUCT … app not serving /healthz` — a fact the harness had explicitly recorded it *could not
determine* — and "retry the row" then loops forever. The sibling `marker_visible` arm has carried
that branch all along.

Per RULE ZERO-B the limited-RBAC tenant is this repo's **default** audience, which is what makes a
missing UNKNOWN branch a correctness bug rather than a cosmetic one.

⚠️ **The first attempt at this fix was itself unreachable.** It added the branch but left it *below*
the tunnel verdict and gated on `PF_UNKNOWN_POLLS <= PF_DEATHS` — a **tautology**, because both were
incremented adjacently on the same poll. Measured over **4000 random interleavings, `U > D` never
occurred**, so the branch could not fire for the very tenant it was written for, while the tunnel arm
above it said "retry the row" forever. The two arms were ranking identical evidence in **opposite
orders**. Both now test UNKNOWN first, on poll classes, and a test pins the ORDER — not merely the
presence of the message, which a constant-false guard leaves untouched.

## ✅ B504 — CORRECTION: the "13% of runs reach generation 2" figure was a DENOMINATOR ERROR

**CLOSED as a correction.** I measured 36 of 280 archived SUCCESS lines carrying `2 generation(s)`
and reported **13%**. Only **36 of the 280** carry the counter at all — the other 244 predate the
instrumentation and could not have reported a generation. The true rate is **36/36 = 100%**.

Confirmed at a SECOND operating point the same day: the organic e2e above was **6/6**. Two trees,
two runs, 42/42 — so this is a mechanism, not a rate.

It inverts the conclusion I had drawn: I argued the generation-1 burn might be worth keeping because
it was "the only thing exercising the rebuild path". That reading is **refuted by evidence, not by
argument** — the rebuild path was traversed 42/42 times and did **not** catch the stale-`pf_port`
bug that lived precisely there, because the only organic transition is **pod→pod**, which inherits a
correct port. The broken transitions need a downgrade, and there are **0 in 280**. Accidental
traversal with no assertion is not coverage.

Also unverifiable and withdrawn: the "113 tunnel deaths" figure. The archive contains **zero**
`HARNESS-TUNNEL` lines and the only death count ever recorded is `1`; run 8's logs died in `/tmp`
(B454). That number survives only in prose.

## ✅ B505 — `_pick_pod` binding a Terminating pod: CLOSED on evidence, not fixed

**CLOSED.** Filed as a race worth fixing; it is guarded by construction. `_all_pods_on_img` lists
`.items[*]` with **no** phase or `deletionTimestamp` filter and requires *every* labelled pod to
report the new image — so an old-image Terminating pod (default grace **30s**, none of the six
manifests set `terminationGracePeriodSeconds`) keeps it false. **The rollout the verify itself
triggers is waited out by construction, so it is not the trigger.**

Reaching `_pick_pod` with a doomed pod needs a NEW-image pod deleted *after* that gate, caught in
the ≤5s before its readiness flips; at the rebuild sites `_pf_classify`'s readiness check pre-empts
it. Observed: **0 anomalies in 280 attempts**.

If `_pick_pod` is edited for another reason, emitting `.status.containerStatuses[0].ready` and
adding `&& $3=="true"` is ~20 characters and strictly narrows the set. Not worth its own change.

## ✅ B506 — the post-cap event FLOOD buries the diagnostic at the moment of failure

Reproduced at defaults (600s / 5s / cap 5): **120** reported deaths vs **4** real rebuilds, and
`PF_EVENTS` is **120 lines of which 5 are distinct** — 116 repetitions of "the generation cap is
spent". Both failure arms replay the whole channel, so the diagnostic printed at the exact moment of
failure is 120 lines with 4 informative ones. That buries the channel B502 just made load-bearing.

The counter overcount is cosmetic; the flood is not.

Fixed on **both** death paths. The first attempt guarded only the pods-Ready path; the
kubectl-unqueryable path — the one a broken kubeconfig or a stale token takes, i.e. the likeliest
non-tunnel failure a tenant hits — still incremented a death and emitted an event on **every** poll,
uncapped (measured 120/120 there against 5/5 on the fixed path). Both are now flag-guarded, with
`PF_DEATHS++` inside the rebuild branch and `PF_POSTCAP_POLLS` counted and **reported** rather than
dropped.
Measured before/after at the documented defaults: **120 deaths / 120 event lines (6 distinct)** →
**6 deaths / 6 event lines / 0 duplicates**, with 115 post-cap polls reported separately.

## ✅ B507 — generation 1 is burned deterministically by a startup race

`wait_for` polls at t=0, before the backgrounded port-forward can bind. Measured: the archive's
bind→health-up gap is **exactly one poll interval, 36/36** (29×5s, 7×6s — `POLL_INTERVAL_SECONDS=5`,
zero variance), and `kubectl`'s cold-start floor alone is 22–24ms against a ~1ms first curl.

Cost, in order of importance:

- **It destroys the discriminating power of the counters #1052 added.** `1 tunnel death, 2
  generations` is now the *healthy* baseline, so a genuine single death is invisible — a gate green
  at the same count is blind to the thing it counts.
- The real generation budget is **4**, not 5.
- ~5.5s × 6 apps ≈ **33s per e2e**, ~3m18s per six-row matrix run.

Fixed, and the first version had a race of its own: `kill` is **asynchronous**, so the probe
connected to the **dying previous generation** and reported a bind that never happened while the new
`kubectl` failed `EADDRINUSE` (measured — "broke after 0 attempts" against a corpse). `PF_PID` would
then point at a dead process and every rebuild would re-run the same race, burning the generation cap
on a tunnel that never rebuilt. Shipped: reap the old listener first, then probe, and abort the wait
if the **new** `kubectl` is already dead (reported through `_pf_ev`, the channel that survives).

`_start_pf` polls `/dev/tcp/127.0.0.1/$app_local_port` for up to ~2s after backgrounding, before
returning. **PROVEN LIVE**: a full `make verify` after the fix returned 6/6 apps at **1 generation
and 0 tunnel deaths**, against 6/6 at 2 generations with 1 death each before. The probe is the **local socket**, so it asks only "did kubectl bind?" and cannot
mask a slow or broken app. Deliberately **not** a sleep in `wait_for` — that would tax all six other
waits, including the Gitea forward, which has the identical race.

## 🔴 B509 — `check-lib-sourcing` reads a lib-function name inside a QUOTED GREP ARGUMENT as a call

Measured 2026-08-27. A test asserting that both verdict arms can report UNKNOWN was written as
`grep -cE '^[[:space:]]*(die|log_error) "UNKNOWN' "$V"`. That puts the literals `die` and
`log_error` in the file as **grep pattern text**, and the gate reported:

    ERROR: scripts/test-verify-pf-readiness.sh calls die() but never sources lib/os.sh
    ERROR: scripts/test-verify-pf-readiness.sh calls log_error() but never sources lib/os.sh
    make: *** [Makefile:1686: check-lib-sourcing] Error 1

Neither is a call. The workaround was to match the **message** rather than the verb, with the reason
recorded inline so nobody reverts it.

Worth knowing before attempting a fix: this gate was repaired on 2026-08-27 (#1055 arc) and its
commit body records **four plausible fixes that were implemented and REFUTED by running them** — in
particular `executable_text()` is *not* the fix on the source side, because it blanks quoted string
contents and the lib path lives inside the quotes. The same function would be the obvious fix here
and would break the source side again.

So this is a **known false-positive shape, not an open bug to patch casually**. The cheap mitigation
is the one applied: do not put a lib-function name inside a quoted grep argument. Any real fix must
distinguish a call position from a pattern position, which is the shell-grammar problem the gate has
already lost twice.

Severity is LOW — the gate fails **closed** (a false RED, not a false green), which is the safe
direction.

## 🔴 B510 — the go_get verification `podman run` is UNTESTED on cgroup v1, and its failure names the wrong cause

`14-selfbuilt-build.sh` verifies the `go_get` override by running the built image:

    "$ENGINE" run --rm --entrypoint /busybox/sh "$local_ref" ...

`BUILDAH_ISOLATION` does **not** apply to `podman run` — measured, it is a buildah knob and `run`
ignores it. So the cgroup-v1 fix that makes the BUILD work says nothing about this line. If
`podman run` also fails there, the `if` takes its else branch and warns:

    no shell in <ref> — the go_get override is UNVERIFIED

which is a confidently wrong cause: it blames the image's contents for a container-runtime failure.
That is the same class as the two paths B-fixed in #1067 (`16-engine-trust-check.sh` blaming Harbor
trust, `jumpbox-run.sh` blaming unqualified-search-registries).

Genuinely unknown: no walk log reaches this line, because `selfbuilt` never got past the build on a
photon row. The next photon row that completes a self-built image settles it.

Fix if it does fail: distinguish "the runtime could not start a container" from "the image has no
shell" before choosing the message — the two need different remedies and only one is about the image.

## 🔴 B511 — rootless DOCKER on cgroup v1 has no mitigation and no diagnostic, and the matrix has that cell

`engine_build_isolation` correctly returns empty for docker: `BUILDAH_ISOLATION` is a buildah knob
and `docker build` ignores it entirely (measured — a bogus value changes nothing). So the cgroup-v1
mitigation covers podman only.

`jumpbox-matrix` includes a **photon × docker** cell. Whether rootless docker builds on a cgroup-v1
host is **UNVERIFIED** — Docker's rootless documentation does not address cgroup versions. If it
cannot, that cell fails with no mitigation available and no message naming the cause, since
`18-engine-check.sh`'s new cgroup note is podman-scoped.

Settle it by running the photon × docker jumpbox cell and reading whether the build's RUN step
survives. If it does not, the honest fix is a DISCLOSURE in `engine-check` (as the rootful-docker
sudo cost is disclosed) rather than a mitigation that does not exist.

## 🔴 B512 — `/supervisors/summaries`'s `.info` carries more fields than the lister enumerates

`_ss_supervisors_list` reads `.supervisor`, `.info.name` and `.info.config_status`. The repo's own
evidence docs record `.info.APIEndpoint` and other `.items[0].info.*` fields, so the enumeration in
the code comment — and the claim that the payload carries no cluster field — is narrower than
measured reality.

The claim that matters is the negative one: **that summaries carries no cluster moid**, which is why
`VKS_SUPERVISOR` cannot be joined to the upgrade's `_SS_MOID`. That is the load-bearing fact behind
the refusal shipped in lab #124, and it is currently `inferred` from the fields the code happens to
read, not from the payload.

Settle it read-only, on a live lab:

    curl ... /api/vcenter/namespace-management/supervisors/summaries | jq '.items[0] | keys, (.info | keys)'

If a cluster field DOES exist, the refusal can become a real selector — map the chosen Supervisor to
its cluster moid and steer the upgrade properly, which is what an operator asked for.
